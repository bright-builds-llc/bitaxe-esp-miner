interface:
  display_name: "Advance Parity"
  short_description: "Advance one audited parity checklist item"
  default_prompt: "Use $advance-parity to select or resume one parity checklist item, persist its plan, verify the work, and publish progress."
policy:
  allow_implicit_invocation: false
