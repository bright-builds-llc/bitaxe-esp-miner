# V2 channel successor after incomplete host evidence

Contract ID: `str005-v2-serial-cleanup-successor-v1`.

This prospective amendment belongs to
`task-str005-v2-cleanup-successor-amendment`; the existing
`task-str005-v2-serial-qualification` continues to own hardware and mining.
It supplements the immutable [base](str005-v2-serial-qualification.md),
[clock](str005-v2-serial-clock-amendment.md) and
[permission](str005-v2-serial-permission-amendment.md) contracts. Publish the
reviewed amendment before implementation and publish verified implementation
before effects. Existing standing authorization covers the bounded continuation.

## Exact predecessor and non-claims

Channel002 is permanently unverified. Its context, result and seal are:

- Context: `b2c889e617a1e1f976c9dfc1fd25a889edb62a515fbcbdc88c7ccf3df2fdb179`.
- Result: `675230a92e4f6ebd78ca0cf1748019c364772b5f31742075dda3c3c38af7b4b0`.
- Seal: `a12a3c25cc89c137d12ece4bc80c4896546f7f2d37bfaabf36109004b2b26c80`.

Its original reviewer reports `stop_evidence_incomplete` / `v2_baseline`.
The separate parent-observed first cleanup failure is
`v2_listener_inventory_shape`. The corrected evaluator may inspect the existing
authenticated baseline, protocol, restoration and accounting observations, but
must not change the original result or create an accepted projection.

The missing private pool-port absence observation cannot be reconstructed.
Browser closure, fixture listener/socket closure, actual process exit and fresh
process-group absence support present ownership readiness; they do not prove
that the original port-absence observation happened. This amendment permits
fresh admission after those independently checked current ownership facts.
It does not waive complete cleanup for a successful new Channel or Share result.
Never recover the old private port from logs, process arguments, runtime
inspection or discovery, or retain an endpoint/port hash as a substitute.

The accepted Noise campaign remains the protocol ancestor. The actually installed
before-update identity is Channel002's firmware
`097050c09e9def6a9bf57df254a597a0f287bfa1`, ELF
`76639dc29251dcc3001164e159fd2ca8db142796df46c88b07b25345ae9d0498`.
Fresh authenticated possession must confirm it before any write. Do not admit
using the older Noise image or import a private baseline identifier.

## Effect-free successor preparation

Add two actions to `just stratum-v2-serial`:

- `prepare-channel-successor --private-root FAILED`: independently inspect the
  exact sealed Channel002 and current host ownership, then exclusively create
  protected sibling `FAILED.successor-readiness.json`.
- `review-channel-successor --private-root FAILED`: revalidate that receipt and
  its bound inputs read-only, without recollecting or repairing historical proof.

These commands never open the device, access credentials/signing authority,
start a fixture, modify the failed tree or allocate a new effect context. Receipt
creation is not effect authorization. Reject noncanonical paths, symlinks,
unsafe modes, existing/pending receipts, changed input bytes/membership and
conflicting or incomplete evidence. Neither a generic failure nor another
Channel ordinal qualifies for this class.

Independently verify:

1. Original context/assignment, thirteen artifacts, complete preflight/evaluator
   inventory, original result and seal, native receipts, permission closure and
   accepted Noise ancestry. Require the exact unverified disposition above and
   no conflicting browser/device/fixture failure.
1. All five installation records and four maximum-probe/cycle joins, exact
   candidate identity, healthy startup and preserved settings. Initial selection
   uses authenticated initial accounting, not the first page event. This is
   forensic verification of available facts, not a replacement historical judge.
1. The one correlated Channel protocol exchange, immutable terminal device
   resource release, fresh restoration, final browser release and unchanged
   original/qualification ledgers. Require no Work Lease signing, issued/loaded
   grant, reservation, work, submission, renewal, observer or heartbeat fault.
   Expected ledger: next18, last17, charged1560000 ms, pending false.
1. Exact original parent-observation provenance and supporting operator copies;
   real browser closure, supervisor zero exit, fixture zero exit/reap and closed
   peer/socket/listener. Require no invented resources/cleanup receipt.
1. Fresh absence of every recorded supervisor/fixture/installation owner,
   including the earlier unclaimed launch owner, descendants and process group; no holders of either recorded serial node and
   no listener on the known supervisor port. Check recorded owner identities,
   not a reused PID alone. Missing current ownership proof blocks preparation.
   Preserve the unclaimed launch's uncollected historical exit code; never infer
   zero exit from present absence. Include recorded parent identities where
   available, without inventing start times or treating tool-session IDs as PIDs.

The closed versioned receipt binds failed context/result/seal, inspected input
hashes, the installed before-source, unchanged accounting, checker identity and
current ownership observations. It explicitly records `hardwareQualified: false`
and `historicalCleanupComplete: false`. Its only classification is
`ready_for_fresh_channel`; its status remains `unverified`. No accepted Channel,
private pool value, grant or reusable application possession belongs in it.

The receipt has exactly these keys: `schema`, `amendmentSha256`, `failedRoot`,
`failedContextSha256`, `failedResultSha256`, `failedSealSha256`, `status`,
`classification`, `hardwareQualified`, `historicalCleanupComplete`, `beforeSource`,
`predecessor`, `ledger`, `original`, `inspectedInputs`, `currentOwnership`,
`checkerIdentity` and `nonClaims`. Its schema is
`str005-v2-channel-successor-readiness-v1`. `beforeSource` contains exactly
`firmware_commit` and `app_elf_sha256`; `predecessor` contains exactly `root`,
`resultSha256` and `sealSha256`. `ledger` and `original` use their existing closed
accounting schemas. `inspectedInputs` is the exact complete sealed file inventory,
with entries `{path, sha256, length}` and no raw snapshot contents.

`currentOwnership` has exactly `schema`, `source`, `observedAtUnixMs`,
`ownerCount`, `processGroupsAndChildrenAbsent`, `serialNodeCount`,
`serialHoldersAbsent`, `supervisorListenerAbsent`, `unclaimedOwnerAbsent`,
`parentReferenceCount` and `parentReferencesAbsent`. Its schema is
`str005-v2-successor-ownership-v1`, source is `successor-collector`, all absence
flags are true, and counts are derived from deduplicated pinned records.
A recorded parent PID without a recorded start identity requires that PID to be
absent now; if occupied, fail closed without identifying an unrelated process
as the old parent. This is present readiness, not historical parent-exit proof.

`checkerIdentity` contains exactly `firmwareCommit` and `sources`, where sources
are the fixed complete repository-owned checker dependency inventory entries
`{path, sha256, length}`, bound to clean published code and the successor
evaluator. `nonClaims` is exactly, in order: `historical-pool-port-absence`,
`historical-cleanup-complete`, `accepted-channel`, `mining-acceptance`,
`cycle-or-baseline-transfer`, `effect-authority`.

## Exclusive context and correction binding

Add `preflight --supersede-channel RECEIPT`, mutually exclusive with
`--supersede-permission` and available only for Channel. New admissions use
`str005-v2-serial-context-v3`. Keep v1/v2 historical readers exact; neither can
regain live eligibility. Reject new Channel001/002, Channel004 or generic retries.

The v3 context retains the existing fields and `permissionSupersession: null`,
and adds `cleanupSupersession`. Channel003 requires the closed object
`{failedRoot, failedContextSha256, failedResultSha256, failedSealSha256, receiptPath, receiptSha256}`. The root is the exact Channel002 root; the receipt
is its deterministic sibling. Share001 requires `cleanupSupersession: null`
and the independently accepted same-pair v3 Channel003 predecessor.

For Channel003, `predecessor` still binds accepted Noise; `before_source` binds
only the independently inspected installed Channel002 candidate. For Share,
before-source and all candidate/source/artifact identities equal accepted
Channel003. Original campaign identity and both ledgers remain unchanged.
The contract binding adds `cleanup` for this exact document. Retain complete
transitive source/artifact/native inventories and verify receipt/context/seal
bytes before each effect, after full historical inspection at admission.

Before assignment, require a changed clean published firmware/package/evaluator
with the verified host corrections. Gate may remain unchanged and must retain
its existing foreground-gesture regression and exact pin. No protocol, safety,
stack, timing or acceptance threshold changes are required or authorized here.

Preflight runs the fixed software-only command:

```text
node --test scripts/str005-v2-serial/host-resources.test.mjs scripts/str005-v2-serial/continuity-baseline.test.mjs scripts/str005-v2-serial/cleanup-rehearsal.test.mjs
```

Bound it to 180000 ms and 65536 combined output bytes with filtered environment;
require zero exit and no signal. Never accept supplied test verdicts, extra
arguments or raw output as evidence. Record only the closed versioned
`host-correction.json` with fields `schema`, `firmwareCommit`, `gateCommit`,
`gateBundleSha256`, `checkerSha256`, `command`, `exitCode`, `signal` and
`elapsedMs`. Its schema is `str005-v2-host-correction-v1`; exit code is zero,
signal is null, and elapsed milliseconds are a nonnegative integer at most
180000\. The checker digest must match the bound evaluator source. No raw output
is retained. Preserve the existing Gate correction check and
its separate receipt. Share inherits both exact same-pair correction receipts.

Recheck clean publication, task activity, artifacts, sealed failure and current
host ownership after software/native checks and immediately before assignment.
Reserve Channel003 exclusively before child creation; interrupted or duplicate
assignment stays consumed. Recheck current ownership at serve admission. The
prepared receipt cannot override current ownership, task or source failures.

## Rehearsal and hardware workflow

Before effects, exercise real host child processes and sockets through private
readiness, source-bound cleanup preparation, browser-witness input, actual
supervisor/fixture exit, OS listener/process absence checks, receipt construction,
finalization and independent review. Device/browser inputs in this rehearsal
are explicitly synthetic and never hardware evidence. Test-only context,
publication, native-audit and serial-device prerequisites may also be synthetic;
correction execution is stubbed only while constructing a synthetic test
context, avoiding recursive preflight tests. Actual child identities, TCP
listener, private HTTP handoff, process exits and macOS ownership/listener
collector remain real. No production CLI flag or environment variable can
select these test seams. Include configured and
ownership-acquiring page events before authenticated ready observations. Also
prove an open listener, missing witness, changed identity or failed cleanup
cannot produce a passing receipt. Keep the private port only in its owning
collector's memory until the source-bound absence receipt is committed; the
parent must observe completion or preserve a typed failure before exiting.

Publish the verified implementation and clean exact package. Prepare/review the
successor receipt and admit Channel003. Detector-admit exactly one Ultra205,
prove the installed before-source through native foreground Web Serial, then
perform the initial state-preserving install plus four fresh update/reconnect/
65536-byte bidirectional cycles. Run the Channel exchange, freshly restore,
collect accounting, completely release owned resources, seal and independently
review. No previous cycles count and no task/report publication changes HEAD
between accepted Channel and Share.

Only accepted Channel003 permits Share001 on the same exact pair, with its own
fresh page, baseline, possession and four cycles. Fresh accounting must confirm
next ordinal18, last17, charged1560000 ms and pending false before issuance.
Use one existing normal180000-ms reservation, conservative400MHz/1100mV profile,
existing signed renewals, and a real independently verified accepted ASIC share.
Then execute existing heartbeat suppression, device-local revocation/shutdown
within three seconds, ordered stop, bounded cooling, restoration and complete
cleanup. Expected completed accounting is next19, last18, charged1740000 ms,
pending false. Never reset either ledger or refund a consumed reservation.

All base bounds remain, including the 2800-ms heartbeat deadline, work-clock
amendment, one passive observer's360-second lifetime, recovery wait and native
resource margins. No external pool, factory reset, boot ownership change,
crypto fork, physical pin/UART action or device-control fallback is introduced.

On failure preserve the earliest cause and complete cleanup; no consumed context
can restart. This finite amendment admits no fourth Channel or second Share.
Further progress requires independently verified corrections and an applicable
published admission contract. On complete success, follow the main task's
archival and separate STR-005 promotion requirements; this amendment alone
changes no parity row. Parity remains90/95 until independent promotion passes.
