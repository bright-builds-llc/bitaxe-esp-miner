# Parity work closure

- Parity row: `API-009`
- Final status: `implemented`
- Outcome: `blocked`
- Verification claimed: `no`
- Plan SHA-256: `bdb2fd84545705e1322faa9c1ddcb5448f9cc356e6d0dbc6b5442103f2411a8f`
- Active task: `task-parity-api009-command-effect-evidence-audit`

## Closure reason

The sole detector-gated attempt-024 admitted one ready Ultra 205 and the exact
pushed package. It established trusted runtime identity, genuine notification,
positive block count, one pause with stopped hardware, and the complete live
latency-tolerant IDENTIFY transaction: ready, one exact rendered observation,
natural expiry, and live cleared confirmation with no replay.

The campaign then issued one resume request and confirmed API-visible resume
intent. This proves the attempt-023 15-second host-orchestration blocker is
fixed. Before active mining returned, a firmware marker reported an unchanged
stale safety observation while the campaign was armed, hardware was ready, and
the primary pool was reconnecting. The firmware treated that pre-active
reactivation state as terminal `safety_stale`. Active resume confirmation,
dismissal, and restart therefore did not run.

The closed result confirms terminal hardware safe stop, USB cleanup, private
modes, fixture cleanup, and result sealing. No recovery pause was needed after
the firmware's terminal safe stop. Public command-effects evidence was
correctly withheld.

## Verification

The exact package binds pushed source `a5e0a447dd6eb60eb9bc444740bbea332c71a734`
and pinned reference `c1915b0a63bfabebdb95a515cedfee05146c1d50`.
Closed command evidence v6 records notification, block count, pause, stopped
hardware, one IDENTIFY request, ready/rendered/cleared confirmation, one resume
request, and resume-intent confirmation. The final safe fields prove hardware
safe stop, USB cleanup, protected modes, fixture termination, result integrity,
and projection withholding.

## Next safe action

Keep API-009 `implemented`. Create a fresh software-only plan for the exact
reactivation state: after a prior active epoch and operator pause, a safety
freshness lapse while the campaign is not yet active must safe-stop prepared
hardware without consuming the resumable lease. A later fresh observation may
reprepare and reconnect; a safety lapse during active mining must remain
terminal. Add a live-shaped regression before defining attempt-025.

## Non-claims

This closure does not claim active resume recovery, notification dismissal,
block-count preservation after dismissal, canonical restart, same-device
restart recovery, public parity evidence, or API-009 verification. It exposes
no origin, hostname, port, USB/network identity, credential, worker, address,
password, token, sensor value, or raw trace.
