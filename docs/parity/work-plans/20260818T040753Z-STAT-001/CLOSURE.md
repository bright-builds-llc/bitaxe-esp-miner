# Parity work closure

- Parity row: `STAT-001`
- Final status: `implemented`
- Outcome: `blocked`
- Verification claimed: `no`
- Plan SHA-256: `59091a318cedc84e682a0d231edaa266149e13614689ec651c6b95719f9be431`
- Active task: `task-parity-stat001-hashrate-monitor`

## Closure reason

Pushed source `0abd10add7187f0cb968dc73355c1aa41fed23a9` adds the
complete software diagnostic required by attempt-018's closed `panic` reset.
The campaign serial analyzer now classifies seven ESP-IDF/Rust panic families,
twelve closed task families, first-signature observation order, recognized-line
count, and an explicit `unknown` fallback when a panic reboot occurs without a
captured signature. Diagnostics schema v4 retains only closed labels and counts;
it stores no raw line, task name, address, backtrace, or payload fragment.

The hashrate evaluator identity now binds 21 repository-owned source paths,
including the serial reducer, diagnostic schema, and panic vocabulary, with
package-local Bazel runfiles and synthetic real-process fixture coverage. Every
focused and mandatory software, firmware, package, privacy, reference, parity,
and progress gate passed.

This plan intentionally authorized no hardware and cannot retroactively
classify attempt-018. It therefore cannot verify `STAT-001`; the checklist
digest, progress history, README, and active row status remain unchanged.

## Next safe action

A separate immutable `STAT-001` hardware plan may consider exactly one fresh
attempt-019 only from clean pushed `0abd10ad`, after rebinding the workflow,
validator, plan/task digest, ordinal, protected roots, diagnostic schema v4,
21-source identity, and fixtures. It must define the exact detector, package,
conservative profile, input safety, privacy, recovery, cleanup, retry, stop, and
promotion contract before device access. Any recurrence must use the new closed
panic signature/task/count tuple to select a targeted source diagnosis; never
retry by ordinal alone.

## Non-claims

This closure does not identify attempt-018's panic mechanism or task, verify
live hashrate accuracy, authorize attempt-019, prove a complete 20-window
campaign, terminal rates, safe stop, submit outcome, profitability, arbitrary
profiles/pools, other ASICs/boards, unbounded mining, updates, recovery, or
release readiness. It uses no hardware, credentials, protected attempt data,
external services, or public projection.
