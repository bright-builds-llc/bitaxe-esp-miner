# Parity work closure

- Parity row: `API-009`
- Final status: `implemented`
- Outcome: `blocked`
- Verification claimed: `no`
- Plan SHA-256: `466b878f67b5664cec18071f5ce94fb47d70b9692bf54fd2baec64be6fe2e936`
- Active task: `task-parity-api009-command-effect-evidence-audit`

## Closure reason

The immutable task contract, exact clean pushed source and package
`8e89891f445f49493d3909fb2e1b4c30795c5dce`, focused pause/session/checkpoint
and real-process tests, complete software/privacy/reference gates, sole
protected detector, and private-mode admission all passed. Exactly one
board-205 ESP32-S3 was admitted, USB cleanup completed, and the sole
attempt-010 ran without a retry.

The sealed private v8 result proves the resumable-pause fix on hardware. A
genuine positive block and five qualified accepted shares preceded one pause
request, confirmed pause, and confirmed same-lease resumable safe stop. Exactly
one resume request was confirmed and mining returned active under the same
boot and package. This crosses the exact attempt-009 pause-confirmation blocker
without weakening terminal shutdown or safety admission.

The campaign then issued exactly one IDENTIFY request and emitted its rendered
checkpoint, but the signal did not describe the expected frame or the pinned
30-second effect duration and no pre-effect operator-readiness checkpoint
existed. The user's later normal-screen description arrived after multiple
waiting turns, outside that bounded effect window. It is therefore neither
positive nor negative evidence about what the display showed while IDENTIFY
was active. No rendered confirmation was issued. The campaign correctly
withheld the cleared toggle, dismissal, restart, and public projection, then
closed with the typed hardware-blocked wrapper result. Safe stop and USB
cleanup are confirmed; recovery was unnecessary; private modes, seal,
no-symlink, redaction, and public withholding checks pass.

The corrected accepted outcome is `stop_authority_boundary`: the observation
authority expired before a physical report could be bound to the live effect.
API-009 remains `implemented` because the complete conjunctive five-command
device-user quorum is absent.

## Next safe action

Do not create attempt-011 or reuse the expired rendered checkpoint. The
software-only continuation at
`docs/parity/work-plans/20260813T144901Z-API-009/PLAN.md` must first make the
checkpoint transaction pre-armed, ordered, and self-describing. Any later
hardware ordinal requires that regression-backed fix and a fresh complete
hardware contract.

## Non-claims

This closure does not verify or promote API-009. It does not claim physical
IDENTIFY rendering or non-rendering, IDENTIFY clearing, notification dismissal,
software restart, or the complete five-command quorum. It does not treat the
late statistics/block page as evidence of the earlier display state, infer a
positive observation from the API checkpoint, reuse an expired confirmation,
authorize attempt-011, or expose protected device, network, credential,
process, path, sensor, or trace material.
