# STAT-001 worklog

## 2026-08-16T18:31:30Z | Attempt-007 selection and plan checkpoint

- Source commit: `f62935a49609abe685efd03c94b15bf6cd126f7b`
- Actions: Ran the clean synchronized selector, classified SELF-001 and
  BAP-002 as concrete dependency/authorization blockers, audited consumed
  attempt-006, and bound the pushed v12/v6 reason-classifier correction to
  fresh attempt-007.
- Verification: Source and pinned reference are clean; wrapper-007,
  attempt-007, and the public projection are absent; no protected attempt or
  credential content was accessed; the selector reports no open plan.
- Evidence: Attempt-006 crossed all preceding trust, terminal, safe-stop,
  cleanup, seal, mode, and privacy boundaries before the collapsed watchdog
  discriminator. Pushed production-shaped and real-child regressions now
  preserve the exact evaluator-owned cause at that boundary.
- Outcome: STAT-001 is first actionable, and one fresh hardware ordinal is
  eligible after the immutable plan and attempt rebind are fully gated,
  committed, pushed, and packaged.
- Verification: Ordered Cargo, Bright Builds, canonical tests, package,
  parity/progress, redaction, reference, selector, and diff gates pass. The
  admitted immutable plan SHA-256 is
  `c543d685dd575bacdf50bab0ced33360ed4278bbc47f73d2d4ceae909954a94d`.
- Blocker or next safe action: Commit and push the immutable plan/task
  contract, then rebind implementation files without touching `PLAN.md`.

## 2026-08-16T18:43:58Z | Attempt-007 workflow rebind

- Plan commit: `cf2c498e37fdff2fe77ae21a1bca9bd5bc606239`
  (pushed and synchronized before implementation).
- Actions: Rebound the protected wrapper/attempt roots, immutable plan and
  SHA-256 admission, Rust validator ordinal, canonical and generated
  TypeScript contracts, Bazel input, current-task guard, and fresh-process
  fixtures from consumed attempt-006 to attempt-007.
- Verification: Four focused Rust contract tests pass; Bazel contract
  generation is synchronized; and the hashrate-filtered automation suite
  passes through fresh processes and runfiles. The immutable plan is unchanged.
- Evidence: The former ordinal, plan, hash, and protected-root bindings are
  absent from active hashrate producer/consumer sources. Campaign-result v12,
  network-continuity v6, the twenty-value watchdog vocabulary, safety limits,
  and campaign behavior are unchanged.
- Outcome: The minimum rebind is complete and regression-guarded; no detector,
  credential, protected attempt, or device input has been accessed.
- Blocker or next safe action: Run the complete pre-hardware gate, review the
  diff for simplification and unintended behavior, then commit and push the
  exact source before rebuilding the package or detecting hardware.

## 2026-08-16T18:49:52Z | Pre-hardware gate complete

- Candidate source parent: `cf2c498e37fdff2fe77ae21a1bca9bd5bc606239`.
- Verification: Ordered Cargo format, strict Clippy, all-target build,
  all-feature tests, Bright Builds, focused contract and generated-contract
  checks, fresh-process automation, complete flash tests, canonical tests,
  package, parity/progress, redaction, reference, selector, diff, immutable-
  plan, fresh-path, and consumed-binding gates all pass.
- Simplification review: The complete diff is an exact attempt-ordinal and
  immutable-admission rebind across the existing producer, validator, generated
  contracts, runfiles, and tests. No campaign, watchdog, safety, privacy,
  effect, recovery, or public projection logic changed.
- Outcome: The implementation is eligible to become the exact pushed package
  source for the one authorized detector and conditional attempt-007 capture.
- Blocker or next safe action: Commit and push this implementation, rebuild
  and validate the clean commit-bound package, then run only the two frozen
  hardware commands in order.

## 2026-08-16T19:08:10Z | Attempt-007 terminal checkpoint

- Implementation commit: `ec9bedd390142c63b180fe73291ae7035cf83c0f`
  (pushed and synchronized before device admission).
- Actions: Rebuilt and admitted the exact clean board-205 package, ran the
  linked detector once, then consumed attempt-007 with the linked capture
  command exactly once. No retry or attempt-008 was run.
- Verification: The protected wrapper and attempt roots are mode `0700`; all
  seven attempt artifacts are mode `0600`; the campaign-result and network
  digests match; the result is redacted; and the public projection is absent.
- Evidence: Package and runtime identity were trusted, runtime attestation had
  no parse failure, production serial was clean, fresh safety and confirmed
  safe stop passed, USB cleanup was ready, and terminal HTTP, WebSocket, and
  persisted-pool state were valid. The campaign covered 15 of 20 required
  windows before failing closed as `watchdog_unresponsive` with the sealed
  value-free discriminator `watchdog_feed_stale`.
- Outcome: `stop_hardware_blocker`. STAT-001 remains `implemented`, parity
  promotion remains false, and the checklist and progress history are
  unchanged.
- Blocker or next safe action: Use a separate immutable software-only plan to
  trace watchdog checkpoint/feed scheduling, feed ownership, and task
  lifecycle and add a targeted regression-backed correction if the source
  proves one. Attempt-007 is consumed; no unchanged retry or attempt-008 is
  authorized.

## 2026-08-16T19:14:29Z | Final closure audit

- Verification: Ordered Cargo format, strict Clippy, all-target build, and
  all-feature tests passed. Bright Builds, all 45 Bazel test targets, the
  canonical firmware package, parity report, unchanged parity progress,
  redaction, reference integrity, selector, immutable-plan hash, and diff
  checks passed with the closure present.
- Diagnostic note: The first parity-report process completed its report but
  returned transient host error `os error 35`; process-state inspection showed
  no repository failure, and one immediate read-only rerun completed with
  `validation_errors: none`. No validation was waived.
- Simplification review: Closure changes only append the consumed attempt's
  bounded closed result and next safe action. No source, checklist field,
  progress record, public evidence, dependency, or immutable plan changed.
- Outcome: The one-row invocation is ready for its closure commit and push.
  STAT-001 remains `implemented` at 74 verified rows and 78.7 percent reported
  completion; the selector reports no open plan.
