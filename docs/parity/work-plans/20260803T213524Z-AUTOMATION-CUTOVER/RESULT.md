# Typed Automation Checklist Migration Result

The migration ledger maps each changed row to its semantic replacement. Software-equivalent workflows retain their prior status. `REL-09`, `V12-HOSTNAME-205`, and `V12-PACKAGE-IDENTITY-205` are downgraded to `implemented` because active consumers reject their legacy evidence schemas.

No historical evidence was rewritten, and no hardware claim was promoted.
