# Parity work closure

- Parity row: `UI-004`
- Final status: `implemented`
- Outcome: `blocked`
- Verification claimed: `no`
- Plan SHA-256: `ce9b94f1a3336500bbbb6adc0ab51d5c4a26f5ea44eba72928dd07b6dca42dd7`
- Active task: `task-parity-ui004-live-browser-attempt-001`

## Closure reason

The clean pushed implementation and exact package at
`bf5b74f98cdb117ca5682b0118a61743db85856f` passed every software, privacy,
reference, integrity, package, detector, and pre-effect gate. The sole detector
admitted one Ultra 205, and the sole conditional attempt-001 completed without
a hardware retry. Its independently validated private operator projection
proved the exact package, one normal software restart, disabled mining and
hardware control, and complete device cleanup.

The isolated named Playwright session then loaded all seven embedded routes at
desktop and mobile sizes, opened and closed responsive navigation, kept both
password inputs blank, preserved the no-file update guard and visible
OTAWWW-unavailable state, observed retained logs plus the live log WebSocket,
kept all observed resources same-origin and successful, recorded zero console
errors or warnings, and closed completely. Twenty private artifacts were bound
by digest in the mode-`0600` browser attestation.

The plan's single projector invocation then stopped as `evidence_invalid`
before publication. Shell redirection created `projector.stdout` and
`projector.stderr` inside the protected wrapper with the process default mode
before the projector validated that tree. Those two files were therefore mode
`0644`, and the projector correctly rejected `private file mode is invalid`.
No public projection or candidate was created. Cleanup restored every private
directory/file to mode `0700`/`0600`; the browser session and USB transaction
were already closed. The immutable plan permits no second projector run, so
UI-004 remains `implemented`.

## Next safe action

Start a fresh software-only UI-004 continuation from this clean pushed closure.
Preserve attempt-001 and its private operator/browser artifacts without
reinterpretation. Add a production-shaped regression for shell-created
projector capture files and freeze an owner-only `umask 077` before those
redirections. Because a closure commit advances repository HEAD beyond the
exact package source, the continuation must also define and independently
validate a narrow source-binding rule for unchanged captured firmware/static
artifacts and a separately recorded projector source identity. It may then
allow exactly one new software-only projection/validation transaction over the
preserved attempt, with no detector, flash, restart, or browser rerun. If that
binding cannot be made exact without weakening evidence, require a fresh plan
and hardware ordinal instead.

## Non-claims

This closure does not verify or promote UI-004, does not publish live browser
evidence, and does not claim physical panel rendering, physical input, settings
or theme submission, firmware upload, OTA or OTAWWW, recovery behavior,
scoreboard or swarm population, mining, ASIC work, pool access, hardware
control, other boards, release readiness, or unbounded browser/device behavior.
The successful private observations are not public parity evidence until a
fresh immutable continuation independently validates and publishes their exact
join.
