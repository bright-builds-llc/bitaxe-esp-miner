# API-009 worklog

## 2026-08-12T18:24:05Z | Plan checkpoint

- Source commit: `cde304c6a7a0bae0af89164f36394ab451e2c370`.
- Actions: Selected API-009 first after truthfully closing attempt-005 and
  joined its required IDENTIFY document to the Rust confirmation owner,
  TypeScript campaign parent, buffered process port, and public CLI streams.
- Verification: Attempt-005 proves the readiness fix, confirmed pause/resume,
  same active boot/package, and arrival at the rendered checkpoint. Source
  inspection proves the campaign writes a private typed request but the
  awaiting automation parent exposes no live checkpoint signal before child
  settlement.
- Evidence: No new hardware action or parity evidence is claimed. Only closed
  checkpoint categories and source contracts inform this plan; private
  attempt-005 values remain ignored.
- Outcome: The next material step is a concurrent redaction-safe checkpoint
  supervisor, not a firmware change or automatic physical confirmation.
- Blocker or next safe action: Run plan-only gates, commit and push this
  immutable checkpoint, then reproduce the silent parent boundary at the real
  child-process seam before implementing the minimal fix.

## 2026-08-12T18:36:45Z | Live checkpoint handoff implemented

- Source commit: pending verified implementation commit on top of
  `c5fe3e6c353558be22d19b5f9ed5e9b5667ac18b`.
- Actions: Added a focused operator-checkpoint supervisor around the already
  pending campaign promise. It admits only the ordered rendered/cleared private
  documents, emits each closed signal exactly once through the injected stderr
  sink, performs no acknowledgement, and stops polling when the child settles.
  The public CLI still writes one final result envelope to stdout. No general
  process-start API was added.
- Verification: The production-shaped regression failed before the supervisor
  with two valid required documents and zero signals. It passes after the fix.
  Fake and real-child tests cover live pre-settlement publication, exactly-once
  ordering, a partially visible producer write, settlement cancellation,
  missing, malformed, wrong-mode, and wrong-order documents, sink formatting,
  sensitive-output absence, and campaign-failure precedence. The complete 243
  automation-test set, TypeScript production/test builds, and Bright Builds
  checks pass.
- Evidence: Signals contain only schema, command, observation, and required
  status. They contain no origin, hostname, port, USB/network identity,
  credential, endpoint, path, secret, raw trace, or physical-observation claim.
  No device action or parity projection occurred.
- Outcome: The confirmed buffered-parent handoff gap is fixed without changing
  firmware, upstream's 30-second IDENTIFY duration, confirmation ownership,
  process interfaces, or public evidence schemas.
- Blocker or next safe action: Run the full ordered mandatory gates, exact
  firmware/package build, immutable-plan and privacy review, then commit and
  push the exact source before any detector or hardware action.

## 2026-08-12T18:40:01Z | Source gate complete

- Source commit: pending implementation commit on top of
  `c5fe3e6c353558be22d19b5f9ed5e9b5667ac18b`.
- Actions: Completed the simplification pass by isolating checkpoint parsing,
  supervision, formatting, and real-process tests in focused modules while
  retaining the existing `ProcessPort.run` contract. Kept the CLI and primary
  campaign module within the managed file-length boundary.
- Verification: Ordered Cargo format, strict all-target/all-feature Clippy,
  Cargo build and tests, Bright Builds, all 41 Bazel tests, parity, progress,
  redaction, reference, exact firmware build, TypeScript production/test
  builds, selector, unique task, immutable plan digest, reference cleanliness,
  public-projection absence, diff, and sensitive-output checks pass. The
  isolated parity tail passed after the immediately preceding all-target Bazel
  run twice encountered transient macOS resource exhaustion.
- Evidence: This is software evidence only. The production stderr signal is a
  closed four-field document; stdout remains the single final automation
  envelope, and no operational or identifying value is exposed.
- Outcome: The exact implementation is eligible to commit and push. No device
  action has occurred under this plan.
- Blocker or next safe action: Commit and push the exact source, verify remote
  synchronization and cleanliness, then build the exact package. Only a fresh
  successful board-205 detector may make attempt-006 eligible.

## 2026-08-12T18:48:08Z | Attempt-006 closed

- Source commit: `17835217b5ec1d9e7d33363cbf346a0d4762d332`.
- Actions: Built the exact clean package, admitted exactly one board-205
  ESP32-S3 through the fresh protected detector, and ran the sole bounded
  attempt-006 with the new live checkpoint stream attached. No retry and no
  early or inferred confirmation was run.
- Verification: The sealed protected result and private modes pass. Package,
  runtime identity, attestation, protocol gate, same-boot/package binding,
  safe stop, and USB cleanup are trusted. The campaign observed a genuine
  positive block, three qualified and accepted shares, confirmed pause,
  confirmed resume, and active mining after resume. The parent emitted the
  closed rendered checkpoint while the child was live. It remained
  unconfirmed, no cleared checkpoint was created, and exactly one IDENTIFY
  request occurred. The terminal transition later sampled stale safety at a
  deadline.
- Evidence: The protected v8 result preserved
  `network_correlation_failed` / `safety_prerequisites_stale`; the redacted
  wrapper reported `hardware_blocked`. No origin, hostname, port, USB/network
  identity, credential, endpoint, private path, checkpoint secret, raw trace,
  or inferred physical observation is published. The public projection is
  absent.
- Outcome: The live host handoff is hardware-proven, but the required physical
  rendered observation was not acknowledged during its exact 30-second
  interval. API-009 remains `implemented` without partial promotion.
- Blocker or next safe action: Close without retry. Any fresh continuation must
  require an explicit pre-effect operator-ready occurrence before detector or
  campaign start, then use the proven live prompt and typed request-once
  confirmations. Do not reuse attempt-006 or confirm its expired checkpoint.
