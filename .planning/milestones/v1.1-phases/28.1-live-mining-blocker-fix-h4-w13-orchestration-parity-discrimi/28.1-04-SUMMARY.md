---
phase: 28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi
plan: 04
subsystem: firmware-investigation-flags
tags: [rust, esp32s3, bm1366, flag-retirement, single-dispatch-control, fail-closed]

# Dependency graph
requires:
  - phase: 28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi
    provides: "Plan 02: continuous listener + regeneration cadence as unconditional bridge defaults; W13 unconditional retention; bounded-read rollback branch behind continuous == false"
  - phase: 28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi
    provides: "Plan 03: probes wired without reading investigation flags"
provides:
  - Clean investigation-flag inventory matching the locked disposition table (four retired modes gone from Rust source; parser-test fixtures in the pure crate exempt and untouched)
  - single_dispatch_bounded_read opt-out control lever wired into new_for_phase27 (continuous = !enabled) restoring the pre-28.1 single-dispatch bounded-read fail-closed behavior
  - JOB_REDISPATCH_INTERVAL_MS deleted; require_diagnostic_nonce / require_uart_proof_for_production / clear_rx_before_production_work levers intact
  - Fail-closed default build proof post-retirement and the 28.1-flag-disposition-note.md run-doc comparability record
affects: [28.1-05 hardware evidence runs, phase27 run-doc comparability]

# Tech tracking
tech-stack:
  added: []
  patterns: [compile-time opt-out control lever selecting the dormant rollback branch]

key-files:
  created:
    - .planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-flag-disposition-note.md
  modified:
    - firmware/bitaxe/src/asic_adapter/work_result_investigation.rs
    - firmware/bitaxe/src/asic_adapter.rs
    - firmware/bitaxe/src/live_stratum_runtime.rs

key-decisions:
  - "W13 retention marker renamed to asic_work_result_trace=post_init_retention (bootstrap=initialized_no_mining preserved) because the acceptance greps forbid retired mode literals anywhere in firmware/"
  - "Control mode suppresses BridgeStep::Regenerate at the interpreter so single_dispatch_bounded_read reproduces the pre-28.1 pump exactly (dispatch only on fresh pool work)"
  - "Dead initialized_no_mining_bootstrap fallback branches deleted with the helper (provably unreachable since Plan 02: guarded by levers that force the helper false)"

patterns-established:
  - "Retired investigation modes become unconditional defaults; the only compile-time levers left are explicit opt-outs (rollback) and opt-ins (diagnostics)"

requirements-completed: []
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 28.1-2026-07-07T00-34-17
generated_at: 2026-07-07T02:38:33Z

# Metrics
duration: 7min
completed: 2026-07-07
---

# Phase 28.1 Plan 04: Flag Retirement and Control Lever Summary

**Four dead investigation modes (continuous_result_task, job_redispatch_pump, initialized_no_mining_gate, skip_boot_diagnostic_work) retired from Rust source with JOB_REDISPATCH_INTERVAL_MS, replaced by one new single_dispatch_bounded_read opt-out that provably restores the pre-28.1 single-dispatch bounded-read fail-closed pump for the Plan 05 A/B control run — landed as one independently revertible commit plus a run-doc disposition note**

## Performance

- **Duration:** ~7 min
- **Started:** 2026-07-07T02:32:07Z
- **Completed:** 2026-07-07T02:38:33Z
- **Tasks:** 2
- **Files modified:** 4 (3 code, 1 note created)

## Accomplishments

- **Task 1 (flag retirement + control lever):** Deleted `continuous_result_task_enabled`, `job_redispatch_pump_enabled`, `phase27_initialized_no_mining_bootstrap`, `skip_boot_diagnostic_work`, `initialized_no_mining_bootstrap_gate`, and `JOB_REDISPATCH_INTERVAL_MS` from `firmware/bitaxe/src/asic_adapter/work_result_investigation.rs`, along with the stale re-export block in `asic_adapter.rs` and two provably dead `initialized_no_mining_bootstrap` fallback branches on the opt-out diagnostic path (unreachable since Plan 02: they fire only when a rollback lever is on, and the helper returned false whenever a lever was on).
- Added `pub fn single_dispatch_bounded_read_enabled()` via the existing `investigation_modes_contain` parser and wired `new_for_phase27()` to `continuous: !asic_adapter::single_dispatch_bounded_read_enabled()`. In control mode the bridge behaves exactly like the pre-28.1 pump: one dispatch per queued pool work, no listener arming (`maybe_arm_continuous_listener` early-returns), no regeneration (`BridgeStep::Regenerate` suppressed at the interpreter when `!continuous`), bounded 10 s read with deadline, and fail-closed `Blocked { reason: ProductionAsicBlocker::ResultTimeout }` on silence — the only remaining path that emits `production_result_timeout`.
- Rollback levers `require_diagnostic_nonce`, `require_uart_proof_for_production`, and opt-in `clear_rx_before_production_work` plus all frequency-ramp/baud/delay modes are byte-identical.
- **Task 2 (fail-closed proof + disposition note):** Recorded the post-retirement fail-closed default proof (`just build` with no action_env green; `BITAXE_MINING_EVIDENCE_MODE` gate intact at `build.rs:7`, `asic_adapter.rs:200`, `mining_evidence_mode.rs:11`; package script `--investigation` passthrough intact; `just verify-reference` clean at `c1915b0a`) and wrote `28.1-flag-disposition-note.md` with the locked disposition table, run-doc comparability statements (legacy invocations are no-ops; `production_result_timeout` disappears by design, replaced by `asic_bridge=timeout_streak count=<n>`; `single_dispatch_bounded_read` is the only reproduction path), and rollback levers.

## Task Commits

1. **Task 1: retire obsolete investigation flags and add single_dispatch_bounded_read** - `3969df0` (feat)
2. **Task 2: flag disposition note + fail-closed default proof** - `10ccd9b` (docs)

## Files Created/Modified

- `firmware/bitaxe/src/asic_adapter/work_result_investigation.rs` - retired helpers and JOB_REDISPATCH_INTERVAL_MS deleted; `single_dispatch_bounded_read_enabled()` added; keepers unchanged
- `firmware/bitaxe/src/asic_adapter.rs` - retired re-exports replaced with `single_dispatch_bounded_read_enabled` re-export; W13 marker renamed to `asic_work_result_trace=post_init_retention bootstrap=initialized_no_mining`; dead bootstrap fallback branches removed
- `firmware/bitaxe/src/live_stratum_runtime.rs` - `new_for_phase27` continuous wiring; Regenerate suppression in control mode; field doc updated
- `.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-flag-disposition-note.md` - disposition table, run-doc comparability, rollback levers, fail-closed proof

## Verification Evidence

- `cargo fmt --all` clean; `cargo clippy --all-targets --all-features -- -D warnings` green; `cargo build --all-targets --all-features` green; `cargo test --all-features` 0 failures; `just test` 36/36 pass; `just build` xtensa firmware green (ELF rebuilt against modified sources: retired marker literal absent from ELF, `post_init_retention` present)
- Acceptance greps all pass:
  - `grep -rn "continuous_result_task_enabled\|job_redispatch_pump_enabled\|phase27_initialized_no_mining_bootstrap" firmware/ crates/ tools/ scripts/` — ZERO matches
  - `grep -rn "continuous_result_task\|job_redispatch_pump\|initialized_no_mining_gate\|skip_boot_diagnostic_work" firmware/ tools/ scripts/` — ZERO matches
  - Same pattern over `crates/` excluding `crates/bitaxe-asic/src/work_result_investigation.rs` — ZERO matches (parser-test fixtures are the only survivors, in that one file)
  - `grep -rn "JOB_REDISPATCH_INTERVAL_MS" firmware/ crates/` — ZERO matches
  - `single_dispatch_bounded_read` present in `work_result_investigation.rs`; `single_dispatch_bounded_read_enabled` present in `live_stratum_runtime.rs:699`
  - All three keeper levers present; `ResultTimeout` retained at the bounded-read site (`live_stratum_runtime.rs:1019` only)
  - `git diff --stat crates/bitaxe-asic/src/work_result_investigation.rs` empty (fixtures untouched)
- Note checks: file exists; `production_result_timeout` and `single_dispatch_bounded_read` present; `grep -c "^---$"` == 2 (frontmatter delimiters only)
- Fail-closed proofs: `just build` (no action_env) green; `BITAXE_MINING_EVIDENCE_MODE` grep non-empty; `phase27-live-hardware-bridge-package.sh --help` shows `--investigation MODE` passthrough; `just verify-reference` clean at `c1915b0a63bfabebdb95a515cedfee05146c1d50`

## Decisions Made

- Renamed the W13 retention marker prefix to `asic_work_result_trace=post_init_retention` (from the retired-literal prefix) because the plan's acceptance greps forbid retired mode literals anywhere in `firmware/`; the `bootstrap=initialized_no_mining` substring that Plan 05 and the success ladder grep for is preserved, and the rename is recorded in the disposition note for run-doc comparability
- Suppressed `BridgeStep::Regenerate` at the pump interpreter when `continuous == false` so the control run cannot regenerate work — Plan 02's orchestrator cadence would otherwise fire Regenerate ~2 s after a control-mode dispatch, deviating from the pre-28.1 behavior the A/B run must reproduce
- Deleted `initialized_no_mining_bootstrap_gate()` (no callers) as a mode-string helper for the retired `initialized_no_mining_gate` mode
- Deleted the two dead `initialized_no_mining_bootstrap` fallback branches with the helper rather than leaving unreachable code behind the opt-out levers

## Deviations from Plan

### Auto-fixed Issues

None — the plan's actions and acceptance criteria were executed as written. The marker rename and dead-branch deletion above are consequences the acceptance greps mandate (zero retired-mode literals in `firmware/`), recorded under Decisions Made for verifier clarity.

**Total deviations:** 0
**Impact on plan:** None.

## Issues Encountered

None. Host clippy/build/test do not compile the firmware crate (esp-idf-sys host gate); the firmware-side changes were compile-verified through the `just build` xtensa target, with ELF string inspection confirming the retired marker literal is gone and the renamed marker is present.

## Known Stubs

None — the control lever selects the fully implemented bounded-read branch retained by Plan 02; nothing is placeholder.

## Threat Flags

None — no new endpoints, auth paths, file access, or schema surface. Flag retirement narrowed the compile-time surface; mining enablement stays keyed on `BITAXE_MINING_EVIDENCE_MODE` + hardware ack env with the fail-closed `just build` proof recorded (T-28.1-14); the disposition note records the no-op legacy invocations and the exact reproduction lever with a one-commit revert path (T-28.1-15); full gates plus ELF marker inspection guard against deletion drift, and the crate parser fixtures are diff-verified untouched (T-28.1-16).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 28.1-05 default hardware runs build via `scripts/phase27-live-hardware-bridge-package.sh` with NO `--investigation`; the J4 control run builds with `--investigation single_dispatch_bounded_read` and should show `production_result_timeout` if the chip stays silent
- Run docs must grep `bootstrap=initialized_no_mining` (unchanged) — the retention trace prefix is now `post_init_retention` per the disposition note
- No blockers

***
*Phase: 28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi*
*Completed: 2026-07-07*

## Self-Check: PASSED

All 4 claimed files plus the SUMMARY exist on disk and both claimed commits (3969df0, 10ccd9b) exist in git history.
