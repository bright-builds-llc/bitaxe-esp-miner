---
phase: 28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi
plan: 05
subsystem: hardware-evidence
tags: [ultra205, bm1366, hardware-evidence, upstream-ab, blocker-disposition, redacted-evidence]

# Dependency graph
requires:
  - phase: 28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi
    provides: "Plans 01-04: parity-default orchestration, probes, flag disposition, single_dispatch_bounded_read control lever"
provides:
  - J3 parity-default run doc with the full success ladder dispositioned per rung (J2c confound FALSIFIED; chip-alive-but-no-nonces)
  - J4 control run doc proving single_dispatch_bounded_read restores the pre-28.1 fail-closed pump (regression guard REACHABLE)
  - Final blocker disposition firmware-nonce-production backed by an executed upstream v2.14.0 A/B (upstream mines on the same board; recovery verified; pin clean)
affects: [follow-up upstream wire-byte diff phase, STR-09/CFG-07 future promotion path]

# Tech tracking
tech-stack:
  added: []
  patterns: [foreground flash-monitor + local pool-input-bridge watcher (J2c procedure) as the reliable hardware capture path, upstream factory release image + managed nvs_partition_gen NVS seeding for phase-gated A/B]

key-files:
  created:
    - .planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-run-J3-parity-default.md
    - .planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-run-J4-control-single-dispatch.md
    - .planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-blocker-disposition.md
  modified: []

key-decisions:
  - "A/B trigger judged MET: J3's rx_chunk lines are 1:1 register-read probe responses, not work-result RX, so R7-as-intended is not met — treating probe RX as R7 would have overclaimed resolved"
  - "Upstream image obtained via official release download (v2.14.0, closest release to the pin at v2.14.0+11) instead of a pin build — zero pin-dirtying risk, provenance recorded with sha256"
  - "Upstream configured from its own committed config-205.cvs (stock pool defaults, stock donation user) plus local wifi only, selftest=0 — no local pool credentials used in the A/B"

patterns-established:
  - "Phase-gated destructive A/B: official-release image + seeded NVS + same-session full-factory Rust recovery + verify-reference, all evidence category-labeled"

requirements-completed: []
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 28.1-2026-07-07T00-34-17
generated_at: 2026-07-07T03:32:17Z

# Metrics
duration: 51min
completed: 2026-07-07
---

# Phase 28.1 Plan 05: Hardware Evidence Ladder, Control Run, and Upstream A/B Summary

**Detector-gated Ultra 205 runs prove the 28.1 orchestration fixes work (22 dispatches at ~2 s cadence, J2c confound falsified) and the chip is alive but not hashing under Rust firmware; the executed upstream v2.14.0 A/B mined 171 nonces with accepted shares on the same board, isolating the blocker to firmware-nonce-production with recovery verified and the pin clean**

## Performance

- **Duration:** ~51 min (2026-07-07T02:41:47Z -> 03:32:17Z), including three full 360 s hardware captures plus the A/B and recovery flashes
- **Completed:** 2026-07-07T03:32:17Z
- **Tasks:** 3
- **Files created:** 3 committed run/disposition docs

## Accomplishments

- **Task 1 (J3 parity-default):** `just verify-reference` PASS -> `just detect-ultra205` PASS (exactly one likely port, board-info OK) -> parity-default package (no `--investigation`) -> foreground `just flash-monitor ... capture-timeout-seconds=360` with the J2c-style local pool-input-bridge watcher. Full trusted 360 s capture. Ladder: R1/R2/R3/R5/R6 MET, R4 MET-with-caveat (probe responses RECEIVED post-init and post-dispatch but classified `register_read_unparsed`/`production_result_malformed` — the fixed chip-identity frame), R7/R8 NOT MET (zero `result_correlated`, zero shares), negative check MET (zero `production_result_timeout`). **R3 milestone recorded: 22 `work_dispatched` at 2050-2110 ms spacing with `job_redispatched extranonce2_counter=1..19` — the J2c confound is falsified.** Power delta -390 mW after dispatch: chip not hashing.
- **Task 2 (J4 control):** `--investigation single_dispatch_bounded_read` package (ELF identity verified via baked commit string before flash and `observed_firmware_commit` after). Control semantics confirmed on hardware: exactly one dispatch, ZERO `job_redispatched`, zero listener/timeout_streak markers, fail-closed `asic_production_status=fail_closed reason=production_result_malformed` ~150 ms post-dispatch (the probe response preempted the 10 s `production_result_timeout` deadline — only a fully silent chip can reach the timeout branch in this build). **Regression-guard verdict: REACHABLE.**
- **Task 3 (A/B + disposition):** Trigger MET (R3 met, stalled before R7, probes = chip-alive-but-no-nonces). Executed the phase-gated upstream A/B: official `esp-miner-factory-205-v2.14.0.bin` (sha256 recorded) + NVS generated from upstream's own `config-205.cvs` with local wifi and `selftest=0` via managed `.embuild` `nvs_partition_gen.py`. **Upstream mined immediately: first nonce ~17.5 s, 171 `asic_result` nonce lines, 68 scoreboard entries, 14 submits / 11 accepted responses in 380 s, hardware version-rolling active.** Mandatory same-session recovery executed: Rust parity-default factory reflash + NVS reseed, trusted markers verified (`phase27_safety_bring_up=complete`, `bootstrap=initialized_no_mining`), `just verify-reference` PASS. `disposition: firmware-nonce-production`.

## Task Commits

1. **Task 1: J3 parity-default run doc** - `85a9cca` (docs)
2. **Task 2: J4 control run doc** - `d650db8` (docs)
3. **Task 3: blocker disposition + A/B evidence** - `10ba825` (docs)

## Files Created/Modified

- `28.1-run-J3-parity-default.md` - 9-rung ladder table with quoted timestamped markers; J2c falsification milestone; redaction-clean
- `28.1-run-J4-control-single-dispatch.md` - control expectations dispositioned; regression-guard verdict REACHABLE; probe/bounded-read interaction documented
- `28.1-blocker-disposition.md` - disposition firmware-nonce-production; A/B provenance/commands/recovery; exact non-claims; next-step recommendation (upstream `BM1366_SERIALTX_DEBUG` wire-byte diff)

## Verification Evidence

- Detector gate recorded before every flash sequence; all captures `capture_status=timed_out_after_trusted_output`, `trusted_output=true`, wall clock >= 480 s per hardware run
- Redaction sweeps on all three committed docs: `poolurl|pooluser|poolpassword|ssid=` zero matches; IPv4/MAC regex zero matches; no `http(s)://`, no raw pool host, no addresses, no nonce payloads; `grep -c "^---$"` = 2 (frontmatter only) on each
- `grep -n "work_dispatched"` and `grep -n "c1915b0a"` match in J3 doc; `single_dispatch_bounded_read` present in J4 doc; `disposition: firmware-nonce-production` exact-match in disposition doc with `28.1-run-J3` citations and an "Exact non-claims" section
- `git diff --stat docs/parity/checklist.md` empty (no checklist promotion); `just verify-reference` PASS at plan completion
- Host gates green (fmt/clippy/build/test + earlier `just test` state; no Rust code changed in this plan)

## Decisions Made

- **R7 judged NOT MET despite literal `rx_chunk` lines:** each of the 3 rx_chunk occurrences correlates 1:1 with a ReadChipId TX (chip detect, post-init probe, post-dispatch probe); zero unsolicited/work-result RX existed. Treating probe echoes as R7 would have produced a false `resolved` disposition.
- **Release-download path for the upstream image** (plan's documented fallback used as primary in practice): eliminates pin-dirtying and toolchain risk; provenance = release tag + filename + sha256; pin is v2.14.0+11 so v2.14.0 is behaviorally closest.
- **No local pool credentials in the A/B:** upstream stock `config-205.cvs` defaults (public stock pool + stock donation user) were sufficient to prove mining; wifi was the only local input (seeded via NVS, contents never printed/committed).
- **`selftest=0` in the seeded upstream NVS** (stock config has 1) so the 380 s window captured mining rather than the self-test flow; recorded as a config deviation in the disposition doc.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] nvs_partition_gen.py needs the managed IDF python env**
- **Found during:** Task 3 (A/B NVS generation)
- **Issue:** System python3 lacks `esp_idf_nvs_partition_gen`; the raw IDF script import failed.
- **Fix:** Invoked the generator through the managed `.embuild/espressif/python_env/idf5.5_py3.14_env` interpreter (sanctioned managed-tool path per repo guidance).
- **Files modified:** none in-repo (scratchpad script only)

### Notes

- The plan's R4 wording anticipated `register_read_parsed` or `register_read_silent`; hardware produced the Plan 03 third outcome (`register_read_unparsed` post-init, `production_result_malformed` post-dispatch) — a RECEIVED response either way. Documented as "MET (with parse caveat)" in J3 rather than forcing a binary that hides the discriminating fact. The production-path parser not classifying the chip-identity frame as a register read is a candidate small follow-up (does not affect the disposition).
- J4's expected `production_result_timeout` was unreachable because the chip answered the post-dispatch probe within the bounded read; the fail-closed family was proven via the malformed-read branch instead. Explicitly dispositioned in the J4 doc.

**Total deviations:** 1 auto-fixed (environment), 2 documented expectation nuances
**Impact on plan:** None on scope; all three artifacts and the disposition contract delivered as specified.

## Issues Encountered

- The J3 session ended at device-time ~63 s (pool-side reconnect -> safe stop -> pending settings-patch effect applied -> runtime stopped) and the device idled for the rest of the 360 s window. All ladder rungs were already dispositioned from the first ~63 s (22 dispatches); recorded in the J3 run doc.

## Known Stubs

None — this plan produced evidence documents only; no code changed.

## Threat Flags

None — no new code surface. T-28.1-17..21 mitigations all executed: category labels + per-doc regex sweeps (17), detector gate before every flash (18), A/B ran only under its documented trigger with same-session verified recovery (19), release-download image left the pin untouched with `verify-reference` PASS (20), ladder rungs individually dispositioned with zero checklist edits and promotion recorded only as a validator-gated follow-up (21).

## User Setup Required

None.

## Next Phase Readiness

- **Blocker isolated:** `firmware-nonce-production`. The documented follow-up is an out-of-tree upstream pin build with `BM1366_SERIALTX_DEBUG`/RX debug to capture known-good wire bytes and diff against Rust TX (J3 capture retained locally). Priority observation for that diff: upstream negotiates pool version-rolling and its nonces carry nonzero version_bits, while the Rust client does not negotiate a version mask (currently deferred scope).
- STR-09/CFG-07 promotion remains ineligible (R8 not met for Rust firmware); validator gate unchanged.

***
*Phase: 28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi*
*Completed: 2026-07-07*

## Self-Check: PASSED

All 4 claimed files exist on disk and all three task commits (85a9cca, d650db8, 10ba825) exist in git history.
