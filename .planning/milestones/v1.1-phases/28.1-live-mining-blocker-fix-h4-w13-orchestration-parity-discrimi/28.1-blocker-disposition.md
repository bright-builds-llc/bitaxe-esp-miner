---
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 28.1-2026-07-07T00-34-17
generated_at: 2026-07-07T03:30:00Z
---

# Phase 28.1 Blocker Disposition

disposition: firmware-nonce-production

Date: 2026-07-07 (UTC)
Board: 205 (Bitaxe Ultra 205, BM1366), detector-gated port `/dev/cu.usbmodem1101` (single likely port; `espflash board-info --chip esp32s3 --non-interactive` OK; MAC redacted)
Rust source commits: J3 at `04ef41679e2f69252a259672797faadff7a83657`, J4/A-B/recovery at `85a9cca...`/`d650db810177`
Reference pin: `c1915b0a63bfabebdb95a515cedfee05146c1d50` — `just verify-reference` PASS at plan completion (pin untouched; the A/B used an official release binary, no pin build)

## Trigger evaluation (per the Plan 05 phase-gated procedure)

- 28.1-run-J3-parity-default.md: R3 MET (22 `asic_production_status=work_dispatched` at ~2 s cadence, extranonce2 counter 1..19) — the orchestration fixes work and the J2c confound is falsified.
- J3 stalled before R7: zero `asic_production_status=result_correlated`, zero shares; the only RX lines were 1:1 register-read probe responses. Probes indicate **chip-alive-but-no-nonces** (register reads answered post-init and post-dispatch; power delta -390 mW, i.e. not hashing).
- Therefore the documented A/B trigger was MET and the user-approved upstream A/B was EXECUTED (not skipped).

## Upstream A/B evidence

**Upstream image provenance:** official release download (fallback path in the plan-body procedure; no pin build, pin left untouched): bitaxeorg/ESP-Miner release `v2.14.0`, asset `esp-miner-factory-205-v2.14.0.bin`, sha256 `77ea24e81a92fa11a6d3ae7f1da89b533143849b3386766ce6ba06ae05dc161d`. The reference pin `c1915b0` is `v2.14.0` + 11 commits, so `v2.14.0` is the closest released build to the pin. On-device identity confirmed in the capture: `app_init: Project name: esp-miner`, `App version: v2.14.0`, `Device Model: Ultra`, `Board Version: 205`, `ASIC: 1x BM1366 (112 cores)`.

**Configuration:** an NVS partition was generated from the upstream-committed `reference/esp-miner/config-205.cvs` (board identity + upstream stock pool defaults + upstream stock donation user, exactly as committed upstream) with two local changes: `wifissid`/`wifipass` filled from the local wifi credentials file (opaque local runtime input; contents never printed or committed) and `selftest` set to 0 so the capture went straight to mining. Generated with the managed `.embuild` ESP-IDF v5.5.4 `nvs_partition_gen.py`. wifi_config: local-owner-supplied. pool_config: upstream-stock-default (as committed in `reference/esp-miner/config-205.cvs`; no local pool credentials used).

**Exact flash/monitor commands:**

1. `espflash write-bin --chip esp32s3 --port /dev/cu.usbmodem1101 --non-interactive 0x0 esp-miner-factory-205-v2.14.0.bin`
2. `espflash write-bin --chip esp32s3 --port /dev/cu.usbmodem1101 --non-interactive 0x9000 upstream-nvs.bin`
3. `espflash monitor --chip esp32s3 --port /dev/cu.usbmodem1101 --non-interactive` captured locally for 380 s

No full-chip erase was run. Flash scope beyond app/OTA: the upstream factory image spans the full 15.8 MB upstream layout (bootloader, partition table, NVS, phy, factory app, spiffs www, ota_0/ota_1, otadata, coredump region), and the seeded NVS overwrote 0x9000..0xF000 — recorded here per the procedure; recovery below restores the full Rust layout.

**Observed behavior (redacted; device-ms timestamps):**

- ASIC detected and initialized: `I (6282) common: Chip 0 detected` -> full BM1366 init at 485 MHz, vcore set to 1200 mV.
- Stratum session to the upstream stock default pool (endpoint category: upstream-stock-default) subscribed, authorized, difficulty set by pool.
- **The BM1366 PRODUCES NONCES under upstream firmware on this exact board:** first hardware nonce at ~17.5 s after boot; 171 `asic_result:` nonce lines and 68 `scoreboard: New #N` entries across the 380 s capture, continuing through the final seconds (`I (378452) asic_result: ...`). Hardware version-rolling active (nonzero `version_bits` on results; upstream negotiated `mining.configure` version-rolling with the pool).
- **14 `mining.submit` share submissions with 11 accepted responses (`"error":null,"result":true`) observed inside the capture window** (remaining submits were near the capture cut). These are UPSTREAM-firmware shares — they prove board/chip/pool health only and make NO Rust parity claim.
- Thermal/power corroboration: fan controller at 100% output holding ~62.6 C at the setpoint boundary — the chip is drawing hashing-class power under upstream, versus the FALLING power delta (-390 mW) under Rust firmware in J3.

**Recovery (mandatory, executed in the same session):**

1. Rebuilt the Rust parity-default package (`bash scripts/phase27-live-hardware-bridge-package.sh`).
2. `just flash-monitor board=205 port=/dev/cu.usbmodem1101 image=bazel-bin/firmware/bitaxe/bitaxe-ultra205-factory.bin evidence-dir=<local-gitignored-dir> capture-timeout-seconds=360 wifi-credentials=wifi-credentials.json` — full Rust factory image + NVS reseed (`nvs_seed_status=provided`), wall clock >= 480 s.
3. Recovery capture verified trusted Rust boot markers: `I (1893) ... phase27_safety_bring_up=complete` and `I (10043) ... asic_work_result_trace=post_init_retention bootstrap=initialized_no_mining`; `capture_status=timed_out_after_trusted_output`, `trusted_output=true`, `observed_firmware_commit=d650db810177` (HEAD), `observed_reference_commit=c1915b0a...`.
4. `just verify-reference` — PASS (`reference clean: c1915b0a63bfabebdb95a515cedfee05146c1d50`).

## Disposition rationale (evidence per rung)

| Question | Answer | Evidence |
| --- | --- | --- |
| Does the Rust orchestration dispatch work correctly? | YES | 28.1-run-J3-parity-default.md R3: 22 dispatches, ~2 s cadence, fresh extranonce2; J2c confound falsified |
| Is the chip/UART path alive under Rust firmware? | YES | 28.1-run-J3-parity-default.md R4: register-read probe answered post-init AND post-dispatch (fixed chip-identity frame); 28.1-run-J4-control-single-dispatch.md corroborates |
| Does the chip hash under Rust firmware? | NO | J3 R5: power delta -390 mW after dispatch; R7/R8 not met (zero nonces/shares in 360 s across 22 dispatches) |
| Is the old fail-closed behavior still reachable (regression guard)? | YES | 28.1-run-J4-control-single-dispatch.md: single dispatch, zero redispatch, fail-closed at ~150 ms post-dispatch |
| Does the board/ASIC mine under upstream firmware? | YES | A/B above: 171 nonce results, 68 scoreboard entries, 14 submits / 11 accepted responses in 380 s |

**Conclusion: `firmware-nonce-production`.** The hardware is healthy (upstream mines on the same board, same session). The Rust firmware dispatches work on a correct cadence to a chip that is alive and answering register reads, yet the cores never hash (flat-to-falling power, zero nonces). The blocker is isolated to the Rust firmware's nonce-production path — some difference in init/work programming (not orchestration, which this phase fixed and proved).

## Exact non-claims (extending the phase27-04-defer-note tiers)

- **No Rust accepted/rejected share claim.** The Share tier (`result_correlated` + submit classification) remains NOT MET for the Rust firmware; R8 was not met in J3/J4. The upstream A/B shares are explicitly NOT Rust parity evidence.
- **No checklist promotion is performed by this phase.** `docs/parity/checklist.md` has no diff. STR-09/CFG-07 stay below `verified`; promotion would require Rust-firmware accepted/rejected hardware share proof per the Phase 28 validator (`tools/parity/src/main.rs:1194-1240`, `--fail-on-invalid-verified`). Not eligible yet — R8 was not met.
- **No hashrate or mining-parity claim for Rust firmware.** Blocked (UART proof) tier position improves to: post-dispatch register-read RX proven (probe), but post-dispatch WORK-RESULT RX remains NOT MET.
- Category labels only in this document; no raw pool URLs/ports/users/workers/passwords, Wi-Fi values, IPs, MACs, device URLs, share payload fields, or NVS secret values are recorded.

## Next-step recommendation

Per the approved plan for the `firmware-nonce-production` outcome: the documented follow-up is an upstream wire-byte diff — build upstream from the pin with `BM1366_SERIALTX_DEBUG` (and RX debug) enabled out-of-tree, capture the exact init + job byte streams that DO produce nonces, and diff against the Rust TX from the J3 capture. Observed upstream facts worth prioritizing in that diff (recorded as observations, not conclusions): upstream negotiated pool version-rolling (`mining.configure`) and its results carry nonzero `version_bits` (hardware version rolling active), while the Rust client does not negotiate a version mask today (explicitly deferred scope in 28.1-CONTEXT.md). If three or more fix attempts fail after the wire diff, stop and re-question the architecture per the approved plan.

***

*Phase: 28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi*
*Plan: 28.1-05 Task 3*
