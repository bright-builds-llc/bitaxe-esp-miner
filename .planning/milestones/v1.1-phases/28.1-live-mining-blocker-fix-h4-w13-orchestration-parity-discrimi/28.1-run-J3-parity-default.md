---
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 28.1-2026-07-07T00-34-17
generated_at: 2026-07-07T03:05:00Z
---

# Phase 28.1 Run J3 — Parity-Default Hardware Evidence Ladder

Date: 2026-07-07 (UTC)
Board: 205 (Bitaxe Ultra 205, BM1366)
Port: `/dev/cu.usbmodem1101` (detector-gated)
Source commit: `04ef41679e2f69252a259672797faadff7a83657`
Reference commit: `c1915b0a63bfabebdb95a515cedfee05146c1d50` (`just verify-reference` PASS before hardware work)
Package: parity-default (`bash scripts/phase27-live-hardware-bridge-package.sh`, NO `--investigation`)
Package manifest: `bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json` (`source_commit` matches HEAD `04ef416...`)
pool_config: local-owner-supplied (category label only; raw pool values never read, printed, or committed)
wifi_config: local-owner-supplied (category label only)

## Detector gate

`just detect-ultra205` succeeded before any flash work: exactly one likely ESP USB serial port found and `espflash board-info --chip esp32s3 --port /dev/cu.usbmodem1101 --non-interactive` succeeded. Redacted detector observation:

```text
Chip type:         esp32s3 (revision v0.2)
Crystal frequency: 40 MHz
Flash size:        16MB
Features:          WiFi, BLE, Embedded Flash
MAC address:       [redacted-mac]
port=/dev/cu.usbmodem1101
```

## Commands

1. `just verify-reference` — PASS (`reference clean: c1915b0a63bfabebdb95a515cedfee05146c1d50`)
2. `just detect-ultra205` — PASS (above)
3. `bash scripts/phase27-live-hardware-bridge-package.sh` — build green; manifest + enablement ledger written
4. Foreground capture (per the J2c procedure; the background evidence-wrapper capture is known-broken per RESEARCH Pitfall 5):
   `just flash-monitor board=205 port=/dev/cu.usbmodem1101 image=bazel-bin/firmware/bitaxe/bitaxe-ultra205-factory.bin evidence-dir=<local-gitignored-dir> capture-timeout-seconds=360 wifi-credentials=wifi-credentials.json`
   with a local pool-input-bridge watcher applying `scripts/phase21-pool-input-bridge.sh --device-url <derived-from-this-session-monitor-output> --pool-credentials pool-credentials.json --out-dir <local-gitignored-dir>` once exactly one origin-only device URL candidate appeared in the fresh monitor log (`axeos_settings_patch=accepted writes=4` observed at device-ms 16913; bridge applied once).
5. Capture ran the full 360 s (`capture_status=timed_out_after_trusted_output`, `trusted_output=true`); agent wall clock >= 500 s. Observed on-device identity: `observed_firmware_commit=04ef41679e2f`, `observed_reference_commit=c1915b0a...`.

## Success ladder

All timestamps are device milliseconds from the capture log. Every quoted line is verbatim except redaction placeholders.

| Rung | Disposition | Evidence |
| --- | --- | --- |
| R1 boot retention (W13) | **MET** | `I (10043) ... asic_work_result_trace=post_init_retention bootstrap=initialized_no_mining` (marker prefix renamed per 28.1-flag-disposition-note.md; `bootstrap=initialized_no_mining` unchanged) |
| R2 bridge active | **MET** | `I (17523) ... h4_continuous_result=listener_armed` |
| R3 dispatch cadence (J2c falsification) | **MET** | 22 `asic_production_status=work_dispatched` markers at 19313, 21363, 23433, ... 58803, 59603, 61713 ms. Consecutive spacings: 2050-2110 ms (within [1 s, 4 s]) for 20 of 21 gaps; the single 800 ms gap (58803 -> 59603) is a fresh-pool-work dispatch, not regeneration. 20 `asic_bridge=job_redispatched extranonce2_counter=<n>` markers, counter 1..19 monotonic then reset to 1 after fresh pool work — e.g. `I (21333) ... asic_bridge=job_redispatched extranonce2_counter=1` |
| R4 register-read probe | **MET (with parse caveat)** | Post-init: `I (10083) ... asic_probe=register_read_tx stage=post_init` then a response WAS received — `I (10103) ... asic_uart_trace=rx_chunk hex=aa 55 13 66 00 00 00 00 00 00 05` classified `I (10123) ... asic_probe=register_read_unparsed stage=post_init` (the received-but-unparsed third outcome from 28.1-03; neither `register_read_parsed` nor `register_read_silent` appeared). Post-dispatch: `I (19343) ... asic_probe=register_read_tx stage=post_dispatch` then `I (19483) ... asic_uart_trace=rx_chunk hex=aa 55 13 66 00 00 00 00 00 00 05` classified `I (19503) ... h4_continuous_result=read_error_continue reason=production_result_malformed`. The response bytes are the fixed BM1366 chip-identity register frame (identity constant `13 66`; no pool-derived material) — identical to the chip-detect response at 1983 ms. **Chip is ALIVE and the RX path works at the post-init baud, both post-init and post-dispatch.** |
| R5 power delta | **MET (marker present; diagnostic value: NOT hashing)** | `I (19283) ... asic_probe=power_baseline mw=1400` and `I (26453) ... asic_probe=power_delta baseline_mw=1400 after_mw=1010 delta_mw=-390`. A BM1366 hashing at 485 MHz would show a ~10 W-class jump; a flat/negative delta indicates cores are not hashing. |
| R6 voltage parity | **MET** | `I (1133) ... safety_voltage_effect=write setpoint_v=1.200 register=0xc6` |
| R7 RX evidence | **NOT MET** | Zero `asic_production_status=result_correlated` in 360 s. The 3 `asic_uart_trace=rx_chunk` lines (1983, 10103, 19483 ms) each correlate 1:1 with a preceding ReadChipId TX (chip detect at 1953, post-init probe at 10063, post-dispatch probe at 19323). Zero unsolicited RX and zero nonce/work-result frames across 22 dispatches and 189 `result_read_attempt` polls (188 `asic_bridge=timeout_streak` markers). R7 as intended — post-dispatch WORK-RESULT UART evidence — is not met. |
| R8 share outcome | **NOT MET** | Zero `share_submission_status=` lines. No accepted/rejected share claim is made. |
| Negative check | **MET** | Zero `production_result_timeout` occurrences — disappears by design in the parity-default package (see 28.1-flag-disposition-note.md; replaced by `asic_bridge=timeout_streak count=<n>`, observed up to count=188). |

## J2c confound falsified

**R3 is met: the J2c confound is FALSIFIED.** Under parity-default orchestration the bridge dispatched real pool work 22 times at the upstream ~2 s cadence with fresh extranonce2 per regeneration (counter 1..19, reset on fresh pool work), while the continuous listener stayed armed and polling. Dispatch is no longer starved by the armed listener. This milestone stands independently of R7/R8.

## Observed behavior (redacted)

- Boot -> safety bring-up complete at 1893 ms (`phase27_safety_bring_up=complete`, `phase27_safety_bring_up=power_probe_bus_retained`), chip detected (`asic_status=chip_detected chips=1`), full mining-ready init, W13 default retention at 10043 ms, post-init probe response received at 10103 ms.
- Pool settings applied via the repo-owned phase21 pool-input-bridge helper at 16913 ms; live Stratum reached `subscribed` (18963), `authorized` (19023), `active` (19243).
- First dispatch 19313 ms; steady 2 s regeneration cadence with `job_redispatched` markers through 58773 ms; fresh pool work at 59553 ms (`active` logged again) followed by dispatch at 59603 and one regeneration at 61683.
- At 62783 ms the session moved to `reconnecting`, then `phase25_safe_stop_status=complete` and `phase25_live_stratum_status=stopped` (62813), with `axeos_settings_patch=effects_applied` (62823) as the final log line; the device stayed silent for the remaining ~5 minutes of the 360 s capture window. Safe-stop invariants held (`mining=disabled hardware_control=disabled work_submission=disabled`).
- No nonce, no `result_correlated`, no share activity at any point.

## Conclusion

The Phase 28.1 orchestration fixes WORK as designed: W13 default retention, dispatch-before-poll, 2 s regeneration with fresh extranonce2, timeout-as-telemetry, and the discriminating probes all behaved exactly per plan. The discrimination outcome is **chip-alive-but-no-nonces**: the BM1366 answers register reads on the retained UART both post-init and post-dispatch (RX path proven at the production baud), core voltage setpoint is written (1.200 V), but measured power FALLS after dispatch (1400 -> 1010 mW, delta -390 mW) and zero work-result frames appear across 22 dispatches in 360 s.

Ladder position: stalls between R6 and R7 (no work-result RX, no shares). Per the Plan 05 phase-gated procedure, this satisfies the upstream A/B trigger condition (>= 3 `work_dispatched` with the ladder stalled before R7, probes indicating chip-alive-but-no-nonces). The A/B disposition is recorded in `28.1-blocker-disposition.md`.

Non-claims: no accepted/rejected share claim; no checklist promotion; no hashrate claim. Raw pool URLs/ports/users/workers/passwords, Wi-Fi values, IPs, MACs, device URLs, and NVS secrets are not committed; the local capture directory is gitignored session state.

***

*Phase: 28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi*
*Plan: 28.1-05 Task 1*
