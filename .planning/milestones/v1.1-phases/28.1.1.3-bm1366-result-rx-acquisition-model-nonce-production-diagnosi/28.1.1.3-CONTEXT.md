---
generated_by: gsd-discuss-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1.3-2026-07-09T03-02-32
generated_at: 2026-07-09T03:02:32Z
---

# Phase 28.1.1.3: BM1366 Result RX Acquisition Model Nonce-Production Diagnosis - Context

**Gathered:** 2026-07-08
**Status:** Ready for planning
**Mode:** YOLO recommended decisions accepted

<domain>
## Phase Boundary

This phase isolates whether Rust short-poll `try_read_exact` plus partial-frame `clear_rx` discards BM1366 nonce bytes versus upstream long-block `SERIAL_rx` / `receive_work`. Phase 28.1.1.2 falsified `match_upstream_register_read_poll` (A/B regressed with panic; no default promotion). Job bytes remain matched from 28.1.1.1 and must not be reopened.

Out of scope: Phase 29/30 promotion; reintroducing `post_max_baud_delay_2000`; promoting register-read poll to default; speculative investigation-flag spam.

</domain>

<decisions>
## Implementation Decisions

### Evidence ladder
- **D-01:** Retain Phase 28.1.1.1 facts: job fields 11/11 match; below-job events 12/12; no Rust `result_correlated` / share submit under aligned source work.
- **D-02:** Retain Phase 28.1.1.2 facts: `match_upstream_register_read_poll` falsified (`ab_outcome: regressed`); mutex-sleep fix retained; investigation mode off-by-default; `next_hypothesis: result_rx_acquisition_model`.
- **D-03:** Ultra 205 USB recovered after operator reboot; hardware A/B is unblocked (`just detect-ultra205` passes).

### Primary hypothesis
- **D-04:** First investigation is `result_rx_acquisition_model`: Rust continuous path polls with short slices (`MAX_POLL_SLICE_MS=100`) via `try_read_exact(11, poll_timeout_ms)`; on partial reads it logs `partial_frame`, calls `clear_rx`, and maps the error to `Pending` — potentially discarding in-flight nonce bytes. Upstream blocks in `receive_work` → `SERIAL_rx(..., 10000)` without TX-driven result probes.
- **D-05:** Do not treat `result_read_attempt` as TX probes; it is an RX-poll log marker (28.1.1.2 research).

### Comparator / metrics (first deliverable)
- **D-06:** Extend or compose result-path tooling to make `partial_frame`, `clear_rx`, `rx_idle`, `rx_complete`, `result_correlated`, and `fake_pool_submit_observed` first-class redaction-safe counts before any firmware default change.
- **D-07:** Prefer composing `scripts/phase28.1.1.2-result-path-compare.mjs` / diagnostic wrappers and 28.1.1.1 source-work fake-pool capture over a parallel stack.
- **D-08:** Committed reports: counts/booleans/category labels only; no raw UART hex, credentials, or device endpoints.

### A/B and patch policy
- **D-09:** Run exactly one detector-gated ≥360s fake-pool A/B aimed at RX acquisition (e.g. longer poll slice, retain partial buffer without `clear_rx`, or upstream-like long-block receive) — choose the single highest-signal lever from research, investigation-only first.
- **D-10:** Patch default firmware only if A/B shows `ab_outcome: improved` with `result_correlated` and/or `fake_pool_submit_observed: true`. One confirmed divergence at a time; focused unit/fixture test for pure-crate changes.
- **D-11:** No new investigation flags unless evidence names a concrete behavior difference requiring one.
- **D-12:** Do not reopen job-byte construction; do not reintroduce `post_max_baud_delay_2000`; do not default-on register-read poll.

### Hardware and verification
- **D-13:** Every hardware run: `just detect-ultra205`, `just verify-reference`, board `205`, ≥360s capture, wall clock ≥420s.
- **D-14:** Prefer deterministic source-work fake-pool alignment for submit comparability without live-pool secrets.
- **D-15:** `passed` only with `result_correlated` plus share-submission marker; else `gaps_found` / `blocked_safe_prerequisite`. Phase 30 checklist promotion deferred.

### the agent's Discretion
- Exact A/B lever among longer poll / no-clear-on-partial / long-block receive is an implementation choice after counting `partial_frame` in ignored logs.
- Keep plan count small: metrics → one A/B → optional patch → final evidence.

</decisions>

<specifics>
## Specific Ideas

- Count `asic_uart_trace=partial_frame` and `clear_rx` from ignored hardware logs first.
- One targeted RX-acquisition A/B before any default change.
- Keep raw logs under ignored `scratch/` or `.planning/phases/**/hardware-runs/`.

</specifics>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase truth
- `.planning/ROADMAP.md` — Phase 28.1.1.3 scope
- `.planning/STATE.md` — handoff from 28.1.1.2
- `.planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-final-evidence-redacted.md`
- `.planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-patch-disposition-redacted.md`
- `.planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-RESEARCH.md` — Pitfall 4 partial_frame/clear_rx
- `.planning/phases/28.1.1.1-bm1366-upstream-golden-comparator-and-nonce-production-gap-r/28.1.1.1-final-evidence-redacted.md`

### Tooling
- `scripts/phase28.1.1.2-result-path-compare.mjs`
- `scripts/phase28.1.1.2-result-path-diagnostic.sh`
- `scripts/phase28.1.1.1-source-work-aligned-capture.sh`
- `scripts/phase28.1.1-wire-parity-capture.sh`
- `scripts/phase27-live-hardware-bridge-package.sh`

### Code
- `firmware/bitaxe/src/asic_adapter/uart.rs` — `try_read_exact`, `partial_frame`, `clear_rx`
- `firmware/bitaxe/src/asic_adapter/production.rs` — `result_read_attempt` / Pending mapping
- `firmware/bitaxe/src/live_stratum_runtime.rs` — continuous poll / `MAX_POLL_SLICE_MS`
- `reference/esp-miner/components/asic/asic_common.c` — `receive_work` / `SERIAL_rx`
- `reference/esp-miner/main/tasks/asic_result_task.c`

### Policy
- `AGENTS.md` — detector gate, redaction, timeouts
- `docs/adr/0012-parity-verification-evidence.md`
- `docs/parity/checklist.md` — STR-09, CFG-07, ASIC-11 (no verified promotion here)

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- Result-path comparator + diagnostic from 28.1.1.2
- Source-work fake-pool capture from 28.1.1.1
- UART trace markers already emit `partial_frame`, `clear_rx`, `rx_idle`, `rx_complete`

### Patterns to Follow
- Investigation-only A/B first; default promotion only on improved correlate/submit
- Fail closed to `gaps_found`; never overclaim Phase 30

</code_context>

<deferred>
## Deferred Ideas

- Phase 29 evidence automation
- Phase 30 STR-09/CFG-07 verified promotion
- ASIC enable/power/reset sequencing (D-07 item 2 from 28.1.1.2) — only after RX acquisition falsified

</deferred>
