---
phase: 28.1.1-bm1366-nonce-production-wire-parity
plan: 13
type: execute
gap_closure: true
wave: 12
depends_on: [28.1.1-12]
files_modified:
  - scripts/phase28.1.1-accepted-state-diagnostic.sh
  - scripts/phase28.1.1-accepted-state-diagnostic-test.sh
  - .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan13-attempt-redacted.md
  - .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VALIDATION.md
  - .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-UAT.md
  - .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-REVIEW.md
  - .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md
  - .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-13-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [STR-09, ASIC-11, CFG-07]
user_setup:
  - "Keep the currently connected Ultra 205 on barrel/DC power and USB until the fresh Plan 13 checkpoint requests an action."
  - "At each recovery or lifecycle checkpoint, perform only the named one-use action and return its exact Plan 13 response token before the persisted monotonic deadline."
must_haves:
  truths:
    - "Plan 13 begins only from the clean committed Plan 12 completion HEAD, integrates the real adapter, reruns all gates, and locks a new clean hardware_exact_head before packaging"
    - "One fresh boot-session-bound state chain, located only by its opaque resume handle, owns detector, private credential binding/consumption, reference, package/reinit, lease-owned physical lifecycle, post-capture validation, terminal classification, and atomic active-to-tombstone cleanup"
    - "The currently connected board receives at most five detector attempts through the exact finite recovery order; no Plan 11 state or evidence is reused"
    - "Only lifecycle_status=match may reach gaps_found_same_chain_production_markers_absent or passed_same_chain_hardware; invalid lifecycle reaches blocked_safe_evidence_invalid with verification_result=gaps_found"
    - "Closure remains category-only, denylist-clean, checklist/debug/older-evidence/Phase-30 immutable, and requirements stay pending for Phase 30"
  artifacts:
    - path: scripts/phase28.1.1-accepted-state-diagnostic.sh
      provides: "Real plan13-prevalidated adapter for the Plan 12 runner"
      contains: "plan13-prevalidated"
    - path: .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan13-attempt-redacted.md
      provides: "Category-only exact-head lifecycle and production outcome"
      contains: "terminal_outcome"
    - path: .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md
      provides: "Truthful terminal closure and exact non-claims"
      contains: "verification_result"
  key_links:
    - from: scripts/phase28.1.1-accepted-state-diagnostic.sh
      to: scripts/phase28.1.1-exact-head-hardware-attempt.sh
      via: "real effects are invoked only from validated plan13-prevalidated dispatch"
      pattern: "plan13-prevalidated|run-validated-effect"
    - from: .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan13-attempt-redacted.md
      to: scripts/phase28.1.1-strict-production-evidence.mjs
      via: "closed same-chain projection"
      pattern: "lifecycle_status|result_correlated|power_delta_class|share_submission_status"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1-2026-07-09T19-24-27
generated_at: 2026-07-11T04:05:00Z
---

<objective>
Integrate the deterministic Plan 12 runner with the repo-owned hardware path, then execute one fresh exact-head Ultra 205 lifecycle/production chain and close the phase conservatively.

Purpose: separate physical checkpoints and closure judgment from tooling construction while preserving the currently connected fresh entry, finite recovery, credential secrecy, exact-head identity, retained-package lifecycle, and Phase 30 ownership.

Output: one clean hardware exact head, one finite terminal outcome, category-only evidence, synchronized validation/UAT/review/verification/state/roadmap artifacts, and a verified commit/push only after all gates pass.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/index.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/verification.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-CONTEXT.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-RESEARCH.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VALIDATION.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-UAT.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-REVIEW.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-11-SUMMARY.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-12-PLAN.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-12-SUMMARY.md
@.planning/debug/phase28.1.1-cold-start-capture-gap.md
@scripts/phase28.1.1-hardware-attempt-state.mjs
@scripts/phase28.1.1-exact-head-hardware-attempt.sh
@scripts/phase28.1.1-strict-production-evidence.mjs
@scripts/phase28.1.1-roadmap-phase30-immutability.mjs
@scripts/phase28.1.1-accepted-state-diagnostic.sh
@scripts/phase28.1.1-accepted-state-diagnostic-test.sh
@scripts/process-group.sh
@scripts/detect-ultra205.sh

<hardware_contract>
Use the exported `exact-head-attempt-v2` schema/effect/transition authority; do not reproduce or relax it. Entry begins from the currently connected board with fresh Plan 13 state, zero counters, credential/reference/package/capture fields at `not_run`, and no Plan 11 input. The entry checkpoint token is `plan13-connected-entry-v1`, expected response `ultra205-remains-connected`, deadline creation plus 600000 ms.

Attempt creation freshly derives `boot_session_sha256` exactly as Plan 12 specifies: Linux `/proc/sys/kernel/random/boot_id` or macOS `/usr/sbin/sysctl -n kern.boottime`, normalized and locally SHA-256 hashed with its platform tag, with only the digest persisted. The runner re-observes it before every effect, continuation before/after input, lease CAS/socket delivery, and classification. Unavailable/malformed observation or mismatch blocks before hardware, socket, PTY, or classifier progress; no deadline from the old boot session is interpreted under a new monotonic clock.

`detector_board_info` runs `just detect-ultra205` and succeeds only for exactly one board-205 ESP32-S3 port. On failure, permit exactly four one-use 300000 ms checkpoints in order: USB replug with barrel retained (`plan13-usb-replugged-v1`); both paths removed at least five seconds then barrel followed by USB (`plan13-both-power-restored-v1`); one RESET press (`plan13-reset-pressed-v1`); one BOOT/RESET board-info assist (`plan13-boot-reset-assisted-v1`). Maximum detector attempts are five. Repeat, reorder, expiry, dirty/wrong HEAD, boot-session change/unavailability, or fifth failure stops `blocked_safe_attempt_prerequisite`. Never erase, factory reset, raw-write, stress/fault-test, scan, infer a stale target, or improvise flash.

After detection, bind credentials without opening them, verify the pinned reference, package the exact clean hardware HEAD, and invoke the sole credential consumer effect for flash plus a strict five-stage reinit capture of at least 360000 ms with shell allowance at least 420 seconds. Require exact reference/manifest/factory-image/log/classifier digests and destroy the private credential capability after consumption.

`lifecycle_start` owns the single long-lived lease. It persists the armed prohibited-sentinel counts/digest before returning removal token `plan13-both-power-paths-removed`. The lease child alone accepts the translated PTY input, observes at least 5000 ms continuous USB absence, returns restore token `plan13-barrel-then-usb-restored`, accepts reappearance at elapsed `<=60000 ms`, and runs one retained-package `--no-reset` capture for at least 360000 ms. It records attestation status, absence/reappearance measurements, capture timestamps/category, both raw-log digests, classifier input/output digests, PID-start/lease identity, and capture-complete sentinel counts/digest in state. The global runner lock is used only for bounded CAS transitions, never held for the capture. Persist `capture_complete` before `post_capture_detector_board_info`.
</hardware_contract>

<checkpoint_contract>
Every user handoff is a `## CHECKPOINT REACHED` block copied from the current closed state. It contains exactly these public fields: `resume_handle`, `checkpoint_id`, `checkpoint_token`, `checkpoint_generation`, `exact_head`, `attempt_state`, `lifecycle_substate`, `created_monotonic_ms`, `monotonic_deadline_ms`, `expected_response_token`, `expected_user_action`, `detector_attempt_count`, `usb_replug_count`, `both_power_count`, `reset_count`, `boot_reset_count`, `reinit_five_stage_result`, `capture_category`, and `process_running`. `resume_handle` is the random 64-hex public locator; it is not a path or secret-bearing state. The block never contains attempt paths, port paths, credential paths/content, opaque credential IDs/digests, lifecycle lease/capability IDs/digests, PIDs/start fingerprints, boot-session digest/raw observation, effect nonce, raw-log paths/content, endpoints, device/network identities, or secrets.

The seven exact checkpoints are:

| checkpoint_id | checkpoint_token | expected_response_token | expected_user_action | deadline |
| --- | --- | --- | --- | --- |
| `plan13-connected-entry` | `plan13-connected-entry-v1` | `ultra205-remains-connected` | `keep-connected` | creation + 600000 ms |
| `plan13-recovery-usb-replug` | `plan13-usb-replug-recovery-v1` | `plan13-usb-replugged-v1` | `replug-usb-with-barrel-retained` | creation + 300000 ms |
| `plan13-recovery-both-power` | `plan13-both-power-recovery-v1` | `plan13-both-power-restored-v1` | `remove-both-wait-five-seconds-restore-barrel-then-usb` | creation + 300000 ms |
| `plan13-recovery-reset` | `plan13-reset-recovery-v1` | `plan13-reset-pressed-v1` | `press-reset-once` | creation + 300000 ms |
| `plan13-recovery-boot-reset` | `plan13-boot-reset-recovery-v1` | `plan13-boot-reset-assisted-v1` | `perform-boot-reset-board-info-assist-once` | creation + 300000 ms |
| `plan13-lifecycle-removal` | `plan13-armed-removal-v1` | `plan13-both-power-paths-removed` | `remove-both-power-paths-and-attest` | creation + 300000 ms |
| `plan13-lifecycle-restore` | `plan13-barrel-usb-restore-v1` | `plan13-barrel-then-usb-restored` | `restore-barrel-then-usb` | attestation acceptance + 60000 ms |

Fresh continuations use only these repo-owned commands; no continuation receives an attempt path:

- `bash scripts/phase28.1.1-exact-head-hardware-attempt.sh resolve-checkpoint --resume-handle <resume_handle>` resolves exactly one mode-0600 private index record and re-emits the public block after state/head/boot validation.
- `bash scripts/phase28.1.1-exact-head-hardware-attempt.sh deliver-token --resume-handle <resume_handle> --checkpoint-token <checkpoint_token> --response-token <expected_response_token>` validates and consumes that exact checkpoint, then resumes the closed runner.

Before accepting any response, `deliver-token` validates unique handle resolution, exact checkpoint ID/token/generation/response, clean `hardware_exact_head`, freshly equal boot-session digest, unexpired deadline both before and after input parsing, expected attempt/lifecycle substate, manifest/reference identities when present, private capability mode/owner/digest when still sealed, and process PID-start/lease/liveness when `process_running=true`. It compare-and-swaps the checkpoint to consumed before action. For the two lifecycle rows, only after those validations may it open the private mode-0600 lease-bound Unix socket; the child independently validates handle digest, generation, response, effect nonce, lease/owner, fresh boot session, head, substate, and deadline before one PTY write. Missing, malformed, wrong, stale, ambiguous, repeated, reordered, expired, dirty, wrong-head, boot mismatch/unavailable, wrong-state, wrong-owner, missing socket, or dead/reused-process input is never inferred or repaired; it uses the closed Plan 12 blocker mapping and one cleanup path.

Every normal, blocked, crash, or abandoned terminal removes every live attempt/socket/capability/process/lock reference and atomically replaces/removes the live active mapping with a mode-0600 tombstone in the mode-0700 private index root. The tombstone has exactly `schema_version`, retained `resume_handle_sha256` (never the clear handle and never cleared), `attempt_generation`, `terminal_status`, `terminal_category`, and `cleanup_time_category`; it has no attempt path, socket, PID/start fingerprint, capability/lease/effect identity, process reference, credential, secret, timestamp, or extra field. Resolution order is exact: validate missing/canonical syntax; one valid active mapping resolves; otherwise a digest-matching tombstone returns `resume_handle_stale`; no active mapping and no tombstone returns `resume_handle_wrong`; any collision, duplicate, mismatched digest, or ambiguous match blocks with `resume_handle_ambiguous`. Tombstones are retained through Plan 13 final verification. Bounded pruning is permitted only after final verification and closure-commit synchronization and must preserve the current run's tombstone/audit result. Only the lease owner observes USB or runs the retained monitor; continuations merely validate and deliver the two exact PTY tokens.
</checkpoint_contract>

<head_contract>
Before adapter edits, fetch and require a clean completed Plan 12. Require `28.1.1-12-SUMMARY.md` to name the 40-hex `tooling_implementation_head` and passed gates, require the current HEAD history to contain that task commit followed only by the normal Plan 12 metadata commit, then capture current `git rev-parse HEAD` as `plan12_completion_head`. Accept sync state `0 0`, or local-ahead-only `<N> 0` only when every ahead commit belongs to completed Plan 12; stop on behind count, both-side divergence, dirty state, missing summary/task commit/metadata commit, or conflict. After the adapter commit and full gates, record the new clean `hardware_exact_head`. Every detector/reference/package/reinit/lifecycle/classifier identity must equal that hardware head. Later evidence/closure commits are documentation identities and must not be presented as the packaged firmware head.
</head_contract>

<credential_contract>
The agent and presence validator never open credential contents. Raw canonical paths exist only in the ignored mode-0600 capability; state retains non-promoted random binding IDs/digest and categories. Only `flash_reinit_runtime`, rooted at `scripts/phase27-live-hardware-bridge-evidence.sh`, may consume the capability: `tools/flash` reads Wi-Fi for NVS and `scripts/phase21-pool-input-bridge.sh` plus its helper reads pool JSON for runtime configuration. Paths, opaque IDs, private hashes, and contents are never traced or promoted. No lifecycle/post-capture action may access credentials.
</credential_contract>

<terminal_contract>
Use exactly the five Plan 12 terminals and only the Plan 12 closed `blocker_reason` enum/mapping; Plan 13 prose and adapters may not create an additional string. Every terminal except passed records `verification_result=gaps_found`; passed records `verification_result=passed` and `phase30_promotion_input=eligible`. Invalid lifecycle evidence maps to `blocked_safe_evidence_invalid`; only exact lifecycle `match` with consistent identities/digests/sentinels may map to production-markers-absent or passed. Passed additionally requires `result_correlated=true`, `power_delta_class=rising_hashing`, and exactly one `accepted|rejected`. Requirements and checklist remain pending for Phase 30 in every route.
</terminal_contract>

<standards_alignment>
Repo-local guidance governs detector-first connected hardware, credential boundaries, 360/420-second budgets, finite recovery, cleanup, redaction, evidence promotion, and frontmatter. Bright Builds verification/Rust rules require sync and the exact mandatory gates before both commits. The debug artifact and prior evidence remain immutable.
</standards_alignment>

<scope_contract>
The ten `files_modified` entries are the warning threshold because Task 1 owns the existing two-file adapter/test seam, Task 2 owns one new redacted attempt artifact, and Task 3 must synchronize the eight already-established lifecycle closure artifacts as one atomic consistency set. Task 3 does not add implementation logic: it updates validation, UAT, review, verification, summary, state, and roadmap from the same closed projection and checks them together. Splitting those closure files across commits would create transient contradictory phase status; no unrelated checklist, debug, prior-evidence, requirement, or Phase 30 file is writable.
</scope_contract>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Integrate the real prevalidated adapter and lock hardware exact HEAD</name>
  <files>scripts/phase28.1.1-accepted-state-diagnostic.sh, scripts/phase28.1.1-accepted-state-diagnostic-test.sh</files>
  <read_first>.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-12-SUMMARY.md, scripts/phase28.1.1-hardware-attempt-state.mjs, scripts/phase28.1.1-exact-head-hardware-attempt.sh, scripts/phase28.1.1-exact-head-hardware-attempt-test.sh, scripts/phase28.1.1-accepted-state-diagnostic.sh, scripts/phase28.1.1-accepted-state-diagnostic-test.sh, scripts/phase27-live-hardware-bridge-evidence.sh, scripts/phase21-pool-input-bridge.sh, tools/flash/src/main.rs, scripts/process-group.sh</read_first>
  <action>Apply `<head_contract>` before editing. Before the first Plan 13 edit, run `node scripts/phase28.1.1-roadmap-phase30-immutability.mjs check-zero-frontmatter --files .planning/ROADMAP.md` and record the passing pre-Plan-13 assertion; ROADMAP is intentionally frontmatterless, and this command must neither require nor introduce YAML frontmatter. Then run `node scripts/phase28.1.1-roadmap-phase30-immutability.mjs snapshot-protected --set phase28.1.1-prior-evidence-v1 --output hardware-runs/phase28.1.1/attempt-control/plan13-prior-evidence-v1.json`; require the ignored parent mode 0700, manifest mode 0600, and retain it unchanged through Task 3. Add only `--mode plan13-prevalidated`: it accepts runner-owned fixed effect dispatch/capability inputs, bypasses legacy duplicate detector/credential/reference/package preflight, never accepts a caller command, and exposes the existing repo-owned package/flash/reinit/lifecycle primitives only to the validated runner. Preserve legacy `blocked|hardware` behavior. Ensure the sole flash_reinit_runtime path consumes credentials exactly as `<credential_contract>` specifies; the lifecycle path accepts no credential arguments.

Extend deterministic tests to prove direct/unvalidated entry is rejected before sentinels; prevalidated effect calls do not duplicate preflight; private paths/IDs/digests never appear in stdout, stderr, trace, or promoted fixtures; fresh Linux/macOS boot-session fixtures are enforced at the adapter boundary; lifecycle_start owns all USB/PTY/monitor observations; forbidden armed actions remain zero; resolve/deliver cannot bypass the private index/socket validation; and existing legacy tests remain green. Run focused state/effect/classifier/diagnostic/process/Phase30 tests, shellcheck/shfmt, and exactly `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Commit, require clean state, and record the 40-hex HEAD as hardware_exact_head. Do not push yet and do not touch hardware in this task.</action>
  <verify><automated>bash scripts/phase28.1.1-exact-head-hardware-attempt-test.sh &amp;&amp; node scripts/phase28.1.1-hardware-attempt-state-test.mjs &amp;&amp; node scripts/phase28.1.1-strict-production-evidence-test.mjs &amp;&amp; bash scripts/phase28.1.1-accepted-state-diagnostic-test.sh &amp;&amp; bash scripts/phase13-monitor-capture-test.sh &amp;&amp; node scripts/phase28.1.1-roadmap-phase30-immutability-test.mjs &amp;&amp; shellcheck scripts/phase28.1.1-accepted-state-diagnostic.sh scripts/phase28.1.1-accepted-state-diagnostic-test.sh &amp;&amp; shfmt -d scripts/phase28.1.1-accepted-state-diagnostic.sh scripts/phase28.1.1-accepted-state-diagnostic-test.sh &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features</automated></verify>
  <acceptance_criteria>Pre-edit state is clean and behind=0 under the exact allowed head rule; the Plan 12 summary's tooling_implementation_head exists in current history and current clean HEAD is captured as plan12_completion_head; ROADMAP has exactly zero standalone frontmatter delimiters before Plan 13 edits; the explicit prior-evidence baseline is captured before edits with 0700/0600 modes; plan13-prevalidated is the only real adapter entry and performs no duplicate preflight; only flash_reinit_runtime can consume private credentials; lifecycle has no credential surface; boot and resume/IPC boundaries stay fail-closed; all focused and legacy tests pass; mandatory Rust gates run in order before commit; the post-commit worktree is clean at recorded hardware_exact_head; no hardware or push occurred.</acceptance_criteria>
  <done>The real adapter is committed and one immutable hardware software identity is ready for the checkpoint.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Execute one fresh connected exact-head hardware and lifecycle chain</name>
  <files>.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan13-attempt-redacted.md</files>
  <read_first>AGENTS.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-12-SUMMARY.md, scripts/phase28.1.1-hardware-attempt-state.mjs, scripts/phase28.1.1-exact-head-hardware-attempt.sh, scripts/phase28.1.1-strict-production-evidence.mjs, scripts/phase28.1.1-accepted-state-diagnostic.sh, scripts/process-group.sh, scripts/detect-ultra205.sh</read_first>
  <action>Rerun Task 1 focused suites and mandatory Rust gates; require clean HEAD equal hardware_exact_head. Run `bash scripts/phase28.1.1-exact-head-hardware-attempt.sh begin-attempt --hardware-exact-head "$hardware_exact_head"`; it creates the ignored 0700 attempt directory, 0600 state/index, freshly observed boot-session digest, and random opaque resume handle without hardware, then returns the exact `plan13-connected-entry` block from `<checkpoint_contract>` and waits for `ultra205-remains-connected`. Every fresh continuation first runs the exact `resolve-checkpoint` command and resumes only with the exact `deliver-token` command from `<checkpoint_contract>`; enforce that contract before any action, then execute `<hardware_contract>` only through the runner. Use the exact four one-use detector recovery checkpoint rows if necessary; bind credentials privately; verify reference; package, flash, and capture strict reinit; then arm the lifecycle lease and return the exact removal/restore rows at their respective subordinate states.

At removal/restore checkpoints accept only `plan13-both-power-paths-removed` and `plan13-barrel-then-usb-restored` before their deadlines. The lease child performs the USB observations and retained monitor; continuations validate then deliver through the private lease-bound socket and never open the PTY directly. On failure, timeout, cancellation, boot-session/head change, missing/malformed/wrong/stale/ambiguous handle, or abandoned checkpoint, TERM then bounded KILL the whole process group, verify death, remove every live socket/capability/lock/attempt/process reference, and atomically replace the active digest slot with the Plan 12 minimal tombstone using the matching normal/blocked/crash/abandoned cleanup category. If death is unproved choose blocked_safe_unresolved_process and promote no positive evidence; its cleanup still leaves only the tombstone, never a live process reference.

After capture_complete, run post-capture detector and the pure classifier over only the exact chain. Write the tracked artifact from the closed projection with categories, counts, booleans, safe durations/timestamps, public source/reference/manifest identities, and terminal ID. Do not promote credential/path/private-capability hashes, raw logs, endpoints, port path, or device/network identities. Run the tri-state denylist.</action>
  <verify><automated>node scripts/phase28.1.1-hardware-attempt-state-test.mjs &amp;&amp; bash scripts/phase28.1.1-exact-head-hardware-attempt-test.sh &amp;&amp; node scripts/phase28.1.1-strict-production-evidence-test.mjs &amp;&amp; bash scripts/phase28.1.1-accepted-state-diagnostic-test.sh &amp;&amp; bash scripts/phase13-monitor-capture-test.sh &amp;&amp; bash scripts/phase28.1.1-promoted-evidence-denylist.sh .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan13-attempt-redacted.md</automated></verify>
  <acceptance_criteria>Fresh Plan 13 state imports no Plan 11 attempt input and persists a freshly derived boot-session digest; every handoff contains the opaque resume handle plus exactly the other public fields/row values, excludes every named private field, resolves to exactly one private record, and is consumed only after head/boot/state/lease/deadline validation before and after parsing and before socket open; malformed/wrong/stale/missing/ambiguous handles never reach hardware or PTY; detector attempts are at most five in exact order; every hardware effect uses hardware_exact_head and the crash-closed runner; credentials are consumed once only; both captures meet budgets; all named sentinel counts/digests satisfy invariants; normal/blocked/crash/abandoned cleanup proves death or selects unresolved-process terminal, removes every live reference, and atomically leaves the minimal retained tombstone so the terminal handle is stale while a never-valid handle is wrong; one exact terminal is emitted and evidence is category-only/denylist-clean.</acceptance_criteria>
  <done>One finite same-head chain reaches a truthful terminal without stale reuse, secret exposure, unsafe retry, or orphan process.</done>
</task>

<task type="auto">
  <name>Task 3: Synchronize validation and close, commit, and push without Phase 30 overpromotion</name>
  <files>.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan13-attempt-redacted.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VALIDATION.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-UAT.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-REVIEW.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-13-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-13-PLAN.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan13-attempt-redacted.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VALIDATION.md, scripts/phase28.1.1-roadmap-phase30-immutability.mjs, scripts/phase28.1.1-strict-production-evidence.mjs, docs/parity/checklist.md, .planning/debug/phase28.1.1-cold-start-capture-gap.md, .planning/STATE.md, .planning/ROADMAP.md</read_first>
  <action>Before formal lifecycle validation, add Plan 12/13 rows to VALIDATION mapping STR-09, ASIC-11, CFG-07, every T-12/T-13 threat, exact automated commands, hardware observations, and manual physical actions. Mark only evidence actually obtained. Then update the named evidence, UAT, REVIEW, VERIFICATION, SUMMARY, STATE, and Phase 28.1.1 ROADMAP section so every artifact agrees on plan12_completion_head, hardware_exact_head, reference/manifest identities, recovery/detector counts, lifecycle timings/status, digests/categories, cleanup, result/power/share, blocker, terminal, and verification result.

Apply `<terminal_contract>` literally. Only passed_same_chain_hardware may mark Phase 28.1.1 passed and set phase30_promotion_input=eligible; STR-09/ASIC-11/CFG-07 and checklist rows remain pending Phase 30. Keep docs/parity/checklist.md, the debug artifact, the exact eleven-file protected prior-evidence set, and exact Phase 30 section/Requirements/Plans/Progress projection unchanged while updating only the intended Phase 28.1.1 ROADMAP section. Compare `hardware-runs/phase28.1.1/attempt-control/plan13-prior-evidence-v1.json` with `node scripts/phase28.1.1-roadmap-phase30-immutability.mjs compare-protected --set phase28.1.1-prior-evidence-v1 --baseline hardware-runs/phase28.1.1/attempt-control/plan13-prior-evidence-v1.json`; this baseline was captured before Task 1 edits and must not be regenerated. Run the denylist on every promoted/closure artifact. Run `node scripts/phase28.1.1-roadmap-phase30-immutability.mjs check-frontmatter --files .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan13-attempt-redacted.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VALIDATION.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-UAT.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-REVIEW.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-13-SUMMARY.md .planning/STATE.md` and require exactly two standalone delimiters at the top of each explicitly named frontmatter-bearing file. Then run `node scripts/phase28.1.1-roadmap-phase30-immutability.mjs check-zero-frontmatter --files .planning/ROADMAP.md` and require exactly zero standalone delimiters after the intended Phase 28.1.1 ROADMAP update; do not add ROADMAP YAML frontmatter.

Before closure commit run exactly `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; then all focused tests, `cargo fmt --all -- --check`, `just verify-reference`, denylist, both frontmatter-shape commands, protected-baseline comparison, other immutability helpers, `git diff --check`, and formal lifecycle validation. Commit only truthful closure, push main, then require clean worktree, upstream counts `0 0`, zero live Plan 13 processes, no live active mapping/socket/capability/process reference, the current run's retained tombstone/audit result, and `resume_handle_stale` for the terminal resume handle. Remove the ignored baseline manifest only after the successful protected comparison and push. Only after final verification and closure-commit synchronization may bounded pruning remove older unrelated tombstones; it must preserve the current run's audit result.</action>
  <verify><automated>cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; node scripts/phase28.1.1-hardware-attempt-state-test.mjs &amp;&amp; bash scripts/phase28.1.1-exact-head-hardware-attempt-test.sh &amp;&amp; node scripts/phase28.1.1-strict-production-evidence-test.mjs &amp;&amp; node scripts/phase28.1.1-roadmap-phase30-immutability-test.mjs &amp;&amp; bash scripts/phase28.1.1-accepted-state-diagnostic-test.sh &amp;&amp; bash scripts/phase13-monitor-capture-test.sh &amp;&amp; cargo fmt --all -- --check &amp;&amp; just verify-reference &amp;&amp; bash scripts/phase28.1.1-promoted-evidence-denylist.sh .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan13-attempt-redacted.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VALIDATION.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-UAT.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-REVIEW.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-13-SUMMARY.md .planning/STATE.md .planning/ROADMAP.md &amp;&amp; node scripts/phase28.1.1-roadmap-phase30-immutability.mjs check-frontmatter --files .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan13-attempt-redacted.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VALIDATION.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-UAT.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-REVIEW.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-13-SUMMARY.md .planning/STATE.md &amp;&amp; node scripts/phase28.1.1-roadmap-phase30-immutability.mjs check-zero-frontmatter --files .planning/ROADMAP.md &amp;&amp; node scripts/phase28.1.1-roadmap-phase30-immutability.mjs compare-protected --set phase28.1.1-prior-evidence-v1 --baseline hardware-runs/phase28.1.1/attempt-control/plan13-prior-evidence-v1.json &amp;&amp; git diff --exit-code HEAD -- docs/parity/checklist.md .planning/debug/phase28.1.1-cold-start-capture-gap.md .planning/REQUIREMENTS.md &amp;&amp; node scripts/phase28.1.1-roadmap-phase30-immutability.mjs --baseline-ref HEAD --roadmap .planning/ROADMAP.md --phase 30 &amp;&amp; node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 28.1.1 --require-plans --require-verification --raw &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>VALIDATION contains Plan 12/13 requirement/threat/command/hardware/manual mappings before lifecycle validation; closure artifacts agree on one exact terminal and all required identities/boot/effect/measurements/digests; invalid lifecycle is blocked_safe_evidence_invalid with gaps_found while only lifecycle match reaches production-markers-absent or passed; checklist/debug/requirements/exact eleven-file prior-evidence set/exact Phase 30 projection are unchanged against their correct baselines while the intended Phase 28.1.1 ROADMAP update is preserved; requirements remain pending; denylist, the exact two-delimiter frontmatter command for only named frontmatter-bearing files, `check-zero-frontmatter --files .planning/ROADMAP.md` before and after Plan 13, protected comparison, diff, and lifecycle checks pass without requiring or introducing ROADMAP YAML frontmatter; mandatory Rust gates run in exact order before commit; after push worktree is clean, upstream is 0 0, no live Plan 13 process/active mapping/socket/capability remains, the current run's retained tombstone/audit result remains, and the terminal handle returns `resume_handle_stale`.</acceptance_criteria>
  <done>Phase 28.1.1 closes truthfully from one exact-head chain while Phase 30 retains sole promotion ownership.</done>
</task>
</tasks>

<threat_model>
## ASVS Level 1 Threat Model

| ID | ASVS / STRIDE | Threat | Severity | Mitigation | Blocking condition |
| --- | --- | --- | --- | --- | --- |
| T-13-01 | V1 / Spoofing | Hardware runs from stale/mixed source identity | critical | Plan12-to-adapter clean head gates and digest-bound chain | Any detector/package/capture/classifier identity differs |
| T-13-02 | V4 / Tampering | USB observations occur outside lifecycle lease | critical | lifecycle_start composite owns all subordinate observations | Any unleased process touches USB/PTY/monitor sentinels |
| T-13-03 | V8 / Information Disclosure | Credential/raw/private identity escapes | critical | private capability, sole consumer, denylist, category projection | Any secret/path/private digest is printed or promoted |
| T-13-04 | V14 / Denial of Service | Recovery is repeated, reordered, destructive, or unbounded | critical | four one-use actions and five detector attempts | Any sixth/reordered/destructive action occurs |
| T-13-05 | V14 / Denial of Service | Lifecycle child survives failure/checkpoint abandonment | critical | process-group TERM/KILL and verified death | Death unproved without unresolved-process terminal |
| T-13-06 | V5 / Repudiation | Invalid lifecycle becomes production evidence | high | exact terminal routing and digest/sentinel consistency | Invalid lifecycle reaches production-markers-absent or passed |
| T-13-07 | V5 / Repudiation | Structural markers or lifecycle alone promote requirements | high | four-field pass conjunction and Phase 30 immutability | Checklist/requirements promoted in Plan 13 |
| T-13-08 | V4 / Tampering | Closure rewrites debug/history/Phase 30 | high | exact immutability helpers and diff checks | Protected bytes change |
| T-13-09 | V1 / Spoofing | A continuation resolves no attempt or another attempt and reaches private PTY state | critical | Opaque handle, unique 0600 index, lease-bound Unix socket, double validation | Wrong/stale/missing/ambiguous handle opens socket or writes PTY |
| T-13-10 | V4 / Tampering | Host reboot resets monotonic time while retained state still appears valid | critical | Fresh platform boot-session digest at creation/effect/continuation/CAS/classification | Unavailable or changed boot observation does not block |

All high and critical threats are release-blocking.
</threat_model>

<verification>
Task 1 establishes a clean committed real adapter without hardware. Task 2 performs the only detector-gated physical chain with bounded checkpoints and 360/420-second budgets. Task 3 synchronizes validation before lifecycle verification, runs the mandatory Rust sequence before closure commit, protects all immutable surfaces, and pushes only after truthful closure passes.
</verification>

<success_criteria>
Plan 13 is complete when the adapter and hardware identities are clean, one fresh connected attempt reaches exactly one terminal, all lease/measurement/digest/sentinel invariants hold or block safely, evidence is category-only, closure artifacts agree, Phase 30 remains unpromoted, verification passes, and main is pushed clean at 0 0 with no live process.
</success_criteria>

<output>After execution, create `28.1.1-13-SUMMARY.md`; update only the named Plan 13 evidence and closure artifacts; keep checklist, debug, prior evidence, and exact Phase 30 surfaces immutable.</output>
