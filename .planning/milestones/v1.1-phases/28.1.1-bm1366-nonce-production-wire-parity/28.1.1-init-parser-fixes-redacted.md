# Phase 28.1.1 Init And Parser Verdict

init_parser_status: already_aligned
raw_bytes_committed: false
register_read_is_chip_alive_only: true
credential_contents_read: false

## Evidence Basis

The current redacted field-level diff records BM1366 init categories as aligned:
chip-detect prelude, static init frames, dynamic difficulty mask, dynamic PLL,
dynamic HCN, and post-init/post-dispatch register-read parsing all match the
known upstream anchors or committed fixture behavior.

The post-fix Rust run still shows no correlated nonce or share classification,
but its evidence includes sustained work dispatch and register-read parser proof.
That places the remaining blocker after chip-alive init/parser proof, not in a
specific evidence-backed init or parser divergence.

## Tests

tests:
- `cargo test -p bitaxe-asic bm1366 -- --nocapture` passed with 71 passed, 1 ignored.
- `cargo test -p bitaxe-asic result -- --nocapture` passed with 18 passed.
- `cargo test -p bitaxe-asic register -- --nocapture` passed with 8 passed.

## Exact Non-Claims

- Register-read parsing remains chip-alive proof only.
- Register-read parsing does not satisfy ASIC-11 result correlation.
- This artifact does not claim live nonce production.
- This artifact does not claim accepted or rejected live share classification.
