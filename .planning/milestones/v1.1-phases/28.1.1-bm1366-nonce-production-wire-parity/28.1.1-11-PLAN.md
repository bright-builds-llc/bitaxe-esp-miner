---
phase: 28.1.1-bm1366-nonce-production-wire-parity
plan: 11
type: execute
gap_closure: true
wave: 10
depends_on: [28.1.1-10]
files_modified:
  - crates/bitaxe-stratum/src/v1/live_runtime.rs
  - crates/bitaxe-api/src/logs.rs
  - crates/bitaxe-asic/src/bm1366/accepted_state.rs
  - scripts/phase28.1.1-accepted-state-compare.mjs
  - scripts/phase28.1.1-accepted-state-compare-test.mjs
  - scripts/phase28.1.1-promoted-evidence-denylist.patterns
  - scripts/phase28.1.1-promoted-evidence-denylist.sh
  - scripts/phase28.1.1-promoted-evidence-denylist-test.sh
  - scripts/phase28.1.1-accepted-state-diagnostic.sh
  - scripts/phase28.1.1-accepted-state-diagnostic-test.sh
  - .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-accepted-state-baseline-redacted.md
  - .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-accepted-state-lifecycle-redacted.md
  - .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-UAT.md
  - .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-REVIEW.md
  - .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md
  - .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-11-SUMMARY.md
  - .planning/debug/phase28.1.1-cold-start-capture-gap.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [STR-09, ASIC-11, CFG-07]
user_setup:
  - "Begin the hardware task with both barrel/DC power and USB unplugged; connect barrel power and then USB only when prompted."
  - "At the armed lifecycle checkpoint, remove both barrel/DC power and USB, attest that both paths are disconnected, wait for the measured five-second absence confirmation, then restore barrel power and USB only when prompted."
must_haves:
  truths:
    - "Empty, partial, or one-sided incomplete accepted-state inputs can never classify as match in either the Rust classifier or standalone JavaScript comparator"
    - "The diagnostic replay window is 180 seconds at a fixed 2-second cadence, while lifecycle reappearance at elapsed time less than or equal to 60000 ms is accepted, any later reappearance is rejected, and a documented 10-second monitor-start margin plus a real repeated-cadence regression test leaves another replay strictly before expiry"
    - "A lifecycle member records explicit operator attestation for both power paths plus at least 5000 ms of continuously measured USB absence, without claiming electronic proof of barrel removal"
    - "Promoted-evidence denial fails on a forbidden match and on regex/tool errors; only an explicit no-match result passes"
    - "Hardware arming is impossible until an exact-HEAD package produces a stable, strict five-stage reinit member and all detector/reference/package-identity gates pass"
    - "Phase 30, STR-09, ASIC-11, and CFG-07 remain pending unless their separate same-chain hardware proof exists; lifecycle structure alone never promotes them"
  artifacts:
    - path: crates/bitaxe-api/src/logs.rs
      provides: "Bounded replay timing contract and real-schedule host tests"
      contains: "ACCEPTED_STATE_REPLAY_WINDOW_MS"
    - path: crates/bitaxe-asic/src/bm1366/accepted_state.rs
      provides: "Fail-closed five-stage Rust accepted-state comparison"
      contains: "classify_accepted_state"
    - path: scripts/phase28.1.1-accepted-state-compare.mjs
      provides: "Fail-closed five-stage standalone comparison"
      contains: "compareAcceptedState"
    - path: scripts/phase28.1.1-promoted-evidence-denylist.sh
      provides: "Explicit tri-state promoted-evidence redaction gate"
      contains: "rg"
    - path: scripts/phase28.1.1-accepted-state-diagnostic.sh
      provides: "Attested, measured, retained-package cold-start workflow"
      contains: "both-power"
    - path: .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-accepted-state-lifecycle-redacted.md
      provides: "Category-only reinit/cold-start outcome with accurate measurement limits"
      contains: "usb_absence"
  key_links:
    - from: scripts/phase28.1.1-accepted-state-diagnostic.sh
      to: crates/bitaxe-api/src/logs.rs
      via: "60-second reattach deadline plus 10-second startup reserve inside the 180-second replay window"
      pattern: "reattach|REPLAY_WINDOW|MONITOR_START"
    - from: scripts/phase28.1.1-accepted-state-diagnostic.sh
      to: scripts/phase28.1.1-accepted-state-lifecycle-compare.mjs
      via: "strict self-comparison of the five-stage reinit prerequisite followed by reinit/cold-start comparison"
      pattern: "reinit-preflight|lifecycle"
    - from: scripts/phase28.1.1-promoted-evidence-denylist.sh
      to: .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md
      via: "explicit no-match-only promotion gate"
      pattern: "denylist|redaction"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1-2026-07-09T19-24-27
generated_at: 2026-07-10T04:14:30Z
---

<objective>
Close the evidence-correctness defects that blocked Plan 10, then make one bounded attempt to capture the physical Ultra 205 cold-start lifecycle without changing ASIC initialization or overstating the original phase goal.

Purpose: make incomplete accepted-state inputs, replay timing, total-power instructions, and evidence redaction fail closed before any renewed hardware run; restore a bounded recovery route for the currently unplugged board; capture only exact-HEAD, detector-gated, category-only evidence.

Output: format-clean Rust, strict five-stage comparison, a race-safe replay/reattach contract, measured USB absence after operator attestation, a tri-state denylist gate, deterministic fixtures, and conservative physical lifecycle evidence or a precise blocked result. Phase 30 share promotion remains separate.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/index.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/verification.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-CONTEXT.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VALIDATION.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-REVIEW.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-UAT.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-10-SUMMARY.md
@.planning/debug/phase28.1.1-cold-start-capture-gap.md

<interfaces>
Accepted-state completeness is the exact closed set `post_enumerate`, `post_mining_ready`, `post_max_baud`, `post_mask_reload`, and `post_first_work`. Both comparison members must contain every stage; empty, mutually partial, or one-sided incomplete inputs are `unavailable`, never `match`.

The timing contract is numeric and closed: replay window `180000 ms`, replay cadence `2000 ms`, maximum reattach budget `60000 ms` from operator attestation, monitor-start reserve `10000 ms`, and continuously observed USB absence at least `5000 ms`. Reappearance observed at `reappearance_elapsed_ms <= 60000` passes; absence at exactly `60000 ms` or reappearance at `reappearance_elapsed_ms > 60000` fails. The replay arithmetic is explicit: latest accepted reappearance `60000 ms` + monitor-start reserve `10000 ms` + the next cadence opportunity `2000 ms` = `72000 ms`, and `72000 ms < 180000 ms`. A test must consume every earlier cadence through the latest permitted attachment plus startup reserve, then prove that next replay remains due.

The power boundary has two different claims. `operator_attested_both_power_paths_removed=true` records the human action. `usb_absence_measured_ms>=5000` records the host-observable interval. `barrel_removal_electronically_verified=false` must remain explicit; the USB observation is not electronic proof of barrel removal.

From lifecycle `armed` through `capture-complete`, prohibit package, flash, reset, board-info, network discovery, and all credential path/content access. Post-capture detector/board-info is allowed only after capture completes. Replay remains diagnostic-only, default-off, transport-only, and never adds an ASIC read, sleep, task, thread, or initialization delay.
</interfaces>

<recovery_contract>
The hardware task starts with both USB and barrel power disconnected. Ask the user to connect barrel power and then USB before the first `just detect-ultra205`. If detection or `board-info` fails, allow each of these user-assisted, non-destructive recoveries at most once, rerunning only repo-owned `just detect-ultra205` after each: USB replug with barrel retained; both-power removal for at least five seconds followed by barrel then USB; one RESET press; one ESP32-S3 BOOT/RESET bootloader-entry assist for `espflash board-info`. Stop `blocked_safe_prerequisite` after this finite sequence. Never erase, factory-reset, write raw flash, run fault/stress actions, scan the network, infer a stale target, or improvise a flash command. Package/flash begins only after exactly one board-205 port passes the detector.
</recovery_contract>

<standards_alignment>
Repo-local guidance fixes the Ultra 205 detector, credential-path, 360-second capture, 420-second wall-clock, redaction, recovery, hardware-first, and frontmatter boundaries. Bright Builds architecture and testing rules keep completeness and timing decisions host-testable while firmware/shell remain thin effect adapters; code-shape rules require shallow guards and explicit optional naming; verification and Rust rules require the ordered workspace checks before every commit. No active standards override applies.
</standards_alignment>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Restore rustfmt cleanliness in an isolated commit</name>
  <files>crates/bitaxe-stratum/src/v1/live_runtime.rs</files>
  <read_first>AGENTS.md, standards/core/verification.md, standards/languages/rust.md, crates/bitaxe-stratum/src/v1/live_runtime.rs, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md</read_first>
  <action>Fetch remote refs and confirm the execution worktree is clean and based on the expected `main` before editing. Run `cargo fmt --all` to apply only the two verifier-recorded formatting differences in `crates/bitaxe-stratum/src/v1/live_runtime.rs`. Review the diff and fail if any semantic token, any other file, or any behavior changes. Keep this correction separate from every functional edit. Before the isolated formatting commit, run the mandatory Rust sequence in order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Commit only this formatting diff.</action>
  <verify><automated>cargo fmt --all -- --check && git diff --check && test "$(git diff --name-only | sort -u)" = "crates/bitaxe-stratum/src/v1/live_runtime.rs"</automated></verify>
  <acceptance_criteria>`cargo fmt --all -- --check` and `git diff --check` exit 0 before commit; the pre-commit sequence exits 0 in the mandated order; the pre-commit diff names only `crates/bitaxe-stratum/src/v1/live_runtime.rs` and matches rustfmt's two recorded layout changes; the commit contains no functional behavior.</acceptance_criteria>
  <done>The workspace format gate is green in a dedicated style-only commit, so later functional commits cannot conceal the pre-existing drift.</done>
</task>

<task type="auto">
  <name>Task 2: Make accepted-state completeness, replay timing, and evidence denial fail closed</name>
  <files>crates/bitaxe-api/src/logs.rs, crates/bitaxe-asic/src/bm1366/accepted_state.rs, scripts/phase28.1.1-accepted-state-compare.mjs, scripts/phase28.1.1-accepted-state-compare-test.mjs, scripts/phase28.1.1-promoted-evidence-denylist.patterns, scripts/phase28.1.1-promoted-evidence-denylist.sh, scripts/phase28.1.1-promoted-evidence-denylist-test.sh</files>
  <read_first>crates/bitaxe-api/src/logs.rs, crates/bitaxe-asic/src/bm1366/accepted_state.rs, scripts/phase28.1.1-accepted-state-compare.mjs, scripts/phase28.1.1-accepted-state-compare-test.mjs, scripts/phase28.1.1-accepted-state-lifecycle-compare.mjs, scripts/phase28.1.1-accepted-state-lifecycle-compare-test.mjs, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-REVIEW.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md</read_first>
  <action>In `bitaxe-api`, extend the replay window to exactly 180 seconds while retaining the 2-second cadence. Name the 60-second reattach deadline and 10-second monitor-start reserve in the host-testable contract. Replace the last-millisecond-only test with a real schedule test that consumes every due replay from arming through 70 seconds, then proves another replay is due at 72 seconds and the cadence still expires at 180 seconds. Assert the closed arithmetic directly: latest accepted reappearance `60000 ms` + monitor-start reserve `10000 ms` + next cadence `2000 ms` = `72000 ms < 180000 ms`. Do not change firmware arming, ASIC reads, initialization order, tasks, sleeps, or default-off behavior.

In the Rust classifier, require the exact five-stage set on both sides before any `Match` result; missing stages on either or both sides yield `Unavailable`, identify the first missing stage, and route to lifecycle parity. Apply the same completeness rule to the standalone JavaScript comparator. Update existing complete-match fixtures and add separate Arrange/Act/Assert tests for empty inputs, matching three-of-five inputs, and one-sided missing input in both Rust and Node. Preserve closed recommendations and category-only output.

Add a repo-owned promoted-evidence denylist with a checked-in pattern file. The shell gate must use `set -euo pipefail`, capture the search tool's status explicitly, fail with a clear forbidden-match error on status 0, pass only on status 1 (no match), and fail on status 2 or any other regex/tool error. Do not use `! rg`, `|| true`, or any construct that converts tool failure into success. Add deterministic fixtures for clean pass, forbidden sentinel match, malformed pattern, and injected search-tool error without real credentials or secret values. Before the functional commit, run `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features` in order, plus the focused Node, denylist, and shellcheck tests. Commit atomically after all checks pass.</action>
  <verify><automated>cargo test -p bitaxe-api logs && cargo test -p bitaxe-asic accepted_state && node scripts/phase28.1.1-accepted-state-compare-test.mjs && bash scripts/phase28.1.1-promoted-evidence-denylist-test.sh && shellcheck scripts/phase28.1.1-promoted-evidence-denylist.sh scripts/phase28.1.1-promoted-evidence-denylist-test.sh && cargo fmt --all -- --check</automated></verify>
  <acceptance_criteria>Focused Rust/Node tests exit 0 and separately cover empty, partial-mutual, and one-sided missing stage sets; no incomplete comparison returns `match`; replay constants are exactly 180000/2000/60000/10000 ms and the test consumes the repeated cadence through `60000 + 10000`, proves the next replay due at `72000`, and asserts `72000 < 180000`; the denylist test proves no-match pass, match failure, malformed-regex failure, and injected-tool failure; `rg -n "! rg|\|\| true" scripts/phase28.1.1-promoted-evidence-denylist.sh` has no matches; the mandatory Rust pre-commit sequence and `cargo fmt --all -- --check` pass.</acceptance_criteria>
  <done>All pure accepted-state and timing decisions fail closed, and the evidence denial gate cannot mistake a parser/tool error for clean evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce attested five-second power absence and strict lifecycle arming</name>
  <files>scripts/phase28.1.1-accepted-state-diagnostic.sh, scripts/phase28.1.1-accepted-state-diagnostic-test.sh</files>
  <read_first>scripts/phase28.1.1-accepted-state-diagnostic.sh, scripts/phase28.1.1-accepted-state-diagnostic-test.sh, scripts/phase13-monitor-capture.sh, scripts/detect-ultra205.sh, scripts/phase28.1.1-accepted-state-lifecycle-compare.mjs, crates/bitaxe-api/src/logs.rs, .planning/debug/phase28.1.1-cold-start-capture-gap.md, AGENTS.md</read_first>
  <action>Make the lifecycle branch an explicit handshake. After all detector/reference/manifest/reinit checks and the selected port-presence check pass, emit `armed` and wait on stdin for the exact closed operator token `both-power-paths-removed`; reject EOF, timeout, cancellation, or any other value. Begin a monotonic measurement only after that attestation. The selected USB port must be absent at attestation and remain continuously absent for at least 5000 ms; fail immediately if it is present or reappears early. After the measured threshold, emit the restore prompt and wait for the exact closed token `barrel-then-usb-restored`; reject EOF, cancellation, a late token, or any other value. Use the closed reappearance rule: a port observed present at `reappearance_elapsed_ms <= 60000` passes, absence observed at exactly `60000 ms` fails immediately, and presence first observed at `60001 ms` or later fails. Reserve/document 10000 ms for monitor startup inside the 180-second replay window; explicitly preserve the next 2000 ms cadence opportunity because `60000 + 10000 + 2000 = 72000 < 180000`. Attach the existing monitor with `--no-reset` for at least 360 seconds, and retain the no-package/no-flash/no-reset/no-board-info/no-discovery/no-credential boundary until capture completes.

Use an injectable monotonic-clock fixture rather than loop-count claims. Record only `operator_attested_both_power_paths_removed: true`, measured USB-absence duration/category, `barrel_removal_electronically_verified: false`, the fixed timing contract, and existing redacted lifecycle fields. Do not claim that USB absence electronically proves barrel removal. Make the accepted-state/reinit path fail before exposing an armable manifest/log unless its raw capture has one stable boot, one listener-ready marker, zero hazard markers, and the lifecycle comparator accepts the exact five-stage member against itself. Never synthesize unavailable stages for this arming gate.

Extend shell fixtures to prove: wrong/absent attestation fails; wrong/absent restore token fails; an abandoned armed or restore wait reaches its injected deadline and exits with no live child; 4999 ms continuous absence fails; exactly 5000 ms passes; early reappearance fails; presence observed at exactly 60000 ms passes; absence observed at exactly 60000 ms fails; presence first observed at 60001 ms is rejected; exact five-stage reinit is mandatory; monitor uses 360 seconds and `--no-reset`; and the arming trace contains none of the forbidden preflight/flash/credential actions. Give both stdin waits explicit monotonic deadlines and install cleanup for EOF, timeout, cancellation, interrupt, and normal exit so a child capture cannot survive a failed handshake. Keep recovery actions outside the armed interval and bounded by this plan's recovery contract. Before commit, run `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features` in order, then the lifecycle Node/shell and shellcheck suites. Commit only after all pass.</action>
  <verify><automated>node scripts/phase28.1.1-accepted-state-lifecycle-compare-test.mjs && bash scripts/phase28.1.1-accepted-state-diagnostic-test.sh && shellcheck scripts/phase28.1.1-accepted-state-diagnostic.sh scripts/phase28.1.1-accepted-state-diagnostic-test.sh && cargo fmt --all -- --check</automated></verify>
  <acceptance_criteria>The wrapper test deterministically proves attestation, restore-token validation, injected-deadline self-termination for abandoned armed and restore waits, 4999 ms rejection, 5000 ms acceptance, early-return rejection, independent fixtures for present at exactly 60000 ms passing, absent at exactly 60000 ms failing, and present at 60001 ms failing, plus strict five-stage reinit arming and child cleanup on every failed handshake; test traces prove no package/flash/reset/board-info/discovery/credential action occurs from `armed` through `capture-complete`; output explicitly distinguishes operator attestation, measured USB absence, and absent electronic barrel proof; `rg -n -- "--no-reset|360|5000|60000|60001|72000|180000|both-power-paths-removed|barrel-then-usb-restored" scripts/phase28.1.1-accepted-state-diagnostic.sh scripts/phase28.1.1-accepted-state-diagnostic-test.sh` finds the closed contract and its fixtures; all focused and mandatory pre-commit checks pass.</acceptance_criteria>
  <done>The cold-start workflow cannot accept a brief USB bounce, cannot arm from partial reinit evidence, and cannot overstate what the host observed.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 4: Recover the unplugged board, capture exact-HEAD lifecycle evidence, and close conservatively</name>
  <files>.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-accepted-state-baseline-redacted.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-accepted-state-lifecycle-redacted.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-UAT.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-REVIEW.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-11-SUMMARY.md, .planning/debug/phase28.1.1-cold-start-capture-gap.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>AGENTS.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-CONTEXT.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-UAT.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-REVIEW.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md, .planning/debug/phase28.1.1-cold-start-capture-gap.md, scripts/phase28.1.1-accepted-state-diagnostic.sh, scripts/phase28.1.1-promoted-evidence-denylist.sh</read_first>
  <action>Confirm Tasks 1-3 are committed, `main` is clean, and the software commit is the intended exact HEAD. Run the full focused tests, then `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features` in order, followed by `cargo fmt --all -- --check`. Rerun a focused phase code review against WR-01 through WR-03 and the Task 1 formatting isolation. Update `28.1.1-REVIEW.md` with command-backed resolution evidence only if the reviewer finds all three warnings closed; otherwise stop before hardware. Before the review-closure commit, rerun `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features` in order. Commit the review closure, require a clean worktree, and treat that resulting commit as the exact HEAD for packaging. Do not begin hardware while any check or code-review warning remains open.

The device begins unplugged. Before asking for initial plug-in, create an ignored per-run directory and return the structured checkpoint `checkpoint_id=ultra205-initial-plugin`, `checkpoint_token=initial-plugin-v1`, with `pty_session_id=null`, the absolute ignored `run_directory`, `exact_head`, `manifest_source_commit=null`, `manifest_checksum_sha256=null`, `reinit_five_stage_result=not_run`, `arming_state=not_started`, a numeric `monotonic_deadline_ms` exactly 600000 ms after checkpoint creation, `expected_response_token=barrel-then-usb-connected`, `expected_user_action=connect barrel/DC power then USB`, and `process_running=false`. Ask the user to perform that action and respond with the exact token. Run `just detect-ultra205` only after a fresh continuation validates the checkpoint as specified below; accept only exactly one board-205 ESP32-S3 port with successful `board-info`. If it fails, follow the finite recovery contract in this plan, recording only action categories and outcomes; stop blocked after the bounded attempts. After detection passes, verify credential file presence by path only, run `just verify-reference`, build the accepted-state package from clean exact HEAD, and require the package manifest `source_commit` to equal HEAD. Do not read or print credential contents.

Capture the exact-HEAD flash/reinit member with redaction enabled for at least 360 seconds and a shell wall clock of at least 420 seconds. Require exactly one boot, exactly one listener readiness marker, zero overflow/panic/software-reset hazards, the exact five closed stages with equivalent duplicates only, and strict self-comparison success. If this baseline is incomplete, write `blocked_safe_prerequisite` and stop without asking for a cold start.

If and only if the reinit baseline is complete, rerun the detector/reference/manifest preflight and start the retained-package lifecycle wrapper in a PTY. Wait until it reports `armed`, persist the live checkpoint state in the ignored run directory, pass the same deadline into the wrapper's own bounded stdin wait, and return `checkpoint_id=ultra205-lifecycle-armed-removal`, `checkpoint_token=armed-removal-v1`, the live `pty_session_id`, absolute ignored `run_directory`, `exact_head`, exact `manifest_source_commit`, exact SHA-256 `manifest_checksum_sha256`, `reinit_five_stage_result=pass`, `arming_state=armed_waiting_for_attestation`, a numeric `monotonic_deadline_ms` exactly 300000 ms after the armed return, `expected_response_token=both-power-paths-removed`, `expected_user_action=disconnect both barrel/DC power and USB and attest both are removed`, and `process_running=true`. Send the attestation token to the PTY only after a fresh continuation validates that state and the user supplies that exact response. Poll until the wrapper reports the measured continuous five-second USB-absence confirmation and restore prompt. Persist and return the next checkpoint as `checkpoint_id=ultra205-barrel-usb-restore`, `checkpoint_token=barrel-usb-restore-v1`, with the same live `pty_session_id`, absolute ignored `run_directory`, `exact_head`, `manifest_source_commit`, `manifest_checksum_sha256`, `reinit_five_stage_result=pass`, `arming_state=absence_confirmed_waiting_for_restore`, `monotonic_deadline_ms=attestation_monotonic_ms+60000`, `expected_response_token=barrel-then-usb-restored`, `expected_user_action=reconnect barrel power and then USB`, and `process_running=true`. Send that exact restore token only after the user performs the ordered action and a fresh continuation validates the state. The wrapper must accept port presence at elapsed `<=60000 ms`, reject absence at `60000 ms` and any later presence, then capture at least 360 seconds with `--no-reset` and a wall-clock allowance at least 420 seconds. Do not package, flash, reset, run board-info/discovery, or access credentials until `capture-complete`; then run the post-capture detector.

Every checkpoint return must use a `## CHECKPOINT REACHED` block containing every field named above, even when a value is explicitly `null` or `not_run`; the ignored state file and returned block must agree byte-for-byte for the scalar identity/state fields. Before accepting a user response, writing to the PTY, or performing the next action, every fresh continuation agent must validate: the supplied `checkpoint_id` and `checkpoint_token` equal the persisted current state; `git rev-parse HEAD` equals `exact_head`; the worktree is clean; the manifest exists and its `source_commit` plus computed SHA-256 equal `manifest_source_commit` and `manifest_checksum_sha256` when non-null; the ignored run directory exists and remains ignored; the numeric monotonic deadline has not passed; `reinit_five_stage_result` and `arming_state` match the expected transition; and PTY/session liveness exactly matches `process_running`. A missing, stale, expired, or mismatched field must never be inferred or repaired. It must trigger cleanup of any live child and a conservative `blocked_safe_prerequisite` stop before any PTY write or user action is accepted.

Define one mandatory cleanup path for all failure exits and handoffs: poll the recorded PTY/session first; when `process_running=true`, send an interrupt/termination through the PTY control, poll to a bounded exit, escalate to the execution environment's termination control if needed, and verify by a final poll that no process remains. The wrapper must also self-terminate at the persisted monotonic deadline if no continuation ever resumes it. Mark `process_running=false` in the ignored state and record a conservative blocked outcome without promoting raw data. Apply this cleanup on every timeout, cancellation, user response after its deadline, invalid response, failed continuation validation, PTY/write error, hardware/preflight failure after session start, and explicitly abandoned checkpoint. A continuation that cannot prove final process death must stop and report the unresolved live-session risk; it may not proceed, commit positive evidence, or silently leave the watcher running.

Keep raw monitor/detector/package logs in ignored scratch. Promote only redacted counts, booleans, closed categories, exact source/reference identities, the bounded operator attestation, measured USB absence, explicit lack of electronic barrel proof, and exact conclusions. Run the new denylist over every promoted artifact; any match or tool error blocks commit. Confirm `docs/parity/checklist.md` is unchanged. Update UAT, debug, baseline/lifecycle evidence, REVIEW warning-resolution evidence, VERIFICATION, STATE, ROADMAP, and `28.1.1-11-SUMMARY.md` consistently. A completed lifecycle may close the physical UAT gap, but Phase 28.1.1 remains `gaps_found` unless the original D-18 same-chain correlated-result, hashing-rise, and accepted/rejected-share truth is independently present. Never synthesize evidence, mark STR-09/ASIC-11/CFG-07 complete, edit Phase 30 promotion, or broaden this run into Phase 30 share proof.

Before the evidence/closure commit, rerun `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features` in order, then `cargo fmt --all -- --check`, all focused Node/shell tests, `just verify-reference`, the promoted-evidence denylist, `git diff --check`, and formal lifecycle validation. Commit only a truthful passed or blocked result. Push `main` only after all gates pass, then require a clean worktree and zero ahead/behind against `origin/main`.</action>
  <verify><automated>cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features && cargo fmt --all -- --check && node scripts/phase28.1.1-accepted-state-compare-test.mjs && node scripts/phase28.1.1-accepted-state-lifecycle-compare-test.mjs && bash scripts/phase28.1.1-promoted-evidence-denylist-test.sh && bash scripts/phase28.1.1-accepted-state-diagnostic-test.sh && just verify-reference && bash scripts/phase28.1.1-promoted-evidence-denylist.sh .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-accepted-state-baseline-redacted.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-accepted-state-lifecycle-redacted.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-UAT.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-REVIEW.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-11-SUMMARY.md .planning/debug/phase28.1.1-cold-start-capture-gap.md .planning/STATE.md .planning/ROADMAP.md && git diff --exit-code HEAD -- docs/parity/checklist.md && node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 28.1.1 --require-plans --require-verification --raw && git diff --check</automated></verify>
  <acceptance_criteria>All software fixes and the independent review-closure record are committed before packaging; detector/reference/package identity pass on that clean exact HEAD; a complete strict five-stage reinit exists before any cold-start request; all three named checkpoint blocks carry PTY/session ID or explicit null, ignored run directory, exact HEAD, manifest source commit/SHA-256 checksum identity, reinit five-stage result, arming state, monotonic deadline, expected response token/action, and process-running state; each continuation validates the persisted state, identity, liveness, token, and deadline before accepting an action or writing to the PTY; timeout/cancellation/delay/failure/abandonment always polls, terminates, and proves no live child remains or stops on an explicit unresolved-session risk, while the wrapper independently self-terminates when a continuation never resumes; the lifecycle capture, if armed, records explicit operator attestation plus at least 5000 ms continuous USB absence, accepts reappearance only at elapsed `<=60000 ms`, rejects absence at `60000 ms` and presence at `60001 ms`, waits for the exact restore checkpoint/token, uses the retained package with no flash/reset, captures for at least 360 seconds with a 420-second-or-greater wall clock, and runs detector only after capture; raw logs remain ignored and every promoted artifact passes the tri-state denylist; code-review warnings WR-01 through WR-03 have command-backed resolution evidence; `cargo fmt --all -- --check`, full tests, lifecycle validation, and `git diff --check` pass; checklist and Phase 30 promotion remain unchanged; the final commit is pushed only with clean zero-ahead/behind state.</acceptance_criteria>
  <done>The physical lifecycle gap is either proven with exact, redacted, measured evidence or stopped at a documented bounded prerequisite, while the original nonce/share goal and Phase 30 promotion remain conservatively evidence-gated.</done>
</task>
</tasks>

<threat_model>
## ASVS Level 1 Threat Model

| ID | ASVS / STRIDE | Threat | Severity | Mitigation | Blocking condition |
| --- | --- | --- | --- | --- | --- |
| T-11-01 | V5 / Tampering | Empty or partial accepted-state members are promoted as a match | high | Exact five-stage completeness in Rust and JS with empty/partial/one-sided tests | Any incomplete input returns `match` |
| T-11-02 | V5 / Repudiation | A brief USB bounce is described as the required cold start | high | Exact operator attestation plus monotonic continuous USB absence of at least 5000 ms | Missing attestation, non-monotonic timing, or less than 5000 ms absence |
| T-11-03 | V5 / Repudiation | USB absence is overstated as electronic proof of barrel removal | high | Store operator action and USB measurement separately; force `barrel_removal_electronically_verified=false` | Evidence claims electronic barrel proof |
| T-11-04 | V14 / Denial of Service | Reattach completes after the last useful replay | high | Reappearance at elapsed `<=60000 ms`, rejection after that closed boundary, 10-second startup reserve, next 2-second cadence at 72000 ms, 180-second replay, and real repeated-schedule tests | Boundary fixtures drift, timing arithmetic fails, or no post-margin replay remains |
| T-11-05 | V14 / Tampering | Replay alters ASIC state or default firmware behavior | critical | Change timing contract only; retain diagnostic gate and transport-only logging | Any new ASIC command/read, sleep, task/thread, init-order, or default-on change |
| T-11-06 | V4 / Tampering | Cold-start branch silently packages, flashes, resets, discovers, or reads credentials after arming | critical | Trace-tested hard boundary from `armed` through `capture-complete` | Any prohibited action appears in the armed trace |
| T-11-07 | V1 / Tampering | Wrong or stale firmware is compared across lifecycle members | critical | Clean exact-HEAD implementation commit, manifest identity, retained package, no rebuild between members | HEAD/manifest mismatch or package changes after reinit |
| T-11-08 | V4 / Spoofing | Ambiguous/replaced USB device contaminates evidence | high | Exactly one detector-selected board-205 ESP32-S3 port before package and after capture | Zero/multiple ports, port mismatch, or failed board-info |
| T-11-09 | V8 / Information Disclosure | Credentials, endpoints, identifiers, raw frames/registers, or network data enter tracked evidence | critical | Path-only preflight, ignored raw logs, closed output, tri-state denylist, secret-noise tests | Any sensitive/raw value is read, printed, promoted, or tracked |
| T-11-10 | V8 / Tampering | Malformed regex or failed search tool is mistaken for a clean denylist result | high | Explicit status 0/1/other handling and malformed/tool-error fixtures | Any tool error exits successfully |
| T-11-11 | V14 / Denial of Service | Recovery becomes destructive or unbounded | critical | Finite user-assisted replug/reset/bootloader sequence using repo-owned detector only | Erase, factory reset, raw write, stress/fault action, scan, improvised flash, or retries beyond the bound |
| T-11-12 | V1 / Repudiation | Partial reinit evidence arms a physical action | high | Stable boot/listener/hazard checks plus strict five-stage self-comparison before arming | Any missing stage, conflicting duplicate, hazard, or unstable boot is accepted |
| T-11-13 | V8 / Repudiation | Lifecycle implementation is used to overpromote mining/share requirements | high | Preserve D-18 same-chain gate and leave checklist/Phase 30 untouched | STR-09, ASIC-11, CFG-07, or Phase 30 changes without eligible same-chain evidence |
| T-11-14 | V14 / Denial of Service | A checkpoint continuation acts on stale identity or silently leaves a watcher alive | critical | Three named tokenized checkpoint states, exact HEAD/manifest/run-dir/session/deadline validation, and mandatory poll/terminate/final-liveness cleanup on every non-success path | Any continuation accepts stale state or any timeout, cancellation, failed continuation, or abandoned checkpoint leaves an unverified child process |

All high and critical threats are release-blocking. There are no accepted unmitigated high-severity threats in this plan.
</threat_model>

<verification>
Run task-scoped tests after each change, the mandated Rust sequence before every commit, and `cargo fmt --all -- --check` at each final boundary. Review diffs for functional-core/imperative-shell separation, exact numeric timing, forbidden arming actions, no credential access, accurate measurement claims, no checklist changes, and no hardware action before strict reinit completeness. Hardware execution must use at least 360-second captures and at least 420-second shell wall clocks.
</verification>

<success_criteria>
Plan 11 succeeds when all verifier/reviewer evidence-correctness warnings are regression guarded, the unplugged Ultra 205 follows only the finite safe recovery route, and the exact-HEAD lifecycle pair is either captured with five complete redacted stages and measured power-removal semantics or closed `blocked_safe_prerequisite` at a named gate. Success does not imply nonce production, a live accepted/rejected share, or Phase 30 promotion.
</success_criteria>

<output>After execution, create `28.1.1-11-SUMMARY.md` and update the named UAT, debug, evidence, review, verification, state, and roadmap artifacts with the exact observed result.</output>
