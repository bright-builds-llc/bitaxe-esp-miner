---
phase: 28.1.1-bm1366-nonce-production-wire-parity
plan: 16
type: execute
gap_closure: true
wave: 14
depends_on: [28.1.1-15]
files_modified:
  - .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan16-qualification-redacted.md
  - .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan16-attempt-redacted.md
  - .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-16-SUMMARY.md
autonomous: false
requirements: [STR-09, ASIC-11, CFG-07]
user_setup:
  - "Keep the Ultra 205 on its normal barrel-power and native USB connectors; no external UART, probes, jumpers, pins, test points, or electrical manipulation is authorized."
  - "Be available for two broker-gated both-power removal/restoration sequences: one transport qualification and, only if it passes, one fresh formal production chain."
must_haves:
  truths:
    - "The Plan-15 software authority is committed, clean, synchronized, and remains byte-identical through qualification and the formal production chain"
    - "One and only one non-invasive native-USB cold qualification proves OS-native application-byte delivery, same stable physical identity, changed enumeration identity, a new internally consistent cold session, complete session-tagged replay, and full cleanup"
    - "Qualification facts are destroyed after validation and contribute no formal result, power, share, verification, or Phase 30 claim"
    - "Only a passing qualification may start one fresh formal authority on the same unchanged exact HEAD; any prerequisite, qualification, or cleanup failure stops without retry"
    - "Formal passed closure requires one same-chain result_correlated observation, hashing-class power rise, and exactly one live accepted or rejected share outcome"
    - "A valid completed formal chain missing any pass signal closes gaps_found; malformed, incomplete, mixed-chain, or cleanup-uncertain evidence closes through the conservative invalid/prerequisite terminal"
    - "Every private resource is destroyed and committed evidence contains only redacted categories, counts, durations, booleans, and digests"
  artifacts:
    - path: .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan16-qualification-redacted.md
      provides: "Category-only terminal record for the one-shot native transport qualification"
      contains: "qualification_status"
    - path: .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan16-attempt-redacted.md
      provides: "Category-only terminal record for the fresh same-chain production attempt"
      contains: "verification_result"
  key_links:
    - from: .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan16-qualification-redacted.md
      to: .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan16-attempt-redacted.md
      via: "records qualification cleanup and same exact HEAD, never product evidence"
      pattern: "cleanup|exact_head"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1-2026-07-09T19-24-27
generated_at: 2026-07-13T04:58:23Z
---

<objective>
Run one bounded non-invasive native-USB qualification and, only if it passes and cleans up, one fresh formal Phase 28.1.1 production-evidence chain on the same unchanged exact HEAD.

Purpose: close the remaining transport ambiguity without weakening the same-chain product gate or invoking the dormant direct-UART path.

Output: truthful redacted qualification and formal terminal artifacts, complete private cleanup, and either passed closure or an exact conservative gap/blocker outcome.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
@/Users/peterryszkiewicz/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/verification.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-CONTEXT.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-15-PLAN.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-15-SUMMARY.md
@scripts/diagnose-ultra205-late-attach.sh
@scripts/ultra205-late-attach-broker.sh
@scripts/ultra205-transport-qualification.sh
@scripts/phase28.1.1-exact-head-hardware-attempt.sh

<hardware_contract>
Use only the provided barrel-power and native USB connectors. Before each chain run `just detect-ultra205` and accept exactly one board-205 port only when board-info succeeds. Private traces stay beneath a mode-0700 ignored root with mode-0600 files. Each cold capture is at least 360 seconds and each agent/shell wall clock is at least 420 seconds.

For removal, the exact-node lifecycle owner and watcher must be running before the action is shown. A response token may be delivered only after that owner observes disappearance and the full bounded absence interval succeeds. Native qualification restoration waits for `action_token=late-attach-os-native-watcher-armed-v2`; formal restoration waits for `action_token=plan13-restore-watcher-armed-v1`; both require `response_required=false`, then instruct barrel power followed by USB. The owner observes appearance automatically; no repository response token follows restoration. Preserve stable physical identity and require a new enumeration identity. A zero-byte stream is invalid evidence and is never retried.
</hardware_contract>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Establish the clean exact-HEAD qualification authority</name>
  <files>.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan16-qualification-redacted.md</files>
  <read_first>AGENTS.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-15-SUMMARY.md, scripts/diagnose-ultra205-late-attach.sh, scripts/ultra205-late-attach-broker.sh, scripts/ultra205-transport-qualification.sh</read_first>
  <action>Require branch main, an empty worktree, a configured upstream with behind count zero (clean ahead-only commits from this wrapper are allowed), the Plan-15 commit as HEAD, and a clean read-only `just verify-reference`. Before authority creation, run `just detect-ultra205` exactly once and require exactly one board 205 port with successful reset-explicit board-info; record detector output privately. Do not run detector or any other reset-capable command after the owner is armed. Check only presence/mode of required ignored credential paths if the qualification wrapper requires them; never open, print, or summarize credential contents. Create one opaque late-attach authority with a 360-second capture and a shell/tool wall clock of at least 420 seconds. Start its exact-node owner and wait until it publishes the removal action only after the watcher is armed. Persist or escrow the category-only public locator before any fallible validation.</action>
  <verify><automated>test -z "$(git status --porcelain=v1)" &amp;&amp; read -r ahead behind &lt;&lt;&lt;"$(git rev-list --left-right --count HEAD...@{upstream})" &amp;&amp; test "$behind" -eq 0 &amp;&amp; qualification_status="$(bash scripts/diagnose-ultra205-late-attach.sh status resume-handle="$PLAN16_QUALIFICATION_HANDLE")" &amp;&amp; rg -q '^action_token=late-attach-removal-watcher-armed-v2$' &lt;&lt;&lt;"$qualification_status" &amp;&amp; rg -q '^response_required=true$' &lt;&lt;&lt;"$qualification_status"</automated></verify>
  <acceptance_criteria>One clean exact Plan-15 HEAD with no upstream-behind commits is pinned; exactly one Ultra 205 port passed the sole pre-authority detector/board-info run; broker status proves the exact-node owner/watcher is armed before removal is published; post-arm verification performs no detector/reset; no credential contents, raw identifiers, hardware stress, direct UART, or source edits occur.</acceptance_criteria>
  <done>The one-shot qualification is armed at the exact removal checkpoint.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Remove both Ultra 205 power paths for qualification</name>
  <files>None — physical connector action only</files>
  <action>After the lifecycle owner reports its exact removal watcher armed, remove barrel power and native USB from the Ultra 205. Do not touch pins, probes, headers, jumpers, or any other electrical interface.</action>
  <instructions>Remove the barrel-power connector, then unplug the Ultra 205 native USB connector. Leave both disconnected. Reply only after both connectors are physically removed; the continuation must still wait for owner-observed exact-node disappearance and the full absence interval before delivering any broker response token.</instructions>
  <verification>The executor confirms the selected exact node disappeared under the already-running lifecycle owner, the stable physical identity record is retained privately, the full bounded absence interval passed, and no unexpected serial holder or process appeared.</verification>
  <resume-signal>Reply `plan16-qualification-both-power-removed` after barrel power and native USB are both disconnected.</resume-signal>
  <done>The owner, not the response alone, has proved disappearance and bounded absence.</done>
</task>

<task type="auto">
  <name>Task 3: Accept proved qualification removal and arm response-free restoration</name>
  <files>.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan16-qualification-redacted.md</files>
  <read_first>scripts/ultra205-late-attach-broker.sh, scripts/ultra205-late-attach-worker.sh, scripts/serial-session-trace.sh</read_first>
  <action>Ignore an early human response until private status proves exact-node disappearance and the complete absence interval. Deliver the one allowed removal response through the active opaque authority, verify lease/owner/head/session/identity again, and wait until the same lifecycle emits exactly `action_token=late-attach-os-native-watcher-armed-v2` with `response_required=false`. Do not publish restoration earlier and do not create a post-restoration repository response token.</action>
  <verify><automated>qualification_status="$(bash scripts/diagnose-ultra205-late-attach.sh status resume-handle="$PLAN16_QUALIFICATION_HANDLE")" &amp;&amp; rg -q '^attempt_state=waiting_restore$' &lt;&lt;&lt;"$qualification_status" &amp;&amp; rg -q '^action_token=late-attach-os-native-watcher-armed-v2$' &lt;&lt;&lt;"$qualification_status" &amp;&amp; rg -q '^response_required=false$' &lt;&lt;&lt;"$qualification_status"</automated></verify>
  <acceptance_criteria>The removal response is accepted only after owner proof; the restore watcher is armed under the same valid authority; no reset, scan, retry, source edit, or unrelated process action occurred.</acceptance_criteria>
  <done>The response-free restore watcher is armed for qualification.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Restore qualification power in the required order</name>
  <files>None — physical connector action only</files>
  <action>Only after the response-free restore watcher is armed, reconnect barrel power and then native USB. Do not press reset or touch any other electrical interface.</action>
  <instructions>Reconnect the barrel-power connector first. Then reconnect the Ultra 205 native USB connector. No broker response token is expected after restoration; the already-running owner observes enumeration automatically.</instructions>
  <verification>The executor waits for automatic appearance, bounded readiness, equal stable physical identity, a changed enumeration identity, expected ownership, nonzero OS-native application bytes, and complete session-tagged replay.</verification>
  <resume-signal>Reply `plan16-qualification-power-restored` after barrel power and then native USB are reconnected.</resume-signal>
  <done>Power is restored in order while the lifecycle owner observes the new enumeration.</done>
</task>

<task type="auto">
  <name>Task 5: Close qualification once and conditionally create a fresh formal authority</name>
  <files>.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan16-qualification-redacted.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan16-attempt-redacted.md</files>
  <read_first>scripts/ultra205-late-attach-classifier.mjs, scripts/ultra205-transport-qualification.sh, scripts/phase28.1.1-exact-head-hardware-attempt.sh</read_first>
  <action>Allow the bounded 360-second qualification capture to finish with at least a 420-second wall clock. Validate privately, write a two-delimiter frontmatter category-only qualification terminal, terminate and reap the owner tree, remove holders/sockets/capabilities, destroy the qualification authority, and prove cleanup. If zero bytes, wrong identity/session, incomplete replay, invalid evidence, head drift, or cleanup uncertainty occurs, close once as the exact safe blocker and write the formal artifact with the exact conservative projection `formal_status: not_started`, `qualification_terminal: blocked_safe_prerequisite`, `verification_result: gaps_found`, and `phase30_promotion_input: ineligible`; create no formal authority and route directly to Task 9's qualification-only closure without retry. If and only if native_cold_delivers passes, recheck that HEAD/protected source paths/reference are unchanged and clean with upstream behind count zero, then create one fresh opaque formal authority with distinct attempt/evidence identity and an empty product projection. Run its fixed preflight/reflash/reinit path using ignored credential files only as repo-owned runtime inputs, capture/validate the monitored original reinit evidence, and stop when the formal removal watcher is armed.</action>
  <verify><automated>qualification_status="$(bash scripts/diagnose-ultra205-late-attach.sh status resume-handle="$PLAN16_QUALIFICATION_HANDLE")" &amp;&amp; rg -q '^status=closed$' &lt;&lt;&lt;"$qualification_status" &amp;&amp; rg -q '^cleanup_complete=true$' &lt;&lt;&lt;"$qualification_status" &amp;&amp; node scripts/phase28.1.1-roadmap-phase30-immutability.mjs check-frontmatter --files .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan16-qualification-redacted.md &amp;&amp; bash scripts/phase28.1.1-promoted-evidence-denylist.sh .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan16-qualification-redacted.md &amp;&amp; test -z "$(git status --porcelain=v1 -- scripts crates firmware tools Justfile AGENTS.md)" &amp;&amp; read -r ahead behind &lt;&lt;&lt;"$(git rev-list --left-right --count HEAD...@{upstream})" &amp;&amp; test "$behind" -eq 0</automated></verify>
  <acceptance_criteria>Repo-owned status proves qualification closed exactly once with cleanup_complete=true; the artifact has exactly one frontmatter block and passes the evidence denylist; protected source paths remain unchanged; invalid qualification creates the exact truthful not-started/gaps_found/ineligible formal projection and no authority; passing qualification contributes no product fact; the optional formal authority is fresh, exact-HEAD identical, and reaches its removal checkpoint only after one valid reinit chain.</acceptance_criteria>
  <done>Either qualification stopped conservatively, or one fresh formal authority is armed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: Remove both Ultra 205 power paths for the formal chain</name>
  <files>None — physical connector action only</files>
  <action>If Task 5 produced a fresh formal removal checkpoint, remove barrel power and native USB after its exact-node watcher is armed. If qualification did not pass, this checkpoint is not reached.</action>
  <instructions>Remove the barrel-power connector, then unplug the Ultra 205 native USB connector. Leave both disconnected. Reply only after both are physically removed; the continuation must still wait for owner-observed disappearance and the full absence interval.</instructions>
  <verification>The executor proves disappearance and bounded absence under the fresh formal owner, with unchanged stable physical identity input, clean exact HEAD, and no unexpected holder/process.</verification>
  <resume-signal>Reply `plan16-formal-both-power-removed` after both connectors are disconnected.</resume-signal>
  <done>The formal owner has proved disappearance and bounded absence.</done>
</task>

<task type="auto">
  <name>Task 7: Accept proved formal removal and arm response-free restoration</name>
  <files>.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan16-attempt-redacted.md</files>
  <read_first>scripts/phase28.1.1-exact-head-hardware-attempt.sh, scripts/serial-session-trace.sh</read_first>
  <action>Wait for the fresh formal owner to prove exact-node disappearance and the full absence interval before delivering its one removal response. Revalidate head/lease/owner/session/identity, then wait for exactly `action_token=plan13-restore-watcher-armed-v1` with `response_required=false`. Do not publish restoration early and do not send a repository token after restore.</action>
  <verify><automated>formal_status="$(bash scripts/phase28.1.1-exact-head-hardware-attempt.sh resolve-checkpoint --resume-handle "$PLAN16_FORMAL_HANDLE")" &amp;&amp; rg -q '^action_token=plan13-restore-watcher-armed-v1$' &lt;&lt;&lt;"$formal_status" &amp;&amp; rg -q '^response_required=false$' &lt;&lt;&lt;"$formal_status"</automated></verify>
  <acceptance_criteria>The fresh formal authority remains the sole lifecycle owner and the response-free restore watcher is armed without retry, reset, scan, source edit, or qualification-handle reuse.</acceptance_criteria>
  <done>The response-free formal restore watcher is armed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 8: Restore formal-chain power in the required order</name>
  <files>None — physical connector action only</files>
  <action>Reconnect barrel power and then native USB only after the formal response-free restore watcher is armed.</action>
  <instructions>Reconnect the barrel-power connector first, then reconnect native USB. Do not press reset or touch any pins, probes, headers, jumpers, or other electrical interfaces. No broker response token follows restoration.</instructions>
  <verification>The formal owner automatically observes appearance, stable physical identity, a new enumeration identity, expected ownership, nonzero application delivery, session-tagged cold replay, and the 360-second capture.</verification>
  <resume-signal>Reply `plan16-formal-power-restored` after barrel power and then native USB are reconnected.</resume-signal>
  <done>The fresh formal chain continues under the same owner after ordered restoration.</done>
</task>

<task type="auto">
  <name>Task 9: Classify, redact, clean up, and verify the formal terminal</name>
  <files>.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan16-attempt-redacted.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-16-SUMMARY.md</files>
  <read_first>scripts/phase28.1.1-hardware-attempt-state.mjs, scripts/phase28.1.1-strict-production-evidence.mjs, scripts/phase28.1.1-roadmap-phase30-immutability.mjs, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-CONTEXT.md</read_first>
  <action>If Task 5 recorded `formal_status: not_started`, do not require a formal handle and do not run Tasks 6-8: verify the qualification tombstone/cleanup plus the exact not-started/gaps_found/ineligible projection, then complete the same redaction, lifecycle, test, summary, and commit gates below. Otherwise let the formal cold capture run at least 360 seconds with a wall clock of at least 420 seconds. Validate one internally consistent formal chain containing cold lifecycle proof, all five accepted-state stages, and its product observations. Close passed only when that same chain has result_correlated, hashing-class power rise, and exactly one accepted/rejected live share. A valid complete chain missing any member closes gaps_found; invalid/malformed/mixed/cleanup-uncertain evidence closes through the conservative terminal. Run the allowed terminal detector, then destroy and reap every process, holder, socket, capability, and live handle. Produce only redacted category/count/duration/boolean/digest evidence; scan for credential values and local identifiers before promotion. Run focused closure tests, Phase 30 immutability, `just verify-reference`, `git diff --check`, and the mandatory Rust sequence before committing the evidence/summary. Do not push; the outer wrapper owns the final push after independent verification passes.</action>
  <verify><automated>formal_artifact=.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan16-attempt-redacted.md &amp;&amp; if rg -q '^formal_status: not_started$' "$formal_artifact"; then qualification_status="$(bash scripts/diagnose-ultra205-late-attach.sh status resume-handle="$PLAN16_QUALIFICATION_HANDLE")" &amp;&amp; rg -q '^status=closed$' &lt;&lt;&lt;"$qualification_status" &amp;&amp; rg -q '^cleanup_complete=true$' &lt;&lt;&lt;"$qualification_status" &amp;&amp; rg -q '^qualification_terminal: blocked_safe_prerequisite$' "$formal_artifact" &amp;&amp; rg -q '^verification_result: gaps_found$' "$formal_artifact" &amp;&amp; rg -q '^phase30_promotion_input: ineligible$' "$formal_artifact"; else test -n "${PLAN16_FORMAL_HANDLE:-}" &amp;&amp; bash scripts/phase28.1.1-exact-head-hardware-attempt.sh verify-terminal --resume-handle "$PLAN16_FORMAL_HANDLE" --redacted-artifact "$formal_artifact"; fi &amp;&amp; node scripts/phase28.1.1-roadmap-phase30-immutability.mjs check-frontmatter --files .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan16-qualification-redacted.md "$formal_artifact" &amp;&amp; bash scripts/phase28.1.1-promoted-evidence-denylist.sh .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-plan16-qualification-redacted.md "$formal_artifact" &amp;&amp; node scripts/phase28.1.1-strict-production-evidence-test.mjs &amp;&amp; node scripts/phase28.1.1-roadmap-phase30-immutability-test.mjs &amp;&amp; bash scripts/phase28.1.1-exact-head-hardware-attempt-test.sh &amp;&amp; just verify-reference &amp;&amp; node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 28.1.1 --allow-stale-verification --require-plans &amp;&amp; git diff --check &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features</automated></verify>
  <acceptance_criteria>Qualification-only closure needs no formal handle and proves closed cleanup plus the exact not-started/gaps_found/ineligible projection; otherwise the repo-owned terminal verifier proves artifact/tombstone/head/lifecycle/terminal consistency and zero live resources; both artifacts have exactly one frontmatter block and pass the evidence denylist; the formal artifact is truthful and redacted; passed appears only for the exact same-chain three-signal conjunction; all other outcomes route conservatively; lifecycle validation passes; Phase 30 is unchanged unless later phase verification authorizes promotion; focused/reference/Rust gates pass before the evidence commit; no secret, raw credential, device path, USB identity, PID, IP, endpoint, MAC, SSID, or NVS value is committed.</acceptance_criteria>
  <done>Plan 16 ends with a truthful one-shot formal terminal, complete cleanup, and no wrapper push.</done>
</task>
</tasks>

<verification>
- Exactly one qualification chain runs, and the formal chain exists only after passing qualification cleanup.
- Qualification and formal roots, handles, and product projections are distinct while exact HEAD remains identical.
- Both cold sessions obey owner-before-action removal and response-free armed restoration.
- Both captures are at least 360 seconds with at least 420 seconds of wall-clock allowance.
- Formal passed closure requires result correlation, hashing-class power rise, and accepted/rejected share in one chain.
- Private traces prove cleanup; committed artifacts are redacted and contain no secrets or local identifiers.
</verification>

<success_criteria>
- Non-invasive native USB delivers a valid cold session or closes once as invalid without retry.
- A fresh formal chain runs only after qualification authority destruction and cleanup proof.
- Closure is `passed` only for the strict three-signal same-chain gate; otherwise it is the exact truthful conservative outcome.
- No direct UART or pin manipulation occurs or is surfaced.
- The outer wrapper may push only after independent phase verification reports passed.
</success_criteria>

<threat_model>
## Assets
- User hardware, exact-head provenance, pool/Wi-Fi secrets, private USB/process identities, production evidence integrity, and Phase 30 eligibility.

## Trust Boundaries
- Human connector actions cross into a repo-owned lifecycle owner; local credentials cross only into fixed runtime consumers; raw device/process observations stay private; classifiers alone project closed public categories.

## Abuse Cases And Mitigations
- Early or false physical response: owner-observed disappearance and bounded absence gate token delivery.
- Stale qualification or cross-chain claim: one-shot destruction, fresh formal authority, same exact HEAD, distinct roots, and empty formal product projection.
- Silent USB mistaken for firmware failure: zero-byte capture is invalid and stops without retry.
- Secret/local identifier leakage: ignored 0700/0600 roots, category-only artifacts, and explicit redaction scans.
- Hardware escalation: only normal barrel/native-USB connector actions; direct UART, pins, probes, resets, scans, raw writes, and stress actions are excluded.

## Security Verification
- Validate one-shot authority consumption, owner/lease ordering, exact-head continuity, zero resource leakage, redaction sentinels, conservative terminals, and Phase 30 immutability.
</threat_model>
