---
phase: 26-telemetry-and-parity-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - crates/bitaxe-api/src/runtime_projection.rs
  - crates/bitaxe-api/src/lib.rs
  - crates/bitaxe-api/src/mining.rs
  - crates/bitaxe-api/src/statistics.rs
  - crates/bitaxe-api/src/scoreboard.rs
  - crates/bitaxe-api/src/telemetry.rs
  - crates/bitaxe-api/BUILD.bazel
autonomous: true
requirements:
  - API-11
  - API-12
  - API-13
user_setup: []
must_haves:
  truths:
    - "API view builders serialize the same projection-backed mining state for system info, statistics, scoreboard, and live WebSocket payloads."
    - "Statistics rows appear only when the projection marks a bounded runtime sample; no request-time history is fabricated."
    - "Scoreboard remains an upstream-shaped empty array unless the projection has parsed-response-backed share outcome material."
    - "WebSocket full and cadence frames do not emit stale active-mining state after safe stop."
  artifacts:
    - path: "crates/bitaxe-api/src/runtime_projection.rs"
      provides: "Pure adapter from Phase 26 projection state to API snapshot, statistics, scoreboard, and telemetry payloads."
    - path: "crates/bitaxe-api/src/statistics.rs"
      provides: "Upstream-compatible statistics response builder consuming explicit runtime sample markers."
    - path: "crates/bitaxe-api/src/scoreboard.rs"
      provides: "Upstream-compatible scoreboard response builder that remains empty without parsed-response-backed share outcome material."
    - path: "crates/bitaxe-api/src/telemetry.rs"
      provides: "Live WebSocket full/diff payload tests using projection-backed current JSON."
  key_links:
    - from: "crates/bitaxe-api/src/runtime_projection.rs"
      to: "crates/bitaxe-stratum/src/v1/telemetry_projection.rs"
      via: "Consumes `RuntimeTelemetryProjection` state rather than endpoint-local counters."
      pattern: "RuntimeTelemetryProjection"
    - from: "crates/bitaxe-api/src/runtime_projection.rs"
      to: "crates/bitaxe-api/src/statistics.rs"
      via: "Statistics response receives a drained `RuntimeProjectionSampleMarker` or an empty sample slice."
      pattern: "RuntimeProjectionSampleMarker|statistics_response"
    - from: "crates/bitaxe-api/src/runtime_projection.rs"
      to: "crates/bitaxe-api/src/telemetry.rs"
      via: "Live telemetry full/diff tests serialize the same projection-backed SystemInfoWire JSON."
      pattern: "SystemInfoWire|LiveTelemetryPlanner"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 26-2026-07-05T03-48-38
generated_at: 2026-07-05T03:57:00Z
---

<objective>
Wire the Phase 26 projection into AxeOS-compatible API and WebSocket outputs without adding unsupported public fields.

Purpose: Phase 26 decisions D-05 through D-13 and D-18 require `/api/system/info`, `/api/system/statistics`, `/api/system/scoreboard`, `/api/ws`, and `/api/ws/live` to serialize projection-derived values while preserving upstream response shapes and redaction-before-fan-out.
Output: Pure API projection adapters and tests proving no fabricated statistics, no unsupported scoreboard rows, and no stale WebSocket active-mining state after safe stop.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-telemetry-and-parity-closure/26-CONTEXT.md
@.planning/phases/26-telemetry-and-parity-closure/26-RESEARCH.md
@.planning/phases/26-telemetry-and-parity-closure/26-VALIDATION.md
@.planning/phases/26-telemetry-and-parity-closure/26-01-SUMMARY.md
@crates/bitaxe-stratum/src/v1/telemetry_projection.rs
@crates/bitaxe-api/src/snapshot.rs
@crates/bitaxe-api/src/wire.rs
@crates/bitaxe-api/src/mining.rs
@crates/bitaxe-api/src/statistics.rs
@crates/bitaxe-api/src/scoreboard.rs
@crates/bitaxe-api/src/telemetry.rs
@crates/bitaxe-api/src/websocket_state.rs
@crates/bitaxe-api/src/lib.rs
@crates/bitaxe-api/BUILD.bazel

<interfaces>
Existing contracts the executor must preserve:

- `SystemInfoWire::from_snapshot(&ApiSnapshot)` is the public `/api/system/info` mapping and must keep established AxeOS field names.
- `statistics_response(current_timestamp, maybe_columns, samples)` is the public `/api/system/statistics` builder; pass `&[]` when no projection-backed sample exists.
- `scoreboard_response(entries)` is the public `/api/system/scoreboard` builder; pass `&[]` unless a parsed-response-backed share outcome can be serialized without violating redaction rules.
- `LiveTelemetryPlanner` and `WebSocketState` already implement full-on-connect and 500 ms cadence diff semantics; Phase 26 must make the current JSON payload correct, not replace the cadence engine.
- Plan 26-01 defines `RuntimeProjectionSampleMarker`, `RuntimeProjectionSampleSource`, and `RuntimeTelemetryProjection::drain_pending_sample_marker()`. Plan 26-02 must consume only an explicit marker argument for statistics samples; it must not create samples from repeated calls with a fresh timestamp alone.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 26-02-01: Add projection-backed API view builders</name>
  <files>crates/bitaxe-api/src/runtime_projection.rs, crates/bitaxe-api/src/lib.rs, crates/bitaxe-api/src/mining.rs, crates/bitaxe-api/src/statistics.rs, crates/bitaxe-api/src/scoreboard.rs, crates/bitaxe-api/src/telemetry.rs, crates/bitaxe-api/BUILD.bazel</files>
  <read_first>
    - .planning/phases/26-telemetry-and-parity-closure/26-CONTEXT.md
    - .planning/phases/26-telemetry-and-parity-closure/26-RESEARCH.md
    - .planning/phases/26-telemetry-and-parity-closure/26-VALIDATION.md
    - .planning/phases/26-telemetry-and-parity-closure/26-01-SUMMARY.md
    - crates/bitaxe-stratum/src/v1/telemetry_projection.rs
    - crates/bitaxe-api/src/runtime_projection.rs
    - crates/bitaxe-api/src/snapshot.rs
    - crates/bitaxe-api/src/wire.rs
    - crates/bitaxe-api/src/mining.rs
    - crates/bitaxe-api/src/statistics.rs
    - crates/bitaxe-api/src/scoreboard.rs
    - crates/bitaxe-api/src/telemetry.rs
    - crates/bitaxe-api/src/lib.rs
    - crates/bitaxe-api/BUILD.bazel
  </read_first>
  <behavior>
    - Test 1: `SystemInfoWire::from_snapshot` shows accepted/rejected counters, hashrate, lifecycle, blocker, fallback, and post-stop state from the projection-backed `ApiSnapshot`.
    - Test 2: statistics response contains one bounded row only when an explicit `RuntimeProjectionSampleMarker` is supplied and remains an upstream-compatible empty statistics array when no marker exists.
    - Test 3: scoreboard response remains `[]` for blocked-safe-prerequisite, fake-pool-only, stale-generation, stopped, or missing-share-outcome projection states.
    - Test 4: live telemetry connect and cadence frames serialize the projection-backed JSON and contain no stale `"active"` mining activity after a `SafeStopped` event.
  </behavior>
  <action>
    Create `crates/bitaxe-api/src/runtime_projection.rs` per D-05, D-07, D-08, D-09, D-10, D-12, D-13, and D-18. Define `ProjectedApiViews` with fields `snapshot: ApiSnapshot`, `statistics_samples: Vec<StatisticsSample>`, `scoreboard_entries: Vec<ScoreboardEntry>`, and `telemetry_payload: serde_json::Value`.

    Implement `project_api_views(base: ApiSnapshot, projection: &RuntimeTelemetryProjection, maybe_sample_marker: Option<RuntimeProjectionSampleMarker>, timestamp_ms: u64, response_time_ms: f64) -> ProjectedApiViews`. The function must replace `base.mining` from `projection.state().clone()`, create a `StatisticsSample::from_snapshot` only when `maybe_sample_marker` is `Some(_)`, keep `statistics_samples` empty when the caller passes `None` even if `timestamp_ms` changes across repeated request-time reads, keep `scoreboard_entries` empty unless the projection exposes parsed-response-backed and redaction-allowed share outcome data, and set `telemetry_payload` with `serde_json::to_value(SystemInfoWire::from_snapshot(&snapshot))`.

    Export `pub mod runtime_projection;` and public helpers from `crates/bitaxe-api/src/lib.rs`. Add `"src/runtime_projection.rs"` to `crates/bitaxe-api/BUILD.bazel`. Keep public response fields upstream-compatible; do not add non-upstream JSON keys for source labels, evidence tiers, blocker details, or redaction metadata. Put richer source/evidence semantics in internal tests and later Phase 26 evidence artifacts.
  </action>
  <verify>
    <automated>bazel test //crates/bitaxe-api:tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg "pub struct ProjectedApiViews|pub fn project_api_views|RuntimeTelemetryProjection" crates/bitaxe-api/src/runtime_projection.rs` returns matches.
    - `rg "RuntimeProjectionSampleMarker|maybe_sample_marker|StatisticsSample::from_snapshot|scoreboard_entries: Vec<ScoreboardEntry>|serde_json::to_value\\(SystemInfoWire::from_snapshot" crates/bitaxe-api/src/runtime_projection.rs` returns matches.
    - `rg "pub mod runtime_projection;" crates/bitaxe-api/src/lib.rs` returns exactly one match.
    - `rg "\"src/runtime_projection.rs\"" crates/bitaxe-api/BUILD.bazel` returns exactly one match.
    - `rg "projection_statistics_empty_without_bounded_sample|projection_statistics_ignores_repeated_request_time_reads_without_sample_marker|projection_scoreboard_empty_without_parsed_share_outcome|projection_live_telemetry_safe_stop_not_active" crates/bitaxe-api/src/runtime_projection.rs crates/bitaxe-api/src/telemetry.rs crates/bitaxe-api/src/statistics.rs crates/bitaxe-api/src/scoreboard.rs` returns matches.
    - `rg "sourceLabel|evidenceTier|redactionStatus|poolURL|poolUser|poolPassword|device_url|raw_bm1366_frame" crates/bitaxe-api/src/runtime_projection.rs` returns no matches outside redaction-denylist tests.
  </acceptance_criteria>
  <done>API-11, API-12, and API-13 have a pure adapter layer that emits existing AxeOS wire shapes from the Phase 26 projection and refuses request-time statistics fabrication or unsupported scoreboard rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 26-02-02: Prove upstream-compatible API and WebSocket projection outputs</name>
  <files>crates/bitaxe-api/src/runtime_projection.rs, crates/bitaxe-api/src/mining.rs, crates/bitaxe-api/src/statistics.rs, crates/bitaxe-api/src/scoreboard.rs, crates/bitaxe-api/src/telemetry.rs</files>
  <read_first>
    - .planning/phases/26-telemetry-and-parity-closure/26-CONTEXT.md
    - .planning/phases/26-telemetry-and-parity-closure/26-RESEARCH.md
    - .planning/phases/26-telemetry-and-parity-closure/26-VALIDATION.md
    - .planning/phases/26-telemetry-and-parity-closure/26-01-SUMMARY.md
    - crates/bitaxe-stratum/src/v1/telemetry_projection.rs
    - crates/bitaxe-api/src/runtime_projection.rs
    - crates/bitaxe-api/src/snapshot.rs
    - crates/bitaxe-api/src/wire.rs
    - crates/bitaxe-api/src/mining.rs
    - crates/bitaxe-api/src/statistics.rs
    - crates/bitaxe-api/src/scoreboard.rs
    - crates/bitaxe-api/src/telemetry.rs
  </read_first>
  <behavior>
    - Test 1: projected `SystemInfoWire` keeps existing AxeOS-compatible public fields and reflects projection mining counters, lifecycle, hashrate inputs, fallback, blocker, and safe-stop state.
    - Test 2: repeated `project_api_views(..., None, timestamp_ms + n, ...)` calls never append statistics rows; only `Some(RuntimeProjectionSampleMarker)` yields one bounded sample.
    - Test 3: projected scoreboard remains `[]` for blocked, stopped, stale, fake-pool-only, or missing parsed-share outcome states.
    - Test 4: `LiveTelemetryPlanner` full-on-connect and 500 ms cadence diff frames use `telemetry_payload` from `ProjectedApiViews` and reject stale active-mining state after safe stop.
  </behavior>
  <action>
    Add focused Arrange/Act/Assert tests in `crates/bitaxe-api/src/runtime_projection.rs` and adjacent API modules per D-05 through D-13 and D-18. Use the Plan 26-01 projection types directly; do not introduce firmware mocks in this plan.

    The tests must prove the public wire shape remains upstream-compatible while internal evidence semantics stay out of the JSON payload. Assert that no public `sourceLabel`, `evidenceTier`, `redactionStatus`, extra pool fields, or sample-source keys are added. Use `RuntimeProjectionSampleMarker` and `RuntimeProjectionSampleSource` in tests to document that sample provenance is internal to the projection and evidence artifacts.

    Keep route and firmware shell wiring out of this plan; Plan 26-03 owns producer and firmware consumer wiring after these pure API contracts exist.
  </action>
  <verify>
    <automated>bazel test //crates/bitaxe-api:tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg "projection_system_info_preserves_axeos_fields|projection_statistics_ignores_repeated_request_time_reads_without_sample_marker|projection_scoreboard_empty_without_parsed_share_outcome|projection_live_telemetry_safe_stop_not_active" crates/bitaxe-api/src/runtime_projection.rs crates/bitaxe-api/src/telemetry.rs crates/bitaxe-api/src/statistics.rs crates/bitaxe-api/src/scoreboard.rs` returns matches.
    - `rg "sourceLabel|evidenceTier|redactionStatus|poolURL|poolUser|poolPassword|device_url|raw_bm1366_frame" crates/bitaxe-api/src/runtime_projection.rs crates/bitaxe-api/src/telemetry.rs crates/bitaxe-api/src/statistics.rs crates/bitaxe-api/src/scoreboard.rs` returns no matches outside redaction-denylist tests.
    - `rg "RuntimeProjectionSampleMarker|RuntimeProjectionSampleSource|maybe_sample_marker" crates/bitaxe-api/src/runtime_projection.rs` returns matches in implementation and tests.
    - `bazel test //crates/bitaxe-api:tests` passes.
  </acceptance_criteria>
  <done>Pure API projection tests prove upstream-compatible serialization, internal-only sample provenance, no fabricated request-time statistics, conservative scoreboard output, and safe-stop WebSocket payload correctness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Projection core -> API DTOs | Internal mining state becomes public HTTP JSON and WebSocket JSON. |
| Sample marker -> statistics response | Runtime event provenance determines whether a public statistics row is emitted. |
| API DTOs -> downstream firmware routes | Pure projection views become the contract Plan 26-03 route shells must consume. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-26-05 | Information Disclosure | `project_api_views` and WebSocket payloads | mitigate | Serialize only upstream-compatible DTOs and test denylisted pool, target, extranonce, share, device, Wi-Fi, NVS, token, and raw ASIC values. |
| T-26-06 | Tampering | Statistics sample materialization | mitigate | Require an explicit `RuntimeProjectionSampleMarker`; repeated request-time reads with changing timestamps must keep statistics samples empty. |
| T-26-07 | Tampering | Scoreboard view builder | mitigate | Keep scoreboard entries empty unless parsed-response-backed, redaction-allowed share outcome material exists. |
| T-26-08 | Repudiation | `/api/ws/live` safe-stop payloads | mitigate | Test full-on-connect and cadence diff payloads after safe stop so no stale active-mining payload can be claimed as current. |
</threat_model>

<verification>
Run `bazel test //crates/bitaxe-api:tests` after pure API adapter changes. Firmware producer and route compilation is intentionally deferred to Plan 26-03 to keep this plan scoped to pure API contracts.
</verification>

<success_criteria>
Plan 26-02 is complete when API pure tests pass, the projection adapter exposes upstream-compatible system info/statistics/scoreboard/live telemetry views, statistics samples require a drained runtime-event sample marker, scoreboard output remains conservative when evidence is absent, and WebSocket full/diff payload tests cannot emit stale active mining after safe stop.
</success_criteria>

<output>
After completion, create `.planning/phases/26-telemetry-and-parity-closure/26-02-SUMMARY.md`.
</output>
