---
phase: 26-telemetry-and-parity-closure
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - firmware/bitaxe/src/live_stratum_runtime.rs
  - firmware/bitaxe/src/runtime_snapshot.rs
  - firmware/bitaxe/src/http_api.rs
  - firmware/bitaxe/src/websocket_api.rs
autonomous: true
requirements:
  - API-11
  - API-12
  - API-13
user_setup: []
must_haves:
  truths:
    - "Phase 25 runtime producers fold lifecycle, hashrate, submit classification, blocked prerequisite, and safe-stop events into RuntimeTelemetryProjection."
    - "Firmware HTTP and WebSocket consumers serialize projection-backed views through the Plan 26-02 API contract."
    - "Bounded statistics samples are drained from runtime-event sample markers and are not created by repeated route reads."
    - "Safe-stop producer events reset active mining before HTTP or WebSocket serialization."
  artifacts:
    - path: "firmware/bitaxe/src/live_stratum_runtime.rs"
      provides: "Phase 25 runtime producer wiring into `RuntimeTelemetryProjection::fold` for lifecycle, submit classification, blocked prerequisite, and safe-stop events."
    - path: "firmware/bitaxe/src/runtime_snapshot.rs"
      provides: "Firmware bridge holding projection state, draining sample markers, and exposing projected API view helpers."
    - path: "firmware/bitaxe/src/http_api.rs"
      provides: "HTTP handlers consuming projected system info, statistics, and scoreboard views."
    - path: "firmware/bitaxe/src/websocket_api.rs"
      provides: "WebSocket bridge preserving route/session semantics while using projection-backed live payloads."
  key_links:
    - from: "firmware/bitaxe/src/live_stratum_runtime.rs"
      to: "crates/bitaxe-stratum/src/v1/telemetry_projection.rs"
      via: "Phase 25 lifecycle, hashrate, blocked prerequisite, submit classification, and safe-stop producers call `RuntimeTelemetryProjection::fold`."
      pattern: "RuntimeTelemetryEvent|fold"
    - from: "firmware/bitaxe/src/runtime_snapshot.rs"
      to: "crates/bitaxe-stratum/src/v1/telemetry_projection.rs"
      via: "Projection state is stored with generation/sequence metadata and drains `RuntimeProjectionSampleMarker` only once per runtime boundary."
      pattern: "RuntimeTelemetryProjection|drain_pending_sample_marker|RuntimeProjectionSampleMarker"
    - from: "firmware/bitaxe/src/runtime_snapshot.rs"
      to: "crates/bitaxe-api/src/runtime_projection.rs"
      via: "Snapshot helpers call `project_api_views` with the drained marker and current projection state."
      pattern: "project_api_views"
    - from: "firmware/bitaxe/src/http_api.rs"
      to: "firmware/bitaxe/src/runtime_snapshot.rs"
      via: "Handlers call projection-backed helpers before serializing `/api/system/info`, statistics, scoreboard, and live WebSocket payloads."
      pattern: "collect_projected_api_views|projected_system_info|projected_statistics|projected_scoreboard|projected_live_telemetry_payload"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 26-2026-07-05T03-48-38
generated_at: 2026-07-05T04:15:00Z
---

<objective>
Wire Phase 25 runtime event producers and firmware route consumers into the Phase 26 projection.

Purpose: Checker feedback identified that Plan 26-02 consumed projection-backed views without a task wiring runtime producers into `RuntimeTelemetryProjection::fold`. This plan closes that gap per D-01, D-03, D-04, D-06, D-10, D-13, and D-18 while keeping Plan 26-02 scoped to pure API contracts.
Output: Firmware producer wiring, projected snapshot helpers, HTTP/WebSocket consumer wiring, and compile/test evidence that runtime events reach the shared projection before serialization.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-telemetry-and-parity-closure/26-CONTEXT.md
@.planning/phases/26-telemetry-and-parity-closure/26-RESEARCH.md
@.planning/phases/26-telemetry-and-parity-closure/26-VALIDATION.md
@.planning/phases/26-telemetry-and-parity-closure/26-01-SUMMARY.md
@.planning/phases/26-telemetry-and-parity-closure/26-02-SUMMARY.md
@crates/bitaxe-stratum/src/v1/telemetry_projection.rs
@crates/bitaxe-stratum/src/v1/live_runtime.rs
@crates/bitaxe-stratum/src/v1/production_work.rs
@crates/bitaxe-stratum/src/v1/submit_response.rs
@crates/bitaxe-api/src/runtime_projection.rs
@crates/bitaxe-api/src/telemetry.rs
@firmware/bitaxe/src/live_stratum_runtime.rs
@firmware/bitaxe/src/runtime_snapshot.rs
@firmware/bitaxe/src/http_api.rs
@firmware/bitaxe/src/websocket_api.rs

<interfaces>
Existing contracts the executor must preserve:

- Plan 26-01 creates `RuntimeTelemetryProjection`, `RuntimeTelemetryEvent`, `RuntimeTelemetrySequence`, `RuntimeProjectionSampleMarker`, `RuntimeProjectionSampleSource`, and `RuntimeTelemetryProjection::drain_pending_sample_marker()`.
- Plan 26-02 creates `ProjectedApiViews` and `project_api_views(base, projection, maybe_sample_marker, timestamp_ms, response_time_ms)`.
- `firmware/bitaxe/src/live_stratum_runtime.rs` is the Phase 25 firmware shell that already observes Stratum lifecycle, submit classification, blocked prerequisites, and safe-stop publication.
- `firmware/bitaxe/src/runtime_snapshot.rs` is the existing command-visible API snapshot bridge; this plan must keep it a thin adapter around the pure projection and API view builders.
- `LiveTelemetryPlanner` and `WebSocketState` already own full-on-connect, 500 ms diff cadence, max-client, unregister, and hibernation semantics. This plan must feed them the projection-backed current JSON, not replace their session logic.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 26-03-01: Wire Phase 25 runtime producers into projection fold</name>
  <files>firmware/bitaxe/src/live_stratum_runtime.rs, firmware/bitaxe/src/runtime_snapshot.rs</files>
  <read_first>
    - .planning/phases/26-telemetry-and-parity-closure/26-CONTEXT.md
    - .planning/phases/26-telemetry-and-parity-closure/26-RESEARCH.md
    - .planning/phases/26-telemetry-and-parity-closure/26-01-SUMMARY.md
    - crates/bitaxe-stratum/src/v1/telemetry_projection.rs
    - crates/bitaxe-stratum/src/v1/live_runtime.rs
    - crates/bitaxe-stratum/src/v1/production_work.rs
    - crates/bitaxe-stratum/src/v1/submit_response.rs
    - firmware/bitaxe/src/live_stratum_runtime.rs
    - firmware/bitaxe/src/runtime_snapshot.rs
  </read_first>
  <behavior>
    - Test 1: live runtime lifecycle transitions fold `RuntimeTelemetryEvent::LifecycleChanged` and do not mutate share counters.
    - Test 2: hashrate input and pool difficulty observations fold `HashrateObserved`, `PoolDifficultyObserved`, and `BoundedSampleReady` events so statistics samples can be emitted only at runtime event boundaries.
    - Test 3: submit response classification folds `SubmitClassified` with current `PoolSessionGeneration`; accepted/rejected counters do not advance for missing intent, stale generation, blocked prerequisite, stopped socket, timeout, malformed, reconnect, no observed share, or fake-pool-only observations.
    - Test 4: safe-stop publication folds `Blocked` and `SafeStopped` events before any API/WebSocket serialization helper can observe active mining.
  </behavior>
  <action>
    Update `firmware/bitaxe/src/runtime_snapshot.rs` per D-01, D-03, D-04, D-07, D-12, and D-13 to store the Phase 26 `RuntimeTelemetryProjection` alongside existing command-visible state, guarded by the same concurrency boundary used for runtime snapshots. Use monotonic `RuntimeTelemetrySequence` values generated by the firmware shell; do not derive sequence from wall-clock time alone.

    Update `firmware/bitaxe/src/live_stratum_runtime.rs` so Phase 25 runtime producer sites call a narrow helper such as `publish_runtime_telemetry_event(RuntimeTelemetryEvent)` or `with_runtime_projection(|projection| projection.fold(event))`. Wire producer events for submit classification, lifecycle/connection status, hashrate inputs, pool difficulty, blocked prerequisite reasons, and safe-stop postconditions. Each event must carry the current `PoolSessionGeneration` or monotonic sequence metadata required by Plan 26-01.

    For statistics sampling, fold `RuntimeTelemetryEvent::BoundedSampleReady` only when a runtime producer observes a real boundary such as lifecycle update, hashrate input update, submit classification, blocked prerequisite, or safe-stop. Do not create markers in HTTP handlers, WebSocket handlers, repeated polling loops, or request-time timestamp reads. Preserve redaction rules: no pool URLs, ports, workers, owner addresses, passwords, targets, extranonces, share payloads, socket errors, device URLs, IP addresses, MAC addresses, Wi-Fi values, NVS secrets, tokens, raw Stratum payloads, or raw BM1366 frames may be stored in projection debug/log values or committed tests.
  </action>
  <verify>
    <automated>bazel test //crates/bitaxe-stratum:tests //crates/bitaxe-api:tests && bazel build //firmware/bitaxe:firmware</automated>
  </verify>
  <acceptance_criteria>
    - `rg "RuntimeTelemetryEvent|RuntimeTelemetryProjection|fold\\(" firmware/bitaxe/src/live_stratum_runtime.rs firmware/bitaxe/src/runtime_snapshot.rs` returns matches.
    - `rg "SubmitClassified|LifecycleChanged|HashrateObserved|PoolDifficultyObserved|Blocked|SafeStopped|BoundedSampleReady" firmware/bitaxe/src/live_stratum_runtime.rs firmware/bitaxe/src/runtime_snapshot.rs` returns matches.
    - `rg "RuntimeProjectionSampleMarker|drain_pending_sample_marker|request-time|request_time" firmware/bitaxe/src/runtime_snapshot.rs firmware/bitaxe/src/live_stratum_runtime.rs` returns matches in code or tests proving samples are producer-boundary only.
    - `rg "record_accepted_share|record_rejected_share" firmware/bitaxe/src/live_stratum_runtime.rs firmware/bitaxe/src/runtime_snapshot.rs` returns no matches outside legacy tests; production-visible counter changes must flow through `SubmitClassified`.
    - `rg "poolURL|poolPort|poolUser|poolPassword|device_url|target=|extranonce=|share_payload|socket_error|raw_bm1366_frame|token=" firmware/bitaxe/src/live_stratum_runtime.rs firmware/bitaxe/src/runtime_snapshot.rs` returns no raw secret-bearing values outside denylist tests or comments.
    - `bazel test //crates/bitaxe-stratum:tests //crates/bitaxe-api:tests && bazel build //firmware/bitaxe:firmware` passes or an exact ESP-IDF/toolchain blocker is recorded in the plan summary.
  </acceptance_criteria>
  <done>Phase 25 runtime producer events reach `RuntimeTelemetryProjection::fold` with generation/sequence metadata, counter gates, bounded sample markers, blocked prerequisites, and safe-stop reset semantics before serialization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 26-03-02: Wire firmware HTTP and WebSocket consumers</name>
  <files>firmware/bitaxe/src/runtime_snapshot.rs, firmware/bitaxe/src/http_api.rs, firmware/bitaxe/src/websocket_api.rs</files>
  <read_first>
    - .planning/phases/26-telemetry-and-parity-closure/26-CONTEXT.md
    - .planning/phases/26-telemetry-and-parity-closure/26-RESEARCH.md
    - .planning/phases/26-telemetry-and-parity-closure/26-02-SUMMARY.md
    - crates/bitaxe-api/src/runtime_projection.rs
    - crates/bitaxe-api/src/telemetry.rs
    - firmware/bitaxe/src/runtime_snapshot.rs
    - firmware/bitaxe/src/http_api.rs
    - firmware/bitaxe/src/websocket_api.rs
  </read_first>
  <behavior>
    - Test 1: `/api/system/info` builds `SystemInfoWire` from `ProjectedApiViews.snapshot` and does not invent endpoint-local mining counters.
    - Test 2: `/api/system/statistics` drains a pending `RuntimeProjectionSampleMarker` through the snapshot helper and does not call `empty_statistics_response(timestamp_ms, None)` directly from the route as the only behavior.
    - Test 3: `/api/system/scoreboard` calls a projection-backed helper and returns `scoreboard_response(&[])` only as the conservative result from projected views when no parsed-response-backed share outcome exists.
    - Test 4: `/api/ws/live` connect and cadence paths serialize `ProjectedApiViews.telemetry_payload` from the same helper as HTTP API routes, and no stale active-mining frame is emitted after safe stop.
  </behavior>
  <action>
    Add concrete helpers in `firmware/bitaxe/src/runtime_snapshot.rs`: `collect_projected_api_views(timestamp_ms: u64, response_time_ms: f64) -> bitaxe_api::ProjectedApiViews`, `projected_system_info(timestamp_ms: u64)`, `projected_statistics(timestamp_ms: u64)`, `projected_scoreboard(timestamp_ms: u64)`, and `projected_live_telemetry_payload(timestamp_ms: u64)`. These helpers must call Plan 26-01's sample-marker drain exactly once per runtime boundary and pass the resulting marker into Plan 26-02's `project_api_views`.

    Update `firmware/bitaxe/src/http_api.rs` so `handle_system_info`, `handle_statistics`, `handle_scoreboard`, `websocket_live_handler`, and `broadcast_live_telemetry_cadence` consume those projection-backed helpers. Preserve `handle_with_access_gate`, `plan_websocket_upgrade`, route paths, raw `/api/ws` log stream compatibility, and the 500 ms cadence task. Do not add hardware probing, target discovery, network scans, raw credential reads, raw pool values, or new public API keys.

    Update `firmware/bitaxe/src/websocket_api.rs` only if needed to pass projection-backed payloads through existing session state. Preserve `/api/ws` raw retained-log stream compatibility; if adding lifecycle markers, they must be redaction-safe labels emitted from the projection, not raw pool/share/network context.
  </action>
  <verify>
    <automated>bazel test //crates/bitaxe-api:tests && bazel build //firmware/bitaxe:firmware</automated>
  </verify>
  <acceptance_criteria>
    - `rg "collect_projected_api_views|projected_system_info|projected_statistics|projected_scoreboard|projected_live_telemetry_payload" firmware/bitaxe/src/runtime_snapshot.rs firmware/bitaxe/src/http_api.rs` returns matches.
    - `rg "drain_pending_sample_marker|project_api_views" firmware/bitaxe/src/runtime_snapshot.rs` returns matches.
    - `rg "empty_statistics_response\\(timestamp_ms, None\\)" firmware/bitaxe/src/http_api.rs` returns no direct route-handler match unless it is behind the projection-backed helper contract.
    - `rg "scoreboard_response\\(&\\[\\]\\)" firmware/bitaxe/src/http_api.rs` returns no direct route-handler match unless it is the conservative output from projected views.
    - `rg "projected_live_telemetry_payload|live_connect_frame|live_cadence_frame" firmware/bitaxe/src/http_api.rs firmware/bitaxe/src/websocket_api.rs` returns matches showing WebSocket payloads are planned from projection-backed current JSON.
    - `rg "phase25_safe_stop|safe_blocked|work_submission.*blocked|blockedReason" firmware/bitaxe/src/runtime_snapshot.rs firmware/bitaxe/src/http_api.rs crates/bitaxe-api/src/runtime_projection.rs` returns matches in code or tests.
    - `bazel test //crates/bitaxe-api:tests && bazel build //firmware/bitaxe:firmware` passes or an exact ESP-IDF/toolchain blocker is recorded in the plan summary.
  </acceptance_criteria>
  <done>HTTP and WebSocket firmware shells serialize the Phase 26 projection through existing AxeOS-compatible DTOs, and no route-local counter/statistics/scoreboard rule can bypass the projection gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Firmware runtime producers -> projection state | ESP-IDF socket, ASIC, lifecycle, hashrate, blocked prerequisite, and safe-stop observations enter the typed projection fold. |
| Projection state -> HTTP/WebSocket route helpers | Projection-backed API views become public JSON and retained telemetry. |
| Runtime snapshot mutex -> serialization | Shared firmware state is cloned and serialized across concurrent HTTP/WebSocket handlers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-26-09 | Tampering | `live_stratum_runtime` producer wiring | mitigate | Fold only typed events carrying monotonic sequence and generation metadata; do not mutate production-visible counters directly. |
| T-26-10 | Repudiation | Sample marker producer boundary | mitigate | Create `BoundedSampleReady` markers only from runtime event boundaries and drain them once through snapshot helpers. |
| T-26-11 | Information Disclosure | Firmware projection/log labels | mitigate | Preserve redaction denylist for pool, share, socket, device, Wi-Fi, NVS, token, and raw BM1366 values before storage or fan-out. |
| T-26-12 | Repudiation | HTTP/WebSocket safe-stop serialization | mitigate | Route `/api/system/info`, statistics, scoreboard, and `/api/ws/live` through the same projection-backed helpers after safe-stop fold. |
</threat_model>

<verification>
Run `bazel test //crates/bitaxe-stratum:tests //crates/bitaxe-api:tests` after producer wiring, then `bazel build //firmware/bitaxe:firmware` because firmware route and runtime files change. If firmware build is blocked by local ESP-IDF/toolchain prerequisites, record the exact blocker in the plan summary and keep API/WebSocket hardware claims below verified.
</verification>

<success_criteria>
Plan 26-03 is complete when Phase 25 runtime lifecycle, hashrate, submit classification, blocked prerequisite, and safe-stop producers fold into `RuntimeTelemetryProjection`; firmware route helpers drain bounded sample markers only from runtime boundaries; `/api/system/info`, `/api/system/statistics`, `/api/system/scoreboard`, `/api/ws`, and `/api/ws/live` consume projection-backed views; and firmware compile or an exact local blocker is recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/26-telemetry-and-parity-closure/26-03-SUMMARY.md`.
</output>
