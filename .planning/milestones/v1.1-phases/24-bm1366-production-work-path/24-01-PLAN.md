---
phase: 24-bm1366-production-work-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/bitaxe-asic/src/bm1366.rs
  - crates/bitaxe-asic/src/bm1366/production.rs
  - crates/bitaxe-asic/BUILD.bazel
autonomous: true
requirements: [ASIC-09, ASIC-12]
user_setup: []
must_haves:
  truths:
    - "Diagnostic BM1366 work and trusted production BM1366 work use distinct Rust types."
    - "Production BM1366 commands can emit adapter actions without firmware constructing raw frames."
    - "Production ASIC failures render stable redaction-safe category labels."
  artifacts:
    - path: "crates/bitaxe-asic/src/bm1366/production.rs"
      provides: "production BM1366 command, status, payload, and blocker taxonomy"
    - path: "crates/bitaxe-asic/src/bm1366.rs"
      provides: "public production module export"
    - path: "crates/bitaxe-asic/BUILD.bazel"
      provides: "Bazel source registration for the production module"
  key_links:
    - from: "crates/bitaxe-asic/src/bm1366/production.rs"
      to: "crates/bitaxe-asic/src/bm1366/work.rs"
      via: "ProductionWorkPayload wraps Bm1366WorkPayload"
      pattern: "ProductionWorkPayload.*Bm1366WorkPayload"
    - from: "crates/bitaxe-asic/src/bm1366/production.rs"
      to: "crates/bitaxe-asic/src/bm1366/command.rs"
      via: "Bm1366ProductionCommand emits Bm1366AdapterAction"
      pattern: "Bm1366ProductionCommand|Bm1366AdapterAction"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 24-2026-07-05T00-27-27
generated_at: 2026-07-05T00:33:00Z
---

<objective>
Create the type-level BM1366 production boundary required before Stratum work can dispatch trusted work.

Purpose: Phase 24 cannot safely correlate production work if diagnostic payloads, diagnostic statuses, and trusted production actions remain interchangeable.

Output: `crates/bitaxe-asic` exports a production module with production-only work payloads, commands, statuses, and redaction-safe blocker reasons.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-bm1366-production-work-path/24-CONTEXT.md
@.planning/phases/24-bm1366-production-work-path/24-RESEARCH.md
@.planning/phases/24-bm1366-production-work-path/24-VALIDATION.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md

<interfaces>
From `crates/bitaxe-asic/src/bm1366/command.rs`:
```rust
pub enum Bm1366Command {
    SendDiagnosticWork(Bm1366WorkPayload),
}

pub enum Bm1366AdapterAction {
    WriteFrame(FrameBytes),
    ReadExact { len: usize, timeout_ms: u32 },
    PublishStatus(AsicInitStatus),
}
```

From `crates/bitaxe-asic/src/bm1366/work.rs`:
```rust
pub struct Bm1366JobId { /* private */ }
pub struct Bm1366WorkFields { /* public fixed work fields */ }
pub struct Bm1366WorkPayload { /* private fixed payload bytes */ }
```

From `crates/bitaxe-asic/src/bm1366/result.rs`:
```rust
pub struct Bm1366NonceResult {
    pub job_id: Bm1366JobId,
    pub nonce: u32,
    pub asic_index: u8,
    pub core_id: u8,
    pub small_core_id: u8,
    pub version_bits: u32,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 24-01-01: Add BM1366 production primitives</name>
  <read_first>
    - `.planning/phases/24-bm1366-production-work-path/24-CONTEXT.md`
    - `.planning/phases/24-bm1366-production-work-path/24-RESEARCH.md`
    - `crates/bitaxe-asic/src/bm1366.rs`
    - `crates/bitaxe-asic/src/bm1366/command.rs`
    - `crates/bitaxe-asic/src/bm1366/work.rs`
    - `crates/bitaxe-asic/src/bm1366/result.rs`
    - `crates/bitaxe-asic/BUILD.bazel`
  </read_first>
  <files>crates/bitaxe-asic/src/bm1366/production.rs</files>
  <behavior>
    - Test 1: `Bm1366ProductionCommand::SendProductionWork` emits a `Bm1366AdapterAction::WriteFrame` for a production payload.
    - Test 2: `Bm1366ProductionCommand::ReadProductionResult` emits `Bm1366AdapterAction::READ_RESULT_FRAME`.
    - Test 3: every `ProductionAsicBlocker::as_str()` value is lower-case snake-case and contains no raw frame, pool, target, endpoint, or credential text.
  </behavior>
  <action>Create `crates/bitaxe-asic/src/bm1366/production.rs` per D-01, D-03, D-10, and D-11. Define `ProductionWorkPayload { job_id: Bm1366JobId, payload: Bm1366WorkPayload }` with `new(job_id: Bm1366JobId, fields: Bm1366WorkFields)`, `job_id()`, and `payload()` accessors. Define `Bm1366ProductionCommand` with exactly `SendProductionWork(ProductionWorkPayload)` and `ReadProductionResult`. Its `adapter_actions()` must return `Vec<Bm1366AdapterAction>` and must construct BM1366 job frames inside `crates/bitaxe-asic`, not in firmware. Define `ProductionAsicBlocker` with exact variants and labels: `PrerequisiteBlocked => "production_prerequisite_blocked"`, `AsicInitFailed => "production_asic_init_failed"`, `UartFailed => "production_uart_failed"`, `ResetFailed => "production_reset_failed"`, `ResultTimeout => "production_result_timeout"`, `ResultMalformed => "production_result_malformed"`, `WorkStale => "production_work_stale"`, `JobUncorrelated => "production_job_uncorrelated"`, `DuplicateResult => "production_duplicate_result"`, `WrongSession => "production_wrong_session"`, and `TargetMismatch => "production_target_mismatch"`. Define `ProductionAsicStatus` with exact variants `InitializedForProduction`, `WorkDispatched`, `ResultCorrelated`, and `FailClosed { reason: ProductionAsicBlocker }`, plus `as_str()`/`reason()` accessors suitable for redaction-safe logs.</action>
  <verify>
    <automated>bazel test //crates/bitaxe-asic:tests</automated>
    <automated>! rg -n "SendDiagnosticWork|raw_bm1366_frame|target=|extranonce=|share_payload=|pool_config|device_url|password|token" crates/bitaxe-asic/src/bm1366/production.rs</automated>
  </verify>
  <acceptance_criteria>
    - `rg "pub enum Bm1366ProductionCommand|SendProductionWork|ReadProductionResult" crates/bitaxe-asic/src/bm1366/production.rs` finds all three names.
    - `rg "pub enum ProductionAsicBlocker|production_result_timeout|production_job_uncorrelated|production_target_mismatch" crates/bitaxe-asic/src/bm1366/production.rs` finds all listed labels.
    - `rg "pub enum ProductionAsicStatus|InitializedForProduction|WorkDispatched|ResultCorrelated|FailClosed" crates/bitaxe-asic/src/bm1366/production.rs` finds all listed variants.
    - `! rg -n "SendDiagnosticWork" crates/bitaxe-asic/src/bm1366/production.rs` succeeds.
  </acceptance_criteria>
  <done>Production BM1366 primitives exist in `bitaxe-asic`, are unit-tested, and cannot be confused with diagnostic `SendDiagnosticWork` by name or type.</done>
</task>

<task type="auto">
  <name>Task 24-01-02: Export production module through Bazel and Rust module roots</name>
  <read_first>
    - `crates/bitaxe-asic/src/bm1366.rs`
    - `crates/bitaxe-asic/src/bm1366/production.rs`
    - `crates/bitaxe-asic/BUILD.bazel`
  </read_first>
  <files>crates/bitaxe-asic/src/bm1366.rs, crates/bitaxe-asic/BUILD.bazel</files>
  <action>Export the new module by adding `pub mod production;` to `crates/bitaxe-asic/src/bm1366.rs`. Add `"src/bm1366/production.rs"` to the `srcs` list in `crates/bitaxe-asic/BUILD.bazel`. Keep the existing `foo.rs` plus `foo/` module layout per Rust standards. Do not modify `reference/esp-miner`.</action>
  <verify>
    <automated>bazel test //crates/bitaxe-asic:tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg "pub mod production;" crates/bitaxe-asic/src/bm1366.rs` finds exactly one export.
    - `rg "\"src/bm1366/production.rs\"" crates/bitaxe-asic/BUILD.bazel` finds the Bazel source entry.
    - `bazel test //crates/bitaxe-asic:tests` passes.
  </acceptance_criteria>
  <done>The production module is reachable to downstream crates and included in the canonical Bazel test target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| diagnostic BM1366 mode -> production BM1366 mode | Existing diagnostic work must not become production evidence by type reuse. |
| ASIC core -> firmware shell | Firmware may interpret adapter actions but must not construct raw BM1366 frames. |
| failure categories -> logs/evidence | Failures cross into committed evidence only as redaction-safe labels. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-24-01 | Spoofing | `Bm1366ProductionCommand` | mitigate | Production work uses production-only command types; `SendDiagnosticWork` is absent from `production.rs`. |
| T-24-02 | Tampering | BM1366 frame construction | mitigate | Frame construction stays in `crates/bitaxe-asic`; firmware receives adapter actions only. |
| T-24-03 | Information Disclosure | production blocker labels | mitigate | `ProductionAsicBlocker::as_str()` returns stable labels only and tests reject sensitive sentinel text. |
| T-24-04 | Repudiation | production status labels | mitigate | `ProductionAsicStatus` has explicit initialized, dispatched, correlated, and fail-closed states for later evidence. |
</threat_model>

<verification>
Run `bazel test //crates/bitaxe-asic:tests` and the static `rg` checks in each task. Do not run hardware in this plan.
</verification>

<success_criteria>
`crates/bitaxe-asic` has a tested, Bazel-registered production BM1366 type boundary that satisfies ASIC-09 and the production failure taxonomy portion of ASIC-12 without exposing raw frames or secrets.
</success_criteria>

<output>
After completion, create `.planning/phases/24-bm1366-production-work-path/24-01-SUMMARY.md`.
</output>
