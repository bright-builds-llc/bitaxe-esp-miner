---
phase: 28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase28.1.1.2-result-path-compare.mjs
  - scripts/phase28.1.1.2-result-path-compare-test.mjs
autonomous: true
requirements: [STR-09, CFG-07, ASIC-11]
user_setup: []
must_haves:
  truths:
    - "A repo-owned result-path comparator emits D-05 count/boolean fields without raw UART hex or credentials"
    - "When upstream chip_id_read_tx greatly exceeds Rust and Rust has job_tx + result_read_attempt without result_correlated, recommended_investigation is match_upstream_register_read_poll"
    - "The recommender never defaults to post_max_baud_delay_2000 for that gap shape"
  artifacts:
    - path: "scripts/phase28.1.1.2-result-path-compare.mjs"
      provides: "Redaction-safe result-path counts/order report composing 28.1.1.1 parsers"
      contains: "match_upstream_register_read_poll"
    - path: "scripts/phase28.1.1.2-result-path-compare-test.mjs"
      provides: "Fixture tests for D-05 fields, recommender, and redaction sentinels"
      contains: "result_correlated"
  key_links:
    - from: "scripts/phase28.1.1.2-result-path-compare.mjs"
      to: "scripts/phase28.1.1.1-below-job-byte-sequence-compare.mjs"
      via: "compose/reuse lineEvents classifyFrame summarizeLog"
      pattern: "chip_id_read_tx|result_read_attempt|fake_pool_submit"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1.2-2026-07-09T02-18-49
generated_at: 2026-07-09T02:27:00Z
---

<objective>
Ship Wave 0 redaction-safe result-path comparator and fixture tests so upstream-vs-Rust RX/poll asymmetry is first-class evidence before any firmware A/B or patch.

Purpose: Phase 28.1.1.1 proved job fields 11/11 and below-job events 12/12 while Rust still lacks `result_correlated` and fake-pool submit. D-04/D-05 require a counts/order comparator before patches (D-09).
Output: `scripts/phase28.1.1.2-result-path-compare.mjs` + `scripts/phase28.1.1.2-result-path-compare-test.mjs`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-CONTEXT.md
@.planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-RESEARCH.md
@.planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-VALIDATION.md
@scripts/phase28.1.1.1-below-job-byte-sequence-compare.mjs
@scripts/phase28.1.1.1-below-job-byte-sequence-compare-test.mjs

<interfaces>
<!-- Executor should reuse these event names from the 28.1.1.1 comparator — do not invent parallel taxonomies. -->

From scripts/phase28.1.1.1-below-job-byte-sequence-compare.mjs:
- Events already classified: `chip_id_read_tx`, `result_read_attempt`, `rx_result_frame`, `register_read_parsed`, `result_correlated`, `fake_pool_submit`, `job_tx`, …
- CLI shape: `--upstream <log> --rust <log> --out <report.md>`
- Pitfall: old `recommendInvestigation` returns `post_max_baud_delay_2000` for jobs+result_read+no correlate — **must not** be copied into the 28.1.1.2 recommender (D-01 falsified that A/B).

D-05 required report fields (exact keys):
- `chip_id_read_tx` (upstream_count / rust_count)
- `result_read_attempt` (upstream_count / rust_count; label upstream `0` as `expected_no_marker` in prose, not as missing RX)
- `rx_result_frame` (or nonce-class RX count)
- `register_read_parsed`
- `result_correlated` (boolean derived from status markers)
- `fake_pool_submit_observed` (boolean)
- `recommended_investigation` (closed enum)
- `raw_bytes_committed: false`
- `credential_contents_read: false`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement result-path comparator CLI</name>
  <files>scripts/phase28.1.1.2-result-path-compare.mjs</files>
  <read_first>
    - scripts/phase28.1.1.1-below-job-byte-sequence-compare.mjs
    - .planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-CONTEXT.md (D-04, D-05, D-06)
    - .planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-RESEARCH.md (Pattern 1, Pitfalls 1/2/5)
  </read_first>
  <behavior>
    - Missing upstream or rust log → `comparison_status: blocked_safe_prerequisite`
    - Fixture with upstream chip_id_read_tx≫rust, rust job_tx>0, result_read_attempt>0, result_correlated=0 → `recommended_investigation: match_upstream_register_read_poll`
    - Same gap shape must NOT recommend `post_max_baud_delay_2000`
    - Report Markdown contains D-05 keys and never contains multi-byte hex frame dumps or credential sentinels
  </behavior>
  <action>
    Create `scripts/phase28.1.1.2-result-path-compare.mjs` (Node ESM, stdlib only) accepting `--upstream`, `--rust`, `--out` (per D-06 compose with 28.1.1.1 — reuse/import or carefully duplicate `lineEvents`/`classifyFrame`/`summarizeLog` from `scripts/phase28.1.1.1-below-job-byte-sequence-compare.mjs`; do not invent a parallel capture stack).

    Emit a Markdown report with at least these exact fields (D-05):
    - `chip_id_read_tx_upstream` / `chip_id_read_tx_rust` (integer counts)
    - `result_read_attempt_upstream` / `result_read_attempt_rust`
    - `rx_result_frame_upstream` / `rx_result_frame_rust`
    - `register_read_parsed_upstream` / `register_read_parsed_rust`
    - `result_correlated: true|false` (true iff rust log contains `asic_production_status=result_correlated` or event `result_correlated`)
    - `fake_pool_submit_observed: true|false` (true iff fake-pool submit event/marker present in either log or companion report path if passed; default from log event `fake_pool_submit`)
    - `recommended_investigation:` one of `match_upstream_register_read_poll` | `result_rx_acquisition_model` | `asic_enable_power_sequencing` | `none` | (blocked → `none`)
    - `raw_bytes_committed: false`
    - `credential_contents_read: false`
    - `phase30_promotion_input: pending` (never claim verified promotion; D-15)

    Recommender rules (D-07/D-08, RESEARCH Pitfall 5):
    1. If blocked → `none`
    2. Else if `chip_id_read_tx_upstream >= 10 * max(chip_id_read_tx_rust, 1)` AND rust `job_tx > 0` AND rust `result_correlated == false` → `match_upstream_register_read_poll`
    3. Else if rust `job_tx > 0` AND rust `result_read_attempt > 0` AND rust `result_correlated == false` AND rust `partial_frame` count is high (if countable) → `result_rx_acquisition_model`
    4. Else if rust still gapped after those → `asic_enable_power_sequencing` only when enable/power markers are missing; otherwise `none`
    5. **Never** return `post_max_baud_delay_2000` from this script

    Do not write raw UART hex frames into `--out`. Counts and booleans only.
  </action>
  <acceptance_criteria>
    - `test -f scripts/phase28.1.1.2-result-path-compare.mjs`
    - `rg -n "match_upstream_register_read_poll" scripts/phase28.1.1.2-result-path-compare.mjs`
    - `rg -n "post_max_baud_delay_2000" scripts/phase28.1.1.2-result-path-compare.mjs` returns exit 1 (no matches) OR only appears inside a comment/string that rejects it — prefer zero matches
    - `rg -n "raw_bytes_committed: false" scripts/phase28.1.1.2-result-path-compare.mjs`
    - `node scripts/phase28.1.1.2-result-path-compare.mjs --help >/dev/null`
  </acceptance_criteria>
  <verify>
    <automated>node scripts/phase28.1.1.2-result-path-compare.mjs --help >/dev/null</automated>
  </verify>
  <done>Comparator CLI exists, documents usage, and encodes D-05 fields plus the non-falsified recommender.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fixture tests for D-05 fields, recommender, and redaction</name>
  <files>scripts/phase28.1.1.2-result-path-compare-test.mjs</files>
  <read_first>
    - scripts/phase28.1.1.1-below-job-byte-sequence-compare-test.mjs
    - scripts/phase28.1.1.2-result-path-compare.mjs
    - .planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-VALIDATION.md
  </read_first>
  <behavior>
    - Test A: synthetic upstream log with many `chip_id_read_tx` / `0x52` TX lines + rust log with job_tx, many `result_read_attempt`, zero `result_correlated` → report includes `recommended_investigation: match_upstream_register_read_poll` and `result_correlated: false` and `fake_pool_submit_observed: false`
    - Test B: rust log with `asic_production_status=result_correlated` + fake_pool_submit marker → `result_correlated: true` and `fake_pool_submit_observed: true`
    - Test C: missing upstream file → `comparison_status: blocked_safe_prerequisite`
    - Test D: report text must not match credential/hex dump sentinels (`poolPassword`, `stratum+tcp://`, long hex runs of job frames)
    - Test E: gap shape from Test A must not contain `recommended_investigation: post_max_baud_delay_2000`
  </behavior>
  <action>
    Create `scripts/phase28.1.1.2-result-path-compare-test.mjs` using `node:assert` and temp fixture files (same pattern as `phase28.1.1.1-below-job-byte-sequence-compare-test.mjs`). Fixtures may include synthetic short hex for classification, but assertions must prove the **report** never echoes credential sentinels or full job-frame hex dumps. Cover STR-09 submit boolean, CFG-07 redaction, and ASIC-11 `result_correlated` boolean derivation.
  </action>
  <acceptance_criteria>
    - `node scripts/phase28.1.1.2-result-path-compare-test.mjs` exits 0
    - `rg -n "match_upstream_register_read_poll" scripts/phase28.1.1.2-result-path-compare-test.mjs`
    - `rg -n "fake_pool_submit_observed|result_correlated" scripts/phase28.1.1.2-result-path-compare-test.mjs`
  </acceptance_criteria>
  <verify>
    <automated>node scripts/phase28.1.1.2-result-path-compare-test.mjs</automated>
  </verify>
  <done>Wave 0 unit path is green; VALIDATION.md Wave 0 comparator/test boxes can be checked during Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ignored capture logs → committed Markdown report | Untrusted UART/monitor text crosses into redacted evidence |
| Synthetic fixture logs → CI assertions | Test inputs must not teach the script to echo secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28112-01 | I | result-path-compare.mjs | mitigate | Emit counts/booleans only; assert `raw_bytes_committed: false`; unit test rejects credential/hex dump sentinels (CFG-07) |
| T-28112-02 | S | recommended_investigation / share markers | mitigate | `fake_pool_submit_observed` and `result_correlated` are booleans only; never claim accepted/rejected live share; `phase30_promotion_input: pending` |
| T-28112-03 | T | comparator CLI args | accept | Local developer paths only; no network; malformed logs → blocked_safe_prerequisite |
</threat_model>

<verification>
- `node scripts/phase28.1.1.2-result-path-compare-test.mjs`
- `node scripts/phase28.1.1.2-result-path-compare.mjs --help >/dev/null`
</verification>

<success_criteria>
- D-04/D-05 comparator exists with fixture coverage.
- Recommender prefers `match_upstream_register_read_poll` for the known count asymmetry and never defaults to `post_max_baud_delay_2000`.
</success_criteria>

<output>
After completion, create `.planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-01-SUMMARY.md`
</output>
