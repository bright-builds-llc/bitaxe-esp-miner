---
phase: 28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos
plan: 02
type: execute
wave: 2
depends_on:
  - 28.1.1.4-01
files_modified:
  - crates/bitaxe-config/src/catalog.rs
  - crates/bitaxe-asic/src/bm1366/mining_ready.rs
  - crates/bitaxe-asic/src/bm1366/upstream_init_frames.rs
  - scripts/phase28.1.1.4-init-sequencing-diagnostic.sh
  - scripts/phase28.1.1.4-init-sequencing-diagnostic-test.sh
  - .planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-init-sequencing-redacted.md
  - .planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-ab-ticket_mask_asic_difficulty-redacted.md
autonomous: true
requirements: [STR-09, ASIC-11, CFG-07]
user_setup: []
must_haves:
  truths:
    - "Rust MiningReadyConfig::ultra_205_single_chip programs ticket mask from ASIC family difficulty 256 (not pool stratumdiff 1000), with fixture DIFFICULTY_256_FRAME and unit coverage"
    - "Diagnostic wrapper composes detect-ultra205, verify-reference, existing golden/source-work helpers, comparator, and exactly one ≥360s fake-pool A/B for ticket_mask_asic_difficulty"
    - "Redacted A/B evidence records ab_outcome, difficulty_mask_match, power_delta_class, result_correlated, fake_pool_submit_observed without hex or credentials"
  artifacts:
    - path: "crates/bitaxe-asic/src/bm1366/mining_ready.rs"
      provides: "ASIC-256 ticket-mask difficulty source for Ultra 205 mining-ready"
      contains: "difficulty"
    - path: "scripts/phase28.1.1.4-init-sequencing-diagnostic.sh"
      provides: "Detector-gated diagnostic + forced ticket-mask A/B wrapper"
      contains: "ticket_mask_asic_difficulty"
    - path: ".planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-ab-ticket_mask_asic_difficulty-redacted.md"
      provides: "Redacted ≥360s A/B outcome"
      contains: "ab_outcome:"
  key_links:
    - from: "crates/bitaxe-asic/src/bm1366/mining_ready.rs"
      to: "crates/bitaxe-config/src/catalog.rs"
      via: "AsicProfile difficulty accessor (=256 for BM1366)"
      pattern: "difficulty\\(\\)|asic\\(\\)\\.difficulty"
    - from: "scripts/phase28.1.1.4-init-sequencing-diagnostic.sh"
      to: "scripts/phase28.1.1.1-source-work-aligned-capture.sh"
      via: "compose fake-pool capture path"
      pattern: "source-work|duration-seconds|360"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1.4-2026-07-09T03-35-01
generated_at: 2026-07-09T03:45:00Z
---

<objective>
Apply the one confirmed divergence patch — ticket-mask difficulty source ASIC family **256** instead of pool `stratumdiff` **1000** — with pure-crate fixture tests, then run a detector-gated ≥360 s fake-pool hardware A/B and record redacted evidence.

Purpose: RESEARCH verified upstream `BM1366_init` uses `family.asic.difficulty` (256) for reg `0x14` while Rust uses pool difficulty 1000; D-10/D-12 allow exactly one focused patch + one A/B this phase. Final PLL/HCN already match — do not touch job bytes or falsified levers (D-13).
Output: catalog difficulty field + mining_ready patch + `DIFFICULTY_256_FRAME` fixture; diagnostic wrapper; redacted baseline + A/B reports.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-CONTEXT.md
@.planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-RESEARCH.md
@crates/bitaxe-asic/src/bm1366/mining_ready.rs
@crates/bitaxe-asic/src/bm1366/upstream_init_frames.rs
@crates/bitaxe-config/src/catalog.rs
@scripts/phase28.1.1.4-init-sequencing-compare.mjs
@scripts/phase28.1.1.1-source-work-aligned-capture.sh
@scripts/phase28.1.1.1-upstream-golden-capture.sh
@scripts/phase28.1.1.3-rx-acquisition-diagnostic.sh
@scripts/phase27-live-hardware-bridge-package.sh

<interfaces>
Confirmed patch target (D-10 / RESEARCH):
```rust
// mining_ready.rs today (WRONG source for ticket mask):
difficulty: f64::from(defaults.primary_pool().difficulty()), // 1000

// Upstream bm1366.c:
uint16_t difficulty = GLOBAL_STATE->DEVICE_CONFIG.family.asic.difficulty; // 256
```

Catalog: `AsicProfile` currently has no `difficulty` field — add `difficulty: u16` with BM1366 = 256 (device_config.h), accessor `difficulty()`, breadcrumb to reference. Other ASIC profiles: set from upstream `device_config.h` counterparts when present; if unknown, use a documented placeholder only for non-205 profiles that are NotHardwareVerified (do not invent Ultra 205 values).

Fixture: replace dynamic-frame assertion from `DIFFICULTY_1000_FRAME` to new `DIFFICULTY_256_FRAME` with provenance comment citing `device_config.h` ASIC_BM1366.difficulty=256 and `get_difficulty_mask`. Keep `DIFFICULTY_1000_FRAME` constant only if still useful for mask-math unit tests; dynamic mining-ready test must assert 256.

Hardware (D-14):
1. `just detect-ultra205` — exactly one port + board-info success
2. `just verify-reference`
3. Prefer fake-pool via `phase28.1.1.1-source-work-aligned-capture.sh` composition
4. `--duration-seconds` / capture-timeout ≥ 360; agent wall clock ≥ 420
5. Never modify `reference/esp-miner` (D-15); scratch-only upstream helpers
6. Do not print/read wifi or pool credential file contents

A/B label: `ticket_mask_asic_difficulty`
- Baseline: pre-patch or documented current behavior compare (scratch logs OK for baseline category proof)
- Candidate: build/flash with ASIC-256 ticket mask (this patch is the candidate; investigation flags off-by-default — D-12)
- Record `ab_outcome: improved|unchanged|regressed|blocked_safe_prerequisite`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Catalog ASIC difficulty 256 + mining_ready ticket-mask patch + fixtures</name>
  <files>crates/bitaxe-config/src/catalog.rs, crates/bitaxe-asic/src/bm1366/mining_ready.rs, crates/bitaxe-asic/src/bm1366/upstream_init_frames.rs</files>
  <read_first>
    - crates/bitaxe-asic/src/bm1366/mining_ready.rs (ultra_205_single_chip, difficulty_mask_value, dynamic frame tests)
    - crates/bitaxe-asic/src/bm1366/upstream_init_frames.rs (DIFFICULTY_1000_FRAME)
    - crates/bitaxe-config/src/catalog.rs (AsicProfile + BM1366 constants)
    - reference/esp-miner/main/device_config.h (ASIC_BM1366.difficulty — read-only)
    - .planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-RESEARCH.md (Pattern 5, Pitfall 2)
  </read_first>
  <behavior>
    - `MiningReadyConfig::ultra_205_single_chip(1).difficulty == 256.0` (not 1000.0)
    - `difficulty_mask_value(256.0)` matches expected reverse-bit mask for ASIC-256
    - `mining_ready_dynamic_init_frames` asserts frames[6] == `DIFFICULTY_256_FRAME` (not DIFFICULTY_1000_FRAME)
    - FREQUENCY_485_FRAME and NONCE_SPACE_485_FRAME assertions remain
    - Catalog BM1366 profile exposes `difficulty() == 256` with reference breadcrumb
  </behavior>
  <action>
    Per D-10 / RESEARCH primary lever (only this divergence):

    1. Add `difficulty: u16` to `AsicProfile` in `catalog.rs` with `pub const fn difficulty(&self) -> u16`. Set BM1366 Ultra 205 profile to **256** citing `reference/esp-miner/main/device_config.h` (`ASIC_BM1366.difficulty`). Fill other catalog ASIC profiles from the same upstream header when values exist; keep non-205 as NotHardwareVerified without claiming hardware parity.

    2. Change `MiningReadyConfig::ultra_205_single_chip` to set:
       `difficulty: f64::from(catalog.asic().difficulty())`
       — **not** `defaults.primary_pool().difficulty()`. Add a short comment that ticket mask follows ASIC family difficulty (upstream BM1366_init), while pool stratumdiff remains a Stratum/share concern.

    3. Add `DIFFICULTY_256_FRAME` in `upstream_init_frames.rs` with provenance (computed via `get_difficulty_mask(256)` / Rust `difficulty_mask_value(256.0)`). Update `mining_ready_dynamic_init_frames_match_upstream_computed_values` to assert `DIFFICULTY_256_FRAME`. Keep a unit test that `difficulty_mask_value(1000.0)` still matches the power-of-two rule (pool math unchanged) without using 1000 as mining-ready default.

    4. Do **not** change job-byte construction, post_max_baud defaults, register-read poll, or long-block RX (D-13).

    Run focused tests: `cargo test -p bitaxe-asic mining_ready` and `cargo test -p bitaxe-config` (or Bazel equivalents if that is the repo norm for these crates).
  </action>
  <acceptance_criteria>
    - `rg -n "difficulty:\\s*f64::from\\(catalog\\.asic\\(\\)\\.difficulty\\(\\)\\)" crates/bitaxe-asic/src/bm1366/mining_ready.rs`
    - `rg -n "DIFFICULTY_256_FRAME" crates/bitaxe-asic/src/bm1366/upstream_init_frames.rs crates/bitaxe-asic/src/bm1366/mining_ready.rs`
    - `rg -n "fn difficulty\\(&self\\)" crates/bitaxe-config/src/catalog.rs`
    - `rg -n "difficulty:\\s*256" crates/bitaxe-config/src/catalog.rs`
    - `rg -n "primary_pool\\(\\)\\.difficulty\\(\\)" crates/bitaxe-asic/src/bm1366/mining_ready.rs` returns no matches in `ultra_205_single_chip` body (pool difficulty must not drive ticket mask)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p bitaxe-asic mining_ready -- --nocapture</automated>
  </verify>
  <done>Ticket mask uses ASIC difficulty 256 with fixture + unit proof; pool stratumdiff no longer drives reg 0x14.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Diagnostic wrapper + ≥360s fake-pool A/B evidence</name>
  <files>scripts/phase28.1.1.4-init-sequencing-diagnostic.sh, scripts/phase28.1.1.4-init-sequencing-diagnostic-test.sh, .planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-init-sequencing-redacted.md, .planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-ab-ticket_mask_asic_difficulty-redacted.md</files>
  <read_first>
    - scripts/phase28.1.1.3-rx-acquisition-diagnostic.sh
    - scripts/phase28.1.1.3-rx-acquisition-diagnostic-test.sh
    - scripts/phase28.1.1.1-source-work-aligned-capture.sh
    - scripts/phase28.1.1.4-init-sequencing-compare.mjs
    - AGENTS.md (detect-ultra205, capture-timeout ≥360, redaction, fake-pool preferred)
    - .planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-CONTEXT.md (D-10, D-12, D-14, D-15)
  </read_first>
  <behavior>
    - Dry-run: requires --port, --wifi-credentials, --duration-seconds≥360, --capture-dir; refuses duration &lt; 360; does not recommend falsified knobs; prints forced A/B label `ticket_mask_asic_difficulty`
    - Hardware path: detect-ultra205 → verify-reference → baseline compare (scratch or fresh) → package/flash ASIC-256 build → ≥360s fake-pool capture → compare → write redacted A/B
    - If detect/credentials/capture blocked → write `ab_outcome: blocked_safe_prerequisite` precisely (discretion); do not invent frame bytes
  </behavior>
  <action>
    Create `scripts/phase28.1.1.4-init-sequencing-diagnostic.sh` composing existing helpers (D-09), modeled on `phase28.1.1.3-rx-acquisition-diagnostic.sh`:

    Required flags: `--port`, `--wifi-credentials`, `--duration-seconds` (≥360), `--capture-dir` (must be gitignored), optional `--dry-run`.

    Flow:
    1. Enforce duration ≥ 360
    2. Prefer composing `phase28.1.1.1-source-work-aligned-capture.sh` / phase27 bridge package for Rust flash+monitor (fake-pool preferred over live pool credentials)
    3. Run `phase28.1.1.4-init-sequencing-compare.mjs` for baseline (may use existing ignored scratch upstream SERIALTX + pre-patch rust log if still available for category baseline; after patch, rust capture is the candidate)
    4. Forced single A/B label: `ticket_mask_asic_difficulty` — flash/run the ASIC-256 patched firmware only (no second lever, no investigation flag spam — D-12)
    5. Capture timeout ≥360 s; do not treat early exit as failure until timeout elapses unless hard flash error
    6. Write redacted Markdown under the phase dir:
       - `28.1.1.4-init-sequencing-redacted.md` — baseline comparator fields (D-07 keys)
       - `28.1.1.4-ab-ticket_mask_asic_difficulty-redacted.md` — must include:
         - `ab_label: ticket_mask_asic_difficulty`
         - `ab_outcome: improved|unchanged|regressed|blocked_safe_prerequisite`
         - `difficulty_mask_match: true|false`
         - `difficulty_mask_class_upstream` / `difficulty_mask_class_rust`
         - `frequency_final_match` / `nonce_space_match`
         - `power_delta_class:`
         - `result_correlated:` / `fake_pool_submit_observed:`
         - `capture_timeout_seconds:` (≥360)
         - `board: 205` / `port:` path category only if needed (local OK; redact before any share)
         - `raw_bytes_committed: false` / `credential_contents_read: false`
         - `phase30_promotion_input: pending`
         - `job_byte_patch_applied: false` and falsified-lever false flags

    `improved` definition for later Plan 03 (record honestly here): `result_correlated: true` and/or `fake_pool_submit_observed: true` (D-11). Optionally note `rising_hashing` as corroboration only.

    Create `scripts/phase28.1.1.4-init-sequencing-diagnostic-test.sh` dry-run assertions (duration floor, forced label, no falsified knobs, gitignore capture-dir check).

    Before hardware: run `just detect-ultra205` and `just verify-reference`. If detection fails, stop with blocked evidence — do not guess.

    Never commit raw logs; keep under `scratch/` or ignored `hardware-runs/`.
  </action>
  <acceptance_criteria>
    - `test -f scripts/phase28.1.1.4-init-sequencing-diagnostic.sh`
    - `test -f scripts/phase28.1.1.4-init-sequencing-diagnostic-test.sh`
    - `bash scripts/phase28.1.1.4-init-sequencing-diagnostic-test.sh` exits 0
    - `rg -n "ticket_mask_asic_difficulty" scripts/phase28.1.1.4-init-sequencing-diagnostic.sh`
    - `rg -n "duration_seconds|360" scripts/phase28.1.1.4-init-sequencing-diagnostic.sh`
    - `rg -n "^ab_outcome:" .planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-ab-ticket_mask_asic_difficulty-redacted.md`
    - `rg -n "raw_bytes_committed: false" .planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-ab-ticket_mask_asic_difficulty-redacted.md`
    - `rg -n "job_byte_patch_applied: false" .planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-ab-ticket_mask_asic_difficulty-redacted.md`
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase28.1.1.4-init-sequencing-diagnostic-test.sh</automated>
  </verify>
  <done>Diagnostic dry-run green; redacted ≥360s A/B (or precise blocked_safe_prerequisite) recorded for ticket_mask_asic_difficulty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wifi-credentials.json / pool-credentials → flash tools | Pass-by-path only; never read/print/commit contents |
| UART monitor → evidence Markdown | Hex and endpoints must stay in ignored scratch; committed docs use categories |
| reference/esp-miner → scratch clone | Upstream edits only in scratch; verify-reference must stay clean |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28114-05 | I | diagnostic.sh + A/B Markdown | mitigate | Category labels only; `credential_contents_read: false`; redaction sweep before commit (CFG-07) |
| T-28114-06 | I | uart_trace / SERIALTX captures | mitigate | Store under gitignored capture-dir/scratch; reports forbid hex dumps |
| T-28114-07 | T | reference/esp-miner | mitigate | `just verify-reference` before/after; D-15 scratch-only golden helpers |
| T-28114-08 | E | overclaim STR-09 verified | mitigate | `phase30_promotion_input: pending`; Plan 03/04 own disposition; no checklist verified edits |
| T-28114-09 | D | unsafe voltage/fan stress | accept | Out of scope; only standard bring-up + fake-pool mining path |
</threat_model>

<verification>
- `cargo test -p bitaxe-asic mining_ready` passes with DIFFICULTY_256_FRAME
- `bash scripts/phase28.1.1.4-init-sequencing-diagnostic-test.sh` passes
- Redacted A/B file exists with `ab_outcome` and ≥360 capture timeout field
</verification>

<success_criteria>
- Ticket-mask ASIC-256 is the sole code divergence introduced
- One detector-gated ≥360 s fake-pool A/B (or precise blocked record) exists
- No job-byte / falsified-lever / reference-tree mutations
</success_criteria>

<output>
After completion, create `.planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-02-SUMMARY.md`
</output>
