---
phase: 28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase28.1.1.4-init-sequencing-compare.mjs
  - scripts/phase28.1.1.4-init-sequencing-compare-test.mjs
  - firmware/bitaxe/src/live_stratum_runtime.rs
autonomous: true
requirements: [CFG-07, ASIC-11, STR-09]
user_setup: []
must_haves:
  truths:
    - "A repo-owned init-sequencing comparator emits enable/voltage/reset presence, difficulty_mask_class_*, frequency_final_match, nonce_space_match, power_delta_class, result_correlated, and fake_pool_submit_observed without raw UART hex or credentials"
    - "When upstream last-pre-job 0x14 class is diff_256 and Rust is diff_1000 (or mask_eq_pow2_le_512 with pool source), recommended_investigation is ticket_mask_asic_difficulty"
    - "recommended_investigation never emits post_max_baud_delay_2000, match_upstream_register_read_poll, or upstream_like_long_block_receive"
  artifacts:
    - path: "scripts/phase28.1.1.4-init-sequencing-compare.mjs"
      provides: "Redaction-safe init-sequencing / dynamic-frame category report composing 28.1.1.2 parsers"
      contains: "ticket_mask_asic_difficulty"
    - path: "scripts/phase28.1.1.4-init-sequencing-compare-test.mjs"
      provides: "Fixture tests for diff_256 vs diff_1000, power_delta_class, falsified-knob ban"
      contains: "difficulty_mask_match"
  key_links:
    - from: "scripts/phase28.1.1.4-init-sequencing-compare.mjs"
      to: "scripts/phase28.1.1.2-result-path-compare.mjs"
      via: "compose/reuse lineEvents classifyFrame summarizeLog"
      pattern: "difficulty_mask_tx|frequency_tx|nonce_space_tx|power_delta"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1.4-2026-07-09T03-35-01
generated_at: 2026-07-09T03:45:00Z
---

<objective>
Ship Wave 0 redaction-safe init-sequencing comparator (and optional compact firmware summary marker) so dynamic mining-ready frames `0x14` / `0x08` / `0x10` are category-compared against upstream SERIALTX, with `power_delta_class` as fast feedback, before any ticket-mask patch or hardware A/B.

Purpose: D-02 re-scopes `asic_enable_power_sequencing` as cores-idle despite enable present; D-04/D-07 require golden category diff of dynamic frames never previously asserted against live upstream SERIALTX; RESEARCH confirms ticket-mask ASIC-256 vs pool-1000 mismatch.
Output: `scripts/phase28.1.1.4-init-sequencing-compare.mjs` + test; optional `asic_init_sequencing_summary` marker (counts only, no hex).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-CONTEXT.md
@.planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-RESEARCH.md
@.planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-VALIDATION.md
@scripts/phase28.1.1.2-result-path-compare.mjs
@scripts/phase28.1.1.3-rx-acquisition-compare.mjs
@crates/bitaxe-asic/src/bm1366/mining_ready.rs

<interfaces>
<!-- Compose 28.1.1.2 — do not invent a parallel taxonomy (D-09). -->

From scripts/phase28.1.1.2-result-path-compare.mjs:
- `classifyFrame`: register `0x14` → `difficulty_mask_tx`, `0x08` → `frequency_tx`, `0x10` → `nonce_space_tx`
- Events: `asic_enable_active`, `reset_pulse`, `job_tx`, `result_correlated`, `fake_pool_submit`, …
- CLI shape to mirror: `--upstream <log> --rust <log> --out <report.md>`

Confirmed RESEARCH mismatch (encode in fixtures, not guess):
- Upstream last-pre-job `0x14` class: `diff_256` (ASIC family difficulty)
- Rust current: `diff_1000` / `mask_eq_pow2_le_512` from pool `stratumdiff` 1000
- Final `0x08` PLL and `0x10` HCN match fixtures when comparing last ramp / nonce-space frames
- Upstream may have two inits; comparator MUST select last complete mining-ready window before first `job_tx` (or report both indices)

D-07 required report fields (exact keys):
- `asic_enable_active_rust: N`
- `voltage_write_present_rust: true|false` (from Set ASIC voltage / asic_power markers)
- `reset_pulse_rust: N`
- `difficulty_mask_class_upstream: diff_256|diff_1000|diff_16|diff_1000_or_512|other`
- `difficulty_mask_class_rust: diff_256|diff_1000|diff_1000_or_512|other`
- `difficulty_mask_match: true|false`
- `frequency_final_match: true|false`
- `frequency_ramp_present_upstream: true|false`
- `frequency_ramp_present_rust: true|false`
- `nonce_space_match: true|false`
- `power_delta_class: rising_hashing|flat|falling|unavailable`
- `result_correlated: true|false`
- `fake_pool_submit_observed: true|false`
- `recommended_investigation:` closed enum below
- `raw_bytes_committed: false`
- `credential_contents_read: false`
- `phase30_promotion_input: pending`
- `hypothesis_rescope: cores_idle_despite_enable_present` (D-02)

Closed `recommended_investigation` set for this phase:
`ticket_mask_asic_difficulty` | `match_upstream_chip_enumerate_before_init` | `none`

HARD BAN (fail tests if emitted): `post_max_baud_delay_2000`, `match_upstream_register_read_poll`, `upstream_like_long_block_receive`, `asic_enable_power_sequencing` as “enable GPIO missing” claim.

power_delta_class rules (D-06; fast feedback only — not phase pass gate):
- `unavailable` if marker missing or `unavailable=true`
- `rising_hashing` if `delta_mw >= 2000`
- `falling` if `delta_mw <= -200`
- `flat` otherwise
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement init-sequencing comparator CLI with D-07 fields</name>
  <files>scripts/phase28.1.1.4-init-sequencing-compare.mjs</files>
  <read_first>
    - scripts/phase28.1.1.2-result-path-compare.mjs
    - scripts/phase28.1.1.3-rx-acquisition-compare.mjs
    - .planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-CONTEXT.md (D-02, D-04, D-06, D-07, D-08, D-09)
    - .planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-RESEARCH.md (Patterns 1–4, Pitfalls 1/4/5, truth table)
  </read_first>
  <behavior>
    - Missing upstream or rust log → `comparison_status: blocked_safe_prerequisite`, `recommended_investigation: none`
    - Fixture: upstream last-pre-job `0x14` class `diff_256`, rust `diff_1000`/`diff_1000_or_512`, enable/reset present, `power_delta_class: falling`, no correlate → `recommended_investigation: ticket_mask_asic_difficulty`, `difficulty_mask_match: false`
    - Fixture: both sides last `0x14` `diff_256`, final PLL/HCN match, still no correlate → `difficulty_mask_match: true`, `recommended_investigation: match_upstream_chip_enumerate_before_init` (D-05 path; Plan 03 owns disposition)
    - Never recommend falsified knobs; report has no multi-byte hex dumps or credential sentinels
    - Compare last mining-ready window before first job_tx (do not treat early upstream `diff_16` as sole golden)
  </behavior>
  <action>
    Create `scripts/phase28.1.1.4-init-sequencing-compare.mjs` (Node ESM, stdlib only) accepting `--upstream`, `--rust`, `--out` (per D-09 compose with 28.1.1.2 — import `lineEvents` / `classifyFrame` / `summarizeLog` from `scripts/phase28.1.1.2-result-path-compare.mjs`; do not invent a parallel capture stack).

    Implement payload-class derivation for `difficulty_mask_tx` frames: map 4-byte payload through the same power-of-two + reverse-bits rule as Rust `difficulty_mask_value` / upstream `get_difficulty_mask`, then label nearest known difficulty input (`16` / `256` / `512` / `1000`). Prefer source-aware labels when rust source is known (`diff_1000` for pool stratumdiff); allow `diff_1000_or_512` when wire-only (RESEARCH Pitfall 3).

    Select the **last** complete mining-ready window before first `job_tx` for both logs (RESEARCH Pattern 2). Compare:
    - last `0x14` class → `difficulty_mask_*`
    - last `0x08` before job vs known FREQUENCY_485 class / equal payload class → `frequency_final_match` (do not fail solely on ramp count asymmetry from double upstream init — Pitfall 4)
    - last `0x10` → `nonce_space_match`
    - `frequency_ramp_present_*` = true if ≥2 `frequency_tx` events in the selected window

    Parse `asic_probe=power_delta ... delta_mw=N` (or `unavailable=true`) into `power_delta_class` per D-06 rules above.

    Recommender (D-07 / D-04 / D-05):
    1. If blocked → `none`
    2. Else if `difficulty_mask_match == false` AND rust class is pool-1000 family while upstream is `diff_256` → `ticket_mask_asic_difficulty`
    3. Else if dynamic frames match (`difficulty_mask_match` && `frequency_final_match` && `nonce_space_match`) AND still no `result_correlated` → `match_upstream_chip_enumerate_before_init`
    4. Else → `none`
    5. **Never** emit falsified knobs listed in HARD BAN

    Emit Markdown with all D-07 keys listed in `<interfaces>`. Set `hypothesis_rescope: cores_idle_despite_enable_present` (D-02). Keep `phase30_promotion_input: pending` (D-13/D-16).
  </action>
  <acceptance_criteria>
    - `test -f scripts/phase28.1.1.4-init-sequencing-compare.mjs`
    - `rg -n "ticket_mask_asic_difficulty" scripts/phase28.1.1.4-init-sequencing-compare.mjs`
    - `rg -n "power_delta_class" scripts/phase28.1.1.4-init-sequencing-compare.mjs`
    - `rg -n "difficulty_mask_class_upstream" scripts/phase28.1.1.4-init-sequencing-compare.mjs`
    - `rg -n "match_upstream_chip_enumerate_before_init" scripts/phase28.1.1.4-init-sequencing-compare.mjs`
    - `rg -n "phase28.1.1.2-result-path-compare" scripts/phase28.1.1.4-init-sequencing-compare.mjs`
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/phase28.1.1.4-init-sequencing-compare.mjs</automated>
  </verify>
  <done>Comparator CLI exists, composes 28.1.1.2 parsers, emits D-07 category fields and closed recommender.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 fixture tests + optional compact summary marker</name>
  <files>scripts/phase28.1.1.4-init-sequencing-compare-test.mjs, firmware/bitaxe/src/live_stratum_runtime.rs</files>
  <read_first>
    - scripts/phase28.1.1.3-rx-acquisition-compare-test.mjs
    - scripts/phase28.1.1.4-init-sequencing-compare.mjs
    - firmware/bitaxe/src/live_stratum_runtime.rs (power_delta probe region ~950–977)
    - .planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-VALIDATION.md
  </read_first>
  <behavior>
    - Unit fixtures cover: ticket-mask mismatch → `ticket_mask_asic_difficulty`; frames match + no correlate → `match_upstream_chip_enumerate_before_init`; blocked missing logs; `power_delta_class` falling/rising/unavailable
    - Assert report body never matches multi-byte hex dump patterns like `55 AA 51` sequences as committed content, and never contains `poolPassword` / `poolURL` / credential JSON keys
    - Assert HARD BAN strings never appear as `recommended_investigation` values
    - Optional: if uart_trace flood would hide frame classes, emit once-per-session `asic_init_sequencing_summary mask_class=... ramp_steps=N hcn_set=1` (counts/classes only, no hex) — D-08 discretion; skip firmware edit if comparator can classify from existing uart_trace alone
  </behavior>
  <action>
    Create `scripts/phase28.1.1.4-init-sequencing-compare-test.mjs` using `node:assert` (mirror 28.1.1.3 test style). Include synthetic upstream/rust log fixtures (inline strings, not committed scratch hex dumps) that encode:
    1. Confirmed mismatch: upstream last `0x14` → class `diff_256`, rust → `diff_1000`, enable/reset present, `delta_mw=-390` → falling, recommend `ticket_mask_asic_difficulty`
    2. Match path: both `diff_256`, final PLL/HCN match classes, still no correlate → recommend `match_upstream_chip_enumerate_before_init`
    3. Missing log → blocked
    4. Falsified-knob ban: after running recommender on gap fixtures, assert recommendation ∉ HARD BAN set

    Optional (D-08 discretion): if needed for flood-safe parsing, add a single compact `asic_init_sequencing_summary` log line in `live_stratum_runtime.rs` near mining-ready complete / first dispatch — **counts and class labels only, never hex**. Prefer skipping firmware change when existing `asic_uart_trace` / SERIALTX lines already suffice for Wave 0 tests.

    Do not patch `mining_ready.rs` difficulty in this plan (Plan 02 owns the ASIC-256 patch).
  </action>
  <acceptance_criteria>
    - `test -f scripts/phase28.1.1.4-init-sequencing-compare-test.mjs`
    - `node scripts/phase28.1.1.4-init-sequencing-compare-test.mjs` exits 0
    - `rg -n "ticket_mask_asic_difficulty" scripts/phase28.1.1.4-init-sequencing-compare-test.mjs`
    - `rg -n "post_max_baud_delay_2000|match_upstream_register_read_poll|upstream_like_long_block_receive" scripts/phase28.1.1.4-init-sequencing-compare-test.mjs` (ban assertions present)
    - `rg -n "difficulty_mask_match" scripts/phase28.1.1.4-init-sequencing-compare-test.mjs`
  </acceptance_criteria>
  <verify>
    <automated>node scripts/phase28.1.1.4-init-sequencing-compare-test.mjs</automated>
  </verify>
  <done>Wave 0 compare-test green; recommender and redaction bans locked; optional compact marker only if needed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| USB monitor logs → comparator → committed Markdown | Untrusted device logs may contain UART hex, SSIDs, IPs; committed reports must stay category/boolean only |
| Local credential files → flash/monitor (not this plan) | Credentials must never be read into comparator inputs or reports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28114-01 | I | init-sequencing-compare.mjs report | mitigate | Emit field categories/counts/booleans only; unit tests fail on hex dumps and credential key leakage (CFG-07) |
| T-28114-02 | I | optional asic_init_sequencing_summary | mitigate | Counts/class labels only; never log raw frame hex |
| T-28114-03 | E | phase30_promotion_input / checklist | mitigate | Hard-code `phase30_promotion_input: pending`; no checklist edits in this plan (D-13) |
| T-28114-04 | T | reference/esp-miner | accept | This plan does not touch reference; later plans use scratch-only helpers + verify-reference |
</threat_model>

<verification>
- `node scripts/phase28.1.1.4-init-sequencing-compare-test.mjs` passes
- Sample CLI run on synthetic fixtures writes report with D-07 keys and `raw_bytes_committed: false`
</verification>

<success_criteria>
- Comparator detects confirmed ticket-mask class mismatch and recommends `ticket_mask_asic_difficulty`
- Falsified investigation knobs cannot appear as recommendations
- No raw UART hex or credentials in committed report output
</success_criteria>

<output>
After completion, create `.planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-01-SUMMARY.md`
</output>
