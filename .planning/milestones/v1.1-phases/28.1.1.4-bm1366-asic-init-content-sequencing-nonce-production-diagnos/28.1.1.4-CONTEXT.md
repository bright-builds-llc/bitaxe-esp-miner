---
generated_by: gsd-discuss-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1.4-2026-07-09T03-35-01
generated_at: 2026-07-09T03:35:01Z
---

# Phase 28.1.1.4: BM1366 ASIC Init-Content Sequencing Nonce-Production Diagnosis - Context

**Gathered:** 2026-07-08
**Status:** Ready for planning
**Mode:** YOLO recommended decisions accepted

<domain>
## Phase Boundary

This phase isolates the deferred `asic_enable_power_sequencing` hypothesis, re-scoped as **init/content sequencing that leaves the BM1366 UART-alive but not hashing**. Prior evidence already shows `asic_enable`, voltage write, and reset markers present, with register-read proof at 1 MBaud and falling INA260 power after dispatch. Job bytes (11/11), register-read poll cadence, and long-block RX acquisition are falsified and must not be reopened.

First lever: golden byte diff of dynamic mining-ready frames regs `0x14` (difficulty/ticket mask), `0x08` (PLL), `0x10` (HCN/nonce-space) vs upstream SERIALTX. If those match, disposition routes to chip-enumerate (`55 AA 52 05 00 00 0A`) without a second speculative patch in this phase.

Out of scope: Phase 29/30 promotion; job-byte patches; falsified investigation defaults (`post_max_baud_delay_2000`, `match_upstream_register_read_poll`, `upstream_like_long_block_receive`); claiming “enable GPIO missing.”

</domain>

<decisions>
## Implementation Decisions

### Evidence ladder
- **D-01:** Retain 28.1.1.1–28.1.1.3 facts: job 11/11; below-job 12/12; register-read poll falsified; long-block RX falsified; no Rust `result_correlated` / fake-pool submit.
- **D-02:** Re-scope `asic_enable_power_sequencing` as cores-idle despite enable/power/reset present (J3: power_delta −390 mW).
- **D-03:** Ultra 205 detector-gated; hardware A/B unblocked.

### Primary hypothesis
- **D-04:** First investigation is dynamic mining-ready frames `0x14` / `0x08` / `0x10` never golden-asserted against upstream runtime SERIALTX bytes (static frames already fixture-matched).
- **D-05:** If dynamic frames match under aligned capture, do not invent a second patch; set `next_hypothesis: match_upstream_chip_enumerate_before_init`.
- **D-06:** Fast feedback: `asic_probe=power_delta` → `power_delta_class` (`rising_hashing` / `flat` / `falling` / `unavailable`). Phase gate remains `result_correlated` + `fake_pool_submit_observed`.

### Comparator (first deliverable)
- **D-07:** Redaction-safe init-sequencing comparator composing 28.1.1.1–28.1.1.3 parsers: enable/voltage/reset presence, `power_delta_class`, dynamic-frame field match/mismatch categories (no raw hex in commits), `recommended_investigation` that never re-emits falsified knobs.
- **D-08:** Optional compact firmware marker `asic_init_sequencing_summary` (counts only, no hex).
- **D-09:** Prefer composing existing scripts over a parallel capture stack.

### A/B and patch policy
- **D-10:** Capture upstream SERIALTX golden (scratch-only) and Rust UART traces for the three dynamic frames; if mismatch, one focused pure-crate/firmware patch with fixture test, then one detector-gated ≥360 s fake-pool A/B.
- **D-11:** Patch default only if `ab_outcome: improved` with `result_correlated` and/or `fake_pool_submit_observed: true`.
- **D-12:** One confirmed divergence at a time; investigation flags off-by-default; no speculative flag spam.
- **D-13:** Bans: no job-byte patches; no falsified investigation defaults; no Phase 30 checklist `verified` edits.

### Hardware and verification
- **D-14:** `just detect-ultra205`, `just verify-reference`, board 205, ≥360 s, wall clock ≥420 s; fake-pool preferred.
- **D-15:** Never modify `reference/esp-miner`; use scratch upstream capture helpers only.
- **D-16:** `passed` only with `result_correlated` + share-submission marker; else `gaps_found` / `blocked_safe_prerequisite`. Phase 30 promotion deferred.

### the agent's Discretion
- Exact comparator field names and whether compact firmware markers ship in Plan 01 vs 02.
- If upstream SERIALTX capture is blocked, record `blocked_safe_prerequisite` precisely rather than guessing frame bytes.

</decisions>

<specifics>
## Specific Ideas

- Dynamic frames in `crates/bitaxe-asic/src/bm1366/mining_ready.rs` are the highest-signal remaining suspect per NEXT-AGENT-PROMPT.
- Use power_delta class as ~7 s hashing discriminator before waiting full 360 s for correlate.
- Keep raw logs under ignored `scratch/` or `.planning/phases/**/hardware-runs/`.

</specifics>

<canonical_refs>
## Canonical References

### Phase truth
- `.planning/ROADMAP.md` — Phase 28.1.1.4 scope
- `.planning/STATE.md` — handoff from 28.1.1.3
- `.planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-final-evidence-redacted.md`
- `.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/NEXT-AGENT-PROMPT.md`
- `.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-blocker-disposition.md`
- `.planning/phases/27-live-hardware-asic-and-stratum-bridge/chip-detect-upstream-diff.md`

### Tooling
- `scripts/phase28.1.1.1-wire-field-compare.mjs`
- `scripts/phase28.1.1.1-upstream-golden-capture.sh`
- `scripts/phase28.1.1.1-source-work-aligned-capture.sh`
- `scripts/phase28.1.1.3-rx-acquisition-compare.mjs`
- `scripts/phase27-live-hardware-bridge-package.sh`
- `scripts/phase28.1.1-wire-parity-capture.sh`

### Code
- `crates/bitaxe-asic/src/bm1366/mining_ready.rs` — dynamic frames 0x14/0x08/0x10
- `firmware/bitaxe/src/asic_adapter/work_result_investigation.rs`
- `firmware/bitaxe/src/safety_adapter/phase27_bring_up.rs`
- `firmware/bitaxe/src/live_stratum_runtime.rs` — power_delta probe
- `firmware/bitaxe/src/asic_adapter/uart.rs` — UART TX traces

### Policy
- `AGENTS.md` — detector gate, redaction, timeouts
- `docs/adr/0012-parity-verification-evidence.md`
- `docs/parity/checklist.md` — no verified promotion here

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- Wire-field and RX-acquisition comparators from 28.1.1.1–28.1.1.3
- Upstream golden capture + source-work fake-pool helpers
- Power delta probe already emits redaction-safe mw markers

### Patterns to Follow
- Investigation-only first; default promotion only on improved correlate/submit
- Fail closed to `gaps_found`; never overclaim Phase 30

</code_context>

<deferred>
## Deferred Ideas

- Chip-enumerate `0x0A` sequencing (next if dynamic frames match)
- Version-rolling negotiation (secondary)
- Phase 29 evidence automation / Phase 30 verified promotion

</deferred>
