---
phase: 28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ
plan: 02
type: execute
wave: 2
depends_on:
  - 28.1.1.5-01
files_modified:
  - crates/bitaxe-asic/src/bm1366/chip_detect.rs
  - crates/bitaxe-asic/src/bm1366/mining_ready.rs
  - firmware/bitaxe/src/asic_adapter.rs
  - firmware/bitaxe/src/asic_adapter/chip_detect_investigation.rs
  - firmware/bitaxe/src/asic_adapter/uart.rs
  - scripts/phase28.1.1.5-chip-enumerate-diagnostic.sh
  - scripts/phase28.1.1.5-chip-enumerate-diagnostic-test.sh
  - .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-chip-enumerate-redacted.md
  - .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-ab-count_asic_chips_rx_loop_parity-redacted.md
autonomous: true
requirements: [STR-09, ASIC-11, CFG-07]
user_setup: []
must_haves:
  truths:
    - "Rust chip-detect implements upstream-like drain-until-idle + soft preamble/CRC retry and hands counted chips into mining-ready, with pure-crate/firmware fixture coverage, without changing ReadChipId 0x0A frame bytes"
    - "Diagnostic wrapper composes detect-ultra205, verify-reference, existing golden/source-work helpers, comparator, and exactly one ≥360s fake-pool A/B for count_asic_chips_rx_loop_parity"
    - "Redacted A/B evidence records ab_outcome, chip_count_source_class, enumerate_to_mining_ready_gap_class, power_delta_class, result_correlated, fake_pool_submit_observed, wire_parity_ticket_mask_retained without hex or credentials"
  artifacts:
    - path: "crates/bitaxe-asic/src/bm1366/chip_detect.rs"
      provides: "Multi-frame count / soft-retry semantics for chip-ID RX"
      contains: "count_asic|drain|soft|validate"
    - path: "scripts/phase28.1.1.5-chip-enumerate-diagnostic.sh"
      provides: "Detector-gated diagnostic + forced RX-loop A/B wrapper"
      contains: "count_asic_chips_rx_loop_parity"
    - path: ".planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-ab-count_asic_chips_rx_loop_parity-redacted.md"
      provides: "Redacted ≥360s A/B outcome"
      contains: "ab_outcome:"
  key_links:
    - from: "firmware/bitaxe/src/asic_adapter.rs"
      to: "crates/bitaxe-asic/src/bm1366/chip_detect.rs"
      via: "ReadChipId interpret uses count-loop / counted handoff"
      pattern: "validate_single_chip_detect|count_asic|detected_chips|chip_count"
    - from: "scripts/phase28.1.1.5-chip-enumerate-diagnostic.sh"
      to: "scripts/phase28.1.1.1-source-work-aligned-capture.sh"
      via: "compose fake-pool capture path"
      pattern: "source-work|duration-seconds|360"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1.5-2026-07-09T11-47-00
generated_at: 2026-07-09T12:00:00Z
---

<objective>
Apply the one confirmed divergence patch — upstream-like `count_asic_chips` RX loop (drain-until-idle + soft preamble/CRC retry) plus counted-chip handoff into mining-ready — with pure-crate/firmware fixture tests, then run a detector-gated ≥360 s fake-pool hardware A/B labeled `count_asic_chips_rx_loop_parity` and record redacted evidence.

Purpose: RESEARCH verified ReadChipId TX bytes and Ultra 205 address_interval=256 already match; remaining diverge is single-shot RX + catalog expected chips (D-04/D-05/D-12). Keep ASIC-256 ticket mask (D-15). Do not patch `0x0A` frame bytes (D-02).
Output: chip_detect / adapter / mining-ready handoff patch + fixtures; diagnostic wrapper; redacted baseline + A/B reports.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-CONTEXT.md
@.planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-RESEARCH.md
@crates/bitaxe-asic/src/bm1366/chip_detect.rs
@crates/bitaxe-asic/src/bm1366/mining_ready.rs
@crates/bitaxe-asic/src/bm1366/command.rs
@firmware/bitaxe/src/asic_adapter.rs
@firmware/bitaxe/src/asic_adapter/chip_detect_investigation.rs
@firmware/bitaxe/src/asic_adapter/uart.rs
@scripts/phase28.1.1.5-chip-enumerate-compare.mjs
@scripts/phase28.1.1.1-source-work-aligned-capture.sh
@scripts/phase28.1.1.1-upstream-golden-capture.sh
@scripts/phase28.1.1.4-init-sequencing-diagnostic.sh
@scripts/phase27-live-hardware-bridge-package.sh

<interfaces>
Forced A/B label (RESEARCH PRIMARY / D-05): `count_asic_chips_rx_loop_parity`

Plan 02 rename rule (only from Wave 0 comparator output — do not guess mid-flash):
- Keep `count_asic_chips_rx_loop_parity` unless Wave 0 redacted report clearly shows address_interval mismatch → `counted_chip_address_interval`, or RX loop already matching with pure timing gap → `enumerate_to_mining_ready_gap`.
- Default expected path: keep `count_asic_chips_rx_loop_parity`.

Confirmed patch targets (D-12 / RESEARCH Patterns 2–3):
```rust
// chip_detect.rs today (single-shot):
let detected_chips = 1; // hardcoded

// asic_adapter.rs ReadChipId:
let response = uart.read_exact(BM1366_RESULT_FRAME_LEN, *timeout_ms)?;
validate_single_chip_detect_response(&response, *expected_chips)

// asic_adapter.rs mining-ready handoff:
let chip_count = preflight.expected_chips(); // ignores detect outcome
```

Upstream control-flow contract (breadcrumbs only — no GPL copy of C expression):
- Loop `SERIAL_rx` until received==0 (idle timeout)
- Soft `continue` on preamble/CRC/chip-id mismatch
- Break/fail on wrong length / UART hard error
- Accumulate valid chip-ID frames; require counted == expected (Ultra 205: 1)
- Pass counted chips into `mining_ready_init(..., chip_count)`
- Keep ASIC-256 ticket-mask source (`catalog.asic().difficulty()`)

Investigation flag policy (D-14):
- Prefer investigation flag off-by-default OR default-candidate with fixture tests — RESEARCH prefers RX-loop parity as the candidate under A/B.
- Exactly one focused change set covering RX loop + counted handoff; no second speculative patch (version-rolling deferred).

HARD BANS this plan:
- Do NOT modify `Bm1366Command::ReadChipId` frame bytes / `0x0A` payload (D-02)
- Do NOT reopen job-byte / post_max_baud / register-read poll / long-block defaults (D-15)
- Do NOT change ticket mask away from ASIC-256 unless A/B clearly regresses and retaining it is unsafe (default: keep)
- Never modify `reference/esp-miner` (D-17)

Hardware (D-16):
1. `just detect-ultra205` — exactly one port + board-info success
2. `just verify-reference`
3. Prefer fake-pool via `phase28.1.1.1-source-work-aligned-capture.sh` composition
4. `--duration-seconds` / capture-timeout ≥ 360; agent wall clock ≥ 420
5. Never print/read wifi or pool credential file contents
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: count_asic_chips RX-loop + counted handoff patch + fixtures</name>
  <files>crates/bitaxe-asic/src/bm1366/chip_detect.rs, crates/bitaxe-asic/src/bm1366/mining_ready.rs, firmware/bitaxe/src/asic_adapter.rs, firmware/bitaxe/src/asic_adapter/chip_detect_investigation.rs, firmware/bitaxe/src/asic_adapter/uart.rs</files>
  <read_first>
    - crates/bitaxe-asic/src/bm1366/chip_detect.rs
    - crates/bitaxe-asic/src/bm1366/command.rs (ReadChipId — do not change encoding)
    - crates/bitaxe-asic/src/bm1366/mining_ready.rs (mining_ready_init chip_count, ASIC-256 difficulty)
    - firmware/bitaxe/src/asic_adapter.rs (run_chip_detect_actions, run_mining_ready_init_actions, ReadChipId interpret)
    - firmware/bitaxe/src/asic_adapter/uart.rs (read_exact / read_accumulate)
    - reference/esp-miner/components/asic/asic_common.c (count_asic_chips — read-only breadcrumbs)
    - .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-RESEARCH.md (Patterns 2–3, Pitfalls 1/2/4/5)
    - Wave 0 compare report / fixtures from Plan 01 if present (confirm forced_ab_label)
  </read_first>
  <behavior>
    - Pure-crate: soft-retry on bad preamble/CRC does not hard-fail the count loop; idle/zero-length ends loop; wrong length fails closed; valid single Ultra 205 frame yields counted=1
    - Adapter: after ReadChipId TX (+ WaitTxDone), drain-until-idle multi-frame RX before returning from chip-detect
    - Mining-ready receives counted chips (not only catalog expected); Ultra 205 address_interval remains 256; difficulty remains ASIC-256
    - `Bm1366Command::ReadChipId` frame bytes unchanged (no `command.rs` encoding edit)
    - Investigation flag off-by-default OR clearly documented default-candidate with fixtures (D-14); A/B exercises the candidate path
  </behavior>
  <action>
    Per D-12 / RESEARCH primary lever `count_asic_chips_rx_loop_parity` (only this divergence family):

    1. Extend `chip_detect.rs` with pure count-loop helpers that mirror upstream control flow (breadcrumbs only — independent Rust design, no GPL C copy): accumulate valid chip-ID frames; soft-continue on preamble/CRC/chip-id mismatch; fail on wrong length; exit on idle/no-bytes; compare counted vs expected. Keep/adapt `validate_single_chip_detect_response` for single-frame unit cases; add multi-frame tests.

    2. Update firmware `ReadChipId` interpret path in `asic_adapter.rs` (and uart helpers if needed) to loop read until idle timeout instead of one `read_exact` then hard-fail. Prefer reusing existing `read_accumulate` / `read_exact` primitives; add drain-until-idle only as needed (Pitfall 4).

    3. Hand counted chips from chip-detect into `run_mining_ready_init_actions` / `mining_ready_init_decision` / `Bm1366InitPlan::mining_ready_init(..., chip_count)` so `chip_count_source_class` becomes `counted_rx` structurally. Do not change Ultra 205 ticket-mask difficulty source away from ASIC-256 (D-15).

    4. Optional compact `asic_chip_enumerate_summary` marker (counts only, no hex) only if uart_trace cannot feed Plan 01 comparator — prefer skip (D-11).

    5. Do **not** change `command.rs` ReadChipId encoding, job bytes, post_max_baud defaults, register-read poll, or long-block RX.

    Run focused tests: `cargo test -p bitaxe-asic chip_detect` and any mining_ready handoff tests touched; firmware unit tests if present for the adapter helpers.
  </action>
  <acceptance_criteria>
    - `rg -n "ReadChipId" crates/bitaxe-asic/src/bm1366/command.rs` still shows existing encoding; `git diff -- crates/bitaxe-asic/src/bm1366/command.rs` empty (no frame-byte patch)
    - `rg -n "count|drain|idle|soft|retry|accumulate" crates/bitaxe-asic/src/bm1366/chip_detect.rs` shows new multi-frame count semantics
    - `rg -n "expected_chips\\(\\)" firmware/bitaxe/src/asic_adapter.rs` no longer solely drives mining-ready chip_count without counted handoff (counted path present)
    - `rg -n "difficulty\\(\\)|DIFFICULTY_256|asic\\(\\)\\.difficulty" crates/bitaxe-asic/src/bm1366/mining_ready.rs` still reflects ASIC-256 retention
    - `cargo test -p bitaxe-asic chip_detect` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p bitaxe-asic chip_detect -- --nocapture</automated>
  </verify>
  <done>RX-loop + counted handoff implemented with fixtures; ReadChipId bytes and ASIC-256 ticket mask untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Diagnostic wrapper + ≥360s fake-pool A/B evidence</name>
  <files>scripts/phase28.1.1.5-chip-enumerate-diagnostic.sh, scripts/phase28.1.1.5-chip-enumerate-diagnostic-test.sh, .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-chip-enumerate-redacted.md, .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-ab-count_asic_chips_rx_loop_parity-redacted.md</files>
  <read_first>
    - scripts/phase28.1.1.4-init-sequencing-diagnostic.sh
    - scripts/phase28.1.1.4-init-sequencing-diagnostic-test.sh
    - scripts/phase28.1.1.1-source-work-aligned-capture.sh
    - scripts/phase28.1.1.5-chip-enumerate-compare.mjs
    - AGENTS.md (detect-ultra205, capture-timeout ≥360, redaction, fake-pool preferred)
    - .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-CONTEXT.md (D-12, D-14, D-16, D-17)
  </read_first>
  <behavior>
    - Dry-run: requires --port, --wifi-credentials, --duration-seconds≥360, --capture-dir; refuses duration &lt; 360; does not recommend falsified knobs; prints forced A/B label `count_asic_chips_rx_loop_parity` (or Wave 0 rename if documented)
    - Hardware path: detect-ultra205 → verify-reference → baseline compare (scratch or fresh) → package/flash RX-loop candidate → ≥360s fake-pool capture → compare → write redacted A/B
    - If detect/credentials/capture blocked → write `ab_outcome: blocked_safe_prerequisite` precisely; do not invent frame bytes
  </behavior>
  <action>
    Create `scripts/phase28.1.1.5-chip-enumerate-diagnostic.sh` composing existing helpers (D-10), modeled on `phase28.1.1.4-init-sequencing-diagnostic.sh`:

    Required flags: `--port`, `--wifi-credentials`, `--duration-seconds` (≥360), `--capture-dir` (must be gitignored), optional `--dry-run`.

    Flow:
    1. Enforce duration ≥ 360
    2. Prefer composing `phase28.1.1.1-source-work-aligned-capture.sh` / phase27 bridge package for Rust flash+monitor (fake-pool preferred)
    3. Run `phase28.1.1.5-chip-enumerate-compare.mjs` for baseline (scratch upstream SERIALTX + pre-patch rust log OK for category baseline)
    4. Forced single A/B label: `count_asic_chips_rx_loop_parity` — flash/run the RX-loop + counted-handoff candidate only (no second lever, no version-rolling — D-12/D-07)
    5. Capture timeout ≥360 s; do not treat early exit as failure until timeout elapses unless hard flash error
    6. Write redacted Markdown under the phase dir:
       - `28.1.1.5-chip-enumerate-redacted.md` — baseline comparator fields (D-08 keys)
       - `28.1.1.5-ab-count_asic_chips_rx_loop_parity-redacted.md` — must include:
         - `ab_label: count_asic_chips_rx_loop_parity` (or documented Wave 0 rename)
         - `ab_outcome: improved|unchanged|regressed|blocked_safe_prerequisite`
         - `read_chip_id_tx_match: true|false`
         - `chip_count_source_class_rust:`
         - `address_interval_match:`
         - `enumerate_to_mining_ready_gap_class:`
         - `power_delta_class:`
         - `result_correlated:` / `fake_pool_submit_observed:`
         - `wire_parity_ticket_mask_retained: true|false`
         - `capture_timeout_seconds:` (≥360)
         - `board: 205`
         - `raw_bytes_committed: false` / `credential_contents_read: false`
         - `phase30_promotion_input: pending`
         - `read_chip_id_byte_patch_applied: false`
         - `job_byte_patch_applied: false` and falsified-lever false flags

    `improved` definition for later Plan 03 (record honestly here): `result_correlated: true` and/or `fake_pool_submit_observed: true` (D-13). Optionally note `rising_hashing` as corroboration only.

    Create `scripts/phase28.1.1.5-chip-enumerate-diagnostic-test.sh` dry-run assertions (duration floor, forced label, no falsified knobs, gitignore capture-dir check).

    Before hardware: run `just detect-ultra205` and `just verify-reference`. If detection fails, stop with blocked evidence — do not guess.

    Never commit raw logs; keep under `scratch/` or ignored `hardware-runs/`.
  </action>
  <acceptance_criteria>
    - `test -f scripts/phase28.1.1.5-chip-enumerate-diagnostic.sh`
    - `test -f scripts/phase28.1.1.5-chip-enumerate-diagnostic-test.sh`
    - `bash scripts/phase28.1.1.5-chip-enumerate-diagnostic-test.sh` exits 0
    - `rg -n "count_asic_chips_rx_loop_parity" scripts/phase28.1.1.5-chip-enumerate-diagnostic.sh`
    - `rg -n "duration_seconds|360" scripts/phase28.1.1.5-chip-enumerate-diagnostic.sh`
    - `rg -n "^ab_outcome:" .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-ab-count_asic_chips_rx_loop_parity-redacted.md`
    - `rg -n "raw_bytes_committed: false" .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-ab-count_asic_chips_rx_loop_parity-redacted.md`
    - `rg -n "read_chip_id_byte_patch_applied: false" .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-ab-count_asic_chips_rx_loop_parity-redacted.md`
    - `rg -n "wire_parity_ticket_mask_retained:" .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-ab-count_asic_chips_rx_loop_parity-redacted.md`
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase28.1.1.5-chip-enumerate-diagnostic-test.sh</automated>
  </verify>
  <done>Diagnostic dry-run green; redacted ≥360s A/B (or precise blocked_safe_prerequisite) recorded for count_asic_chips_rx_loop_parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wifi-credentials.json / pool-credentials → flash tools | Pass-by-path only; never read/print/commit contents |
| UART monitor → evidence Markdown | Hex and endpoints must stay in ignored scratch; committed docs use categories |
| reference/esp-miner → scratch clone | Upstream edits only in scratch; verify-reference must stay clean |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28115-06 | I | diagnostic.sh + A/B Markdown | mitigate | Category labels only; `credential_contents_read: false`; redaction sweep before commit (CFG-07) |
| T-28115-07 | I | uart_trace / SERIALTX captures | mitigate | Store under gitignored capture-dir/scratch; reports forbid hex dumps |
| T-28115-08 | T | reference/esp-miner | mitigate | `just verify-reference` before/after; D-17 scratch-only golden helpers |
| T-28115-09 | E | overclaim STR-09 verified / share outcomes | mitigate | `phase30_promotion_input: pending`; Plan 03/04 own disposition; no checklist verified edits; record correlate/submit honestly |
| T-28115-10 | D | unsafe voltage/fan stress | accept | Out of scope; only standard bring-up + fake-pool mining path |
</threat_model>

<verification>
- `cargo test -p bitaxe-asic chip_detect` passes with multi-frame / soft-retry fixtures
- `bash scripts/phase28.1.1.5-chip-enumerate-diagnostic-test.sh` passes
- Redacted A/B file exists with `ab_outcome` and ≥360 capture timeout field
</verification>

<success_criteria>
- `count_asic_chips_rx_loop_parity` is the sole code divergence family introduced (RX loop + counted handoff)
- One detector-gated ≥360 s fake-pool A/B (or precise blocked record) exists
- No ReadChipId byte patch, job-byte / falsified-lever / reference-tree mutations; ASIC-256 ticket mask retained
</success_criteria>

<output>
After completion, create `.planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-02-SUMMARY.md`
</output>
