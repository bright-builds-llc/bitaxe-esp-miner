---
phase: 27-live-hardware-asic-and-stratum-bridge
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - crates/bitaxe-stratum/src/v1/live_runtime.rs
  - crates/bitaxe-stratum/src/v1/mining_loop.rs
  - crates/bitaxe-stratum/src/v1/fake_pool.rs
  - firmware/bitaxe/src/live_stratum_runtime.rs
  - firmware/bitaxe/src/asic_adapter/production.rs
autonomous: true
requirements: [STR-08, STR-09, ASIC-10, ASIC-11]
user_setup: []
must_haves:
  truths:
    - "Hardware nonce reads become ProductionNonceObservation stamped with dispatch generation, not inferred from ASIC bytes alone."
    - "Live runtime queues mining.submit only when correlate_nonce_result returns SubmitIntent for current generation."
    - "Uncorrelated, stale, duplicate, malformed, or blocked observations fail closed without share claims."
    - "Pool submit responses classify only when matching PendingSubmit intent and request id exist."
    - "Phase 27 hardware evidence mode waits for submit classification or explicit blocker before bounded pump exit."
  artifacts:
    - path: "crates/bitaxe-stratum/src/v1/live_runtime.rs"
      provides: "queue_submit_share and bridge action emission"
    - path: "firmware/bitaxe/src/live_stratum_runtime.rs"
      provides: "observation feedback loop and extended pump budget"
    - path: "firmware/bitaxe/src/asic_adapter/production.rs"
      provides: "ReadProductionResult observation return to bridge"
  key_links:
    - from: "firmware/bitaxe/src/asic_adapter/production.rs"
      to: "crates/bitaxe-stratum/src/v1/production_work.rs"
      via: "ProductionNonceObservation { observed_generation, result }"
      pattern: "ProductionNonceObservation"
    - from: "firmware/bitaxe/src/live_stratum_runtime.rs"
      to: "crates/bitaxe-stratum/src/v1/live_runtime.rs"
      via: "queue_submit_share pushes SendSubmitShare action"
      pattern: "SendSubmitShare|queue_submit_share"
    - from: "firmware/bitaxe/src/live_stratum_runtime.rs"
      to: "crates/bitaxe-stratum/src/v1/submit_response.rs"
      via: "classify_submit_response on pool response after pending submit"
      pattern: "classify_submit_response|PendingSubmit"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 27-2026-07-05T14-51-50
generated_at: 2026-07-05T15:00:00Z
---

<objective>
Close the observation-to-submit loop: BM1366 result reads feed production correlation and emit intent-tied mining.submit inside the live socket runtime.

Purpose: STR-09 and ASIC-11 require correlated nonce observations before any share claim. Phase 25 can classify pool responses but never queues submit from live ASIC work today.
Output: Pure runtime submit queuing, firmware observation feedback, and submit-classification-ready pump loop for Phase 27 evidence.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-live-hardware-asic-and-stratum-bridge/27-CONTEXT.md
@.planning/phases/27-live-hardware-asic-and-stratum-bridge/27-RESEARCH.md
@.planning/phases/27-live-hardware-asic-and-stratum-bridge/27-01-SUMMARY.md
@crates/bitaxe-stratum/src/v1/production_work.rs
@crates/bitaxe-stratum/src/v1/submit_response.rs
@crates/bitaxe-stratum/src/v1/fake_pool.rs
@firmware/bitaxe/src/live_stratum_runtime.rs
@docs/parity/evidence/phase-25-live-stratum-runtime-and-safe-stop/share-outcome.md

<interfaces>
From Plan 27-01:
```rust
// ProductionAsicExecutor returns parsed Bm1366NonceResult after ReadProductionResult
// Bridge stores last_dispatch_generation: PoolSessionGeneration
```

From `crates/bitaxe-stratum/src/v1/production_work.rs`:
```rust
pub struct ProductionNonceObservation {
    pub observed_generation: PoolSessionGeneration,
    pub result: Bm1366NonceResult,
}
pub enum CorrelationOutcome {
    SubmitIntent(SubmitIntent),
    Blocked { reason: ProductionAsicBlocker },
}
```

Existing `LiveRuntimeAction::SendSubmitShare { intent, request_id, message }` is handled in `write_runtime_action` but never queued by pure runtime today.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 27-02-01: Add pure live-runtime submit queuing after correlation</name>
  <files>crates/bitaxe-stratum/src/v1/live_runtime.rs, crates/bitaxe-stratum/src/v1/mining_loop.rs, crates/bitaxe-stratum/src/v1/fake_pool.rs</files>
  <read_first>
    - crates/bitaxe-stratum/src/v1/live_runtime.rs
    - crates/bitaxe-stratum/src/v1/production_work.rs
    - crates/bitaxe-stratum/src/v1/mining_loop.rs
    - crates/bitaxe-stratum/src/v1/fake_pool.rs
    - crates/bitaxe-stratum/src/v1/mining.rs
  </read_first>
  <behavior>
    - Test 1: `apply_bridge_observation(observation)` with correlated result queues exactly one `SendSubmitShare` with matching generation on `drain_actions()`.
    - Test 2: `CorrelationOutcome::Blocked` queues no submit action and records blocker on runtime state.
    - Test 3: Wrong-session observation returns blocked outcome without submit action.
    - Test 4: `queue_submit_share` builds `StratumV1ClientMessage::SubmitShare` using existing redaction-safe mining helpers; Debug paths do not leak raw job id, extranonce, nonce, or target.
    - Test 5: Fake-pool live bridge test mirrors `correlate_runtime_submit_intent` pattern for deterministic host coverage.
  </behavior>
  <action>
    Per D-02, D-07, D-08: add `LiveStratumRuntime::apply_bridge_observation(&mut self, observation: ProductionNonceObservation) -> Result<BridgeObservationOutcome, StratumV1Error>` that calls `production_registry.correlate_nonce_result(observation)` and on `SubmitIntent` invokes new `queue_submit_share(intent, request_id)` pushing `LiveRuntimeAction::SendSubmitShare`. On `Blocked`, call `runtime_state.block_work_submission(reason.as_str())` and return outcome without submit action.

    Add `GuardedMiningLoopSource::Live` if evidence labels need distinction from `Notify`; otherwise document reuse of `Notify` in tests only.

    Extend `fake_pool.rs` host tests with live-bridge fixture: notify → dispatch generation → observation → submit intent → single `SendSubmitShare` action.

    Do not classify accepted/rejected pool responses in pure crate; classification stays firmware + `submit_response.rs`.
  </action>
  <verify>
    <automated>bazel test //crates/bitaxe-stratum:tests --test_output=errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg "apply_bridge_observation|queue_submit_share|BridgeObservationOutcome" crates/bitaxe-stratum/src/v1/live_runtime.rs` finds bridge API.
    - `rg "live_bridge|apply_bridge_observation" crates/bitaxe-stratum/src/v1/fake_pool.rs` finds deterministic bridge test.
    - `bazel test //crates/bitaxe-stratum:tests --test_output=errors` passes.
  </acceptance_criteria>
  <done>Pure live runtime can queue intent-tied mining.submit only after successful nonce correlation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 27-02-02: Wire firmware observation feedback and submit classification loop</name>
  <files>firmware/bitaxe/src/live_stratum_runtime.rs, firmware/bitaxe/src/asic_adapter/production.rs</files>
  <read_first>
    - firmware/bitaxe/src/live_stratum_runtime.rs
    - firmware/bitaxe/src/asic_adapter/production.rs
    - crates/bitaxe-stratum/src/v1/submit_response.rs
    - .planning/phases/27-live-hardware-asic-and-stratum-bridge/27-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: After `SendProductionWork`, bridge executes `ReadProductionResult`, stamps `ProductionNonceObservation { observed_generation: dispatch.generation, result }`, and calls `apply_bridge_observation`.
    - Test 2: Successful correlation publishes `asic_production_status=result_correlated` before socket submit write.
    - Test 3: Uncorrelated observation publishes fail-closed ASIC status and does not call `write_runtime_action` with `SendSubmitShare`.
    - Test 4: Phase 27 mode uses extended pump iteration budget or stop-when-classified condition so submit pool response can arrive before `VerificationCleanup`.
    - Test 5: Classified accepted/rejected responses publish category-only `share_submission_status` markers without raw pool JSON.
  </behavior>
  <action>
    Per D-03, D-06, D-07, D-08, D-09:
    - Extend ASIC bridge in `live_stratum_runtime.rs`: after successful `SendProductionWork`, run `ReadProductionResult` via executor; wrap result as `ProductionNonceObservation` using stored dispatch generation (never infer generation from parsed bytes).
    - Feed observation into `runtime.apply_bridge_observation`; drain new actions including `SendSubmitShare`.
    - On `SubmitIntent` path publish `asic_production_status=result_correlated`; on blockers publish fail-closed status per D-06.
    - Add Phase 27 conditional pump budget: e.g. `PHASE27_LIVE_BRIDGE_PUMP_ITERATIONS` (≥32) or loop until `maybe_pending_submit` classifies, submit blocker recorded, or safe-stop trigger—document constant in module. Preserve Phase 25 default 16 iterations when Phase 27 mode inactive.
    - Reuse existing `PendingSubmit` + `classify_submit_response` path; emit redaction-safe markers `share_submission_status=accepted|rejected|blocked_safe_prerequisite` as category labels only.
    - Firmware tests with fake socket + fake executor prove full notify→dispatch→read→correlate→submit→classify sequence without hardware.
  </action>
  <verify>
    <automated>bazel test //crates/bitaxe-stratum:tests //crates/bitaxe-safety:tests && bazel build //firmware/bitaxe:firmware</automated>
  </verify>
  <acceptance_criteria>
    - `rg "ReadProductionResult|apply_bridge_observation|ProductionNonceObservation|PHASE27_LIVE_BRIDGE_PUMP" firmware/bitaxe/src/live_stratum_runtime.rs` finds observation loop.
    - `rg "asic_production_status=result_correlated|share_submission_status=" firmware/bitaxe/src/live_stratum_runtime.rs` finds classification markers.
    - `! rg -n "stratum[+]tcp://|poolURL|poolUser|poolPassword|target=|extranonce=|share_payload|raw_bm1366_frame" firmware/bitaxe/src/live_stratum_runtime.rs firmware/bitaxe/src/asic_adapter/production.rs` succeeds.
    - `bazel build //firmware/bitaxe:firmware` passes.
  </acceptance_criteria>
  <done>Live firmware feeds hardware observations into correlation, emits mining.submit only with submit intent, and classifies pool responses without overclaiming fake-pool proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| parsed ASIC result -> observation | Generation stamped at firmware boundary from dispatch context. |
| observation -> submit intent | Pure `correlate_nonce_result` gate before any `mining.submit`. |
| socket write -> pool response | Untrusted pool JSON classified only with matching `PendingSubmit`. |
| classified outcome -> evidence markers | Category labels only in retained logs and committed artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-27-06 | Spoofing / Tampering | `apply_bridge_observation` | mitigate | Wrong-session, stale, duplicate, and target-mismatch blockers fail closed. |
| T-27-07 | Repudiation | submit queuing | mitigate | `SendSubmitShare` carries `SubmitIntent` + monotonic `request_id` bound to classifier. |
| T-27-08 | Information Disclosure | share markers | mitigate | Category-only `share_submission_status`; forbidden-pattern scans on firmware bridge files. |
| T-27-09 | Tampering | socket-only submit | mitigate | Static tests prove no `SendSubmitShare` without prior correlated observation in bridge tests. |
</threat_model>

<verification>
Run `bazel test //crates/bitaxe-stratum:tests --test_output=errors`.
Run `bazel build //firmware/bitaxe:firmware`.
Run forbidden-secret pattern scan on `firmware/bitaxe/src/live_stratum_runtime.rs`.
</verification>

<success_criteria>
Live bridge completes notify→dispatch→read→correlate→submit→classify path in firmware tests; STR-09 and ASIC-11 behaviors are implemented at code level pending Phase 27 hardware evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/27-live-hardware-asic-and-stratum-bridge/27-02-SUMMARY.md`.
</output>
