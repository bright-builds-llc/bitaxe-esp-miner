---
phase: 23-redacted-operator-evidence-workflow
plan: 03
type: execute
wave: 2
depends_on: ["23-01", "23-02"]
files_modified:
  - Justfile
  - scripts/BUILD.bazel
  - scripts/phase23-redacted-operator-evidence.sh
  - scripts/phase23-redacted-operator-evidence-test.sh
autonomous: true
requirements: [EVD-07, STR-10, REL-09, CFG-07, EVD-09]
user_setup: []
must_haves:
  truths:
    - "Operator has one repo-owned command path for Phase 23 evidence workflow execution."
    - "Hardware-capable evidence starts with `just detect-ultra205` and blocks on absent, ambiguous, wrong-board, or failed board-info detection."
    - "Local Wi-Fi and pool credential paths are accepted as runtime-only inputs and committed summaries use category labels only."
    - "API and WebSocket captures never use stale targets, network scans, mDNS, ARP, router state, or unrelated evidence."
    - "The workflow can write passed, blocked, pending, or deferred slot artifacts and then invoke the Phase 23 validator."
  artifacts:
    - path: "Justfile"
      provides: "`phase23-evidence` recipe"
      contains: "phase23-evidence"
    - path: "scripts/BUILD.bazel"
      provides: "Bazel sh_binary and sh_test targets"
      contains: "phase23_redacted_operator_evidence"
    - path: "scripts/phase23-redacted-operator-evidence.sh"
      provides: "thin repo-owned workflow shell"
    - path: "scripts/phase23-redacted-operator-evidence-test.sh"
      provides: "synthetic blocked-path, redaction, and validator integration tests"
  key_links:
    - from: "Justfile"
      to: "scripts/phase23-redacted-operator-evidence.sh"
      via: "bazel run //scripts:phase23_redacted_operator_evidence"
      pattern: "phase23-evidence"
    - from: "scripts/phase23-redacted-operator-evidence.sh"
      to: "tools/parity operator-evidence"
      via: "post-write validator invocation"
      pattern: "operator-evidence --evidence-root"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 23-2026-07-04T22-53-37
generated_at: 2026-07-04T23:00:00Z
---

<objective>
Create the repo-owned Phase 23 operator workflow shell and synthetic verification target.

Purpose: Operators need one rerunnable, detector-gated flow for package or flash evidence, runtime-only credential inputs, bounded mining evidence capture, telemetry capture, safe-stop capture, redaction, redaction review, and evidence review. The shell owns orchestration only; pure inventory and leak decisions stay in `tools/parity`.

Output: `just phase23-evidence ...` runs the Phase 23 workflow through Bazel, and `bazel test //scripts:phase23_redacted_operator_evidence_test` proves blocked paths and redaction behavior without real local secrets.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-redacted-operator-evidence-workflow/23-CONTEXT.md
@.planning/phases/23-redacted-operator-evidence-workflow/23-RESEARCH.md
@.planning/phases/23-redacted-operator-evidence-workflow/23-VALIDATION.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@Justfile
@scripts/BUILD.bazel
@scripts/detect-ultra205.sh
@scripts/phase21-live-mining-evidence.sh
@scripts/phase21-live-mining-evidence-test.sh
@scripts/phase21-pool-credentials-json.mjs
@scripts/phase21-pool-input-bridge.sh
@scripts/phase17-live-http-api-smoke.sh
@scripts/phase17-websocket-capture.mjs
@docs/parity/evidence/phase-23-redacted-operator-evidence-workflow/evidence-contract.md
@tools/parity/src/operator_evidence.rs
</context>

<interfaces>
Expected command surface:

```bash
just phase23-evidence --evidence-root docs/parity/evidence/phase-23-redacted-operator-evidence-workflow --manifest bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json --mode blocked
```

Hardware-capable execution must begin inside the script by invoking `just detect-ultra205`. Operators may pass `--pool-credentials pool-credentials.json` or `--wifi-credentials wifi-credentials.json`; the script may pass those paths only to repo-owned helpers and must write committed category labels such as `pool_config: local-owner-supplied`, never raw values.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add just-reachable workflow shell</name>
  <read_first>
    - `.planning/phases/23-redacted-operator-evidence-workflow/23-CONTEXT.md`
    - `.planning/phases/23-redacted-operator-evidence-workflow/23-RESEARCH.md`
    - `.planning/phases/23-redacted-operator-evidence-workflow/23-VALIDATION.md`
    - `AGENTS.md`
    - `Justfile`
    - `scripts/BUILD.bazel`
    - `scripts/detect-ultra205.sh`
    - `scripts/phase21-live-mining-evidence.sh`
    - `scripts/phase21-pool-credentials-json.mjs`
    - `scripts/phase21-pool-input-bridge.sh`
    - `scripts/phase17-live-http-api-smoke.sh`
    - `scripts/phase17-websocket-capture.mjs`
    - `tools/parity/src/operator_evidence.rs`
    - `scripts/phase23-redacted-operator-evidence.sh`
  </read_first>
  <files>Justfile, scripts/BUILD.bazel, scripts/phase23-redacted-operator-evidence.sh</files>
  <action>Create `scripts/phase23-redacted-operator-evidence.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. Add a `phase23-evidence *args:` recipe to `Justfile` that runs `bazel run //scripts:phase23_redacted_operator_evidence -- {{ args }}`. Add `sh_binary(name = "phase23_redacted_operator_evidence", srcs = ["phase23-redacted-operator-evidence.sh"], data = ["phase17-websocket-capture.mjs", "phase17-live-http-api-smoke.sh", "phase21-live-mining-evidence.sh", "phase21-pool-input-bridge.sh", "phase21-pool-credentials-json.mjs", "//tools/parity:report"])` to `scripts/BUILD.bazel`. Implement D-01, D-02, D-03, D-04, D-12, D-13, D-14, D-18, and D-19: accept `--evidence-root PATH`, `--manifest PATH`, `--mode blocked|hardware`, optional `--pool-credentials PATH`, optional `--wifi-credentials PATH`, optional `--duration-seconds N`, optional `--flash-evidence-json PATH`, and optional `--device-url ORIGIN`; in `hardware` mode run `just detect-ultra205` first and block if detection is absent, ambiguous, not board `205`, or board-info fails; never use stale ports, stale `DEVICE_URL`, mDNS, ARP, router state, network scans, or unrelated evidence; write or update the required slot files with `slot_status`, `board: 205`, `redaction_status`, `raw_artifacts_committed: no`, command category labels, and exact non-claims; record `pool_config: local-owner-supplied` and `wifi_config: local-owner-supplied` when those arguments are present without reading, printing, summarizing, or copying credential contents into committed evidence; call `bazel run //tools/parity:report -- operator-evidence --evidence-root "$evidence_root" --require-redaction-passed` after slot writes. Keep the shell thin: delegate pool parsing to the existing Phase 21 helper only when needed, delegate API/WebSocket capture to existing Phase 17 helpers, and keep the pure inventory/redaction decision in `tools/parity`.</action>
  <acceptance_criteria>
    - `test -x scripts/phase23-redacted-operator-evidence.sh`
    - `rg -n "phase23-evidence|phase23_redacted_operator_evidence" Justfile scripts/BUILD.bazel`
    - `rg -n "just detect-ultra205|--mode blocked\\|hardware|pool_config: local-owner-supplied|wifi_config: local-owner-supplied|raw_artifacts_committed: no|operator-evidence" scripts/phase23-redacted-operator-evidence.sh`
    - `rg -n "network_scan=disabled|stale DEVICE_URL|mDNS|ARP|router state|accepted/rejected share outcomes remain non-claims|Phase 25" scripts/phase23-redacted-operator-evidence.sh`
  </acceptance_criteria>
  <verify>
    <automated>bazel build //scripts:phase23_redacted_operator_evidence && bazel test //tools/parity:tests</automated>
  </verify>
  <done>The Phase 23 workflow is reachable through `just phase23-evidence`, registered in Bazel, starts hardware mode with `just detect-ultra205`, writes redacted/category-only slot artifacts, and invokes the `tools/parity` operator-evidence validator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add synthetic workflow tests</name>
  <read_first>
    - `.planning/phases/23-redacted-operator-evidence-workflow/23-CONTEXT.md`
    - `.planning/phases/23-redacted-operator-evidence-workflow/23-RESEARCH.md`
    - `.planning/phases/23-redacted-operator-evidence-workflow/23-VALIDATION.md`
    - `scripts/phase21-live-mining-evidence-test.sh`
    - `scripts/phase23-redacted-operator-evidence.sh`
    - `scripts/BUILD.bazel`
    - `scripts/phase23-redacted-operator-evidence-test.sh`
  </read_first>
  <files>scripts/phase23-redacted-operator-evidence-test.sh, scripts/BUILD.bazel</files>
  <behavior>
    - Test 1: `--mode blocked` creates all eleven slot files, sets missing hardware-dependent slots to `blocked`, `pending`, or `deferred`, and passes the operator-evidence validator.
    - Test 2: Synthetic pool credential fixture values are never present in committed slot files; only `pool_config: local-owner-supplied` and `raw_pool_values_committed: no` remain.
    - Test 3: Synthetic API/WebSocket target values, IPs, MACs, Wi-Fi names, tokens, NVS values, targets, extranonces, share payloads, socket errors, and raw BM1366 frame strings are absent from committed slot files.
    - Test 4: Hardware-mode detector failure writes blocked detector and board-info slots and exits non-zero without trying package, flash, API, WebSocket, or pool helper commands.
  </behavior>
  <action>Create `scripts/phase23-redacted-operator-evidence-test.sh` using the Phase 21 test style with temporary directories, fake helper scripts, `assert_contains`, and `assert_not_contains`. Register it as `sh_test(name = "phase23_redacted_operator_evidence_test", srcs = ["phase23-redacted-operator-evidence-test.sh"], data = ["phase23-redacted-operator-evidence.sh", "phase17-websocket-capture.mjs", "phase17-live-http-api-smoke.sh", "phase21-live-mining-evidence.sh", "phase21-pool-input-bridge.sh", "phase21-pool-credentials-json.mjs"], env_inherit = ["HOME", "NODE_BIN", "NVM_DIR", "PATH"], tags = ["local"])` in `scripts/BUILD.bazel`. Implement D-09, D-10, D-11, D-12, D-17, and D-18 using synthetic values only. The test must not read real `wifi-credentials.json`, real `pool-credentials*.json`, or any ignored local credential file. Test data must use fake sentinels only and must assert those sentinels do not survive under `docs/parity/evidence/phase-23-redacted-operator-evidence-workflow` or the test temp evidence root.</action>
  <acceptance_criteria>
    - `test -f scripts/phase23-redacted-operator-evidence-test.sh`
    - `rg -n "phase23_redacted_operator_evidence_test|phase23-redacted-operator-evidence-test.sh" scripts/BUILD.bazel`
    - `rg -n "assert_not_contains|sentinel-pool.invalid|sentinel-password|sentinel-extra|sentinel-share|raw_bm1366_frame|pool_config: local-owner-supplied|raw_pool_values_committed: no" scripts/phase23-redacted-operator-evidence-test.sh`
    - `rg -n "PHASE23_DETECT_MUST_FAIL|must not have been called|just detect-ultra205|board-info" scripts/phase23-redacted-operator-evidence-test.sh`
  </acceptance_criteria>
  <verify>
    <automated>bazel test //scripts:phase23_redacted_operator_evidence_test //tools/parity:tests</automated>
  </verify>
  <done>The script test proves blocked-mode slot creation, synthetic secret redaction, detector-failure fail-closed behavior, and validator integration without real credential files or real hardware.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| operator CLI arguments to workflow shell | Optional credential paths, device targets, duration, manifest paths, and mode selection enter a shell script and must be parsed into safe command decisions. |
| detector result to hardware-capable actions | Hardware mode can continue only after same-session `just detect-ultra205` and board-info succeed for board `205`. |
| runtime captures to committed evidence root | Logs, API responses, WebSocket frames, and safe-stop markers must be redacted before entering committed slot artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-23-01 | Tampering / Repudiation | `phase23-redacted-operator-evidence.sh` slot writer | mitigate | Task 1 writes every required slot in blocked or hardware modes and invokes the operator-evidence validator after slot creation. |
| T-23-02 | Information Disclosure | credential and capture handling | mitigate | Task 1 records category labels only; Task 2 asserts synthetic pool, owner, target, extranonce, share, socket, NVS/settings, device URL, IP, MAC, Wi-Fi, token, and raw BM1366 frame values do not survive. |
| T-23-03 | Spoofing / Tampering | `--device-url` and API/WebSocket capture | mitigate | Task 1 rejects stale target derivation and scans, accepts only explicit origin-only or same-session trusted flash evidence paths, and records blocked API/WebSocket slots otherwise. |
| T-23-04 | Elevation of Privilege / Tampering | just/Bazel workflow command | mitigate | Task 1 adds a repo-owned `just phase23-evidence` command and keeps package, flash, API, WebSocket, and parity validation behind existing repo-owned helpers. |
| T-23-05 | Repudiation / Integrity | workflow conclusion slots | mitigate | Task 1 writes exact non-claim text and Task 2 verifies blocked mode cannot imply Phase 24/25/26 mining behavior. |
</threat_model>

<verification>
Run the Phase 23 script and validator checks from the validation contract:

```bash
bazel test //scripts:phase23_redacted_operator_evidence_test //tools/parity:tests
just phase23-evidence --evidence-root docs/parity/evidence/phase-23-redacted-operator-evidence-workflow --manifest bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json --mode blocked
bazel run //tools/parity:report -- operator-evidence --evidence-root docs/parity/evidence/phase-23-redacted-operator-evidence-workflow --require-redaction-passed
```
</verification>

<success_criteria>
Plan 03 succeeds when the workflow is `just`-reachable, Bazel-owned, detector-gated for hardware mode, redaction-safe for runtime-only credential inputs, able to emit blocked or pending slots truthfully, and covered by a synthetic sh_test target. Agents must attempt hardware mode first when detection and credentials are available; see `AGENTS.md` → Evidence Workflow: Hardware-First Default.
</success_criteria>

<output>
After completion, create `.planning/phases/23-redacted-operator-evidence-workflow/23-03-SUMMARY.md`.
</output>
