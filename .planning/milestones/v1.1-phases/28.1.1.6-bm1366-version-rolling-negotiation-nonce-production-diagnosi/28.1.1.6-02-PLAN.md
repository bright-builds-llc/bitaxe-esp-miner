---
phase: 28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi
plan: 02
type: execute
wave: 2
depends_on:
  - 28.1.1.6-01
files_modified:
  - crates/bitaxe-stratum/src/v1/mining.rs
  - scripts/phase28.1.1.6-version-rolling-diagnostic.sh
  - scripts/phase28.1.1.6-version-rolling-diagnostic-test.sh
  - .planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-version-rolling-redacted.md
  - .planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-ab-negotiated_version_mask_work_field_parity-redacted.md
autonomous: true
requirements: [STR-09, ASIC-11, CFG-07]
user_setup: []
must_haves:
  truths:
    - "Rust stops discarding maybe_version_mask and attaches negotiated mask to MiningWork while keeping Bm1366WorkFields.version as base notify LE bytes, with pure-crate fixtures including optional increment_bitmask host semantics"
    - "Diagnostic wrapper composes detect-ultra205, verify-reference, existing source-work helpers, comparator, and exactly one ≥360s fake-pool A/B for negotiated_version_mask_work_field_parity"
    - "Redacted A/B evidence records ab_outcome, mask_applied_to_work, job_version_field_class, power_delta_class, result_correlated, fake_pool_submit_observed, wire_parity_ticket_mask_retained, wire_parity_rx_loop_retained without hex or credentials"
  artifacts:
    - path: "crates/bitaxe-stratum/src/v1/mining.rs"
      provides: "Mask attach on MiningWork; base notify version retained on wire fields"
      contains: "maybe_version_mask"
    - path: "scripts/phase28.1.1.6-version-rolling-diagnostic.sh"
      provides: "Detector-gated diagnostic + forced work-field A/B wrapper"
      contains: "negotiated_version_mask_work_field_parity"
    - path: ".planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-ab-negotiated_version_mask_work_field_parity-redacted.md"
      provides: "Redacted ≥360s A/B outcome"
      contains: "ab_outcome:"
  key_links:
    - from: "crates/bitaxe-stratum/src/v1/mining.rs"
      to: "MiningWork.maybe_version_mask"
      via: "stop discard; attach mask; keep fields.version = notify.version.to_le_bytes()"
      pattern: "maybe_version_mask|to_le_bytes"
    - from: "scripts/phase28.1.1.6-version-rolling-diagnostic.sh"
      to: "scripts/phase28.1.1.1-source-work-aligned-capture.sh"
      via: "compose fake-pool capture path"
      pattern: "source-work|duration-seconds|360"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1.6-2026-07-09T13-37-00
generated_at: 2026-07-09T13:50:00Z
---

<objective>
Apply the one confirmed divergence patch — stop discarding `maybe_version_mask`, attach negotiated mask to `MiningWork` (upstream `bm_job.version_mask`), keep BM1366 UART job `version` as base notify version, add pure-crate midstate/`increment_bitmask` fixtures — then run a detector-gated ≥360 s fake-pool hardware A/B labeled `negotiated_version_mask_work_field_parity` and record redacted evidence.

Purpose: RESEARCH verified configure+store already land; remaining diverge is work-builder discard (D-04/D-11). Keep ASIC-256 + RX-loop (D-14). Do **not** implement ASIC mask reload (D-05/D-14). Do not OR mask into job version bytes or expand `num_midstates` on the wire.
Output: mining.rs patch + fixtures; diagnostic wrapper; redacted baseline + A/B reports.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-CONTEXT.md
@.planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-RESEARCH.md
@crates/bitaxe-stratum/src/v1/mining.rs
@crates/bitaxe-stratum/src/v1/live_runtime.rs
@crates/bitaxe-asic/src/bm1366/work.rs
@scripts/phase28.1.1.6-version-rolling-compare.mjs
@scripts/phase28.1.1.1-source-work-aligned-capture.sh
@scripts/phase28.1.1.5-chip-enumerate-diagnostic.sh
@scripts/phase27-live-hardware-bridge-package.sh

<interfaces>
Forced A/B label (RESEARCH PRIMARY / D-04/D-11): `negotiated_version_mask_work_field_parity`

HOW to apply mask (RESEARCH — do not invent encoding):
1. Job UART `version` bytes = base notify version only (`notify.version.to_le_bytes()`). Never OR/XOR mask into `Bm1366WorkFields.version`.
2. Persist negotiated mask on `MiningWork` (new field e.g. `maybe_version_mask: Option<VersionMask>`).
3. Host midstate / `increment_bitmask` helpers + fixtures are pure-crate only; do **not** change `Bm1366WorkPayload` layout or set `num_midstates` ≠ `0x01`.
4. ASIC register reload after configure is **out of scope** — next_hypothesis only (D-05).
5. Keep existing ASIC result → `version_bits` submit path unchanged.

Confirmed patch target (D-04):
```rust
// mining.rs today:
fn build_work_fields_with_extranonce2(..., maybe_version_mask: Option<VersionMask>) -> Result<Bm1366WorkFields, ...> {
    let _ = maybe_version_mask; // REMOVE discard
    ...
    Ok(Bm1366WorkFields { ..., version: notify.version.to_le_bytes() }) // KEEP base
}

// MiningWork today has no mask field — add maybe_version_mask and set it from builder
```

Upstream fixture vector for optional `increment_bitmask` (breadcrumbs only — independent Rust):
- version=0x20000004, mask=0x00ffff00 → 0x20000104, 0x20000204, 0x20000304, 0x20000404
  (reference/esp-miner/components/stratum/test/test_mining.c:108-120)

HARD BANS this plan:
- Do NOT implement ASIC mask reload / `SetVersionMask` outside init (D-05/D-14)
- Do NOT OR mask into job version UART bytes or expand midstates on wire (job-byte patch ban)
- Do NOT reopen falsified levers; retain RX-loop + ASIC-256 (D-14)
- Never modify `reference/esp-miner` (D-16)
- Do NOT edit Phase 30 checklist verified rows

Hardware (D-15):
1. `just detect-ultra205` — exactly one port + board-info success
2. `just verify-reference`
3. Prefer fake-pool via `phase28.1.1.1-source-work-aligned-capture.sh` composition
4. `--duration-seconds` / capture-timeout ≥ 360; agent wall clock ≥ 420
5. Never print/read wifi or pool credential file contents
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Attach negotiated mask to MiningWork + pure-crate fixtures</name>
  <files>crates/bitaxe-stratum/src/v1/mining.rs</files>
  <read_first>
    - crates/bitaxe-stratum/src/v1/mining.rs (discard site ~167-193; existing nonzero-mask test ~259-277)
    - crates/bitaxe-stratum/src/v1/live_runtime.rs (configure + with_version_mask pass-through — verify only)
    - crates/bitaxe-asic/src/bm1366/work.rs (num_midstates=0x01 — do not change layout)
    - .planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-RESEARCH.md (HOW section, Pitfalls 1/3/5)
    - Wave 0 compare report / fixtures from Plan 01 if present (confirm forced_ab_label)
  </read_first>
  <behavior>
    - Nonzero negotiated mask builds successfully and is stored on `MiningWork.maybe_version_mask` (Some)
    - `work.fields.version == notify.version.to_le_bytes()` with nonzero mask (base notify; not rolled/masked)
    - Optional pure helper `increment_bitmask` (or equivalent) matches upstream test vectors when implemented
    - `Bm1366WorkPayload` / `num_midstates` unchanged; no ASIC reload calls added
    - Existing zero-mask / notify tests still pass
  </behavior>
  <action>
    Per D-04 / RESEARCH primary lever `negotiated_version_mask_work_field_parity` (only this divergence family):

    1. Remove `let _ = maybe_version_mask;` discard. Thread the mask onto `MiningWork` (add `pub maybe_version_mask: Option<VersionMask>` or equivalent). Update `MiningWorkBuilder::build` to set the field from `self.maybe_version_mask`.

    2. Keep `Bm1366WorkFields.version = notify.version.to_le_bytes()` always — fixture-assert with nonzero mask (Pitfall 1). Do not change `work.rs` job layout / `num_midstates`.

    3. Add pure-crate fixtures:
       - Nonzero mask attach + base version unchanged (extend/replace existing `0x1fff_e000` test)
       - Optional: `increment_bitmask` host helper + vectors from RESEARCH (version=0x20000004, mask=0x00ffff00 → successive increments). Independent Rust design; breadcrumbs only — no GPL C copy.

    4. Optional compact `version_rolling_summary` stratum/firmware marker (counts only, no hex) only if Plan 01 comparator cannot classify `mask_applied_to_work` after patch — prefer skip if MiningWork field + existing logs suffice (D-10).

    5. Do **not** call ASIC `SetVersionMask` / `version_mask_payload` outside existing init path. Do not patch job bytes, RX-loop, or ticket mask.

    Run focused tests: `cargo test -p bitaxe-stratum mining` (or Bazel equivalent for the mining module).
  </action>
  <acceptance_criteria>
    - `rg -n "let _ = maybe_version_mask" crates/bitaxe-stratum/src/v1/mining.rs` returns no matches
    - `rg -n "maybe_version_mask" crates/bitaxe-stratum/src/v1/mining.rs` shows field on MiningWork + builder attach
    - `rg -n "to_le_bytes\\(\\)" crates/bitaxe-stratum/src/v1/mining.rs` still sets version from notify
    - `git diff -- crates/bitaxe-asic/src/bm1366/work.rs` empty (no job layout change)
    - `rg -n "SetVersionMask|version_mask_payload|ASIC_set_version_mask" crates/bitaxe-stratum/src` shows no new ASIC reload path
    - `cargo test -p bitaxe-stratum mining` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p bitaxe-stratum mining -- --nocapture</automated>
  </verify>
  <done>Mask attached to MiningWork with fixtures; base notify version retained on wire fields; no ASIC reload; no job-byte layout change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Diagnostic wrapper + ≥360s fake-pool A/B evidence</name>
  <files>scripts/phase28.1.1.6-version-rolling-diagnostic.sh, scripts/phase28.1.1.6-version-rolling-diagnostic-test.sh, .planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-version-rolling-redacted.md, .planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-ab-negotiated_version_mask_work_field_parity-redacted.md</files>
  <read_first>
    - scripts/phase28.1.1.5-chip-enumerate-diagnostic.sh
    - scripts/phase28.1.1.5-chip-enumerate-diagnostic-test.sh
    - scripts/phase28.1.1.1-source-work-aligned-capture.sh
    - scripts/phase28.1.1.6-version-rolling-compare.mjs
    - AGENTS.md (detect-ultra205, capture-timeout ≥360, redaction, fake-pool preferred)
    - .planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-CONTEXT.md (D-11, D-12, D-14, D-15, D-16)
  </read_first>
  <behavior>
    - Dry-run: requires --port, --wifi-credentials, --duration-seconds≥360, --capture-dir; refuses duration &lt; 360; does not recommend falsified knobs; prints forced A/B label `negotiated_version_mask_work_field_parity`
    - Hardware path: detect-ultra205 → verify-reference → baseline compare (scratch or fresh) → package/flash work-field candidate → ≥360s fake-pool capture → compare → write redacted A/B
    - If detect/credentials/capture blocked → write `ab_outcome: blocked_safe_prerequisite` precisely; do not invent frame bytes or implement ASIC reload
  </behavior>
  <action>
    Create `scripts/phase28.1.1.6-version-rolling-diagnostic.sh` composing existing helpers (D-09), modeled on `phase28.1.1.5-chip-enumerate-diagnostic.sh`:

    Required flags: `--port`, `--wifi-credentials`, `--duration-seconds` (≥360), `--capture-dir` (must be gitignored), optional `--dry-run`.

    Flow:
    1. Enforce duration ≥ 360
    2. Prefer composing `phase28.1.1.1-source-work-aligned-capture.sh` / phase27 bridge package for Rust flash+monitor (fake-pool preferred — already returns version-rolling.mask)
    3. Run `phase28.1.1.6-version-rolling-compare.mjs` for baseline (scratch upstream SERIALTX + pre-patch rust log OK for category baseline)
    4. Forced single A/B label: `negotiated_version_mask_work_field_parity` — flash/run the mask-on-MiningWork candidate only (no ASIC reload, no second lever — D-11/D-14)
    5. Capture timeout ≥360 s; do not treat early exit as failure until timeout elapses unless hard flash error
    6. Write redacted Markdown under the phase dir:
       - `28.1.1.6-version-rolling-redacted.md` — baseline comparator fields (D-07 keys)
       - `28.1.1.6-ab-negotiated_version_mask_work_field_parity-redacted.md` — must include:
         - `ab_label: negotiated_version_mask_work_field_parity`
         - `ab_outcome: improved|unchanged|regressed|blocked_safe_prerequisite`
         - `configure_observed:` / `mask_stored_class:` / `mask_applied_to_work:`
         - `job_version_field_class:` (expect `base_notify` after patch)
         - `version_mask_tx_class:` / `version_bits_on_submit_path:`
         - `power_delta_class:`
         - `result_correlated:` / `fake_pool_submit_observed:`
         - `wire_parity_ticket_mask_retained: true|false`
         - `wire_parity_rx_loop_retained: true|false`
         - `capture_timeout_seconds:` (≥360)
         - `board: 205`
         - `raw_bytes_committed: false` / `credential_contents_read: false`
         - `phase30_promotion_input: pending`
         - `asic_mask_reload_applied: false`
         - `job_byte_patch_applied: false` and falsified-lever false flags

    `improved` definition for later Plan 03 (record honestly here): `result_correlated: true` and/or `fake_pool_submit_observed: true` (D-12). Optionally note `rising_hashing` as corroboration only. Note Pitfall 5: fake-pool mask equals init default — unchanged hashing is a valid falsification path.

    Create `scripts/phase28.1.1.6-version-rolling-diagnostic-test.sh` dry-run assertions (duration floor, forced label, no falsified knobs, gitignore capture-dir check).

    Before hardware: run `just detect-ultra205` and `just verify-reference`. If detection fails, stop with blocked evidence — do not guess.

    Never commit raw logs; keep under `scratch/` or ignored `hardware-runs/`.
  </action>
  <acceptance_criteria>
    - `test -f scripts/phase28.1.1.6-version-rolling-diagnostic.sh`
    - `test -f scripts/phase28.1.1.6-version-rolling-diagnostic-test.sh`
    - `bash scripts/phase28.1.1.6-version-rolling-diagnostic-test.sh` exits 0
    - `rg -n "negotiated_version_mask_work_field_parity" scripts/phase28.1.1.6-version-rolling-diagnostic.sh`
    - `rg -n "duration_seconds|360" scripts/phase28.1.1.6-version-rolling-diagnostic.sh`
    - `rg -n "^ab_outcome:" .planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-ab-negotiated_version_mask_work_field_parity-redacted.md`
    - `rg -n "raw_bytes_committed: false" .planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-ab-negotiated_version_mask_work_field_parity-redacted.md`
    - `rg -n "asic_mask_reload_applied: false" .planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-ab-negotiated_version_mask_work_field_parity-redacted.md`
    - `rg -n "wire_parity_rx_loop_retained:" .planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-ab-negotiated_version_mask_work_field_parity-redacted.md`
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase28.1.1.6-version-rolling-diagnostic-test.sh</automated>
  </verify>
  <done>Diagnostic dry-run green; redacted ≥360s A/B (or precise blocked_safe_prerequisite) recorded for negotiated_version_mask_work_field_parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wifi-credentials.json / pool-credentials → flash tools | Pass-by-path only; never read/print/commit contents |
| UART monitor → evidence Markdown | Hex and endpoints must stay in ignored scratch; committed docs use categories |
| reference/esp-miner → scratch clone | Upstream edits only in scratch; verify-reference must stay clean |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28116-06 | I | diagnostic.sh + A/B Markdown | mitigate | Category labels only; `credential_contents_read: false`; redaction sweep before commit (CFG-07) |
| T-28116-07 | I | uart_trace / SERIALTX captures | mitigate | Store under gitignored capture-dir/scratch; reports forbid hex dumps |
| T-28116-08 | T | reference/esp-miner | mitigate | `just verify-reference` before/after; D-16 scratch-only golden helpers |
| T-28116-09 | E | overclaim STR-09 verified / share outcomes | mitigate | `phase30_promotion_input: pending`; Plan 03/04 own disposition; no checklist verified edits; record correlate/submit honestly |
| T-28116-10 | D | unsafe voltage/fan stress | accept | Out of scope; only standard bring-up + fake-pool mining path |
</threat_model>

<verification>
- `cargo test -p bitaxe-stratum mining` passes with mask-attach + base-version fixtures
- `bash scripts/phase28.1.1.6-version-rolling-diagnostic-test.sh` passes
- Redacted A/B file exists with `ab_outcome` and ≥360 capture timeout field
</verification>

<success_criteria>
- `negotiated_version_mask_work_field_parity` is the sole code divergence family introduced (mask-on-MiningWork; base version retained)
- One detector-gated ≥360 s fake-pool A/B (or precise blocked record) exists
- No ASIC reload, job-byte / falsified-lever / reference-tree mutations; RX-loop + ASIC-256 retained
</success_criteria>

<output>
After completion, create `.planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-02-SUMMARY.md`
</output>
