---
phase: 28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase28.1.1.6-version-rolling-compare.mjs
  - scripts/phase28.1.1.6-version-rolling-compare-test.mjs
autonomous: true
requirements: [CFG-07, ASIC-11, STR-09]
user_setup: []
must_haves:
  truths:
    - "A repo-owned version-rolling comparator emits configure_observed, mask_stored_class, mask_applied_to_work, version_mask_tx_class, version_bits_on_submit_path, power_delta_class, result_correlated, and fake_pool_submit_observed without raw UART hex or credentials"
    - "When configure is observed and mask is stored but mask_applied_to_work is false, forced_ab_label and preferred recommended_investigation are negotiated_version_mask_work_field_parity"
    - "recommended_investigation never emits post_max_baud_delay_2000, match_upstream_register_read_poll, upstream_like_long_block_receive, ticket_mask_asic_difficulty, or count_asic_chips_rx_loop_parity"
  artifacts:
    - path: "scripts/phase28.1.1.6-version-rolling-compare.mjs"
      provides: "Redaction-safe version-rolling category report composing 28.1.1.5/2 parsers"
      contains: "negotiated_version_mask_work_field_parity"
    - path: "scripts/phase28.1.1.6-version-rolling-compare-test.mjs"
      provides: "Fixture tests for mask_stored vs mask_applied diverge, falsified-knob ban, forced_ab_label"
      contains: "forced_ab_label"
  key_links:
    - from: "scripts/phase28.1.1.6-version-rolling-compare.mjs"
      to: "scripts/phase28.1.1.5-chip-enumerate-compare.mjs"
      via: "compose/reuse lineEvents classifyFrame summarizeLog / power_delta_class"
      pattern: "phase28.1.1.5-chip-enumerate-compare|phase28.1.1.2-result-path-compare"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1.6-2026-07-09T13-37-00
generated_at: 2026-07-09T13:50:00Z
---

<objective>
Ship Wave 0 redaction-safe version-rolling comparator so configure/mask-store vs mask-applied-to-work, version_mask TX class, version_bits submit path, and power_delta/correlate/submit are category-compared before any work-field patch or hardware A/B.

Purpose: D-02/D-07/D-08 require golden category evidence that configure+store is insufficient without work-field apply; RESEARCH forces first A/B label `negotiated_version_mask_work_field_parity` (D-04/D-11).
Output: `scripts/phase28.1.1.6-version-rolling-compare.mjs` + test; optional compact firmware markers deferred (skip if existing logs suffice — D-10).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-CONTEXT.md
@.planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-RESEARCH.md
@.planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-VALIDATION.md
@scripts/phase28.1.1.5-chip-enumerate-compare.mjs
@scripts/phase28.1.1.2-result-path-compare.mjs
@crates/bitaxe-stratum/src/v1/mining.rs
@crates/bitaxe-stratum/src/v1/live_runtime.rs

<interfaces>
<!-- Compose 28.1.1.5 / 28.1.1.2 — do not invent a parallel taxonomy (D-09). -->

From scripts/phase28.1.1.2-result-path-compare.mjs:
- `classifyFrame`, `lineEvents`, `summarizeLog`
- Events: `job_tx`, `result_correlated`, `fake_pool_submit`, `version_mask_tx` when present

From scripts/phase28.1.1.5-chip-enumerate-compare.mjs:
- Reuse `power_delta_class` rules and retention flags where practical
- Do **not** reopen `count_asic_chips_rx_loop_parity` as a recommender (HARD BAN this phase)

Confirmed RESEARCH facts (encode in fixtures, not guess):
- `mining.configure` already lands; negotiated mask stored in `LiveStratumRuntime`
- Mask passed into `MiningWorkBuilder::with_version_mask` but discarded in `build_work_fields_with_extranonce2` (`let _ = maybe_version_mask`)
- Job 11/11 covers **base notify version only** — not rolled-version / mask-on-work semantics (D-02)
- Fake pool returns `version-rolling.mask=1fffe000` (equals init default)

D-07 required report fields (exact keys):
- `configure_observed: true|false`
- `mask_stored_class: stored|missing|unavailable`
- `mask_applied_to_work: true|false`
- `mask_value_class: default_1fffe000|other_nonzero|zero|unavailable`
- `version_mask_tx_class: prelude_3|init_register|runtime_reload|none|unavailable`
- `job_version_field_class: base_notify|rolled_or_masked|unavailable`
- `version_bits_on_submit_path: present|absent|unavailable`
- `power_delta_class: rising_hashing|flat|falling|unavailable`
- `result_correlated: true|false`
- `fake_pool_submit_observed: true|false`
- `wire_parity_ticket_mask_retained: true|false`
- `wire_parity_rx_loop_retained: true|false`
- `forced_ab_label: negotiated_version_mask_work_field_parity|none`
- `recommended_investigation:` closed enum below
- `raw_bytes_committed: false`
- `credential_contents_read: false`
- `phase30_promotion_input: pending`
- `asic_mask_reload_recommended: false` (always false this phase — D-05/D-14; next_hypothesis only after A/B)

Closed `recommended_investigation` set for this phase (D-08):
`negotiated_version_mask_work_field_parity` | `pool_negotiated_mask_asic_reload` | `none`

HARD BAN (fail tests if emitted as recommended_investigation or forced_ab_label):
`post_max_baud_delay_2000`, `match_upstream_register_read_poll`, `upstream_like_long_block_receive`, `ticket_mask_asic_difficulty`, `count_asic_chips_rx_loop_parity`

`forced_ab_label` default (RESEARCH Pattern 2 / D-04/D-11):
1. If blocked → `none`
2. Else if configure_observed + mask_stored + `mask_applied_to_work: false` + still no `result_correlated` → `negotiated_version_mask_work_field_parity`
3. Else if mask already applied to work + still no correlate → `none` (Plan 02/03 own ASIC-reload next_hypothesis; Wave 0 must not force ASIC reload as A/B)
4. Else → `none`
5. **Never** emit HARD BAN labels

Recommender preference (D-08):
1. If blocked → `none`
2. Else if mask stored but not applied to work → `negotiated_version_mask_work_field_parity`
3. Else if mask applied to work + still no correlate → `pool_negotiated_mask_asic_reload` (recommend only; do not implement)
4. Else → `none`

power_delta_class rules (D-06; fast feedback only — not phase pass gate):
- `unavailable` if marker missing or `unavailable=true`
- `rising_hashing` if `delta_mw >= 2000`
- `falling` if `delta_mw <= -200`
- `flat` otherwise
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement version-rolling comparator CLI with D-07 fields</name>
  <files>scripts/phase28.1.1.6-version-rolling-compare.mjs</files>
  <read_first>
    - scripts/phase28.1.1.5-chip-enumerate-compare.mjs
    - scripts/phase28.1.1.2-result-path-compare.mjs
    - .planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-CONTEXT.md (D-02, D-04, D-06, D-07, D-08, D-09, D-10)
    - .planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-RESEARCH.md (Patterns 1–2, Pitfalls 2/4/5, Already Matched table)
  </read_first>
  <behavior>
    - Missing upstream or rust log → `comparison_status: blocked_safe_prerequisite`, `recommended_investigation: none`, `forced_ab_label: none`
    - Fixture: configure_observed true, mask_stored_class stored, mask_applied_to_work false, power_delta falling, no correlate → `forced_ab_label: negotiated_version_mask_work_field_parity`, `recommended_investigation: negotiated_version_mask_work_field_parity`
    - Fixture: mask_applied_to_work true + still no correlate → `recommended_investigation: pool_negotiated_mask_asic_reload`, `forced_ab_label: none`
    - Never recommend falsified knobs including `count_asic_chips_rx_loop_parity`; never recommend ASIC reload as forced A/B this wave
    - Report has no multi-byte hex dumps or credential sentinels
  </behavior>
  <action>
    Create `scripts/phase28.1.1.6-version-rolling-compare.mjs` (Node ESM, stdlib only) accepting `--upstream`, `--rust`, `--out` (per D-09 compose with 28.1.1.5/2 — import `lineEvents` / `classifyFrame` / `summarizeLog` from `scripts/phase28.1.1.2-result-path-compare.mjs`; reuse power_delta / retention helpers from 28.1.1.5 where practical; do not invent a parallel capture stack).

    Classify configure/mask/work/submit categories from markers and frame events only (no committed hex). Detect configure / mask-store / version_mask_tx via existing event taxonomy or compact markers already emitted by uart_trace / stratum logs — never require raw hex in fixtures (D-07).

    Derive `mask_applied_to_work` from structural markers (e.g. work-builder / MiningWork mask-present markers) when available; otherwise treat pre-patch Rust as `false` when configure+store are true (matches known discard site). Prefer category markers over inventing hex parsers.

    Recommender (D-08 / D-04):
    1. If blocked → `none`
    2. Else if mask stored but not applied → `negotiated_version_mask_work_field_parity`
    3. Else if mask applied + still no `result_correlated` → `pool_negotiated_mask_asic_reload`
    4. Else → `none`
    5. **Never** emit HARD BAN strings

    Emit `forced_ab_label` per RESEARCH Pattern 2 rules in `<interfaces>` (default `negotiated_version_mask_work_field_parity` for expected Ultra 205 fixture).

    Emit Markdown with all D-07 keys listed in `<interfaces>`. Keep `phase30_promotion_input: pending` (D-14/D-17). Assert `wire_parity_ticket_mask_retained` + `wire_parity_rx_loop_retained` from prior phase retention when markers present (status only — not recommenders). Set `asic_mask_reload_recommended: false` always in Wave 0 reports.
  </action>
  <acceptance_criteria>
    - `test -f scripts/phase28.1.1.6-version-rolling-compare.mjs`
    - `rg -n "negotiated_version_mask_work_field_parity" scripts/phase28.1.1.6-version-rolling-compare.mjs`
    - `rg -n "forced_ab_label" scripts/phase28.1.1.6-version-rolling-compare.mjs`
    - `rg -n "mask_applied_to_work" scripts/phase28.1.1.6-version-rolling-compare.mjs`
    - `rg -n "pool_negotiated_mask_asic_reload" scripts/phase28.1.1.6-version-rolling-compare.mjs`
    - `rg -n "phase28.1.1.2-result-path-compare" scripts/phase28.1.1.6-version-rolling-compare.mjs`
    - `rg -n "asic_mask_reload_recommended: false" scripts/phase28.1.1.6-version-rolling-compare.mjs`
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/phase28.1.1.6-version-rolling-compare.mjs && rg -n "forced_ab_label|negotiated_version_mask_work_field_parity|HARD_BAN|count_asic_chips_rx_loop_parity" scripts/phase28.1.1.6-version-rolling-compare.mjs</automated>
  </verify>
  <done>Comparator CLI exists, composes prior parsers, emits D-07 category fields, closed recommender, and forced_ab_label. Behavioral recommender/ban fixtures are gated by Task 2 compare-test (Wave 0 behavioral gate).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 fixture tests locking recommender and bans</name>
  <files>scripts/phase28.1.1.6-version-rolling-compare-test.mjs</files>
  <read_first>
    - scripts/phase28.1.1.5-chip-enumerate-compare-test.mjs
    - scripts/phase28.1.1.6-version-rolling-compare.mjs
    - .planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-VALIDATION.md
  </read_first>
  <behavior>
    - Unit fixtures cover: configure+mask_stored + mask_applied false → `forced_ab_label: negotiated_version_mask_work_field_parity`; mask applied + no correlate → recommend `pool_negotiated_mask_asic_reload` with forced_ab none; blocked missing logs; `power_delta_class` falling/rising/unavailable
    - Assert report body never matches multi-byte hex dump patterns like `55 AA 52` / `55 AA 51` sequences as committed content, and never contains `poolPassword` / `poolURL` / credential JSON keys
    - Assert HARD BAN strings never appear as `recommended_investigation` or `forced_ab_label` values (including `count_asic_chips_rx_loop_parity`)
    - Assert `asic_mask_reload_recommended: false` always
    - No firmware marker required in this plan (D-10: skip if existing logs suffice)
  </behavior>
  <action>
    Create `scripts/phase28.1.1.6-version-rolling-compare-test.mjs` using `node:assert` (mirror 28.1.1.5 test style). Include synthetic upstream/rust log fixtures (inline strings, not committed scratch hex dumps) that encode:
    1. Expected Ultra 205 diverge: configure_observed, mask_stored, mask_applied_to_work false, `delta_mw=-390` → falling, `forced_ab_label: negotiated_version_mask_work_field_parity`, recommend `negotiated_version_mask_work_field_parity`
    2. Mask-applied path: mask_applied_to_work true, still no correlate → recommend `pool_negotiated_mask_asic_reload`, `forced_ab_label: none`
    3. Missing log → blocked / `forced_ab_label: none`
    4. Falsified-knob ban: after running recommender on gap fixtures, assert recommendation and forced_ab_label ∉ HARD BAN set (including `count_asic_chips_rx_loop_parity` and `ticket_mask_asic_difficulty`)
    5. Never recommend ASIC reload as forced A/B label

    Do not patch `mining.rs` / attach mask in this plan (Plan 02 owns the patch). Do not edit firmware markers unless comparator cannot classify from existing traces — prefer skip (D-10).
  </action>
  <acceptance_criteria>
    - `test -f scripts/phase28.1.1.6-version-rolling-compare-test.mjs`
    - `node scripts/phase28.1.1.6-version-rolling-compare-test.mjs` exits 0
    - `rg -n "negotiated_version_mask_work_field_parity" scripts/phase28.1.1.6-version-rolling-compare-test.mjs`
    - `rg -n "post_max_baud_delay_2000|match_upstream_register_read_poll|upstream_like_long_block_receive|ticket_mask_asic_difficulty|count_asic_chips_rx_loop_parity" scripts/phase28.1.1.6-version-rolling-compare-test.mjs` (ban assertions present)
    - `rg -n "forced_ab_label" scripts/phase28.1.1.6-version-rolling-compare-test.mjs`
    - `rg -n "mask_applied_to_work" scripts/phase28.1.1.6-version-rolling-compare-test.mjs`
  </acceptance_criteria>
  <verify>
    <automated>node scripts/phase28.1.1.6-version-rolling-compare-test.mjs</automated>
  </verify>
  <done>Wave 0 compare-test green; recommender, forced_ab_label, and redaction bans locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| USB monitor logs → comparator → committed Markdown | Untrusted device logs may contain UART hex, SSIDs, IPs; committed reports must stay category/boolean only |
| Local credential files → flash/monitor (not this plan) | Credentials must never be read into comparator inputs or reports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28116-01 | I | version-rolling-compare.mjs report | mitigate | Emit field categories/counts/booleans only; unit tests fail on hex dumps and credential key leakage (CFG-07) |
| T-28116-02 | I | synthetic fixtures in compare-test | mitigate | Inline category markers only; never commit raw UART hex dumps |
| T-28116-03 | E | phase30_promotion_input / checklist | mitigate | Hard-code `phase30_promotion_input: pending`; no checklist edits in this plan (D-14) |
| T-28116-04 | E | overclaim share outcomes | mitigate | Comparator records correlate/submit booleans only; does not claim STR-09 verified |
| T-28116-05 | T | reference/esp-miner | accept | This plan does not touch reference; later plans use scratch-only helpers + verify-reference |
</threat_model>

<verification>
- `node scripts/phase28.1.1.6-version-rolling-compare-test.mjs` passes
- Sample CLI run on synthetic fixtures writes report with D-07 keys and `raw_bytes_committed: false`
</verification>

<success_criteria>
- Comparator detects expected Ultra 205 mask-stored-but-not-applied diverge and sets `forced_ab_label: negotiated_version_mask_work_field_parity`
- Falsified investigation knobs (including enumerate RX-loop) cannot appear as recommendations
- No raw UART hex or credentials in committed report output; no ASIC-reload forced A/B
</success_criteria>

<output>
After completion, create `.planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-01-SUMMARY.md`
</output>
