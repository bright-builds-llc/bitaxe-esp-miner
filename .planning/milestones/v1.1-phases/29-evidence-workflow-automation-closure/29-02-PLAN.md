---
phase: "29"
plan: "02"
type: execute
wave: 2
depends_on:
  - "29-01"
files_modified:
  - scripts/phase23-redacted-operator-evidence.sh
  - scripts/phase23-redacted-operator-evidence-test.sh
  - scripts/phase25-live-stratum-evidence.sh
  - scripts/phase25-live-stratum-evidence-test.sh
  - scripts/phase27-live-hardware-bridge-evidence.sh
  - scripts/phase27-live-hardware-bridge-evidence-test.sh
  - scripts/phase28-evidence.sh
  - scripts/phase28-evidence-test.sh
  - scripts/BUILD.bazel
  - Justfile
autonomous: true
requirements:
  - EVD-07
  - EVD-08
  - EVD-09
  - REL-09
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 29-2026-07-13T00-19-45
generated_at: 2026-07-13T00:34:00.000Z
must_haves:
  truths:
    - Phase 25 and Phase 27 finish through one explicit finalizer and invoke strict operator validation exactly once and last.
    - A passing validator never masks a detector, prerequisite, capture, safe-stop, or earlier validator failure.
    - Blocked and detector-failure paths still leave a complete eleven-slot root before returning nonzero.
    - `just phase28-evidence` is Bazel-owned, deterministic, cross-link-only, and preserves the previous destination on failure.
  artifacts:
    - path: scripts/phase25-live-stratum-evidence.sh
      provides: Phase 25 complete-root terminal finalization.
    - path: scripts/phase27-live-hardware-bridge-evidence.sh
      provides: Phase 27 complete-root terminal finalization.
    - path: scripts/phase28-evidence.sh
      provides: Thin Phase 28 consolidation and validation wrapper.
    - path: scripts/phase28-evidence-test.sh
      provides: Deterministic atomic-consolidation regression coverage.
    - path: Justfile
      provides: Human-facing `phase28-evidence` command.
  key_links:
    - from: scripts/phase25-live-stratum-evidence.sh
      to: tools/parity:report
      via: complete-operator-evidence then operator-evidence --profile phase25
    - from: scripts/phase27-live-hardware-bridge-evidence.sh
      to: tools/parity:report
      via: complete-operator-evidence then operator-evidence --profile phase27
    - from: Justfile
      to: scripts/BUILD.bazel
      via: bazel run //scripts:phase28_evidence
---

<objective>
Wire the typed evidence-root core into the Phase 23/25/27 wrappers and deliver the Bazel-owned Phase 28 consolidation command.

Purpose: close the manual validate/consolidate gaps at the operator command surface while preserving existing detector, hardware, credential, safe-stop, redaction, and exact-claim behavior.
Output: single-finalizer Phase 25/27 workflows, explicit Phase 23 profile use, a new atomic Phase 28 wrapper, deterministic regression tests, Bazel targets, and a Just alias.

The ten-file surface is intentionally split into two sequential ownership clusters: Task 1 touches only existing Phase 23/25/27 wrappers and their paired tests; Task 2 exclusively owns the new Phase 28 wrapper plus shared `scripts/BUILD.bazel` and `Justfile` routing. No task edits the other task's wrapper files.
</objective>

<execution_context>
@.planning/phases/29-evidence-workflow-automation-closure/29-CONTEXT.md
@.planning/phases/29-evidence-workflow-automation-closure/29-RESEARCH.md
@.planning/phases/29-evidence-workflow-automation-closure/29-01-SUMMARY.md
</execution_context>

<threat_model>
## Threat Model

**Assets:** local credentials, redacted evidence roots, true workflow exit status, prior valid Phase 28 output, exact parity non-claims.

**Trust boundaries:** wrapper arguments and environment; hardware/detector command output; shell-to-parity CLI calls; Phase 27 source root to Phase 28 destination.

| Threat | Severity | Mitigation | Verification |
| --- | --- | --- | --- |
| Credential or device value leaks through command traces | High | Preserve category-label arguments, never echo credential contents, keep fake parity traces path/category-only. | Wrapper tests seed forbidden sentinels and assert they are absent from roots and stdout. |
| Early return skips operator validation | High | Route every post-setup terminal path through one explicit `finalize_evidence` function. | Trace tests assert one finalizer and one final operator-evidence call for every disposition. |
| Passing validator masks original failure | High | Capture base, mining-allow, and operator statuses separately; return failure if any failed. | Matrix tests cover all status combinations. |
| Partial blocked roots validate accidentally | High | Call typed completion before validators on blocked and detector-failure paths. | Tests assert all eleven files exist before the first validation trace event. |
| Phase 28 overwrites arbitrary/nested destination | High | Delegate canonical/nested/symlink checks and staging promotion to the typed Rust core. | Wrapper regression asserts rejection and unchanged destination digest. |
| Raw Phase 27 artifacts copied into Phase 28 | High | Wrapper passes paths only; Rust core renders allowlisted cross-links/categories. | Tests compare destination inventory and reject copied source-only sentinel files. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Route Phase 23, Phase 25, and Phase 27 through explicit profiles and terminal validation</name>
  <files>scripts/phase23-redacted-operator-evidence.sh, scripts/phase23-redacted-operator-evidence-test.sh, scripts/phase25-live-stratum-evidence.sh, scripts/phase25-live-stratum-evidence-test.sh, scripts/phase27-live-hardware-bridge-evidence.sh, scripts/phase27-live-hardware-bridge-evidence-test.sh</files>
  <read_first>
    - scripts/phase23-redacted-operator-evidence.sh
    - scripts/phase23-redacted-operator-evidence-test.sh
    - scripts/phase25-live-stratum-evidence.sh
    - scripts/phase25-live-stratum-evidence-test.sh
    - scripts/phase27-live-hardware-bridge-evidence.sh
    - scripts/phase27-live-hardware-bridge-evidence-test.sh
    - tools/parity/src/main.rs
    - tools/parity/src/operator_evidence/profile.rs
    - AGENTS.md
  </read_first>
  <action>
Before wrapper production edits, extend the three existing paired test scripts with redaction-safe fake-parity trace capture and assertions for explicit profiles, complete blocked roots, exact invocation count/order, and failure precedence. Run the Bazel tests and record the expected red failures locally; do not commit the red state.

Update Phase 23's terminal validation to pass `--profile phase23`, preserving all current output and blocked/hardware behavior.

For Phase 25 and Phase 27, replace post-setup branch-local terminal exits with a `workflow_status` accumulator and one `finalize_evidence` function. The function must call, in this exact order: (1) `complete-operator-evidence --profile phase25|phase27 --evidence-root "$evidence_root" --workflow-status "$workflow_status"`; (2) existing `mining-allow` only when its manifest is complete/applicable; (3) `operator-evidence --profile phase25|phase27 --evidence-root "$evidence_root" --require-redaction-passed` exactly once and last. Capture each exit status under `set -euo pipefail` without suppressing or replacing it. Return nonzero if the original workflow, completion, mining-allow, or operator validation failed.

Do not change detector selection, board `205` checks, hardware command construction, credential path handling, 360-second hardware minimum, redaction categories, share-outcome derivation, or safe-stop logic. Help/argument-validation exits before workflow setup need not create roots and must not arm finalization.

Extend each shell test's fake parity executable to append one redaction-safe argv event per invocation. Assert exact order and count for blocked mode, hardware success fixture, missing credentials/prerequisites, detector failure, mining-allow failure, operator failure, and prior-workflow failure. Assert all eleven slots exist before validation. No test may print real evidence paths containing local identifiers or credential content.
  </action>
  <verify>
    <automated>bazel test //scripts:phase23_redacted_operator_evidence_test //scripts:phase25_live_stratum_evidence_test //scripts:phase27_live_hardware_bridge_evidence_test</automated>
  </verify>
  <acceptance_criteria>
    - Phase 23 invokes `operator-evidence --profile phase23` with `--require-redaction-passed`.
    - Phase 25 and Phase 27 each contain exactly one `finalize_evidence()` definition and one terminal `operator-evidence` invocation.
    - Test traces show `complete-operator-evidence`, then applicable `mining-allow`, then `operator-evidence` last.
    - Detector-failure tests assert all eleven canonical slot files exist and the wrapper still returns nonzero.
    - Failure-matrix tests prove no passing validator masks an earlier nonzero status.
    - The three Bazel script test targets exit 0.
  </acceptance_criteria>
  <commit_gate>
Before committing this task, run these commands separately and in this exact order, stopping on the first failure: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Then rerun the task-specific Bazel verification. Do not commit unless every command passes.
  </commit_gate>
  <done>All three existing workflows use explicit profiles, Phase 25/27 route every post-setup outcome through one finalizer, trace/failure tests pass, and the full ordered Rust gate passes before commit.</done>
</task>

<task type="auto">
  <name>Task 2: Add the atomic Phase 28 wrapper, Bazel targets, and Just command</name>
  <files>scripts/phase28-evidence.sh, scripts/phase28-evidence-test.sh, scripts/BUILD.bazel, Justfile</files>
  <read_first>
    - tools/parity/src/main.rs
    - tools/parity/src/operator_evidence/generation.rs
    - scripts/phase23-redacted-operator-evidence.sh
    - scripts/phase27-live-hardware-bridge-evidence.sh
    - scripts/BUILD.bazel
    - Justfile
    - docs/parity/evidence/phase-28-hardware-evidence-and-checklist-promotion/evidence-contract.md
    - AGENTS.md
  </read_first>
  <action>
Before implementing the wrapper, create `scripts/phase28-evidence-test.sh` and its `phase28_evidence_test` Bazel target with accepted, rejected, blocked, root-relation, deterministic-rerun, raw-sentinel, and failure-preservation assertions. Run the target and record the expected red failure caused by the missing/incomplete wrapper locally; do not commit the red state.

Create `scripts/phase28-evidence.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. Require repo-relative `--phase27-root PATH` and repo-relative `--evidence-root PATH`; accept an optional `PARITY_COMMAND` test override using the existing wrapper convention, but do not accept credentials, ports, device URLs, or raw-log inputs. Render and log only category-safe operation names.

The wrapper must call `consolidate-phase28-evidence --phase27-root "$phase27_root" --evidence-root "$evidence_root"`, then call `operator-evidence --profile phase28 --evidence-root "$evidence_root" --require-redaction-passed` exactly once and last. Let the Rust core own root relationship checks, staging, atomic replacement, and deterministic rendering; do not duplicate that logic in shell.

Create `scripts/phase28-evidence-test.sh` with isolated temporary fixtures for accepted, rejected, and blocked source outcomes. Cover missing/contradictory source fields, equal/nested roots, source-only forbidden sentinel files, unknown destination files, forced consolidation failure, forced validation failure, deterministic reruns, and unchanged-destination digests. Trace only command categories and booleans.

Add `sh_binary(name = "phase28_evidence", srcs = ["phase28-evidence.sh"], data = ["//tools/parity:report"])` and `sh_test(name = "phase28_evidence_test", srcs = ["phase28-evidence-test.sh"], data = ["phase28-evidence.sh"], env_inherit = ["HOME", "PATH"], tags = ["local"])` to `scripts/BUILD.bazel`. Add `phase28-evidence *args:` to `Justfile` invoking `bazel run //scripts:phase28_evidence -- {{ args }}`.
  </action>
  <verify>
    <automated>bazel test //scripts:phase28_evidence_test //tools/parity:tests</automated>
  </verify>
  <acceptance_criteria>
    - `Justfile` contains a `phase28-evidence *args:` recipe routing to `//scripts:phase28_evidence`.
    - `scripts/BUILD.bazel` contains `phase28_evidence` and `phase28_evidence_test` targets with `//tools/parity:report` available to the wrapper.
    - The wrapper accepts only source root, destination root, and the established test command override; it has no credential, port, device URL, or raw-log flags.
    - Trace tests show consolidation first and strict Phase 28 operator validation exactly once and last.
    - Accepted, rejected, and blocked fixture tests exit 0 without changing their outcome category.
    - Failure tests prove the previous destination digest is unchanged.
    - `bazel test //scripts:phase28_evidence_test //tools/parity:tests` exits 0.
  </acceptance_criteria>
  <commit_gate>
Before committing this task, run these commands separately and in this exact order, stopping on the first failure: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Then rerun the task-specific Bazel verification. Do not commit unless every command passes.
  </commit_gate>
  <done>The Phase 28 test target was written first, the wrapper exposes only repo-relative source/destination inputs, all atomic/deterministic/redaction cases pass, and the full ordered Rust gate passes before commit.</done>
</task>

</tasks>

<verification>
- Run `cargo fmt --all`.
- Run `cargo clippy --all-targets --all-features -- -D warnings`.
- Run `cargo build --all-targets --all-features`.
- Run `cargo test --all-features`.
- Run `bazel test //tools/parity:tests //scripts:phase23_redacted_operator_evidence_test //scripts:phase25_live_stratum_evidence_test //scripts:phase27_live_hardware_bridge_evidence_test //scripts:phase28_evidence_test`.
- Run `just parity`.
- Run `git diff --check`.
</verification>

<success_criteria>
- Phase 25 and Phase 27 always finish with strict operator-evidence validation after any applicable mining-allow check.
- Every post-setup disposition creates a complete root and preserves the original failure status.
- `just phase28-evidence` deterministically consolidates Phase 27 into a full Phase 28 root and validates it.
- No wrapper copies or prints raw evidence, credentials, device identifiers, or private endpoints.
</success_criteria>

<output>
After completion, create `.planning/phases/29-evidence-workflow-automation-closure/29-02-SUMMARY.md` with task commits, exact trace assertions, commands run, and any residual filesystem/platform risk.
</output>
