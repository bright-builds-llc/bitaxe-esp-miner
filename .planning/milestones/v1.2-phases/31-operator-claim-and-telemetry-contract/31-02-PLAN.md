---
phase: "31"
plan: "02"
type: execute
wave: 2
depends_on:
  - "31-01"
files_modified:
  - crates/bitaxe-api/src/observation.rs
  - crates/bitaxe-api/src/lib.rs
  - crates/bitaxe-api/src/snapshot.rs
  - crates/bitaxe-api/src/wire.rs
  - crates/bitaxe-api/src/telemetry.rs
  - crates/bitaxe-api/BUILD.bazel
  - firmware/bitaxe/src/safety_adapter.rs
  - firmware/bitaxe/src/safety_adapter/observation_store.rs
  - firmware/bitaxe/src/safety_adapter/phase27_bring_up.rs
  - firmware/bitaxe/src/runtime_snapshot.rs
autonomous: true
requirements:
  - OBS-01
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 31-2026-07-13T19-47-51
generated_at: 2026-07-13T20:14:00.000Z
must_haves:
  truths:
    - Operator-facing power and thermal facts publish explicit independent truth beside unchanged AxeOS numeric compatibility fields.
    - API, WebSocket, log, and evidence consumers can read observation state without collecting sensors or changing sample metadata.
    - Repeated consumer reads return the same boot session, sequence, and acquisition time until a producer publishes a new successful sample.
    - Phase 31 installs no sensor producer, I2C transaction, hardware effect, or query-time freshness computation.
  artifacts:
    - path: crates/bitaxe-api/src/observation.rs
      provides: Stable wire projection for explicit per-fact truth and producer stamp metadata.
    - path: firmware/bitaxe/src/safety_adapter/observation_store.rs
      provides: Read-only consumer snapshot store initialized to explicit unavailable observations.
    - path: firmware/bitaxe/src/runtime_snapshot.rs
      provides: API snapshot composition from stored state rather than request-time safety collection.
  key_links:
    - from: crates/bitaxe-api/src/wire.rs
      to: crates/bitaxe-safety/src/observation.rs
      via: ObservationWire projection maps state and stamp separately from compatibility numeric fields.
    - from: firmware/bitaxe/src/runtime_snapshot.rs
      to: firmware/bitaxe/src/safety_adapter/observation_store.rs
      via: collect_api_snapshot clones a stored immutable observation snapshot.
---

<objective>
Expose truthful observation state through API projections and make every consumer read-only.

Purpose: satisfy OBS-01 at the operator boundary while preserving AxeOS numeric compatibility and preventing request traffic from minting freshness.
Output: explicit wire truth fields, deterministic projection tests, and a firmware observation store whose consumer API cannot acquire or refresh samples.
</objective>

<execution_context>
@.planning/phases/31-operator-claim-and-telemetry-contract/31-CONTEXT.md
@.planning/phases/31-operator-claim-and-telemetry-contract/31-RESEARCH.md
@.planning/phases/31-operator-claim-and-telemetry-contract/31-VALIDATION.md
@.planning/phases/31-operator-claim-and-telemetry-contract/31-01-PLAN.md
</execution_context>

<threat_model>
## Threat Model

**Assets:** operator-visible truth, producer stamps, compatibility wire stability, separation between observation and hardware effects.

**Trust boundaries:** safety observation into API snapshot; snapshot into JSON/WebSocket projections; firmware producer-side storage into request-side consumers.

Use OWASP ASVS Level 1 as the baseline. Every High-severity threat blocks completion until its mitigation and verification pass.

| Threat ID | STRIDE category | Component | Severity | Disposition | Mitigation plan | Verification |
| --- | --- | --- | --- | --- | --- | --- |
| T-31-03 | Spoofing | Compatibility projection | High | Mitigate | Serialize explicit per-fact state independently and map numeric fallback in a separate adapter. | Golden tests pair zero numeric fields with unavailable/fault/stale truth and reject `fresh`. |
| T-31-08 | Tampering | Observation store | High | Mitigate | `collect_api_snapshot()` clones an observation store; only a producer-only replace function can mutate it. | Repeated system-info/WebSocket reads assert identical stamps and store revision. |
| T-31-09 | Tampering | Aggregate API projection | High | Mitigate | Publish one truth object for each power/thermal/tachometer fact. | Tests mix fresh, stale, unavailable, and fault states in one snapshot. |
| T-31-10 | Elevation of Privilege | Firmware adapter | High | Mitigate | Initialize the store to unavailable and add no I2C/GPIO/control calls or background producer. | Source-boundary test scans the new module for ESP-IDF/I2C/GPIO/effect terms. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add independent API truth projections without changing compatibility numerics</name>
  <files>crates/bitaxe-api/src/observation.rs, crates/bitaxe-api/src/lib.rs, crates/bitaxe-api/src/snapshot.rs, crates/bitaxe-api/src/wire.rs, crates/bitaxe-api/src/telemetry.rs, crates/bitaxe-api/BUILD.bazel</files>
  <read_first>
    - crates/bitaxe-safety/src/observation.rs
    - crates/bitaxe-safety/src/power.rs
    - crates/bitaxe-safety/src/thermal.rs
    - crates/bitaxe-api/src/lib.rs
    - crates/bitaxe-api/src/snapshot.rs
    - crates/bitaxe-api/src/wire.rs
    - crates/bitaxe-api/src/telemetry.rs
    - crates/bitaxe-api/src/runtime_projection.rs
    - crates/bitaxe-api/fixtures/system-info-safe-ultra205.json
    - crates/bitaxe-api/BUILD.bazel
  </read_first>
  <action>
Create `crates/bitaxe-api/src/observation.rs` with a serde wire projection that never owns truth. Define `ObservationStateWire` using exact lower-snake-case values `fresh`, `stale`, `unavailable`, and `fault`; define `ObservationStampWire` with `bootSession`, `sequence`, and `acquiredAtMs`; define `ObservationTruthWire` with `state`, optional `stamp`, optional typed `reason`, and no numeric measurement field. Implement `From<&Observation<T>>` so fresh/stale/fault-last-good map their original stamp without modification and unavailable/fault-without-last-good have no stamp.

Extend `SafeTelemetrySnapshot` and `SystemInfoWire` with independent truth fields for power, bus voltage, current, chip temperature, VR temperature, and fan RPM. Use stable camelCase JSON names `powerStatus`, `voltageStatus`, `currentStatus`, `chipTempStatus`, `vrTempStatus`, and `fanRpmStatus`. Keep existing numeric keys and their fallback values byte-compatible. Ensure live WebSocket full/diff projection includes changes to these truth objects through the existing nested diff algorithm.

Add a host-testable pure `ObservationStore` abstraction in this crate. Its read method clones already-stamped observations without consulting clocks or producers; its replacement method accepts a complete supplied snapshot. Firmware may wrap this abstraction, but the invariant tests must run in the default host workspace.

Add Arrange/Act/Assert tests and update safe fixtures for: unavailable plus numeric zero; a legitimate fresh numeric zero with fresh truth; stale last-good with unchanged stamp; fault with/without last-good; mixed independent states; serde field names; full-on-connect and diff-only WebSocket behavior. Export the module in `lib.rs` and list it in `BUILD.bazel`.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-api --all-features safety_telemetry && cargo test -p bitaxe-api --all-features telemetry && bazel test //crates/bitaxe-api:tests && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - JSON serialization uses exactly `fresh`, `stale`, `unavailable`, and `fault` state strings.
    - System-info exposes exactly the six new `*Status` truth fields while existing numeric field names and fallback values remain unchanged.
    - Tests prove compatibility zero can be unavailable or legitimately fresh and therefore carries no truth by itself.
    - Mixed-state tests prove one failed fact does not change unaffected fact status.
    - API Cargo and Bazel tests exit 0.
  </acceptance_criteria>
  <commit_gate>
Before committing this task, run separately and in exact order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Then rerun the automated verification. Do not commit unless every command passes.
  </commit_gate>
  <done>System-info and WebSocket projections publish explicit per-fact truth and producer metadata beside unchanged compatibility numerics.</done>
</task>

<task type="auto">
  <name>Task 2: Make firmware API collection consume stored observations only</name>
  <files>firmware/bitaxe/src/safety_adapter.rs, firmware/bitaxe/src/safety_adapter/observation_store.rs, firmware/bitaxe/src/safety_adapter/phase27_bring_up.rs, firmware/bitaxe/src/runtime_snapshot.rs</files>
  <read_first>
    - firmware/bitaxe/src/runtime_snapshot.rs
    - firmware/bitaxe/src/safety_adapter.rs
    - firmware/bitaxe/src/safety_adapter/power.rs
    - firmware/bitaxe/src/safety_adapter/thermal.rs
    - firmware/bitaxe/src/safety_adapter/phase27_bring_up.rs
    - crates/bitaxe-api/src/snapshot.rs
    - crates/bitaxe-safety/src/observation.rs
    - crates/bitaxe-stratum/src/v1/telemetry_projection.rs
  </read_first>
  <action>
Add `firmware/bitaxe/src/safety_adapter/observation_store.rs` as the sole Phase 31 storage boundary. Use `OnceLock<Mutex<...>>` with an explicit all-unavailable default. Expose a consumer function `observation_snapshot()` that clones stored state without reading clocks, sensors, environment variables, or hardware. Expose a crate-private producer function named `replace_observations_from_producer` for Phase 32 and existing producer-side bring-up integration; it accepts already-stamped pure observations and performs only the mutex replacement. On poison, return explicit unavailable observations and log a category-only warning.

Change `runtime_snapshot::collect_api_snapshot()` to call `observation_snapshot()` and project it into `SafeTelemetrySnapshot`; remove its call to `collect_safety_report()`. Move any retained Phase 27 report publication to the producer-side completion path in `phase27_bring_up.rs` or an equivalent one-time publication point so request traffic never invokes acquisition/collection. Do not start a task, read I2C, compute query-time staleness, or change active control behavior.

Use the host-testable pure store from Task 1 and add firmware adapter/source tests proving two or more system-info, WebSocket, statistics, log/evidence projection reads leave boot session, sequence, acquisition time, state, and stored bytes unchanged; producer replacement advances only the supplied successful observation; fault/stale replacement retains original stamp; mutex fallback is unavailable. Add a source-boundary test that the new firmware store contains no `esp_idf`, `i2c`, `gpio`, `fan`, `voltage`, `reset`, or effect invocation.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-api --all-features projection && cargo test --all-features && bazel test //crates/bitaxe-api:tests && just build && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `runtime_snapshot.rs` no longer calls `collect_safety_report()` from `collect_api_snapshot()`.
    - `observation_store.rs` contains `observation_snapshot` and `replace_observations_from_producer` with an unavailable default.
    - Repeated-read tests compare and preserve boot session, sequence, acquisition time, state, and value.
    - The new store performs no ESP-IDF, I2C, GPIO, fan, voltage, reset, or hardware-effect call.
    - Full Cargo tests, API Bazel tests, and `git diff --check` exit 0.
  </acceptance_criteria>
  <commit_gate>
Before committing this plan, run separately and in exact order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Then rerun the automated verification. Do not commit unless every command passes.
  </commit_gate>
  <done>All operator projections consume immutable stored observations, repeated reads cannot mint freshness, and Phase 31 adds no producer or hardware effect.</done>
</task>

</tasks>

<verification>
- `cargo test -p bitaxe-api --all-features`
- `bazel test //crates/bitaxe-api:tests`
- `cargo test --all-features`
- `just build`
- `rg -n "collect_safety_report" firmware/bitaxe/src/runtime_snapshot.rs` returns no match inside `collect_api_snapshot`.
- `git diff --check`
</verification>

<success_criteria>
- Operators see independent four-state truth for power and thermal facts beside unchanged numeric compatibility fields.
- Repeated API/WebSocket/log/evidence reads cannot change producer sequence or acquisition time.
- The firmware consumer boundary reads one stored snapshot and performs no sensor or control effect.
- OBS-01 is covered by deterministic unit, golden, projection, and source-boundary tests.
</success_criteria>
