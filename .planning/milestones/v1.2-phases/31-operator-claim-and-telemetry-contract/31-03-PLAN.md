---
phase: "31"
plan: "03"
type: execute
wave: 3
depends_on:
  - "31-02"
files_modified:
  - crates/bitaxe-api/src/v12_settings.rs
  - crates/bitaxe-api/src/lib.rs
  - crates/bitaxe-api/src/settings.rs
  - crates/bitaxe-api/BUILD.bazel
  - tools/parity/src/v12_admission.rs
  - tools/parity/src/main.rs
  - tools/parity/BUILD.bazel
autonomous: true
requirements:
  - CFG-08
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 31-2026-07-13T19-47-51
generated_at: 2026-07-13T20:15:00.000Z
must_haves:
  truths:
    - Hostname is the complete constructible v1.2 settings write authority.
    - Broader known, unknown, credential, control, and mixed-field inputs preserve compatibility response handling but cannot create v1.2 writes or effects.
    - Eligible Phase 31 claims are closed and exact; every excluded category has a typed ineligible reason and cannot be promoted through strings or schema growth.
    - Phase 31 completion and passing tests do not promote checklist rows or authorize hardware, credentials, archived lineage, OTA, mining, other boards, or broad production claims.
  artifacts:
    - path: crates/bitaxe-api/src/v12_settings.rs
      provides: Closed hostname-only settings authority and compatibility-only decisions.
    - path: tools/parity/src/v12_admission.rs
      provides: Typed exact-claim eligibility and deterministic exclusion validation.
  key_links:
    - from: crates/bitaxe-api/src/settings.rs
      to: crates/bitaxe-api/src/v12_settings.rs
      via: Broad compatibility parse result is translated into a closed v1.2 authority decision before persistence planning.
    - from: tools/parity/src/main.rs
      to: tools/parity/src/v12_admission.rs
      via: Phase 31 checklist/claim validation delegates to a typed row-scoped admission policy.
---

<objective>
Close the v1.2 settings and claim admission boundaries around hostname and exact Phase 31 claims.

Purpose: make schema growth, compatibility parsing, and broad string claims incapable of minting unsupported v1.2 authority.
Output: a pure hostname-only settings capability classifier, typed exclusion/admission validation, and regression coverage for CFG-08; live persistence integration remains owned by Phase 33.
</objective>

<execution_context>
@.planning/phases/31-operator-claim-and-telemetry-contract/31-CONTEXT.md
@.planning/phases/31-operator-claim-and-telemetry-contract/31-RESEARCH.md
@.planning/phases/31-operator-claim-and-telemetry-contract/31-VALIDATION.md
@.planning/phases/31-operator-claim-and-telemetry-contract/31-02-PLAN.md
</execution_context>

<threat_model>
## Threat Model

**Assets:** settings persistence authority, secret-bearing fields, hardware-control isolation, exact parity claims, archived-lineage prohibition.

**Trust boundaries:** untrusted JSON into compatibility parser; parser result into the effect-free v1.2 capability classifier; checklist/evidence text into parity admission.

Use OWASP ASVS Level 1 as the baseline. Every High-severity threat blocks completion until its mitigation and verification pass.

| Threat ID | STRIDE category | Component | Severity | Disposition | Mitigation plan | Verification |
| --- | --- | --- | --- | --- | --- | --- |
| T-31-04 | Elevation of Privilege | Settings capability | High | Mitigate | Construct `V12SettingsChange` only from an exact hostname-only field set; generic patches never implement the capability type. | Table-driven tests cover every schema field and mixed payloads. |
| T-31-11 | Information Disclosure | Compatibility decision | High | Mitigate | Return a category-only compatibility decision with no raw values or effect plan. | Tests assert redaction-safe Debug/serde output and absence of persistence types. |
| T-31-05 | Spoofing | Claim admission | High | Mitigate | Use closed `V12PromotableClaim` and `V12ExclusionReason` enums; no string-to-eligible conversion. | Exhaustive exclusion fixtures and wrong-row tests fail closed. |
| T-31-12 | Elevation of Privilege | Capability exclusions | High | Mitigate | Encode archived Phase 28.1.1, mining, UART/pins, OTA, other boards, and broad claims only as exclusions. | Source/fixture tests require each exact exclusion and no eligible variant. |
| T-31-13 | Information Disclosure | Public compatibility response | Medium | Mitigate | Keep existing generic public bodies; typed detail remains internal and redaction-safe. | Existing settings error/redaction tests plus new compatibility-only cases pass. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add the effect-free hostname-only v1.2 settings capability</name>
  <files>crates/bitaxe-api/src/v12_settings.rs, crates/bitaxe-api/src/lib.rs, crates/bitaxe-api/src/settings.rs, crates/bitaxe-api/BUILD.bazel</files>
  <read_first>
    - crates/bitaxe-api/src/settings.rs
    - crates/bitaxe-api/src/lib.rs
    - crates/bitaxe-api/BUILD.bazel
    - crates/bitaxe-config/src/settings.rs
    - crates/bitaxe-config/src/persistence.rs
    - reference/esp-miner/main/http_server/http_server.c
    - reference/esp-miner/main/nvs_config.c
  </read_first>
  <action>
Create `crates/bitaxe-api/src/v12_settings.rs` with a validated non-empty `Hostname` domain type that reuses the existing hostname schema validation without exposing generic schema authority. Define `V12SettingsChange::Hostname(Hostname)`, `V12SettingsExclusionReason` with category-only variants for `BroaderKnownField`, `UnknownField`, `MixedFieldSet`, `CredentialField`, `HardwareControlField`, `MiningOrSelfTestField`, and `EmptyPatch`, and `V12SettingsDecision::{Authorized(V12SettingsChange), CompatibilityOnly(V12SettingsExclusionReason)}`. The only authorized field set is exactly one `hostname` key with a valid value.

Refactor only the pure settings parse/classification boundary to retain the original input field-name set long enough to classify it. Keep malformed JSON and invalid hostname mapped to existing generic public errors. For all well-formed non-hostname or mixed payloads, preserve the established compatibility classification selected by current tests but produce no `V12SettingsChange`, NVS write, commit/reload plan, or post-persistence effect. Do not include raw values in exclusion reasons or Debug output.

Add table-driven pure tests for valid hostname, invalid hostname, every other known schema field, unknown field, empty object, and hostname mixed with credentials/fan/voltage/frequency/mining/self-test/display fields. Assert that only exact hostname produces a capability and that all other results contain no persistence/effect type. Add the new module to `lib.rs` and Bazel. Do not modify `firmware/bitaxe/src/http_api.rs` or wire this classifier to live settings persistence: Phase 33 owns validation, write, commit, reload, durability, and live handler integration.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-api --all-features settings && bazel test //crates/bitaxe-api:tests && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `V12SettingsChange` has exactly one variant: `Hostname(Hostname)`.
    - Only a payload whose field set is exactly `hostname` can return `V12SettingsDecision::Authorized`.
    - Table-driven tests cover every non-hostname NVS schema field and assert no v1.2 write/effect authority.
    - Mixed hostname-plus-broader and secret-bearing inputs construct no persistence/effect capability and reveal no raw values.
    - Existing generic public error bodies and unknown-field compatibility tests remain green.
  </acceptance_criteria>
  <commit_gate>
Before committing this task, run separately and in exact order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Then rerun the automated verification. Do not commit unless every command passes.
  </commit_gate>
  <done>The pure classifier grants a v1.2 capability only to validated hostname, keeps every broader field effect-free, and defers live persistence integration to Phase 33.</done>
</task>

<task type="auto">
  <name>Task 2: Add typed exact-claim admission and final Phase 31 regression gates</name>
  <files>tools/parity/src/v12_admission.rs, tools/parity/src/main.rs, tools/parity/BUILD.bazel</files>
  <read_first>
    - .planning/phases/31-operator-claim-and-telemetry-contract/31-CONTEXT.md
    - .planning/phases/31-operator-claim-and-telemetry-contract/31-RESEARCH.md
    - .planning/REQUIREMENTS.md
    - docs/parity/checklist.md
    - docs/adr/0006-parity-checklist-as-audit-evidence.md
    - docs/adr/0012-parity-verification-evidence.md
    - tools/parity/src/claim_ladder.rs
    - tools/parity/src/safety_allow.rs
    - tools/parity/src/mining_allow.rs
    - tools/parity/src/main.rs
    - tools/parity/BUILD.bazel
  </read_first>
  <action>
Create `tools/parity/src/v12_admission.rs`. Define closed `V12PromotableClaim` variants only for `ObservationTruthContract` and `HostnamePatchAllowlist`. Define `V12ExclusionReason` variants for active fan/voltage/reset/power/ASIC control, self-test effects, watchdog intervention/load, mining, archived Phase 28.1.1, credentials, direct UART/pins, OTA/recovery, other boards, telemetry history, runtime UI/display/input/BAP, and broad production/verified promotion. Define `V12AdmissionDecision::{Eligible(V12PromotableClaim), Ineligible(V12ExclusionReason)}` with no parser from arbitrary strings into eligible variants.

Add row/capability-scoped validation that OBS-01 evidence can support only `ObservationTruthContract`, CFG-08 evidence can support only `HostnamePatchAllowlist`, and one eligible claim cannot authenticate the other. Current Phase 31 completion must not change checklist statuses; the validator only enforces admissible vocabulary and explicit non-claims. Integrate it into `tools/parity/src/main.rs` through a small module call and list the file in `tools/parity/BUILD.bazel`.

Add Arrange/Act/Assert tests named with the `phase31_` prefix for both eligible cases, every exclusion variant, wrong-row bundles, missing evidence, string/schema growth attempts, broad `verified`/`production ready` language, and preservation of current checklist statuses. Add a source-boundary assertion that the eligible enum contains no excluded category tokens. Run `just parity` and confirm it reports `validation_errors: none` without promoting any row.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-parity --all-features phase31_ && bazel test //tools/parity:tests && just test && just parity && just verify-reference && for requirement in OBS-01 CFG-08; do rg -n "^\\| ${requirement} \\| Phase 31 \\| (Pending|Complete) \\|$" .planning/REQUIREMENTS.md; done && node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs verify lifecycle 31 --expect-id 31-2026-07-13T19-47-51 --expect-mode yolo --require-plans --raw && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `V12PromotableClaim` contains only `ObservationTruthContract` and `HostnamePatchAllowlist`.
    - `V12ExclusionReason` contains every excluded category named in Phase 31 context and requirements.
    - `phase31_` tests prove wrong-row, broad-string, schema-growth, and every excluded category fail closed.
    - `docs/parity/checklist.md` status cells are unchanged by Phase 31.
    - `cargo test -p bitaxe-parity --all-features phase31_`, parity Bazel tests, `just parity`, `just verify-reference`, and `git diff --check` exit 0.
  </acceptance_criteria>
  <commit_gate>
Before committing this plan, run separately and in exact order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Then rerun the automated verification. Do not commit unless every command passes.
  </commit_gate>
  <done>Hostname is the sole v1.2 settings authority, exact Phase 31 claims are closed and row-scoped, all excluded categories fail closed, and no checklist row is promoted.</done>
</task>

</tasks>

<verification>
- `cargo test -p bitaxe-api --all-features settings`
- `cargo test -p bitaxe-parity --all-features phase31_`
- `bazel test //crates/bitaxe-api:tests //tools/parity:tests`
- `just test`
- `just parity`
- `just verify-reference`
- `for requirement in OBS-01 CFG-08; do rg -n "^\| ${requirement} \| Phase 31 \| (Pending|Complete) \|$" .planning/REQUIREMENTS.md; done`
- `node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs verify lifecycle 31 --expect-id 31-2026-07-13T19-47-51 --expect-mode yolo --require-plans --raw`
- `git diff --check`
</verification>

<success_criteria>
- CFG-08 exposes hostname as the complete v1.2 PATCH write authority.
- Broader, mixed, credential, control, and unknown fields produce no v1.2 writes or effects.
- Typed claim admission cannot represent any excluded capability as eligible.
- Phase 31 passes repository-local tests without hardware, secrets, archived-lineage work, or checklist promotion.
</success_criteria>
