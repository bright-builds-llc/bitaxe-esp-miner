---
phase: 34-provenance-runtime-health-and-coherent-operator-snapshot
plan: 02
type: execute
wave: 2
depends_on:
  - 34-01
files_modified:
  - crates/bitaxe-api/BUILD.bazel
  - crates/bitaxe-api/fixtures/api/system-info-ultra205-safe.json
  - crates/bitaxe-api/src/lib.rs
  - crates/bitaxe-api/src/operator_snapshot.rs
  - crates/bitaxe-api/src/runtime_projection.rs
  - crates/bitaxe-api/src/snapshot.rs
  - crates/bitaxe-api/src/wire.rs
  - firmware/bitaxe/BUILD.bazel
  - firmware/bitaxe/src/boot_evidence.rs
  - firmware/bitaxe/src/log_buffer.rs
  - firmware/bitaxe/src/runtime_snapshot.rs
  - tools/parity/BUILD.bazel
  - tools/parity/src/main.rs
  - tools/parity/src/operator_evidence.rs
  - tools/parity/src/operator_snapshot_evidence.rs
autonomous: true
requirements:
  - OBS-06
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 34-2026-07-15T03-26-15
generated_at: 2026-07-15T03:55:22Z
must_haves:
  truths:
    - Every public operator snapshot carries one opaque per-boot session and one nonzero revision assigned exactly once to the captured facts.
    - Revisions increase without reuse within a boot, while a new boot changes the session and restarts the revision sequence without claiming cross-boot ordering.
    - System-info, live WebSocket, retained-log markers, and evidence projections preserve the same typed session/revision pair for each captured snapshot instead of independently minting or inferring identity.
    - Runtime projections never substitute fixture values, host checkout state, wall-clock time, request count, or presentation labels for boot-session or revision truth.
  artifacts:
    - crates/bitaxe-api/src/operator_snapshot.rs owns the opaque session, nonzero revision, checked sequence, marker, and evidence-safe parsing contract.
    - firmware/bitaxe/src/runtime_snapshot.rs is the sole firmware authority that assigns a session/revision pair to one immutable ApiSnapshot before any public projection.
    - tools/parity/src/operator_snapshot_evidence.rs validates correlated API, WebSocket, retained-log, and evidence-document projections without contacting hardware.
  key_links:
    - firmware/bitaxe/src/boot_evidence.rs supplies the existing hardware-RNG boot session to the runtime snapshot owner; no second nonce is generated.
    - crates/bitaxe-api/src/runtime_projection.rs and crates/bitaxe-api/src/wire.rs copy the pair from ApiSnapshot into both system-info and live telemetry JSON.
    - firmware/bitaxe/src/log_buffer.rs retains the exact marker emitted for the captured pair, and tools/parity/src/operator_evidence.rs projects and validates that pair for later evidence workflows.
---

<objective>
Bind every read-only operator projection to one opaque boot session and a monotonic operator-snapshot revision.

Purpose: Let operators and later evidence workflows distinguish coherent facts from mixed-boot or independently sampled data without starting hardware work or inventing runtime truth.
Output: Typed snapshot identity, one firmware capture authority, additive system-info/WebSocket fields, retained correlation markers, and a pure evidence coherence validator.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-CONTEXT.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-PRD.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-RESEARCH.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-VALIDATION.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-01-PLAN.md

The repo-local GSD, no-hardware, evidence-redaction, archived-lineage, functional-core/imperative-shell, Rust, testing, and verification rules materially constrain this plan. Plan 01 owns build identity and must complete before this plan changes its shared API/runtime files.
</context>

<threat_model>
- **T-34-05 (high):** API, WebSocket, logs, or evidence could combine facts from different captures or boots while appearing coherent. Assign the typed pair once at the firmware capture boundary, copy it through projections, and reject mixed-session or uncorrelated evidence.
- **T-34-08 (high):** Concurrent requests could repeat, regress, or race snapshot revisions. Use one checked atomic sequence per boot, prohibit zero and wraparound, and test interleaved capture order.
- **T-34-09 (high):** Fixture, host-checkout, request-time, or presentation data could be substituted for runtime identity. Keep fixture construction explicitly test-only, derive the runtime session only from the initialized boot observer, and add source guards against fallback derivation.
- **T-34-10 (medium):** Evidence parsing could accept malformed, duplicate, partial, or reordered correlation fields. Parse into the same strict domain types, require exactly one pair per projection, require retained-marker membership, and enforce nondecreasing chronology within one session.
- **T-34-11 (medium):** Correlation work could accidentally start self-tests, watchdog intervention, sensor reads, mining, credentials, or hardware evidence. Limit implementation to already-collected read-only snapshots and host-side parsing; add forbidden-effect source assertions and run no hardware commands.
</threat_model>

<tasks>

<task type="auto" id="34-02-01">
  <name>Task 1: Define the typed operator-snapshot identity and additive wire contract</name>
  <files>crates/bitaxe-api/BUILD.bazel, crates/bitaxe-api/fixtures/api/system-info-ultra205-safe.json, crates/bitaxe-api/src/lib.rs, crates/bitaxe-api/src/operator_snapshot.rs, crates/bitaxe-api/src/runtime_projection.rs, crates/bitaxe-api/src/snapshot.rs, crates/bitaxe-api/src/wire.rs</files>
  <read_first>AGENTS.md, standards/core/architecture.md, standards/core/testing.md, standards/languages/rust.md, .planning/REQUIREMENTS.md, .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-CONTEXT.md, .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-RESEARCH.md, .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-01-PLAN.md, crates/bitaxe-api/BUILD.bazel, crates/bitaxe-api/fixtures/api/system-info-ultra205-safe.json, crates/bitaxe-api/src/lib.rs, crates/bitaxe-api/src/boot_identity.rs, crates/bitaxe-api/src/runtime_projection.rs, crates/bitaxe-api/src/snapshot.rs, crates/bitaxe-api/src/wire.rs</read_first>
  <action>Create dependency-free domain types `BootSessionId`, `OperatorSnapshotRevision`, `OperatorSnapshotIdentity`, and `OperatorSnapshotSequence` in `operator_snapshot.rs`. `BootSessionId` must serialize as exactly 32 lowercase hexadecimal characters while exposing no ordinal or timestamp semantics. `OperatorSnapshotRevision` must reject zero. `OperatorSnapshotSequence` starts before revision 1, returns 1 on its first `next_identity(session)` call, uses checked addition, and returns an explicit exhaustion error instead of wrapping or saturating. Add the identity to `ApiSnapshot`; keep the deterministic value used by `safe_ultra_205()` explicitly fixture-only and require firmware to overwrite it before public use. Add exact JSON fields `bootSession` and `operatorSnapshotRevision` to `SystemInfoWire`. Ensure `project_system_info` and `project_api_views` only copy both values from the already-captured `ApiSnapshot`; they must not accept a separate session or revision argument or increment a sequence. Update the safe system-info fixture after Plan 01's identity fields are present. Add focused Arrange/Act/Assert tests for valid round trips, malformed/uppercase/wrong-length sessions, zero revision, first revision, strictly increasing revisions, exhaustion, DTO field names, and exact equality between the system-info DTO and live telemetry JSON produced from one base snapshot.</action>
  <verify><automated>cargo test -p bitaxe-api operator_snapshot &amp;&amp; cargo test -p bitaxe-api direct_system_info_projection &amp;&amp; cargo test -p bitaxe-api projection_system_info_preserves_axeos_fields &amp;&amp; bazel test //crates/bitaxe-api:tests</automated></verify>
  <acceptance_criteria>
    - `crates/bitaxe-api/src/operator_snapshot.rs` contains public domain types named `BootSessionId`, `OperatorSnapshotRevision`, `OperatorSnapshotIdentity`, and `OperatorSnapshotSequence`.
    - `SystemInfoWire` serializes exactly `bootSession` as 32 lowercase hexadecimal characters and `operatorSnapshotRevision` as a nonzero integer.
    - The direct system-info and live telemetry projection tests assert equal session and revision values from the same `ApiSnapshot`.
    - Unit tests reject zero, wraparound, uppercase, nonhex, short, and long inputs without `unwrap()` or a saturating revision fallback.
    - No projection function generates a nonce, reads Git, reads a clock, or increments the revision independently.
  </acceptance_criteria>
  <done>The pure API contract can represent and serialize one coherent capture identity without admitting malformed or contradictory states.</done>
</task>

<task type="auto" id="34-02-02">
  <name>Task 2: Assign one runtime pair and retain its correlation marker</name>
  <files>firmware/bitaxe/BUILD.bazel, firmware/bitaxe/src/boot_evidence.rs, firmware/bitaxe/src/log_buffer.rs, firmware/bitaxe/src/runtime_snapshot.rs</files>
  <read_first>AGENTS.md, .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-PRD.md, .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-VALIDATION.md, crates/bitaxe-api/src/operator_snapshot.rs, crates/bitaxe-api/src/runtime_projection.rs, crates/bitaxe-api/src/snapshot.rs, firmware/bitaxe/BUILD.bazel, firmware/bitaxe/src/boot_evidence.rs, firmware/bitaxe/src/http_api.rs, firmware/bitaxe/src/log_buffer.rs, firmware/bitaxe/src/main.rs, firmware/bitaxe/src/runtime_snapshot.rs, firmware/bitaxe/src/runtime_uptime.rs, firmware/bitaxe/src/websocket_api.rs</read_first>
  <action>Expose the boot observer's existing hardware-RNG session as `BootSessionId`; do not generate a second session or use RTC ordinal, reset reason, MAC, uptime, wall clock, source commit, or host data as the session. In `runtime_snapshot.rs`, add the sole boot-lifetime `OperatorSnapshotSequence` owner. At the beginning of each complete snapshot capture, reserve the next revision exactly once, collect the config/mining/platform/telemetry facts into one `ApiSnapshot`, attach the reserved `OperatorSnapshotIdentity`, and pass that immutable snapshot to the existing projection functions. Concurrent HTTP and WebSocket captures must obtain unique, strictly increasing revisions from the same boot session. Emit and retain exactly one redacted marker for each completed capture using `operator_snapshot session=&lt;32-lowerhex&gt; revision=&lt;nonzero-u64&gt; redacted=true`; do not emit a marker for an abandoned or partially constructed capture. Extend the existing firmware source filegroup for host-side source guards. Keep HTTP routes, WebSocket envelope/cadence, log download bytes other than the additive marker, sensor producers, settings persistence, heartbeat, PATCH, restart, and Plan 01 identity semantics unchanged. Do not start a task, self-test, watchdog action, sensor transaction, hardware command, or evidence run.</action>
  <verify><automated>cargo test -p bitaxe-parity phase34_operator_snapshot_runtime_source_guard &amp;&amp; bazel test //crates/bitaxe-api:tests //tools/parity:tests &amp;&amp; just build</automated></verify>
  <acceptance_criteria>
    - Firmware source contains one boot-session authority and one operator-snapshot sequence authority; no alternate RNG, fixture, Git, clock, MAC, or label fallback exists.
    - Every system-info and live telemetry payload receives its pair from the captured `ApiSnapshot`, and the retained marker contains that exact pair.
    - Source-guard tests prove revision assignment precedes projection, occurs once per completed capture, and uses checked monotonic sequencing under concurrent callers.
    - The marker is exactly `operator_snapshot session=&lt;32-lowerhex&gt; revision=&lt;nonzero-u64&gt; redacted=true` and contains no network identity, credential, target, path, PID, package digest, or secret.
    - Existing heartbeat, PATCH, restart, WebSocket envelope, and log-download behavior tests remain green, with no hardware or effectful verification command added.
  </acceptance_criteria>
  <done>Firmware publishes uniquely revisioned coherent snapshots from one boot session and leaves a retained redaction-safe correlation breadcrumb for each completed capture.</done>
</task>

<task type="auto" id="34-02-03">
  <name>Task 3: Validate coherent evidence projections without hardware</name>
  <files>tools/parity/BUILD.bazel, tools/parity/src/main.rs, tools/parity/src/operator_evidence.rs, tools/parity/src/operator_snapshot_evidence.rs</files>
  <read_first>AGENTS.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-CONTEXT.md, .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-RESEARCH.md, crates/bitaxe-api/src/operator_snapshot.rs, tools/parity/BUILD.bazel, tools/parity/src/main.rs, tools/parity/src/operator_evidence.rs, tools/parity/src/operator_evidence/profile.rs, tools/parity/src/operator_evidence/tests.rs, tools/parity/src/release_evidence.rs</read_first>
  <action>Add a pure evidence projector/validator that parses `bootSession` and `operatorSnapshotRevision` from system-info and live-WebSocket JSON, parses the exact retained `operator_snapshot` marker, and converts every value through the shared `BootSessionId` and `OperatorSnapshotRevision` domain types. Its redacted document projection must use exact fields `operator_snapshot_boot_session: &lt;32-lowerhex&gt;` and `operator_snapshot_revision: &lt;nonzero-u64&gt;`. Integrate it as an opt-in OBS-06 coherence check in operator-evidence validation without changing historical Phase 23/25/27/28 acceptance. When enabled, require exactly one pair per API/WebSocket evidence projection, one boot session across all inputs, a retained marker for every projected pair, and nondecreasing artifact chronology; reject missing halves, duplicate/conflicting fields, mixed sessions, zero/regressing revisions, a projected revision absent from retained logs, synthetic fixture labels, and host-checkout substitution. Allow two surfaces to share a revision only when their full projected pair is identical, representing the same captured snapshot. Add table-driven Arrange/Act/Assert tests for coherent same-capture projection, later monotonic capture, mixed boot, regression, missing marker, duplicate field, malformed session, partial input, and historical-validation non-regression. Do not generate, capture, promote, or contact any hardware evidence.</action>
  <verify><automated>cargo test -p bitaxe-parity operator_snapshot_evidence &amp;&amp; cargo test -p bitaxe-parity operator_evidence &amp;&amp; bazel test //tools/parity:tests</automated></verify>
  <acceptance_criteria>
    - `operator_snapshot_evidence.rs` parses API, WebSocket, and retained-log inputs through shared typed session/revision constructors.
    - Coherent fixtures produce exact redacted fields `operator_snapshot_boot_session` and `operator_snapshot_revision`; malformed, partial, mixed-session, regressing, duplicate, and unlogged pairs fail with stable category text.
    - Tests prove repeated revision is accepted only for the identical session/revision pair and strictly later captures use a greater revision in the same session.
    - Existing Phase 23/25/27/28 operator-evidence fixtures pass unchanged because OBS-06 coherence remains an explicit opt-in until the owning later evidence workflow enables it.
    - The implementation contains no detector, serial, flash, monitor, network, credential, self-test, watchdog, mining, OTA, direct-UART/pin, or Phase 28.1.1 invocation.
  </acceptance_criteria>
  <done>Host evidence tooling can prove or reject OBS-06 coherence from supplied read-only artifacts without fabricating fields or interacting with a device.</done>
</task>

</tasks>

<verification>
- [ ] `cargo test -p bitaxe-api operator_snapshot` proves strict session/revision types, checked monotonic sequencing, and identical direct/live projection fields.
- [ ] `cargo test -p bitaxe-parity operator_snapshot_evidence` proves same-session correlation, retained-marker membership, chronology, malformed-input rejection, and historical-profile non-regression.
- [ ] Firmware source guards prove one boot session, one revision authority, assignment before projection, one retained marker per completed capture, and no forbidden fallback or effect path.
- [ ] `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features` pass in that order before any Plan 02 commit.
- [ ] `bazel test //...`, `just build`, `just package`, `just verify-reference`, and `git diff --check` pass without hardware.
- [ ] The final diff is limited to the declared files and contains no credentials, network identities, device paths, hardware commands, active watchdog/self-test behavior, archived Phase 28.1.1 work, or parity promotion.
</verification>

<success_criteria>
- System-info and live WebSocket snapshots expose the exact same additive session/revision fields derived from their captured `ApiSnapshot`.
- The existing boot observer supplies one opaque session, and revisions are unique, nonzero, checked, and strictly increasing within that boot.
- Retained logs carry an exact redacted correlation marker for every completed public snapshot.
- Evidence projection accepts coherent same-boot chronology and fails closed on mixed, malformed, missing, duplicated, fabricated, or regressing identity.
- No hardware, credentials, direct UART/pins, OTA, mining, active health intervention, Phase 28.1.1 work, Phase 35 execution, or broad promotion occurs.
</success_criteria>

<output>
After completion, create `.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-02-SUMMARY.md`. Do not push. Hardware and correlated current-package qualification remain Phase 35 work.
</output>
