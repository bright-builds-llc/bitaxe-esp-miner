---
phase: 34-provenance-runtime-health-and-coherent-operator-snapshot
plan: "09"
type: execute
wave: 9
depends_on:
  - "34-08"
autonomous: true
gap_closure: true
phase_lifecycle_id: 34-2026-07-15T03-26-15
lifecycle_mode: interactive
generated_by: gsd-plan-phase
generated_at: "2026-07-16T01:33:45.905Z"
requirements:
  - OBS-06
files_modified:
  - crates/bitaxe-api/src/logs.rs
  - crates/bitaxe-api/src/operator_snapshot_publication.rs
  - crates/bitaxe-api/src/lib.rs
  - crates/bitaxe-api/BUILD.bazel
  - firmware/bitaxe/src/log_buffer.rs
  - firmware/bitaxe/src/operator_snapshot_retention.rs
  - firmware/bitaxe/src/operator_snapshot_retention_production_tests.rs
  - firmware/bitaxe/src/runtime_snapshot.rs
  - firmware/bitaxe/src/http_api.rs
  - firmware/bitaxe/src/main.rs
  - firmware/bitaxe/BUILD.bazel
  - tools/parity/src/operator_snapshot_evidence.rs
  - tools/parity/src/phase34_source_guard.rs
must_haves:
  truths:
    - "The operator-snapshot marker and runtime-health record are admitted to retained storage as one typed fallible transaction under one mutex acquisition; callers cannot observe or report a partial pair."
    - "OperatorSnapshotPublisher accepts different retention and issuance error types, and OperatorSnapshotPublishError preserves the exact stage-local source plus ordering-lock health without coercing either source into the other."
    - "Poisoned, unavailable, zero-capacity, or pair-insufficient retained storage returns a stable redacted error to the production publisher instead of reporting success."
    - "A concrete production retention failure consumes its revision, skips the actual HTTP/WebSocket issue closure, releases publication ownership, and permits the next attempt to use one strictly greater revision."
    - "Existing retained-log downloads, replay, raw WebSocket logs, public wire shapes, heartbeat, PATCH, and restart behavior remain unchanged."
  artifacts:
    - path: crates/bitaxe-api/src/logs.rs
      provides: "Pure bounded atomic pair append with capacity proof and typed errors"
    - path: crates/bitaxe-api/src/operator_snapshot_publication.rs
      provides: "Publisher contract with independent RetentionError and IssueError channels"
    - path: crates/bitaxe-api/src/lib.rs
      provides: "Public re-export of the revised two-error publication contract"
    - path: firmware/bitaxe/src/log_buffer.rs
      provides: "One-lock fallible production transaction over the singleton retained buffer"
    - path: firmware/bitaxe/src/operator_snapshot_retention.rs
      provides: "Small production retention adapter that calls the real one-lock log-buffer transaction and returns its concrete error"
    - path: firmware/bitaxe/src/operator_snapshot_retention_production_tests.rs
      provides: "Host crate root for behavioral tests compiling the exact production retention and log-buffer modules"
    - path: firmware/bitaxe/src/runtime_snapshot.rs
      provides: "Concrete publisher integration with typed retention and heterogeneous issuance errors"
    - path: firmware/bitaxe/src/http_api.rs
      provides: "System-info, cadence, and WebSocket-connect migrations that preserve existing public behavior"
  key_links:
    - from: firmware/bitaxe/src/runtime_snapshot.rs
      to: firmware/bitaxe/src/operator_snapshot_retention.rs
      via: "the publisher retain closure returns the concrete production retention result without unconditional success"
    - from: firmware/bitaxe/src/operator_snapshot_retention.rs
      to: firmware/bitaxe/src/log_buffer.rs
      via: "the named adapter calls retain_operator_snapshot_pair exactly once and returns RetainedPairStorageError"
    - from: firmware/bitaxe/src/log_buffer.rs
      to: crates/bitaxe-api/src/logs.rs
      via: "one mutex guard performs capacity preflight and one atomic pair append"
    - from: firmware/bitaxe/src/operator_snapshot_retention_production_tests.rs
      to: crates/bitaxe-api/src/operator_snapshot_publication.rs
      via: "behavioral tests combine the exact production adapter with OperatorSnapshotPublisher using distinct retention and issue error types"
    - from: firmware/bitaxe/src/http_api.rs
      to: firmware/bitaxe/src/runtime_snapshot.rs
      via: "system-info, cadence, and WebSocket-connect callers distinguish Issuance while retaining their existing fallback/status behavior"
---

<objective>
Close OBS-06 by making the concrete retained correlation adapter transactional and fallible before any public snapshot issuance, while allowing retention and issuance to keep their different concrete error types.

Purpose: prevent HTTP or live-WebSocket publication from succeeding when the matching retained marker and runtime-health pair was dropped or only partly retained.
Output: a two-error publisher contract, typed atomic pair retention, a named production adapter, host-compiled production-module regressions, migrated firmware callers, and redaction-safe fail-closed behavior.
</objective>

<execution_context>
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-CONTEXT.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-RESEARCH.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-REVIEW.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-VERIFICATION.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-07-SUMMARY.md
</execution_context>

<standards>
Repo-local evidence-integrity, redaction, no-hardware, no-credential, Phase 35, direct-UART/pin, and archived-lineage rules are binding. Bright Builds functional-core/imperative-shell, parse-at-boundaries, illegal-state, testing, verification, code-shape, and Rust standards materially govern this plan. No active standards override applies.
</standards>

<threat-model level="ASVS-L1" block="high">

| ID | Threat | Severity | Required mitigation and verification |
| --- | --- | --- | --- |
| T-34-G11 | The production adapter reports retention success after mutex poison or zero-capacity fallback drops both records. | critical | Return a typed error from one concrete transaction and propagate it from the publisher retain closure; assert issue is not called. |
| T-34-G12 | Two independent appends retain only the marker or only the health line. | high | Preflight the complete newline-normalized pair against usable capacity and append both while one guard is held; on failure retain neither new line. |
| T-34-G13 | Failure retries or reuses an externally meaningful revision. | high | Consume the failed revision, release the lock, and assert the next successful publication receives exactly the next greater revision. |
| T-34-G14 | Diagnostics disclose retained content, session, revision, device, network, or credential values. | high | Errors and logs expose only closed redacted reason categories; tests inspect Debug/Display output. |
| T-34-G15 | Host tests validate only an injected generic closure or source text rather than firmware production behavior. | high | Own `operator_snapshot_retention.rs` as the small production adapter and compile that exact source together with the exact `log_buffer.rs` in `retained_pair_production_tests`; combine it with the production `OperatorSnapshotPublisher` in behavioral tests. Source guards may supplement only call-site wiring. |
| T-34-G16 | A single publisher error parameter forces retention failures into unrelated issuance types such as `Infallible`, `anyhow::Error`, cadence errors, or `esp_err_t`, causing false success or lossy coercion. | high | Change the publisher and error enum to independent `RetentionError` and `IssueError` parameters; preserve each variant's source and lock health, and compile/test every production caller with its real issue error without changing wire/status behavior. |

</threat-model>

<commit-contract>
Each task ends in one atomic task commit and no push. Before every task commit, run exactly in order from the repository root: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Stop on the first failure. Then run the task's focused checks and `git diff --check`. Capture deterministic RED failures before production edits and commit only GREEN states.
</commit-contract>

<tasks>

<task type="auto" id="34-09-01">
  <name>Add one typed atomic retained-pair transaction</name>
  <read-first>
    - crates/bitaxe-api/src/logs.rs
    - firmware/bitaxe/src/log_buffer.rs
    - firmware/bitaxe/src/main.rs
    - firmware/bitaxe/BUILD.bazel
    - .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-REVIEW.md
  </read-first>
  <files>
    - crates/bitaxe-api/src/logs.rs
    - crates/bitaxe-api/BUILD.bazel
    - firmware/bitaxe/src/log_buffer.rs
    - firmware/bitaxe/src/operator_snapshot_retention.rs
    - firmware/bitaxe/src/operator_snapshot_retention_production_tests.rs
    - firmware/bitaxe/BUILD.bazel
  </files>
  <action>
Add focused Arrange/Act/Assert RED tests to `RetainedLogBuffer` and the production retention seam for: an empty buffer; capacity one byte smaller than the complete normalized pair; successful pair append with exactly one newline per record; failure leaving a byte-identical preexisting buffer; and a poisoned production mutex. Tests must prove no partial marker or health line appears.

In `bitaxe-api`, add a typed `RetainedPair`/equivalent constructor that accepts the marker and runtime-health strings, rejects empty or embedded-incomplete records according to the existing line contract, normalizes exactly one trailing newline per record, computes the complete byte requirement with checked arithmetic, and exposes no unvalidated partial state. Add a `RetainedLogBuffer::try_append_pair`/equivalent operation returning a closed `RetainedPairError` with at least `StorageUnavailable` and `PairExceedsCapacity`; it must preflight usable capacity before mutation and append both records under the caller's single exclusive borrow. If the buffer is empty/zero-capacity or cannot retain the full pair simultaneously, return an error without changing existing bytes or counters. Preserve existing single-line append/download/replay behavior.

In firmware `log_buffer.rs`, add `retain_operator_snapshot_pair(marker, runtime_health) -> Result<(), RetainedPairStorageError>`. The closed concrete error must retain stable redacted categories for mutex poison, unavailable/zero-capacity storage, and the pure pair-append failure without record content. The function must initialize the singleton normally, acquire `LOG_BUFFER` exactly once, map poison to `MutexPoisoned`, reject the zero-capacity fallback or insufficient capacity, and invoke the pure pair append once. Do not recover poison, perform two locks, call the existing infallible single-line helper twice, or log raw record content. Provide a test-only install/reset/poison seam only if required to host-test the actual module, scoped so production has one singleton authority and tests serialize access to it.

Create the named small production module `firmware/bitaxe/src/operator_snapshot_retention.rs`. Its adapter accepts the two completed retained records, calls the real `crate::log_buffer::retain_operator_snapshot_pair` exactly once, returns `Result<(), RetainedPairStorageError>` without translating it to an issuance error or unconditional success, and emits the existing two `log::info!` records only after transactional retention succeeds. It must contain no duplicate retention algorithm or test-only modeled substitute.

Create `operator_snapshot_retention_production_tests.rs` as a host-test crate root and declare `retained_pair_production_tests` with explicit `srcs` containing that harness, the exact production `operator_snapshot_retention.rs`, and the exact production `log_buffer.rs` (with an explicit crate root when Bazel requires it). The focused tests must call the concrete adapter. Source-string assertions are not behavioral proof. Do not wire `runtime_snapshot.rs` yet; Task 34-09-02 owns the atomic publisher/API migration so Task 34-09-01 remains buildable under the current single-error publisher signature.

Run the exact commit-contract sequence, focused commands, review the diff, and create one atomic commit for task 34-09-01.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-api retained_pair &amp;&amp; bazel test //firmware/bitaxe:retained_pair_production_tests //crates/bitaxe-api:tests &amp;&amp; git diff --check</automated>
  </verify>
  <done>One concrete one-lock operation either retains the complete correlation pair or returns a typed error without partial mutation.</done>
  <acceptance-criteria>
    - The pair type normalizes exactly one newline per record and rejects invalid or overflowed pair construction.
    - Zero/insufficient capacity and poison return distinct stable categories without raw retained content.
    - Failed pair append leaves preexisting bytes and retained counters unchanged and exposes neither new line.
    - Successful append makes both complete records downloadable in marker-then-health order under one mutex acquisition.
    - The named production adapter calls the real one-lock API once, returns its concrete typed error unchanged, and logs both existing retained records only after success.
    - `retained_pair_production_tests` explicitly compiles `operator_snapshot_retention.rs` and `log_buffer.rs` together and executes the concrete adapter on the host.
    - Task 34-09-01 has one atomic commit created only after the exact Rust sequence and focused checks pass.
  </acceptance-criteria>
</task>

<task type="auto" id="34-09-02">
  <name>Propagate production retention failure before external issuance</name>
  <read-first>
    - crates/bitaxe-api/src/operator_snapshot_publication.rs
    - crates/bitaxe-api/src/lib.rs
    - firmware/bitaxe/src/operator_snapshot_retention.rs
    - firmware/bitaxe/src/operator_snapshot_retention_production_tests.rs
    - firmware/bitaxe/src/runtime_snapshot.rs
    - firmware/bitaxe/src/http_api.rs
    - firmware/bitaxe/src/main.rs
    - tools/parity/src/operator_snapshot_evidence.rs
    - tools/parity/src/phase34_source_guard.rs
    - firmware/bitaxe/BUILD.bazel
  </read-first>
  <files>
    - crates/bitaxe-api/src/operator_snapshot_publication.rs
    - crates/bitaxe-api/src/lib.rs
    - firmware/bitaxe/src/operator_snapshot_retention.rs
    - firmware/bitaxe/src/operator_snapshot_retention_production_tests.rs
    - firmware/bitaxe/src/runtime_snapshot.rs
    - firmware/bitaxe/src/http_api.rs
    - firmware/bitaxe/src/main.rs
    - firmware/bitaxe/BUILD.bazel
    - tools/parity/src/operator_snapshot_evidence.rs
    - tools/parity/src/phase34_source_guard.rs
  </files>
  <action>
First add RED unit tests in `operator_snapshot_publication.rs` with intentionally different sentinel retention and issuance error types. Require `publish` to accept them simultaneously, require `OperatorSnapshotPublishError::Retention` to preserve the exact retention source and lock health, require `Issuance` to preserve the exact issue source and lock health, and keep the reentrant/sequence-exhausted lock-health behavior. Update existing Arrange/Act/Assert publication tests without weakening completion-order, poison-recovery, failure-consumes-revision, or lock-release coverage.

Change `OperatorSnapshotPublisher::publish` to use separate generic parameters `RetentionError` and `IssueError`: the retain closure returns `Result<(), RetentionError>`, the issue closure returns `Result<T, IssueError>`, and the result is `OperatorSnapshotPublishError<RetentionError, IssueError>`. Change the enum, its `maybe_lock_health`, `Display`, and any error-trait implementation to the same two-parameter shape. `Retention { source: RetentionError, lock_health }` and `Issuance { source: IssueError, lock_health }` must preserve their respective source values and health; do not use a shared wrapper, type erasure, string coercion, `anyhow` conversion in the pure crate, or a fake common error. Keep the public re-export in `crates/bitaxe-api/src/lib.rs` and update API tests to compile the two-error contract.

Then add RED behavioral regressions in `operator_snapshot_retention_production_tests.rs` that combine the production `OperatorSnapshotPublisher` with the exact concrete `operator_snapshot_retention.rs` adapter and real `log_buffer.rs` transaction compiled by `retained_pair_production_tests`. Arrange unavailable/zero-capacity storage, publish one completed snapshot, and assert: the result is `OperatorSnapshotPublishError<RetainedPairStorageError, DistinctIssueError>::Retention` with the concrete retention source and lock health; the actual recording issue closure is not called; neither retained line exists; the failed revision is consumed; a subsequent publication after installing usable storage succeeds at exactly the next revision; and the ordering mutex is released. Add equivalent poisoned-storage and insufficient-capacity cases, each testing one concern. Inspect error Debug/Display text for absence of session, revision, retained line content, paths, network values, and credentials. These behavioral tests are authoritative; parity source guards only supplement proof that production call sites use the adapter.

Register `operator_snapshot_retention` from `main.rs` and wire `runtime_snapshot.rs` to that module. Remove the local infallible `retain_completed_operator_snapshot` implementation and have the publisher retain closure return the module's concrete `Result<(), RetainedPairStorageError>` directly. The internal helper becomes `publish_operator_snapshot<Publication, T, IssueError> -> Result<T, OperatorSnapshotPublishError<RetainedPairStorageError, IssueError>>`; `publish_projected_system_info`, `publish_projected_live_telemetry_payload`, and `log_recovered_publication_lock` must carry the correct two-error type. Do not map the storage error into the issue type, retry, restore, decrement, or reuse the revision.

Migrate every affected firmware caller and test explicitly: internal `collect_api_snapshot` and projection/scoreboard/statistics paths use `IssueError = Infallible` while retaining the concrete fallible storage error and their existing internal fail-closed handling; `/api/system/info` uses `IssueError = anyhow::Error` and preserves its existing handler error/status behavior; live cadence uses `IssueError = LiveCadenceIssueError`, continues to match only issuance serialization failure specially, and keeps retention in the existing redacted `snapshot_publication` fallback; live WebSocket connect uses `IssueError = sys::esp_err_t`, returns the issuance source exactly as before, and maps non-issuance publication failures to the same `ESP_FAIL`. Update `http_api.rs` match annotations/patterns and focused tests so the Rust compiler proves these four heterogeneous issue types coexist with `RetainedPairStorageError`. Do not change HTTP/WebSocket payloads, public status shapes or codes, PATCH, restart, heartbeat, retained record text, or raw/retained log behavior.

Strengthen `operator_snapshot_evidence.rs` and `phase34_source_guard.rs` only as supplementary wiring guards: require `runtime_snapshot.rs` to call the named production adapter, require that adapter to call `log_buffer::retain_operator_snapshot_pair`, require the two-error publisher signature, and forbid unconditional `Ok::<(), IssueError>(())`, two `append_runtime_log_line` calls in the retention path, issue-after-failed-retain, or a test-only modeled adapter. Do not present source matching as proof of retention behavior. Keep Plan 34-07 completion-order assertions. Run the exact commit-contract sequence, focused commands, canonical build, review the diff, and create one atomic commit for task 34-09-02.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-api operator_snapshot_publication &amp;&amp; cargo test -p bitaxe-parity operator_snapshot_evidence &amp;&amp; cargo test -p bitaxe-parity phase34_source_guard &amp;&amp; bazel test //firmware/bitaxe:retained_pair_production_tests //firmware/bitaxe:operator_snapshot_publication_tests //tools/parity:tests //crates/bitaxe-api:tests &amp;&amp; just build &amp;&amp; git diff --check</automated>
  </verify>
  <done>The real publisher cannot externally issue an identity-bearing snapshot unless its complete retained correlation pair was stored transactionally.</done>
  <acceptance-criteria>
    - `OperatorSnapshotPublisher` and `OperatorSnapshotPublishError<RetentionError, IssueError>` accept different error types and preserve each variant's source plus lock health.
    - `runtime_snapshot.rs` returns the concrete production adapter result directly and contains no unconditional success or two independent retained appends after the pair operation.
    - `retained_pair_production_tests` compiles the exact adapter and log-buffer sources and combines them behaviorally with the production publisher; unavailable, poison, and insufficient-capacity failures retain no partial pair and call no issue closure.
    - A failed revision is consumed, the lock releases, and the next successful publication uses exactly one greater revision.
    - Internal infallible projections, system-info `anyhow::Error`, cadence `LiveCadenceIssueError`, and WebSocket-connect `esp_err_t` callers compile and preserve their previous public wire/status behavior.
    - Error and log categories contain no session, revision, retained content, path, device, network, or credential value.
    - Existing API/WebSocket bodies and errors, raw/retained log routes, replay, heartbeat, PATCH, and restart regressions remain green.
    - Task 34-09-02 has one atomic commit created only after the exact Rust sequence and focused checks pass.
  </acceptance-criteria>
</task>

</tasks>

<verification>

- Confirm the heterogeneous-error RED test fails under the current single-`E` publisher contract and each production-adapter RED test fails because the current concrete closure always reports success; make them green without a shared error coercion or modeled retention seam.
- Run `cargo test -p bitaxe-api retained_pair`, `cargo test -p bitaxe-api operator_snapshot_publication`, `cargo test -p bitaxe-parity operator_snapshot_evidence`, and `cargo test -p bitaxe-parity phase34_source_guard`.
- Run `bazel test //firmware/bitaxe:retained_pair_production_tests //firmware/bitaxe:operator_snapshot_publication_tests //tools/parity:tests //crates/bitaxe-api:tests`, then the exact final Rust sequence, `bazel test //...`, `just build`, `just package`, `just verify-reference`, and `git diff --check`.
- Inspect the `retained_pair_production_tests` Bazel rule and compile actions to confirm its `srcs` include the exact production `operator_snapshot_retention.rs` and `log_buffer.rs`; confirm its behavioral tests instantiate the production `OperatorSnapshotPublisher`. Source guards alone do not satisfy this check.
- Compile and exercise all four production issue-error migrations: internal `Infallible` projections, system-info `anyhow::Error`, cadence `LiveCadenceIssueError`, and WebSocket-connect `sys::esp_err_t`; compare existing handler matches and focused regressions to prove no public payload, status, or fallback change.
- Validate exactly two standalone frontmatter delimiters and lifecycle `34-2026-07-15T03-26-15` in the execution summary.
- Review for a shared retention/issuance error parameter, source erasure, partial retained pairs, poison recovery that reports success, retry/reuse, issue on retention failure, raw diagnostic content, API shape/status changes, hardware/USB/network/credentials, OTA, Phase 35, direct UART/pins, or archived lineage; any occurrence fails the plan.
- After Plans 34-08 and 34-09 have summaries, run a fresh Phase 34 code review, regression gate, schema-drift check, and independent goal verifier. Only a passing verifier may complete OBS-06/SYS-02 and Phase 34.

</verification>

<success_criteria>

- OBS-06 has production-path proof that every externally issued snapshot has its matching complete retained marker/runtime-health pair.
- Retention and issuance keep independent concrete error types, preserve their exact source and ordering-lock health, and all firmware callers compile without lossy conversion.
- Unavailable, poisoned, empty, or undersized storage fails before issue, consumes the revision, and preserves strict future monotonicity.
- The named production adapter and real one-lock log-buffer module are behaviorally exercised together with the production publisher in the focused Bazel target; source guards remain supplementary.
- All focused and repository-wide software checks pass without changing public wire/status contracts or using hardware, credentials, or Phase 35.

</success_criteria>

<output>
After implementation, create `.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-09-SUMMARY.md` under the normal GSD task-commit contract. Do not push or directly mark OBS-06 complete. Hand control to fresh Phase 34 code review and verification only after both new summaries and all software gates pass.
</output>
