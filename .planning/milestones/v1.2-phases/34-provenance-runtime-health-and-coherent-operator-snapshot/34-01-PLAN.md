---
phase: 34-provenance-runtime-health-and-coherent-operator-snapshot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .bazelrc
  - BUILD.bazel
  - Cargo.lock
  - Cargo.toml
  - crates/bitaxe-api/BUILD.bazel
  - crates/bitaxe-api/Cargo.toml
  - crates/bitaxe-api/fixtures/api/system-info-ultra205-safe.json
  - crates/bitaxe-api/src/build_identity.rs
  - crates/bitaxe-api/src/lib.rs
  - crates/bitaxe-api/src/snapshot.rs
  - crates/bitaxe-api/src/wire.rs
  - crates/bitaxe-core/src/lib.rs
  - firmware/bitaxe/BUILD.bazel
  - firmware/bitaxe/build.rs
  - firmware/bitaxe/src/main.rs
  - firmware/bitaxe/src/runtime_snapshot.rs
  - scripts/BUILD.bazel
  - scripts/build-firmware.sh
  - scripts/build-identity-pathspecs.txt
  - scripts/build-identity-status-test.sh
  - scripts/build-identity-status.sh
  - scripts/build_identity.bzl
  - scripts/package-firmware.sh
  - scripts/package-firmware-test.sh
  - tools/parity/BUILD.bazel
  - tools/parity/src/main.rs
  - tools/parity/src/phase34_source_guard.rs
  - tools/parity/src/release_evidence.rs
  - tools/parity/src/release_gate.rs
  - tools/xtask/BUILD.bazel
  - tools/xtask/Cargo.toml
  - tools/xtask/src/main.rs
  - tools/xtask/src/package_manifest.rs
  - tools/flash/BUILD.bazel
  - tools/flash/Cargo.toml
  - tools/flash/src/main.rs
autonomous: true
requirements:
  - SYS-01
  - SYS-02
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 34-2026-07-15T03-26-15
generated_at: 2026-07-15T03:44:36Z
must_haves:
  truths:
    - One validated provenance stamp is the sole authority for semantic version, build identity, pinned reference commit, ELF, ESP-IDF application descriptor, LCD, API, retained logs, package manifest, and active admission.
    - The human label is derived from a full commit, scoped dirty state, and optional exact release tag; it is never parsed as machine evidence.
    - Relevant dirty source changes rebuild the firmware even when the workspace was already dirty, and every dirty package is rejected before hardware discovery or interaction.
    - Clean release and clean dev packages remain eligible when the full embedded commit, manifest commit, current HEAD, embedded ELF SHA-256, and package digest agree.
  artifacts:
    - crates/bitaxe-api/src/build_identity.rs owns dependency-free parsing, validation, derivation, and serialization of build identity and the versioned provenance stamp.
    - scripts/build-identity-status.sh classifies scoped Git inputs; the xtask materializer invokes the shared Rust authority; scripts/build_identity.bzl only transports declared inputs and outputs.
    - tools/xtask/src/package_manifest.rs owns schema-v3 package identity validation without a live-Git firmware lookup.
  key_links:
    - The Bazel identity rule invokes the Rust materializer to emit one stamp and generated sdkconfig defaults consumed by both firmware and packaging actions; Starlark performs no second derivation.
    - Firmware compile-time values feed semantic version, LCD, system-info/live WebSocket projection, retained logs, pinned reference commit, and ESP-IDF application version from the same typed stamp.
    - Active parity/evidence and tools/flash gates consume full machine fields plus the ESP application descriptor's ELF SHA-256 and reject source_dirty before detector, port resolution, board-info, flash, monitor, or promotion work.
---

<objective>
Replace split Git provenance with one canonical build identity and project it consistently across build, runtime, package, and admission surfaces.

Purpose: Make development and dirty firmware visibly distinguishable without weakening exact-source proof or allowing an unqualified package to reach hardware.
Output: A typed identity core, Bazel workspace-status pipeline, identity-stamped firmware and package, LCD/API/log projections, schema-v3 manifest, and fail-before-hardware admission guards.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-CONTEXT.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-PRD.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-RESEARCH.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-VALIDATION.md
@scripts/source-commit-stamp.sh
@scripts/build-firmware.sh
@firmware/bitaxe/build.rs
@firmware/bitaxe/BUILD.bazel
@tools/xtask/src/package_manifest.rs
@tools/parity/src/release_gate.rs
</context>

<threat_model>
- **T-34-01 (high):** A dirty or ambiguous Git state could be mislabeled clean and admitted to hardware. Use one closed pathspec contract, porcelain status with untracked files, exact tag cardinality, strict stamp validation, and a fresh pre-hardware dirty check.
- **T-34-02 (high):** Bazel could reuse a dirty firmware action after a second source edit. Keep stable identity keys for identity transitions and add all transitive firmware/root/build inputs as declared action inputs for dirty-to-dirty invalidation.
- **T-34-03 (high):** ELF, runtime, and manifest could describe different source states. Feed one generated stamp to every surface and remove live-Git firmware identity queries from Cargo and packaging.
- **T-34-04 (high):** A presentation suffix could be mistaken for exact source proof. Preserve the bare full hash and package digest as machine fields and prohibit admission/evidence code from parsing `build_label`.
- **T-34-12 (medium):** Build metadata could expose local paths, branch names, tags outside the release vocabulary, or secrets. Emit only the closed `STABLE_BITAXE_*` set and the redaction-safe identity fields.
</threat_model>

<tasks>

<task type="auto" id="34-01-01">
  <name>Task 1: Build the typed identity core and scoped Git classifier</name>
  <files>Cargo.toml, crates/bitaxe-api/Cargo.toml, crates/bitaxe-api/BUILD.bazel, crates/bitaxe-api/src/build_identity.rs, crates/bitaxe-api/src/lib.rs, scripts/build-identity-pathspecs.txt, scripts/build-identity-status.sh, scripts/build-identity-status-test.sh, scripts/BUILD.bazel</files>
  <read_first>.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-PRD.md, .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-RESEARCH.md, crates/bitaxe-api/src/lib.rs, crates/bitaxe-api/BUILD.bazel, scripts/source-commit-stamp.sh, scripts/BUILD.bazel, .gitignore</read_first>
  <action>Add a dependency-free `BuildIdentity` domain type whose only identity constructor inputs are a 40-character lowercase hexadecimal commit, a dirty boolean, and an optional validated release tag. Derive the first 12 characters, channel, and label; accept only `v[0-9]+.[0-9]+` or `v[0-9]+.[0-9]+.[0-9]+`; enforce the exact four-state label matrix and 22-character maximum. In the same Rust module, add the sole versioned `BuildProvenance` stamp authority containing `semantic_version`, `build_identity`, and a full lowercase `reference_commit`; its parser/renderer rejects missing, duplicate, unknown, malformed, uppercase, contradictory, non-ASCII, oversized, or unsupported-schema fields. Replace the old source-stamp script with `build-identity-status.sh`, which reads one checked-in pathspec contract and emits exactly five primitive inputs: `STABLE_BITAXE_SOURCE_COMMIT`, `STABLE_BITAXE_SOURCE_DIRTY`, `STABLE_BITAXE_RELEASE_TAG`, `STABLE_BITAXE_SEMANTIC_VERSION`, and `STABLE_BITAXE_REFERENCE_COMMIT`. The script may classify Git and read the declared firmware Cargo version/reference HEAD, but it must not derive label or channel. Use `git status --porcelain=v1 --untracked-files=all -- <pathspecs>` so staged, unstaged, deleted, renamed, and untracked nonignored relevant files are dirty; ignored and excluded planning/docs/evidence/reference/scratch/unrelated-host-tool files remain clean. Filter tags at HEAD through the exact allowed regex; zero emits `unavailable`, one emits that tag, and multiple or invalid Git/reference/version state exits nonzero. Test with isolated temporary Git repositories for all four labels through the Rust materializer, tag cardinality, detached HEAD, missing Git state, relevant dirty forms, ignored files, and planning/docs-only changes. Never emit a branch, path, user identity, remote, or arbitrary tag.</action>
  <verify><automated>cargo test -p bitaxe-api build_identity &amp;&amp; bash scripts/build-identity-status-test.sh &amp;&amp; bazel test //scripts:build_identity_status_test</automated></verify>
  <acceptance_criteria>
    - Unit tests prove clean release, dirty release, clean dev, and dirty dev labels with dirty before dev.
    - Constructor and stamp tests reject every malformed or contradictory redundant value and prove the label bound.
    - Shell tests cover staged, unstaged, deletion/rename, and untracked relevant changes plus ignored and excluded changes.
    - Multiple allowed tags, no repository, and malformed full commit fail closed; detached valid HEAD remains classifiable.
    - Status output contains only the five allowed stable primitive keys and redaction-safe values; label/channel are produced only by the Rust authority.
  </acceptance_criteria>
  <done>Build identity and scoped Git state have one tested, closed derivation authority.</done>
</task>

<task type="auto" id="34-01-02">
  <name>Task 2: Materialize one Bazel identity and stamp the firmware and ESP-IDF descriptor</name>
  <files>.bazelrc, BUILD.bazel, Cargo.lock, scripts/build_identity.bzl, scripts/BUILD.bazel, firmware/bitaxe/BUILD.bazel, scripts/build-firmware.sh, firmware/bitaxe/build.rs, tools/xtask/Cargo.toml, tools/xtask/BUILD.bazel, tools/xtask/src/main.rs</files>
  <read_first>.bazelrc, BUILD.bazel, scripts/BUILD.bazel, firmware/bitaxe/BUILD.bazel, scripts/build-firmware.sh, firmware/bitaxe/build.rs, firmware/bitaxe/sdkconfig.defaults, Cargo.toml</read_first>
  <action>Wire `--workspace_status_command=./scripts/build-identity-status.sh` in `.bazelrc` without enabling global `--stamp`. Add an xtask `materialize-build-provenance` command that calls the shared Rust parser/constructor and emits the canonical versioned stamp plus supplemental sdkconfig defaults containing `CONFIG_APP_PROJECT_VER_FROM_CONFIG=y` and the validated label. Add a small Starlark rule that passes `ctx.info_file` to that executable, ignores ordinary `BUILD_*` keys by selecting only the closed five-key namespace, rejects any unknown `STABLE_BITAXE_*` key, and declares both outputs; it must not derive or revalidate label/channel itself. Replace `source_commit_stamp` with this rule. Define public filegroups for the complete firmware transitive Rust workspace and root/build inputs, and make the firmware action depend on them so a second dirty edit invalidates the action. Pass the provenance stamp and generated defaults to `build-firmware.sh`; create a unique output-local `ESP_IDF_SDKCONFIG`, concatenate tracked defaults followed by generated identity defaults, and keep generated state outside tracked paths. Make `build.rs` require the stamp-projected semantic version, full source/reference commits, label, channel, dirty, and tag environment fields, emit them as compile-time values, and fail with an instruction to run `just build` rather than falling back to live Git. Add a Bazel regression that edits a relevant source twice while dirty and proves the declared identity/firmware action input changes. Keep the normal Cargo `--release` optimization profile independent from product release/dev channel.</action>
  <verify><automated>bazel test //scripts:build_identity_status_test //scripts:build_identity_cache_invalidation_test &amp;&amp; bazel build //firmware/bitaxe:build_provenance_stamp &amp;&amp; just build</automated></verify>
  <acceptance_criteria>
    - Direct Bazel and `just` resolve the same status command; no global `--stamp` is present.
    - The Starlark transport tolerates Bazel `BUILD_*` keys but fails on every unrecognized `STABLE_BITAXE_*` key; only the shared Rust authority derives and validates the stamp.
    - The same declared identity stamp is an input to firmware and package targets.
    - A clean/dirty transition and a second dirty relevant edit both invalidate the correct actions.
    - The output-local sdkconfig sets the app version to the exact build label, and stale tracked/generated sdkconfig state cannot override it.
    - A canonical build without a valid stamp fails closed and never runs `git` from `build.rs`.
  </acceptance_criteria>
  <done>Bazel owns a cache-correct identity stamp consumed by the firmware ELF and ESP-IDF application descriptor.</done>
</task>

<task type="auto" id="34-01-03">
  <name>Task 3: Project the canonical identity to LCD, API, WebSocket, and retained logs</name>
  <files>crates/bitaxe-core/src/lib.rs, crates/bitaxe-api/src/snapshot.rs, crates/bitaxe-api/src/wire.rs, crates/bitaxe-api/fixtures/api/system-info-ultra205-safe.json, firmware/bitaxe/src/main.rs, firmware/bitaxe/src/runtime_snapshot.rs</files>
  <read_first>crates/bitaxe-api/src/build_identity.rs, crates/bitaxe-core/src/lib.rs, crates/bitaxe-api/src/snapshot.rs, crates/bitaxe-api/src/wire.rs, crates/bitaxe-api/src/runtime_projection.rs, firmware/bitaxe/src/main.rs, firmware/bitaxe/src/runtime_snapshot.rs, firmware/bitaxe/src/display_adapter.rs, firmware/bitaxe/src/websocket_api.rs</read_first>
  <action>Make startup display text accept the validated build label and render the fourth line exactly as `fw &lt;build_label&gt;`, removing the 12-character truncation while retaining the 25-character geometry assertion. Populate runtime platform identity from compile-time typed values: keep `version` as the human label and add `semanticVersion`, `sourceCommit`, `referenceCommit`, `appElfSha256`, `buildChannel`, `sourceDirty`, and nullable `releaseTag` to `SystemInfoWire`. Read `appElfSha256` from the running ESP-IDF `esp_app_desc_t` and expose its lowercase 64-character value; this is the non-circular flashed-package identifier shared with host ELF inspection. Because live WebSocket serializes the same DTO, prove both surfaces receive identical fields. Keep `firmware_commit=&lt;full-hash&gt;` and add retained full `reference_commit`/`app_elf_sha256` machine lines plus exactly one retained `runtime_build_identity semantic_version=&lt;version&gt; label=&lt;label&gt; channel=&lt;release|dev&gt; source_dirty=&lt;true|false&gt; release_tag=&lt;tag|unavailable&gt; redacted=true` line. Do not change heartbeat, PATCH, restart, or unrelated response serializers. Add exact JSON/line tests and a source guard proving no runtime code invokes Git or parses presentation suffixes.</action>
  <verify><automated>cargo test -p bitaxe-core &amp;&amp; cargo test -p bitaxe-api system_info &amp;&amp; cargo test -p bitaxe-api runtime_projection &amp;&amp; cargo test -p bitaxe-parity phase34_identity_runtime_source_guard</automated></verify>
  <acceptance_criteria>
    - Every allowed label renders in full and `fw &lt;12-char-hash&gt;-dirty-dev` is exactly 25 characters.
    - System-info and live WebSocket expose identical additive field names, values, and null tag semantics.
    - Runtime/API expose semantic version separately from the human build label, the pinned reference commit, and the running app descriptor ELF SHA-256.
    - Machine logs use full unsuffixed hashes; the structured log uses only validated redaction-safe identity values.
    - Fixtures and regressions prove heartbeat serialization plus PATCH/restart bodies remain byte-for-byte unchanged.
    - No runtime projection substitutes current host checkout state or derives exact proof from the label.
  </acceptance_criteria>
  <done>The operator sees one truthful human identity while machine evidence retains a separate exact full commit.</done>
</task>

<task type="auto" id="34-01-04">
  <name>Task 4: Emit manifest v3 and reject dirty or inconsistent packages before hardware</name>
  <files>Cargo.lock, tools/xtask/Cargo.toml, tools/xtask/BUILD.bazel, tools/xtask/src/main.rs, tools/xtask/src/package_manifest.rs, scripts/package-firmware.sh, scripts/package-firmware-test.sh, firmware/bitaxe/BUILD.bazel, tools/flash/Cargo.toml, tools/flash/BUILD.bazel, tools/flash/src/main.rs, tools/parity/BUILD.bazel, tools/parity/src/main.rs, tools/parity/src/phase34_source_guard.rs, tools/parity/src/release_gate.rs, tools/parity/src/release_evidence.rs</files>
  <read_first>tools/xtask/src/main.rs, tools/xtask/src/package_manifest.rs, scripts/package-firmware.sh, scripts/package-firmware-test.sh, firmware/bitaxe/BUILD.bazel, tools/flash/Cargo.toml, tools/flash/BUILD.bazel, tools/flash/src/main.rs, tools/parity/src/main.rs, tools/parity/src/release_gate.rs, tools/parity/src/release_evidence.rs, scripts/phase33-confirmed-settings-durability.sh</read_first>
  <action>Pass the provenance stamp explicitly to packaging and remove the package environment's live-Git firmware commit lookup. Bump active package output to schema v3, preserve top-level full `source_commit`, and add semantic version, pinned reference commit, inspected 64-character `app_elf_sha256`, and structured `build_identity` with derived `label`, `channel`, `source_dirty`, and nullable `release_tag`; reject any inconsistent projection. Preserve committed historical v2 artifacts unchanged and isolate v2 reading to explicitly historical paths. Update active xtask, release gate, release evidence, and the concrete `tools/flash` `flash`/`flash-monitor` entrypoints to require v3, compare full manifest commit to full HEAD and embedded/logged commit, compare manifest ELF SHA-256 to host inspection and running app descriptor evidence, and compare the final package digest independently. Add one reusable pre-hardware identity admission invoked by `prepare_flash` before `resolve_port`; it must first build/resolve and validate the manifest/stamp, rerun the scoped dirty classifier, and reject explicit-image hardware runs that lack an admitted v3 manifest. If dirty or inconsistent, return a stable category before detector, USB port enumeration, board-info, flash, monitor, credential-file reads, or any device action. Dry-run parser/unit tests may use synthetic inputs without hardware. Accept clean dev and clean release packages. Add manifest fixtures/tests, package-shell tests, tools/flash ordering tests with a call-recording fake environment, source guards, and an automated ESP application-descriptor inspection proving packaged ELF `version` equals `build_label` and its `app_elf_sha256` equals the manifest field.</action>
  <verify><automated>cargo test -p xtask package_manifest &amp;&amp; cargo test -p bitaxe-flash identity_admission &amp;&amp; cargo test -p bitaxe-parity release_gate &amp;&amp; cargo test -p bitaxe-parity release_evidence &amp;&amp; bash scripts/package-firmware-test.sh &amp;&amp; bazel test //scripts:package_firmware_test //tools/xtask:tests //tools/flash:tests //tools/parity:tests &amp;&amp; just package</automated></verify>
  <acceptance_criteria>
    - Schema v3 retains a bare 40-character top-level commit and validates every structured identity relationship.
    - Packaging consumes the ELF's declared stamp and performs no live-Git query for firmware identity.
    - Historical committed v2 evidence is neither rewritten nor accepted by active v3 admission accidentally.
    - Dirty, malformed, stale, or contradictory packages fail before the first detector/device subprocess; clean dev and release fixtures pass.
    - Automated inspection proves manifest label, embedded compile-time label, ESP-IDF application descriptor version, and descriptor ELF SHA-256 agree.
    - No evidence/admission function parses a short label as source proof or relaxes full-commit/package-digest equality.
  </acceptance_criteria>
  <done>Every active package carries consistent schema-v3 identity and dirty work cannot cross the hardware boundary.</done>
</task>

</tasks>

<verification>
- [ ] `cargo test -p bitaxe-api build_identity` and the workspace-status temporary-repository suite pass.
- [ ] All four label states, tag/Git failures, scoped dirtiness, stamp validation, and dirty-to-dirty invalidation have automated coverage.
- [ ] LCD, system-info, live WebSocket, retained logs, manifest v3, and application descriptor agree on the identity projection.
- [ ] Dirty admission fails before any detector or hardware interaction; clean dev and clean release admission pass.
- [ ] `bash scripts/serial-session-trace-test.sh`, `bash scripts/detect-ultra205-test.sh`, and `bash scripts/phase33-confirmed-settings-durability-test.sh` pass without hardware.
- [ ] `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features` pass in that order before the Plan 01 commit.
- [ ] `bazel test //...`, `just build`, `just package`, `just verify-reference`, and `git diff --check` pass.
- [ ] `shellcheck` and `shfmt -d` pass for every changed shell file, and the complete diff contains no secret output, hardware action, archived-lineage use, or public-contract regression.
</verification>

<success_criteria>
- One strict identity stamp owns all firmware and package identity surfaces.
- The exact LCD suffix matrix is visible without truncation while machine proof remains a full bare hash plus package digest.
- Build graph invalidation is correct for identity transitions and consecutive dirty source edits.
- Active schema-v3 admission rejects dirty or inconsistent state before hardware and accepts exact clean dev or release packages.
- No hardware, Phase 35 action, parity promotion, direct UART/pins, credential access, or archived lineage occurs.
</success_criteria>

<output>
After completion, create `.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-01-SUMMARY.md` and commit the Plan 01 implementation, tests, and summary atomically without pushing.
</output>
