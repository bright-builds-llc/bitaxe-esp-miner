---
phase: 34-provenance-runtime-health-and-coherent-operator-snapshot
plan: "05"
type: execute
wave: 5
depends_on:
  - "34-04"
autonomous: true
gap_closure: true
phase_lifecycle_id: 34-2026-07-15T03-26-15
lifecycle_mode: interactive
generated_by: gsd-plan-phase
generated_at: "2026-07-15T18:20:21Z"
requirements:
  - SYS-02
files_modified:
  - Cargo.lock
  - tools/flash/Cargo.toml
  - tools/flash/BUILD.bazel
  - tools/flash/src/main.rs
  - tools/flash/src/package_admission.rs
  - tools/xtask/src/package_manifest.rs
  - tools/parity/src/phase34_source_guard.rs
must_haves:
  truths:
    - "The exact factory merged image selected for flashing is structurally proven to contain the one admitted OTA application image at the factory partition declared by its embedded ESP partition table."
    - "Manifest admission rejects missing or duplicate required artifact kinds before any image selection, port discovery, credential access, or flash effect."
    - "The admitted OTA image is a structurally valid ESP application image whose parsed application descriptor version and ELF SHA-256 exactly match the manifest; marker substrings alone are not identity proof."
    - "Changing factory application bytes and updating only the factory manifest digest still fails closed with a stable identity-admission error."
  artifacts:
    - path: tools/flash/src/package_admission.rs
      provides: "Bounded ESP application-image, application-descriptor, and embedded partition-table admission"
    - path: tools/flash/src/main.rs
      provides: "Fail-closed ordering that binds the selected factory image to the admitted OTA bytes before external effects"
    - path: tools/xtask/src/package_manifest.rs
      provides: "Exactly-one required artifact-kind validation shared by package manifests"
  key_links:
    - from: tools/flash/src/main.rs
      to: tools/flash/src/package_admission.rs
      via: "the resolved factory and OTA paths are passed to one structural identity-admission boundary before port or credential handling"
    - from: tools/flash/src/package_admission.rs
      to: tools/xtask/src/package_manifest.rs
      via: "both boundaries reject ambiguous required artifact kinds and use the manifest identity tuple as the expected descriptor identity"
---

<objective>
Close SYS-02 by making the image selected for factory flashing the same structurally admitted identity that Phase 34 provenance claims.

Purpose: eliminate the digest-rewrite bypass in which a tampered factory sibling can pass while only the OTA sibling's marker bytes are inspected.
Output: exactly-one artifact admission, a bounded ESP image/descriptor/partition parser, selected-factory-to-OTA byte binding, and deterministic tamper regressions that fail before external effects.
</objective>

<execution_context>
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-CONTEXT.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-RESEARCH.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-VALIDATION.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-VERIFICATION.md
</execution_context>

<threat-model level="ASVS-L1" block="high">

| ID | Threat | Severity | Required mitigation and verification |
| --- | --- | --- | --- |
| T-34-G1 | A malicious factory image differs from the admitted OTA image while its manifest digest is recomputed. | critical | Parse the selected factory image's embedded partition table, locate exactly one factory app partition, and compare the embedded app prefix byte-for-byte with the separately admitted OTA image; a tamper regression must fail before port discovery. |
| T-34-G2 | Arbitrary marker substrings or an ambiguous duplicate artifact entry bypass provenance admission. | high | Parse the ESP image header, segment bounds, and application descriptor fields; require exactly one artifact of every required kind and reject duplicates before lookup. |
| T-34-G3 | Malformed offsets, lengths, or partition metadata cause out-of-bounds reads or select attacker-controlled bytes. | high | Use checked arithmetic and bounded slices, require the expected ESP32-S3 factory layout contract, and return stable errors for truncation, overflow, missing/duplicate factory partitions, and size mismatch. |
| T-34-G4 | A software-only identity check becomes a path to credentials, USB, network, flash, UART/pins, OTA execution, or hardware control. | critical | Keep the parser pure over bytes and manifest values; source/order guards and tests must prove every rejection occurs before port, credential, network, or flash effects. Real hardware and credential files are prohibited. |
| T-34-G5 | Failure diagnostics disclose source material, secrets, device identity, paths, or credentials. | high | Emit bounded reason categories and artifact-kind labels only; tests must reject secret-bearing diagnostics and must not read local credential files. |

</threat-model>

<commit-contract>
Each task ends in one atomic task commit and no push. Before every task commit, from the repository root, run this exact Rust sequence in order and stop on the first failure: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Run the task's focused Cargo/Bazel checks and `git diff --check` after that sequence. Do not commit a red regression; capture the deterministic red failure before production edits, then commit only after it is green.
</commit-contract>

<tasks>

<task type="auto" id="34-05-01">
  <name>Reject ambiguous required package artifacts before image selection</name>
  <read-first>
    - .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-VERIFICATION.md
    - tools/xtask/src/package_manifest.rs
    - tools/flash/src/main.rs
    - tools/flash/Cargo.toml
    - tools/flash/BUILD.bazel
  </read-first>
  <files>
    - tools/xtask/src/package_manifest.rs
    - tools/flash/src/main.rs
  </files>
  <action>
First add deterministic Arrange/Act/Assert regressions to the real xtask manifest validator and flash admission path for duplicate `firmware_ota_image` and duplicate `factory_merged_image` entries. Run only those tests and record that they fail because the current first-match `.find(...)` logic accepts ambiguity; do not weaken the assertion or commit the red state.

Replace first-match required-artifact lookup with an exactly-one validator. It must distinguish stable missing and duplicate errors, validate every required kind before resolving a selected image, and preserve the existing schema, digest, clean-worktree, source-commit, reference-commit, build-label, and application-SHA checks. The flash command must invoke this validation before port discovery, credential loading, command printing, process launch, or any filesystem mutation outside normal read-only package access. Add a production-path test that supplies duplicate artifacts plus a later port/credential failure sentinel and proves the duplicate-artifact error wins.

Run the exact commit-contract Rust sequence, then `cargo test -p xtask package_manifest`, `cargo test -p bitaxe-flash identity_admission`, `bazel test //tools/xtask:tests //tools/flash:tests`, and `git diff --check`; review and create one atomic commit for task 34-05-01 only.
  </action>
  <verify>
    <automated>cargo test -p xtask package_manifest &amp;&amp; cargo test -p bitaxe-flash identity_admission &amp;&amp; bazel test //tools/xtask:tests //tools/flash:tests &amp;&amp; git diff --check</automated>
  </verify>
  <done>Every required artifact kind is present exactly once, and ambiguity fails before selection or any external effect.</done>
  <acceptance-criteria>
    - Duplicate OTA and duplicate factory entries each produce their own stable admission category instead of selecting the first match.
    - Missing-kind behavior remains distinct from duplicate-kind behavior.
    - A production-path ordering test proves duplicate rejection precedes port discovery and credential handling.
    - Existing valid manifest and clean-provenance fixtures remain green.
    - Task 34-05-01 has one atomic commit created only after the exact required Rust sequence and focused checks pass.
  </acceptance-criteria>
</task>

<task type="auto" id="34-05-02">
  <name>Structurally bind the selected factory image to the admitted OTA identity</name>
  <read-first>
    - scripts/package-firmware.sh
    - scripts/package-firmware-test.sh
    - tools/flash/src/main.rs
    - tools/flash/Cargo.toml
    - tools/flash/BUILD.bazel
    - tools/xtask/src/partition_contract.rs
    - tools/parity/src/phase34_source_guard.rs
  </read-first>
  <files>
    - Cargo.lock
    - tools/flash/Cargo.toml
    - tools/flash/BUILD.bazel
    - tools/flash/src/main.rs
    - tools/flash/src/package_admission.rs
    - tools/parity/src/phase34_source_guard.rs
  </files>
  <action>
First replace the arbitrary synthetic OTA/factory fixture with a deterministic structurally valid ESP application fixture: ESP image header, bounded segment table, `0xABCD5432` application descriptor, fixed-width version, manifest ELF SHA-256 bytes, and a merged factory image containing an ESP binary partition table at `0x8000` with the factory app at `0x10000`. Add a production-path regression that flips one byte in the factory app region, recomputes the factory artifact digest in the manifest, leaves the OTA artifact unchanged, and expects a stable factory/OTA mismatch before any port or credential sentinel. Run the focused test and record the expected red failure before implementing the parser.

Create `package_admission.rs` as a pure, bounded parser. Use the already workspace-pinned `esp-idf-part` crate to parse the embedded partition table; require exactly one app/factory entry, checked offset/size arithmetic, the expected package layout, and a factory slice large enough for the admitted OTA bytes. Parse the OTA ESP image itself rather than searching the whole file: validate magic, segment count, every segment boundary, descriptor location/magic, the fixed-width application version, and the descriptor ELF SHA-256. Compare parsed version and SHA exactly with the manifest identity. If source-commit embedding remains a requirement, search only validated segment payloads for the exact full commit bytes; descriptor version/SHA must never fall back to marker scanning. Compare the factory partition's OTA-length prefix byte-for-byte to the fully admitted OTA image. Do not compare padding beyond the OTA image and do not invent a second package format.

Wire the module into `resolve_flash_image` so manifest uniqueness, artifact digests, OTA structural identity, factory partition structure, and factory/OTA equality all pass before the selected factory path is returned. Expand the Phase 34 source guard to require that ordering and reject reintroduction of whole-file build-label/SHA marker checks. Error text must be redacted and category-stable. Update Bazel/Cargo dependencies and the public Phase 34 flash-source filegroup for the new module; update `Cargo.lock` only through normal Cargo resolution.

Run the exact commit-contract Rust sequence, then `cargo test -p bitaxe-flash package_admission`, `cargo test -p bitaxe-parity phase34_package_and_hardware_admission_source_guard`, `bazel test //tools/flash:tests //tools/parity:tests //scripts:package_firmware_test`, and `git diff --check`; review and create one atomic commit for task 34-05-02 only.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-flash package_admission &amp;&amp; cargo test -p bitaxe-parity phase34_package_and_hardware_admission_source_guard &amp;&amp; bazel test //tools/flash:tests //tools/parity:tests //scripts:package_firmware_test &amp;&amp; git diff --check</automated>
  </verify>
  <done>The exact selected factory image is admitted through its embedded partition table and contains the exact structurally validated OTA identity; digest-rewritten factory tampering fails before external effects.</done>
  <acceptance-criteria>
    - The valid fixture uses real ESP image, application descriptor, and binary partition-table structures rather than arbitrary marker blobs.
    - Descriptor version and ELF SHA-256 are parsed from bounded fields and match the manifest exactly.
    - The selected factory image contains exactly one factory app partition and its OTA-length prefix equals the admitted OTA image byte-for-byte.
    - A one-byte factory-app tamper plus an updated factory manifest digest fails with the expected stable mismatch category before port discovery, credentials, or flash execution.
    - Truncated headers, overflowing segments, invalid descriptor magic, malformed/missing/duplicate factory partitions, and undersized factory images each fail closed without panic.
    - Task 34-05-02 has one atomic commit created only after the exact required Rust sequence and focused checks pass.
  </acceptance-criteria>
</task>

</tasks>

<verification>

- Confirm both red regressions failed for their intended old behavior before their respective production edits, then confirm they pass without assertion weakening.
- After both task commits, run focused checks: `cargo test -p xtask package_manifest`, `cargo test -p bitaxe-flash package_admission`, `cargo test -p bitaxe-parity phase34_package_and_hardware_admission_source_guard`, and `bazel test //tools/xtask:tests //tools/flash:tests //tools/parity:tests //scripts:package_firmware_test`.
- Run the final exact sequence `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, `cargo test --all-features`, followed by `bazel test //...`, `just build`, `just package`, `just verify-reference`, and `git diff --check`.
- No shell file is modified by this plan, so shellcheck/shfmt are not applicable. If execution unexpectedly changes a shell file, stop, re-plan its ownership, then run the repository's shellcheck and shfmt checks before any commit.
- Review the final diff for unrelated artifact formats, hardware/USB/network/credential access, Phase 35 or OTA execution, direct UART/pins, archived Phase 28 lineage, or secret-bearing diagnostics; any such change fails the plan.
- Do not rerun or edit Phase 34 verification, requirements, roadmap, or state yet. Plans 34-06 and 34-07 must also complete first.

</verification>

<success_criteria>

- SYS-02 is covered by production-path tests proving the selected merged factory image and admitted OTA image are structurally and bytewise bound.
- Artifact ambiguity, image malformation, descriptor mismatch, partition mismatch, and digest-rewritten tampering all fail before external effects.
- Repository-wide software verification passes without hardware, credentials, network access, or scope expansion.

</success_criteria>

<output>
After implementation, create `.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-05-SUMMARY.md` using the normal GSD task-commit and plan-summary contract. Do not push, do not mark SYS-02 complete, and do not rerun Phase 34 verification until Plans 34-05 through 34-07 all have summaries.
</output>
