---
phase: 34-provenance-runtime-health-and-coherent-operator-snapshot
plan: 03
type: execute
wave: 3
depends_on: [34-02]
files_modified:
  - crates/bitaxe-api/BUILD.bazel
  - crates/bitaxe-api/src/lib.rs
  - crates/bitaxe-api/src/platform_identity.rs
  - crates/bitaxe-api/src/operator_snapshot.rs
  - crates/bitaxe-api/src/wire.rs
  - crates/bitaxe-api/fixtures/api/system-info-ultra205-safe.json
  - firmware/bitaxe/src/platform_identity.rs
  - firmware/bitaxe/src/runtime_snapshot.rs
  - firmware/bitaxe/src/main.rs
  - tools/parity/src/phase34_source_guard.rs
autonomous: true
requirements: [SYS-03, SYS-04, SYS-05]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 34-2026-07-15T03-26-15
generated_at: "2026-07-15T03:51:00Z"
must_haves:
  truths:
    - Platform identity facts are embedded or runtime-derived from the running device and never substituted from host fixtures or checkout state.
    - ESP-IDF, static assets, board 205, BM1366, running partition, reset reason, uptime, and heap facts are truthful or explicitly unavailable.
    - All platform facts are captured inside the coherent boot-session/revision snapshot established by Plan 02.
  artifacts:
    - crates/bitaxe-api/src/platform_identity.rs defines typed availability and closed platform/reset vocabularies.
    - firmware/bitaxe/src/platform_identity.rs is the sole ESP-IDF/runtime adapter for platform facts.
    - tools/parity/src/phase34_source_guard.rs rejects fixtures, host Git, and synthetic placeholders from live identity paths.
  key_links:
    - firmware/bitaxe/src/platform_identity.rs supplies one candidate consumed by firmware/bitaxe/src/runtime_snapshot.rs.
    - crates/bitaxe-api/src/operator_snapshot.rs carries platform identity under the Plan 02 session and revision.
---

<objective>
Add truthful running-platform identity and runtime facts to the coherent operator snapshot, with explicit unavailability whenever firmware cannot prove a value.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-CONTEXT.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-RESEARCH.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-02-PLAN.md
@crates/bitaxe-api/src/snapshot.rs
@crates/bitaxe-api/src/wire.rs
@firmware/bitaxe/src/runtime_snapshot.rs
</context>

<threat_model>
- **T-34-13 (high):** Host checkout, fixture, or package-time assumptions could masquerade as running-device truth. Permit only compile-time embedded constants and current ESP-IDF/runtime adapter reads.
- **T-34-14 (high):** Zero/default values could imply proof. Represent every unproved fact with typed explicit unavailability and project that state publicly.
- **T-34-15 (medium):** Facts sampled across different revisions could form an impossible state. Capture one platform candidate inside the Plan 02 coherent snapshot transaction.
</threat_model>

<tasks>

<task type="auto" id="34-03-01">
  <name>Task 1: Define closed platform identity and availability contracts</name>
  <files>crates/bitaxe-api/BUILD.bazel, crates/bitaxe-api/src/lib.rs, crates/bitaxe-api/src/platform_identity.rs, crates/bitaxe-api/src/operator_snapshot.rs, crates/bitaxe-api/src/wire.rs, crates/bitaxe-api/fixtures/api/system-info-ultra205-safe.json</files>
  <read_first>crates/bitaxe-api/src/operator_snapshot.rs, crates/bitaxe-api/src/snapshot.rs, crates/bitaxe-api/src/wire.rs, .planning/REQUIREMENTS.md</read_first>
  <action>Create typed platform facts for ESP-IDF version, AxeOS/static-asset identity, fixed board `205`, fixed ASIC `BM1366`, running partition, decoded reset reason, uptime milliseconds, internal heap free/minimum/largest-block facts, and PSRAM availability. Use a generic or field-specific availability representation that serializes either the proved value or an explicit unavailable state; numeric zero must never authenticate availability. Define a closed reset-reason vocabulary with software CPU reset represented distinctly and unknown codes mapped to unavailable/unknown rather than guessed. Add the complete platform candidate to the coherent operator snapshot from Plan 02 and project additive wire fields without removing upstream-compatible fields.</action>
  <verify><automated>cargo test -p bitaxe-api platform_identity &amp;&amp; cargo test -p bitaxe-api operator_snapshot &amp;&amp; cargo test -p bitaxe-api wire</automated></verify>
  <acceptance_criteria>
    - Tests cover every available and unavailable platform fact plus unknown reset codes.
    - Board and ASIC values are exactly `205` and `BM1366`; no other board receives an availability claim.
    - Zero heap/uptime compatibility values cannot change an unavailable fact to available.
    - System-info fixtures contain the additive identity/runtime fields without removing existing fields.
  </acceptance_criteria>
  <done>The pure API contract can represent every required platform fact without manufacturing proof.</done>
</task>

<task type="auto" id="34-03-02">
  <name>Task 2: Collect running ESP-IDF facts once and project them coherently</name>
  <files>firmware/bitaxe/src/platform_identity.rs, firmware/bitaxe/src/runtime_snapshot.rs, firmware/bitaxe/src/main.rs, tools/parity/src/phase34_source_guard.rs</files>
  <read_first>firmware/bitaxe/src/runtime_snapshot.rs, firmware/bitaxe/src/main.rs, firmware/bitaxe/src/boot_evidence.rs, firmware/bitaxe/src/filesystem.rs, firmware/bitaxe/src/ota_update.rs, tools/parity/src/phase34_source_guard.rs</read_first>
  <action>Add one firmware-owned adapter that reads embedded ESP-IDF/static-asset/build constants and current ESP-IDF partition/reset/uptime/heap APIs, converts them to the pure platform candidate, and performs no write or hardware effect. Capture the candidate only through the Plan 02 snapshot publisher so system-info, WebSocket, retained-log, and evidence projections share its boot session and revision. Fail individual unavailable calls independently rather than dropping other facts. Extend the Phase 34 source guard to reject fixture reads, host Git/process calls, synthetic placeholders, alternate board claims, active reset/watchdog/OTA calls, and request-time mutation in the live path.</action>
  <verify><automated>cargo test -p bitaxe-api platform_identity &amp;&amp; cargo test -p bitaxe-parity phase34_source_guard &amp;&amp; just build</automated></verify>
  <acceptance_criteria>
    - The firmware adapter has no setter, write, reset, watchdog, OTA, mining, credential, UART, or pin capability.
    - All projections use the same Plan 02 session and revision for one captured candidate.
    - One unavailable ESP-IDF call leaves unrelated proved facts intact.
    - Source guards find no host-checkout or fixture substitution in production identity paths.
  </acceptance_criteria>
  <done>The running firmware publishes truthful platform facts or explicit unavailability through one coherent snapshot.</done>
</task>

</tasks>

<verification>
- [ ] Platform availability, reset vocabulary, heap/uptime, board/ASIC, partition, ESP-IDF, and static-asset tests pass.
- [ ] Source guards reject host/fixture substitution and every prohibited effect.
- [ ] `just build`, affected Bazel tests, mandatory Rust gates, and `git diff --check` pass.
</verification>

<success_criteria>
- SYS-03, SYS-04, and SYS-05 are represented truthfully in the coherent operator snapshot.
- No unavailable fact is replaced with zero, a fixture, host state, or a synthetic placeholder.
- No hardware action or Phase 35 evidence work occurs.
</success_criteria>

<output>
After completion, create `.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-03-SUMMARY.md`; use the normal GSD task-commit and plan-summary commit contract, and do not push.
</output>
