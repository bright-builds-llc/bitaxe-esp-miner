---
phase: 34-provenance-runtime-health-and-coherent-operator-snapshot
plan: "07"
type: execute
wave: 7
depends_on:
  - "34-06"
autonomous: true
gap_closure: true
phase_lifecycle_id: 34-2026-07-15T03-26-15
lifecycle_mode: interactive
generated_by: gsd-plan-phase
generated_at: "2026-07-15T18:20:21Z"
requirements:
  - OBS-06
files_modified:
  - crates/bitaxe-api/BUILD.bazel
  - crates/bitaxe-api/src/lib.rs
  - crates/bitaxe-api/src/operator_snapshot_publication.rs
  - firmware/bitaxe/BUILD.bazel
  - firmware/bitaxe/src/http_api.rs
  - firmware/bitaxe/src/runtime_snapshot.rs
  - tools/parity/src/operator_snapshot_evidence.rs
  - tools/parity/src/phase34_source_guard.rs
must_haves:
  truths:
    - "Concurrent snapshot collection may complete in any order, but revisions are assigned only at the serialized publication boundary after collection completes."
    - "Identity attachment, retained marker/runtime-health append, and the actual HTTP response or live-WebSocket send/enqueue call occur under one publication authority, so retained and externally issued revision order cannot regress."
    - "A deterministic reverse-completion regression compiles and runs the production publication transition and proves revisions remain strictly increasing in direct retained and combined HTTP/WebSocket issuance order without sorting."
    - "Poison, reentrancy, retention failure, and issuance failure fail closed without resetting or reusing a revision, release the ordering lock, and preserve the next strictly greater revision."
    - "Existing boot-session identity, API/WebSocket wire and error shapes, redaction, cadence/connect behavior, and projection correlation remain unchanged."
  artifacts:
    - path: crates/bitaxe-api/src/operator_snapshot_publication.rs
      provides: "Host-compatible production transition with injected collection, completion, retention, and issuance adapters plus poison/reentrancy/error semantics"
    - path: firmware/bitaxe/src/runtime_snapshot.rs
      provides: "Collect-before-publish firmware integration covering identity attachment, projection, and retained records"
    - path: firmware/bitaxe/src/http_api.rs
      provides: "Actual `/api/system/info`, live cadence, and live-connect HTTP/WebSocket issuance closures executed under the publication authority"
    - path: tools/parity/src/operator_snapshot_evidence.rs
      provides: "Adversarial reverse-completion test that invokes the production transition with recording HTTP/WebSocket issuers and validates unsorted chronology"
  key_links:
    - from: firmware/bitaxe/src/runtime_snapshot.rs
      to: crates/bitaxe-api/src/operator_snapshot_publication.rs
      via: "one boot-lifetime OperatorSnapshotPublisher collects outside its mutex, then owns identity assignment, completion/projection, retention, and injected issuance until the lock is released"
    - from: firmware/bitaxe/src/http_api.rs
      to: crates/bitaxe-api/src/operator_snapshot_publication.rs
      via: "`handle_system_info`, `broadcast_live_telemetry_cadence`, and the LiveTelemetry branch of `send_websocket_connect_frames` pass the unchanged `send_json`, async broadcast, and synchronous WebSocket send operations as the publisher's issuance adapter"
    - from: firmware/bitaxe/src/http_api.rs
      to: firmware/bitaxe/src/websocket_api.rs
      via: "live connect/cadence frame planning finishes its WebSocket-state borrow before the bounded external send loop, with no reverse call into snapshot publication"
    - from: crates/bitaxe-api/src/operator_snapshot_publication.rs
      to: tools/parity/src/operator_snapshot_evidence.rs
      via: "the host regression imports and executes the same publisher type used by firmware and records retained plus issued bytes in call order"
    - from: firmware/bitaxe/BUILD.bazel
      to: crates/bitaxe-api/BUILD.bazel
      via: "the focused Bazel publication suite compiles the production transition and the parity integration regression on the host"
---

<objective>
Close OBS-06 by moving revision assignment from collection start to a serialized, completion-ordered publication boundary that owns the actual external issuance call.

Purpose: prevent reverse-completing HTTP and live-WebSocket captures from appending or externally issuing revisions in decreasing order.
Output: one host-runnable production transition, firmware collect-before-publish integration through the real HTTP/WebSocket send sites, and deterministic barrier-controlled evidence proving monotonic retained and externally issued order.
</objective>

<execution_context>
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-CONTEXT.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-VALIDATION.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-VERIFICATION.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-02-SUMMARY.md
</execution_context>

<threat-model level="ASVS-L1" block="high">

| ID | Threat | Severity | Required mitigation and verification |
| --- | --- | --- | --- |
| T-34-G11 | Concurrent captures reserve revisions before completion and then publish retained/public records in decreasing revision order. | high | Collect unnumbered candidates first and serialize identity allocation, identity attachment, retention, and projection issuance under one publication authority; reverse completion must still issue revisions 1 then 2. |
| T-34-G12 | A lock covers sequence allocation and DTO construction while the HTTP/WebSocket send remains outside it. | high | `OperatorSnapshotPublisher::publish` owns the injected issuance adapter through the actual `send_json`, `httpd_ws_send_frame`, or `httpd_ws_send_frame_async` call; source guards reject return-then-send wiring and the behavioral regression records issuance from the production transition itself. |
| T-34-G13 | Poison, exhaustion, retention failure, or issuance failure resets/reuses an identity, leaves the lock held, or silently emits stale truth. | high | Recover the poisoned inner sequence without reset, report lock health, fail closed on exhaustion, consume but never retry a failed revision, release through RAII on every result path, and prove the next successful publication is strictly greater. |
| T-34-G14 | Evidence is sorted after capture and hides the order actually issued. | high | Record append order directly in the adversarial test and validate it without sorting; retain candidate labels to prove completion order was intentionally reversed. |
| T-34-G15 | The concurrency fix changes public shapes, breaks health correlation, leaks secrets, or introduces network/hardware effects. | critical | Reuse the existing identity fields and projections, retain redacted records inside the boundary, and prohibit credentials, endpoints, device identifiers, USB/network, OTA, UART/pins, or hardware/mining effects. |
| T-34-G16 | A publication closure re-enters the same authority or acquires locks in reverse order, causing a deadlock; slow or unbounded work creates indefinite head-of-line blocking. | high | Add a thread-local RAII reentrancy guard that returns `Reentrant` before collection, collect all platform/config/Wi-Fi/safety/runtime-health facts outside the ordering mutex, release WebSocket-state guards before sending, and allow inside only checked allocation, bounded projection/serialization, two bounded retained appends, one HTTP write, or at most `MAX_WEBSOCKET_CLIENTS` send/enqueue calls with no sleep/retry/polling. |

</threat-model>

<commit-contract>
Each task ends in one atomic task commit and no push. Before every task commit, from the repository root, run this exact Rust sequence in order and stop on the first failure: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Run the task's focused Cargo/Bazel checks and `git diff --check` after that sequence. Do not commit a red regression; capture the deterministic red failure before production edits, then commit only after it is green.
</commit-contract>

<tasks>

<task type="auto" id="34-07-01">
  <name>Add the host-runnable production publication transition</name>
  <read-first>
    - .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-VERIFICATION.md
    - .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-REVIEW.md
    - crates/bitaxe-api/BUILD.bazel
    - crates/bitaxe-api/src/lib.rs
    - crates/bitaxe-api/src/operator_snapshot.rs
    - crates/bitaxe-api/src/settings.rs
    - firmware/bitaxe/BUILD.bazel
    - firmware/bitaxe/src/http_api.rs
    - firmware/bitaxe/src/runtime_snapshot.rs
    - firmware/bitaxe/src/websocket_api.rs
    - tools/parity/BUILD.bazel
  </read-first>
  <files>
    - crates/bitaxe-api/BUILD.bazel
    - crates/bitaxe-api/src/lib.rs
    - crates/bitaxe-api/src/operator_snapshot_publication.rs
    - firmware/bitaxe/BUILD.bazel
  </files>
  <action>
Create `operator_snapshot_publication.rs` as the small production transition used verbatim by firmware and compiled by host tests. Define `OperatorSnapshotPublisher { sequence: Mutex<OperatorSnapshotSequence> }`, `OperatorSnapshotLockHealth::{Healthy, RecoveredPoison}`, `OperatorSnapshotPublication<T> { output: T, lock_health: OperatorSnapshotLockHealth }`, and `OperatorSnapshotPublishError<E>` with exact variants `Reentrant`, `SequenceExhausted { lock_health }`, `Retention { source, lock_health }`, and `Issuance { source, lock_health }`; every error exposes its lock health when one was acquired. Export these types from `lib.rs` and add the new source to both the `bitaxe_api` library and `phase34_identity_sources` filegroup in `crates/bitaxe-api/BUILD.bazel`.

Implement `OperatorSnapshotPublisher::new()` and one public generic method with this semantic signature: `publish(boot_session, collect, complete, retain, issue)`, where `collect: FnOnce() -> Candidate` runs before locking; after locking, checked `next_identity` runs, `complete: FnOnce(Candidate, OperatorSnapshotIdentity) -> Publication` attaches identity and constructs the unchanged projection, `retain: FnOnce(&Publication) -> Result<(), E>` appends retained state, and `issue: FnOnce(Publication) -> Result<T, E>` performs the final external issue. The mutex remains held from identity allocation through `issue` return. Retention precedes issuance. A retention failure skips issuance; an issuance failure leaves already-retained chronology intact. Either failure consumes its revision, returns the typed stage error after RAII unlock, and a later success receives a strictly greater revision. Exhaustion never wraps, resets, or calls any fallback.

At method entry, use a thread-local RAII publication-depth guard. A same-thread nested call returns `Reentrant` before invoking `collect` rather than waiting on its own mutex; `Drop` clears the guard on success, error, or unwind. When `Mutex::lock` reports poison, recover `poisoned.into_inner()`, retain the existing `OperatorSnapshotSequence`, clear only poison status, and return/report `RecoveredPoison`; never replace the state with `OperatorSnapshotSequence::new()`. Keep clocks, RNG, Git, MAC/device values, logging, serialization, ESP-IDF, and firmware effects out of this module.

Add focused Arrange/Act/Assert tests in this production module, using barriers/channels and no sleeps: collection runs outside the lock; a second completion cannot enter while the first issue adapter is open; direct publication order is `[1, 2]` without sorting; retention failure skips issue and releases the lock; issuance failure releases the lock, leaves its revision consumed, and the next issued revision is greater; a caught panic poisons the mutex and the next call reports `RecoveredPoison` without resetting the sequence; and nested publication returns `Reentrant` without executing nested collection. Existing allocator uniqueness tests may still sort only to prove set uniqueness, but no test may describe that as publication chronology.

Add `operator_snapshot_publication_tests` in `firmware/bitaxe/BUILD.bazel` as the focused host suite over `//crates/bitaxe-api:tests` and `//tools/parity:tests`; do not create an ESP-IDF-only test seam or copy the transition into a test model. Run the exact commit-contract Rust sequence, then `cargo test -p bitaxe-api operator_snapshot_publication`, `bazel test //crates/bitaxe-api:tests`, and `git diff --check`; review and create one atomic commit for task 34-07-01 only.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-api operator_snapshot_publication &amp;&amp; bazel test //crates/bitaxe-api:tests &amp;&amp; git diff --check</automated>
  </verify>
  <done>The host-tested production transition serializes identity, retention, and injected issuance with explicit poison, reentrancy, and stage-failure behavior.</done>
  <acceptance-criteria>
    - `crates/bitaxe-api/src/operator_snapshot_publication.rs` contains the exact `OperatorSnapshotPublisher`, `OperatorSnapshotLockHealth`, `OperatorSnapshotPublication`, and `OperatorSnapshotPublishError` types and one `publish(boot_session, collect, complete, retain, issue)` transition used by later firmware wiring.
    - Tests prove collection precedes lock acquisition, the mutex remains held through the issue adapter, and direct publication order is `[1, 2]` without sorting.
    - Tests prove poison recovery retains the next revision, nested publication fails before nested collection, and retention/issuance errors release the lock without retry, rollback, reset, or revision reuse.
    - Work while ordered is structurally bounded: no collection, sleep, retry, polling, device access, or unbounded queue/loop exists in the production transition.
    - `crates/bitaxe-api/BUILD.bazel` compiles the new module in the library/tests and `firmware/bitaxe/BUILD.bazel` exposes `operator_snapshot_publication_tests` without copying production logic.
    - Task 34-07-01 has one atomic commit created only after the exact required Rust sequence and focused checks pass.
  </acceptance-criteria>
</task>

<task type="auto" id="34-07-02">
  <name>Wire retained records and real HTTP/WebSocket issuance through the production transition</name>
  <read-first>
    - AGENTS.md
    - standards/core/architecture.md
    - standards/core/testing.md
    - standards/core/verification.md
    - standards/languages/rust.md
    - .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-CONTEXT.md
    - .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-VALIDATION.md
    - .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-VERIFICATION.md
    - .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-REVIEW.md
    - crates/bitaxe-api/src/operator_snapshot.rs
    - crates/bitaxe-api/src/operator_snapshot_publication.rs
    - crates/bitaxe-api/src/runtime_projection.rs
    - crates/bitaxe-api/src/snapshot.rs
    - crates/bitaxe-api/src/telemetry.rs
    - crates/bitaxe-api/src/websocket_state.rs
    - crates/bitaxe-api/src/wire.rs
    - crates/bitaxe-api/BUILD.bazel
    - firmware/bitaxe/BUILD.bazel
    - firmware/bitaxe/src/runtime_snapshot.rs
    - firmware/bitaxe/src/http_api.rs
    - firmware/bitaxe/src/log_buffer.rs
    - firmware/bitaxe/src/websocket_api.rs
    - tools/parity/BUILD.bazel
    - tools/parity/src/operator_snapshot_evidence.rs
    - tools/parity/src/phase34_source_guard.rs
    - .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-02-PLAN.md
  </read-first>
  <files>
    - firmware/bitaxe/src/runtime_snapshot.rs
    - firmware/bitaxe/src/http_api.rs
    - tools/parity/src/operator_snapshot_evidence.rs
    - tools/parity/src/phase34_source_guard.rs
  </files>
  <action>
First replace the modeled publication-flow test in `operator_snapshot_evidence.rs` with a deterministic regression that imports and executes the production `bitaxe_api::OperatorSnapshotPublisher`. Use two named candidates, `HttpSystemInfoCapture1` and `LiveWebSocketCapture2`, plus channels/barriers and no sleeps. Start capture 1 and block it inside `collect`; start capture 2, require its collection to complete and its whole publication to finish, then release capture 1. The injected `complete` adapter must attach the assigned identity and build an identity-bearing JSON payload plus matching retained marker/runtime-health record. The injected `retain` adapter appends directly to one shared retained history. The injected issuers must be distinct `issue_http_response` and `issue_live_websocket_frame` adapters that append the exact serialized bytes to one combined `IssuedPayload::{Http, LiveWebSocket}` history. Assert candidate completion/issuance order is `[LiveWebSocketCapture2, HttpSystemInfoCapture1]`, both direct revision streams are `[1, 2]` without sorting, every issued payload decodes to its retained boot session/revision/runtime-health values, and the existing chronology validator accepts the direct retained/public stream. This test must fail to compile or fail behaviorally if replaced with reservation-only `OperatorSnapshotSequence`; source strings are supplementary, not its transition under test.

In `runtime_snapshot.rs`, replace `OPERATOR_SNAPSHOT_SEQUENCE` and `reserve_operator_snapshot_identity` with exactly one boot-lifetime `OnceLock<OperatorSnapshotPublisher>`. Define an unnumbered `OperatorSnapshotCandidate` containing the already-cloned runtime projection/sample marker/block-found state plus all collected platform/config/Wi-Fi/safety/runtime-health facts needed to complete `ApiSnapshot`. All platform/config/Wi-Fi/safety/runtime-health collection and sample-marker selection finish in the `collect` adapter before the publisher mutex. The `complete` adapter attaches the assigned identity exactly once and constructs either `ApiSnapshot`, `SystemInfoWire`, or `ProjectedApiViews`. The `retain` adapter appends the exact existing marker and runtime-health record before issuance. Keep `collect_api_snapshot`, statistics, scoreboard, and other identity-less compatibility callers on this same transition, but they may use a no-I/O issue adapter that returns their existing value.

Replace the return-then-send entrypoints for the two identity-bearing public surfaces with issuer-taking functions: `publish_projected_system_info(timestamp_ms, issue)` and `publish_projected_live_telemetry_payload(timestamp_ms, issue)`. They invoke the supplied `FnOnce` only as the production publisher's `issue` adapter while its ordering mutex is held. They must not expose an identity-bearing DTO that `http_api.rs` can send after return. Preserve pending sample-marker drain policy exactly: system-info and both live-WebSocket paths do not drain, statistics drains once, and scoreboard does not drain.

In `http_api.rs`, move all three real identity-bearing issuance sites into those issuer closures. `handle_system_info` must call `publish_projected_system_info` and invoke the existing `send_json(request, &system_info)` inside the closure, retaining HTTP 200, `application/json`, body, access-gate, and `anyhow` propagation behavior. `broadcast_live_telemetry_cadence` must call `publish_projected_live_telemetry_payload`; inside its closure call `websocket_api::live_cadence_frame`, serialize the unchanged envelope/diff with `serde_json::to_string`, and call `broadcast_websocket_text_frame`, whose bounded loop invokes `httpd_ws_send_frame_async` for the at-most-`MAX_WEBSOCKET_CLIENTS` snapshot. The LiveTelemetry branch of `send_websocket_connect_frames` must likewise plan `live_connect_frame`, serialize, and call `send_websocket_text_frame`/`httpd_ws_send_frame` inside the issuance closure. Keep the Logs branch, ping frames, client registration/unregistration, stale-client logging, cadence interval/thread, `ESP_FAIL`/`ESP_OK` results, and serialization/send-failure categories unchanged.

Map `SequenceExhausted` and `Reentrant` fail closed: system-info returns the existing handler error path without a fabricated body; live-connect returns `ESP_FAIL`; cadence logs one redaction-safe internal category and emits no frame. For retention or issuance failure, do not retry or reuse the revision. The retained-before-issue record remains valid when a send fails; the next publication receives a greater revision. Log `RecoveredPoison` without resetting sequence. Ensure the publisher guard is released before any caller-side error logging or stale-session cleanup that could acquire another mutex.

Keep ordered work bounded and document/check the lock order: no platform/config/Wi-Fi/safety/runtime-health collection, sleeps, retries, polling, or hardware calls occur while ordered; retained appends are two bounded buffer writes; system-info issues one bounded DTO write; live issuance plans/serializes one bounded frame and performs at most `MAX_WEBSOCKET_CLIENTS` send/enqueue calls. `websocket_api::live_connect_frame`, `live_cadence_frame`, and `client_sessions` must release `WEBSOCKET_STATE` before any ESP-IDF send, and no log-buffer or WebSocket-state path may call back into `publish_projected_*`.

Update `operator_snapshot_evidence.rs` and `phase34_source_guard.rs` guards to inspect `HTTP_API_SOURCE` as well as runtime/API sources. Require collection before the production `publish` call, exactly one publisher static, identity attachment/projection/retention inside its adapters, `send_json` inside the system-info issuer, async broadcast inside the live-cadence issuer, and synchronous WebSocket send inside the live-connect issuer. Reject the old `projected_*` return-then-send pattern, reservation-only functions, second publisher instances, sorting in the adversarial regression, after-unlock retained append, identity fallback, nested publication from retain/issue, and WebSocket/log locks held across entry into the publisher. Do not assert only strings as proof of ordering; the production-transition regression is mandatory.

Run the exact commit-contract Rust sequence, then `cargo test -p bitaxe-api operator_snapshot_publication`, `cargo test -p bitaxe-parity operator_snapshot_evidence`, `cargo test -p bitaxe-parity phase34_source_guard`, `bazel test //firmware/bitaxe:operator_snapshot_publication_tests //crates/bitaxe-api:tests //tools/parity:tests`, `just build`, and `git diff --check`; review and create one atomic commit for task 34-07-02 only.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-api operator_snapshot_publication &amp;&amp; cargo test -p bitaxe-parity operator_snapshot_evidence &amp;&amp; cargo test -p bitaxe-parity phase34_source_guard &amp;&amp; bazel test //firmware/bitaxe:operator_snapshot_publication_tests //crates/bitaxe-api:tests //tools/parity:tests &amp;&amp; just build &amp;&amp; git diff --check</automated>
  </verify>
  <done>Firmware assigns revisions after collection completion and holds one production authority through retained records and the real HTTP/WebSocket send or enqueue boundary.</done>
  <acceptance-criteria>
    - `runtime_snapshot.rs` has exactly one `OnceLock<OperatorSnapshotPublisher>`, one unnumbered candidate collection path before `publish`, and no reservation-only function or identity allocation before candidate collection.
    - `handle_system_info` performs `send_json` inside `publish_projected_system_info`'s issue adapter; `broadcast_live_telemetry_cadence` performs `live_cadence_frame` serialization and `httpd_ws_send_frame_async` broadcast inside `publish_projected_live_telemetry_payload`'s issue adapter; the LiveTelemetry branch of `send_websocket_connect_frames` performs `live_connect_frame` serialization and `httpd_ws_send_frame` inside that same kind of issue adapter. No other file contains an identity-bearing HTTP/WebSocket send site.
    - The reverse-completion regression imports the production publisher, blocks capture 1 so capture 2 completes collection and publication first, and asserts direct retained and combined `IssuedPayload::{Http, LiveWebSocket}` revisions `[1, 2]` without sorting while preserving matching boot session/revision/runtime-health values.
    - Poison recovery does not reset sequence; retention/issuance failure and reentrancy release/fail before deadlock, never retry or reuse a revision, and allow the next publication to issue a strictly greater revision.
    - Collection and all potentially effectful device reads occur outside the ordering mutex; inside work is limited to bounded attachment/projection/serialization, two retained appends, one HTTP write, or at most `MAX_WEBSOCKET_CLIENTS` live send/enqueue calls with no sleep/retry/polling or reverse lock acquisition.
    - Existing system-info HTTP status/content type/body, live connect envelope, live cadence/diff and hibernation behavior, WebSocket send/unregister errors, ASIC settings, statistics, scoreboard, raw-log, heartbeat, PATCH, restart, and boot-session fixtures remain green and wire-compatible.
    - No fallback identity, credentials, endpoint/device data, USB/network, OTA, UART/pin, hardware, or mining effect is introduced.
    - Task 34-07-02 has one atomic commit created only after the exact required Rust sequence and focused checks pass.
  </acceptance-criteria>
</task>

</tasks>

<verification>

- Confirm each deterministic adversarial regression failed for the intended reservation/return-before-issuance defect before implementation and passes while importing the production transition, without sorting or weakened assertions.
- Run focused checks: `cargo test -p bitaxe-api operator_snapshot_publication`, `cargo test -p bitaxe-parity operator_snapshot_evidence`, `cargo test -p bitaxe-parity phase34_source_guard`, and `bazel test //firmware/bitaxe:operator_snapshot_publication_tests //crates/bitaxe-api:tests //tools/parity:tests`.
- Run the final exact sequence `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, `cargo test --all-features`, followed by `bazel test //...`, `just build`, `just package`, `just verify-reference`, and `git diff --check`.
- No shell file is modified by this plan, so shellcheck/shfmt are not applicable. If execution unexpectedly changes one, stop and re-plan before running the repository shell checks.
- Review the diff and evidence for sorting that hides chronology, modeled copies of the production transition, multiple publication authorities, return-then-send HTTP/WebSocket wiring, after-unlock projection/retention/issuance, poison reset, reentrant deadlock, unbounded ordered work, changed wire/error shapes, secret or local-identifier output, hardware/USB/network/credential work, Phase 35, OTA execution, direct UART/pins, or archived Phase 28 lineage; any occurrence fails the plan.
- Only after Plans 34-05, 34-06, and 34-07 all have completed summaries and the working tree is reviewed may the orchestrator run a fresh Phase 34 verification. Individual plan execution must not mark OBS-06/SYS-02/HLT-02/HLT-04 complete or edit roadmap/requirements/state.

</verification>

<success_criteria>

- OBS-06: reverse-completing captures receive revisions in completion/publication order, and direct retained plus actual HTTP/WebSocket issuance never decrease.
- One host-tested production authority covers identity allocation, identity attachment, retained marker/runtime-health records, public projection construction, and the final unchanged HTTP/WebSocket send or enqueue call.
- Poison, failure, and reentrancy paths release safely without reset/reuse/deadlock; ordered work is bounded and existing API/WebSocket wire and error semantics remain unchanged.
- All focused and repository-wide software checks pass, after which a fresh Phase 34 verifier—not an individual plan executor—decides requirement status.

</success_criteria>

<output>
After implementation, create `.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-07-SUMMARY.md` using the normal GSD task-commit and plan-summary contract. Do not push. When and only when Plans 34-05 through 34-07 all have summaries and final software verification is green, hand control to the Phase 34 verification workflow; do not directly mark requirements complete.
</output>
