---
phase: 36-substantive-evidence-admission-and-exact-re-promotion
plan: "03"
type: execute
wave: 3
depends_on:
  - "36-02"
autonomous: true
phase_lifecycle_id: 36-2026-07-23T15-20-53
lifecycle_mode: yolo
generated_by: gsd-plan-phase
generated_at: "2026-07-23T15:45:00Z"
requirements:
  - SYS-02
  - EVD-11
  - EVD-12
  - EVD-14
  - EVD-15
files_modified:
  - tools/parity/src/phase36_promotion.rs
  - tools/parity/src/phase36_promotion/
  - tools/parity/src/operator_evidence/generation/
  - tools/parity/src/main.rs
  - tools/parity/fixtures/phase36/
  - tools/parity/BUILD.bazel
  - scripts/phase36-evidence-test.sh
  - docs/parity/checklist.md
  - docs/parity/evidence/phase-36-substantive-evidence-admission-and-exact-re-promotion/
must_haves:
  truths:
    - "Every affected row has explicit prerequisites and a digest of its exact admitted facts."
    - "Unsupported rows demote atomically; the matrix never requires four positive promotions."
    - "Attempt 31 is classified read-only and can only produce eligible claim facts or typed insufficiency."
    - "Typed insufficiency never authorizes or starts hardware."
  artifacts:
    - path: tools/parity/src/phase36_promotion.rs
      provides: "Claim-specific decision matrix and exact non-claim preservation"
    - path: docs/parity/evidence/phase-36-substantive-evidence-admission-and-exact-re-promotion/
      provides: "Shareable successor projection, matrix, verdict, and manifest only"
  key_links:
    - from: tools/parity/src/phase36_promotion.rs
      to: tools/parity/src/phase36_evidence/
      via: "separate substantive/runtime/effect claim prerequisites and fact digests"
    - from: tools/parity/src/operator_evidence/generation/
      to: docs/parity/checklist.md
      via: "validated staged exchange with byte-identical rollback and unrelated-row preservation"
---

<objective>
Implement claim-specific correction, atomic successor publication, and the safe Attempt 31 re-evaluation command.

Purpose: replace the invalid all-four generic promotion with exact per-claim evidence decisions while preserving history and every non-claim.
Output: typed matrix, atomic publisher, safe offline classifier, and either exact re-promotion or deterministic demotion/insufficiency.
</objective>

<execution_context>
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-CONTEXT.md
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-RESEARCH.md
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-VALIDATION.md
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-02-SUMMARY.md
</execution_context>

<threat-model>

| ID | Threat | Severity | Required mitigation and verification |
| --- | --- | --- | --- |
| T36-05 | Matrix, checklist, verdict, or manifest partially publishes and contradicts another artifact. | high | Complete staging validation, sync, atomic exchange, rollback, and failure injection at every boundary. |
| T36-03 | Generic root eligibility grants a claim without its substantive facts. | high | Closed per-claim prerequisite types and mandatory claim-fact digests. |
| T36-06 | Attempt 31 insufficiency starts a hardware fallback. | high | A terminal typed stop; structural/real-process denylist for detector, credentials, flash, serial, and network mutations. |
| T36-02 | Successor publication obscures or rewrites Phase 35 history. | high | Explicit supersession fingerprints, immutable prior generation, and one authoritative-current resolver. |

</threat-model>

<tasks>

<task type="auto" id="36-03-01">
  <name>Implement the claim-specific decision matrix and exact checklist projection</name>
  <read_first>
    - tools/parity/src/phase36_evidence.rs
    - tools/parity/src/phase36_evidence/substance.rs
    - tools/parity/src/phase36_evidence/runtime_identity.rs
    - tools/parity/src/phase36_evidence/effects.rs
    - tools/parity/src/phase35_promotion/types.rs
    - tools/parity/src/phase35_promotion/evaluator.rs
    - tools/parity/src/phase35_promotion/checklist.rs
    - docs/parity/checklist.md
    - .planning/v1.2-MILESTONE-AUDIT.md
  </read_first>
  <files>
    - tools/parity/src/phase36_promotion.rs
    - tools/parity/src/phase36_promotion/types.rs
    - tools/parity/src/phase36_promotion/evaluator.rs
    - tools/parity/src/phase36_promotion/checklist.rs
    - tools/parity/src/phase36_promotion/tests.rs
    - tools/parity/BUILD.bazel
  </files>
  <action>
Define `Phase36ClaimDecision` with `Promote`, `Demote`, `Preserve`, and `DoNotPromote`, plus a closed reason enum. Evaluate the four affected scopes independently:

- hostname durability requires confirmed storage, reload, exactly-once reboot, restoration, and cleanup facts;
- package/runtime identity requires `ValidatedObservedRuntimeIdentity` and exact-package join;
- coherent snapshot requires `ValidatedSensorSubstance` and `SubstantiveSnapshotJoin`;
- passive runtime health requires `ValidatedRuntimeHealthSubstance`;
- every passive-without-actuation positive claim requires `ValidatedIndependentEffectInterval`.

Every decision stores the digest of its exact admitted facts. Remove the Phase 35 invariant that exactly four positive promotions must exist. Preserve all excluded scopes and every unrelated checklist row byte-identically. Add a resolver that marks one Phase 36 generation authoritative while retaining explicit fingerprints of the historical Phase 35 generation and root. Reject absent, generic, reused-wrong-claim, or mismatched fact digests.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-parity phase36_promotion --all-features &amp;&amp; bazel test //tools/parity:phase36_promotion_tests &amp;&amp; just parity &amp;&amp; git diff --check</automated>
  </verify>
  <done>Each affected checklist row is independently supported, corrected, or preserved.</done>
  <acceptance_criteria>
    - Source contains `Promote`, `Demote`, `Preserve`, and `DoNotPromote`.
    - Tests exercise every claim with its own missing prerequisite and assert only that row changes.
    - A test proves zero, one, two, three, and four promotions are structurally valid when their prerequisites match.
    - A byte-identity test proves all unrelated checklist rows and exact non-claims remain unchanged.
    - Every positive decision contains a non-empty lowercase 64-hex claim-fact digest.
  </acceptance_criteria>
</task>

<task type="auto" id="36-03-02">
  <name>Publish successor decisions atomically with full rollback coverage</name>
  <read_first>
    - tools/parity/src/phase36_promotion.rs
    - tools/parity/src/operator_evidence/generation/phase35.rs
    - tools/parity/src/operator_evidence/generation/tests/promotion.rs
    - docs/parity/evidence-policy.md
    - scripts/redaction-exceptions.tsv
  </read_first>
  <files>
    - tools/parity/src/operator_evidence/generation/phase36.rs
    - tools/parity/src/operator_evidence/generation.rs
    - tools/parity/src/operator_evidence/generation/tests/phase36.rs
    - tools/parity/src/main.rs
    - docs/parity/evidence/phase-36-substantive-evidence-admission-and-exact-re-promotion/
    - docs/parity/checklist.md
  </files>
  <action>
Add a Phase 36 publisher that stages exactly a commit-redacted versioned typed fact projection, claim decision matrix, verdict, and manifest. The typed projection must include every non-secret substantive fact needed to reconstruct the sensor, health, runtime-identity, effect, and provenance decisions; it may use digests only for protected identities and source artifacts. It fingerprints the existing checklist rather than copying the historical checklist tree. Validate successor schema, typed-fact completeness, prior-root/generation fingerprints, current evaluator identity, exact claim-fact digests, redaction, destination ownership, and complete matrix before exchange. Sync files/directories, exchange the generation and checklist atomically, and restore both byte-identically on every pre/post-exchange injected failure.

Never copy protected Attempt 31 inputs or raw document bodies into the committed generation. The manifest must name the superseded Phase 35 generation and authoritative Phase 36 generation using digests only.
  </action>
  <verify>
    <automated>bazel test //tools/parity:phase36_promotion_tests //tools/parity:operator_evidence_generation_tests &amp;&amp; just verify-redaction &amp;&amp; just parity &amp;&amp; git diff --check</automated>
  </verify>
  <done>Corrected claim decisions and checklist changes are all-or-nothing and privacy-safe.</done>
  <acceptance_criteria>
    - Failure injection covers every staging, validation, sync, exchange, and post-exchange boundary.
    - Every failure leaves generation and checklist byte-identical to their pre-run state.
    - The committed generation contains only the versioned typed fact projection, matrix, verdict, and manifest.
    - Exhaustive one-field mutation tests prove the typed projection contains every field used by the evaluator and no decision depends on a fact absent from the projection.
    - `just verify-redaction` and `just parity` exit 0.
  </acceptance_criteria>
</task>

<task type="auto" id="36-03-03">
  <name>Classify immutable Attempt 31 and apply only its exact supported decisions</name>
  <read_first>
    - tools/parity/src/main.rs
    - tools/parity/src/phase36_evidence.rs
    - tools/parity/src/phase36_promotion.rs
    - scripts/phase36-evidence-test.sh
    - docs/parity/evidence/phase-35-detector-gated-correlated-evidence-and-exact-parity-promotion/manifest.json
    - docs/hardware/hardware-attempt-policy.md
    - .planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-HARDWARE-EVIDENCE.md
  </read_first>
  <files>
    - tools/parity/src/main.rs
    - scripts/phase36-evidence-test.sh
    - docs/parity/evidence/phase-36-substantive-evidence-admission-and-exact-re-promotion/
    - docs/parity/checklist.md
  </files>
  <action>
Add an explicit-path offline command that snapshots only the caller-supplied protected Attempt 31 companion artifacts, runs the three pure classifiers, builds the claim matrix, and publishes only through the Phase 36 atomic publisher. Do not search ignored roots or display their existence, names, paths, or values.

If any protected companion artifact is absent or not safely supplied, emit the exact per-claim insufficiency categories plus aggregate `immutable_artifacts_insufficient` and atomically demote/correct only unsupported public rows. Emit the aggregate only when one or more requirement-bearing component assessments are insufficient; never emit it for a fully sufficient root. Preserve the hostname decision only when its existing admitted facts remain valid. The command must stop after classification/public correction. It must contain no detector, board-info, credential, package build, flash, monitor, serial-control, target discovery, HTTP mutation, reboot, restoration, or hardware-attempt invocation. A future run requires a separate later Phase 36 plan after `immutable_artifacts_insufficient`; this task must not create that plan or broker.
  </action>
  <verify>
    <automated>bazel test //scripts:phase36_evidence_test //tools/parity:phase36_evidence_tests //tools/parity:phase36_promotion_tests &amp;&amp; just verify-redaction &amp;&amp; just parity &amp;&amp; git diff --check</automated>
  </verify>
  <done>Attempt 31 is exactly re-evaluated without rewriting private evidence or triggering hardware.</done>
  <acceptance_criteria>
    - An eligible synthetic root publishes only the exact supported rows and claim-fact digests.
    - Missing sensor, health, runtime-identity, and effect inputs each produce their exact typed insufficiency and correction.
    - Each missing component also produces aggregate `immutable_artifacts_insufficient`; a fully sufficient synthetic root does not.
    - A structural test rejects any effectful hardware or target-discovery token in the command path.
    - Existing Phase 35 evidence bytes and root digest remain unchanged.
    - Public output contains no protected path/value and passes `just verify-redaction`.
  </acceptance_criteria>
</task>

</tasks>

<verification>

- Run all task commands and full Rust pre-commit checks.
- Run `just test`, `just parity`, `just verify-reference`, and `just verify-redaction`.
- Verify the Phase 35 admitted directory and root digest are unchanged.
- Review the checklist diff for only affected rows and byte-identical exact non-claims.
- Confirm no hardware, credentials, target discovery, or archived-lineage work occurred.

</verification>

<success_criteria>

- All five Phase 36 requirements have exact claim-specific automated evidence.
- Unsupported historical claims are corrected atomically.
- Attempt 31 remains immutable and is never used beyond what its independent facts prove.
- Hardware remains unauthorized by this plan.

</success_criteria>

<output>
After completion, create `36-03-SUMMARY.md`; use atomic task commits and do not push.
</output>
