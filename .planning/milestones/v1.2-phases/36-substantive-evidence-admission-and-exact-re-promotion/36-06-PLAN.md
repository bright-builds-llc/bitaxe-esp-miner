---
phase: 36-substantive-evidence-admission-and-exact-re-promotion
plan: "06"
type: execute
wave: 6
depends_on:
  - "36-05"
autonomous: true
gap_closure: true
phase_lifecycle_id: 36-2026-07-23T15-20-53
lifecycle_mode: yolo
generated_by: gsd-plan-phase
generated_at: "2026-07-24T20:02:44Z"
requirements:
  - SYS-02
  - EVD-11
  - EVD-12
  - EVD-14
files_modified:
  - target/private-evidence/phase36-gap/
  - .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-GAP-HARDWARE-CANDIDATE.json
must_haves:
  truths:
    - "Exactly one fresh exact-current-source Phase 36 hardware command reaches one detector-admitted Ultra 205 over the provided USB and barrel-power connectors."
    - "The attempt uses the exact admitted board-205 package and independently observes runtime source, reference, ELF, package, boot/session, and same-device identity."
    - "System-info, live WebSocket, retained correlation/runtime-health, and substantive sensor facts come from the same fresh boot session and operator-snapshot revision."
    - "One broker-owned append-only ledger covers the complete bounded effect interval, restoration, and cleanup without any unbrokered effect."
    - "The private root is sealed and immutable, while a separate commit-redacted projection contains only closed facts, categories, and public provenance."
    - "Any detector, board, target, credential, recovery, evidence, cleanup, or repeated-boundary failure stops without a blind retry."
  artifacts:
    - path: target/private-evidence/phase36-gap/
      provides: "Mode-0700 ignored parent containing one sealed fresh attempt child and distinct mode-0600 wrapper logs"
    - path: .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-GAP-HARDWARE-CANDIDATE.json
      provides: "Commit-redacted candidate projection and sealed private-root digest"
  key_links:
    - from: scripts/phase36-substantive-evidence.sh
      to: just detect-ultra205
      via: "exactly one broker-owned detector invocation before credential access or device effects"
      pattern: "detect-ultra205"
    - from: target/private-evidence/phase36-gap/
      to: .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-GAP-HARDWARE-CANDIDATE.json
      via: "private classification first, distinct commit-redacted derivation second, unchanged private digest"
    - from: .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-GAP-HARDWARE-CANDIDATE.json
      to: tools/parity/src/phase36_evidence/
      via: "offline Plan 36-07 admission only; this plan does not publish canonical evidence"
---

<objective>
Run one detector-gated, exact-current-source, USB-only Ultra 205 attempt that captures the missing substantive, runtime-identity, and independent-effect evidence.

Purpose: obtain one truthful fresh candidate after the broker and all software boundaries are verified, without retrying unchanged conditions or widening hardware authority.
Output: one sealed private attempt root plus a distinct commit-redacted candidate projection; no canonical promotion or reconciliation.
</objective>

<execution_context>
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-CONTEXT.md
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-RESEARCH.md
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-VERIFICATION.md
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-05-SUMMARY.md
@docs/parity/evidence-policy.md
@docs/hardware/hardware-attempt-policy.md
@docs/hardware/esp-device-session.md
@AGENTS.md
</execution_context>

<context>
This is the single hardware-acquisition plan authorized by the Phase 36 gap closure. It uses only the device's provided USB and barrel-power connectors. Direct UART, pins, pads, headers, GPIO, test points, probes, jumpers, solder, injected signals, and every archived Phase 28.1.1 path are prohibited.

The immutable Phase 35 root digest remains `0401e7b485df2d1ccfc67e63845f98b6217816a184901bf0595d03af3219757d`. Do not modify Attempt 31 or any Phase 35 byte. Plan 36-04 remains paused and unchanged.
</context>

<threat-model>

## ASVS Level

OWASP ASVS Level 1. All high-severity threats are blocking.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Exact source/package to detector | Software provenance must be frozen before any target, credential, USB, or device effect. |
| Detector to private credentials | Exactly one board-205 capability must exist before an opaque mode-0600 Wi-Fi credential path is admitted. |
| Broker to Ultra 205 | Only closed ledgered operations cross to the device over provided USB or same-session read-only application transports. |
| Monitor to DEVICE_URL | One origin may be derived only from the fresh corresponding detector and monitor session and remains broker memory-only. |
| Private root to public candidate | Private classifiers consume immutable secret-sanitized inputs before a distinct commit-redacted candidate is written. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-36-06-01 | Spoofing | Detector/board capability | mitigate | Run `just detect-ultra205` exactly once inside the broker; require exactly one candidate and successful `espflash board-info --chip esp32s3 --non-interactive`, board `205`, and same admitted physical-device identity. |
| T-36-06-02 | Tampering | Exact package and private root | mitigate | Bind clean current HEAD, reference, manifest, ELF, factory image, executable, package, target, and evaluator digests before detector; hash and seal the attempt child after cleanup. |
| T-36-06-03 | Repudiation | Hardware effect interval | mitigate | Broker appends/fyncs typed authorization and result/closure records for every operation and preserves the first failure through restoration and cleanup. |
| T-36-06-04 | Information Disclosure | Credentials, DEVICE_URL, USB identity, responses | mitigate | Credential path is opaque and 0600; NeverPersistRaw remains memory-only; ProtectedOperational stays in 0600 files below the 0700 ignored root; terminal/candidate output contains only categories/digests. |
| T-36-06-05 | Denial of Service | Flash, monitor, API/WS capture | mitigate | Use bounded repo-owned commands, at least 360-second capture windows, at least 420-second shell wall clock, process-group cleanup, and a typed recovery path. |
| T-36-06-06 | Elevation of Privilege | Hardware authority | mitigate | Closed broker effects, board-205 target lock, USB/barrel-only rule, and explicit denial of control, self-test, watchdog, mining, credentials, OTA, other boards, UART, and pin/probe paths. |

</threat-model>

<tasks>

<task type="auto" id="36-06-01">
  <name>Freeze exact-current-source package and pass every no-hardware preflight gate</name>
  <read_first>
    - AGENTS.md
    - .codex/tasks/lessons.md
    - .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-05-SUMMARY.md
    - scripts/phase36-substantive-evidence.sh
    - scripts/phase36-substantive-evidence-test.sh
    - tools/parity/src/phase36_broker.rs
    - docs/parity/evidence-policy.md
    - docs/hardware/hardware-attempt-policy.md
    - docs/hardware/esp-device-session.md
    - .gitignore
  </read_first>
  <files>
    - target/private-evidence/phase36-gap/
  </files>
  <action>
Per D-01 through D-03 and D-27, run the complete fresh software gate from Plan 36-05, the Rust pre-commit sequence, `just test`, `just parity`, `just verify-reference`, `just verify-redaction`, and the repo-owned Phase 36 `preflight` mode. Require a clean exact current `HEAD`, the expected pinned reference, `xtensa-esp32s3-espidf`, board `205`, BM1366 package metadata, exact firmware ELF/application/factory/executable/manifest/package relationships, and evaluator identity closure. Freeze their digests into a single-use preflight capability.

Prepare `target/private-evidence/phase36-gap/` as mode 0700 only after `git check-ignore -q target/private-evidence/phase36-gap` succeeds. Select a fresh ordinal whose supervisor-owned child does not exist. Immediately before the later hardware launch, assert that child is still absent and create distinct mode-0600 wrapper stdout/stderr sibling files under the parent, never below or through the intended child.

Do not inspect credential metadata or contents in preflight; credential-path validation remains after detector admission. Do not admit any pool credential. Define the recovery contract before hardware: the intended terminal device state is the exact admitted package with no configuration mutation; a typed partial-flash failure may invoke at most one separately ledgered same-image recovery flash solely to restore bootability, never to collect eligible evidence or convert the attempt to success. All other failures perform cleanup, seal non-promotion, and stop.

This task must invoke zero detector, board-info, USB, serial, credential-content, DEVICE_URL, HTTP/WebSocket, flash, monitor, reboot, or hardware operations.
  </action>
  <verify>
    <automated>just phase36-substantive-evidence mode=preflight board=205 capture-timeout-seconds=360 private-parent=target/private-evidence/phase36-gap attempt-handle-file=target/private-evidence/phase36-gap/attempt.handle candidate-output=.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-GAP-HARDWARE-CANDIDATE.json &amp;&amp; bazel test --nocache_test_results //tools/parity:phase36_broker_tests //tools/parity:phase36_evidence_tests //scripts:phase36_substantive_evidence_test &amp;&amp; just verify-reference &amp;&amp; just verify-redaction &amp;&amp; git diff --check</automated>
  </verify>
  <done>A single-use exact-current-source preflight capability and fresh protected parent are ready, with zero hardware or credential-content access.</done>
  <acceptance_criteria>
    - Preflight reports only a closed success category and public digests.
    - Current HEAD, reference, target, board, ELF, factory, executable, manifest, package, and evaluator identities agree.
    - `git check-ignore -q target/private-evidence/phase36-gap` exits 0 and the parent mode is 0700.
    - The selected attempt child is absent and wrapper stdout/stderr siblings are separate mode-0600 files.
    - Preflight performs no credential metadata or content access, and credential contents never reach a file, terminal, command output, digest, fixture, or candidate.
    - Preflight runs zero detector, board-info, USB, serial, HTTP, WebSocket, flash, monitor, reboot, or hardware operation.
  </acceptance_criteria>
</task>

<task type="auto" id="36-06-02">
  <name>Execute and seal one exact broker-owned Ultra 205 evidence attempt</name>
  <read_first>
    - .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-05-SUMMARY.md
    - scripts/phase36-substantive-evidence.sh
    - tools/parity/src/phase36_broker.rs
    - tools/parity/src/phase36_evidence/capture.rs
    - docs/hardware/esp-device-session.md
    - docs/hardware/hardware-attempt-policy.md
    - docs/parity/evidence-policy.md
    - AGENTS.md
  </read_first>
  <files>
    - target/private-evidence/phase36-gap/
    - .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-GAP-HARDWARE-CANDIDATE.json
  </files>
  <action>
Per D-04 through D-18 and the explicitly later-plan authority required by D-27, invoke the repo-owned hardware command exactly once:

`just phase36-substantive-evidence mode=hardware board=205 capture-timeout-seconds=360 private-parent=target/private-evidence/phase36-gap attempt-handle-file=target/private-evidence/phase36-gap/attempt.handle candidate-output=.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-GAP-HARDWARE-CANDIDATE.json wifi-credentials=wifi-credentials.json`

The command must consume the single-use preflight capability and perform all device-capable work through one fresh broker process. The broker must:

1. Start its append-only ledger before any effect.
2. Invoke `just detect-ultra205` exactly once, require exactly one likely ESP USB serial port, successful ESP32-S3 board-info, board `205`, and the admitted physical device; stop before credential access or device effects on zero, multiple, wrong-board, wrong-target, or board-info failure.
3. Only after detector success, validate that the opaque Wi-Fi credential input is an ignored, owner-controlled, non-symlink regular file with mode 0600; then open it only for the minimum in-memory Wi-Fi operation. Never print, persist, digest, summarize, or copy its contents, and reject every pool credential.
4. Flash only the exact admitted board-205 package through the repo-owned command and use at least 360 seconds for every flash-monitor/monitor capture with a shell/tool wall clock of at least 420 seconds.
5. Use the qualified receive-only runtime observer for authoritative application serial facts. Derive DEVICE_URL only in broker memory from the same fresh detector plus corresponding monitor session when exactly one origin-only URL exists; stop on zero, multiple, stale, malformed, or redacted candidates.
6. Within that same boot/session, capture the exact API system-info document, live-WebSocket document, and retained correlation/runtime-health facts. Require identical opaque boot session and operator-snapshot revision across all three; validate actual power current/voltage/wattage, independent temperature and tachometer state/value/sequence/stamp/reason fields, supervisor/checkpoint/health/staleness facts, and unproved watchdog participation.
7. Independently observe runtime source commit, pinned reference, application ELF digest, boot/session identity, same physical device, board 205/BM1366, and running exact package; package fields alone have zero observational authority.
8. Ledger every authorized, invoked, completed/failed, and closed operation. Reject unknown, missing, duplicate, reordered, unclosed, or unbrokered effects. No active control, self-test, watchdog intervention, mining, pool, credential inspection/promotion, OTA/recovery update, other-board, direct UART/pin/probe, or archived Phase 28.1.1 effect is representable.
9. Restore the declared safe terminal state and clean up all children, process groups, sockets, serial holders, file descriptors, and temporary capabilities. A same-image recovery flash is permitted only for the predeclared typed partial-flash recovery boundary and can never make the evidence eligible.
10. Preserve the earliest failure, seal the private child immutable as `SealedEligible` or `SealedNonPromotion`, run private classifiers on immutable secret-sanitized inputs, and derive a distinct commit-redacted candidate without changing the private digest.

On detector, board, target, credential, recovery, evidence, privacy, cleanup, or identity failure, record the closed authoritative boundary signature and stop. Do not rerun this command, reuse the root, change only timing/ordinal/category, or attempt a blind recovery. Any future attempt would require a distinct diagnosed boundary, a real-boundary regression, a verified fix or authorized measured remediation, a fresh plan/ordinal/package/preflight, and the progress-gated policy. A repeated post-fix signature selects `stop_repeated_boundary`.
  </action>
  <verify>
    <automated>just phase36-substantive-evidence mode=inspect-candidate board=205 private-parent=target/private-evidence/phase36-gap attempt-handle-file=target/private-evidence/phase36-gap/attempt.handle candidate-output=.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-GAP-HARDWARE-CANDIDATE.json &amp;&amp; just verify-redaction &amp;&amp; just verify-reference &amp;&amp; git diff --check</automated>
  </verify>
  <done>One and only one attempt is sealed with complete cleanup and a separate redacted candidate, or it stops at an exact typed blocker without retry.</done>
  <acceptance_criteria>
    - The candidate records exactly one hardware-command invocation and exactly one `just detect-ultra205` invocation.
    - Detector facts prove exactly one ESP32-S3 board-205 candidate before credential access or device effects.
    - Every flash/monitor capture records a duration bound of at least 360 seconds and the command wall clock is at least 420 seconds.
    - API, WebSocket, retained, substantive sensor, runtime-health, and runtime-identity facts share one exact boot/session and operator-snapshot revision.
    - The runtime identity is independently observed and joined to exact source, reference, ELF, manifest, executable, factory image, and package digests.
    - The broker ledger covers the complete interval and admits no prohibited or bypassed operation.
    - Restoration and cleanup prove no live child, process-group descendant, socket, serial holder, file descriptor, or capability remains.
    - The private child and wrapper siblings have correct 0700/0600 ownership, the child is sealed/non-reusable, and the commit-redacted candidate contains no ProtectedOperational or NeverPersistRaw value.
    - Attempt 31 and all Phase 35 bytes remain unchanged, including root digest `0401e7b485df2d1ccfc67e63845f98b6217816a184901bf0595d03af3219757d`.
    - Failure produces a closed stop outcome and no second hardware invocation.
  </acceptance_criteria>
</task>

</tasks>

<verification>

- Task 36-06-01 must pass before Task 36-06-02 may consume hardware authority.
- Run the hardware command exactly once; all later verification is offline and must not invoke detector, USB, serial, credentials, flash, monitor, HTTP/WebSocket, reboot, or hardware.
- Validate private/candidate digest separation, permissions, sealing, earliest-failure precedence, process cleanup, and effect-ledger completeness.
- Run `just verify-redaction`, `just verify-reference`, `git diff --check`, and compare the Phase 35 evidence directory against HEAD.
- Do not run `just parity` against a candidate as though it were canonical admission; Plan 36-07 owns classification and atomic publication.

</verification>

<success_criteria>

- One fresh detector-gated exact-current-source Ultra 205 attempt is fully accounted for.
- Exact substantive, runtime-health, runtime-identity, and independent-effect facts are captured from one session or an exact typed blocker is sealed.
- Private and public artifacts obey the evidence policy and cleanup is complete.
- There is no blind retry, promotion, canonical reconciliation, direct UART/pin work, archived-lineage work, or effect-scope expansion.

</success_criteria>

<output>
After completion, create `36-06-SUMMARY.md`; use atomic task commits only for the commit-redacted candidate and summary, do not commit private artifacts, do not push, and do not resume Plan 36-04.
</output>
