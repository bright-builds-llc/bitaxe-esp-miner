---
phase: 36-substantive-evidence-admission-and-exact-re-promotion
plan: "02"
type: execute
wave: 2
depends_on:
  - "36-01"
autonomous: true
phase_lifecycle_id: 36-2026-07-23T15-20-53
lifecycle_mode: yolo
generated_by: gsd-plan-phase
generated_at: "2026-07-23T15:45:00Z"
requirements:
  - SYS-02
  - EVD-11
  - EVD-12
  - EVD-14
  - EVD-15
files_modified:
  - tools/parity/src/phase36_evidence/
  - tools/parity/src/operator_snapshot_evidence.rs
  - tools/device-session/src/model.rs
  - tools/parity/src/main.rs
  - tools/parity/fixtures/phase36/
  - tools/parity/BUILD.bazel
must_haves:
  truths:
    - "Sensor and runtime-health claims are admitted from typed substantive facts joined to the exact operator snapshot, never from identity membership alone."
    - "Runtime identity is replayed from independently observed device-session evidence and joined to the exact package."
    - "No-actuation is independently complete or typed insufficient; the supervisor Boolean has zero authority."
  artifacts:
    - path: tools/parity/src/phase36_evidence/substance.rs
      provides: "Typed sensor/runtime-health facts and provenance join"
    - path: tools/parity/src/phase36_evidence/runtime_identity.rs
      provides: "Observed device-session replay and exact-package join"
    - path: tools/parity/src/phase36_evidence/effects.rs
      provides: "Independent legacy effect completeness and insufficiency reducer"
  key_links:
    - from: tools/parity/src/phase36_evidence/substance.rs
      to: tools/parity/src/operator_snapshot_evidence.rs
      via: "composition around the unchanged identity/revision/retained-membership validator"
    - from: tools/parity/src/phase36_evidence/runtime_identity.rs
      to: tools/device-session/src/model.rs
      via: "replay of observed Boot B and same-device facts"
---

<objective>
Implement the substantive snapshot/health, observed runtime-identity, and independent effect-completeness admission core.

Purpose: close the three production-path gaps found by the v1.2 milestone audit without inferring missing evidence or touching hardware.
Output: pure typed validators, mutation regressions, and safe explicit-root classification.
</objective>

<execution_context>
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-CONTEXT.md
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-RESEARCH.md
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-VALIDATION.md
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-01-SUMMARY.md
</execution_context>

<threat-model>

| ID | Threat | Severity | Required mitigation and verification |
| --- | --- | --- | --- |
| T36-03 | Package fields or supervisor booleans masquerade as independent runtime/no-actuation observations. | high | Separate domain types, provenance-bearing replay, and exact rejection fixtures. |
| T36-01 | Substantive document parsing leaks protected field values in errors. | high | Closed error categories and canary-based output tests. |
| T36-02 | Mixed sessions, revisions, or evidence sources are joined into an eligible claim. | high | Parse-time provenance join with illegal mixed states unrepresentable. |

</threat-model>

<tasks>

<task type="auto" id="36-02-01">
  <name>Admit substantive sensor and runtime-health facts with typed provenance</name>
  <read_first>
    - tools/parity/src/phase36_evidence/contract.rs
    - tools/parity/src/operator_snapshot_evidence.rs
    - crates/bitaxe-safety/src/observation.rs
    - crates/bitaxe-safety/src/sensor_acquisition.rs
    - crates/bitaxe-api/src/runtime_health.rs
    - crates/bitaxe-api/src/wire.rs
    - .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-CONTEXT.md
    - .planning/v1.2-MILESTONE-AUDIT.md
  </read_first>
  <files>
    - tools/parity/src/phase36_evidence/substance.rs
    - tools/parity/src/phase36_evidence/tests/substance.rs
    - tools/parity/src/operator_snapshot_evidence.rs
    - tools/parity/fixtures/phase36/
  </files>
  <action>
Keep `validate_operator_snapshot_documents` behavior unchanged. Compose a Phase 36 parser that reads the exact API/WebSocket/retained documents and constructs:

- `ObservationState` with only `fresh`, `stale`, `unavailable`, `fault`;
- atomic INA260 power facts for current, voltage, wattage;
- independent temperature and tachometer facts;
- value presence rules, producer sequence, monotonic acquisition stamp, and typed failure reason;
- runtime-health lifecycle state, supervisor availability, checkpoint category, sequence, age, health/staleness, and independent task-watchdog availability;
- `SubstantiveSnapshotJoin` binding every fact set to one exact boot session and operator snapshot revision.

Reject compatibility zero as freshness, fresh without a stamp, unavailable/fault with manufactured current values, reused unrelated stamps, mixed boot sessions/revisions, missing retained substance, invalid checkpoint chronology, and watchdog claims derived from supervisor visibility. Produce separate `ValidatedSensorSubstance` and `ValidatedRuntimeHealthSubstance` types and claim-fact digests.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-parity phase36_substance --all-features &amp;&amp; bazel test //tools/parity:phase36_evidence_tests &amp;&amp; git diff --check</automated>
  </verify>
  <done>Snapshot and health eligibility requires every substantive field and its exact provenance.</done>
  <acceptance_criteria>
    - The source contains `ValidatedSensorSubstance`, `ValidatedRuntimeHealthSubstance`, and `SubstantiveSnapshotJoin`.
    - Mutation tests change each sensor state/value/stamp/reason and each health/checkpoint/watchdog field independently and assert rejection or a changed claim digest.
    - A regression proves the old identity-only Phase 35 document shape yields typed insufficiency rather than eligibility.
    - Existing operator snapshot tests remain byte-for-byte behavior compatible and pass.
  </acceptance_criteria>
</task>

<task type="auto" id="36-02-02">
  <name>Replay independently observed runtime identity and exact package correlation</name>
  <read_first>
    - tools/parity/src/phase36_evidence/contract.rs
    - tools/device-session/src/model.rs
    - tools/device-session/src/reducer.rs
    - tools/parity/src/phase35_evidence/contract.rs
    - scripts/phase35-correlated-evidence-root.sh
    - docs/hardware/esp-device-session.md
  </read_first>
  <files>
    - tools/parity/src/phase36_evidence/runtime_identity.rs
    - tools/parity/src/phase36_evidence/tests/runtime_identity.rs
    - tools/device-session/src/model.rs
    - tools/parity/fixtures/phase36/
  </files>
  <action>
Reuse the device-session observation/result vocabulary to replay an explicitly supplied immutable request, event ledger, private result, and public projection. Construct `ValidatedObservedRuntimeIdentity` only when the replay proves same physical device, Boot B source commit, reference commit, `app_elf_sha256`, boot/session identity, and exact package correlation. Bind manifest, executable, factory image, firmware ELF, and package digests.

Support a narrower paired terminal-result/public-projection input only when its schema independently proves each admitted field; it must never inherit missing event authority. Reject package-derived `runtime-identity.json` as an observation, derived booleans without observations, public/private disagreement, different physical device, source/reference/ELF drift, and missing ledger steps. Render only closed categories and digests.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-parity phase36_runtime_identity --all-features &amp;&amp; cargo test -p bitaxe-device-session --all-features &amp;&amp; bazel test //tools/parity:phase36_evidence_tests</automated>
  </verify>
  <done>SYS-02/EVD-11 can be supported only by replayed observed runtime identity.</done>
  <acceptance_criteria>
    - The source contains `ValidatedObservedRuntimeIdentity`.
    - Tests prove manifest/package fields alone produce `runtime_identity_observation_insufficient`.
    - Tests mutate source, reference, ELF, device identity, boot session, and projection agreement one at a time.
    - Errors and projections contain no protected observation values.
  </acceptance_criteria>
</task>

<task type="auto" id="36-02-03">
  <name>Classify independent no-actuation coverage for immutable legacy evidence</name>
  <read_first>
    - tools/parity/src/phase36_evidence/contract.rs
    - scripts/phase35-correlated-evidence-effects.sh
    - scripts/phase35-correlated-evidence-document.sh
    - tools/parity/src/phase35_evidence.rs
    - docs/hardware/hardware-attempt-policy.md
    - docs/parity/evidence-policy.md
  </read_first>
  <files>
    - tools/parity/src/phase36_evidence/effects.rs
    - tools/parity/src/phase36_evidence/tests/effects.rs
    - tools/parity/fixtures/phase36/
    - tools/parity/src/main.rs
  </files>
  <action>
Define a closed legacy effect vocabulary for package probe/flash, passive observation, hostname read, hostname PATCH, hostname restoration, exactly-once approved reboot, and cleanup. Parse only explicitly supplied immutable artifacts and construct `ValidatedIndependentEffectInterval` when an independently owned record covers authorization, invocation, result, order, and closure for every effect-capable path over the complete bounded interval.

Treat `no_actuation_verified=true`, its digest, supervisor chronology, source guards, and cleanup alone as non-authoritative. Reject unknown/missing/duplicate/out-of-order/unclosed effects, direct unledgered command paths, ambiguous ownership, incomplete interval bounds, and any prohibited active-control/self-test/watchdog/mining/credential/OTA/other-board category. When legacy artifacts do not prove completeness, emit exactly `independent_effect_observation_insufficient`; do not search, discover, or invoke hardware.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-parity phase36_effects --all-features &amp;&amp; bazel test //tools/parity:phase36_evidence_tests //scripts:phase36_evidence_test &amp;&amp; just verify-redaction</automated>
  </verify>
  <done>EVD-14 no-actuation is independently admitted or deterministically insufficient.</done>
  <acceptance_criteria>
    - The source contains `ValidatedIndependentEffectInterval`.
    - The existing supervisor Boolean is rejected in a named regression.
    - Completeness tests cover every allowed effect and every missing/duplicate/out-of-order/unclosed case.
    - The insufficiency path performs zero detector, credential, network mutation, flash, monitor, serial-control, or hardware calls.
  </acceptance_criteria>
</task>

</tasks>

<verification>

- Run every task command and the full Phase 36 Bazel suite.
- Run the Rust pre-commit sequence in order.
- Run `just verify-redaction`, `just parity`, `just verify-reference`, and `git diff --check`.
- Confirm no protected values, Phase 35 mutations, or hardware authorization were introduced.

</verification>

<success_criteria>

- SYS-02/EVD-11 use independent runtime observation.
- EVD-12 validates the actual sensor and health substance with typed provenance.
- EVD-14 no longer accepts self-attestation.
- EVD-15 receives separate claim-fact types, not generic eligibility.

</success_criteria>

<output>
After completion, create `36-02-SUMMARY.md`; use atomic task commits and do not push.
</output>
