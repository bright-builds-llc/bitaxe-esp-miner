---
phase: 35-detector-gated-correlated-evidence-and-exact-parity-promotion
plan: "02"
type: execute
wave: 2
depends_on:
  - "35-01"
autonomous: true
phase_lifecycle_id: 35-2026-07-17T17-00-37
lifecycle_mode: yolo
generated_by: gsd-plan-phase
generated_at: "2026-07-17T20:31:00Z"
requirements:
  - CFG-12
  - EVD-10
  - EVD-11
  - EVD-12
  - EVD-13
  - EVD-14
files_modified:
  - scripts/phase35-correlated-evidence.sh
  - scripts/phase35-correlated-evidence-test.sh
  - scripts/phase35-correlated-evidence-fixture.sh
  - scripts/BUILD.bazel
  - justfile
must_haves:
  truths:
    - "The supervisor exits before target, credential, flash, reset, monitor, HTTP, or promotion work unless the detector proves exactly one board-205 candidate."
    - "One exact-current-package run records Boot A, a storage-confirmed PATCH/readback, one approved normal reboot, same-board Boot B, restoration, and cleanup in strict order."
    - "Every failure after mutation attempts and proves restoration; every failure seals the root as non-promotable and retries require a new root."
    - "Raw artifacts stay mode-0600 under a mode-0700 gitignored root and shareable output contains only allowed redacted fields."
  artifacts:
    - path: scripts/phase35-correlated-evidence.sh
      provides: "Thin detector/package/capture/reboot/restoration/cleanup supervisor"
    - path: scripts/phase35-correlated-evidence-test.sh
      provides: "Simulation and failure-injection contract"
    - path: justfile
      provides: "Human command surface `just phase35-evidence`"
  key_links:
    - from: scripts/phase35-correlated-evidence.sh
      to: tools/flash/src/package_admission.rs
      via: "Gate 1 emits the typed manifest-v3/executable/factory ExactPackageCapability before detector or effects"
    - from: scripts/phase35-correlated-evidence.sh
      to: scripts/detect-ultra205.sh
      via: "mandatory first capability gate"
    - from: scripts/phase35-correlated-evidence.sh
      to: scripts/phase33-confirmed-settings-durability.sh
      via: "reused package, passive-monitor, approved reboot, restoration, and cleanup semantics"
    - from: scripts/phase35-correlated-evidence.sh
      to: tools/parity/src/phase35_evidence.rs
      via: "closed root is validated before it can reach promotion"
---

<objective>
Build the thin imperative supervisor and its complete simulated failure matrix.

Purpose: satisfy CFG-12, EVD-10, EVD-13, and the capture/cleanup portions of EVD-11/12/14 without duplicating Phase 33 transport semantics or touching hardware during implementation.
Output: one repo-owned command, shell supervisor, Bazel target, and deterministic fixture harness.
</objective>

<execution_context>
@.planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-CONTEXT.md
@.planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-RESEARCH.md
@.planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-VALIDATION.md
</execution_context>

<threat-model>

| ID | Threat | Severity | Required mitigation and verification |
| --- | --- | --- | --- |
| T-35-09 | Target or credential inputs are touched before the detector proves one board 205. | critical | Put detector/board-info in the first executable gate; fixture canaries fail if later commands run after zero/multiple/wrong-board outcomes. |
| T-35-10 | Monitor/reboot behavior resets unexpectedly or leaks a serial holder. | high | Use the complete passive monitor flags, record/reap PID/PGID/descendants, and require no unexpected holder before reuse and close. |
| T-35-11 | Mutation succeeds but failure skips restoration or cleanup. | critical | Install fail-fast traps after the first mutation; restoration readback and cleanup are mandatory root-close facts and are injected at every failure boundary. |
| T-35-12 | Raw targets, identities, hostname, credentials, or local process/device values reach committed evidence. | critical | Keep raw artifacts protected and feed only the typed redacted projection to shareable staging; run forbidden-key/value canaries. |

</threat-model>

<tasks>

<task type="auto" id="35-02-01">
  <name>Implement the detector-gated two-epoch shell supervisor</name>
  <read_first>
    - AGENTS.md
    - scripts/detect-ultra205.sh
    - scripts/phase33-confirmed-settings-durability.sh
    - scripts/serial-session-trace.sh
    - scripts/package-firmware.sh
    - tools/parity/src/phase35_evidence.rs
    - scripts/BUILD.bazel
    - justfile
  </read_first>
  <files>
    - scripts/phase35-correlated-evidence.sh
    - scripts/BUILD.bazel
    - justfile
  </files>
  <action>
Create `phase35-correlated-evidence.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. Keep it an imperative supervisor; reuse/extract Phase 33 helpers where needed instead of reimplementing serial ownership or reboot semantics.

The exact gate order is:

1. establish a gitignored mode-0700 content-addressed run root and mode-0600 files, compute its root-contract digest, and initialize an append-only event chain whose first predecessor is that digest;
2. run pure Gate 1 through `tools/flash/src/package_admission.rs`: prove clean/current source commit, pinned clean reference commit, manifest schema `manifest-v3`, exact manifest digest, immutable executable-image digest, frozen factory-image digest, complete package digest, package/runtime-identity digest, and lifecycle ID `35-2026-07-17T17-00-37`; emit and bind one typed `ExactPackageCapability`;
3. invoke the sole reset-capable `just detect-ultra205` exactly once, accept only exactly one `board=205` candidate with successful `espflash board-info --chip esp32s3 --non-interactive`, and emit one typed `DetectorRunCapability` bound to the run ID and stable physical-identity digest;
4. only then accept ignored `wifi-credentials.json` and the exact single origin derived from fresh same-session repo-owned monitor output;
5. flash the locked package and capture Boot A using at least 360 seconds;
6. collect pre-PATCH coherent projection, issue the hostname PATCH, require the committed response, require storage-confirmed immediate readback, and collect the matching Boot A coherent projection;
7. issue exactly one existing approved normal reboot only after the response/readback, reap all holders, reacquire the same physical-identity digest, and capture Boot B;
8. require `software_cpu`, ordinal `N+1`, matching storage-confirmed hostname digest, and a coherent Boot B projection;
9. restore the original hostname, require storage-confirmed restoration readback, reap the full process tree, prove no unexpected serial holder, close inventory, and append typed current-head, reference-cleanliness, exact-package/runtime-identity, lifecycle, no-actuation, restoration, cleanup, redaction, and chronology facts;
10. recompute the ordered event sequence and every predecessor-event digest, then invoke `validate-phase35-evidence` with the exact-package capability, detector capability, root-contract digest, detector/target-lock digests, frozen factory-image digest, boot epochs, and complete admission facts;
11. emit only a redacted result/root digest for downstream admission.

Use the complete retained-runtime monitor contract `--chip esp32s3 --before no-reset-no-sync --after no-reset --no-reset --non-interactive`. Set defaults/floors `capture-timeout-seconds=360` and tool wall clock `420`; reject smaller explicit values.

Do not read credential file contents in shell except by passing the path to the existing repo-owned flash command. Never echo commands containing credential paths/values or raw origin/target/hostname. A zero-byte capture, stale/multiple/malformed origin, detector drift, identity drift, unexpected reset, restoration failure, or cleanup failure seals the root `non_promotion` and exits nonzero. No retry may mutate the same root.

The script owns the detector invocation. Callers must not run `just detect-ultra205` separately for the attempt. Add a `preflight-only=true` mode that executes Gate 1, returns the redacted `ExactPackageCapability` digest/current-HEAD equality result, and exits before Gate 2; this mode must never invoke the detector.

Add a Bazel `sh_binary` named `phase35_correlated_evidence` and a just recipe:
`phase35-evidence *args: bazel run //scripts:phase35_correlated_evidence -- {{args}}`.
  </action>
  <verify>
    <automated>bash -n scripts/phase35-correlated-evidence.sh &amp;&amp; bazel build //scripts:phase35_correlated_evidence &amp;&amp; git diff --check</automated>
  </verify>
  <done>The command surface exists and every effect is structurally unreachable before current-head/package and detector capability gates.</done>
  <acceptance_criteria>
    - The script starts with the required shebang and `set -euo pipefail`.
    - Static ordering tests can identify detector success before any credential, flash, reset, monitor, target, HTTP, or promotion function call.
    - Tests prove exactly one detector invocation exists in a full attempt and `preflight-only=true` invokes zero detectors.
    - Gate 1 records `manifest-v3`, executable-image, factory-image, package, runtime-identity, and capability digests and proves source/package current-HEAD equality.
    - Every event has a monotonic sequence and exact predecessor digest rooted at the root-contract digest.
    - The script rejects capture timeouts below 360 seconds and documents a 420-second minimum caller wall clock.
    - The passive monitor invocation contains all six required contract arguments.
    - Root and raw file permissions are explicitly set to 0700 and 0600.
    - The just recipe routes only through `//scripts:phase35_correlated_evidence`.
  </acceptance_criteria>
</task>

<task type="auto" id="35-02-02">
  <name>Add simulation and failure-injection coverage for every supervisor boundary</name>
  <read_first>
    - scripts/phase35-correlated-evidence.sh
    - scripts/phase33-confirmed-settings-durability-test.sh
    - scripts/phase33-confirmed-settings-durability-fixture.sh
    - .planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-VALIDATION.md
    - scripts/BUILD.bazel
  </read_first>
  <files>
    - scripts/phase35-correlated-evidence-test.sh
    - scripts/phase35-correlated-evidence-fixture.sh
    - scripts/BUILD.bazel
  </files>
  <action>
Create a hermetic fixture that replaces detector, package, flash, monitor, HTTP, restart, serial-holder, and parity-validator commands with recording stubs. Tests must not access hardware, networks, ignored credential contents, or real device paths.

Add one Arrange/Act/Assert test for: zero candidates; multiple candidates; board-info failure; wrong board; source/reference/manifest-v3/executable-image/factory-image/package/runtime-identity drift; package-capability/detector-capability/root-contract/target-lock drift; broken event predecessor; command-canary proof that no target/credential/effect ran; timeout below 360; stale/multiple/malformed origin; zero-byte capture; pre-PATCH mismatch; PATCH not committed; immediate storage readback mismatch; reboot before response/readback; missing/additional reboot; wrong reset category; same-board identity drift; Boot B value mismatch; current-head/reference/lifecycle/no-actuation recheck failure; restoration failure; cleanup/PID/holder leak; raw-field redaction failure; validator rejection; and success ordering.

For every failure after PATCH, assert restoration was attempted before cleanup and the root is sealed non-promotion. For every failure before PATCH, assert cleanup occurred and no restoration mutation was attempted. On success assert exact ordered checkpoints, one reboot, one restoration, complete cleanup, and exactly one validator invocation.

Add `sh_test(name = "phase35_correlated_evidence_test", ...)` with only fixture/script data and `PATH` inheritance.
  </action>
  <verify>
    <automated>bazel test //scripts:phase35_correlated_evidence_test //scripts:phase33_confirmed_settings_durability_test &amp;&amp; git diff --check</automated>
  </verify>
  <done>The full capture state machine and its recovery semantics are proven without a board.</done>
  <acceptance_criteria>
    - `bazel test //scripts:phase35_correlated_evidence_test` exits 0 without USB or network access.
    - A detector-failure test asserts zero later command canaries were touched.
    - Every post-PATCH injected failure asserts restoration-before-cleanup and sealed non-promotion.
    - Success asserts one Gate 1 exact-package capability, one Gate 2 detector capability and detector invocation, one Gate 3 flash/package, two epochs, one approved reboot, one restoration, no holders, one complete event chain, and one validator call.
    - Test output and fixtures contain no real path, hostname, origin, IP/MAC, SSID, pool, credential, address, worker, token, or secret.
  </acceptance_criteria>
</task>

</tasks>

<verification>

- Run both task commands.
- Run `bazel test //scripts:phase35_correlated_evidence_test //scripts:phase33_confirmed_settings_durability_test //tools/parity:tests`.
- Run `just verify-reference`, `just parity`, and `git diff --check`.
- Inspect the script control flow for a single detector capability gate preceding all sensitive/effectful operations and for traps covering every post-mutation exit.
- Do not run hardware, flash, monitor, HTTP, or read ignored credentials in this plan.

</verification>

<success_criteria>

- EVD-10 detector/board-info failure stops all downstream work.
- CFG-12/EVD-13 capture order and one-reboot durability semantics are exact and simulated.
- EVD-11/EVD-12 root and epoch inputs are emitted under the typed Plan 35-01 contract.
- EVD-14 restoration, cleanup, redaction, current-head, reference, package, and no-actuation gates are test-covered.

</success_criteria>

<output>
After completion, create `.planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-02-SUMMARY.md`; use normal GSD atomic task commits and do not push.
</output>
