---
phase: 35-detector-gated-correlated-evidence-and-exact-parity-promotion
plan: "03"
type: execute
wave: 3
depends_on:
  - "35-01"
  - "35-02"
autonomous: true
phase_lifecycle_id: 35-2026-07-17T17-00-37
lifecycle_mode: yolo
generated_by: gsd-plan-phase
generated_at: "2026-07-17T20:32:00Z"
requirements:
  - EVD-14
  - EVD-15
files_modified:
  - tools/parity/src/phase35_promotion.rs
  - tools/parity/src/operator_evidence/generation.rs
  - tools/parity/src/operator_evidence/generation/tests/promotion.rs
  - tools/parity/src/main.rs
  - tools/parity/BUILD.bazel
  - docs/parity/checklist.md
  - scripts/phase35-promotion-contract-test.sh
  - scripts/BUILD.bazel
must_haves:
  truths:
    - "Every Phase 35 claim scope has exactly one typed promote or do-not-promote decision."
    - "Only dedicated narrow v1.2 operator-runtime rows can promote and only from one eligible evidence-root digest."
    - "All non-allowlisted checklist rows remain byte-for-byte unchanged, including STR-09, CFG-07, ASIC-11, and Phase 28.1.1 non-claims."
    - "Lifecycle/test completion is never accepted as parity evidence and staged output is published only after all gates pass."
  artifacts:
    - path: tools/parity/src/phase35_promotion.rs
      provides: "Closed exact-promotion matrix and exhaustive exclusion reasons"
    - path: docs/parity/checklist.md
      provides: "Dedicated narrow v1.2 row identities without widening legacy claims"
    - path: scripts/phase35-promotion-contract-test.sh
      provides: "Byte-preservation and no-promotion contract"
  key_links:
    - from: tools/parity/src/phase35_promotion.rs
      to: tools/parity/src/phase35_evidence.rs
      via: "only ValidatedPhase35Evidence can produce promotion decisions"
    - from: tools/parity/src/phase35_promotion.rs
      to: tools/parity/src/operator_evidence/generation.rs
      via: "complete decision matrix is staged and validated before atomic exchange"
---

<objective>
Implement the exhaustive exact-promotion matrix and atomic publication boundary.

Purpose: satisfy EVD-15 and the final admission portion of EVD-14 without over-promoting broader legacy rows.
Output: typed claim decisions, dedicated narrow row IDs, staged atomic generation, and preservation/source-policy tests.
</objective>

<execution_context>
@.planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-CONTEXT.md
@.planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-RESEARCH.md
@.planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-VALIDATION.md
</execution_context>

<threat-model>

| ID | Threat | Severity | Required mitigation and verification |
| --- | --- | --- | --- |
| T-35-13 | Broad legacy checklist rows are marked verified from narrower passive evidence. | critical | Introduce dedicated narrow row IDs and permit promotion only through an enum allowlist tied to evidence facts. |
| T-35-14 | A new or excluded claim scope silently lacks a non-promotion decision. | high | Use an exhaustive closed claim-scope enum and completeness test that compares every scope/row to exactly one decision. |
| T-35-15 | Lifecycle, tests, or administrative completion is treated as parity proof. | high | Reject those evidence categories explicitly and require one eligible Phase 35 root digest for every promoted row. |
| T-35-16 | A crash or concurrent edit leaves evidence and checklist in split-brain state. | high | Stage complete output, validate fingerprints/inventory/decisions, then use existing atomic directory/file exchange; preserve previous generation on failure. |

</threat-model>

<tasks>

<task type="auto" id="35-03-01">
  <name>Implement the closed Phase 35 promotion decision matrix</name>
  <read_first>
    - tools/parity/src/phase35_evidence.rs
    - tools/parity/src/v12_admission.rs
    - tools/parity/src/operator_evidence/generation.rs
    - tools/parity/src/operator_evidence/generation/tests/promotion.rs
    - docs/parity/checklist.md
    - docs/parity/evidence/phase-30-live-share-outcome-and-verified-promotion/disposition.md
    - scripts/phase30-no-promotion-contract-test.sh
  </read_first>
  <files>
    - tools/parity/src/phase35_promotion.rs
    - tools/parity/src/main.rs
    - tools/parity/BUILD.bazel
    - docs/parity/checklist.md
  </files>
  <action>
Create `phase35_promotion.rs` with:

- `Phase35ClaimScope`: dedicated passive hostname durability, exact source/reference/package identity, coherent operator snapshot, passive runtime-health projection, plus exclusions for active control, self-test effects, watchdog intervention, mining/Stratum/ASIC, archived Phase 28.1.1, credentials, direct UART/pins, OTA, other boards, lifecycle/test-only proof, and broader/unmapped rows.
- `Phase35PromotionDecision::{Promote { row_id, evidence_root_digest }, DoNotPromote { scope, reason }}`.
- `Phase35NonPromotionReason` with one stable serialized reason per exclusion and ineligible-gate condition.
- `evaluate_phase35_promotion(&ValidatedPhase35Evidence, &ChecklistSnapshot) -> Result<Phase35PromotionMatrix, Phase35PromotionError>`.

Add dedicated narrow checklist row IDs for only the exact eligible passive claims when existing rows include broader semantics. Row text must say what is proven, board `205`, and the admitted root digest/category; it must not imply active control, self-test effects, watchdog intervention, mining, credentials, OTA, or other-board parity.

The evaluator must enumerate every `Phase35ClaimScope` exactly once, reject duplicate/missing decisions, require the same eligible root digest for all promotions, reject lifecycle/test/summary/security artifacts as evidence, fingerprint the preexisting checklist, and prove every non-allowlisted row is unchanged. Before it constructs any `Promote`, it must live-recheck lifecycle ID `35-2026-07-17T17-00-37`, current HEAD against `ExactPackageCapability.source_commit`, reference cleanliness/commit, manifest-v3, executable-image, frozen factory-image, package/runtime identity, detector capability, root-contract/event chain, and no-actuation fact. Preserve STR-09, CFG-07, ASIC-11, Phase 30 disposition/conclusion, and archived Phase 28.1.1 rows/statuses exactly.

Wire `admit-phase35-evidence --root <path> --staging <path>` into `main.rs`. It must first run the Plan 35-01 validator, independently live-recheck the exact-package capability, detector capability, lifecycle/current-head/reference/runtime-identity/no-actuation facts, create a complete staged evidence/checklist projection, validate it, and invoke the existing atomic generation exchange only after every decision and fingerprint passes. It must never edit in place before validation.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-parity phase35_promotion --all-features &amp;&amp; bazel test //tools/parity:tests //scripts:phase30_no_promotion_contract_test &amp;&amp; just parity &amp;&amp; git diff --check</automated>
  </verify>
  <done>Only exact dedicated v1.2 rows are promotable and the decision matrix explicitly refuses every broader/excluded scope.</done>
  <acceptance_criteria>
    - `phase35_promotion.rs` contains the three required enums and `evaluate_phase35_promotion`.
    - A completeness test fails when any `Phase35ClaimScope` or checklist row is unclassified, duplicated, or mapped to multiple decisions.
    - Every `Promote` contains the same validated Phase 35 evidence-root digest.
    - Tests reject lifecycle, plan, summary, test, verification, and security artifacts as promotion evidence.
    - Tests reject stale current HEAD, dirty/wrong reference, manifest/executable/factory/package/runtime drift, detector/root/event-chain drift, lifecycle mismatch, and false no-actuation even when the stored root otherwise parses.
    - STR-09, CFG-07, ASIC-11, Phase 30 non-promotions, Phase 28.1.1, and all non-allowlisted rows remain byte-for-byte unchanged.
    - `just parity` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" id="35-03-02">
  <name>Add atomic-generation and exact-preservation contract tests</name>
  <read_first>
    - tools/parity/src/phase35_promotion.rs
    - tools/parity/src/operator_evidence/generation.rs
    - tools/parity/src/operator_evidence/generation/tests/promotion.rs
    - scripts/phase30-no-promotion-contract-test.sh
    - docs/parity/checklist.md
    - scripts/BUILD.bazel
  </read_first>
  <files>
    - tools/parity/src/operator_evidence/generation.rs
    - tools/parity/src/operator_evidence/generation/tests/promotion.rs
    - scripts/phase35-promotion-contract-test.sh
    - scripts/BUILD.bazel
  </files>
  <action>
Extend generation tests to stage an eligible root, derive the complete decision matrix, render the redacted evidence/checklist changes, validate all digests/fingerprints, and atomically exchange the generation. Inject failures before validation, after validation but before exchange, and during exchange; assert the previous admitted generation/checklist remains authoritative and no partial promotion is visible.

Create `phase35-promotion-contract-test.sh` to snapshot all checklist rows and evidence paths, run synthetic eligible/ineligible admissions, and assert:

- eligible admission changes only the dedicated allowlisted rows and new Phase 35 admitted evidence paths;
- every non-allowlisted row is byte-identical;
- excluded evidence produces typed non-promotion and no checklist status change;
- direct UART/pins, credentials, mining, watchdog intervention, active effects, OTA, other boards, and archived Phase 28.1.1 can never appear in a promoted row;
- raw identifier/value canaries never appear in the shareable generation.

Register `//scripts:phase35_promotion_contract_test` as a local `sh_test`.
  </action>
  <verify>
    <automated>bazel test //tools/parity:tests //scripts:phase35_promotion_contract_test //scripts:phase30_no_promotion_contract_test &amp;&amp; just parity &amp;&amp; git diff --check</automated>
  </verify>
  <done>Promotion and checklist publication are atomic, exhaustive, redacted, and preserve every excluded claim.</done>
  <acceptance_criteria>
    - Failure-injection tests prove no partial generation/checklist update is authoritative.
    - The contract test reports zero changed non-allowlisted rows.
    - The contract test rejects all excluded-category promotion canaries and all raw-value canaries.
    - Both Phase 30 and Phase 35 no-promotion contract tests pass together.
    - `bazel test //tools/parity:tests //scripts:phase35_promotion_contract_test //scripts:phase30_no_promotion_contract_test` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>

- Run both task commands.
- Run the full ordered Rust gate and all parity/no-promotion tests.
- Run `just verify-reference`, `just parity`, and `git diff --check`.
- Review the exact checklist diff; any changed row outside the dedicated allowlist fails the plan.
- Confirm no hardware, credential, target, flash, monitor, HTTP mutation, restart, UART/pin, OTA, mining, or other-board action occurs.

</verification>

<success_criteria>

- EVD-15 has an exhaustive exact allowlist and typed non-promotion for every excluded/broad claim.
- EVD-14 publication occurs only after all validated evidence, redaction, inventory, current-head, reference, cleanup, restoration, and lifecycle gates.
- Existing non-claims and non-allowlisted checklist rows are unchanged.
- Atomic failure leaves the previous admitted generation authoritative.

</success_criteria>

<output>
After completion, create `.planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-03-SUMMARY.md`; use normal GSD atomic task commits and do not push.
</output>
