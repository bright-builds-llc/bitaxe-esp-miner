---
phase: 32-shared-i2c-and-read-only-sensor-acquisition
plan: 03
type: execute
wave: 3
depends_on:
  - 32-01
  - 32-02
files_modified:
  - firmware/bitaxe/src/main.rs
  - firmware/bitaxe/src/operator_sensor_runtime.rs
  - firmware/bitaxe/src/safety_adapter.rs
  - firmware/bitaxe/src/safety_adapter/observation_store.rs
  - firmware/bitaxe/src/runtime_snapshot.rs
  - crates/bitaxe-api/src/observation.rs
  - tools/parity/src/main.rs
  - docs/evidence/phase-32/README.md
autonomous: true
requirements:
  - OBS-02
  - OBS-03
  - OBS-04
  - OBS-05
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 32-2026-07-13T23-12-34
generated_at: 2026-07-13T23:30:00Z
must_haves:
  truths:
    - Normal boot constructs one I2C0 owner, preserves startup display, and moves the owner into one 500 ms sensor producer.
    - The producer continues after individual failures and replaces one complete observation snapshot without holding the store lock during I2C.
    - Repeated API reads clone stored truth without acquiring sensors, advancing stamps, or refreshing stale values.
    - Firmware/package verification is clean and any hardware run is exactly-one-board-205, read-only, bounded, redacted, and non-promotional.
  artifacts:
    - firmware/bitaxe/src/operator_sensor_runtime.rs owns the one normal producer loop and source-local sequences.
    - firmware/bitaxe/src/main.rs wires startup borrow then move ownership.
    - docs/evidence/phase-32/README.md documents admissible redacted evidence and explicit non-claims.
  key_links:
    - Producer results feed `replace_observations_from_producer`; runtime/API consumers call only `observation_snapshot`.
    - Hardware verification stays pending until a repo-owned wrapper satisfies the full private serial-session trace and sanitized-summary contract.
---

<objective>
Integrate the sole sensor producer, preserve request-side purity, and verify Phase 32 end to end.

Purpose: Deliver attributable bounded read-only observations on real firmware without startup-display regression, API blocking, active effects, or premature parity promotion.
Output: One producer task, normal-boot ownership wiring, store/API regression guards, full build/package proof, and an explicit hardware-evidence pending record.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-shared-i2c-and-read-only-sensor-acquisition/32-01-SUMMARY.md
@.planning/phases/32-shared-i2c-and-read-only-sensor-acquisition/32-02-SUMMARY.md
@.planning/phases/32-shared-i2c-and-read-only-sensor-acquisition/32-VALIDATION.md
@firmware/bitaxe/src/main.rs
@firmware/bitaxe/src/safety_adapter/observation_store.rs
@firmware/bitaxe/src/runtime_snapshot.rs
</context>

<threat_model>
- **T-32-03 (high):** Request traffic could acquire hardware or advance provenance. Mitigate with a sole producer task, clone-only store methods, short store-lock replacement, and repeated-request tests.
- **T-32-04 (high):** Hardware verification could widen into sensitive, destructive, active, mining, or archived work. Mitigate with detector-first board-205 gating, repo-owned commands, ≥360-second bounds, redaction, exact no-actuation scans, and Phase 35-only promotion.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Start one deadline-based producer and publish complete snapshots</name>
  <files>firmware/bitaxe/src/main.rs, firmware/bitaxe/src/operator_sensor_runtime.rs, firmware/bitaxe/src/safety_adapter.rs, firmware/bitaxe/src/safety_adapter/observation_store.rs, tools/parity/src/main.rs</files>
  <read_first>firmware/bitaxe/src/main.rs, firmware/bitaxe/src/safety_adapter.rs, firmware/bitaxe/src/safety_adapter/observation_store.rs, firmware/bitaxe/src/runtime_uptime.rs, firmware/bitaxe/src/safety_adapter/phase27_bring_up.rs, .planning/phases/32-shared-i2c-and-read-only-sensor-acquisition/32-CONTEXT.md, .planning/phases/32-shared-i2c-and-read-only-sensor-acquisition/32-02-SUMMARY.md</read_first>
  <action>Add `operator_sensor_runtime.rs` and normal-boot wiring. On the non-Phase-27 path, construct one bounded `BitaxeI2cBus`, call startup rendering with a mutable borrow, log an explicit display outcome, then move the bus into one named producer thread even if display rendering failed. The thread owns one boot/session ID, one power sequence, one temperature sequence, one tachometer sequence, and a deadline-based 500 ms cadence. Each sweep attempts the atomic INA260 triple, then temperature, then tachometer regardless of earlier failure; passes all outcomes and current monotonic time to the pure reducer; then briefly replaces one complete observation snapshot. Project the single stamped power acquisition into `power_watts`, `bus_voltage_volts`, and `current_amps` with the same stamp; map EMC2101 external temperature to `chip_temp_celsius`; map tachometer RPM to `fan_rpm`; keep `vr_temp_celsius` explicitly unavailable. Skip missed deadlines to the next future slot and log a redaction-safe overrun category; do not run blocking retries or catch-up bursts. Add `phase32_runtime_source_guard` to the host-runnable parity tests for these exact ownership, projection, and prohibited-call properties. Preserve the existing isolated Phase 27 branch without routing normal runtime through it.</action>
  <verify><automated>cargo test -p bitaxe-parity phase32_runtime_source_guard &amp;&amp; just build</automated></verify>
  <acceptance_criteria>
    - `main.rs` contains one normal-path `BitaxeI2cBus::new` and moves that owner into `operator_sensor_runtime` after display rendering.
    - `operator_sensor_runtime.rs` contains the named 500 ms cadence and separate power/temperature/tachometer sequence state.
    - The producer continues to later acquisitions after any individual `Result::Err`.
    - `power_watts`, `bus_voltage_volts`, and `current_amps` share one stamp; external temperature maps to `chip_temp_celsius`; tachometer maps to `fan_rpm`; `vr_temp_celsius` remains explicitly unavailable.
    - Store replacement happens after all hardware calls; no I2C call occurs while the observation-store mutex is held.
    - Display failure logs unavailable but does not prevent the producer start.
    - Normal runtime source contains no active/control, credential, direct-UART/pin, OTA, mining, or archived-lineage call.
  </acceptance_criteria>
  <done>One long-lived producer owns I2C0 after bounded startup display and publishes failure-isolated stamped observations every 500 ms.</done>
</task>

<task type="auto">
  <name>Task 2: Prove API consumers remain clone-only and unaffected by sensor failures</name>
  <files>crates/bitaxe-api/src/observation.rs, firmware/bitaxe/src/safety_adapter/observation_store.rs, firmware/bitaxe/src/runtime_snapshot.rs, firmware/bitaxe/src/operator_sensor_runtime.rs, tools/parity/src/main.rs</files>
  <read_first>firmware/bitaxe/src/safety_adapter/observation_store.rs, firmware/bitaxe/src/runtime_snapshot.rs, crates/bitaxe-api/src/observation.rs, crates/bitaxe-api/src/telemetry.rs, .planning/phases/31-operator-claim-and-telemetry-contract/31-CONTEXT.md</read_first>
  <action>Add host-runnable `bitaxe-api` tests around `ObservationStore` and complete `TelemetryObservations` fixtures representing producer sweeps with success and per-source failures, then read projections repeatedly. Assert sequences and acquisition times remain byte-identical across reads, one failed source becomes fault/stale while unaffected facts remain usable, and only a later producer-supplied replacement advances the affected sequence. Add `phase32_consumer_source_guard` to `bitaxe-parity` proving firmware `observation_store.rs` and `runtime_snapshot.rs` contain no I2C/acquisition callback and that runtime snapshots still call only `observation_snapshot()`. Keep API/WebSocket/retained/evidence projections as copies; do not add refresh callbacks, clocks, or I2C access to consumer code.</action>
  <verify><automated>cargo test -p bitaxe-api observation &amp;&amp; cargo test -p bitaxe-api telemetry &amp;&amp; cargo test -p bitaxe-parity phase32_consumer_source_guard &amp;&amp; just build</automated></verify>
  <acceptance_criteria>
    - Tests count zero sensor acquisitions during repeated API snapshot reads.
    - Tests prove repeated reads preserve all supplied boot/session, sequence, acquisition-time, state, and reason fields.
    - Tests prove a failed temperature does not block API access or fresh power/tachometer observations, with equivalent coverage for other sources.
    - `runtime_snapshot.rs` still reaches telemetry only through `observation_snapshot()`.
  </acceptance_criteria>
  <done>OBS-05 is regression-guarded at the firmware/API boundary and request traffic remains effect-free.</done>
</task>

<task type="auto">
  <name>Task 3: Build, package, and record hardware verification pending</name>
  <files>tools/parity/src/main.rs, docs/evidence/phase-32/README.md</files>
  <read_first>AGENTS.md, .planning/phases/32-shared-i2c-and-read-only-sensor-acquisition/32-VALIDATION.md, tools/flash/src/main.rs, tools/parity/src/main.rs, justfile, docs/evidence/README.md</read_first>
  <action>Add only the minimum Phase 32 implementation-evidence inventory/non-claim documentation and pure parity validation needed to recognize software evidence without promoting checklist rows. Run the complete ordered Rust gate, `just build`, `just package`, and `just verify-reference`. Do not run `just detect-ultra205`, flash, monitor, API-on-device, credentials, or any hardware command in this plan: the existing wrappers do not yet record the mandatory private mode-0700/mode-0600 serial-session identity, PID/PGID, descendant, file-descriptor-holder, and cleanup trace with a separately sanitized shareable summary, and `redact-evidence=true` still preserves raw serial paths. Record that exact blocker as hardware evidence pending. Do not improvise scans, resets, raw writes, direct UART/pins, fault injection, mining, OTA, another board, or archived work. Do not mark parity rows verified; Phase 35 owns promotion.</action>
  <verify><automated>cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; just build &amp;&amp; just package &amp;&amp; just verify-reference</automated></verify>
  <acceptance_criteria>
    - All four ordered Rust commands exit 0.
    - Canonical firmware build and package commands exit 0 and `just verify-reference` exits 0.
    - `docs/evidence/phase-32/README.md` states Phase 35-only promotion and lists exact excluded active/sensitive categories.
    - `docs/evidence/phase-32/README.md` records hardware evidence pending because no repo-owned wrapper satisfies the complete private-trace/sanitized-summary contract.
    - No detector, flash, monitor, credential, API-on-device, reset, or other hardware command runs in this plan.
    - No checklist row changes to `verified` in Phase 32.
  </acceptance_criteria>
  <done>Host, firmware, package, reference, and any eligible read-only hardware checks are clean without widening scope or promoting parity.</done>
</task>

</tasks>

<verification>
- [ ] OBS-02, OBS-03, OBS-04, and OBS-05 each have automated behavior coverage.
- [ ] `cargo fmt --all` exits 0.
- [ ] `cargo clippy --all-targets --all-features -- -D warnings` exits 0.
- [ ] `cargo build --all-targets --all-features` exits 0.
- [ ] `cargo test --all-features` exits 0.
- [ ] Canonical firmware build/package and `just verify-reference` pass.
- [ ] No hardware action occurs; the exact serial-session wrapper blocker is recorded as pending.
- [ ] Diff contains no active-effect reachability, secret material, archived-lineage work, or parity promotion.
</verification>

<success_criteria>
- All tasks completed and committed.
- Startup display and periodic sensors share exactly one long-lived serialized I2C0 lifecycle.
- Fresh facts are attributable to producer-owned stamps; failures remain independent and request-side reads are effect-free.
- The full software gate is green and hardware status is either eligible read-only evidence or an explicit pending non-claim.
</success_criteria>

<output>
After completion, create `.planning/phases/32-shared-i2c-and-read-only-sensor-acquisition/32-03-SUMMARY.md`.
</output>
