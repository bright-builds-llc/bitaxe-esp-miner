---
phase: 32-shared-i2c-and-read-only-sensor-acquisition
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - firmware/bitaxe/src/display_adapter.rs
  - firmware/bitaxe/src/safety_adapter.rs
  - firmware/bitaxe/src/safety_adapter/i2c_bus.rs
  - firmware/bitaxe/src/safety_adapter/ina260.rs
  - firmware/bitaxe/src/safety_adapter/emc2101.rs
  - tools/parity/src/main.rs
autonomous: true
requirements:
  - OBS-02
  - OBS-03
  - OBS-04
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 32-2026-07-13T23-12-34
generated_at: 2026-07-13T23:30:00Z
must_haves:
  truths:
    - One normal-runtime I2C0 driver supports bounded startup display access and bounded read-only sensor access.
    - The periodic sensor capability cannot call EMC2101 configuration/fan writes or DS4432U/voltage writes.
    - Every register read uses one finite repeated-start transaction with a real millisecond-to-tick conversion.
  artifacts:
    - firmware/bitaxe/src/safety_adapter/i2c_bus.rs exposes separated bounded display and read-only sensor capabilities.
    - firmware/bitaxe/src/display_adapter.rs renders through a borrowed bounded adapter rather than owning a raw driver.
    - INA260 and EMC2101 readers return the typed pure acquisition inputs from Plan 01.
  key_links:
    - Display rendering borrows and returns the exact bus later moved to the producer.
    - Sensor readers can reach only closed allowlisted read registers.
---

<objective>
Create the bounded shared I2C0 and read-only device-adapter boundary.

Purpose: Preserve startup display behavior while structurally preventing unbounded transactions and active/control writes from the normal sensor runtime.
Output: One reusable I2C0 owner, bounded display borrow, closed read-only INA260/EMC2101 ports, and source-level regression guards.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-shared-i2c-and-read-only-sensor-acquisition/32-01-SUMMARY.md
@.planning/phases/32-shared-i2c-and-read-only-sensor-acquisition/32-RESEARCH.md
@firmware/bitaxe/src/display_adapter.rs
@firmware/bitaxe/src/safety_adapter/i2c_bus.rs
@firmware/bitaxe/src/safety_adapter/ina260.rs
@firmware/bitaxe/src/safety_adapter/emc2101.rs
</context>

<threat_model>
- **T-32-02 (high):** Generic bus access or legacy initialization could perform an actuator/control write. Mitigate with separate capability types, private write helpers, closed read enums, and source tests that the normal producer cannot name active functions.
- **T-32-02B (high):** Raw embedded-hal display access could block forever. Mitigate with a bounded trait adapter forwarding `TickType::new_millis(I2C_TRANSACTION_TIMEOUT_MS).ticks()`.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Make the shared I2C0 transaction boundary finite and capability-scoped</name>
  <files>firmware/bitaxe/src/safety_adapter.rs, firmware/bitaxe/src/safety_adapter/i2c_bus.rs, tools/parity/src/main.rs</files>
  <read_first>firmware/bitaxe/src/safety_adapter/i2c_bus.rs, firmware/bitaxe/src/display_adapter.rs, firmware/bitaxe/src/safety_adapter/ds4432u.rs, firmware/bitaxe/src/safety_adapter/emc2101.rs, firmware/bitaxe/src/safety_adapter/phase27_bring_up.rs, reference/esp-miner/main/i2c_bitaxe.c, .planning/phases/32-shared-i2c-and-read-only-sensor-acquisition/32-RESEARCH.md</read_first>
  <action>Refactor `BitaxeI2cBus` so one constructor owns `I2cDriver` and every normal-path transaction uses a named finite millisecond constant converted with `TickType::new_millis(...).ticks()`. Replace separate register-pointer write then read with `I2cDriver::write_read` repeated-start. Introduce separated borrowed capabilities: a startup display adapter implementing the exact embedded-hal trait required by `ssd1306`, and a periodic sensor reader that accepts only typed INA260 read registers `0x01/0x02/0x03` and EMC2101 read registers `0x01/0x10/0x46/0x47`. Keep generic write support private to legacy Phase 27 modules or behind an explicitly active capability that the normal producer cannot construct. Add a host-runnable `phase32_i2c_source_guard` test in `bitaxe-parity` that reads the firmware sources and checks finite timeout conversion, repeated-start use, one normal driver construction, and absence of `BLOCK` in the display adapter. Do not change the archived/diagnostic path's authorization or invoke it from normal boot.</action>
  <verify><automated>cargo test -p bitaxe-parity phase32_i2c_source_guard &amp;&amp; just build</automated></verify>
  <acceptance_criteria>
    - `i2c_bus.rs` contains `TickType::new_millis` and `write_read` for sensor register reads.
    - No normal-path I2C call passes the integer named `*_MS` directly as `TickType_t`.
    - The startup display adapter implements finite-timeout read/write/transaction methods and never forwards `BLOCK`.
    - The periodic sensor capability cannot express registers outside INA260 `0x01/0x02/0x03` and EMC2101 `0x01/0x10/0x46/0x47`.
    - Tests prove display success and error both return the same bus borrow to the caller.
  </acceptance_criteria>
  <done>The normal runtime has one finite-timeout bus with structurally separated startup-display and periodic-read capabilities.</done>
</task>

<task type="auto">
  <name>Task 2: Convert legacy readers into closed read-only typed acquisitions</name>
  <files>firmware/bitaxe/src/safety_adapter/ina260.rs, firmware/bitaxe/src/safety_adapter/emc2101.rs, firmware/bitaxe/src/safety_adapter.rs, tools/parity/src/main.rs</files>
  <read_first>firmware/bitaxe/src/safety_adapter/ina260.rs, firmware/bitaxe/src/safety_adapter/emc2101.rs, crates/bitaxe-safety/src/sensor_acquisition.rs, reference/esp-miner/main/power/INA260.c, reference/esp-miner/main/thermal/EMC2101.c</read_first>
  <action>Change INA260 and EMC2101 normal readers to accept only the read-only sensor capability and return the exact typed raw acquisition results consumed by Plan 01. INA260 must collect all three two-byte registers before returning one logical outcome. Temperature and tachometer must return separate outcomes so later reads continue after failure. Keep `emc2101::init`, `set_fan_duty_percent`, DS4432U, and other active functions isolated from these readers. Add a host-runnable `phase32_sensor_source_guard` in `bitaxe-parity` covering the normal reader module and explicit no-actuation identifiers rather than deleting dormant historical code.</action>
  <verify><automated>cargo test -p bitaxe-safety sensor_acquisition &amp;&amp; cargo test -p bitaxe-parity phase32_sensor_source_guard &amp;&amp; just build</automated></verify>
  <acceptance_criteria>
    - INA260 normal read returns one typed triple only after current, bus voltage, and power bytes are all present.
    - EMC2101 exposes independent external-temperature and tachometer read functions through the read-only capability.
    - A test/source guard proves the normal reader or future producer cannot call `emc2101::init`, `set_fan_duty_percent`, `write_register`, DS4432U, voltage, reset, ASIC, mining, fault-stimulus, self-test, credential, UART/pin, OTA, or archived-lineage paths.
    - Dormant Phase 27 active helpers remain preserved and are not newly reachable from normal boot.
  </acceptance_criteria>
  <done>Firmware device adapters supply only bounded read-only acquisition results to the pure Phase 32 core.</done>
</task>

</tasks>

<verification>
- [ ] Narrow I2C/display/INA260/EMC2101 tests pass.
- [ ] `cargo fmt --all` passes before commit.
- [ ] `cargo clippy --all-targets --all-features -- -D warnings` passes before commit.
- [ ] `cargo build --all-targets --all-features` passes before commit.
- [ ] `cargo test --all-features` passes before commit.
- [ ] `rg 'BLOCK|emc2101::init|set_fan_duty_percent|ds4432u'` confirms no prohibited identifier is reachable from the normal producer/read-only modules except explicit negative tests.
</verification>

<success_criteria>
- All tasks completed and committed.
- Display and sensor access share one finite-timeout driver without shared-mutex ownership.
- Sensor register access is closed and read-only.
- Active helpers remain dormant and unreachable from Phase 32 normal runtime.
</success_criteria>

<output>
After completion, create `.planning/phases/32-shared-i2c-and-read-only-sensor-acquisition/32-02-SUMMARY.md`.
</output>
