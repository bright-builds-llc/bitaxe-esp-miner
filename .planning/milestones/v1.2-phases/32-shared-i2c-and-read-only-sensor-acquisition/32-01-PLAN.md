---
phase: 32-shared-i2c-and-read-only-sensor-acquisition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/bitaxe-safety/src/lib.rs
  - crates/bitaxe-safety/src/sensor_acquisition.rs
  - crates/bitaxe-safety/src/observation.rs
  - crates/bitaxe-safety/src/power.rs
  - crates/bitaxe-safety/src/thermal.rs
autonomous: true
requirements:
  - OBS-03
  - OBS-04
  - OBS-05
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 32-2026-07-13T23-12-34
generated_at: 2026-07-13T23:30:00Z
must_haves:
  truths:
    - INA260 current, bus voltage, and power become fresh only as one validated stamped acquisition.
    - Temperature and tachometer can succeed or fail independently without borrowing each other's sequence or stamp.
    - Read and validation failures preserve the exact last-good stamp and never advance a sequence.
    - Staleness is decided by producer-supplied monotonic time rather than API request time.
  artifacts:
    - crates/bitaxe-safety/src/sensor_acquisition.rs owns pure raw decoding and sweep reduction.
    - crates/bitaxe-safety/src/observation.rs exposes only the minimal typed transitions needed by the producer reducer.
  key_links:
    - Raw INA260 and EMC2101 results feed Phase 31 Observation values through one pure reducer.
    - Reducer outputs remain compatible with bitaxe-api TelemetryObservations without acquiring hardware.
---

<objective>
Build the pure read-only sensor acquisition and failure-isolation core.

Purpose: Make sensor decoding, atomic publication, independent fact truth, source-local provenance, and stale/fault behavior deterministic before ESP-IDF integration.
Output: Typed raw sensor results, pure decoders/reducer, and exhaustive host tests for OBS-03 through OBS-05.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-shared-i2c-and-read-only-sensor-acquisition/32-CONTEXT.md
@.planning/phases/32-shared-i2c-and-read-only-sensor-acquisition/32-RESEARCH.md
@.planning/phases/32-shared-i2c-and-read-only-sensor-acquisition/32-VALIDATION.md
@crates/bitaxe-safety/src/observation.rs
@crates/bitaxe-safety/src/power.rs
@crates/bitaxe-safety/src/thermal.rs
</context>

<threat_model>
- **T-32-01 (high):** Partial or invalid reads could be labeled fresh. Mitigate with typed raw outcomes, atomic INA260 admission, checked decoding, and tests that failure leaves all three power facts on the prior stamp.
- **T-32-03 (high):** A consumer could mint provenance. Keep boot/session, monotonic time, and sequences as explicit reducer inputs supplied only by the producer shell; add repeated-consumer-read regression coverage in later plans.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Define typed sensor decoders and read-only acquisition results</name>
  <files>crates/bitaxe-safety/src/lib.rs, crates/bitaxe-safety/src/sensor_acquisition.rs, crates/bitaxe-safety/src/power.rs, crates/bitaxe-safety/src/thermal.rs</files>
  <read_first>crates/bitaxe-safety/src/observation.rs, crates/bitaxe-safety/src/power.rs, crates/bitaxe-safety/src/thermal.rs, firmware/bitaxe/src/safety_adapter/ina260.rs, firmware/bitaxe/src/safety_adapter/emc2101.rs, reference/esp-miner/main/power/INA260.c, reference/esp-miner/main/thermal/EMC2101.c, .planning/phases/32-shared-i2c-and-read-only-sensor-acquisition/32-CONTEXT.md</read_first>
  <action>Create `sensor_acquisition.rs` as a pure module exported from `lib.rs`. Define typed raw byte inputs/outcomes for one INA260 triple, one EMC2101 external temperature, and one tachometer count. Decode INA260 current with `i16::from_be_bytes` at 1.25 mA/bit, voltage with `u16::from_be_bytes` at 1.25 mV/bit, and power at 10 mW/bit. Decode EMC2101 external temperature as signed 11-bit 0.125 °C units and classify open/short/sentinel/out-of-range values explicitly. Decode tachometer with checked division by 5_400_000, checked narrowing, and explicit zero/sentinel handling. Reuse `ThermalReading`, `TachometerReading`, `PowerObservation`, `Observation`, and stable typed reasons; do not add free-form public error strings or any ESP-IDF dependency.</action>
  <verify><automated>cargo test -p bitaxe-safety sensor_acquisition</automated></verify>
  <acceptance_criteria>
    - `crates/bitaxe-safety/src/lib.rs` contains `pub mod sensor_acquisition;`.
    - Tests prove INA260 raw `0xffff` decodes as negative signed current rather than `65535 * 1.25` mA.
    - Tests cover EMC2101 positive, negative, open/short/sentinel, zero tachometer, sentinel tachometer, and overflow-safe cases.
    - The new module contains no `esp_idf`, `I2cDriver`, GPIO, fan-write, voltage-write, reset, ASIC, mining, credential, UART, pin, or OTA effect surface.
  </acceptance_criteria>
  <done>All raw sensor inputs produce typed validated values or typed non-fresh outcomes with exact unit-test vectors.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce one failure-isolated producer sweep into stamped observations</name>
  <files>crates/bitaxe-safety/src/sensor_acquisition.rs, crates/bitaxe-safety/src/observation.rs, crates/bitaxe-safety/src/power.rs, crates/bitaxe-safety/src/thermal.rs</files>
  <read_first>crates/bitaxe-safety/src/sensor_acquisition.rs, crates/bitaxe-safety/src/observation.rs, crates/bitaxe-api/src/observation.rs, crates/bitaxe-api/src/snapshot.rs, .planning/phases/31-operator-claim-and-telemetry-contract/31-CONTEXT.md</read_first>
  <action>Define a pure reducer that receives previous power/temperature/tachometer observations, one boot session, per-source prior sequences, producer monotonic time, and the three typed acquisition outcomes. On a fully valid INA260 triple, advance the single power sequence exactly once and stamp the complete power acquisition. On temperature and tachometer successes, advance only their own sequences. On read or validation failure, construct the stable typed fault while preserving the exact prior last-good stamp and sequence. Add a producer-time stale transition using `ProducerCadenceExpired` without reading a clock internally. Keep compatibility numeric projections outside this reducer.</action>
  <verify><automated>cargo test -p bitaxe-safety sensor_acquisition &amp;&amp; cargo test -p bitaxe-api observation</automated></verify>
  <acceptance_criteria>
    - A test proves one failed INA260 member prevents all power facts from receiving a new sequence or acquisition time.
    - Tests prove temperature success plus tachometer failure and tachometer success plus temperature failure preserve the successful fact independently.
    - Tests prove read/validation faults retain the exact last-good `BootSessionId`, `ObservationSequence`, and `MonotonicMillis`.
    - Tests prove stale transitions occur only when producer-supplied elapsed time crosses the named threshold.
    - No public reducer function reads wall time, monotonic time, a mutex, the API, or hardware.
  </acceptance_criteria>
  <done>The pure reducer satisfies atomic power, independent thermal facts, failure isolation, and producer-owned provenance for OBS-03 through OBS-05.</done>
</task>

</tasks>

<verification>
- [ ] `cargo test -p bitaxe-safety` passes.
- [ ] `cargo test -p bitaxe-api observation` passes.
- [ ] `cargo fmt --all` passes before commit.
- [ ] `cargo clippy --all-targets --all-features -- -D warnings` passes before commit.
- [ ] `cargo build --all-targets --all-features` passes before commit.
- [ ] `cargo test --all-features` passes before commit.
</verification>

<success_criteria>
- All tasks completed and committed atomically.
- Every successful fact owns a producer-supplied stamp; failures never mint freshness.
- Power acquisition is all-or-nothing while temperature and tachometer remain independent.
- No firmware or active-effect dependency enters the pure core.
</success_criteria>

<output>
After completion, create `.planning/phases/32-shared-i2c-and-read-only-sensor-acquisition/32-01-SUMMARY.md`.
</output>
