---
phase: 33-confirmed-settings-durability
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - firmware/bitaxe/src/http_api.rs
  - firmware/bitaxe/src/runtime_snapshot.rs
  - firmware/bitaxe/src/settings_adapter.rs
  - tools/parity/BUILD.bazel
  - tools/parity/src/main.rs
  - tools/parity/src/phase33_source_guard.rs
autonomous: true
requirements:
  - CFG-09
  - CFG-10
  - CFG-11
  - CFG-13
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 33-2026-07-14T01-50-49
generated_at: 2026-07-14T02:20:35Z
must_haves:
  truths:
    - Firmware serializes each authorized hostname transaction from NVS mutation through confirmed snapshot publication.
    - Reload independently opens read-only NVS, fails on missing or invalid confirmation evidence, and does not publish while loading.
    - Immediate system-info reads only the confirmed snapshot and never an overlay of requested writes.
    - Compatibility-only and invalid requests never open writable NVS or schedule hardware/network effects.
  artifacts:
    - firmware/bitaxe/src/settings_adapter.rs owns the transaction lock, candidate reload, exact reconciliation, and atomic publication.
    - firmware/bitaxe/src/http_api.rs routes the pure authority decision and responds only after confirmed execution.
    - tools/parity/src/phase33_source_guard.rs protects the firmware boundary against optimistic overlays and broad write plans.
  key_links:
    - The API route passes only a validated Hostname into the firmware transaction.
    - runtime_snapshot projects hostname solely from the atomically published confirmed NVS snapshot.
---

<objective>
Integrate the confirmation contract with ESP-IDF NVS and the v1.2 API route.

Purpose: Turn PATCH success and immediate system-info readback into storage-confirmed firmware truth without expanding Phase 33 into global revision semantics.
Output: A serialized fallible NVS transaction, atomic confirmed snapshot publication, exact route behavior, and firmware source/build guards.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-confirmed-settings-durability/33-01-SUMMARY.md
@.planning/phases/33-confirmed-settings-durability/33-RESEARCH.md
@.planning/phases/33-confirmed-settings-durability/33-VALIDATION.md
@firmware/bitaxe/src/settings_adapter.rs
@firmware/bitaxe/src/http_api.rs
@firmware/bitaxe/src/runtime_snapshot.rs
@tools/parity/src/main.rs
</context>

<threat_model>
- **T-33-01 (high):** The broad route could bypass exact authority. Route exclusively through the Plan 01 decision and guard against broad planned writes.
- **T-33-02 (high):** Requested writes could be overlaid onto public state after a failed reload. Delete the overlay path and publish only a reconciled candidate.
- **T-33-03 (high):** ESP-IDF handles could serialize writes but release before reload/publication. Hold one process-lifetime mutex through atomic publication.
- **T-33-04 (high):** Firmware logs could expose body or hostname values. Log stable stage/category only and preserve generic public errors.
- **T-33-05 (high):** Poisoning or post-commit failure could manufacture an empty/default success. Fail closed, retain the last confirmed snapshot, and expose post-commit uncertainty only in value-free diagnostics.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build a serialized fallible NVS confirmation adapter</name>
  <files>firmware/bitaxe/src/settings_adapter.rs, firmware/bitaxe/src/runtime_snapshot.rs, tools/parity/src/phase33_source_guard.rs, tools/parity/src/main.rs, tools/parity/BUILD.bazel</files>
  <read_first>firmware/bitaxe/src/settings_adapter.rs, firmware/bitaxe/src/runtime_snapshot.rs, crates/bitaxe-api/src/settings.rs, crates/bitaxe-api/src/v12_settings.rs, crates/bitaxe-config/src/persistence.rs, tools/parity/src/main.rs</read_first>
  <action>Add one process-lifetime settings transaction mutex and hold it from writable NVS open through candidate publication. Split startup's best-effort snapshot refresh from authoritative PATCH confirmation. For PATCH, write the validated hostname even when unchanged, commit, independently reopen the namespace read-only, and build a complete fallible candidate whose hostname key/type/read/parse failures propagate. Reconcile the candidate's typed hostname exactly against the request, then atomically replace `CURRENT_SETTINGS_SNAPSHOT`; readers must continue seeing the previous confirmed snapshot until replacement. Treat mutex poison and publication failure as errors rather than defaulting to an empty snapshot. Never publish inside reload and never apply requested writes as a post-reload overlay. Add a Phase 33 host source guard covering the transaction lock extent, independent read-only reload, candidate-before-publication structure, fallible hostname handling, and absence of optimistic overlay calls.</action>
  <verify><automated>cargo test -p bitaxe-parity phase33_settings_source_guard &amp;&amp; cargo test -p bitaxe-api settings &amp;&amp; just build</automated></verify>
  <acceptance_criteria>
    - The authoritative PATCH adapter has distinct candidate-load and publish operations, and candidate load cannot mutate `CURRENT_SETTINGS_SNAPSHOT`.
    - The transaction lock remains held through exact reconciliation and confirmed snapshot replacement.
    - A hostname key/type/read/parse mismatch or poisoned lock returns a typed stage failure while retaining the prior confirmed snapshot.
    - `settings_adapter.rs` has no authorized-success path that calls `apply_persisted_settings_writes` or otherwise overlays planned values.
    - The source guard is host-runnable through Cargo/Bazel and fails if the route regains broad writes or the adapter publishes before reconciliation.
  </acceptance_criteria>
  <done>Firmware can produce and atomically publish independent NVS confirmation evidence under one transaction lock.</done>
</task>

<task type="auto">
  <name>Task 2: Route v1.2 PATCH and immediate readback through confirmed truth</name>
  <files>firmware/bitaxe/src/http_api.rs, firmware/bitaxe/src/runtime_snapshot.rs, firmware/bitaxe/src/settings_adapter.rs, tools/parity/src/phase33_source_guard.rs</files>
  <read_first>firmware/bitaxe/src/http_api.rs, firmware/bitaxe/src/settings_adapter.rs, firmware/bitaxe/src/runtime_snapshot.rs, crates/bitaxe-api/src/v12_settings.rs, crates/bitaxe-api/src/settings.rs, reference/esp-miner/main/http_server.c</read_first>
  <action>Change `handle_settings_patch` to consume the Plan 01 validation-and-authority decision. Return the existing generic error for malformed/invalid known inputs without opening NVS. Return the existing empty success for compatibility-only inputs without writes, publication, or effects. For exact hostname authority, call the serialized confirmation adapter and emit success only after candidate publication; map every internal stage failure to the stable generic public error without logging values. Remove `apply_persisted_settings_writes(plan.writes())` from the route. Preserve the best-effort netif hostname effect strictly after confirmed success. Keep `runtime_snapshot::apply_settings_snapshot` reading the confirmed store so the next `/api/system/info` sees the reloaded value; do not introduce Phase 34 boot/revision state. Extend source/integration tests for exact routing, generic errors, no-op compatibility, response/effect order, and immediate confirmed projection.</action>
  <verify><automated>cargo test -p bitaxe-api v12_settings &amp;&amp; cargo test -p bitaxe-api settings &amp;&amp; cargo test -p bitaxe-parity phase33_settings_source_guard &amp;&amp; just build</automated></verify>
  <acceptance_criteria>
    - Invalid known and invalid-known mixed bodies return the existing generic error with no writable NVS construction.
    - Empty, unknown, unsupported, and fully valid mixed bodies return empty success with no storage/publication/effect path.
    - Exact valid hostname returns success only after confirmed snapshot publication and then schedules the best-effort live hostname effect.
    - Immediate system-info hostname is sourced only from `current_settings_snapshot()` after confirmed publication.
    - No new global revision, boot-session revision, active hardware control, credential, mining, OTA, UART/pin, or archived-lineage path is added.
  </acceptance_criteria>
  <done>The live API exposes only storage-confirmed hostname truth while preserving compatibility and public error semantics.</done>
</task>

</tasks>

<verification>
- [ ] `cargo test -p bitaxe-api v12_settings` passes.
- [ ] `cargo test -p bitaxe-api settings` passes.
- [ ] `cargo test -p bitaxe-parity phase33_settings_source_guard` passes.
- [ ] `bazel test //tools/parity:all` or the exact affected parity targets pass.
- [ ] `just build` passes for the canonical ESP32-S3 firmware target.
- [ ] `cargo fmt --all` passes before commit.
- [ ] `cargo clippy --all-targets --all-features -- -D warnings` passes before commit.
- [ ] `cargo build --all-targets --all-features` passes before commit.
- [ ] `cargo test --all-features` passes before commit.
</verification>

<success_criteria>
- One mutex serializes every authorized write through atomic confirmed publication.
- Public success and immediate system-info readback derive from independent NVS reload evidence.
- Optimistic requested-write overlays and broad route authority are absent and guarded.
- Compatibility-only/error paths are value-safe and effect-free.
</success_criteria>

<output>
After completion, create `.planning/phases/33-confirmed-settings-durability/33-02-SUMMARY.md`.
</output>
