---
phase: 33-confirmed-settings-durability
plan: 03
type: execute
wave: 3
depends_on:
  - 33-02
files_modified:
  - .planning/phases/33-confirmed-settings-durability/33-VALIDATION.md
  - docs/evidence/phase-33/README.md
  - justfile
  - scripts/BUILD.bazel
  - scripts/phase33-confirmed-settings-durability-test.sh
  - scripts/phase33-confirmed-settings-durability.sh
autonomous: true
requirements:
  - CFG-09
  - CFG-10
  - CFG-11
  - CFG-12
  - CFG-13
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 33-2026-07-14T01-50-49
generated_at: 2026-07-14T02:20:35Z
must_haves:
  truths:
    - Host and simulation gates fail closed before any real-device durability claim is attempted.
    - One detector preflight occurs outside a proof interval containing exactly one approved application restart.
    - Immediate and post-reboot hostname digests match on the same physical Ultra 205 with complete passive-session cleanup.
    - Shareable evidence is redacted and explicitly non-promotional; protected local traces contain no committed identifiers or secrets.
  artifacts:
    - scripts/phase33-confirmed-settings-durability.sh owns the bounded detector-gated proof and cleanup lifecycle.
    - scripts/phase33-confirmed-settings-durability-test.sh simulates success and every fail-closed boundary without hardware.
    - docs/evidence/phase-33/README.md documents evidence shape, redaction, recovery, and non-claims.
  key_links:
    - The wrapper reuses serial-session-trace identity/ownership primitives and the exact passive ESP32-S3 monitor contract.
    - Hardware execution follows ordered Rust, Bazel, firmware build/package, and reference-clean gates.
---

<objective>
Prove confirmed hostname durability across one normal application reboot on the detector-gated Ultra 205.

Purpose: Convert the host-verified confirmation path into bounded same-board hardware evidence without invoking destructive, archived, credential, or promotion workflows.
Output: A simulation-tested repo-owned evidence wrapper, redacted evidence contract, canonical command surface, and one approved hardware run when detection succeeds.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-confirmed-settings-durability/33-02-SUMMARY.md
@.planning/phases/33-confirmed-settings-durability/33-CONTEXT.md
@.planning/phases/33-confirmed-settings-durability/33-RESEARCH.md
@.planning/phases/33-confirmed-settings-durability/33-VALIDATION.md
@scripts/detect-ultra205.sh
@scripts/serial-session-trace.sh
@scripts/phase13-monitor-capture.sh
@justfile
</context>

<threat_model>
- **T-33-04 (high):** Hostname values, origins, credentials, device paths, USB identities, PIDs, or commands could leak to retained evidence. Keep local traces under mode 0700/0600 and commit only categories, digests, counts, booleans, durations, and redacted conclusions.
- **T-33-06 (high):** HTTP recovery or a tty node could be mistaken for same-board proof. Lock a stable physical USB identity digest from the detector-approved session and fail on identity change.
- **T-33-07 (high):** A post-reboot detector or reset-capable monitor could introduce a second reset. Run one detector before the proof interval and use the complete passive no-reset ESP32-S3 monitor command inside it.
- **T-33-08 (high):** The wrapper could escape into destructive, archived, credential, UART/pin, mining, fault-injection, OTA, raw-write, or parity-promotion paths. Enforce a closed command/source guard and an explicit Phase 35-only promotion boundary.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build a fail-closed Phase 33 durability wrapper and simulation suite</name>
  <files>scripts/phase33-confirmed-settings-durability.sh, scripts/phase33-confirmed-settings-durability-test.sh, scripts/BUILD.bazel, justfile, docs/evidence/phase-33/README.md</files>
  <read_first>scripts/detect-ultra205.sh, scripts/serial-session-trace.sh, scripts/phase13-monitor-capture.sh, scripts/BUILD.bazel, justfile, .planning/phases/33-confirmed-settings-durability/33-RESEARCH.md, AGENTS.md</read_first>
  <action>Create a Bash wrapper with `#!/usr/bin/env bash` and `set -euo pipefail` plus a direct fake-backed test. The wrapper must run `just detect-ultra205` exactly once before its proof interval and require board 205 with one successful board-info result. After that preflight and before the proof interval, require installation of the Phase 33 package produced by the preceding canonical build/package gates through repo-owned `just flash` or `just flash-monitor`, using the detector-approved port and at least 360 seconds of capture. Do not permit retained-runtime or skip-flash evidence because Phase 33 has no independent exact running-package identity mechanism. Wait for the flashed runtime to settle, then derive exactly one origin-only `DEVICE_URL` solely from fresh same-session repo-owned monitor output. Never read or print Wi-Fi/pool credential contents. Capture the original non-secret hostname for recovery, generate a non-secret test hostname, PATCH it, require immediate system-info digest equality, arm the passive monitor with `--chip esp32s3 --before no-reset-no-sync --after no-reset --no-reset --non-interactive`, then invoke only `POST /api/system/restart`. Require response-before-effect, service loss/recovery, a fresh same-session origin, stable physical identity across enumeration, post-reboot digest equality, reaped process tree, and zero unexpected serial holders. Use a protected mode-0700 local root and mode-0600 traces; write only redacted shareable output. After the proof interval restore the original hostname via the same confirmed PATCH and immediate readback, without another reboot; report restoration failure explicitly. Add a `just phase33-settings-durability` surface and documentation. The simulation suite must cover success, zero/multiple origins, detector ambiguity, board-info failure, missing/failed required package flash, identity change, extra reset, response/effect reversal, missing/mismatched immediate or post-reboot readback, holder/process leak, timeout, restore failure, forbidden-command source scan, and sensitive-output denylist.</action>
  <verify><automated>bash scripts/phase33-confirmed-settings-durability-test.sh &amp;&amp; bazel test //scripts:phase33_confirmed_settings_durability_test</automated></verify>
  <acceptance_criteria>
    - The detector runs once and only before the proof interval; no post-reboot board-info/detector call exists.
    - The current canonical Phase 33 package is always flashed after detector preflight and before the proof interval, followed by settled-runtime and fresh-origin gates; there is no skip-flash evidence path.
    - The passive monitor command contains every required flag and no reset-default variant can be selected.
    - The proof interval contains exactly one application restart and no raw reset, erase, rollback, power-cycle, OTA, fault/mining/stress, pool credential, direct UART/pin, archived Phase 28.1.1, or parity-promotion command.
    - Simulation tests fail on origin/identity/reset/ordering/readback/cleanup/redaction violations and exercise restoration success/failure.
    - Shareable artifacts contain no hostname value, raw origin/IP/MAC, USB/device path or identity, PID/PGID, SSID, endpoint, address, worker, password, token, NVS secret, or command containing sensitive input.
  </acceptance_criteria>
  <done>The repository owns a deterministic, redacted, same-board, exactly-one-reboot durability proof with complete cleanup and recovery.</done>
</task>

<task type="auto">
  <name>Task 2: Run software gates and the approved Ultra 205 durability proof</name>
  <files>.planning/phases/33-confirmed-settings-durability/33-VALIDATION.md, docs/evidence/phase-33/README.md</files>
  <read_first>.planning/phases/33-confirmed-settings-durability/33-VALIDATION.md, scripts/phase33-confirmed-settings-durability.sh, docs/evidence/phase-33/README.md, AGENTS.md, standards/core/verification.md</read_first>
  <action>Run the ordered Rust gate, all Phase 33 unit/source/simulation Bazel targets, `just build`, `just package`, and `just verify-reference` before hardware. Then run `just detect-ultra205` through the wrapper. If and only if it finds exactly one likely ESP USB serial port and board-info succeeds for board 205, use the returned session/port with the repo-owned wrapper, a capture timeout of at least 360 seconds, and a shell/tool wall clock of at least 420 seconds. A gitignored `wifi-credentials.json` may be passed by path to the repo-owned setup command without reading or printing it. Do not pass pool credentials. Retain complete local traces only under the protected ignored root; promote only a redacted summary recording board category, source/reference/package digests, exact non-sensitive command categories, durations, booleans, hostname digests, observed behavior, cleanup, restoration, and a non-promotional conclusion. If detection is unavailable or any gate fails, record CFG-12 hardware evidence as pending rather than weakening the gate, and stop with Plan 03 and Phase 33 incomplete: that result must produce pending or `gaps_found` verification and prohibits lifecycle completion and push. Update Validation statuses from actual results only; do not mark requirements or parity rows verified here because Phase 35 owns final correlated admission.</action>
  <verify><automated>cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; bazel test //scripts:phase33_confirmed_settings_durability_test //tools/parity:all &amp;&amp; just build &amp;&amp; just package &amp;&amp; just verify-reference</automated></verify>
  <acceptance_criteria>
    - Every software/build/package/reference command passes before the hardware proof begins.
    - A hardware pass includes immediate and post-reboot equality of non-secret hostname digests, one application restart, same physical identity, fresh same-session origin, full cleanup, and successful restoration.
    - A hardware skip/failure leaves CFG-12 and the validation row explicitly pending with the exact non-secret gate category, blocks Plan 03 and Phase 33 completion, and prohibits lifecycle completion or push.
    - Plan 03 can complete only after an eligible Ultra 205 run passes immediate/post-reboot digest equality, same-board identity, exactly-one-restart, cleanup, and restoration gates.
    - Only redacted, secret-scanned evidence is tracked; protected traces and credentials remain ignored and mode restricted.
    - No Phase 33 action modifies parity status, claims Phase 35 admission, invokes archived lineage, or uses direct UART/pins.
  </acceptance_criteria>
  <done>Phase 33 has truthful software and detector-gated hardware evidence with no overclaim or sensitive-data exposure.</done>
</task>

</tasks>

<verification>
- [ ] `bash scripts/phase33-confirmed-settings-durability-test.sh` passes.
- [ ] Affected Bazel script and parity targets pass.
- [ ] `just build`, `just package`, and `just verify-reference` pass.
- [ ] `cargo fmt --all` passes before commit.
- [ ] `cargo clippy --all-targets --all-features -- -D warnings` passes before commit.
- [ ] `cargo build --all-targets --all-features` passes before commit.
- [ ] `cargo test --all-features` passes before commit.
- [ ] Detector-gated hardware evidence passes with a 360-second-or-longer capture and 420-second-or-longer shell budget; pending or failed hardware evidence blocks completion.
- [ ] `git diff --check` and the evidence sensitive-data scan pass.
</verification>

<success_criteria>
- Host simulation proves every fail-closed evidence and cleanup boundary.
- One eligible Ultra 205 run proves the storage-confirmed hostname survives one normal application reboot on the same board; an honestly pending result is a blocker, not success.
- Original hostname restoration completes without a second reboot.
- Committed artifacts are redacted, non-promotional, and free of secrets and local identifiers.
</success_criteria>

<output>
After completion, create `.planning/phases/33-confirmed-settings-durability/33-03-SUMMARY.md`.
</output>
