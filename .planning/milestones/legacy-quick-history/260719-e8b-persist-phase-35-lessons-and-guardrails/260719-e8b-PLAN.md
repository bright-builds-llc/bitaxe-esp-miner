---
quick_id: 260719-e8b
phase: quick
plan: 260719-e8b
type: execute
wave: 1
depends_on: []
mode: quick-full
status: planned
created_at: "2026-07-19T15:18:23Z"
autonomous: true
requirements: []
files_modified:
  - AGENTS.md
  - .codex/tasks/lessons.md
  - .codex/tasks/todo.md
  - scripts/phase35-correlated-evidence-test.sh
  - /Users/peterryszkiewicz/.codex/tasks/lessons.md
must_haves:
  truths:
    - "Five consolidated Phase 35 lessons live in the repository lesson ledger: protected-root ownership, Bazel/runfiles real-process boundaries, earliest typed-failure precedence, ESP-IDF main-task runtime capacity, and the non-equivalence of HTTP route/liveness markers and response readiness."
    - "Repository guidance requires a mode-0700 protected parent, a nonexistent supervisor-owned child, and mode-0600 sibling wrapper output, and it prohibits another ambiguous same-boundary hardware attempt until separately planned redacted pre-mutation instrumentation exists."
    - "The Phase 35 hermetic shell suite rejects an existing child before package admission, detector invocation, or effects, and proves the protected-parent/nonexistent-child/sibling-wrapper creation order plus 0700 directory and 0600 file permissions."
    - "A pending repository todo defines a separate redacted HTTP diagnostic across connection, request transmission, response status/headers, body receipt, and hostname-schema parsing without authorizing hardware."
    - "Diagnostic completeness before costly one-shot external attempts and zsh lowercase `path` mutation of `PATH` exist only in the global lesson ledger, which remains outside Git."
    - "Exactly two atomic repository implementation commits are created, no push occurs, and Phase 35 evidence, checklist, summaries, verification truth, hardware, HTTP diagnostics, archived lineage, and direct UART/pins remain untouched."
  artifacts:
    - path: .codex/tasks/lessons.md
      provides: "Four new stable lesson blocks and a targeted extension of the existing cross-process boundary lesson"
    - path: AGENTS.md
      provides: "Repo-local protected-root and no-ambiguous-retry guardrails outside the managed block"
    - path: .codex/tasks/todo.md
      provides: "Pending, non-authorizing redacted HTTP diagnostic task"
    - path: scripts/phase35-correlated-evidence-test.sh
      provides: "Fresh-process hermetic proof of the existing protected-root rejection, ordering, and permission contract while retaining the complete Phase 35 suite"
    - path: /Users/peterryszkiewicz/.codex/tasks/lessons.md
      provides: "Two global-only four-field lesson blocks"
  key_links:
    - from: AGENTS.md
      to: scripts/phase35-correlated-evidence-test.sh
      via: "Repo guidance describes the protected parent, nonexistent child, and private sibling-output caller contract exercised by the hermetic suite"
      pattern: "protected parent|nonexistent child|0600"
    - from: scripts/phase35-correlated-evidence-test.sh
      to: scripts/phase35-correlated-evidence.sh
      via: "A fresh isolated/runfiles supervisor process exercises the existing production root contract without substituting or changing production helpers"
      pattern: "run_isolated_supervisor|evidence_root_already_exists|file_mode"
    - from: .codex/tasks/todo.md
      to: .planning/STATE.md
      via: "The pending diagnostic preserves the recorded Phase 35 attempt-10 blocker and explicitly grants no hardware authority"
      pattern: "connection|request|response|body|schema"
    - from: .codex/tasks/lessons.md
      to: scripts/phase35-correlated-evidence-test.sh
      via: "Durable lessons name the exact runfiles, typed-failure, runtime-capacity, and HTTP-readiness regressions already observed in Phase 35"
      pattern: "runfiles|typed|stack|response"
---

# Quick Task 260719-e8b: Persist Phase 35 Lessons and Guardrails

## Goal

Convert the approved Phase 35 retrospective into durable repository and global guardrails, then close the remaining protected-root regression gap without changing evidence truth or authorizing another external attempt.

## Context

- `.planning/STATE.md`
- `AGENTS.md`
- `AGENTS.bright-builds.md`
- `standards-overrides.md`
- `standards/index.md`
- `standards/core/verification.md`
- `standards/core/testing.md`
- `standards/languages/rust.md`
- `.codex/tasks/lessons.md`
- `.codex/tasks/todo.md`
- `scripts/phase35-correlated-evidence-test.sh`
- `scripts/phase35-correlated-evidence-root.sh`
- `/Users/peterryszkiewicz/.codex/tasks/lessons.md`

Local guidance and the Bright Builds verification/testing/Rust standards materially constrain this plan: task files remain append-only where required, tests exercise the real shell boundary with Arrange/Act/Assert structure, and every repository commit follows the exact Rust gate before the focused shell/Bazel/reference/parity/lifecycle/diff/redaction gates.

## Tasks

<tasks>

<task type="auto">
  <name>Task 1: Persist repository lessons, guidance, and the diagnostic todo</name>
  <files>.codex/tasks/lessons.md, AGENTS.md, .codex/tasks/todo.md</files>
  <action>Make only localized append/update edits. In `.codex/tasks/lessons.md`, append four stable timestamped four-field lesson blocks for protected-root ownership, earliest typed-failure precedence, ESP-IDF main-task runtime capacity, and HTTP route/liveness markers not proving response readiness. Extend only the existing `lesson-cross-process-tests-use-real-boundaries` section to cover Bazel/runfiles execution, sibling helper/tool resolution, nested build-tool avoidance, and fresh real-process fixtures; retain its stable ID and do not create a duplicate runfiles lesson. Each lesson must contain the numbered Date, What went wrong, Preventive rule, and Trigger signal fields, use category-level wording, and contain no raw target, origin, hostname, network, device, credential, process, or secret value.

Append repo-local guidance to `AGENTS.md` outside the Bright Builds managed block. Require callers of protected evidence workflows to create one mode-0700 parent, prove the supervisor-owned child does not exist immediately before launch, and place wrapper stdout/stderr in separately created mode-0600 sibling files so redirection cannot pre-create the child. Require the supervisor to reject any existing child before package admission, detector discovery, credential access, or effects. State that Phase 35 may not make another hardware attempt at the same ambiguous HTTP boundary until a separate scoped plan implements and software-verifies redacted pre-mutation instrumentation; do not authorize that instrumentation or hardware here.

Append one pending stable timestamped `.codex/tasks/todo.md` block for a separately scoped HTTP diagnostic. Its checklist must distinguish TCP/TLS connection establishment as applicable, request transmission completion, response status/header receipt, body-byte receipt/completion, and JSON plus hostname-schema parsing, while recording only redacted categories, booleans, counts, and bounded durations. Explicitly state that the todo authorizes no hardware, request retry, PATCH, reboot, credential access, evidence promotion, or Phase 35 truth change. Preserve all unrelated lesson/todo blocks and the managed `AGENTS.md` block byte-for-byte.

Before the first repository commit, run the exact Rust commands in order: `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, then `cargo test --all-features`. Then run Bash syntax, `shfmt -d`, `shellcheck`, `bazel test //scripts:phase35_correlated_evidence_test`, `just verify-reference`, `just parity`, Phase 35 lifecycle validation, `git diff --check`, and a mode-0600 temporary-file redaction scan of only added lines through `scripts/phase28.1.1-promoted-evidence-denylist.sh`. Commit exactly these three repository files as one local commit with message `docs(quick-260719-e8b): persist Phase 35 lessons and guardrails`. Do not stage the global lesson file or quick-plan artifacts and do not push.</action>
  <verify>
    <automated>cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; bash -n scripts/phase35-correlated-evidence-root.sh scripts/phase35-correlated-evidence-test.sh &amp;&amp; shfmt -d scripts/phase35-correlated-evidence-root.sh scripts/phase35-correlated-evidence-test.sh &amp;&amp; shellcheck scripts/phase35-correlated-evidence-root.sh scripts/phase35-correlated-evidence-test.sh &amp;&amp; bazel test //scripts:phase35_correlated_evidence_test &amp;&amp; just verify-reference &amp;&amp; just parity &amp;&amp; node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 35 --expect-id 35-2026-07-17T17-00-37 --expect-mode yolo --require-plans --raw &amp;&amp; diff &lt;(sed -n '/&lt;!-- bright-builds-rules-managed:begin --&gt;/,/&lt;!-- bright-builds-rules-managed:end --&gt;/p' AGENTS.md) &lt;(git show HEAD:AGENTS.md | sed -n '/&lt;!-- bright-builds-rules-managed:begin --&gt;/,/&lt;!-- bright-builds-rules-managed:end --&gt;/p') &amp;&amp; git diff --check &amp;&amp; bash -c 'set -euo pipefail; scan="$(mktemp)"; trap '\''rm -f "$scan"'\'' EXIT; chmod 600 "$scan"; git diff --unified=0 -- AGENTS.md .codex/tasks/lessons.md .codex/tasks/todo.md | awk '\''substr($0,1,4) != "+++ " &amp;&amp; substr($0,1,1) == "+" { print substr($0,2) }'\'' &gt;"$scan"; scripts/phase28.1.1-promoted-evidence-denylist.sh "$scan"'</automated>
  </verify>
  <done>The five repository-applicable themes, protected-root/no-ambiguous-retry guidance, and non-authorizing diagnostic todo are durable in one atomic local commit with all mandatory gates passing.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce protected-root ownership through the hermetic shell boundary</name>
  <files>scripts/phase35-correlated-evidence-test.sh</files>
  <action>Extend only `scripts/phase35-correlated-evidence-test.sh` with focused Arrange/Act/Assert hermetic tests of the existing production contract. Do not modify `scripts/phase35-correlated-evidence-root.sh`, `scripts/phase35-correlated-evidence.sh`, or any other production file.

First, pre-create the exact requested child with a sentinel and record its metadata/digest, invoke the isolated/runfiles supervisor as a fresh process, and prove it returns typed `evidence_root_already_exists` before package admission, detector invocation, credential access, or any effect. Prove the child and sentinel remain unmodified after rejection.

Second, have the caller create a mode-0700 protected parent, assert the intended supervisor-owned child is absent immediately before launch, and create separate mode-0600 sibling stdout/stderr files before invoking the isolated/runfiles supervisor once. Prove sibling-before-child ordering from those explicit pre-launch assertions and the post-run artifacts, then prove the supervisor-created child and every directory beneath it are mode 0700 and every file beneath it is mode 0600. Exercise the existing production root contract through the fresh supervisor process rather than directly calling or replacing its helper. Preserve and continue running the existing nested-runner/direct-flash boundary, opaque credential-path ordering, classifier typed-failure precedence, ESP-IDF stack-capacity, complete failure matrix, and success-ordering tests.

Before the second repository commit, repeat the exact Rust sequence in the same order, followed by Bash syntax, `shfmt -d`, `shellcheck`, the focused Phase 35 Bazel target, reference, parity, exact Phase 35 lifecycle, diff, and added-line redaction gates. Review the one-file test diff and confirm the existing production helper plus all Phase 35 evidence/checklist/SUMMARY/verification truth remain unchanged. Commit exactly `scripts/phase35-correlated-evidence-test.sh` as one local commit with message `test(quick-260719-e8b): enforce protected Phase 35 root ownership`. Do not push.</action>
  <verify>
    <automated>cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; bash -n scripts/phase35-correlated-evidence-root.sh scripts/phase35-correlated-evidence-test.sh &amp;&amp; shfmt -d scripts/phase35-correlated-evidence-root.sh scripts/phase35-correlated-evidence-test.sh &amp;&amp; shellcheck scripts/phase35-correlated-evidence-root.sh scripts/phase35-correlated-evidence-test.sh &amp;&amp; bazel test //scripts:phase35_correlated_evidence_test &amp;&amp; just verify-reference &amp;&amp; just parity &amp;&amp; node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 35 --expect-id 35-2026-07-17T17-00-37 --expect-mode yolo --require-plans --raw &amp;&amp; git diff --check &amp;&amp; bash -c 'set -euo pipefail; scan="$(mktemp)"; trap '\''rm -f "$scan"'\'' EXIT; chmod 600 "$scan"; git diff --unified=0 -- scripts/phase35-correlated-evidence-test.sh | awk '\''substr($0,1,4) != "+++ " &amp;&amp; substr($0,1,1) == "+" { print substr($0,2) }'\'' &gt;"$scan"; scripts/phase28.1.1-promoted-evidence-denylist.sh "$scan"' &amp;&amp; test "$(git diff --name-only HEAD -- . ':!.planning/quick/260719-e8b-persist-phase-35-lessons-and-guardrails/260719-e8b-PLAN.md' | wc -l | tr -d ' ')" -eq 1 &amp;&amp; test "$(git diff --name-only HEAD -- scripts/phase35-correlated-evidence-test.sh)" = "scripts/phase35-correlated-evidence-test.sh"</automated>
  </verify>
  <done>Both new fresh-process regressions prove the existing production contract's pre-effect rejection, sibling-before-child ordering, and exact permissions, and the second atomic local commit contains only the Phase 35 test script.</done>
</task>

<task type="auto">
  <name>Task 3: Append and separately review the two global-only lessons</name>
  <files>/Users/peterryszkiewicz/.codex/tasks/lessons.md</files>
  <action>Append exactly two stable timestamped four-field lesson blocks to the global ledger: one requiring diagnostic completeness before costly, scarce, or one-shot external attempts, and one warning that zsh's lowercase special array parameter `path` is tied to `PATH`, so assigning a local variable named `path` mutates command lookup. For the diagnostic lesson, require separately planned, redacted, pre-effect instrumentation that distinguishes the unresolved boundaries before consuming the attempt; do not mention project-specific raw values or authorize hardware. For the zsh lesson, require non-special names such as `target_path` or `candidate_path`, plus a command-availability sanity check after shell helper changes.

Review only the two appended global blocks separately from Git, confirm the numbered Date/What went wrong/Preventive rule/Trigger signal format, and run the existing denylist against a mode-0600 temporary extraction of those blocks. Do not copy either theme into repo lessons, do not add `.planning/LEARNINGS.md`, do not enable GSD global learnings, do not stage or commit the global file, and do not push. Finally verify the repository has exactly the two intended implementation commits from Tasks 1 and 2 and that no hardware, HTTP request, detector, credential, direct UART/pin, archived-lineage, evidence-promotion, checklist, Phase 35 summary, or verification-truth action occurred.</action>
  <verify>
    <automated>rg -n '^## lesson-diagnostic-completeness-before-one-shot-attempt \| 2026-07-19 [0-9]{2}:[0-9]{2}$' "$HOME/.codex/tasks/lessons.md" &amp;&amp; rg -n '^## lesson-zsh-lowercase-path-mutates-path \| 2026-07-19 [0-9]{2}:[0-9]{2}$' "$HOME/.codex/tasks/lessons.md" &amp;&amp; bash -c 'set -euo pipefail; scan="$(mktemp)"; trap '\''rm -f "$scan"'\'' EXIT; chmod 600 "$scan"; awk '\''/^## lesson-diagnostic-completeness-before-one-shot-attempt / { emit=1 } emit { print }'\'' "$HOME/.codex/tasks/lessons.md" &gt;"$scan"; for field in "1. Date:" "2. What went wrong:" "3. Preventive rule:" "4. Trigger signal to catch it earlier:"; do test "$(rg -F -c "$field" "$scan")" -eq 2; done; scripts/phase28.1.1-promoted-evidence-denylist.sh "$scan"' &amp;&amp; ! git ls-files --error-unmatch "$HOME/.codex/tasks/lessons.md" &gt;/dev/null 2&gt;&amp;1 &amp;&amp; test "$(git log -2 --format=%s | rg -c '^(docs|test)\(quick-260719-e8b\):')" -eq 2 &amp;&amp; test "$(git log -2 --format=%s | rg -c '^docs\(quick-260719-e8b\): persist Phase 35 lessons and guardrails$')" -eq 1 &amp;&amp; test "$(git log -2 --format=%s | rg -c '^test\(quick-260719-e8b\): enforce protected Phase 35 root ownership$')" -eq 1 &amp;&amp; test ! -e .planning/LEARNINGS.md &amp;&amp; git diff --check</automated>
  </verify>
  <done>Both global themes exist only in the external lesson ledger, pass separate format/redaction review, remain outside Git, and the repository has exactly the two approved local implementation commits with no push or scope expansion.</done>
</task>

</tasks>

## Threat Model

| Threat | Disposition | Mitigation |
| --- | --- | --- |
| A pre-existing requested evidence child is reused and later effects contaminate sealed evidence. | mitigate | Preserve the existing typed pre-admission rejection and prove through a fresh isolated process that package, detector, credentials, and effects remain untouched while the child remains unmodified. |
| Wrapper redirection creates or weakens the supervisor-owned evidence root. | mitigate | Mode-0700 parent plus independently pre-created mode-0600 sibling output; the child remains absent until the supervisor creates it. |
| Raw local or sensitive values enter durable lessons, guidance, todos, or test fixtures. | mitigate | Category-only wording, synthetic fixtures, added-line scans through the existing denylist, and no hardware/credential access. |
| Later errors overwrite the earliest typed cause and route recovery incorrectly. | mitigate | Preserve the established typed-classifier regression and record earliest-failure precedence as a durable lesson. |
| Managed Bright Builds guidance or unrelated append-only task history is rewritten. | mitigate | Localized section edits, a byte comparison of the managed block, and no whole-file normalization. |
| A pending diagnostic todo is misread as authority for another hardware attempt. | mitigate | Explicit non-authorization language in both AGENTS guidance and the todo; implementation and hardware remain separate future scope. |

## Verification

- Each repository commit runs the exact Rust sequence in order before shell syntax/format/lint, focused Bazel, reference, parity, lifecycle, diff, and redaction checks.
- `bazel test //scripts:phase35_correlated_evidence_test` retains every existing Phase 35 regression and adds both protected-root ownership cases without a production change.
- The managed `AGENTS.md` block, Phase 35 production scripts, planning/evidence/checklist/SUMMARY/verification artifacts, and archived milestone lineage remain unchanged.
- Git history contains two atomic local implementation commits and no push; the global lesson file is reviewed and remains outside Git.

## Completion Boundary

This quick task ends after durable lessons/guardrails, the pending non-authorizing diagnostic todo, two protected-root regressions, two atomic local repository commits, and the separately reviewed global lesson append. It performs no hardware action, HTTP diagnostic implementation or request, detector run, credential access, Phase 35 evidence admission or truth change, direct UART/pin work, archived-lineage work, `.planning/LEARNINGS.md` creation, GSD global-learnings enablement, or push.
