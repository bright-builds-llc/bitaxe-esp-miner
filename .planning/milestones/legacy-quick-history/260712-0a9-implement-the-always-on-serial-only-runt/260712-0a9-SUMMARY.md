---
quick_id: 260712-0a9
status: complete
completed_at: "2026-07-12T06:00:00Z"
---

# Quick Task 260712-0a9 Summary

## Result

Implemented the always-on serial-only runtime heartbeat and its Plan 13 validation boundary.

- Normal firmware starts one boot-lifetime observer immediately after logger initialization.
- The host-tested pure model emits the exact shared-session heartbeat, coalesces delayed wakes, transitions from 1-second to 10-second cadence, and latches listener readiness.
- One canonical ESP-IDF uptime adapter now serves heartbeat, runtime snapshot, and HTTP telemetry reads.
- Heartbeats use `log::info!` only and remain outside retained/API/WebSocket logs; accepted-state replay remains independently scheduled in the same observer.
- Reinit proof remains strict, while retained cold start may combine same-session dedicated evidence with a `listener_armed=true` heartbeat.
- Typed comparator codes map directly to a closed heartbeat blocker vocabulary, and promoted summaries contain categories and counts only.
- Phase 13 monitor completion now uses a private atomic status handoff, so an already-exited fast process is never mistaken for a live timeout while real process-group and descendant cleanup remains fail-closed.

## Commits

- `e122ac1` — `feat(runtime): add serial boot heartbeat`
- `b032b1e` — `feat(plan13): validate runtime heartbeat evidence`
- `1af3f60` — `test(plan13): harden heartbeat verification gates`
- `a38bb0f` — `fix(serial): close fast monitor completion race`

## Verification

- Shellcheck and shfmt passed for all touched shell scripts.
- Seven focused heartbeat model tests passed.
- Firmware observer ownership, lifecycle comparator, state authority, diagnostic adapter, 84-case exact-head, and Phase 13 monitor suites passed directly.
- The monitor regression includes ten repeated immediate exits, a truly live timeout, timeout descendant cleanup, TERM cleanup, and orphan cleanup.
- Fresh `bazel test --nocache_test_results //scripts:phase13_monitor_capture_test` passed.
- All six affected Bazel test targets passed.
- Canonical ESP32-S3 firmware build passed.
- Pinned reference cleanliness, promoted-evidence denylist, Phase 30 immutability, and protected-artifact checks passed.
- Mandatory Rust sequence passed in order before each commit: format, Clippy with warnings denied, all-target/all-feature build, and all-feature tests.
- `git diff --check` passed.

## Hardware Boundary

No detector, board-info, credential access, flash, monitor, reset, network discovery, real-hardware action, or push was performed. The root workflow owns the fresh exact-HEAD commit/push decision and the single detector-gated Plan 13 hardware chain.

## Root Hardware Result

The root workflow pushed exact HEAD `e622253d2fc4aea4589e0dcf5524081b6b054aaf` and ran the single detector-gated Plan 13 chain. Reflash/reinit passed strict original-marker, dedicated-evidence, and heartbeat validation across both cadence phases. The retained cold-start lifecycle then passed watcher arming, USB appearance, passive attachment, stable ownership, bounded capture, and cleanup, but the raw application payload was zero bytes. The typed validator therefore failed closed with absent heartbeat/evidence and the terminal outcome `blocked_safe_evidence_invalid`.

The heartbeat implementation is verified and observable when monitoring spans boot, but late-attached native USB still lacks positive application-byte delivery. No retry was performed; detailed traces remain private and cleanup left zero processes and holders.
