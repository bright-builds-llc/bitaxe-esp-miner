---
quick_id: 260704-g0i
mode: quick
status: planned
created_at: "2026-07-04T16:31:49Z"
---

# Quick Task 260704-g0i: Debug and fix PATCH /api/system timeout blocking Phase 21 pool input bridge

## Goal

Fix the live `PATCH /api/system` timeout that blocks Phase 21 pool-input bridge verification, while preserving the upstream-compatible empty success body and all local redaction rules.

## Tasks

### 1. Firmware PATCH Response And Retained Markers

Files: `firmware/bitaxe/src/http_api.rs`, `firmware/bitaxe/src/controlled_mining_runtime.rs`, `crates/bitaxe-api/src/settings.rs`

Action: Add non-secret retained settings PATCH markers, move settings effects into a bounded post-response task, mirror Phase 21 controlled-runtime markers into `/api/system/logs`, and add a redacted `phase21_pool_settings_consumed=true source=settings_patch` marker only after settings-derived pool config reaches the controlled runtime.

Verify: Focused Cargo tests for settings and controlled runtime; grep confirms no raw pool/device values are logged.

### 2. Pool Input Bridge Helper And Wrapper Gate

Files: `scripts/phase21-pool-input-bridge.sh`, `scripts/phase21-pool-input-bridge-test.sh`, `scripts/phase21-live-mining-evidence.sh`, `scripts/phase21-live-mining-evidence-test.sh`, `scripts/BUILD.bazel`

Action: Add a repo-owned helper that loads JSON credentials, PATCHes the settings route with redacted artifacts only, polls `/api/system/logs` for the consumed marker, and make the Phase 21 live wrapper block before live probes unless the bridge is applied.

Verify: Shell tests cover missing credentials, env-only rejection, curl timeout, HTTP success without consumed marker, HTTP success with marker, and redaction.

### 3. GSD Artifacts And Final Verification

Files: `.planning/quick/260704-g0i-debug-and-fix-patch-api-system-timeout-b/`, `.planning/STATE.md`

Action: Record the quick-task summary and update project state after verification. Run focused checks, hardware verification when detector prerequisites pass, `git diff --check`, and the Rust pre-commit gate before committing locally.

Verify: Summary records exact commands run and any hardware blocker if the device path cannot be safely completed.
