---
phase: 01-foundation-and-gamma-601-boot-log
plan: "09"
type: execute
wave: 7
depends_on:
  - "07"
  - "08"
files_modified:
  - Justfile
  - docs/parity/evidence/phase-01-gamma-601-boot-log.md
  - docs/parity/checklist.md
autonomous: false
requirements:
  - FND-06
  - FND-07
  - FND-08
  - FND-09
  - FND-11
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 1-2026-06-21T00-30-20
generated_at: "2026-06-21T00:55:00Z"
must_haves:
  truths:
    - "Developer can run every required Phase 1 Just command."
    - "Just commands delegate to Bazel-visible targets or repo-owned scripts represented in Bazel."
    - "`just package` and `just parity` route through Bazel targets that run the reference cleanliness guard before trusted evidence is produced."
    - "Gamma 601 hardware smoke is captured when hardware is connected, or remains explicit missing evidence."
  artifacts:
    - path: "Justfile"
      provides: "Human command surface for build, test, package, flash, monitor, flash-monitor, verify-reference, and parity"
    - path: "docs/parity/evidence/phase-01-gamma-601-boot-log.md"
      provides: "Hardware-smoke evidence record or missing-evidence note"
    - path: "docs/parity/checklist.md"
      provides: "Evidence ledger status updates after command and hardware verification"
  key_links:
    - from: "Justfile"
      to: "//firmware/bitaxe:firmware"
      via: "just build"
      pattern: "bazel build //firmware/bitaxe:firmware"
    - from: "Justfile"
      to: "//tools/flash:flash"
      via: "just flash and just flash-monitor"
      pattern: "bazel run //tools/flash:flash"
    - from: "Justfile"
      to: "//firmware/bitaxe:firmware_image"
      via: "just package, guarded package manifest generation, and default ELF flash image resolution"
      pattern: "firmware_image"
    - from: "Justfile"
      to: "//scripts:verify_reference_clean"
      via: "guarded //firmware/bitaxe:firmware_image and //tools/parity:report targets"
      pattern: "verify_reference_clean"
    - from: "docs/parity/evidence/phase-01-gamma-601-boot-log.md"
      to: "docs/parity/checklist.md"
      via: "hardware-smoke evidence reference"
      pattern: "phase-01-gamma-601-boot-log"
---

<objective>
Wire the public Phase 1 command surface and complete the verification gate.

Purpose: Phase 1 is only useful when a developer can operate the workflow through the promised Just commands and when hardware smoke is handled as evidence, not assumption.
Output: `Justfile`, aggregate verification, and Gamma 601 boot/log evidence path.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-RESEARCH.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-VALIDATION.md
@docs/project/seed-layout.md
@docs/project/first-milestone.md
@docs/parity/checklist.md
@PROVENANCE.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the required Just command surface</name>
  <read_first>
    - Justfile if it exists
    - scripts/BUILD.bazel
    - firmware/bitaxe/BUILD.bazel
    - tools/flash/BUILD.bazel
    - tools/parity/BUILD.bazel
    - docs/project/seed-layout.md
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
  </read_first>
  <files>Justfile</files>
  <action>
    Create `Justfile` per D-04, D-11, and D-12 with recipes exactly named `build`, `test`, `package`, `flash`, `monitor`, `flash-monitor`, `verify-reference`, and `parity`. `build` must run `bazel build //firmware/bitaxe:firmware`. `test` must run `bazel test //...`. `package` must run `bazel build //firmware/bitaxe:firmware_image`, whose Plan 08 target runs `//scripts:verify_reference_clean` before package manifest generation. `verify-reference` must run `bazel run //scripts:verify_reference_clean`. `parity` must run `bazel run //tools/parity:report -- report --checklist docs/parity/checklist.md --fail-on-invalid-verified`, whose Plan 07 target runs `//scripts:verify_reference_clean` before report generation. Dirty, missing, or unpinned reference state must block both `just package` and `just parity` before trusted package/parity evidence is emitted. `flash *args`, `monitor *args`, and `flash-monitor *args` must delegate to `bazel run //tools/flash:flash -- flash {{args}}`, `bazel run //tools/flash:flash -- monitor {{args}}`, and `bazel run //tools/flash:flash -- flash-monitor {{args}}` so documented calls such as `just flash board=601 port=/dev/cu.usbmodem101`, `just monitor port=/dev/cu.usbmodem101`, and `just flash-monitor board=601 port=/dev/cu.usbmodem101` work through the key/value aliases implemented by Plan 08. Do not call `cargo`, `espflash`, or shell snippets directly from Just for these workflows; Just must remain a thin wrapper over Bazel-visible targets.
  </action>
  <verify>
    <automated>just --list && just verify-reference && bazel query 'deps(//firmware/bitaxe:firmware_image)' | grep '//scripts:verify_reference_clean' && bazel query 'deps(//tools/parity:report)' | grep '//scripts:verify_reference_clean' && just build && just test && just package && just parity</automated>
  </verify>
  <acceptance_criteria>
    - `just --summary` includes `build test package flash monitor flash-monitor verify-reference parity`.
    - `grep -q 'bazel build //firmware/bitaxe:firmware' Justfile` succeeds.
    - `grep -q 'bazel build //firmware/bitaxe:firmware_image' Justfile` succeeds.
    - `grep -q 'bazel run //scripts:verify_reference_clean' Justfile` succeeds.
    - `grep -q 'bazel run //tools/flash:flash' Justfile` succeeds.
    - `grep -q 'bazel run //tools/parity:report' Justfile` succeeds.
    - `bazel query 'deps(//firmware/bitaxe:firmware_image)' | grep '//scripts:verify_reference_clean'` succeeds.
    - `bazel query 'deps(//tools/parity:report)' | grep '//scripts:verify_reference_clean'` succeeds.
    - Existing package/parity tests prove missing or dirty `reference/esp-miner` blocks `just package` and `just parity` evidence paths before trusted output is written.
  </acceptance_criteria>
  <done>All required public commands exist and route through Bazel-visible targets, with package and parity evidence protected by the reference guard.</done>
</task>

<task type="auto">
  <name>Task 2: Prepare hardware-smoke evidence record</name>
  <read_first>
    - docs/parity/checklist.md
    - docs/parity/evidence/phase-01-gamma-601-boot-log.md if it exists
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-VALIDATION.md
    - docs/project/first-milestone.md
  </read_first>
  <files>docs/parity/evidence/phase-01-gamma-601-boot-log.md</files>
  <action>
    Create `docs/parity/evidence/phase-01-gamma-601-boot-log.md` with headings `Command`, `Board`, `Port`, `Firmware Commit`, `Reference Commit`, `Manifest Path`, `Default Flash Image`, `Log Path`, `Observed Required Log Lines`, and `Conclusion`. Include the exact command template `just flash-monitor board=601 port=<port> evidence-dir=docs/parity/evidence/phase-01-gamma-601-boot-log` and required log patterns `bitaxe-rust boot: board=Gamma 601 asic=BM1370`, `safe_state: mining=disabled asic_work_submission=disabled hardware_control=disabled`, `reset_reason=`, `partition=` or `image_partition=`, `platform_status=` or `psram_status=`, `firmware_commit=` with an actual commit or `firmware_commit=Unavailable`, and `reference_commit=c1915b0a63bfabebdb95a515cedfee05146c1d50`. The `Default Flash Image` field must describe the manifest default as `bitaxe-gamma601.elf`; `bitaxe-gamma601-factory.bin` may be listed only as an additional package artifact, not the default `espflash flash` input. Run `espflash list-ports` as a real precheck: if it exits nonzero, fail the task with the espflash error and do not record a missing-hardware conclusion; if it exits successfully and reports no compatible Gamma 601 serial port, record `Conclusion: missing hardware-smoke evidence - no Gamma 601 serial port visible` and do not mark the related checklist rows `verified`.
  </action>
  <verify>
    <automated>espflash list-ports && grep -q 'just flash-monitor board=601 port=<port>' docs/parity/evidence/phase-01-gamma-601-boot-log.md && grep -q 'bitaxe-rust boot: board=Gamma 601 asic=BM1370' docs/parity/evidence/phase-01-gamma-601-boot-log.md && grep -q 'safe_state: mining=disabled asic_work_submission=disabled hardware_control=disabled' docs/parity/evidence/phase-01-gamma-601-boot-log.md && grep -q 'reset_reason=' docs/parity/evidence/phase-01-gamma-601-boot-log.md && grep -Eq 'partition=|image_partition=' docs/parity/evidence/phase-01-gamma-601-boot-log.md && grep -Eq 'platform_status=|psram_status=' docs/parity/evidence/phase-01-gamma-601-boot-log.md && grep -q 'firmware_commit=' docs/parity/evidence/phase-01-gamma-601-boot-log.md && grep -q 'reference_commit=c1915b0a63bfabebdb95a515cedfee05146c1d50' docs/parity/evidence/phase-01-gamma-601-boot-log.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Command' docs/parity/evidence/phase-01-gamma-601-boot-log.md` succeeds.
    - `grep -q 'Gamma 601' docs/parity/evidence/phase-01-gamma-601-boot-log.md` succeeds.
    - `grep -q 'BM1370' docs/parity/evidence/phase-01-gamma-601-boot-log.md` succeeds.
    - `grep -q 'reset_reason=' docs/parity/evidence/phase-01-gamma-601-boot-log.md` succeeds.
    - `grep -Eq 'partition=|image_partition=' docs/parity/evidence/phase-01-gamma-601-boot-log.md` succeeds.
    - `grep -Eq 'platform_status=|psram_status=' docs/parity/evidence/phase-01-gamma-601-boot-log.md` succeeds.
    - `grep -q 'firmware_commit=' docs/parity/evidence/phase-01-gamma-601-boot-log.md` succeeds.
    - `grep -q 'reference_commit=c1915b0a63bfabebdb95a515cedfee05146c1d50' docs/parity/evidence/phase-01-gamma-601-boot-log.md` succeeds.
    - `grep -q 'Default Flash Image' docs/parity/evidence/phase-01-gamma-601-boot-log.md` succeeds.
    - `grep -q 'bitaxe-gamma601.elf' docs/parity/evidence/phase-01-gamma-601-boot-log.md` succeeds.
  </acceptance_criteria>
  <done>The hardware smoke evidence path exists and preserves missing-evidence semantics when no board is connected.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Gamma 601 flash-monitor smoke</name>
  <read_first>
    - Justfile
    - docs/parity/evidence/phase-01-gamma-601-boot-log.md
    - docs/parity/checklist.md
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
  </read_first>
  <files>docs/parity/evidence/phase-01-gamma-601-boot-log.md, docs/parity/checklist.md</files>
  <action>
    If a Gamma 601 is connected, run `espflash list-ports`, choose the correct port, then run `just flash-monitor board=601 port=<port> evidence-dir=docs/parity/evidence/phase-01-gamma-601-boot-log`. Capture the command, board, port, firmware commit, reference commit, manifest path, default flash image path, log path, observed output, and conclusion in `docs/parity/evidence/phase-01-gamma-601-boot-log.md`. The default flash image path must be the manifest `default_flash_image` ELF (`bitaxe-gamma601.elf`), not the optional factory `.bin`. Update only the relevant boot/log checklist rows to `verified` when the captured log contains all required identity, status, commit, partition/image, reset, platform/PSRAM, reference, and safe-state lines; leave ASIC init, voltage, fan, thermal, power, and mining rows below `verified`. If hardware is not available, leave the evidence document conclusion as missing hardware-smoke evidence and do not mark boot/log rows verified. If `espflash list-ports` returns an error rather than an empty compatible-port list, stop and fix the espflash setup instead of recording missing hardware evidence.
  </action>
  <verify>
    <automated>espflash list-ports</automated>
  </verify>
  <acceptance_criteria>
    - Evidence contains `bitaxe-rust boot: board=Gamma 601 asic=BM1370` before boot/log rows are marked `verified`.
    - Evidence contains `safe_state: mining=disabled asic_work_submission=disabled hardware_control=disabled` before boot/log rows are marked `verified`.
    - Evidence contains `reset_reason=` before boot/log rows are marked `verified`.
    - Evidence contains either `partition=` or `image_partition=` before boot/log rows are marked `verified`.
    - Evidence contains either `platform_status=` or `psram_status=` before boot/log rows are marked `verified`.
    - Evidence contains `firmware_commit=` with an actual commit or `firmware_commit=Unavailable` before boot/log rows are marked `verified`.
    - Evidence contains `reference_commit=c1915b0a63bfabebdb95a515cedfee05146c1d50` before boot/log rows are marked `verified`.
    - Evidence records `Default Flash Image` as `bitaxe-gamma601.elf`, not `bitaxe-gamma601-factory.bin`, before flash evidence is trusted.
    - `docs/parity/checklist.md` does not mark ASIC init, voltage, fan, thermal, power, or mining rows `verified` in this phase.
  </acceptance_criteria>
  <what-built>Complete Phase 1 build/package/flash/monitor workflow plus safe Gamma 601 boot/log firmware.</what-built>
  <how-to-verify>
    1. Connect the Gamma 601 over USB.
    2. Run `espflash list-ports` and select the Gamma 601 port.
    3. Run `just flash-monitor board=601 port=<port> evidence-dir=docs/parity/evidence/phase-01-gamma-601-boot-log`.
    4. Confirm the log contains `bitaxe-rust boot: board=Gamma 601 asic=BM1370`, `safe_state: mining=disabled asic_work_submission=disabled hardware_control=disabled`, `reset_reason=`, `partition=` or `image_partition=`, `platform_status=` or `psram_status=`, `firmware_commit=` with an actual commit or `Unavailable`, and `reference_commit=c1915b0a63bfabebdb95a515cedfee05146c1d50`.
  </how-to-verify>
  <resume-signal>Type `approved` if the smoke output matches, or describe the missing/incorrect log lines.</resume-signal>
  <done>Gamma 601 hardware smoke is recorded when hardware exists; otherwise missing evidence is explicit and no false verification is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user -> Just commands | User arguments flow through Just into Bazel-backed tooling. |
| firmware logs -> evidence ledger | Observed hardware output can update parity status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering/Repudiation | `reference/esp-miner` | mitigate | `just package` and `just parity` route through Bazel targets that run `//scripts:verify_reference_clean` before package/parity evidence is produced. |
| T-06-02 | Tampering/Elevation of Privilege | `Justfile` flash args | mitigate | Just forwards raw args to `tools/flash`, which Plan 08 parses as typed args and executes through `Command` vectors. |
| T-06-03 | Repudiation | Package evidence | mitigate | `just package` builds `//firmware/bitaxe:firmware_image`; hardware evidence records manifest path and commits. |
| T-06-04 | Spoofing/Repudiation | Checklist verification | mitigate | Boot/log rows are verified only after captured hardware-smoke evidence contains required log lines; unavailable hardware stays missing evidence. |
| T-06-05 | Safety | Boot logs and hardware control | mitigate | Hardware smoke acceptance requires the exact safe no-mining/no-control log and forbids marking mining or hardware-control rows verified. |
</threat_model>

<verification>
Run `just verify-reference && bazel query 'deps(//firmware/bitaxe:firmware_image)' | grep '//scripts:verify_reference_clean' && bazel query 'deps(//tools/parity:report)' | grep '//scripts:verify_reference_clean' && just build && just test && just package && just parity`. Run `espflash list-ports` and fail on unexpected espflash errors. With hardware connected, run `just flash-monitor board=601 port=<port> evidence-dir=docs/parity/evidence/phase-01-gamma-601-boot-log`.
</verification>

<success_criteria>
FND-07 is complete when the command surface works through Bazel-backed targets. FND-06/FND-08/FND-09/FND-11 phase verification is complete when automated commands pass and the Gamma 601 smoke outcome is recorded as either verified evidence or explicit missing hardware evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-gamma-601-boot-log/01-09-SUMMARY.md`.
</output>
