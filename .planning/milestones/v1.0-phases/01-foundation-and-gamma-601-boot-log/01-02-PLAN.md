---
phase: 01-foundation-and-gamma-601-boot-log
plan: "02"
type: execute
wave: 1
depends_on:
  - "01"
files_modified:
  - Cargo.toml
  - rust-toolchain.toml
  - .cargo/config.toml
  - MODULE.bazel
autonomous: true
requirements:
  - FND-03
  - FND-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 1-2026-06-21T00-30-20
generated_at: "2026-06-21T00:55:00Z"
must_haves:
  truths:
    - "Developer can inspect one Cargo workspace root that owns Rust dependency versions."
    - "The ESP Rust toolchain and ESP32-S3 target configuration are pinned before packages are added."
    - "Bazel is prepared to mirror Cargo dependencies without replacing Cargo as the Rust dependency authority."
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace root, shared package metadata, and pinned workspace dependencies"
    - path: "rust-toolchain.toml"
      provides: "ESP Rust toolchain pin for firmware-capable builds"
    - path: ".cargo/config.toml"
      provides: "ESP32-S3 target linker/env configuration without forcing host tools to Xtensa"
    - path: "MODULE.bazel"
      provides: "Bzlmod rules_rust and crate_universe wiring"
  key_links:
    - from: "MODULE.bazel"
      to: "Cargo.toml"
      via: "rules_rust crate_universe manifest reference"
      pattern: "crate_universe"
    - from: ".cargo/config.toml"
      to: "firmware/bitaxe"
      via: "target.xtensa-esp32s3-espidf linker/env configuration consumed by Plan 06 firmware build"
      pattern: "xtensa-esp32s3-espidf"
---

<objective>
Create the Rust workspace root, ESP toolchain pin, and Bazel Rust dependency mirror contract.

Purpose: Later crate, firmware, and tool plans need one Rust dependency authority and a reproducible ESP32-S3 toolchain baseline before adding packages.
Output: Root Cargo workspace metadata, ESP target configuration, and Bzlmod crate mirror wiring.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-RESEARCH.md
@docs/project/seed-layout.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/verification.md
@standards/languages/rust.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Cargo workspace root and ESP target pins</name>
  <read_first>
    - Cargo.toml if it exists
    - rust-toolchain.toml if it exists
    - .cargo/config.toml if it exists
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-RESEARCH.md
    - docs/project/seed-layout.md
    - standards/languages/rust.md
  </read_first>
  <files>Cargo.toml, rust-toolchain.toml, .cargo/config.toml</files>
  <action>
    Create root `Cargo.toml` with `[workspace] resolver = "2"` and an empty `members = []` list; Plans 03, 04, and 05 must add members as each package directory is created so Cargo commands never reference missing packages. Set `[workspace.package] edition = "2021"` and `license = "MIT"`. Add `[workspace.dependencies]` pins: `anyhow = "1.0.102"`, `camino = "1.2.3"`, `clap = { version = "4.6.1", features = ["derive"] }`, `embuild = "0.33.1"`, `esp-idf-svc = "0.52.1"`, `esp-idf-sys = "0.37.2"`, `log = "0.4"`, `serde = { version = "1.0.228", features = ["derive"] }`, `serde_json = "1.0.150"`, `sha2 = "0.11.0"`, `tempfile = "3.27.0"`, and `thiserror = "2.0.18"` per D-06 and D-07. Create `rust-toolchain.toml` with `[toolchain] channel = "esp"` and `components = ["rust-src"]`. Create `.cargo/config.toml` with `[target.xtensa-esp32s3-espidf] linker = "ldproxy"` and `[env] MCU = "esp32s3"`; do not add a global `[build] target`, because host tools must remain host-targeted unless the firmware build script passes `--target xtensa-esp32s3-espidf`.
  </action>
  <verify>
    <automated>grep -q 'resolver = "2"' Cargo.toml && grep -q 'esp-idf-svc = "0.52.1"' Cargo.toml && grep -q 'channel = "esp"' rust-toolchain.toml && grep -q 'xtensa-esp32s3-espidf' .cargo/config.toml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'resolver = "2"' Cargo.toml` succeeds.
    - `grep -Fq 'members = []' Cargo.toml` succeeds.
    - `grep -q 'esp-idf-svc = "0.52.1"' Cargo.toml` succeeds.
    - `grep -q 'esp-idf-sys = "0.37.2"' Cargo.toml` succeeds.
    - `grep -q 'channel = "esp"' rust-toolchain.toml` succeeds.
    - `grep -q 'linker = "ldproxy"' .cargo/config.toml` succeeds.
    - `.cargo/config.toml` does not contain a global `[build] target = "xtensa-esp32s3-espidf"` setting.
  </acceptance_criteria>
  <done>The workspace root pins Phase 1 Rust, ESP-IDF, target, and dependency baseline without creating a second Rust dependency truth outside Cargo.</done>
</task>

<task type="auto">
  <name>Task 2: Add Bazel crate mirror wiring</name>
  <read_first>
    - MODULE.bazel
    - Cargo.toml
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-RESEARCH.md
    - docs/project/seed-layout.md
  </read_first>
  <files>MODULE.bazel</files>
  <action>
    Extend `MODULE.bazel` from Plan 01 with rules_rust `crate_universe` wiring against the root Cargo manifests and `Cargo.lock` per D-04 and D-06. Keep Bzlmod as the canonical Bazel module shape and do not introduce WORKSPACE-first dependency management. This plan prepares the mirror; Plan 03 creates the first package members and generates `Cargo.lock` before any Cargo/Bazel Rust tests are required.
  </action>
  <verify>
    <automated>grep -q 'crate_universe' MODULE.bazel && grep -q 'Cargo.toml' MODULE.bazel</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'bazel_dep(name = "rules_rust", version = "0.70.0")' MODULE.bazel` still succeeds.
    - `grep -q 'crate_universe' MODULE.bazel` succeeds.
    - `grep -q 'Cargo.toml' MODULE.bazel` succeeds.
    - `MODULE.bazel` does not add a `WORKSPACE` dependency path.
  </acceptance_criteria>
  <done>Bazel is ready to mirror Cargo dependencies once package members and `Cargo.lock` exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace manifests -> Bazel graph | Cargo dependency truth is mirrored into Bazel. |
| target config -> host tools | ESP target settings must not force host binaries to build for Xtensa. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering/Repudiation | `reference/esp-miner` | transfer | Plan 01 owns dirty/missing reference enforcement; this plan does not trust reference data directly. |
| T-02-02 | Tampering/Elevation of Privilege | Cargo/Bazel dependency graph | mitigate | Keep Cargo metadata authoritative and use Bzlmod/crate_universe rather than ad hoc dependency duplication. |
| T-02-03 | Repudiation | Toolchain provenance | mitigate | Pin Rust edition, ESP toolchain channel, ESP-IDF crate versions, target triple, and MCU in versioned files. |
| T-02-04 | Safety | Host tool target selection | mitigate | Avoid a global Cargo build target so host flash/parity tools do not accidentally build for firmware target. |
</threat_model>

<verification>
Run the task verification commands above. Cargo/Bazel Rust test execution begins after Plans 03-05 create package members and `Cargo.lock`.
</verification>

<success_criteria>
FND-03/FND-04 workspace-root coverage is complete when Cargo owns dependency pins, ESP32-S3 target configuration is explicit, and Bazel is wired to mirror Cargo without replacing it.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-gamma-601-boot-log/01-02-SUMMARY.md`.
</output>
