---
phase: 07-ota-filesystem-and-release-packaging
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - about.toml
  - about.hbs
  - docs/release/cargo-about.html
  - docs/release/ultra-205.md
  - docs/release/license-inventory.md
  - docs/release/provenance-manifest.md
  - docs/parity/evidence/phase-07-ota-filesystem-release.md
autonomous: true
requirements: [REL-05, REL-07, REL-08, REL-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 7-2026-06-28T13-30-15
generated_at: 2026-06-28T14:16:04Z
must_haves:
  truths:
    - "Release documentation has durable sections before implementation plans append evidence."
    - "License/provenance review covers Cargo and non-Cargo release inputs."
    - "REL-03 and REL-08 evidence states are visible without claiming verified parity."
  artifacts:
    - path: "about.toml"
      provides: "cargo-about policy input"
    - path: "docs/release/cargo-about.html"
      provides: "Generated Cargo dependency license report"
    - path: "docs/release/ultra-205.md"
      provides: "Operator guide structure"
    - path: "docs/parity/evidence/phase-07-ota-filesystem-release.md"
      provides: "Phase evidence record with package/live/hardware/gap sections"
  key_links:
    - from: "tools/parity release gate"
      to: "docs/release/license-inventory.md"
      via: "Plan 07-06 validation"
      pattern: "Cargo dependencies|Bazel|ESP-IDF|static assets"
---

<objective>
Create the Wave 0 release documentation, license, provenance, and evidence contracts.

Purpose: Ensure release claims, operator instructions, and compliance review have fixed sections before package, OTA, and recovery work starts adding evidence.
Output: cargo-about config/template plus release, license, provenance, and Phase 7 evidence documents.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-CONTEXT.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-VALIDATION.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-UI-SPEC.md
@PROVENANCE.md
@docs/adr/0013-mit-first-with-gpl-guardrails.md
@reference/esp-miner/readme.md
@reference/esp-miner/flashing.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add cargo-about release inventory config</name>
  <read_first>
    PROVENANCE.md
    docs/adr/0013-mit-first-with-gpl-guardrails.md
    .planning/phases/07-ota-filesystem-and-release-packaging/07-RESEARCH.md REL-05 and Environment Availability
  </read_first>
  <files>about.toml, about.hbs, docs/release/cargo-about.html</files>
  <action>Create `about.toml` and `about.hbs` for `cargo about generate about.hbs --output-file docs/release/cargo-about.html`. Configure accepted license identifiers for the current workspace only where compatible with `PROVENANCE.md`; do not globally bless unknown licenses. Include a `private.ignore = true` style setting only if `cargo-about 0.9.0` supports it locally; otherwise keep the config minimal and let the tool report unresolved crates. If `cargo about --version` is unavailable, install the pinned release tool with `cargo install cargo-about --locked --version 0.9.0` before generating the report. Add comments identifying `cargo-about 0.9.0` as the required Phase 7 tool from research.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test -f about.toml; test -f about.hbs; rg -q --fixed-strings "cargo-about" about.toml; rg -q --fixed-strings "accepted" about.toml; rg -q --fixed-strings "licenses" about.toml; rg -q --fixed-strings "PROVENANCE" about.toml about.hbs; rg -q --fixed-strings "GPL" about.toml about.hbs; cargo about generate about.hbs --output-file docs/release/cargo-about.html; test -s docs/release/cargo-about.html; rg -q --fixed-strings "Cargo" docs/release/cargo-about.html'</automated>
  </verify>
  <acceptance_criteria>
    The automated verify command checks file existence, independently checks `cargo-about`, `accepted`, `licenses`, `PROVENANCE`, and `GPL` with fixed-string `rg -q` searches, generates `docs/release/cargo-about.html`, and fails if the generated report is missing or empty.
    If `cargo-about` is missing, install `cargo-about 0.9.0` with `cargo install cargo-about --locked --version 0.9.0`; do not mark REL-05 complete without the generated Cargo dependency report.
  </acceptance_criteria>
  <done>Cargo license inventory generation has a checked-in config/template and does not silently replace the required non-Cargo inventory.</done>
</task>

<task type="auto">
  <name>Task 2: Create release operator guide structure</name>
  <read_first>
    .planning/phases/07-ota-filesystem-and-release-packaging/07-UI-SPEC.md
    reference/esp-miner/readme.md
    reference/esp-miner/flashing.md
  </read_first>
  <files>docs/release/ultra-205.md</files>
  <action>Create `docs/release/ultra-205.md` with required sections: `Artifact List`, `Local Build And Package`, `USB Flash`, `Monitor`, `Flash And Monitor`, `Firmware OTA`, `AxeOS OTAWWW Status`, `Recovery Page`, `Large Erase`, `Failed Firmware Update`, `Interrupted Static Update`, `Rollback And Boot Validation`, `Evidence Required Before Verified Claims`, and `Release Caveats`. Include exact command names `just build`, `just package`, `just flash board=205`, `just monitor`, and `just flash-monitor board=205`. Use UI spec names `esp-miner.bin`, `www.bin`, `/api/system/OTA`, `/api/system/OTAWWW`, `/recovery`, and `AxeOS`. State that OTAWWW static update is not available for verified parity until the D-16 evidence gate is closed.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; f=docs/release/ultra-205.md; test -f "$f"; for term in "Artifact List" "Local Build And Package" "USB Flash" "Monitor" "Flash And Monitor" "Firmware OTA" "AxeOS OTAWWW Status" "Recovery Page" "Large Erase" "Failed Firmware Update" "Interrupted Static Update" "Rollback And Boot Validation" "Evidence Required Before Verified Claims" "Release Caveats" "just build" "just package" "just flash board=205" "just monitor" "just flash-monitor board=205" "esp-miner.bin" "www.bin" "/api/system/OTA" "/api/system/OTAWWW" "/recovery" "AxeOS"; do rg -q --fixed-strings "$term" "$f"; done'</automated>
  </verify>
  <acceptance_criteria>
    The automated verify command checks every required release-guide section, command, artifact name, route, recovery topic, rollback topic, and interrupted-static-update topic independently.
  </acceptance_criteria>
  <done>The operator guide has all REL-07 topics as durable headings before implementation evidence is added.</done>
</task>

<task type="auto">
  <name>Task 3: Create provenance, license, and Phase 7 evidence records</name>
  <read_first>
    PROVENANCE.md
    docs/parity/checklist.md
    .planning/phases/07-ota-filesystem-and-release-packaging/07-VALIDATION.md
  </read_first>
  <files>docs/release/license-inventory.md, docs/release/provenance-manifest.md, docs/parity/evidence/phase-07-ota-filesystem-release.md</files>
  <action>Create `docs/release/license-inventory.md` with separate sections for Cargo crates, Bazel/rules, ESP-IDF/esp-rs components, espflash/esptool tooling, firmware/static assets, and release artifacts per D-19. Create `docs/release/provenance-manifest.md` with source commit, reference commit, static asset source, recovery page source, GPL review status, and release artifact review sections per D-20. Create `docs/parity/evidence/phase-07-ota-filesystem-release.md` with separate conclusions for package evidence, static/SPIFFS evidence, firmware OTA evidence, OTAWWW REL-03 gap, rollback/recovery evidence, interrupted-update evidence, license/provenance evidence, and final release-readiness status per D-18, D-22, and D-23. Use status wording `not run - hardware verification pending` where live hardware is not yet recorded; do not use `verified` in this initial evidence document.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; inv=docs/release/license-inventory.md; prov=docs/release/provenance-manifest.md; ev=docs/parity/evidence/phase-07-ota-filesystem-release.md; test -f "$inv"; test -f "$prov"; test -f "$ev"; test -s docs/release/cargo-about.html; for term in "Cargo crates" "cargo-about.html" "Bazel" "ESP-IDF" "espflash" "static assets" "release artifacts"; do rg -q --fixed-strings "$term" "$inv"; done; for term in "source commit" "reference commit" "static asset source" "recovery page source" "GPL review status" "release artifact review"; do rg -q --fixed-strings "$term" "$prov"; done; for term in "package evidence" "static/SPIFFS evidence" "firmware OTA evidence" "OTAWWW REL-03 gap" "rollback/recovery evidence" "interrupted-update evidence" "license/provenance evidence" "final release-readiness status" "not run - hardware verification pending"; do rg -q --fixed-strings "$term" "$ev"; done'</automated>
  </verify>
  <acceptance_criteria>
    The automated verify command checks required license-inventory, provenance-manifest, and evidence-rollup terms independently and fails if any required term is missing.
  </acceptance_criteria>
  <done>Release compliance and evidence files exist with the required separation between package, live firmware, hardware, and gap evidence.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dependency-to-release | Third-party dependency metadata becomes a release claim. |
| reference-to-release | GPL-covered upstream assets or behavior can enter release artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | R/T | License inventory | mitigate | Separate Cargo, Bazel, ESP-IDF, tools, static assets, and artifacts so Cargo-only reports cannot satisfy REL-05. |
| T-07-03-02 | R/I | Provenance manifest | mitigate | Require source commit, reference commit, GPL review, static asset source, and recovery page source before release claims. |
| T-07-03-03 | R | Evidence docs | mitigate | Use distinct not-run/gap/package/live/hardware conclusions and forbid initial verified wording. |
</threat_model>

<verification>
Run the fixed-string checks above, `cargo about generate about.hbs --output-file docs/release/cargo-about.html`, and `git diff --check`. If `cargo-about` is missing, install `cargo-about 0.9.0` before verification.
</verification>

<success_criteria>
Wave 0 has release, license, provenance, operator, and evidence surfaces ready for later plans without overclaiming release or OTA parity.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ota-filesystem-and-release-packaging/07-03-SUMMARY.md`.
</output>
