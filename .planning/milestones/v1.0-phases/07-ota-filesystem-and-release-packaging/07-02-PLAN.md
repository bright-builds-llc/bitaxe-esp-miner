---
phase: 07-ota-filesystem-and-release-packaging
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - tools/xtask/src/package_manifest.rs
  - tools/xtask/src/partition_contract.rs
  - tools/xtask/src/main.rs
  - tools/xtask/Cargo.toml
  - tools/xtask/BUILD.bazel
  - tools/flash/src/main.rs
  - firmware/bitaxe/partitions-ultra205.csv
autonomous: true
requirements: [REL-01, REL-04, REL-06]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 7-2026-06-28T13-30-15
generated_at: 2026-06-28T14:16:04Z
must_haves:
  truths:
    - "Manifest v2 preserves `default_flash_image` for `tools/flash`."
    - "Ultra 205 partition names, offsets, and sizes are validated as release data."
    - "Package tests fail if `www.bin`, factory, app, or install-note metadata is omitted."
  artifacts:
    - path: "tools/xtask/src/package_manifest.rs"
      provides: "Typed manifest v2 schema and tests"
    - path: "tools/xtask/src/partition_contract.rs"
      provides: "Ultra 205 partition contract validation"
    - path: "firmware/bitaxe/partitions-ultra205.csv"
      provides: "Rust-owned partition table matching reference intent"
  key_links:
    - from: "tools/flash/src/main.rs"
      to: "tools/xtask manifest v2"
      via: "top-level `default_flash_image` JSON field"
      pattern: "default_flash_image"
---

<objective>
Create the Wave 0 package manifest and partition validation contracts.

Purpose: Prevent release metadata growth from breaking `just flash board=205`, and make partition layout drift visible before package generation changes.
Output: Typed manifest v2 and Ultra 205 partition validation modules with focused tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-CONTEXT.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-RESEARCH.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-VALIDATION.md
@tools/xtask/src/main.rs
@tools/flash/src/main.rs
@scripts/package-firmware.sh
@reference/esp-miner/partitions.csv
@reference/esp-miner/merge_bin.sh

<interfaces>
Existing manifest consumer in `tools/flash/src/main.rs`:
```rust
#[derive(Debug, Deserialize)]
struct PackageManifest {
    default_flash_image: String,
}
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Split and define manifest v2 schema</name>
  <read_first>
    tools/xtask/src/main.rs
    tools/flash/src/main.rs
    .planning/phases/07-ota-filesystem-and-release-packaging/07-CONTEXT.md D-01 through D-05
  </read_first>
  <files>tools/xtask/src/package_manifest.rs, tools/xtask/src/main.rs, tools/xtask/BUILD.bazel</files>
  <behavior>
    - `schema_version` is exactly `2`.
    - Top-level `default_flash_image` remains present and resolves to `bitaxe-ultra205.elf`.
    - Artifact kinds include `firmware_elf`, `firmware_ota_image`, `www_spiffs_image`, `factory_merged_image`, `partition_table`, `otadata_initial`, and optional `update_only_image`.
    - Manifest includes `release_name`, `image_metadata`, `install_notes`, `license_inventory`, and `provenance_manifest`.
  </behavior>
  <action>Move manifest structs/checksum helpers from `main.rs` into `package_manifest.rs` without changing the CLI command yet. Add `PackageManifestV2`, `ReleaseArtifact`, `ArtifactKind`, `ImageMetadata`, `ReleaseNotes`, and validation tests for D-02 through D-05. Keep serde serialization typed; do not assemble JSON with strings. Preserve the existing `PackageManifest` read shape in `tools/flash` by adding a manifest v2 compatibility test there.</action>
  <verify>
    <automated>cargo test -p xtask --all-features package_manifest && cargo test -p bitaxe-flash --all-features manifest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "schema_version: 2|ArtifactKind|firmware_ota_image|www_spiffs_image|factory_merged_image|default_flash_image|license_inventory|provenance_manifest" tools/xtask/src/package_manifest.rs`
    `cargo test -p xtask --all-features package_manifest` passes.
    `cargo test -p bitaxe-flash --all-features manifest` passes.
  </acceptance_criteria>
  <done>Manifest v2 is a typed contract and `tools/flash` default-image compatibility is guarded by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Ultra 205 partition contract validation</name>
  <read_first>
    reference/esp-miner/partitions.csv
    reference/esp-miner/merge_bin.sh
    .planning/phases/07-ota-filesystem-and-release-packaging/07-RESEARCH.md Pattern 3
  </read_first>
  <files>firmware/bitaxe/partitions-ultra205.csv, tools/xtask/src/partition_contract.rs, tools/xtask/src/main.rs, tools/xtask/Cargo.toml, tools/xtask/BUILD.bazel</files>
  <action>Add `firmware/bitaxe/partitions-ultra205.csv` with these exact rows from the reference intent: `nvs` at `0x9000` size `0x6000`, `phy_init` at `0xf000` size `0x1000`, `factory` at `0x10000` size `4M`, `www` data SPIFFS at `0x410000` size `3M`, `ota_0` at `0x710000` size `4M`, `ota_1` at `0xb10000` size `4M`, `otadata` at `0xf10000` size `8k`, and `coredump` size `64K`. Add `esp-idf-part = "0.6.0"` to `tools/xtask` and implement `validate_ultra205_partition_contract(path)`. If `esp-idf-part` cannot parse this CSV shape, write a small typed parser in `partition_contract.rs` with explicit columns and tests; do not parse offsets with regex in package scripts.</action>
  <verify>
    <automated>cargo test -p xtask --all-features partition_contract</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "www,.*spiffs.*0x410000.*3M|ota_0.*0x710000.*4M|ota_1.*0xb10000.*4M|otadata.*0xf10000.*8k" firmware/bitaxe/partitions-ultra205.csv`
    `rg -n "validate_ultra205_partition_contract|0x410000|0xf10000|esp_idf_part|Partition" tools/xtask/src/partition_contract.rs tools/xtask/Cargo.toml`
    `cargo test -p xtask --all-features partition_contract` passes.
  </acceptance_criteria>
  <done>Partition layout is checked as a release contract before package generation depends on it, preserving D-07 and D-17.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add package validation CLI contract</name>
  <read_first>
    tools/xtask/src/main.rs
    .planning/phases/07-ota-filesystem-and-release-packaging/07-VALIDATION.md
  </read_first>
  <files>tools/xtask/src/main.rs, tools/xtask/src/package_manifest.rs, tools/xtask/src/partition_contract.rs</files>
  <action>Add an `xtask validate-package --manifest <path> --partition-table <path>` subcommand that deserializes manifest v2, validates top-level `default_flash_image`, validates required artifact kinds, validates SHA-256 string length/hex, and calls `validate_ultra205_partition_contract`. Add tests using temp files for a passing manifest and failures for missing `www_spiffs_image`, missing `install_notes`, and a factory artifact without offset `0x0`.</action>
  <verify>
    <automated>cargo test -p xtask --all-features validate_package</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "ValidatePackage|validate-package|required artifact.*www_spiffs_image|required artifact.*firmware_ota_image|factory.*0x0" tools/xtask/src/main.rs tools/xtask/src/package_manifest.rs`
    `cargo test -p xtask --all-features validate_package` passes.
  </acceptance_criteria>
  <done>Package validation exists before package generation expands in Plan 07-05.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package-manifest-to-flash-tool | Generated JSON controls what `tools/flash` will flash. |
| reference-partition-to-rust-package | Reference CSV facts become Rust-owned release offsets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | T/D | Manifest default image | mitigate | Keep top-level `default_flash_image` and reject factory images as default flash inputs. |
| T-07-02-02 | T/D | Partition offsets | mitigate | Validate `www`, app slots, and `otadata` offsets/sizes before packaging. |
| T-07-02-03 | R/T | Release metadata | mitigate | Require source/reference commits, checksums, install notes, and artifact kind metadata in manifest v2. |
</threat_model>

<verification>
Run `cargo test -p xtask --all-features package_manifest`, `cargo test -p xtask --all-features partition_contract`, `cargo test -p xtask --all-features validate_package`, `cargo test -p bitaxe-flash --all-features manifest`, and `git diff --check`.
</verification>

<success_criteria>
Manifest v2 and partition tests exist; `tools/flash` compatibility is preserved; package validation can reject missing release artifacts before `just package` is updated.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ota-filesystem-and-release-packaging/07-02-SUMMARY.md`.
</output>
