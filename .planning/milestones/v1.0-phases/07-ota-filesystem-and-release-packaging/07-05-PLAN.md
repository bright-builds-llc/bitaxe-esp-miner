---
phase: 07-ota-filesystem-and-release-packaging
plan: 05
type: execute
wave: 3
depends_on: ["07-02", "07-04", "07-06"]
files_modified:
  - scripts/package-firmware.sh
  - firmware/bitaxe/BUILD.bazel
  - tools/xtask/src/main.rs
  - tools/xtask/src/package_manifest.rs
  - tools/xtask/src/partition_contract.rs
  - tools/flash/src/main.rs
  - docs/release/provenance-manifest.md
autonomous: true
requirements: [REL-01, REL-04, REL-06]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 7-2026-06-28T13-30-15
generated_at: 2026-06-28T14:16:04Z
must_haves:
  truths:
    - "`just package` emits named Ultra 205 release artifacts without manual discovery."
    - "Manifest v2 lists loose app OTA and `www.bin` assets plus factory image metadata."
    - "`just flash board=205` continues to resolve the ELF through `default_flash_image`."
  artifacts:
    - path: "scripts/package-firmware.sh"
      provides: "Release artifact generation wrapper"
    - path: "firmware/bitaxe/BUILD.bazel"
      provides: "Declared Bazel outputs for package artifacts"
    - path: "tools/xtask/src/package_manifest.rs"
      provides: "Manifest v2 generation and validation"
  key_links:
    - from: "Justfile"
      to: "//firmware/bitaxe:firmware_image"
      via: "`just package`"
      pattern: "firmware_image"
    - from: "tools/flash/src/main.rs"
      to: "bitaxe-ultra205-package.json"
      via: "`default_flash_image`"
      pattern: "default_flash_image"
---

<objective>
Expand package generation to release-grade Ultra 205 artifacts and manifest v2.

Purpose: Preserve the existing package/flash workflow while adding direct assets, checksums, offsets, install-note paths, and provenance pointers needed for release inspection.
Output: `just package` produces app image, `www.bin`, factory image, partition/otadata metadata, manifest v2, and keeps flash compatibility.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ota-filesystem-and-release-packaging/07-CONTEXT.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-RESEARCH.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-02-SUMMARY.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-04-SUMMARY.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-06-SUMMARY.md
@scripts/package-firmware.sh
@firmware/bitaxe/BUILD.bazel
@tools/xtask/src/main.rs
@tools/flash/src/main.rs
@reference/esp-miner/merge_bin.sh
@reference/esp-miner/partitions.csv
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Generate `www.bin`, app OTA image, and otadata artifact</name>
  <read_first>
    scripts/package-firmware.sh
    firmware/bitaxe/BUILD.bazel
    firmware/bitaxe/partitions-ultra205.csv
    firmware/bitaxe/static/www/index.html
    firmware/bitaxe/static/www/assets/app.css.gz
    .planning/phases/07-ota-filesystem-and-release-packaging/07-CONTEXT.md D-01 through D-04
  </read_first>
  <files>scripts/package-firmware.sh, firmware/bitaxe/BUILD.bazel, docs/release/provenance-manifest.md</files>
  <action>Extend `scripts/package-firmware.sh` without replacing the existing `just package` -> Bazel -> script -> `xtask` chain. Produce these named artifacts in `$out_dir`: `bitaxe-ultra205.elf`, `esp-miner.bin`, `www.bin`, `otadata-initial.bin`, `bitaxe-ultra205-factory.bin`, and `bitaxe-ultra205-package.json`. Keep `esp-miner.bin` and `www.bin` as loose, owner-identifiable package outputs per D-03. Generate `www.bin` from `firmware/bitaxe/static/www` using ESP-IDF `spiffsgen.py` for size `0x300000`, including `assets/app.css.gz` as the representative gzip static smoke asset; fail with an explicit message naming `IDF_PATH/components/spiffs/spiffsgen.py` if it cannot be found. Generate `otadata-initial.bin` as an 8192-byte erased flash image only if no ESP-IDF generated `ota_data_initial.bin` exists; record which source was used in the manifest metadata. Generate `esp-miner.bin` with `espflash save-image --chip esp32s3 --flash-size 16mb --flash-mode dio --flash-freq 80mhz --partition-table firmware/bitaxe/partitions-ultra205.csv --target-app-partition ota_0`. Keep `bitaxe-ultra205.elf` as the default flash image for `tools/flash`. Do not make an archive bundle the primary output per D-05.</action>
  <verify>
    <automated>bazel build //firmware/bitaxe:firmware_image</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "spiffsgen.py|0x300000|app.css.gz|esp-miner.bin|www.bin|otadata-initial.bin|--target-app-partition|ota_0|bitaxe-ultra205-package.json" scripts/package-firmware.sh firmware/bitaxe/BUILD.bazel`
    `bazel build //firmware/bitaxe:firmware_image` passes and produces `bazel-bin/firmware/bitaxe/www.bin`.
  </acceptance_criteria>
  <done>`www.bin` and app OTA image are package outputs with deterministic names and provenance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write manifest v2 from real package outputs</name>
  <read_first>
    tools/xtask/src/package_manifest.rs
    tools/xtask/src/main.rs
    scripts/package-firmware.sh
    docs/release/ultra-205.md
    docs/release/provenance-manifest.md
  </read_first>
  <files>tools/xtask/src/main.rs, tools/xtask/src/package_manifest.rs, tools/xtask/src/partition_contract.rs, scripts/package-firmware.sh</files>
  <action>Extend `xtask package-firmware` arguments to accept `--firmware-ota-image`, `--www-bin`, `--partition-table`, `--otadata-initial`, `--release-name`, `--install-notes`, `--license-inventory`, and `--provenance-manifest`. Generate schema version 2 with artifact entries and offsets: firmware ELF offset `Unavailable`, firmware OTA image offset `0x10000`, `www_spiffs_image` offset `0x410000`, `otadata_initial` offset `0xf10000`, `factory_merged_image` offset `0x0`, and `partition_table` offset `0x8000` when a binary partition table is present or `Unavailable` for CSV-only. Include `esp-miner.bin` and `www.bin` as first-class manifest artifacts per D-03, not only as members of a merged image. Include SHA-256 for every artifact, source commit, reference commit, ESP-IDF version `v5.5.4`, Rust target `xtensa-esp32s3-espidf`, release name, install notes path, license inventory path, and provenance manifest path. Run `validate-package` before writing success output.</action>
  <verify>
    <automated>cargo test -p xtask --all-features package_manifest && cargo test -p xtask --all-features validate_package && bazel build //firmware/bitaxe:firmware_image</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "schema_version.*2|firmware_ota_image|www_spiffs_image|otadata_initial|factory_merged_image|0x410000|0xf10000|install_notes|license_inventory|provenance_manifest" tools/xtask/src/package_manifest.rs tools/xtask/src/main.rs`
    `cargo test -p xtask --all-features package_manifest` passes.
    `cargo test -p xtask --all-features validate_package` passes.
    `bazel build //firmware/bitaxe:firmware_image` passes.
  </acceptance_criteria>
  <done>Package manifests now contain all D-02 through D-04 release metadata while preserving the default image field.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve flash compatibility with manifest v2</name>
  <read_first>
    tools/flash/src/main.rs
    tools/xtask/src/package_manifest.rs
    .planning/phases/01-foundation-and-gamma-601-boot-log/01-08-SUMMARY.md
  </read_first>
  <files>tools/flash/src/main.rs</files>
  <action>Add or update `bitaxe-flash` tests that deserialize a manifest v2 containing many artifact entries and still resolve only top-level `default_flash_image` to `bitaxe-ultra205.elf`. Keep rejection of `bitaxe-ultra205-factory.bin` as default. If needed, make the manifest struct tolerate extra v2 fields using serde defaults. Do not require operators to pass artifact paths manually for normal `just flash board=205`.</action>
  <verify>
    <automated>cargo test -p bitaxe-flash --all-features manifest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "schema_version|artifacts|default_flash_image|bitaxe-ultra205.elf|bitaxe-ultra205-factory.bin" tools/flash/src/main.rs`
    `cargo test -p bitaxe-flash --all-features manifest` passes.
  </acceptance_criteria>
  <done>`tools/flash` remains compatible with manifest v2 and normal flash use requires no manual artifact discovery.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package artifacts to operator | Generated binaries and metadata become install inputs. |
| manifest to flash CLI | JSON controls the flash default image path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | T/R | Package manifest v2 | mitigate | SHA-256 every artifact, source/reference commits, and release paths are required before success. |
| T-07-05-02 | D | Missing static partition image | mitigate | `just package` fails if `www.bin` is not generated and validated. |
| T-07-05-03 | T/D | Wrong flash default | mitigate | Keep ELF-only `default_flash_image`; tests reject factory image as default. |
</threat_model>

<verification>
Run `cargo test -p xtask --all-features package_manifest`, `cargo test -p xtask --all-features validate_package`, `cargo test -p bitaxe-flash --all-features manifest`, `bazel build //firmware/bitaxe:firmware_image`, `just package`, and `git diff --check`.
</verification>

<success_criteria>
`just package` emits manifest v2 plus named loose assets; `just flash board=205 --dry-run port=/dev/cu.usbmodem101` resolves the manifest default ELF; package validation rejects missing release metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ota-filesystem-and-release-packaging/07-05-SUMMARY.md`.
</output>
