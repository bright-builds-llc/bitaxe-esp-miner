---
phase: 07-ota-filesystem-and-release-packaging
plan: 09
type: execute
wave: 6
depends_on: ["07-08"]
files_modified:
  - tools/parity/src/main.rs
  - docs/parity/checklist.md
  - docs/parity/evidence/phase-07-ultra-205-ota-hardware-smoke.md
autonomous: false
requirements: [REL-01, REL-02, REL-03, REL-04, REL-05, REL-06, REL-07, REL-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 7-2026-06-28T13-30-15
generated_at: 2026-06-28T14:16:04Z
must_haves:
  truths:
    - "Parity tooling blocks release/OTA verified claims without the required evidence class."
    - "Checklist rows distinguish package/workflow evidence from live OTA, recovery, rollback, and interrupted-update evidence."
    - "Final Phase 7 verification runs package, build, parity, release-gate, and hardware checkpoint paths."
  artifacts:
    - path: "tools/parity/src/main.rs"
      provides: "Release/OTA false-verification guard"
    - path: "docs/parity/checklist.md"
      provides: "Phase 7 row statuses and evidence references"
    - path: "docs/parity/evidence/phase-07-ultra-205-ota-hardware-smoke.md"
      provides: "Manual hardware checkpoint evidence"
  key_links:
    - from: "docs/parity/checklist.md"
      to: "docs/parity/evidence/phase-07-ota-filesystem-release.md"
      via: "Phase 7 evidence references"
      pattern: "phase-07-ota-filesystem-release"
---

<objective>
Close Phase 7 with parity gates, checklist updates, and final verification.

Purpose: Ensure release claims cannot outrun hardware, rollback, recovery, license, provenance, and explicit gap evidence.
Output: Extended parity guard, updated checklist rows, final command verification, and hardware checkpoint evidence or explicit pending conclusion.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-CONTEXT.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-VALIDATION.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-08-SUMMARY.md
@tools/parity/src/main.rs
@docs/parity/checklist.md
@docs/parity/evidence/phase-07-ota-filesystem-release.md
@docs/parity/evidence/phase-07-ultra-205-ota-hardware-smoke.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend parity guard for release and OTA evidence</name>
  <read_first>
    tools/parity/src/main.rs
    docs/parity/checklist.md
    docs/parity/evidence/phase-07-ota-filesystem-release.md
  </read_first>
  <files>tools/parity/src/main.rs</files>
  <behavior>
    - `OTA-001` verified requires `hardware-smoke` or `hardware-regression`.
    - `OTA-002` verified requires `hardware-regression` and an evidence note containing `interrupted-update`.
    - `FS-001` verified requires evidence that is not package-only and notes live recovery/static smoke.
    - `REL-003` verified requires release-gate/provenance evidence plus package workflow evidence.
  </behavior>
  <action>Extend checklist validation so update/release-sensitive rows cannot be marked verified from `unit`, `workflow`, `api-compare`, or `pending` evidence alone. Add tests for false verified claims on `FS-001`, `OTA-001`, `OTA-002`, `REL-001`, `REL-002`, and `REL-003`. Keep implemented/package statuses allowed when evidence is `workflow` or `unit` and status is below `verified`. Do not weaken existing safety-critical hardware gates.</action>
  <verify>
    <automated>cargo test -p bitaxe-parity --all-features release_ota_verified_guard</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "release_ota|OTA-001|OTA-002|FS-001|REL-003|interrupted-update|hardware-regression|hardware-smoke" tools/parity/src/main.rs`
    `cargo test -p bitaxe-parity --all-features release_ota_verified_guard` passes.
  </acceptance_criteria>
  <done>Parity tooling blocks high-severity false verified release and OTA claims.</done>
</task>

<task type="auto">
  <name>Task 2: Update Phase 7 checklist rows</name>
  <read_first>
    docs/parity/checklist.md
    docs/parity/evidence/phase-07-ota-filesystem-release.md
    docs/parity/evidence/phase-07-ultra-205-ota-hardware-smoke.md
    docs/release/ultra-205.md
  </read_first>
  <files>docs/parity/checklist.md</files>
  <action>Update rows `FS-001`, `OTA-001`, `OTA-002`, `REL-001`, `REL-002`, and `REL-003` with implementation pointers and evidence references from Phase 7. Use `implemented` with `unit,workflow` or `workflow` evidence for package/static/firmware compile behavior; use `deferred` with `deferred` evidence for `OTA-002` if the D-16 OTAWWW gap is still active; use `not-started` or `implemented` below verified for rollback/recovery/interrupted-update rows unless hardware evidence exists. Add notes that distinguish package evidence, live firmware OTA evidence, live recovery evidence, interrupted-update evidence, release-gate evidence, and explicit gap evidence. Do not mark final V1 parity verified from Phase 7 package evidence alone.</action>
  <verify>
    <automated>just parity</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "FS-001|OTA-001|OTA-002|REL-001|REL-002|REL-003|phase-07-ota-filesystem-release|phase-07-ultra-205-ota-hardware-smoke|REL-03|package evidence|live firmware OTA|interrupted-update" docs/parity/checklist.md`
    `just parity` exits 0 with `validation_errors: none`.
  </acceptance_criteria>
  <done>Checklist reflects Phase 7 implementation and evidence without unsupported verified release claims.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify live Ultra 205 OTA and recovery behavior</name>
  <read_first>
    docs/release/ultra-205.md
    docs/parity/evidence/phase-07-ultra-205-ota-hardware-smoke.md
    bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json
  </read_first>
  <files>docs/parity/evidence/phase-07-ultra-205-ota-hardware-smoke.md, docs/parity/checklist.md</files>
  <action>After automated package/build/parity checks pass, use the standing repo permission for autonomous Ultra 205 hardware verification by running `just detect-ultra205`. If it prints exactly one detected `port=<path>`, run `just flash-monitor board=205 port=<path> evidence-dir=docs/parity/evidence/phase-07-ultra-205-ota-hardware-smoke`, then follow `docs/release/ultra-205.md` to exercise `/api/system/OTA` with `esp-miner.bin`, invalid firmware upload rejection, reboot/boot-validation logs, `/`, representative gzip static asset `/assets/app.css.gz`, missing static redirect, `/recovery`, and `/api/system/OTAWWW` gap response. Record detector output, board-info output, command output, board, port, manifest path, firmware commit, reference commit, and observed results in the evidence doc. If `just detect-ultra205` reports zero ports, multiple ports, or failed board-info, do not run flash/OTA; set the evidence conclusion to exactly `not run - hardware verification pending` and keep affected checklist rows below `verified`.</action>
  <verify>
    <automated>just test && just package && just parity && bazel run //tools/parity:report -- release-gate</automated>
  </verify>
  <acceptance_criteria>
    Automated verification command exits 0.
    Hardware evidence document contains either a completed live run with board, port, manifest, OTA result, rollback/validation result, recovery result, and conclusion, or the exact not-run conclusion.
    `just parity` reports `validation_errors: none` after any checklist adjustment.
  </acceptance_criteria>
  <done>Phase 7 ends with either captured live hardware evidence or explicit pending hardware evidence, never an implicit OTA/recovery verification claim.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checklist-to-release-claim | Markdown status rows become parity/release readiness claims. |
| human-hardware-checkpoint | Manual OTA/recovery actions can affect a connected Ultra 205. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09-01 | R/E | Parity checklist | mitigate | Tooling rejects verified OTA/release rows without hardware/release-gate evidence. |
| T-07-09-02 | D | Hardware checkpoint | mitigate | Use standing Ultra 205 permission only after `just detect-ultra205` identifies one ESP32-S3 USB candidate; record not-run pending evidence if detection is unavailable or ambiguous. |
| T-07-09-03 | R/T | Release readiness | mitigate | Final checks run `just test`, `just package`, `just parity`, and `release-gate` before completion. |
</threat_model>

<verification>
Run `cargo test -p bitaxe-parity --all-features release_ota_verified_guard`, `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, `cargo test --all-features`, `just test`, `just package`, `bazel run //tools/parity:report -- release-gate`, `just parity`, `just detect-ultra205`, and the hardware checkpoint steps when detector output provides an Ultra 205 candidate port.
</verification>

<success_criteria>
All REL-01 through REL-08 rows are represented in plans and checklist evidence; parity tooling prevents false verified claims; final automated checks pass; live hardware evidence is recorded or explicitly pending.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ota-filesystem-and-release-packaging/07-09-SUMMARY.md`.
</output>
