---
phase: 12
plan: 03
type: execute
status: planned
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - scripts/phase12-mining-smoke-preflight.sh
  - docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence.md
  - docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/redaction-review.md
  - docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/detect-ultra205.log
  - docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/safe-boot/flash-command-evidence.json
  - docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/safe-boot/flash-monitor.log
  - docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/chip-detect/flash-command-evidence.json
  - docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/chip-detect/flash-monitor.log
  - docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/mining-smoke-preflight.log
autonomous: true
requirements: [ASIC-07, STR-06, STR-07, EVD-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-06-30T00-13-19
generated_at: 2026-06-30T00:14:49.245Z
must_haves:
  truths:
    - "Executor runs just detect-ultra205 before live Phase 12 hardware evidence"
    - "Evidence records detector-gated capture, chip-detect diagnostic capture, mining preflight status, or exact pending status without running prohibited commands"
    - "Redaction review is non-pending before generated evidence is cited"
  artifacts:
    - path: "docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence.md"
      provides: "Executed Phase 12 evidence log and conclusions"
      contains: "Execution Log"
    - path: "docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/redaction-review.md"
      provides: "Secret redaction review for generated artifacts"
      contains: "Conclusion:"
  key_links:
    - from: "docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence.md"
      to: "tools/flash/src/main.rs"
      via: "Wrapper-generated evidence artifacts"
      pattern: "flash-command-evidence\\.json"
---

<objective>
Run detector-gated Phase 12 evidence capture using repo-owned wrappers and a fail-closed mining preflight hook, or record exact pending status when a gate fails.

Purpose: Phase 12 needs real command evidence where safe, but must not bypass detector, recovery, redaction, or safety gates.
Output: Updated Phase 12 evidence ledger, optional wrapper JSON/log artifacts, and a completed redaction review.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence.md
@docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/README.md
@docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/redaction-review.md
@scripts/detect-ultra205.sh
@docs/parity/evidence/phase-03-ultra-205-bm1366-chip-detect.md
@tools/flash/src/main.rs
@firmware/bitaxe/src/asic_adapter/status.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fail-closed mining smoke preflight hook</name>
  <requirements>STR-06, STR-07, EVD-05</requirements>
  <read_first>
    <file>firmware/bitaxe/src/asic_adapter/status.rs</file>
    <file>firmware/bitaxe/src/main.rs</file>
    <file>crates/bitaxe-stratum/src/v1/mining_loop.rs</file>
  </read_first>
  <files>scripts/phase12-mining-smoke-preflight.sh</files>
  <scope>Repo-owned read-only status hook only. It may inspect wrapper-captured logs and run host tests; it must not open serial ports, write to hardware, contact pools, submit work, or stress the board.</scope>
  <action>
    Add `scripts/phase12-mining-smoke-preflight.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`.

    Required interface:
    - `--log <path>` points at a wrapper-captured `flash-monitor.log`.
    - `--out <path>` writes the preflight conclusion log.
    - No positional arguments.

    Required behavior:
    - Fail clearly if the log path is missing, the output path is missing, or the input log is absent.
    - Require the wrapper log to contain `mining_loop_status=blocked` and `work_submission=disabled` before concluding safe-blocked.
    - Write a structured text conclusion containing `phase12_mining_smoke_preflight=blocked`, `reason=firmware_fail_closed`, `controlled_mining_smoke=not_run`, `bounded_mining_soak=not_run`, and `work_submission=disabled`.
    - If the log contains active mining/share markers but the repo has no bounded mining smoke command and no documented recovery path, fail closed and write `phase12_mining_smoke_preflight=blocked reason=bounded_probe_absent`.
  </action>
  <verify>
    <automated>bash -n scripts/phase12-mining-smoke-preflight.sh</automated>
    <automated>rg -n "phase12_mining_smoke_preflight=blocked|work_submission=disabled|bounded_probe_absent" scripts/phase12-mining-smoke-preflight.sh</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The hook is repo-owned, read-only, and fail-closed.</criterion>
    <criterion>The hook gives Phase 12 an executable STR-06/STR-07 preflight decision without running raw serial writes, pool scripts, or mining stress.</criterion>
  </acceptance_criteria>
  <done>Phase 12 has a bounded mining smoke/soak preflight hook before hardware capture runs.</done>
</task>

<task type="auto">
  <name>Task 2: Run host checks and detector-gated hardware capture</name>
  <requirements>ASIC-07, STR-06, STR-07, EVD-05</requirements>
  <read_first>
    <file>AGENTS.md</file>
    <file>docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence.md</file>
    <file>docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/redaction-review.md</file>
    <file>scripts/detect-ultra205.sh</file>
    <file>tools/flash/src/main.rs</file>
  </read_first>
  <files>docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence.md, docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/redaction-review.md, docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/detect-ultra205.log, docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/safe-boot/flash-command-evidence.json, docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/safe-boot/flash-monitor.log, docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/chip-detect/flash-command-evidence.json, docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/chip-detect/flash-monitor.log, docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/mining-smoke-preflight.log</files>
  <scope>Repo-owned commands and scripts only. Do not run raw serial writes, direct `espflash`, direct `esptool.py`, erase, rollback, voltage/fan stress, direct pool scripts, or unbounded mining stress.</scope>
  <action>
    First run:
    `cargo test -p bitaxe-asic -p bitaxe-stratum -p bitaxe-safety -p bitaxe-api --all-features`
    `cargo test -p bitaxe-parity --all-features`

    Then run the hardware gate and persist its complete output, including the board-info command/output emitted by the detector:
    `bash -lc 'set -euo pipefail; mkdir -p docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence; just detect-ultra205 2>&1 | tee docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/detect-ultra205.log'`

    If detection fails, do not run flash, monitor, or probe commands. Append an `Execution Log` entry with command `just detect-ultra205`, result `blocked`, sanitized detector output summary, conclusion `hardware evidence pending - detector gate did not pass`, and affected packs `detector-package-safe-boot`, `bm1366-chip-detect-init`, `diagnostic-work-result`, `controlled-mining-smoke`, and `bounded-mining-soak`.

    If detection succeeds, copy the printed `port=<path>` and run the default safe-boot wrapper capture:
    `just flash-monitor board=205 port=<path> evidence-dir=docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/safe-boot`

    Then run the existing chip-detect diagnostic path only if detector output confirms exactly one Ultra 205 ESP32-S3 candidate and the safe bench scope remains limited to chip-detect/no-mining:
    `bazel build --action_env=BITAXE_ASIC_DIAGNOSTIC=chip-detect --action_env=BITAXE_HARDWARE_EVIDENCE_ACK=ultra205-chip-detect-safe-bench //firmware/bitaxe:firmware_image`
    `bazel run //tools/flash:flash -- flash-monitor --board 205 --port <path> --image bazel-bin/firmware/bitaxe/bitaxe-ultra205.elf --evidence-dir docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/chip-detect --capture-timeout-seconds 25`

    After safe-boot capture, run the repo-owned mining preflight hook:
    `./scripts/phase12-mining-smoke-preflight.sh --log docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/safe-boot/flash-monitor.log --out docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence/mining-smoke-preflight.log`

    Update the Phase 12 ledger from generated `flash-command-evidence.json`, `flash-monitor.log`, `detect-ultra205.log`, and `mining-smoke-preflight.log`. Record board `205`, selected port, source commit, reference commit, exact commands, package manifest or firmware identity when available, capture status, trusted-output status, detector output, board-info output, observed log markers, redaction status, and conclusion.

    Classify exact observed markers if present: `asic_status=preflight_missing`, `asic_status=chip_detected`, `asic_status=initialized_no_mining`, `mining_loop_status=blocked`, `phase12_mining_smoke_preflight=blocked`, `safe_state: mining=disabled asic_work_submission=disabled hardware_control=disabled`, `axeos_api_route_shell=started`, and `safety_supervisor_step=yield`.

    If only fail-closed, chip-detect/no-mining, safe-blocked, or bounded-probe-absent markers are observed, record that full BM1366 initialization, diagnostic work/result, and live mining smoke/soak remain `hardware evidence pending`. Do not promote them.
  </action>
  <verify>
    <automated>bash -n scripts/phase12-mining-smoke-preflight.sh</automated>
    <automated>cargo test -p bitaxe-asic -p bitaxe-stratum -p bitaxe-safety -p bitaxe-api --all-features</automated>
    <automated>cargo test -p bitaxe-parity --all-features</automated>
    <automated>rg -n "Detector Gate Result|board-info|hardware evidence pending - detector gate did not pass|selected port: /dev/|selected port: COM|safe-boot/flash-command-evidence\\.json|chip-detect/flash-command-evidence\\.json|asic_status=|mining_loop_status=blocked|phase12_mining_smoke_preflight=blocked" docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence.md</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The ledger records `just detect-ultra205` before any flash-monitor entry.</criterion>
    <criterion>If detection failed, the ledger contains `hardware evidence pending - detector gate did not pass` and no generated JSON/log artifact is required.</criterion>
    <criterion>If detection passed, detector output and board-info output are recorded in `detect-ultra205.log` and summarized in the ledger.</criterion>
    <criterion>If detection passed, wrapper-generated safe-boot `flash-command-evidence.json` and `flash-monitor.log` exist under the Phase 12 artifact directory.</criterion>
    <criterion>If chip-detect diagnostic ran, wrapper-generated chip-detect `flash-command-evidence.json` and `flash-monitor.log` exist under the Phase 12 artifact directory.</criterion>
    <criterion>The mining smoke preflight log records either safe-blocked status or blocked status for the absent bounded live-mining probe.</criterion>
    <criterion>The redaction review has a non-pending conclusion after this plan.</criterion>
    <criterion>The ledger does not claim chip-detect/init/work/result/mining evidence unless exact observed markers are present.</criterion>
  </acceptance_criteria>
  <done>Phase 12 has detector-gated evidence or exact pending status with redaction reviewed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| USB host to Ultra 205 | Live wrapper commands flash and monitor a physical board. |
| Serial logs to repository evidence | Logs can contain private configuration or stale identity markers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-03-01 | Spoofing | `just detect-ultra205` | mitigate | Use only detector-selected port after board-info succeeds. |
| T-12-03-02 | Information Disclosure | generated evidence artifacts | mitigate | Complete redaction review before commit and cite only sanitized conclusions. |
| T-12-03-03 | Denial of Service | live hardware commands | mitigate | Permit only repo wrapper commands and stop on missing detector or recovery prerequisites. |
</threat_model>

<verification>
Run targeted host tests, detector-gated hardware command if allowed, redaction review, and evidence ledger checks.
</verification>

<success_criteria>
Phase 12 evidence capture is either safely recorded from the connected Ultra 205 or explicitly pending with exact blocker language and no overclaim.
</success_criteria>

<output>
After completion, create `.planning/phases/12-asic-and-mining-hardware-evidence/12-03-SUMMARY.md`.
</output>
