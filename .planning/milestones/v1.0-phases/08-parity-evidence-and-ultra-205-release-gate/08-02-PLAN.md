---
phase: 08-parity-evidence-and-ultra-205-release-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/parity/evidence/phase-08-ultra-205-release-gate.md
autonomous: true
requirements: [REL-08, EVD-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-06-28T21-51-32
generated_at: 2026-06-28T22:16:47.573Z
must_haves:
  truths:
    - "Phase 8 has a concrete evidence destination before live hardware actions begin."
    - "Ultra 205 hardware detection runs through `just detect-ultra205` before any flash, monitor, HTTP, OTA, or recovery probe."
    - "A reachable device URL is either established and sanitized in evidence, or a concrete blocker is recorded."
    - "No checklist row is promoted when hardware detection or `DEVICE_URL` discovery fails."
  artifacts:
    - path: "docs/parity/evidence/phase-08-ultra-205-release-gate.md"
      provides: "Phase 8 evidence ledger and URL-discovery record"
  key_links:
    - from: "scripts/detect-ultra205.sh"
      to: "docs/parity/evidence/phase-08-ultra-205-release-gate.md"
      via: "detector output captured before live hardware use"
      pattern: "port="
    - from: "docs/parity/evidence/phase-08-ultra-205-release-gate.md"
      to: "08-03-PLAN.md"
      via: "DEVICE_URL status gate"
      pattern: "DEVICE_URL status"
---

<objective>
Create the Phase 8 evidence ledger and establish the hardware and `DEVICE_URL` preconditions for live HTTP/OTA checks.

Purpose: Implement D-05 and D-08 before any live release surfaces are probed.
Output: A committed evidence document containing required fields, hardware detection output, package context, and either an established sanitized `DEVICE_URL` status or a concrete blocker.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-parity-evidence-and-ultra-205-release-gate/08-CONTEXT.md
@.planning/phases/08-parity-evidence-and-ultra-205-release-gate/08-RESEARCH.md
@.planning/phases/08-parity-evidence-and-ultra-205-release-gate/08-VALIDATION.md
@AGENTS.md
@Justfile
@scripts/detect-ultra205.sh
@docs/release/ultra-205.md
@docs/parity/evidence/phase-07-ultra-205-ota-hardware-smoke.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create Phase 8 evidence ledger</name>
  <read_first>
    docs/parity/evidence/phase-07-ultra-205-ota-hardware-smoke.md
    docs/parity/evidence/phase-07-ota-filesystem-release.md
    docs/release/ultra-205.md
    .planning/phases/08-parity-evidence-and-ultra-205-release-gate/08-CONTEXT.md
  </read_first>
  <files>docs/parity/evidence/phase-08-ultra-205-release-gate.md</files>
  <action>Create `docs/parity/evidence/phase-08-ultra-205-release-gate.md` with H2 sections named exactly: `Run Identity`, `Hardware Detection Gate`, `Package Manifest`, `DEVICE_URL Discovery`, `Static And Recovery HTTP Smoke`, `Firmware OTA Accepted Upload`, `Invalid Image Rejection`, `Failed Update Recovery`, `Rollback And Boot Validation`, `OTAWWW Gap Response`, `Large Erase Recovery`, `Interrupted Update Recovery`, `Deferred Scope Review`, `Secret Redaction Review`, and `Final Conclusion`. Initialize every live evidence conclusion to `not run - Phase 8 evidence pending`. Include required field labels: `board`, `port`, `detector result`, `board-info output`, `source commit`, `reference commit`, `package manifest path`, `factory image path`, `firmware OTA image path`, `www.bin path`, `DEVICE_URL status`, `DEVICE_URL source`, and `sanitized URL evidence`. Do not include standalone `---` body separators after the opening frontmatter.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; f=docs/parity/evidence/phase-08-ultra-205-release-gate.md; for term in "## Run Identity" "## Hardware Detection Gate" "## Package Manifest" "## DEVICE_URL Discovery" "## Static And Recovery HTTP Smoke" "## Firmware OTA Accepted Upload" "## Invalid Image Rejection" "## Failed Update Recovery" "## Rollback And Boot Validation" "## OTAWWW Gap Response" "## Large Erase Recovery" "## Interrupted Update Recovery" "## Deferred Scope Review" "## Secret Redaction Review" "## Final Conclusion" "not run - Phase 8 evidence pending" "DEVICE_URL status" "sanitized URL evidence"; do rg -q --fixed-strings "$term" "$f"; done'</automated>
  </verify>
  <acceptance_criteria>
    The fixed-string verify loop finds every required H2 heading and field label.
    `grep -n '^---$' docs/parity/evidence/phase-08-ultra-205-release-gate.md | wc -l` prints `0` unless this file intentionally has YAML frontmatter; if frontmatter is added, the count must be `2`.
  </acceptance_criteria>
  <done>Phase 8 has a concrete, parser-safe evidence destination before hardware actions run.</done>
</task>

<task type="auto">
  <name>Task 2: Run Ultra 205 detection and package preflight</name>
  <read_first>
    docs/parity/evidence/phase-08-ultra-205-release-gate.md
    AGENTS.md
    Justfile
    scripts/detect-ultra205.sh
    docs/release/ultra-205.md
  </read_first>
  <files>docs/parity/evidence/phase-08-ultra-205-release-gate.md</files>
  <action>Run `just package` from the repo root before `just detect-ultra205`. If `just package` fails, treat the task as failed and do not convert the failure into a hardware-detection blocker. When packaging succeeds, record manifest path `bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json`, artifact names `esp-miner.bin`, `www.bin`, `otadata-initial.bin`, and `bitaxe-ultra205-factory.bin`, and all available SHA-256 values from the manifest in the `Package Manifest` section. Then run `just detect-ultra205`. If it exits 0 and prints exactly one `port=<path>`, record the selected port, the `espflash board-info --chip esp32s3 --port <port> --non-interactive` command from detector output, the detected chip, flash size, and MAC in the `Hardware Detection Gate` section. If detection exits nonzero after package evidence has been recorded, record `detector result: blocked - zero, multiple, or invalid Ultra 205 USB candidates` and leave every live evidence section at `not run - hardware detection blocked`; do not run flash, monitor, curl, OTA, erase, or interruption commands.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; f=docs/parity/evidence/phase-08-ultra-205-release-gate.md; section_has() { local heading="## $1"; shift; local content; content=$(awk -v heading="$heading" "BEGIN{inside=0} \\$0==heading{inside=1; next} inside && /^## /{exit} inside{print}" "$f"); for term in "$@"; do printf "%s\n" "$content" | rg -q --fixed-strings "$term"; done; }; just package; for term in "bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json" "esp-miner.bin" "www.bin" "otadata-initial.bin" "bitaxe-ultra205-factory.bin" "sha256"; do rg -q --fixed-strings "$term" "$f"; done; if rg -q --fixed-strings "detector result: passed" "$f"; then for term in "port=" "espflash board-info --chip esp32s3 --port" "ESP32-S3"; do rg -q --fixed-strings "$term" "$f"; done; elif rg -q --fixed-strings "detector result: blocked - zero, multiple, or invalid Ultra 205 USB candidates" "$f"; then for section in "Static And Recovery HTTP Smoke" "Firmware OTA Accepted Upload" "Invalid Image Rejection" "Failed Update Recovery" "Rollback And Boot Validation" "OTAWWW Gap Response" "Large Erase Recovery" "Interrupted Update Recovery"; do section_has "$section" "not run - hardware detection blocked"; done; else printf "missing detector result in %s\n" "$f" >&2; exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    `just package` exits 0 before detector evidence is evaluated.
    Evidence always contains `bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json`, `esp-miner.bin`, `www.bin`, `otadata-initial.bin`, `bitaxe-ultra205-factory.bin`, and available `sha256` values.
    Evidence contains either `detector result: passed` plus `port=` plus `ESP32-S3`, or `detector result: blocked - zero, multiple, or invalid Ultra 205 USB candidates`.
    If detection failed, evidence contains `not run - hardware detection blocked`.
  </acceptance_criteria>
  <done>Hardware and package preconditions are recorded before live release evidence attempts.</done>
</task>

<task type="auto">
  <name>Task 3: Establish DEVICE_URL or record blocker</name>
  <read_first>
    docs/parity/evidence/phase-08-ultra-205-release-gate.md
    docs/parity/evidence/phase-07-ultra-205-ota-hardware-smoke.md
    docs/release/ultra-205.md
    AGENTS.md
  </read_first>
  <files>docs/parity/evidence/phase-08-ultra-205-release-gate.md</files>
  <action>If Task 2 recorded `detector result: blocked`, set `DEVICE_URL status: blocked - hardware detection failed`, keep live HTTP/OTA/recovery sections at `not run - hardware detection blocked`, and stop. Otherwise run `just flash-monitor board=205 port=<path> evidence-dir=docs/parity/evidence/phase-08-ultra-205-release-gate` using the detected port. Inspect the captured monitor output for an IP address, DHCP address, AP address, mDNS hostname, or hostname announcement. If a reachable URL can be derived, run `curl --include --max-time 10 "$DEVICE_URL/"` locally to prove reachability, then record `DEVICE_URL status: established`, `DEVICE_URL source: boot log` or `DEVICE_URL source: operator supplied`, `sanitized URL evidence: redacted-local-device-url`, HTTP status for `/`, and the fact that the exact private URL was not committed. If no URL can be derived or curl fails, record `DEVICE_URL status: blocked - no reachable DEVICE_URL`, list the concrete blocker `no IP, DHCP, Wi-Fi association, AP address, mDNS, hostname, or operator-supplied reachable URL`, and set each live HTTP/OTA/recovery section to `not run - no reachable DEVICE_URL` per D-08.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; f=docs/parity/evidence/phase-08-ultra-205-release-gate.md; section_has() { local heading="## $1"; shift; local content; content=$(awk -v heading="$heading" "BEGIN{inside=0} \\$0==heading{inside=1; next} inside && /^## /{exit} inside{print}" "$f"); for term in "$@"; do printf "%s\n" "$content" | rg -q --fixed-strings "$term"; done; }; count=$(rg -n --fixed-strings "DEVICE_URL status:" "$f" | wc -l | tr -d " "); test "$count" = "1"; if rg -q --fixed-strings "DEVICE_URL status: established" "$f"; then for term in "sanitized URL evidence: redacted-local-device-url" "HTTP status" "exact private URL was not committed"; do rg -q --fixed-strings "$term" "$f"; done; elif rg -q --fixed-strings "DEVICE_URL status: blocked - hardware detection failed" "$f"; then rg -q --fixed-strings "detector result: blocked - zero, multiple, or invalid Ultra 205 USB candidates" "$f"; for section in "Static And Recovery HTTP Smoke" "Firmware OTA Accepted Upload" "Invalid Image Rejection" "Failed Update Recovery" "Rollback And Boot Validation" "OTAWWW Gap Response" "Large Erase Recovery" "Interrupted Update Recovery"; do section_has "$section" "not run - hardware detection blocked"; done; elif rg -q --fixed-strings "DEVICE_URL status: blocked - no reachable DEVICE_URL" "$f"; then rg -q --fixed-strings "no IP, DHCP, Wi-Fi association, AP address, mDNS, hostname, or operator-supplied reachable URL" "$f"; for section in "Static And Recovery HTTP Smoke" "Firmware OTA Accepted Upload" "Invalid Image Rejection" "Failed Update Recovery" "Rollback And Boot Validation" "OTAWWW Gap Response" "Large Erase Recovery" "Interrupted Update Recovery"; do section_has "$section" "not run - no reachable DEVICE_URL"; done; else printf "missing supported DEVICE_URL status in %s\n" "$f" >&2; exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    Evidence contains exactly one `DEVICE_URL status:` line.
    If status is `established`, evidence also contains `sanitized URL evidence: redacted-local-device-url` and `exact private URL was not committed`.
    If status is blocked, evidence contains the concrete blocker and does not contain any checklist promotion language.
  </acceptance_criteria>
  <done>Plan 08-03 can either run live probes against an established local URL or skip them with a documented blocker.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| USB host-to-board | Local host uses USB to identify and flash/monitor a physical Ultra 205. |
| local-network-to-firmware | HTTP probes cross from the local network into firmware update/static/recovery handlers. |
| evidence-to-repo | Hardware logs and URLs move from local machine output into committed Markdown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-08-02-01 | Spoofing | `just detect-ultra205` | mitigate | Continue only when exactly one likely ESP USB serial port is found and board-info succeeds for `esp32s3`. |
| T-08-02-02 | Information Disclosure | `docs/parity/evidence/phase-08-ultra-205-release-gate.md` | mitigate | Record `sanitized URL evidence: redacted-local-device-url`; do not commit exact private URLs, Wi-Fi credentials, pool credentials, private endpoints, or NVS secret values. |
| T-08-02-03 | Denial of Service | `just flash-monitor` | mitigate | Run only after the detector passes; use the package manifest factory image path produced by `just package`. |
| T-08-02-04 | Repudiation | `DEVICE_URL Discovery` | mitigate | Record either reachable URL status with curl status or a concrete blocker before later live probes. |
</threat_model>

<verification>
Run the fixed-string ledger check, `just package`, `just detect-ultra205`, conditional `just flash-monitor board=205 port=<path> evidence-dir=docs/parity/evidence/phase-08-ultra-205-release-gate`, and the branch-aware check for `DEVICE_URL status`.
</verification>

<success_criteria>
The Phase 8 evidence ledger exists; Ultra 205 detection and package preflight are recorded; `DEVICE_URL` is established with sanitized evidence or blocked with a concrete reason; no live checklist promotion occurs in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/08-parity-evidence-and-ultra-205-release-gate/08-02-SUMMARY.md`.
</output>
