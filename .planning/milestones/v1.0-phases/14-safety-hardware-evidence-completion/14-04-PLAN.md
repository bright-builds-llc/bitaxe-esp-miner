---
phase: 14-safety-hardware-evidence-completion
plan: "04"
type: execute
wave: 3
depends_on:
  - "14-03"
files_modified:
  - scripts/phase14-self-test-watchdog-load.sh
  - scripts/phase14-self-test-watchdog-load-test.sh
  - scripts/phase14-display-input.sh
  - scripts/phase14-display-input-test.sh
  - scripts/BUILD.bazel
  - docs/parity/evidence/phase-14-safety-hardware-evidence-completion/self-test-watchdog-load.md
  - docs/parity/evidence/phase-14-safety-hardware-evidence-completion/display-input.md
  - docs/parity/evidence/phase-14-safety-hardware-evidence-completion/self-test-watchdog-load/self-test-watchdog-load.log
  - docs/parity/evidence/phase-14-safety-hardware-evidence-completion/display-input/display-input.log
autonomous: true
requirements:
  - SAFE-05
  - SAFE-06
  - SAFE-08
  - SAFE-09
  - EVD-05
must_haves:
  truths:
    - "Self-test hardware submodes remain below verified unless a safe bounded route proves the exact submode"
    - "Watchdog/load evidence distinguishes supervisor startup/yield markers from bounded load responsiveness"
    - "Runtime display/input claims remain pending unless a real runtime route and physical/log/API/WebSocket observation exists"
  artifacts:
    - path: "scripts/phase14-self-test-watchdog-load.sh"
      provides: "Self-test and watchdog/load evidence wrapper"
      contains: "self_test_hardware_status"
    - path: "scripts/phase14-display-input.sh"
      provides: "Runtime display/input evidence wrapper"
      contains: "runtime_display_input_status"
    - path: "docs/parity/evidence/phase-14-safety-hardware-evidence-completion/self-test-watchdog-load.md"
      provides: "Self-test/watchdog/load conclusion"
      contains: "SELF-001"
    - path: "docs/parity/evidence/phase-14-safety-hardware-evidence-completion/display-input.md"
      provides: "Display/input conclusion"
      contains: "UI-003"
  key_links:
    - from: "scripts/phase14-self-test-watchdog-load.sh"
      to: "firmware/bitaxe/src/safety_adapter/watchdog.rs"
      via: "serial markers"
      pattern: "safety_supervisor=started"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 14-2026-06-30T23-56-34
generated_at: 2026-07-01T00:24:20.624Z
---

<objective>
Add and run Phase 14 self-test, watchdog/load, display, and input evidence wrappers.

Purpose: D-09 through D-12 require safe stimuli and recovery paths before self-test hardware submodes, load stress, runtime display, or input behavior can support verified claims. Startup/yield logs and startup SSD1306 observations are useful only as narrow evidence.
Output: Tested wrapper scripts and evidence summaries that either capture exact safe observations or record pending blockers.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/14-safety-hardware-evidence-completion/14-CONTEXT.md
@.planning/phases/14-safety-hardware-evidence-completion/14-RESEARCH.md
@.planning/phases/14-safety-hardware-evidence-completion/14-VALIDATION.md
@docs/parity/evidence/phase-14-safety-hardware-evidence-completion/README.md
@tools/parity/src/safety_allow.rs
@scripts/BUILD.bazel
@firmware/bitaxe/src/safety_adapter/watchdog.rs
@firmware/bitaxe/src/display_adapter.rs
@crates/bitaxe-safety/src/self_test.rs
@crates/bitaxe-safety/src/watchdog.rs
@docs/parity/evidence/phase-06-display-input-runtime-gap.md
@docs/parity/checklist.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add self-test/watchdog/load and display/input wrapper scripts with tests</name>
  <requirements>SAFE-05, SAFE-06, SAFE-08, SAFE-09, EVD-05</requirements>
  <files>scripts/phase14-self-test-watchdog-load.sh, scripts/phase14-self-test-watchdog-load-test.sh, scripts/phase14-display-input.sh, scripts/phase14-display-input-test.sh, scripts/BUILD.bazel</files>
  <read_first>
    <file>scripts/BUILD.bazel</file>
    <file>scripts/phase14-power-voltage.sh</file>
    <file>tools/parity/src/safety_allow.rs</file>
    <file>firmware/bitaxe/src/safety_adapter/watchdog.rs</file>
    <file>firmware/bitaxe/src/display_adapter.rs</file>
    <file>crates/bitaxe-safety/src/self_test.rs</file>
    <file>crates/bitaxe-safety/src/watchdog.rs</file>
  </read_first>
  <action>Create `scripts/phase14-self-test-watchdog-load.sh` and `scripts/phase14-display-input.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. Each wrapper must accept `--manifest <allow.json>`, `--out-dir <dir>`, and optional `--serial-log <path>`, validate with `bazel run //tools/parity:report -- safety-allow --manifest "$manifest" --surface <surface> --allowed-command <exact command>`, and write pending evidence instead of running unsafe work when prerequisites are absent. `phase14-self-test-watchdog-load.sh` must parse serial/log input for `safety_supervisor=started thread=bitaxe-safety-supervisor cadence_ms=100` and `safety_supervisor_step=yield reason=yield_interval_reached`. It must record `watchdog_supervisor_status: observed` only when both markers exist, `load_stress_status: pending - bounded workload stimulus unavailable` unless a safe bounded workload route exists, and `self_test_hardware_status: pending - no production-safe self-test hardware submode route exists` unless the exact submode, voltage/fan/ASIC interactions, cancel/pass/fail behavior, recovery, and production-mining gate are proven. It must not start self-test hardware, reboot, mine, or stress tasks. `phase14-display-input.sh` must parse `display_status=startup_text_rendered model=SSD1306 size=128x32 address=0x3c` and `display_input_status=runtime_gap reason=hardware_evidence_pending startup_only=true`; it may record startup display evidence but must record `runtime_display_input_status: pending - no runtime display/input route or physical input observation` unless a real runtime route and physical/log/API/WebSocket observation exists. Add shell tests with fake serial logs proving observed markers and pending statuses. Register `phase14_self_test_watchdog_load`, `phase14_self_test_watchdog_load_test`, `phase14_display_input`, and `phase14_display_input_test` in `scripts/BUILD.bazel`.</action>
  <verify>
    <automated>bash -n scripts/phase14-self-test-watchdog-load.sh && bash -n scripts/phase14-self-test-watchdog-load-test.sh && bash -n scripts/phase14-display-input.sh && bash -n scripts/phase14-display-input-test.sh && bazel test //scripts:phase14_self_test_watchdog_load_test //scripts:phase14_display_input_test</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Self-test wrapper contains `watchdog_supervisor_status`, `load_stress_status: pending - bounded workload stimulus unavailable`, and `self_test_hardware_status: pending`.</criterion>
    <criterion>Display wrapper contains `display_status=startup_text_rendered`, `display_input_status=runtime_gap`, and `runtime_display_input_status: pending`.</criterion>
    <criterion>Wrapper tests pass through Bazel and include fake marker-positive and marker-missing cases.</criterion>
  </acceptance_criteria>
  <done>Self-test/watchdog/load and display/input wrappers are tested and cannot overclaim from startup-only markers.</done>
</task>

<task type="auto">
  <name>Task 2: Run self-test/watchdog/load and display/input wrappers or record pending evidence</name>
  <requirements>SAFE-05, SAFE-06, SAFE-08, SAFE-09, EVD-05</requirements>
  <files>docs/parity/evidence/phase-14-safety-hardware-evidence-completion/self-test-watchdog-load.md, docs/parity/evidence/phase-14-safety-hardware-evidence-completion/display-input.md, docs/parity/evidence/phase-14-safety-hardware-evidence-completion/self-test-watchdog-load/self-test-watchdog-load.log, docs/parity/evidence/phase-14-safety-hardware-evidence-completion/display-input/display-input.log</files>
  <read_first>
    <file>docs/parity/evidence/phase-14-safety-hardware-evidence-completion/README.md</file>
    <file>scripts/phase14-self-test-watchdog-load.sh</file>
    <file>scripts/phase14-display-input.sh</file>
    <file>docs/parity/evidence/phase-06-display-input-runtime-gap.md</file>
    <file>docs/parity/checklist.md</file>
  </read_first>
  <action>Run both wrappers through validated allow manifests when detector and package prerequisites are available. If detector, serial log, bounded workload stimulus, self-test route, runtime display route, physical input observation, recovery, or redaction prerequisites are absent, write pending evidence instead of probing. Write `self-test-watchdog-load.md` with board `205`, selected port or `unavailable`, source/reference commits or `unavailable`, package manifest, exact commands, allow manifest paths, `SELF-001` status, `watchdog_supervisor_status`, `load_stress_status: pending - bounded workload stimulus unavailable` unless truly proven, `self_test_hardware_status: pending - no production-safe self-test hardware submode route exists` unless truly proven, and non-claims for voltage/fan/ASIC work. Keep self-test hardware submodes below `verified` per D-10 unless the run proves the exact submode safely, including voltage, fan, ASIC work, fake work, pass/fail/cancel, and production-mining gate behavior. Write `display-input.md` with `IO-001`, `UI-001`, `UI-002`, `UI-003`, startup display marker if observed, `runtime_display_input_status: pending - no runtime display/input route or physical input observation` unless truly proven, and an explicit statement that startup-only SSD1306 evidence cannot verify runtime display pages, screen flow, LVGL parity, or input hardware behavior per D-12.</action>
  <verify>
    <automated>test -f docs/parity/evidence/phase-14-safety-hardware-evidence-completion/self-test-watchdog-load.md && test -f docs/parity/evidence/phase-14-safety-hardware-evidence-completion/display-input.md && test -f docs/parity/evidence/phase-14-safety-hardware-evidence-completion/self-test-watchdog-load/self-test-watchdog-load.log && test -f docs/parity/evidence/phase-14-safety-hardware-evidence-completion/display-input/display-input.log && rg -n "phase14|watchdog_supervisor_status|load_stress_status|self_test_hardware_status|runtime_display_input_status|pending" docs/parity/evidence/phase-14-safety-hardware-evidence-completion/self-test-watchdog-load/self-test-watchdog-load.log docs/parity/evidence/phase-14-safety-hardware-evidence-completion/display-input/display-input.log && rg -n "SELF-001|watchdog_supervisor_status|load_stress_status: pending|self_test_hardware_status: pending|IO-001|UI-001|UI-002|UI-003|runtime_display_input_status: pending|startup-only SSD1306" docs/parity/evidence/phase-14-safety-hardware-evidence-completion/self-test-watchdog-load.md docs/parity/evidence/phase-14-safety-hardware-evidence-completion/display-input.md</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`self-test-watchdog-load.md` contains explicit pending blockers for self-test hardware submodes and load stress unless actual bounded evidence exists.</criterion>
    <criterion>`display-input.md` contains `startup-only SSD1306` and keeps runtime display/input pending unless actual runtime observation exists.</criterion>
    <criterion>`self-test-watchdog-load/self-test-watchdog-load.log` and `display-input/display-input.log` exist and contain Phase 14 status or pending lines.</criterion>
    <criterion>No ad hoc self-test, reboot, workload stress, input actuation, or temporary production route is run.</criterion>
  </acceptance_criteria>
  <done>Self-test, watchdog/load, display, and input evidence is captured narrowly or remains below verified with owner/blocker wording.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Serial/log markers to self-test and watchdog claims | Startup markers can be mistaken for hardware submode or load-stress proof. |
| Display startup output to runtime display/input claims | Physical startup evidence can be over-applied to runtime UI and input behavior. |
| Manifest to wrapper command | Incorrect procedure manifest could authorize unsafe self-test or workload behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-14-04-01 | Tampering | `phase14-self-test-watchdog-load.sh` | mitigate | Require allow-manifest validation and write pending evidence when no bounded workload or self-test route exists. |
| T-14-04-02 | Denial of Service | load/watchdog procedure | mitigate | Do not run load stress unless the manifest and wrapper define bounded workload, timeout, failure markers, recovery, and safe-state checks. |
| T-14-04-03 | Repudiation | display/input evidence | mitigate | Evidence docs must state startup-only versus runtime scope and include exact marker strings. |
| T-14-04-04 | Elevation of Privilege | temporary diagnostics | mitigate | Do not add temporary diagnostic firmware routes unless compile-gated and impossible to expose accidentally; this plan adds no firmware route. |
</threat_model>

<verification>
Plan-level checks:
- `bazel test //scripts:phase14_self_test_watchdog_load_test //scripts:phase14_display_input_test`
- `cargo test -p bitaxe-safety --all-features self_test`
- `cargo test -p bitaxe-safety --all-features watchdog`
- `just parity`
</verification>

<success_criteria>
Self-test hardware, load stress, runtime display, and input parity remain below verified unless exact bounded evidence exists; startup/yield markers are recorded only as narrow observations.
</success_criteria>

<output>
After completion, create `.planning/phases/14-safety-hardware-evidence-completion/14-04-SUMMARY.md`.
</output>
