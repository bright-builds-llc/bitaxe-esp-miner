---
phase: 16-current-commit-release-evidence-completion
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tools/parity/src/release_evidence.rs
  - tools/parity/src/main.rs
  - tools/parity/BUILD.bazel
  - scripts/phase16-http-static-smoke.sh
  - scripts/phase16-http-static-smoke-test.sh
  - scripts/phase16-recovery-regression.sh
  - scripts/phase16-recovery-regression-test.sh
  - scripts/BUILD.bazel
  - docs/parity/evidence/phase-16-current-commit-release-evidence-completion/README.md
  - docs/parity/evidence/phase-16-current-commit-release-evidence-completion/redaction-review.md
autonomous: true
requirements:
  - API-09
  - REL-01
  - REL-02
  - REL-03
  - REL-04
  - REL-07
  - REL-08
  - EVD-05
must_haves:
  truths:
    - "Current-commit package identity is machine-checkable before Phase 16 evidence can support docs or checklist promotion"
    - "HTTP/static evidence covers `/api/system/OTA` route presence in addition to root, static asset, missing static, recovery, WebSocket, API coexistence, and OTAWWW"
    - "Failed-update, interrupted-update, rollback/boot-validation, and erase/recovery operations cannot run without detector, board-info, current manifest, current factory image, explicit URL when needed, allow flags, abort conditions, recovery steps, safe-state markers, and redaction review"
    - "A Phase 16 redaction template exists before generated live logs or API bodies are cited"
  artifacts:
    - path: "tools/parity/src/release_evidence.rs"
      provides: "Current-commit identity and evidence-path validator"
      contains: "ReleaseEvidenceManifest"
    - path: "scripts/phase16-http-static-smoke.sh"
      provides: "Explicit DEVICE_URL HTTP/static/recovery/API/OTA route probe wrapper"
      contains: "POST /api/system/OTA"
    - path: "scripts/phase16-recovery-regression.sh"
      provides: "Phase 16 gated failed-update, interrupted-update, large-erase, and restore wrapper"
      contains: "phase16_gate_status"
    - path: "docs/parity/evidence/phase-16-current-commit-release-evidence-completion/redaction-review.md"
      provides: "Secret and absent-artifact redaction review template"
      contains: "absent artifacts"
  key_links:
    - from: "current git HEAD"
      to: "package manifest source_commit"
      via: "`release-evidence` validator"
      pattern: "release-evidence --manifest"
    - from: "HTTP route probe"
      to: "/api/system/OTA"
      via: "Phase 16 HTTP helper"
      pattern: "POST /api/system/OTA"
    - from: "recovery helper"
      to: "Ultra 205 detector gate"
      via: "`just detect-ultra205` before every live fault/destructive action"
      pattern: "just detect-ultra205"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 16-2026-07-01T12-36-46
generated_at: 2026-07-01T12:59:29Z
---

<objective>
Add the Wave 0 gates required before any Phase 16 live release evidence can be trusted.

Purpose: 16-VALIDATION.md marks this phase non-compliant until current-commit identity, `/api/system/OTA` probe coverage, destructive/recovery gate tightening, and redaction review scaffolding exist. This plan closes those validation gaps without running live flash, OTA, erase, or network probes.
Output: Tested parity validator, tested Phase 16 HTTP and recovery wrappers, and Phase 16 evidence/redaction scaffolding.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/verification.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-current-commit-release-evidence-completion/16-CONTEXT.md
@.planning/phases/16-current-commit-release-evidence-completion/16-RESEARCH.md
@.planning/phases/16-current-commit-release-evidence-completion/16-VALIDATION.md
@tools/parity/src/main.rs
@tools/parity/src/release_gate.rs
@tools/parity/src/safety_allow.rs
@tools/parity/src/mining_allow.rs
@tools/parity/BUILD.bazel
@scripts/phase13-http-static-smoke.sh
@scripts/phase13-http-static-smoke-test.sh
@scripts/phase13-recovery-regression.sh
@scripts/phase13-recovery-regression-test.sh
@scripts/BUILD.bazel

<interfaces>
From `tools/flash/src/main.rs`, `flash-monitor` evidence JSON contains these Phase 16 identity fields:
```json
{
  "command_kind": "flash-monitor",
  "board": "205",
  "firmware_commit": "<full source commit>",
  "reference_commit": "<reference commit>",
  "manifest_path": "<path to bitaxe-ultra205-package.json>",
  "trusted_output": true,
  "observed_firmware_commit": "<short or full firmware_commit marker>",
  "observed_reference_commit": "<reference commit>",
  "conclusion": "passed - wrapper-owned serial boot evidence captured; ..."
}
```

Existing Phase 13 route helper probes these paths and must be extended, not weakened:
```bash
GET /
GET /assets/app.css.gz
GET /phase13-missing-static
GET /recovery
GET /api/system/info
GET /api/phase13-unknown
GET /api/ws
GET /api/ws/live
POST /api/system/OTAWWW
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add current-commit release evidence validator and scaffold</name>
  <requirements>REL-04, REL-07, EVD-05</requirements>
  <files>tools/parity/src/release_evidence.rs, tools/parity/src/main.rs, tools/parity/BUILD.bazel, docs/parity/evidence/phase-16-current-commit-release-evidence-completion/README.md, docs/parity/evidence/phase-16-current-commit-release-evidence-completion/redaction-review.md</files>
  <read_first>
    <file>tools/parity/src/main.rs</file>
    <file>tools/parity/src/release_gate.rs</file>
    <file>tools/parity/BUILD.bazel</file>
    <file>docs/parity/evidence/phase-15-bm1366-mining-evidence-completion.md</file>
    <file>.planning/phases/16-current-commit-release-evidence-completion/16-CONTEXT.md</file>
    <file>.planning/phases/16-current-commit-release-evidence-completion/16-VALIDATION.md</file>
  </read_first>
  <behavior>
    - Test 1: current git HEAD mismatch with package manifest `source_commit` is rejected with `current git HEAD does not match package source_commit`.
    - Test 2: flash evidence `firmware_commit` mismatch with package manifest `source_commit` is rejected with `flash evidence firmware_commit mismatch`.
    - Test 3: flash evidence `observed_firmware_commit` not matching the package source commit prefix is rejected with `observed firmware commit mismatch`.
    - Test 4: flash evidence `trusted_output` not `true` is rejected with `flash evidence is not trusted`.
    - Test 5: evidence paths outside `docs/parity/evidence/phase-16-current-commit-release-evidence-completion` are rejected with `evidence path is outside Phase 16 root`.
    - Test 6: `--require-redaction-passed` rejects missing redaction review or review text that does not contain `redaction_status: passed`.
  </behavior>
  <action>Add `tools/parity/src/release_evidence.rs` with `ReleaseEvidenceManifest`, `ReleaseEvidenceReport`, pure validation helpers, and unit tests. The CLI command must be `bazel run //tools/parity:report -- release-evidence --manifest <package-json> --evidence-root docs/parity/evidence/phase-16-current-commit-release-evidence-completion [--flash-evidence-json <json>] [--redaction-review <md>] [--require-redaction-passed]`. The validator must read the package manifest JSON, read current git HEAD using `git rev-parse HEAD` in the local environment for the CLI path, and require package `source_commit` to equal current HEAD. When `--flash-evidence-json` is present, require `command_kind == "flash-monitor"`, `board == "205"`, `trusted_output == true`, `firmware_commit == package.source_commit`, `reference_commit == package.reference_commit`, `observed_reference_commit == package.reference_commit`, and `observed_firmware_commit` equal to either the full package source commit or a prefix of at least 12 characters. Require `manifest_path` to end with `bitaxe-ultra205-package.json`, require `flash_image_path` or `flash_command` to contain `bitaxe-ultra205-factory.bin`, and require `log_path`, `monitor_log_path`, and the supplied flash evidence JSON path to be under the supplied Phase 16 evidence root. Add `mod release_evidence;`, `ReleaseEvidence(ReleaseEvidenceArgs)`, and `run_release_evidence_command` to `tools/parity/src/main.rs`; add `src/release_evidence.rs` to `tools/parity/BUILD.bazel`. Create `docs/parity/evidence/phase-16-current-commit-release-evidence-completion/README.md` with sections `Scope`, `Required Command Order`, `Current-Commit Identity Gate`, `Live Evidence Packs`, `Blocked Evidence Rules`, `Checklist Promotion Rules`, and `Prohibited Scope`. Create `redaction-review.md` with `redaction_status: pending` and review checkboxes for API bodies, WebSocket frames, recovery logs, destructive logs, terminal snippets, private `DEVICE_URL`, private endpoints, pool credentials, worker secrets, Wi-Fi credentials, API tokens, NVS secret values, local terminal secrets, USB port, MAC address, package paths, and absent artifacts. Do not use standalone body `---` separators.</action>
  <verify>
    <automated>cargo test -p bitaxe-parity --all-features release_evidence && bazel test //tools/parity:tests --test_filter=release_evidence && rg -n "Current-Commit Identity Gate|Required Command Order|redaction_status: pending|DEVICE_URL|Wi-Fi credentials|pool credentials|WebSocket frames|absent artifacts" docs/parity/evidence/phase-16-current-commit-release-evidence-completion/README.md docs/parity/evidence/phase-16-current-commit-release-evidence-completion/redaction-review.md</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`tools/parity/src/release_evidence.rs` defines `ReleaseEvidenceManifest` and validates package source commit, flash evidence commit, trusted output, evidence-root paths, and redaction status.</criterion>
    <criterion>`tools/parity/src/main.rs` exposes `release-evidence` with `--manifest`, `--evidence-root`, `--flash-evidence-json`, `--redaction-review`, and `--require-redaction-passed`.</criterion>
    <criterion>`tools/parity/BUILD.bazel` contains `src/release_evidence.rs`.</criterion>
    <criterion>README and redaction template exist under `docs/parity/evidence/phase-16-current-commit-release-evidence-completion/` and include all required sections/categories.</criterion>
  </acceptance_criteria>
  <done>Current-commit release evidence can be machine-checked before Phase 16 docs/checklist promotion, and redaction review starts from an explicit template.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 16 HTTP/static/recovery route probe wrapper</name>
  <requirements>API-09, REL-01, REL-02, REL-03, EVD-05</requirements>
  <files>scripts/phase16-http-static-smoke.sh, scripts/phase16-http-static-smoke-test.sh, scripts/BUILD.bazel</files>
  <read_first>
    <file>scripts/phase13-http-static-smoke.sh</file>
    <file>scripts/phase13-http-static-smoke-test.sh</file>
    <file>scripts/BUILD.bazel</file>
    <file>crates/bitaxe-api/src/update_plan.rs</file>
    <file>firmware/bitaxe/src/http_api.rs</file>
  </read_first>
  <action>Create `scripts/phase16-http-static-smoke.sh` from the Phase 13 helper pattern, with default `out_dir=docs/parity/evidence/phase-16-current-commit-release-evidence-completion/http-static-recovery` and first log line `phase16_http_static_smoke`. It must accept `--device-url`, `--manifest`, and `--out-dir`; require explicit `DEVICE_URL`; block without scanning when missing or invalid; sanitize target and body snippets using the existing secret redaction categories; and record method, path, sanitized target, actual status, selected headers, sanitized response snippet or marker, expected result, actual result, route conclusion, and final conclusion. Probe these exact routes in this order: `GET /`, `GET /assets/app.css.gz`, `GET /phase16-missing-static`, `GET /recovery`, `GET /api/system/info`, `GET /api/phase16-unknown`, `GET /api/ws`, `GET /api/ws/live`, `POST /api/system/OTA` with an empty body, and `POST /api/system/OTAWWW` with an empty body. For `POST /api/system/OTA`, treat status `400`, `409`, `413`, `415`, `422`, or `500` as route-present when the body/header proves a firmware OTA validation path rather than static fallback; record `ota_route_presence: passed` only when the route is not a static 302/404 fallback. For `POST /api/system/OTAWWW`, keep expected public marker `Wrong API input` and record `otawww_rel03_status: deferred`. Create `scripts/phase16-http-static-smoke-test.sh` with fake curl coverage for missing URL, invalid URL, all required routes including `POST /api/system/OTA`, secret redaction, WebSocket no-upgrade responses, OTAWWW gap response, and no network scanning. Register `phase16_http_static_smoke` and `phase16_http_static_smoke_test` in `scripts/BUILD.bazel`.</action>
  <verify>
    <automated>bash -n scripts/phase16-http-static-smoke.sh && bash -n scripts/phase16-http-static-smoke-test.sh && bazel test //scripts:phase16_http_static_smoke_test && rg -n "phase16_http_static_smoke|POST /api/system/OTA|ota_route_presence|POST /api/system/OTAWWW|otawww_rel03_status|network_scan: disabled" scripts/phase16-http-static-smoke.sh scripts/phase16-http-static-smoke-test.sh scripts/BUILD.bazel</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Helper writes blocked evidence and exits 0 when `DEVICE_URL` is missing or invalid.</criterion>
    <criterion>Helper probes all ten required routes, including `POST /api/system/OTA` and `POST /api/system/OTAWWW`.</criterion>
    <criterion>Helper never scans or infers the target; it uses only explicit `DEVICE_URL`.</criterion>
    <criterion>Test verifies secret values are redacted from body snippets and curl errors.</criterion>
  </acceptance_criteria>
  <done>Live HTTP/static/recovery evidence has Phase 16 route coverage, including the `/api/system/OTA` Wave 0 gap.</done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 16 recovery/destructive preflight wrapper</name>
  <requirements>REL-08, REL-02, REL-01, EVD-05</requirements>
  <files>scripts/phase16-recovery-regression.sh, scripts/phase16-recovery-regression-test.sh, scripts/BUILD.bazel</files>
  <read_first>
    <file>scripts/phase13-recovery-regression.sh</file>
    <file>scripts/phase13-recovery-regression-test.sh</file>
    <file>scripts/phase13-monitor-capture.sh</file>
    <file>scripts/BUILD.bazel</file>
    <file>AGENTS.md</file>
    <file>.planning/phases/16-current-commit-release-evidence-completion/16-CONTEXT.md</file>
  </read_first>
  <action>Create `scripts/phase16-recovery-regression.sh` from the Phase 13 recovery helper pattern with default evidence namespace `phase16_recovery_regression`. It must accept `--manifest`, `--factory-image`, `--ota-image`, `--port`, `--out-dir`, optional `--device-url`, and allow flags `--allow-failed-update`, `--allow-large-erase`, and `--allow-interrupted-ota`. Add a shared `require_phase16_live_action_gate <action-name>` used before every allowed failed-update, large-erase, and interrupted-update action. The gate must write `phase16_gate_status: running`, rerun `just detect-ultra205`, require exactly one `port=`, require detector port equals `--port`, run `espflash board-info --chip esp32s3 --port <port> --non-interactive`, require board-info success, require `--manifest`, `--factory-image`, and `--ota-image` files to exist, require manifest `source_commit` to equal `git rev-parse HEAD`, require manifest artifacts to include `bitaxe-ultra205-factory.bin` and `esp-miner.bin`, record detector output path, board-info output path, abort conditions, recovery steps, post-action safe-state markers, and `phase16_gate_status: passed` before the action. Without an allow flag, write pending evidence and do not run the action. With missing `DEVICE_URL`, block failed-update and interrupted-update without curl. With `--allow-large-erase`, run only the exact command `espflash erase-flash --chip esp32s3 --port <port> --non-interactive`, restore only through `just flash board=205 port=<port> image=<factory-image> manifest=<manifest> evidence-dir=<out-dir>/large-erase-restore`, run `scripts/phase13-monitor-capture.sh`, and rerun `scripts/phase16-http-static-smoke.sh` when `DEVICE_URL` is provided. Do not add raw write commands, voltage/fan/mining stress, network scans, or unbounded fault injection. Create `scripts/phase16-recovery-regression-test.sh` with fake `just`, `espflash`, `curl`, monitor, and HTTP helper commands proving default pending behavior, gate failure on detector mismatch, gate failure on stale manifest `source_commit`, gate success before failed-update, exact large erase command rendering, exact restore command rendering, interrupted timeout evidence fields, and secret redaction. Register `phase16_recovery_regression` and `phase16_recovery_regression_test` in `scripts/BUILD.bazel`.</action>
  <verify>
    <automated>bash -n scripts/phase16-recovery-regression.sh && bash -n scripts/phase16-recovery-regression-test.sh && bazel test //scripts:phase16_recovery_regression_test && rg -n "require_phase16_live_action_gate|phase16_gate_status|just detect-ultra205|espflash board-info --chip esp32s3|espflash erase-flash --chip esp32s3 --port|just flash board=205|source_commit|bitaxe-ultra205-factory\\.bin|esp-miner\\.bin" scripts/phase16-recovery-regression.sh scripts/phase16-recovery-regression-test.sh scripts/BUILD.bazel</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Every allowed failed-update, large-erase, and interrupted-update action calls the shared gate before live mutation.</criterion>
    <criterion>The gate requires detector, board-info, current manifest source commit, factory image, OTA image, abort conditions, recovery steps, and safe-state markers.</criterion>
    <criterion>Helper defaults to pending without allow flags and blocks rather than running curl/erase when prerequisites are missing.</criterion>
    <criterion>Tests prove the exact erase and restore command shapes and reject stale/wrong-device gate inputs.</criterion>
  </acceptance_criteria>
  <done>Phase 16 destructive and fault-injection evidence cannot run without the documented current-commit recovery gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| package/flash evidence to release claims | Manifest and flash evidence decide whether current source can support release parity. |
| explicit DEVICE_URL to HTTP helper | A user-provided URL controls network requests to the device. |
| shell wrapper to physical Ultra 205 flash | Recovery helper can erase, reflash, or fault-inject a connected device when gates pass. |
| generated evidence to repository | Logs, JSON, HTTP bodies, and terminal snippets can leak secrets or private endpoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-16-01-01 | Tampering | `release-evidence` validator | mitigate | Compare current git HEAD, package source commit, flash evidence firmware commit, observed firmware marker, trusted-output flag, and Phase 16 evidence paths before promotion. |
| T-16-01-02 | Information Disclosure | HTTP helper | mitigate | Require explicit `DEVICE_URL`, disable scanning, sanitize target and response snippets, and log private URL categories for redaction review. |
| T-16-01-03 | Denial of Service | recovery wrapper | mitigate | Require detector, board-info, current manifest/factory image, allow flags, abort conditions, recovery steps, and safe-state markers before any erase or interrupted update action. |
| T-16-01-04 | Repudiation | redaction/release docs | mitigate | Create redaction template and validator support so final docs can cite only reviewed artifacts. |
| T-16-01-05 | Elevation of Privilege | OTAWWW claim | mitigate | Keep OTAWWW route evidence as `deferred` unless whole-`www` update plus interrupted-update hardware-regression exists per D-07. |
</threat_model>

<verification>
Plan-level checks:

- `cargo test -p bitaxe-parity --all-features release_evidence`
- `bazel test //tools/parity:tests --test_filter=release_evidence`
- `bazel test //scripts:phase16_http_static_smoke_test //scripts:phase16_recovery_regression_test`
- `just parity`
- If committing Rust or shell changes: `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, `cargo test --all-features`, and `just test`
</verification>

<success_criteria>
Phase 16 Wave 0 validation gaps are closed before live release, OTA, or recovery evidence can run: current-commit identity is machine-checkable, `/api/system/OTA` is probed in HTTP/static evidence, destructive recovery gates apply to every risky action, and redaction review starts from a complete template.
</success_criteria>

<output>
After completion, create `.planning/phases/16-current-commit-release-evidence-completion/16-01-SUMMARY.md`.
</output>
