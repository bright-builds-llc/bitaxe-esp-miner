---
phase: 02-ultra-205-config-and-nvs-model
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - Cargo.lock
  - MODULE.bazel.lock
  - crates/bitaxe-config/Cargo.toml
  - crates/bitaxe-config/BUILD.bazel
  - crates/bitaxe-config/fixtures/ultra-205-defaults.csv
  - crates/bitaxe-config/fixtures/catalog.json
  - crates/bitaxe-config/fixtures/nvs-schema.json
  - crates/bitaxe-config/fixtures/nvs-migrations.json
  - crates/bitaxe-config/fixtures/settings-updates.json
  - crates/bitaxe-config/src/lib.rs
  - crates/bitaxe-config/src/defaults.rs
  - crates/bitaxe-config/src/catalog.rs
autonomous: true
requirements: ["CFG-01", "CFG-02", "CFG-06"]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 2-2026-06-26T15-47-58
generated_at: 2026-06-26T16:10:11.762Z
must_haves:
  truths:
    - "Ultra 205 defaults expose hostname, pool, ASIC, fan, self-test, device, and board values from config-205.cvs."
    - "Board/device/ASIC catalog represents Ultra 205 as the active evidence target and keeps every non-205 upstream board not hardware-verified."
    - "Reference-derived fixtures carry source/provenance metadata and no real operator Wi-Fi or private pool credentials."
  artifacts:
    - path: "crates/bitaxe-config/fixtures/ultra-205-defaults.csv"
      provides: "Golden Ultra 205 seed defaults copied from reference/esp-miner/config-205.cvs with provenance header comments."
    - path: "crates/bitaxe-config/src/defaults.rs"
      provides: "Typed Ultra205Defaults contract and tests for CFG-01."
      exports: ["Ultra205Defaults", "ultra_205_defaults"]
    - path: "crates/bitaxe-config/src/catalog.rs"
      provides: "Typed board/device/ASIC catalog with verification scope."
      exports: ["BoardCatalogEntry", "AsicProfile", "VerificationScope", "ultra_205_catalog_entry", "board_catalog"]
  key_links:
    - from: "crates/bitaxe-config/src/defaults.rs"
      to: "crates/bitaxe-config/fixtures/ultra-205-defaults.csv"
      via: "unit/golden test"
      pattern: "ultra_205_defaults.*config-205"
    - from: "crates/bitaxe-config/src/catalog.rs"
      to: "reference/esp-miner/main/device_config.h"
      via: "module-level reference breadcrumb"
      pattern: "reference/esp-miner/main/device_config.h"
---

<objective>
Create the Phase 2 fixture/test foundation plus typed Ultra 205 defaults and board catalog contracts.

Purpose: CFG-01, CFG-02, and CFG-06 need exact reference-derived data before NVS, validation, and persistence work can build on it.
Output: Fixture files, dependency wiring, defaults module, catalog module, and focused Bazel/Cargo test coverage.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-ultra-205-config-and-nvs-model/02-CONTEXT.md
@.planning/phases/02-ultra-205-config-and-nvs-model/02-RESEARCH.md
@.planning/phases/02-ultra-205-config-and-nvs-model/02-VALIDATION.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@PROVENANCE.md
@reference/esp-miner/config-205.cvs
@reference/esp-miner/main/device_config.h
@crates/bitaxe-config/src/lib.rs
@crates/bitaxe-config/Cargo.toml
@crates/bitaxe-config/BUILD.bazel
@crates/bitaxe-core/src/lib.rs

<interfaces>
From crates/bitaxe-config/src/lib.rs before this plan:
```rust
pub struct Phase1BoardSelection { ... }
impl Phase1BoardSelection {
    pub const fn ultra_205() -> Self;
    pub const fn board(&self) -> BoardTarget;
    pub const fn asic(&self) -> AsicTarget;
    pub const fn device_model(&self) -> &'static str;
    pub const fn board_version(&self) -> u16;
    pub const fn asic_model(&self) -> &'static str;
    pub const fn asic_frequency_mhz(&self) -> u16;
    pub const fn asic_voltage_mv(&self) -> u16;
}
```

From crates/bitaxe-core/src/lib.rs before this plan:
```rust
pub enum BoardTarget { Ultra205 }
pub enum AsicTarget { Bm1366 }
impl AsicTarget { pub const fn display_name(self) -> &'static str; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 2 fixture and Bazel test infrastructure</name>
  <read_first>
    crates/bitaxe-config/Cargo.toml
    crates/bitaxe-config/BUILD.bazel
    Cargo.toml
    MODULE.bazel
    reference/esp-miner/config-205.cvs
    reference/esp-miner/main/device_config.h
    reference/esp-miner/main/nvs_config.c
    PROVENANCE.md
    .planning/phases/02-ultra-205-config-and-nvs-model/02-VALIDATION.md
  </read_first>
  <files>crates/bitaxe-config/Cargo.toml, crates/bitaxe-config/BUILD.bazel, Cargo.lock, MODULE.bazel.lock, crates/bitaxe-config/fixtures/ultra-205-defaults.csv, crates/bitaxe-config/fixtures/catalog.json, crates/bitaxe-config/fixtures/nvs-schema.json, crates/bitaxe-config/fixtures/nvs-migrations.json, crates/bitaxe-config/fixtures/settings-updates.json</files>
  <action>
    Add `serde = { workspace = true }` and `thiserror = { workspace = true }` under `[dependencies]` in `crates/bitaxe-config/Cargo.toml`; add `csv = "1.4.0"` and `serde_json = { workspace = true }` under `[dev-dependencies]`. Update `crates/bitaxe-config/BUILD.bazel` so `rust_library(name = "bitaxe_config")` keeps the current `src/lib.rs` source list for this task, uses deps `//crates/bitaxe-core:bitaxe_core`, `@crates//:serde`, and `@crates//:thiserror`, and makes the fixture files available to tests through `data = glob(["fixtures/**"])` or equivalent runfiles support. Do not add `src/defaults.rs` or `src/catalog.rs` to `srcs` until Task 2 creates those files.

    Create `crates/bitaxe-config/fixtures/ultra-205-defaults.csv` from `reference/esp-miner/config-205.cvs` per D-01 and D-13. Include a provenance comment block at the top with these exact strings: `source=reference/esp-miner/config-205.cvs`, `reference_commit=c1915b0a63bfabebdb95a515cedfee05146c1d50`, and `license_posture=reference-derived GPL-3.0 source data; do not treat as MIT-only until release review`. The fixture data must include the exact rows and values from the reference file, including `hostname=bitaxe`, `stratumurl=public-pool.io`, `stratumport=3333`, `stratumtls=0`, `stratumcert=x`, `stratumuser=bc1qnp980s5fpp8l94p5cvttmtdqy8rvrq74qly2yrfmzkdsntqzlc5qkc4rkq.bitaxe`, `stratumpass=x`, `stratumdiff=1000`, `stratumxnsub=0`, fallback pool rows `fbstratumurl=solo.ckpool.org`, `fbstratumport=3333`, `fbstratumtls=0`, `fbstratumcert=x`, `fbstratumuser=bc1qnp980s5fpp8l94p5cvttmtdqy8rvrq74qly2yrfmzkdsntqzlc5qkc4rkq.bitaxe`, `fbstratumpass=x`, `fbstratumdiff=1000`, `fbstratumxnsum=0`, plus `asicfrequency=485`, `asicvoltage=1200`, `asicmodel=BM1366`, `devicemodel=ultra`, `boardversion=205`, `rotation=0`, `autofanspeed=1`, `fanspeed=100`, `selftest=1`, and `overheat_mode=0`.

    Create machine-readable JSON fixtures for later tasks per D-14:
    - `catalog.json` with an Ultra 205 board entry containing `board_version="205"`, `family="Ultra"`, `asic_model="BM1366"`, `asic_count=1`, `frequency_options=[400,425,450,475,485,500,525,550,575]`, `voltage_options=[1100,1150,1200,1250,1300]`, `default_frequency_mhz=485`, `default_voltage_mv=1200`, `core_count=112`, `small_core_count=894`, `hash_domains=4`, `default_asic_timeout=2000`, `DS4432U=true`, `INA260=true`, `TPS546=false`, `plug_sense=true`, `asic_enable=true`, `power_consumption_target=12`, and `verification_scope="active_ultra_205"`. Include non-205 board versions `2.2`, `102`, `0.11`, `201`, `202`, `203`, `204`, `207`, `302`, `303`, `400`, `401`, `402`, `403`, `600`, `601`, `602`, `603`, `650`, `701`, `702`, and `801` with `verification_scope="not_hardware_verified"`.
    - `nvs-schema.json` with source metadata and every `nvs_key_name` from `reference/esp-miner/main/nvs_config.c` settings rows: `wifissid`, `wifipass`, `hostname`, `stratumprot`, `stratumurl`, `stratumport`, `stratumuser`, `stratumpass`, `stratumdiff`, `stratumxnsub`, `stratumtls`, `stratumcert`, `sv2chantype`, `sv2authpubkey`, `stratumdecode`, `fbstratumprot`, `fbstratumurl`, `fbstratumport`, `fbstratumuser`, `fbstratumpass`, `fbstratumdiff`, `stratumfbxnsub`, `fbstratumtls`, `fbstratumcert`, `fbsv2chantype`, `fbsv2authpubk`, `fbstratumdecode`, `usefbstartum`, `asicfrequency_f`, `asicvoltage`, `oc_enabled`, `display`, `rotation`, `invertscreen`, `displayOffset`, `displayTimeout`, `autofanspeed`, `manualfanspeed`, `minfanspeed`, `temptarget`, `overheat_mode`, `statsFrequency`, `bestdiff`, `selftest`, `swarmconfig`, `themescheme`, `themecolors`, `scoreboard`, `boardversion`, `devicemodel`, `asicmodel`, `plug_sense`, `asic_enable`, `EMC2101`, `EMC2103`, `EMC2302`, `emc_int_temp`, `emc_ideality_f`, `emc_beta_comp`, `temp_offset`, `DS4432U`, `INA260`, `TPS546`, `TMP1075`, `power_cons_tgt`, `selftest_temp`, `selftest_warm`, and `selftest_max`.
    - `nvs-migrations.json` with cases for `asicfrequency -> asicfrequency_f`, `fanspeed -> manualfanspeed`, `stratumprot` u16 to `SV1`/`SV2`, `fbstratumprot` u16 to `SV1`/`SV2`, `sv2chantype` u16 to `extended`/`standard`, `fbsv2chantype` u16 to `extended`/`standard`, and legacy `fbSv2ChanType -> fbsv2chantype`.
    - `settings-updates.json` with valid cases for `frequency=485`, `coreVoltage=1200`, `manualFanSpeed=42`, `autofanspeed=true`, `hostname="bitaxe"`, `stratumPort=3333`, `stratumTLS=0`, and invalid cases for `frequency=0`, `coreVoltage=0`, `manualFanSpeed=101`, `temptarget=67`, `hostname=""`, `stratumPort=65536`, `stratumTLS=4`, `stratumProtocol="SV3"`, and `stratumV2ChannelType="bad"`.
  </action>
  <verify>
    <automated>cargo update && bazel test //crates/bitaxe-config:tests</automated>
  </verify>
  <acceptance_criteria>
    `crates/bitaxe-config/Cargo.toml` contains `serde = { workspace = true }`, `thiserror = { workspace = true }`, `csv = "1.4.0"`, and `serde_json = { workspace = true }`.
    `crates/bitaxe-config/BUILD.bazel` contains `@crates//:serde`, `@crates//:thiserror`, and `fixtures/**`, and still compiles with only `src/lib.rs` before Task 2 creates `src/defaults.rs` and `src/catalog.rs`.
    `rg -n 'bc1qnp980s5fpp8l94p5cvttmtdqy8rvrq74qly2yrfmzkdsntqzlc5qkc4rkq.bitaxe|fbstratumxnsum|asicfrequency,.*485|asicvoltage,.*1200' crates/bitaxe-config/fixtures/ultra-205-defaults.csv` finds matches.
    `rg -n 'verification_scope.*active_ultra_205|verification_scope.*not_hardware_verified|\"205\"|\"601\"|\"BM1366\"' crates/bitaxe-config/fixtures/catalog.json` finds matches.
    `rg -n 'asicfrequency_f|manualfanspeed|fbsv2authpubk|emc_ideality_f|power_cons_tgt|selftest_max' crates/bitaxe-config/fixtures/nvs-schema.json` finds matches.
    `reference/esp-miner` has no modified files in `git status --short reference/esp-miner`.
  </acceptance_criteria>
  <done>Fixture and dependency infrastructure exists, carries provenance, is Bazel-visible, and includes exact Phase 2 seed values plus schema/update cases needed by later tasks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Ultra 205 defaults and board catalog types</name>
  <read_first>
    crates/bitaxe-config/src/lib.rs
    crates/bitaxe-config/src/defaults.rs
    crates/bitaxe-config/src/catalog.rs
    crates/bitaxe-config/fixtures/ultra-205-defaults.csv
    crates/bitaxe-config/fixtures/catalog.json
    crates/bitaxe-core/src/lib.rs
    reference/esp-miner/config-205.cvs
    reference/esp-miner/main/device_config.h
    docs/adr/0014-pivot-to-ultra-205-bm1366-first-parity.md
  </read_first>
  <files>crates/bitaxe-config/src/lib.rs, crates/bitaxe-config/src/defaults.rs, crates/bitaxe-config/src/catalog.rs</files>
  <behavior>
    - Test `ultra_205_defaults_match_config_205_fixture`: `ultra_205_defaults()` returns every D-01 value, including the exact public-pool wallet user and `fbstratumxnsum=0`.
    - Test `ultra_205_catalog_entry_uses_bm1366_reference_values`: board 205 exposes Ultra/BM1366, ASIC count 1, frequency options `[400,425,450,475,485,500,525,550,575]`, voltage options `[1100,1150,1200,1250,1300]`, default 485 MHz, default 1200 mV, DS4432U/INA260 true, TPS546 false, and power target 12.
    - Test `non_205_board_entries_are_not_hardware_verified`: every board version except `205` has `VerificationScope::NotHardwareVerified`.
  </behavior>
  <action>
    Replace the Phase 1 identity-only public surface with module exports while preserving `Phase1BoardSelection::ultra_205()` as a compatibility shim. Add `src/defaults.rs` and `src/catalog.rs`. Update `crates/bitaxe-config/BUILD.bazel` so `rust_library(name = "bitaxe_config")` includes `src/defaults.rs` and `src/catalog.rs` in `srcs`. Add `pub mod defaults;` and `pub mod catalog;` in `src/lib.rs`, and re-export `Ultra205Defaults`, `ultra_205_defaults`, `BoardCatalogEntry`, `AsicProfile`, `BoardCapabilities`, `VerificationScope`, `ultra_205_catalog_entry`, and `board_catalog`.

    In `src/defaults.rs`, define `Ultra205Defaults` with typed fields for `hostname`, primary pool, fallback pool, ASIC frequency/voltage/model, device model, board version, rotation, fan settings, self-test, and overheat mode. Values must be exactly: hostname `bitaxe`; primary URL `public-pool.io`; primary port `3333`; TLS `0`; cert `x`; user `bc1qnp980s5fpp8l94p5cvttmtdqy8rvrq74qly2yrfmzkdsntqzlc5qkc4rkq.bitaxe`; password `x`; difficulty `1000`; extranonce subscribe `0`; fallback URL `solo.ckpool.org`; fallback port `3333`; fallback TLS `0`; fallback cert `x`; fallback user `bc1qnp980s5fpp8l94p5cvttmtdqy8rvrq74qly2yrfmzkdsntqzlc5qkc4rkq.bitaxe`; fallback password `x`; fallback difficulty `1000`; fallback extranonce subscribe `0`; ASIC frequency `485`; ASIC voltage `1200`; ASIC model `BM1366`; device model `ultra`; board version `205`; rotation `0`; auto fan speed `true`; manual fan speed `100`; self-test `true`; overheat mode `false`. Add a module-level breadcrumb `Reference: reference/esp-miner/config-205.cvs` per D-16.

    In `src/catalog.rs`, define typed catalog values from `reference/esp-miner/main/device_config.h` per D-02 and D-03. Include ASIC profiles for `BM1397`, `BM1366`, `BM1368`, `BM1370`, and a BM1370 XP profile with XP frequency options. Include family names `Max`, `Ultra`, `Hex`, `Supra`, `Gamma`, `GammaDuo`, `SupraHex`, and `GammaTurbo`. Include board versions from `default_configs[]`: `2.2`, `102`, `0.11`, `201`, `202`, `203`, `204`, `205`, `207`, `302`, `303`, `400`, `401`, `402`, `403`, `600`, `601`, `602`, `603`, `650`, `701`, `702`, and `801`. Set `VerificationScope::ActiveUltra205` only for board `205`; set `VerificationScope::NotHardwareVerified` for every other board so Gamma 601/BM1370 and TPS546-specific boards cannot inherit Ultra 205 evidence.

    Keep this crate pure: do not add ESP-IDF, NVS, Wi-Fi, HTTP, mining, voltage, fan, thermal, power, or ASIC initialization calls.
  </action>
  <verify>
    <automated>bazel test //crates/bitaxe-config:tests --test_filter=ultra_205_defaults</automated>
    <automated>bazel test //crates/bitaxe-config:tests --test_filter=board_catalog</automated>
  </verify>
  <acceptance_criteria>
    `crates/bitaxe-config/src/defaults.rs` contains `Reference: reference/esp-miner/config-205.cvs`, `Ultra205Defaults`, `public-pool.io`, `solo.ckpool.org`, `bc1qnp980s5fpp8l94p5cvttmtdqy8rvrq74qly2yrfmzkdsntqzlc5qkc4rkq.bitaxe`, `BM1366`, `485`, and `1200`.
    `crates/bitaxe-config/src/catalog.rs` contains `VerificationScope`, `ActiveUltra205`, `NotHardwareVerified`, `BM1366`, `BM1370`, `Gamma`, `TPS546`, and board strings `"205"` and `"601"`.
    `crates/bitaxe-config/src/catalog.rs` contains exact BM1366 arrays `[400, 425, 450, 475, 485, 500, 525, 550, 575]` and `[1100, 1150, 1200, 1250, 1300]`.
    `crates/bitaxe-config/src/lib.rs` re-exports `ultra_205_defaults` and `board_catalog`.
    The two focused Bazel commands in `<verify>` pass.
  </acceptance_criteria>
  <done>Ultra 205 defaults and upstream board catalog are typed, tested, fixture-backed, and scoped so only board 205 is the active evidence target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `reference/esp-miner` -> checked-in fixtures | GPL-covered reference data is transformed into Rust test fixtures. |
| Fixture/catalog data -> later firmware/API code | Pure config data will feed later hardware and API surfaces. |
| Board catalog -> parity evidence | Catalog presence could be mistaken for hardware verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / Elevation of Privilege | `catalog.rs` frequency/voltage/fan defaults | mitigate | Type frequency/voltage/fan values as data only and add tests proving no firmware/hardware adapter files are modified in this plan. |
| T-02-02 | Information Disclosure | `fixtures/ultra-205-defaults.csv` and JSON fixtures | mitigate | Use only upstream public defaults; add provenance metadata; acceptance greps for empty Wi-Fi fields and the public reference pool user only. |
| T-02-03 | Tampering / Denial of Service | `fixtures/nvs-schema.json` | mitigate | Seed the schema fixture with exact active key names including `asicfrequency_f`, `manualfanspeed`, and the upstream typo `usefbstartum`; Plan 02 makes these executable schema tests. |
| T-02-04 | Denial of Service | Future float load behavior | accept | This plan only creates fixture/test infrastructure; Plan 02 owns corrupt float fallback tests and implementation. |
| T-02-05 | Spoofing / Safety governance failure | `catalog.rs` verification scope | mitigate | Add `VerificationScope` and tests asserting every non-205 board is `NotHardwareVerified`. |
</threat_model>

<verification>
Run after both tasks:

```bash
bazel test //crates/bitaxe-config:tests
just parity
git status --short reference/esp-miner
```
</verification>

<success_criteria>
- CFG-01: All D-01 Ultra 205 defaults are exposed from `Ultra205Defaults` and covered by focused tests.
- CFG-02: Board/device/ASIC catalog includes upstream non-205 entries while preventing non-205 hardware verification claims.
- CFG-06: Fixture files exist, carry provenance, are visible to Bazel tests, and contain defaults, catalog, NVS schema, migration, and update cases for downstream plans.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ultra-205-config-and-nvs-model/02-01-SUMMARY.md`.
</output>
