---
phase: 06-safety-controllers-and-self-test
plan: "07"
type: execute
wave: 5
depends_on: ["06-03", "06-04", "06-05", "06-06"]
files_modified:
  - Cargo.lock
  - MODULE.bazel.lock
  - crates/bitaxe-api/Cargo.toml
  - crates/bitaxe-api/BUILD.bazel
  - crates/bitaxe-api/src/lib.rs
  - crates/bitaxe-api/src/snapshot.rs
  - crates/bitaxe-api/src/wire.rs
  - crates/bitaxe-api/src/system.rs
  - crates/bitaxe-api/src/statistics.rs
  - crates/bitaxe-api/fixtures/api/system-info-ultra205-safe.json
  - crates/bitaxe-api/fixtures/api/live-telemetry-cases.json
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-07, SAFE-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-06-28T02-37-37
generated_at: 2026-06-28T03:30:00Z
must_haves:
  truths:
    - "API telemetry no longer blindly treats Phase 6-owned values as trustworthy zeroes."
    - "Fresh safety telemetry maps into system info, statistics, and live telemetry projections."
    - "Unavailable, stale, faulted, or unverified safety telemetry remains visible without false hardware claims."
  artifacts:
    - path: "crates/bitaxe-api/src/snapshot.rs"
      provides: "Safety telemetry report to API snapshot mapping"
      exports: ["SafeTelemetrySnapshot", "SafetyTelemetryStatus", "SafetyTelemetryReport"]
    - path: "crates/bitaxe-api/src/lib.rs"
      provides: "Public API exports for Phase 6 safety telemetry"
      exports: ["SafeTelemetrySnapshot", "SafetyTelemetryStatus", "SafetyTelemetryReport"]
    - path: "crates/bitaxe-api/src/wire.rs"
      provides: "System info projection from safety telemetry"
    - path: "crates/bitaxe-api/src/statistics.rs"
      provides: "Statistics projection from safety telemetry"
    - path: "crates/bitaxe-api/fixtures/api/live-telemetry-cases.json"
      provides: "Fresh/stale/fault/unavailable telemetry projection fixtures"
  key_links:
    - from: "crates/bitaxe-api/src/wire.rs"
      to: "crates/bitaxe-api/src/snapshot.rs"
      via: "SystemInfoWire::from_snapshot reads safe telemetry status"
      pattern: "safe_telemetry"
    - from: "crates/bitaxe-api/src/snapshot.rs"
      to: "crates/bitaxe-safety/src/evidence.rs"
      via: "SafetyCriticalEvidence"
      pattern: "SafetyCriticalEvidence"
    - from: "crates/bitaxe-api/src/lib.rs"
      to: "crates/bitaxe-api/src/snapshot.rs"
      via: "public re-export consumed by firmware"
      pattern: "SafetyTelemetryReport"
---

<objective>
Replace Phase 5 zeroed unavailable hardware telemetry with explicit Phase 6 safety telemetry status and projections.

Purpose: API clients should see fresh, stale, faulted, or unavailable safety telemetry states without silently promoting implementation into hardware verification.
Output: API snapshot, wire, statistics, and fixture updates backed by the safety crate.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-safety-controllers-and-self-test/06-CONTEXT.md
@.planning/phases/06-safety-controllers-and-self-test/06-RESEARCH.md
@.planning/phases/06-safety-controllers-and-self-test/06-VALIDATION.md
@.planning/phases/06-safety-controllers-and-self-test/06-03-SUMMARY.md
@.planning/phases/06-safety-controllers-and-self-test/06-04-SUMMARY.md
@.planning/phases/06-safety-controllers-and-self-test/06-05-SUMMARY.md
@crates/bitaxe-api/src/snapshot.rs
@crates/bitaxe-api/src/wire.rs
@crates/bitaxe-api/src/system.rs
@crates/bitaxe-api/src/statistics.rs
@crates/bitaxe-api/src/telemetry.rs
@crates/bitaxe-safety/src/power.rs
@crates/bitaxe-safety/src/thermal.rs
@crates/bitaxe-safety/src/evidence.rs

<interfaces>
Existing API telemetry fields from `crates/bitaxe-api/src/snapshot.rs`:
```rust
pub struct SafeTelemetrySnapshot {
    pub power_watts: f64,
    pub voltage_volts: f64,
    pub current_amps: f64,
    pub chip_temp_celsius: f64,
    pub chip_temp2_celsius: f64,
    pub vr_temp_celsius: f64,
    pub core_voltage_actual_mv: f64,
    pub actual_frequency_mhz: f64,
    pub expected_hashrate_ghs: f64,
    pub fan_speed_percent: u16,
    pub fan_rpm: u16,
    pub fan2_rpm: u16,
    pub wifi_rssi_dbm: i16,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define explicit safety telemetry status and public exports</name>
  <files>Cargo.lock, MODULE.bazel.lock, crates/bitaxe-api/Cargo.toml, crates/bitaxe-api/BUILD.bazel, crates/bitaxe-api/src/lib.rs, crates/bitaxe-api/src/snapshot.rs</files>
  <read_first>
    <file>crates/bitaxe-api/src/lib.rs</file>
    <file>crates/bitaxe-api/src/snapshot.rs</file>
    <file>crates/bitaxe-safety/src/power.rs</file>
    <file>crates/bitaxe-safety/src/thermal.rs</file>
    <file>crates/bitaxe-safety/src/evidence.rs</file>
    <file>.planning/phases/06-safety-controllers-and-self-test/06-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Test 1: `ApiSnapshot::safe_ultra_205()` reports safety telemetry status `Unavailable` and serializes hardware fields as zero/compatible unavailable values.
    - Test 2: a fresh trustworthy `SafetyTelemetryReport` maps into `SafeTelemetrySnapshot` with status `Fresh` and preserves the supplied hardware evidence.
    - Test 3: stale or faulted safety telemetry maps to visible unavailable/fault status and does not silently preserve old numeric readings as fresh.
  </behavior>
  <action>Add `bitaxe-safety = { path = "../bitaxe-safety" }` to `crates/bitaxe-api/Cargo.toml` and `//crates/bitaxe-safety:bitaxe_safety` to `crates/bitaxe-api/BUILD.bazel`. In `snapshot.rs`, replace the comment and method name `unavailable_until_phase_6()` with explicit Phase 6 semantics: define `SafetyTelemetryStatus::{Fresh, Stale { reason }, Fault { reason }, Unavailable { reason }}` and `SafetyTelemetryReport` containing power, voltage, current, chip temp, temp2, VR temp, actual core voltage, actual frequency, expected hashrate, fan duty, fan RPM, fan2 RPM, Wi-Fi RSSI, and evidence state. Keep `SafeTelemetrySnapshot` as the wire-facing numeric projection, but add fields `status: SafetyTelemetryStatus` and `evidence: SafetyCriticalEvidence`. Implement `SafeTelemetrySnapshot::unavailable(reason: &'static str)` with reason `safety_telemetry_unavailable`, and `SafeTelemetrySnapshot::from_report(report: SafetyTelemetryReport)` that maps only fresh trustworthy readings into numeric fields; stale/fault/unavailable reports publish zero-compatible values and preserve status/evidence. Update `lib.rs` to publicly re-export `SafeTelemetrySnapshot`, `SafetyTelemetryStatus`, and `SafetyTelemetryReport` so firmware can use `bitaxe_api::SafetyTelemetryReport` in Plan 06-08. Use D-17 and D-18 terms in test names.</action>
  <acceptance_criteria>
    - `rg -n "SafetyTelemetryStatus|SafetyTelemetryReport|SafeTelemetrySnapshot::unavailable|from_report|safety_telemetry_unavailable|SafetyCriticalEvidence" crates/bitaxe-api/src/snapshot.rs` finds the new telemetry model.
    - `rg -n "SafetyTelemetryReport|SafetyTelemetryStatus" crates/bitaxe-api/src/lib.rs` finds public exports used by firmware.
    - `cargo test -p bitaxe-api --all-features safety_telemetry_model` passes with tests for fresh, stale, faulted, and unavailable mappings.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p bitaxe-api --all-features safety_telemetry_model && bazel test //crates/bitaxe-api:tests --test_filter=safety_telemetry_model</automated>
  </verify>
  <done>`bitaxe-api` owns and exports explicit Phase 6 safety telemetry status/report types without false hardware claims.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project safety telemetry through system info, statistics, and live telemetry</name>
  <files>crates/bitaxe-api/src/wire.rs, crates/bitaxe-api/src/system.rs, crates/bitaxe-api/src/statistics.rs, crates/bitaxe-api/fixtures/api/system-info-ultra205-safe.json, crates/bitaxe-api/fixtures/api/live-telemetry-cases.json</files>
  <read_first>
    <file>crates/bitaxe-api/src/snapshot.rs</file>
    <file>crates/bitaxe-api/src/wire.rs</file>
    <file>crates/bitaxe-api/src/system.rs</file>
    <file>crates/bitaxe-api/src/statistics.rs</file>
    <file>crates/bitaxe-api/src/telemetry.rs</file>
    <file>crates/bitaxe-api/fixtures/api/system-info-ultra205-safe.json</file>
    <file>.planning/phases/06-safety-controllers-and-self-test/06-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Test 1: `SystemInfoWire` reads Phase 6 public hardware fields from `safe_telemetry` rather than stale zero-only placeholders.
    - Test 2: `StatisticsSample` or the selected statistics helper maps from `SafeTelemetrySnapshot` with explicit status handling.
    - Test 3: live telemetry fixtures include fresh, stale, faulted, and unavailable cases and keep unavailable numeric fields compatible with upstream zero/default values.
  </behavior>
  <action>Update `SystemInfoWire::from_snapshot`, `system_info_from_snapshot` tests, `StatisticsSample` or a new `StatisticsSample::from_snapshot(snapshot, timestamp, response_time)` helper, and the live telemetry fixture so Phase 6 fields are populated from `safe_telemetry`. Update `system-info-ultra205-safe.json` to keep compatible zero values while the internal status is unavailable. Ensure stale/fault/unavailable reports publish zero-compatible values and visible status without preserving old numeric readings as fresh. Use D-17 and D-18 terms in test names.</action>
  <acceptance_criteria>
    - `rg -n "safe_telemetry\\.power_watts|safe_telemetry\\.voltage_volts|safe_telemetry\\.current_amps|safe_telemetry\\.fan_rpm|safe_telemetry\\.chip_temp_celsius" crates/bitaxe-api/src/wire.rs crates/bitaxe-api/src/statistics.rs` finds projection use.
    - `rg -n "safety_telemetry_unavailable|power|voltage|current|fanrpm|temp|vrTemp" crates/bitaxe-api/fixtures/api/system-info-ultra205-safe.json crates/bitaxe-api/fixtures/api/live-telemetry-cases.json` finds fixture coverage.
    - `cargo test -p bitaxe-api --all-features safety_telemetry_projection` passes with tests for fresh, stale, faulted, and unavailable projections.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p bitaxe-api --all-features safety_telemetry_projection && bazel test //crates/bitaxe-api:tests --test_filter=safety_telemetry_projection</automated>
  </verify>
  <done>System info, statistics, and live telemetry consume explicit Phase 6 safety telemetry status, distinguish unavailable/stale/faulted/fresh values, and preserve upstream-compatible numeric fields without false hardware claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Safety telemetry -> public API/WebSocket/statistics | Internal telemetry can become user-visible status and numeric values. |
| Safety evidence -> telemetry freshness claims | Evidence labels can affect whether API values appear trustworthy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Information Disclosure | API telemetry | mitigate | Serialize only operational telemetry/status; do not expose secrets, raw NVS values, or internal hardware evidence payloads. |
| T-06-07-02 | Spoofing | `SafeTelemetrySnapshot` | mitigate | Preserve `SafetyTelemetryStatus` and `SafetyCriticalEvidence`; stale/faulted/unavailable readings do not masquerade as fresh values. |
| T-06-07-03 | Tampering | `SystemInfoWire` and statistics projection | mitigate | Map public numeric fields from `safe_telemetry` only through `SafeTelemetrySnapshot::from_report`. |
</threat_model>

<verification>
Run scoped tests for `bitaxe-api`, then `just test && just parity` after the plan. Before any commit, also run the repo Rust gate from `AGENTS.md`: `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`.
</verification>

<success_criteria>
API telemetry exposes fresh/stale/faulted/unavailable safety status while preserving upstream-compatible fields and non-overclaiming evidence semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/06-safety-controllers-and-self-test/06-07-SUMMARY.md`.
</output>
