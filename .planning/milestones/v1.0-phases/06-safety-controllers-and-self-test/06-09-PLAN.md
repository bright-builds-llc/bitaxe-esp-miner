---
phase: 06-safety-controllers-and-self-test
plan: "09"
type: execute
wave: 7
depends_on: ["06-08"]
files_modified:
  - firmware/bitaxe/src/main.rs
  - firmware/bitaxe/src/display_adapter.rs
  - firmware/bitaxe/src/safety_adapter.rs
  - firmware/bitaxe/src/safety_adapter/watchdog.rs
autonomous: true
requirements: [SAFE-06, SAFE-09]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-06-28T02-37-37
generated_at: 2026-06-28T03:30:00Z
must_haves:
  truths:
    - "Safety supervisor work is bounded and yields at the watchdog cadence."
    - "API/log/WebSocket administration remains available while safety supervision runs."
    - "Runtime display/input parity is explicitly reported as a gap unless hardware evidence exists."
  artifacts:
    - path: "firmware/bitaxe/src/safety_adapter/watchdog.rs"
      provides: "Bounded safety supervisor loop"
    - path: "firmware/bitaxe/src/safety_adapter.rs"
      provides: "Safety supervisor facade"
      exports: ["start_safety_supervisor"]
    - path: "firmware/bitaxe/src/display_adapter.rs"
      provides: "Runtime display/input gap publication"
      exports: ["publish_runtime_display_input_boundary"]
    - path: "firmware/bitaxe/src/main.rs"
      provides: "Supervisor and display/input boundary wiring"
  key_links:
    - from: "firmware/bitaxe/src/main.rs"
      to: "firmware/bitaxe/src/safety_adapter.rs"
      via: "start_safety_supervisor"
      pattern: "start_safety_supervisor"
    - from: "firmware/bitaxe/src/main.rs"
      to: "firmware/bitaxe/src/display_adapter.rs"
      via: "publish_runtime_display_input_boundary"
      pattern: "publish_runtime_display_input_boundary"
---

<objective>
Start a bounded firmware safety supervisor and publish explicit runtime display/input gap status.

Purpose: Phase 6 needs watchdog-friendly safety work and honest display/input parity boundaries without expanding into full LVGL/button parity.
Output: Supervisor wiring, watchdog loop helper, and display/input gap status.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-safety-controllers-and-self-test/06-CONTEXT.md
@.planning/phases/06-safety-controllers-and-self-test/06-RESEARCH.md
@.planning/phases/06-safety-controllers-and-self-test/06-VALIDATION.md
@.planning/phases/06-safety-controllers-and-self-test/06-08-SUMMARY.md
@firmware/bitaxe/src/main.rs
@firmware/bitaxe/src/display_adapter.rs
@firmware/bitaxe/src/http_api.rs
@crates/bitaxe-safety/src/watchdog.rs
@docs/parity/evidence/ultra-205-startup-display-debug-2026-06-27.md
@reference/esp-miner/main/display.c
@reference/esp-miner/main/input.c
@reference/esp-miner/main/screen.c
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Start bounded safety supervisor and preserve display/input gap status</name>
  <files>firmware/bitaxe/src/main.rs, firmware/bitaxe/src/display_adapter.rs, firmware/bitaxe/src/safety_adapter.rs, firmware/bitaxe/src/safety_adapter/watchdog.rs</files>
  <read_first>
    <file>firmware/bitaxe/src/main.rs</file>
    <file>firmware/bitaxe/src/display_adapter.rs</file>
    <file>firmware/bitaxe/src/http_api.rs</file>
    <file>crates/bitaxe-safety/src/watchdog.rs</file>
    <file>docs/parity/evidence/ultra-205-startup-display-debug-2026-06-27.md</file>
    <file>reference/esp-miner/main/display.c</file>
    <file>reference/esp-miner/main/input.c</file>
    <file>reference/esp-miner/main/screen.c</file>
    <file>.planning/phases/06-safety-controllers-and-self-test/06-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Test 1: firmware starts a safety supervisor loop that sleeps/yields at the safety-core `WATCHDOG_YIELD_INTERVAL_MS = 100` cadence and logs skipped/available status without blocking HTTP/WebSocket.
    - Test 2: runtime display/input status logs `display_input_status=runtime_gap reason=hardware_evidence_pending startup_only=true` unless a later hardware-evidenced display path is explicitly added.
    - Test 3: startup SSD1306 rendering remains startup-only and is not treated as full LVGL or input parity.
  </behavior>
  <action>Create `firmware/bitaxe/src/safety_adapter/watchdog.rs` with a bounded loop helper that uses `StepSupervisor`, sleeps `Duration::from_millis(100)`, and logs `safety_supervisor_step=yield reason=yield_interval_reached` when the watchdog decision requires yielding. Add `mod watchdog;` to `safety_adapter.rs` when the child module is created. Expose `start_safety_supervisor()` from `safety_adapter.rs`; call it from `main.rs` after the ASIC boot gate and before `http_api::start_http_api()`. The supervisor must run in a named thread `bitaxe-safety-supervisor` and must log failures as `safety_supervisor=unavailable reason=spawn_failed`. In `display_adapter.rs`, add `pub fn publish_runtime_display_input_boundary()` that logs exactly `display_input_status=runtime_gap reason=hardware_evidence_pending startup_only=true`; call it from `main.rs` after startup display rendering. Do not implement full LVGL carousel, display config, timeout, rotation, inversion, broad button routing, or runtime OLED safety pages in this plan. Preserve administration through API/log/WebSocket surfaces per D-13 through D-16.</action>
  <acceptance_criteria>
    - `rg -n "bitaxe-safety-supervisor|safety_supervisor_step=yield|yield_interval_reached|safety_supervisor=unavailable reason=spawn_failed|Duration::from_millis\\(100\\)" firmware/bitaxe/src/safety_adapter/watchdog.rs firmware/bitaxe/src/safety_adapter.rs firmware/bitaxe/src/main.rs` finds supervisor behavior.
    - `rg -n "mod watchdog;" firmware/bitaxe/src/safety_adapter.rs` finds the watchdog child module declaration.
    - `rg -n "publish_runtime_display_input_boundary|display_input_status=runtime_gap reason=hardware_evidence_pending startup_only=true" firmware/bitaxe/src/display_adapter.rs firmware/bitaxe/src/main.rs` finds display/input gap publication.
    - `! rg -n "lvgl|rotation|inversion|display timeout|button long press|carousel" firmware/bitaxe/src/display_adapter.rs firmware/bitaxe/src/main.rs firmware/bitaxe/src/safety_adapter.rs` returns no matches.
    - `rg -n "start_http_api|start_safety_supervisor" firmware/bitaxe/src/main.rs` confirms the supervisor starts before the API shell and does not replace API/log/WebSocket administration.
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p bitaxe-firmware --target xtensa-esp32s3-espidf && bazel build //firmware/bitaxe:firmware && just test</automated>
  </verify>
  <done>Firmware has a watchdog-friendly safety supervisor and explicit runtime display/input gap status while preserving startup OLED evidence as startup-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Safety supervisor -> firmware runtime | Long-running safety work can starve HTTP/WebSocket/log status if modeled as blocking loops. |
| Runtime display/input parity -> user trust | Startup-only OLED evidence can be misrepresented as runtime display/input parity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09-01 | Denial of Service | Safety supervisor thread | mitigate | Use 100 ms sleep/yield cadence and StepSupervisor decisions; no blocking reference-style loops. |
| T-06-09-02 | Spoofing | Display/input status | mitigate | Log explicit runtime gap status and do not claim startup SSD1306 evidence as full display/input parity. |
| T-06-09-03 | Repudiation | Display/input boundary | mitigate | Publish exact runtime-gap log line and preserve evidence docs in the parity plan. |
</threat_model>

<verification>
Run `cargo build -p bitaxe-firmware --target xtensa-esp32s3-espidf`, `bazel build //firmware/bitaxe:firmware`, and `just test`. Before any commit, also run the repo Rust gate from `AGENTS.md`: `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`.
</verification>

<success_criteria>
Firmware safety supervision is bounded and watchdog-friendly, and runtime display/input scope is explicitly documented through logs instead of overclaiming startup OLED evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/06-safety-controllers-and-self-test/06-09-SUMMARY.md`.
</output>
