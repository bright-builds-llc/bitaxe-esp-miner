---
phase: 06-safety-controllers-and-self-test
plan: "08"
type: execute
wave: 6
depends_on: ["06-07"]
files_modified:
  - Cargo.lock
  - MODULE.bazel.lock
  - firmware/bitaxe/Cargo.toml
  - firmware/bitaxe/BUILD.bazel
  - firmware/bitaxe/src/safety_adapter.rs
  - firmware/bitaxe/src/safety_adapter/power.rs
  - firmware/bitaxe/src/safety_adapter/thermal.rs
  - firmware/bitaxe/src/main.rs
  - firmware/bitaxe/src/runtime_snapshot.rs
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-04, SAFE-07, SAFE-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-06-28T02-37-37
generated_at: 2026-06-28T03:30:00Z
must_haves:
  truths:
    - "Firmware publishes Phase 6 safety telemetry/status through the existing API/WebSocket snapshot path."
    - "Default firmware behavior remains fail-closed and observe-only; missing hardware evidence does not enable voltage, fan, ASIC, or mining effects."
    - "Firmware interprets safety effects without writing DS4432U, PWM, or ASIC reset hardware unless typed evidence gates permit them."
  artifacts:
    - path: "firmware/bitaxe/src/safety_adapter.rs"
      provides: "Firmware safety adapter facade"
      exports: ["collect_safety_report", "interpret_safety_effects"]
    - path: "firmware/bitaxe/src/safety_adapter/power.rs"
      provides: "Ultra 205 power observe-only adapter and voltage effect interpreter"
    - path: "firmware/bitaxe/src/safety_adapter/thermal.rs"
      provides: "Thermal/fan observe-only adapter and fault parser"
    - path: "firmware/bitaxe/src/main.rs"
      provides: "Firmware module declaration for safety adapter"
    - path: "firmware/bitaxe/src/runtime_snapshot.rs"
      provides: "API snapshot integration for safety telemetry"
  key_links:
    - from: "firmware/bitaxe/src/runtime_snapshot.rs"
      to: "firmware/bitaxe/src/safety_adapter.rs"
      via: "collect_safety_report"
      pattern: "collect_safety_report"
    - from: "firmware/bitaxe/src/safety_adapter.rs"
      to: "crates/bitaxe-safety/src/effects.rs"
      via: "typed effect interpretation"
      pattern: "SafetyEffect"
    - from: "firmware/bitaxe/src/main.rs"
      to: "firmware/bitaxe/src/safety_adapter.rs"
      via: "Rust module declaration"
      pattern: "mod safety_adapter"
---

<objective>
Add firmware safety adapter shells that publish explicit safety telemetry/status while keeping hardware effects fail-closed unless evidence gates permit them.

Purpose: Phase 6 must connect pure safety decisions to firmware observability without accidentally enabling unsafe voltage, fan, ASIC, or mining behavior.
Output: Firmware safety adapter modules and runtime snapshot integration.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-safety-controllers-and-self-test/06-CONTEXT.md
@.planning/phases/06-safety-controllers-and-self-test/06-RESEARCH.md
@.planning/phases/06-safety-controllers-and-self-test/06-VALIDATION.md
@.planning/phases/06-safety-controllers-and-self-test/06-07-SUMMARY.md
@firmware/bitaxe/src/runtime_snapshot.rs
@firmware/bitaxe/src/asic_adapter/reset.rs
@firmware/bitaxe/src/asic_adapter/status.rs
@crates/bitaxe-safety/src/power.rs
@crates/bitaxe-safety/src/thermal.rs
@crates/bitaxe-safety/src/fault.rs
@crates/bitaxe-safety/src/effects.rs
@crates/bitaxe-api/src/snapshot.rs
@reference/esp-miner/main/power/DS4432U.c
@reference/esp-miner/main/power/INA260.c
@reference/esp-miner/main/thermal/thermal.c
@reference/esp-miner/main/tasks/fan_controller_task.c
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add observe-only power and thermal firmware safety adapters</name>
  <files>Cargo.lock, MODULE.bazel.lock, firmware/bitaxe/Cargo.toml, firmware/bitaxe/BUILD.bazel, firmware/bitaxe/src/main.rs, firmware/bitaxe/src/safety_adapter.rs, firmware/bitaxe/src/safety_adapter/power.rs, firmware/bitaxe/src/safety_adapter/thermal.rs, firmware/bitaxe/src/runtime_snapshot.rs</files>
  <read_first>
    <file>firmware/bitaxe/Cargo.toml</file>
    <file>firmware/bitaxe/BUILD.bazel</file>
    <file>firmware/bitaxe/src/main.rs</file>
    <file>firmware/bitaxe/src/runtime_snapshot.rs</file>
    <file>crates/bitaxe-api/src/snapshot.rs</file>
    <file>crates/bitaxe-safety/src/power.rs</file>
    <file>crates/bitaxe-safety/src/thermal.rs</file>
    <file>crates/bitaxe-safety/src/effects.rs</file>
    <file>reference/esp-miner/main/power/DS4432U.c</file>
    <file>reference/esp-miner/main/power/INA260.c</file>
  </read_first>
  <behavior>
    - Test 1: default firmware safety report is `Unavailable` with reason `hardware_evidence_pending` and maps into API snapshot telemetry.
    - Test 2: observe-only mode may classify readings but never emits a DS4432U write without a hardware-smoke or hardware-regression evidence token.
    - Test 3: voltage effect interpretation suppresses writes, holds reset low where available, blocks work submission, and logs visible fault status for invalid/stale/faulted power decisions.
  </behavior>
  <action>Add `bitaxe-safety = { path = "../../crates/bitaxe-safety" }` to `firmware/bitaxe/Cargo.toml` and `//crates/bitaxe-safety:bitaxe_safety` to `firmware/bitaxe/BUILD.bazel`. Add `mod safety_adapter;` to `firmware/bitaxe/src/main.rs`. Create `firmware/bitaxe/src/safety_adapter.rs` with `mod power;`, `mod thermal;`, `pub fn collect_safety_report() -> bitaxe_api::SafetyTelemetryReport`, and `pub fn interpret_safety_effects(effects: &[SafetyEffect])`; Plan 06-09 adds `mod watchdog;` when it creates the watchdog child module. Create `safety_adapter/power.rs` with constants mirroring the safety core: `DS4432U_I2C_ADDRESS = 0x48`, `DS4432U_OUTPUT0_REGISTER = 0xF8`, `DS4432U_OUTPUT1_REGISTER = 0xF9`, `INA260_I2C_ADDRESS = 0x40`, `INA260_CURRENT_REGISTER = 0x01`, `INA260_BUS_VOLTAGE_REGISTER = 0x02`, and `INA260_POWER_REGISTER = 0x03`. The default `collect_power_report()` must return unavailable reason `hardware_evidence_pending`; it may expose an observe-only compile-env path named `BITAXE_SAFETY_TELEMETRY=observe-only`, but it must not write DS4432U registers unless the typed `VoltageEffectPlan::WriteDs4432u` is produced with hardware evidence. Create `safety_adapter/thermal.rs` with `EMC2101_I2C_ADDRESS = 0x4C`, thermal/fan unavailable reason `thermal_hardware_evidence_pending`, and parsing from raw readings into `ThermalObservation` without PWM writes by default. Update `runtime_snapshot::collect_api_snapshot()` to set `snapshot.safe_telemetry = SafeTelemetrySnapshot::from_report(safety_adapter::collect_safety_report())`. If an effect requests `HoldResetLow`, call the existing ASIC reset boundary only when a reset peripheral is available; otherwise log `safety_effect=hold_reset_low status=unavailable reason=peripherals_unavailable`. Do not add mining sockets, production BM1366 work submission, raw pool credentials, OTA behavior, or non-205 board behavior.</action>
  <acceptance_criteria>
    - `rg -n "bitaxe-safety" firmware/bitaxe/Cargo.toml firmware/bitaxe/BUILD.bazel` finds firmware dependency wiring.
    - `rg -n "mod safety_adapter" firmware/bitaxe/src/main.rs` finds the parent module declaration.
    - `rg -n "mod power;|mod thermal;" firmware/bitaxe/src/safety_adapter.rs` finds child module declarations.
    - `rg -n "collect_safety_report|interpret_safety_effects|hardware_evidence_pending|BITAXE_SAFETY_TELEMETRY" firmware/bitaxe/src/safety_adapter.rs firmware/bitaxe/src/safety_adapter/power.rs` finds adapter entry points and observe-only gate.
    - `rg -n "DS4432U_I2C_ADDRESS.*0x48|DS4432U_OUTPUT0_REGISTER.*0xF8|DS4432U_OUTPUT1_REGISTER.*0xF9|INA260_I2C_ADDRESS.*0x40|INA260_CURRENT_REGISTER.*0x01|INA260_BUS_VOLTAGE_REGISTER.*0x02|INA260_POWER_REGISTER.*0x03" firmware/bitaxe/src/safety_adapter/power.rs` finds concrete I2C constants.
    - `rg -n "EMC2101_I2C_ADDRESS.*0x4C|thermal_hardware_evidence_pending|ThermalObservation" firmware/bitaxe/src/safety_adapter/thermal.rs` finds thermal adapter constants and status.
    - `rg -n "SafeTelemetrySnapshot::from_report|collect_safety_report" firmware/bitaxe/src/runtime_snapshot.rs` finds snapshot integration.
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p bitaxe-firmware --target xtensa-esp32s3-espidf && bazel build //firmware/bitaxe:firmware && cargo test -p bitaxe-api --all-features safety_telemetry</automated>
  </verify>
  <done>Firmware collects Phase 6 safety telemetry/status through typed adapters while default voltage, fan, ASIC, and mining effects remain observe-only/fail-closed without hardware evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pure safety decisions -> ESP-IDF firmware effects | Typed plans can drive reset, voltage suppression, fan targets, status logs, and telemetry. |
| Firmware telemetry -> API/WebSocket clients | Safety readings/status become externally visible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08-01 | Elevation of Privilege | `safety_adapter/power.rs` | mitigate | Default to unavailable/observe-only; DS4432U writes require typed `WriteDs4432u` produced with hardware evidence. |
| T-06-08-02 | Spoofing | Safety telemetry adapter | mitigate | Default reports are explicit unavailable states and cannot masquerade as fresh sensor data. |
| T-06-08-03 | Information Disclosure | Firmware logs/API telemetry | mitigate | Publish only safety status/reason strings and numeric telemetry; do not log pool credentials, NVS secrets, or raw private config. |
</threat_model>

<verification>
Run `cargo build -p bitaxe-firmware --target xtensa-esp32s3-espidf`, `bazel build //firmware/bitaxe:firmware`, and `cargo test -p bitaxe-api --all-features safety_telemetry`. Before any commit, also run the repo Rust gate from `AGENTS.md`: `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`.
</verification>

<success_criteria>
Firmware surfaces Phase 6 safety telemetry/status while defaulting all hardware-control paths to observe-only or fail-closed unless explicit hardware evidence and typed effect plans permit them.
</success_criteria>

<output>
After completion, create `.planning/phases/06-safety-controllers-and-self-test/06-08-SUMMARY.md`.
</output>
