---
phase: 21-live-mining-and-soak-evidence
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tools/parity/src/mining_allow.rs
  - scripts/phase21-live-mining-evidence.sh
  - scripts/phase21-live-mining-evidence-test.sh
  - scripts/BUILD.bazel
  - docs/parity/evidence/phase-21-live-mining-and-soak-evidence/evidence-contract.md
  - docs/parity/evidence/phase-21-live-mining-and-soak-evidence/redaction-review.md
  - docs/parity/evidence/phase-21-live-mining-and-soak-evidence/readiness-audit.md
  - .planning/phases/21-live-mining-and-soak-evidence/21-VALIDATION.md
autonomous: true
requirements:
  - ASIC-07
  - STR-06
  - STR-07
  - SAFE-09
  - EVD-05
must_haves:
  truths:
    - "Executor cannot run Phase 21 mining smoke or bounded soak without a mining-allow manifest accepted for a Phase 21 command shape."
    - "Executor has a Phase 21 evidence scaffold before any live mining, soak, telemetry, or checklist citation is produced."
    - "Executor records that firmware live mining is blocked by default before attempting live smoke or soak evidence."
    - "Executor has a manifest-backed controlled live-mining runtime/harness enablement contract before mining-smoke or bounded-soak wrapper execution."
    - "No Phase 21 wrapper path stores pool credentials, private DEVICE_URL values, Wi-Fi credentials, or NVS secret values in committed evidence."
  artifacts:
    - path: "tools/parity/src/mining_allow.rs"
      provides: "Phase 21 mining allow command-shape validation"
      contains: "phase21-live-mining-evidence.sh"
    - path: "scripts/phase21-live-mining-evidence.sh"
      provides: "Phase 21 live mining, bounded soak, telemetry, watchdog, and redaction evidence wrapper"
      contains: "network_scan: disabled - DEVICE_URL must be explicit"
    - path: "scripts/phase21-live-mining-evidence-test.sh"
      provides: "Regression coverage for wrapper safety and redaction behavior"
      contains: "phase21 live mining evidence tests passed"
    - path: "docs/parity/evidence/phase-21-live-mining-and-soak-evidence/evidence-contract.md"
      provides: "Phase 21 evidence ladder and stop conditions"
      contains: "preflight, live-mining-enablement, bm1366-init-work-result, live-mining-smoke, bounded-soak, live-api-websocket-telemetry, redaction-review, final-summary"
    - path: "docs/parity/evidence/phase-21-live-mining-and-soak-evidence/readiness-audit.md"
      provides: "Wave 0 live-mining readiness audit and enablement contract"
      contains: "firmware_live_mining_status: blocked_by_default"
  key_links:
    - from: "tools/parity/src/mining_allow.rs"
      to: "scripts/phase21-live-mining-evidence.sh"
      via: "allowed_command validation"
      pattern: "phase21-live-mining-evidence\\.sh"
    - from: "scripts/BUILD.bazel"
      to: "scripts/phase21-live-mining-evidence-test.sh"
      via: "sh_test target"
      pattern: "phase21_live_mining_evidence_test"
    - from: "docs/parity/evidence/phase-21-live-mining-and-soak-evidence/readiness-audit.md"
      to: "firmware/bitaxe/src/main.rs"
      via: "static readiness audit"
      pattern: "mining_loop_status=blocked reason=hardware_evidence_ack_missing"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 21-2026-07-04T01-35-47
generated_at: 2026-07-04T02:02:55Z
---

<objective>
Establish the Phase 21 Wave 0 validation foundation: command-shape gates, a phase-owned mining evidence wrapper, evidence/redaction scaffolds, and a live-mining readiness audit.

Purpose: Phase 21 must not run live mining or bounded soak outside detector, package, allow-manifest, redaction, explicit target, and safe-stop gates.
Output: Phase 21 wrapper/test targets, mining-allow support for the exact wrapper command shape, and committed evidence scaffolds that downstream plans consume.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-live-mining-and-soak-evidence/21-CONTEXT.md
@.planning/phases/21-live-mining-and-soak-evidence/21-RESEARCH.md
@.planning/phases/21-live-mining-and-soak-evidence/21-VALIDATION.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/verification.md
@standards/core/testing.md
@standards/languages/rust.md
@tools/parity/src/mining_allow.rs
@scripts/phase15-controlled-mining.sh
@scripts/phase15-controlled-mining-test.sh
@scripts/BUILD.bazel
@firmware/bitaxe/src/main.rs
@firmware/bitaxe/src/asic_adapter/status.rs
@docs/parity/evidence/phase-15-bm1366-mining-evidence-completion.md
@docs/parity/evidence/phase-20-active-safety-hardware-telemetry-evidence/summary.md

<interfaces>
From `tools/parity/src/mining_allow.rs`:
```rust
pub(crate) struct MiningAllowManifest {
    pub(crate) board: String,
    pub(crate) port: String,
    pub(crate) detector_command: String,
    pub(crate) detector_port: String,
    pub(crate) board_info_command: String,
    pub(crate) board_info_status: String,
    pub(crate) package_manifest: Utf8PathBuf,
    pub(crate) source_commit: String,
    pub(crate) reference_commit: String,
    pub(crate) surface: String,
    pub(crate) claim_tier: String,
    pub(crate) evidence_class: String,
    pub(crate) allowed_command: String,
    pub(crate) allowed_inputs: Value,
    pub(crate) abort_conditions: Vec<String>,
    pub(crate) recovery_steps: Vec<String>,
    pub(crate) post_action_safe_state_markers: Vec<String>,
    pub(crate) prerequisite_artifacts: Vec<Utf8PathBuf>,
    pub(crate) evidence_dir: Utf8PathBuf,
    pub(crate) redaction_reviewer: String,
    pub(crate) checklist_rows: Vec<String>,
}
```

Existing required constants:
```rust
const DETECTOR_COMMAND: &str = "just detect-ultra205";
const REQUIRED_ABORT_CONDITIONS: &[&str] = &[
    "detector_mismatch",
    "board_info_failure",
    "missing_trusted_wrapper_markers",
    "redaction_uncertainty",
    "unsafe_temperature_or_power",
    "watchdog_unresponsive",
];
const REQUIRED_SAFE_STATE_MARKERS: &[&str] = &[
    "safe_state: mining=disabled",
    "hardware_control=disabled",
    "work_submission=disabled",
];
```

The Phase 21 wrapper command shape this plan creates and validates:
```text
scripts/phase21-live-mining-evidence.sh --manifest <manifest> --surface mining-smoke|bounded-soak --out-dir <dir> --chip-detect-summary <path> --work-result-summary <path> --readiness-audit <path> --enablement-summary <path> [--device-url <origin>] [--duration-seconds 60..600] [--curl-bin <path>] [--websocket-helper scripts/phase17-websocket-capture.mjs]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend mining allow validation for Phase 21 command shapes</name>
  <files>tools/parity/src/mining_allow.rs</files>
  <read_first>
    - tools/parity/src/mining_allow.rs
    - .planning/phases/21-live-mining-and-soak-evidence/21-CONTEXT.md
    - .planning/phases/21-live-mining-and-soak-evidence/21-VALIDATION.md
    - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion.md
    - standards/core/testing.md
    - standards/languages/rust.md
  </read_first>
  <behavior>
    - Test 1: A `mining-smoke` manifest passes when `allowed_command` starts with `scripts/phase21-live-mining-evidence.sh`, includes `--surface mining-smoke`, `--manifest`, `--out-dir`, `--chip-detect-summary`, `--work-result-summary`, `--readiness-audit`, `--enablement-summary`, and for `claim_tier=live-pool-smoke` includes `--device-url`.
    - Test 2: A `bounded-soak` manifest passes when `allowed_inputs.duration_seconds` is `300`, `allowed_command` includes `--duration-seconds 300`, and evidence class is `soak`.
    - Test 3: A Phase 21 parity-redaction manifest passes only when the command starts with `rg -n -i` and a token starts with `docs/parity/evidence/phase-21-live-mining-and-soak-evidence`.
    - Test 4: Commands containing `raw-bm1366`, `erase-flash`, `voltage-control`, `fan-control`, or a literal token `stratum` still fail.
  </behavior>
  <action>Update `tools/parity/src/mining_allow.rs` per D-07 and D-08. Add an `is_expected_phase21_command(manifest, tokens)` helper and call it from `validate_allowed_command_scope` after the existing Phase 15 check. Preserve all existing Phase 15 behavior. Require the Phase 21 wrapper to include both `--readiness-audit` and `--enablement-summary` for `mining-smoke` and `bounded-soak` so STR-06 live-mining enablement cannot be bypassed. Require every accepted Phase 21 `MiningAllowManifest` to exercise the full schema fields already defined by the validator: `board`, `port`, `detector_port`, `detector_command`, `board_info_command`, `board_info_status`, `package_manifest`, `source_commit`, `reference_commit`, `surface`, `claim_tier`, `evidence_class`, `allowed_command`, structured `allowed_inputs`, `abort_conditions`, `recovery_steps`, `post_action_safe_state_markers`, `prerequisite_artifacts`, `evidence_dir`, `redaction_reviewer`, and `checklist_rows`. For `bounded-soak`, require the allowed command to include `--duration-seconds` with the same integer stored in `allowed_inputs.duration_seconds`. For `parity-redaction`, accept `docs/parity/evidence/phase-21-live-mining-and-soak-evidence` in addition to the existing Phase 15 evidence path. Do not remove or weaken the current required abort conditions, safe-state markers, board `205`, package identity, live-pool input, or prohibited-token checks.</action>
  <verify>
    <automated>cargo test -p bitaxe-parity --all-features mining_allow</automated>
    <automated>bazel test //tools/parity:tests --test_filter=mining_allow</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "is_expected_phase21_command|phase21-live-mining-evidence\\.sh|--readiness-audit|--enablement-summary|detector_port|board_info_command|redaction_reviewer|phase-21-live-mining-and-soak-evidence" tools/parity/src/mining_allow.rs` returns matches.
    - `cargo test -p bitaxe-parity --all-features mining_allow` exits 0.
    - `bazel test //tools/parity:tests --test_filter=mining_allow` exits 0.
    - `rg -n "raw-bm1366|voltage-control|fan-control|erase-flash" tools/parity/src/mining_allow.rs` still returns prohibited-token coverage.
  </acceptance_criteria>
  <done>Phase 21 wrapper and redaction command shapes are accepted only through the typed mining-allow validator, while unsafe command tokens and missing live-pool requirements remain rejected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Phase 21 live mining evidence wrapper and Bazel test target</name>
  <files>scripts/phase21-live-mining-evidence.sh, scripts/phase21-live-mining-evidence-test.sh, scripts/BUILD.bazel</files>
  <read_first>
    - scripts/phase21-live-mining-evidence.sh
    - scripts/phase21-live-mining-evidence-test.sh
    - scripts/BUILD.bazel
    - scripts/phase15-controlled-mining.sh
    - scripts/phase15-controlled-mining-test.sh
    - scripts/phase17-websocket-capture.mjs
    - .planning/phases/21-live-mining-and-soak-evidence/21-CONTEXT.md
    - .planning/phases/21-live-mining-and-soak-evidence/21-VALIDATION.md
    - standards/core/code-shape.md
    - standards/core/testing.md
  </read_first>
  <behavior>
    - Test 1: Missing manifest records `controlled_mining_status: pending - missing manifest`, `hardware_command_status=not-run`, and does not run curl or WebSocket helpers.
    - Test 2: Missing chip-detect, work-result, readiness audit, or enablement summary records `controlled_mining_status: pending - prerequisite not passed:` and does not run live probes.
    - Test 3: Missing `DEVICE_URL` or missing `BITAXE_POOL_URL`, `BITAXE_POOL_USER`, or `BITAXE_POOL_PASSWORD` records `live_mining_smoke_status: blocked`, `blocker: missing_live_prerequisites`, `controlled_no_share_condition=not-applicable`, `network_scan: disabled - DEVICE_URL must be explicit`, and redacts all pool env names without running curl, WebSocket, flash, or mining wrapper effects.
    - Test 4: `bounded-soak --duration-seconds 300` records `duration_seconds=300`, `abort_condition=watchdog_unresponsive`, `watchdog_status=observed-or-pending`, and `safe_stop_status=confirmed-or-pending`.
    - Test 5: A provided explicit `DEVICE_URL` uses `curl` only for `/api/system/info` and `node scripts/phase17-websocket-capture.mjs` only for `/api/ws/live` with `--duration-ms 10000 --max-frames 5`.
  </behavior>
  <action>Create `scripts/phase21-live-mining-evidence.sh` as a repo-owned Bash wrapper with `#!/usr/bin/env bash` and `set -euo pipefail`. The wrapper must accept exactly these options: `--manifest`, `--surface mining-smoke|bounded-soak`, `--out-dir`, `--chip-detect-summary`, `--work-result-summary`, `--readiness-audit`, `--enablement-summary`, optional `--device-url`, optional `--duration-seconds`, optional `--curl-bin`, optional `--websocket-helper`. Default `--duration-seconds` to `300` for `bounded-soak`; reject non-numeric values and values outside `60..600`. The wrapper must fail closed with machine-readable blocked artifacts when `--enablement-summary` is absent, when it lacks `controlled_live_mining_package_status: ready`, when it contains `controlled_live_mining_package_status: blocked`, or when `DEVICE_URL`, `BITAXE_POOL_URL`, `BITAXE_POOL_USER`, or `BITAXE_POOL_PASSWORD` are absent for a live smoke or soak command. Missing live prerequisites must be recorded as `blocker: missing_live_prerequisites`, never as `controlled-no-share`. Controlled-no-share may be recorded only after an actual controlled run or harness produces artifacts proving pool lifecycle, subscribe/authorize, notify/job flow, BM1366 work dispatch, result path, bounded duration or window, watchdog observations, safe-stop, and redaction. The wrapper must call `bazel run //tools/parity:report -- mining-allow --manifest "$manifest" --surface "$surface" --allowed-command "$allowed_command"` before any live probe. It must write only redacted artifacts under `out_dir`; use `sed` redaction for `https?://`, `wss?://`, IP addresses, MAC addresses, `BITAXE_POOL_URL=`, `BITAXE_POOL_USER=`, `BITAXE_POOL_PASSWORD=`, `DEVICE_URL=`, JSON keys `ssid`, `wifiPass`, `wifiPassword`, `stratumUser`, `stratumPassword`, `stratumCert`, `poolUrl`, `fallbackPoolUrl`, `hostname`, `ip`, `ipAddress`, `gateway`, `netmask`, `dns`, `apiToken`, `apiKey`, `token`, `password`, `user`, and `worker`. Record these exact fields in the main log: `phase21_live_mining_evidence`, `network_scan: disabled - DEVICE_URL must be explicit`, `surface: <surface>`, `mining_allow_status`, `enablement_status`, `controlled_mining_status`, `pool_lifecycle_status`, `subscribe_status`, `authorize_status`, `notify_job_status`, `bm1366_work_dispatch_status`, `result_receive_status`, `share_outcome`, `hashrate_inputs_status`, `api_telemetry_status`, `websocket_frame_status`, `watchdog_status`, `safe_stop_status`, `post_action_safe_state_marker=safe_state: mining=disabled`, `post_action_safe_state_marker=hardware_control=disabled`, `post_action_safe_state_marker=work_submission=disabled`, and `conclusion`. Add `scripts/phase21-live-mining-evidence-test.sh` with focused fake-command tests and register `sh_binary(name = "phase21_live_mining_evidence")` plus `sh_test(name = "phase21_live_mining_evidence_test")` in `scripts/BUILD.bazel`. This implements D-02, D-03, D-04, D-08, D-09, D-10, D-13, D-14, D-15, D-16, D-17, and D-18.</action>
  <verify>
    <automated>bash -n scripts/phase21-live-mining-evidence.sh scripts/phase21-live-mining-evidence-test.sh</automated>
    <automated>bazel test //scripts:phase21_live_mining_evidence_test</automated>
  </verify>
  <acceptance_criteria>
    - `test -x scripts/phase21-live-mining-evidence.sh` exits 0.
    - `rg -n "phase21_live_mining_evidence|--enablement-summary|controlled_live_mining_package_status: ready|network_scan: disabled - DEVICE_URL must be explicit|blocker: missing_live_prerequisites|controlled_no_share_condition=not-applicable|duration_seconds=300|watchdog_status=observed-or-pending|post_action_safe_state_marker=safe_state: mining=disabled" scripts/phase21-live-mining-evidence.sh scripts/phase21-live-mining-evidence-test.sh` returns matches.
    - `rg -n "phase21_live_mining_evidence|phase21_live_mining_evidence_test" scripts/BUILD.bazel` returns matches.
    - `bazel test //scripts:phase21_live_mining_evidence_test` exits 0.
    - `rg -n "erase-flash|rollback|interrupted-update|raw-bm1366|voltage-control|fan-control|network_scan: enabled" scripts/phase21-live-mining-evidence.sh` returns no matches.
  </acceptance_criteria>
  <done>Phase 21 has a tested wrapper that can produce safe blocked, actual controlled no-share, live-pool smoke, or bounded-soak artifacts without leaking secrets, bypassing mining-allow validation, or treating missing live prerequisites as evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Create evidence contract, redaction scaffold, readiness audit, and validation updates</name>
  <files>docs/parity/evidence/phase-21-live-mining-and-soak-evidence/evidence-contract.md, docs/parity/evidence/phase-21-live-mining-and-soak-evidence/redaction-review.md, docs/parity/evidence/phase-21-live-mining-and-soak-evidence/readiness-audit.md, .planning/phases/21-live-mining-and-soak-evidence/21-VALIDATION.md</files>
  <read_first>
    - docs/parity/evidence/phase-21-live-mining-and-soak-evidence/evidence-contract.md
    - docs/parity/evidence/phase-21-live-mining-and-soak-evidence/redaction-review.md
    - docs/parity/evidence/phase-21-live-mining-and-soak-evidence/readiness-audit.md
    - .planning/phases/21-live-mining-and-soak-evidence/21-VALIDATION.md
    - .planning/phases/21-live-mining-and-soak-evidence/21-CONTEXT.md
    - firmware/bitaxe/src/main.rs
    - firmware/bitaxe/src/asic_adapter/status.rs
    - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion.md
    - docs/parity/evidence/phase-20-active-safety-hardware-telemetry-evidence/summary.md
    - AGENTS.md
  </read_first>
  <action>Create `docs/parity/evidence/phase-21-live-mining-and-soak-evidence/evidence-contract.md` with pack names exactly `preflight`, `live-mining-enablement`, `bm1366-init-work-result`, `live-mining-smoke`, `bounded-soak`, `live-api-websocket-telemetry`, `redaction-review`, and `final-summary`. Include the ladder from D-05, tier-stop rule from D-06, prohibited action list from D-08, accepted/rejected-share exact-outcome rule from D-11, controlled fake-pool label rule from D-12, bounded soak range `60..600` from D-13, stop conditions from D-16, telemetry correlation requirements from D-17 and D-18, exact checklist promotion rules from D-20 through D-22, and a rule that `firmware_live_mining_status: blocked_by_default` requires a later `live-mining-enablement` pack with both `controlled_live_mining_package_status: ready` and `controlled_runtime_harness_status: ready` before live smoke or bounded soak may run. Define `live-mining-enablement` as the pack that proves the bounded controlled runtime/harness exists, including settings-to-runtime, Stratum subscribe/authorize/notify, guarded work queue, typed BM1366 dispatch, result parsing, share submission outcome, runtime state, API/WebSocket telemetry, watchdog-yield, and safe-stop required markers. Create `redaction-review.md` with `redaction_status: pending`, `raw_artifacts_committed: no`, artifact rows for every pack, and the exact scan command `rg -n -i "ssid|wifi|password|pool|worker|token|device_url|nvs|stratum|https?://|([0-9]{1,3}\\.){3}[0-9]{1,3}|([[:xdigit:]]{2}:){5}[[:xdigit:]]{2}|secret|credential" docs/parity/evidence/phase-21-live-mining-and-soak-evidence`. Create `readiness-audit.md` by running and recording the static audit command `rg -n "mining_loop_status|work_submission|hardware_evidence_ack|BITAXE_ASIC_DIAGNOSTIC" firmware/bitaxe crates/bitaxe-stratum scripts tools`. The audit must include exact lines `firmware_live_mining_status: blocked_by_default`, `observed_marker: mining_loop_status=blocked reason=hardware_evidence_ack_missing`, `controlled_enablement_required: true`, `enablement_plan: 21-02-PLAN.md`, and `network_scan: disabled`. Update `21-VALIDATION.md` so `21-W0-01`, `21-W0-02`, `21-W0-03`, and `21-W0-04` list Plan `21-01`, Wave `0`, concrete created artifacts, and `Status` as `green` only after this task's commands pass. This implements D-01 through D-23 at scaffold level and keeps all deferred ideas absent.</action>
  <verify>
    <automated>rg -n "preflight, live-mining-enablement, bm1366-init-work-result, live-mining-smoke, bounded-soak, live-api-websocket-telemetry, redaction-review, final-summary|firmware_live_mining_status: blocked_by_default|controlled_enablement_required: true|enablement_plan: 21-02-PLAN.md|redaction_status: pending|21-W0-01|21-W0-04" docs/parity/evidence/phase-21-live-mining-and-soak-evidence/evidence-contract.md docs/parity/evidence/phase-21-live-mining-and-soak-evidence/redaction-review.md docs/parity/evidence/phase-21-live-mining-and-soak-evidence/readiness-audit.md .planning/phases/21-live-mining-and-soak-evidence/21-VALIDATION.md</automated>
    <automated>sh -c 'if rg -n "^---$" docs/parity/evidence/phase-21-live-mining-and-soak-evidence/evidence-contract.md docs/parity/evidence/phase-21-live-mining-and-soak-evidence/redaction-review.md docs/parity/evidence/phase-21-live-mining-and-soak-evidence/readiness-audit.md; then exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    - `docs/parity/evidence/phase-21-live-mining-and-soak-evidence/evidence-contract.md` contains `Phase 21 evidence ladder` and the exact pack-name string `preflight, live-mining-enablement, bm1366-init-work-result, live-mining-smoke, bounded-soak, live-api-websocket-telemetry, redaction-review, final-summary`.
    - `docs/parity/evidence/phase-21-live-mining-and-soak-evidence/redaction-review.md` contains `redaction_status: pending`, `raw_artifacts_committed: no`, and the exact redaction scan pattern from the action.
    - `docs/parity/evidence/phase-21-live-mining-and-soak-evidence/readiness-audit.md` contains `firmware_live_mining_status: blocked_by_default`, `observed_marker: mining_loop_status=blocked reason=hardware_evidence_ack_missing`, `controlled_enablement_required: true`, `enablement_plan: 21-02-PLAN.md`, and `network_scan: disabled`.
    - `.planning/phases/21-live-mining-and-soak-evidence/21-VALIDATION.md` contains `21-W0-01`, `21-W0-02`, `21-W0-03`, `21-W0-04`, and `Plan 21-01`.
    - `rg -n "^---$" docs/parity/evidence/phase-21-live-mining-and-soak-evidence/evidence-contract.md docs/parity/evidence/phase-21-live-mining-and-soak-evidence/redaction-review.md docs/parity/evidence/phase-21-live-mining-and-soak-evidence/readiness-audit.md` returns no matches.
  </acceptance_criteria>
  <done>Wave 0 validation artifacts exist, the Phase 21 evidence tree has redaction controls before citation, and downstream plans have a concrete readiness audit proving the default firmware path is fail-closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| operator env to wrapper | `DEVICE_URL` and pool env var presence crosses into local scripts but values must not be committed or printed raw. |
| wrapper to parity validator | Evidence command text crosses into typed `tools/parity` validation before execution. |
| firmware source to readiness audit | Static source evidence is used to decide whether live smoke may run or must record blocked evidence. |
| evidence tree to git | Generated logs and Markdown cross from local runtime into committed artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-21-01-01 | Information Disclosure | `scripts/phase21-live-mining-evidence.sh` and redaction artifacts | mitigate | Redact URL, IP, MAC, `BITAXE_POOL_URL`, `BITAXE_POOL_USER`, `BITAXE_POOL_PASSWORD`, `DEVICE_URL`, Wi-Fi, token, worker, NVS, and password fields before writing committed logs. |
| T-21-01-02 | Tampering / Denial of Service | `tools/parity/src/mining_allow.rs` command validation | mitigate | Require board `205`, `just detect-ultra205`, board-info `passed`, package identity, abort conditions, safe-state markers, and reject raw BM1366, erase, rollback, voltage, fan, and literal `stratum` command tokens. |
| T-21-01-03 | Spoofing / Information Disclosure | explicit target handling | mitigate | Wrapper records `network_scan: disabled - DEVICE_URL must be explicit`; no mDNS, ARP, router, serial inference, or redacted-log target inference is permitted. |
| T-21-01-04 | Repudiation / Tampering | readiness audit and evidence scaffold | mitigate | Record exact static audit command, source files inspected, observed blocked marker, and controlled enablement requirement before live evidence plans run. |
| T-21-01-05 | Denial of Service | bounded soak wrapper path | mitigate | Enforce `duration_seconds` in `60..600`, required abort condition `watchdog_unresponsive`, and final safe-state markers before an allow manifest can pass. |
| T-21-01-06 | Tampering / Denial of Service | live-mining enablement prerequisite | mitigate | Require `--enablement-summary` and `controlled_live_mining_package_status: ready` before wrapper live smoke or bounded soak execution; blocked enablement produces blocked artifacts instead of live effects. |
</threat_model>

<verification>
Run the task-level automated checks plus:

```bash
bazel test //tools/parity:tests --test_filter=mining_allow //scripts:phase21_live_mining_evidence_test
just parity
just verify-reference
git diff -- reference/esp-miner --exit-code
```
</verification>

<success_criteria>
- `tools/parity` accepts Phase 21 wrapper and redaction command shapes only under the existing board, package, detector, abort, safe-state, and live-pool constraints.
- `scripts/phase21-live-mining-evidence.sh` and its Bazel test exist and prove blocked, controlled no-share, bounded soak, explicit target, and redaction behavior.
- Evidence contract, redaction review, readiness audit, and validation rows exist before hardware-capable Phase 21 plans run.
- No task introduces network scanning, raw BM1366 commands, raw pool commands, active voltage/fan/fault controls, erase, rollback, interrupted-update, or unbounded stress.
</success_criteria>

<output>
After completion, create `.planning/phases/21-live-mining-and-soak-evidence/21-01-SUMMARY.md`.
</output>
