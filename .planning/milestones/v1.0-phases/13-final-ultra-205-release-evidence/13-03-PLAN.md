---
phase: 13-final-ultra-205-release-evidence
plan: "03"
type: execute
wave: 3
depends_on:
  - "13-02"
files_modified:
  - scripts/phase13-http-static-smoke.sh
  - scripts/phase13-http-static-smoke-test.sh
  - scripts/BUILD.bazel
  - docs/parity/evidence/phase-13-final-ultra-205-release-evidence/http-static-recovery.md
  - docs/parity/evidence/phase-13-final-ultra-205-release-evidence/http-static-recovery/http-static-smoke.log
  - docs/parity/evidence/phase-13-final-ultra-205-release-evidence/redaction-review.md
autonomous: true
requirements:
  - API-09
  - REL-01
  - REL-03
  - EVD-05
must_haves:
  truths:
    - "A reachable `DEVICE_URL` is required before live HTTP/static/recovery evidence runs."
    - "Live HTTP evidence covers `/`, `/assets/app.css.gz`, missing static redirect behavior, `/recovery`, API coexistence, WebSocket route coexistence, and OTAWWW gap response."
    - "When `DEVICE_URL` is missing, ambiguous, unreachable, or wrong, the phase records a blocker and does not infer live HTTP success from serial route logs."
  artifacts:
    - path: "scripts/phase13-http-static-smoke.sh"
      provides: "Repo-owned HTTP/static/recovery evidence helper"
      contains: "DEVICE_URL status"
    - path: "scripts/phase13-http-static-smoke-test.sh"
      provides: "Shell tests for helper blocker and probe behavior"
      contains: "phase13_http_static_smoke_test"
    - path: "docs/parity/evidence/phase-13-final-ultra-205-release-evidence/http-static-recovery.md"
      provides: "Live HTTP/static/recovery evidence summary"
      contains: "http_static_status"
  key_links:
    - from: "scripts/phase13-http-static-smoke.sh"
      to: "DEVICE_URL"
      via: "explicit env var or `--device-url` argument only"
      pattern: "DEVICE_URL"
    - from: "crates/bitaxe-api/src/route_shell.rs"
      to: "HTTP probe list"
      via: "Phase 7 route manifest"
      pattern: "/api/system/OTAWWW|/recovery|/api/ws/live"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13-2026-06-30T14-53-46
generated_at: 2026-06-30T15:21:33Z
---

<objective>
Add and run a repo-owned HTTP/static/recovery smoke helper for the just-flashed Ultra 205.

Purpose: Live HTTP evidence must be repeatable and must not depend on ad hoc curl transcripts or serial route registration.
Output: Shell helper, Bazel shell test, HTTP evidence log, summary, and redaction review update.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-final-ultra-205-release-evidence/13-CONTEXT.md
@.planning/phases/13-final-ultra-205-release-evidence/13-UI-SPEC.md
@docs/parity/evidence/phase-13-final-ultra-205-release-evidence/serial-boot.md
@crates/bitaxe-api/src/route_shell.rs
@crates/bitaxe-api/src/static_plan.rs
@crates/bitaxe-api/src/update_plan.rs
@firmware/bitaxe/src/static_files.rs
@firmware/bitaxe/static/www/index.html
@firmware/bitaxe/static/recovery_page.html
@scripts/BUILD.bazel

<interfaces>
From `crates/bitaxe-api/src/static_plan.rs`:
```rust
pub const STATIC_CACHE_CONTROL: &str = "max-age=2592000";
pub const STATIC_REDIRECT_BODY: &str = "Redirect to the captive portal";
```

From `crates/bitaxe-api/src/update_plan.rs`:
```rust
UpdateRouteKind::AxeOsStaticOtaWww => UpdateRequestDecision::OtaWwwGap(otawww_gap())
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the HTTP/static/recovery smoke helper</name>
  <files>scripts/phase13-http-static-smoke.sh, scripts/phase13-http-static-smoke-test.sh, scripts/BUILD.bazel</files>
  <read_first>
    - `scripts/BUILD.bazel`
    - `scripts/detect-ultra205-test.sh`
    - `crates/bitaxe-api/src/route_shell.rs`
    - `crates/bitaxe-api/src/static_plan.rs`
    - `crates/bitaxe-api/src/update_plan.rs`
    - `.planning/phases/13-final-ultra-205-release-evidence/13-UI-SPEC.md`
  </read_first>
  <action>Create `scripts/phase13-http-static-smoke.sh` as a Bash script with `#!/usr/bin/env bash` and `set -euo pipefail`. It must accept `--device-url <url>` or `DEVICE_URL`, `--manifest bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json`, and `--out-dir docs/parity/evidence/phase-13-final-ultra-205-release-evidence/http-static-recovery`. If no URL is provided, write `http-static-smoke.log` with `DEVICE_URL status: blocked - missing DEVICE_URL` and exit 0 per D-06. If a URL is provided, do not scan the network; use only that URL. Use `curl --silent --show-error --max-time 10 --dump-header <file> --output <file> --write-out "%{http_code}"` to probe: `GET /`, `GET /assets/app.css.gz`, `GET /phase13-missing-static`, `GET /recovery`, `GET /api/system/info`, `GET /api/phase13-unknown`, bounded `GET /api/ws`, bounded `GET /api/ws/live`, and `POST /api/system/OTAWWW` with an empty request body. Record method, path, expected status/body marker, actual status, selected headers (`Content-Type`, `Location`, `Cache-Control`, `Content-Encoding`), sanitized body snippet, and conclusion per D-08 and D-11. The helper may create log/header/body artifacts because that reduces manual transcription risk per D-19; it must not create broader machine-readable evidence that duplicates the final ledger. Keep public copy exact per `13-UI-SPEC.md`, including `AxeOS unavailable`, `Open recovery`, `Release metadata`, `AxeOS Recovery`, `Response:`, missing static body `Redirect to the captive portal`, unknown API JSON `{"error":"unknown route"}`, and OTAWWW body `Wrong API input`. Create `scripts/phase13-http-static-smoke-test.sh` with fake `curl` fixtures proving missing URL writes a blocker and a successful fake run records all required paths. Register `phase13_http_static_smoke` as `sh_binary` and `phase13_http_static_smoke_test` as `sh_test` in `scripts/BUILD.bazel`.</action>
  <verify>
    <automated>bash -n scripts/phase13-http-static-smoke.sh && bash -n scripts/phase13-http-static-smoke-test.sh && bazel test //scripts:phase13_http_static_smoke_test</automated>
  </verify>
  <acceptance_criteria>
    - Helper has no network scan path and accepts only explicit `DEVICE_URL` or `--device-url`.
    - Missing URL produces `DEVICE_URL status: blocked - missing DEVICE_URL` and exits without curl.
    - Test proves the helper records `/`, `/assets/app.css.gz`, missing static redirect, `/recovery`, `/api/system/info`, unknown `/api/*`, `/api/ws`, `/api/ws/live`, and `/api/system/OTAWWW`.
    - `scripts/BUILD.bazel` exposes the helper and test through Bazel.
  </acceptance_criteria>
  <done>HTTP/static/recovery evidence can be captured through a repo-owned helper with deterministic shell tests.</done>
</task>

<task type="auto">
  <name>Task 2: Run live HTTP/static/recovery smoke or record DEVICE_URL blocker</name>
  <files>docs/parity/evidence/phase-13-final-ultra-205-release-evidence/http-static-recovery.md, docs/parity/evidence/phase-13-final-ultra-205-release-evidence/http-static-recovery/http-static-smoke.log, docs/parity/evidence/phase-13-final-ultra-205-release-evidence/redaction-review.md</files>
  <read_first>
    - `docs/parity/evidence/phase-13-final-ultra-205-release-evidence/serial-boot.md`
    - `docs/parity/evidence/phase-13-final-ultra-205-release-evidence/package-release-gate.md`
    - `scripts/phase13-http-static-smoke.sh`
    - `docs/parity/checklist.md`
  </read_first>
  <action>Run `scripts/phase13-http-static-smoke.sh --device-url "${DEVICE_URL:-}" --manifest bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json --out-dir docs/parity/evidence/phase-13-final-ultra-205-release-evidence/http-static-recovery`. If `DEVICE_URL` is unset, unreachable, ambiguous, or returns identity inconsistent with the just-flashed package, keep `http_static_status: blocked` and cite the exact reason per D-06. If it passes, write `http-static-recovery.md` with board `205`, selected port from Plan 02, source commit, reference commit, package manifest, sanitized `DEVICE_URL` source, each probed route, status/body/header results, route conclusion, `/recovery` page-load evidence, OTAWWW upload-attempt/no-op gap behavior, response body, and explicit OTAWWW REL-03 gap owner per D-11 and D-12. Update `redaction-review.md` to include `http-static-smoke.log`, route headers, and route body snippets per D-20.</action>
  <verify>
    <automated>test -f docs/parity/evidence/phase-13-final-ultra-205-release-evidence/http-static-recovery/http-static-smoke.log && test -f docs/parity/evidence/phase-13-final-ultra-205-release-evidence/http-static-recovery.md && rg -n "http_static_status: (passed|blocked)|DEVICE_URL status|/assets/app\\.css\\.gz|missing static redirect|/recovery|/api/system/OTAWWW|Wrong API input|redaction" docs/parity/evidence/phase-13-final-ultra-205-release-evidence/http-static-recovery.md docs/parity/evidence/phase-13-final-ultra-205-release-evidence/http-static-recovery/http-static-smoke.log</automated>
  </verify>
  <acceptance_criteria>
    - Live success records every route required by D-08 with status/header/body evidence.
    - Missing or invalid `DEVICE_URL` records `DEVICE_URL status: blocked` and does not fail into network scans or route-registration overclaims.
    - OTAWWW is recorded as the REL-03 gap with public response `Wrong API input`, not as static-update success.
    - Redaction review scope includes all HTTP logs, headers, body snippets, and sanitized device URL handling.
  </acceptance_criteria>
  <done>Live HTTP/static/recovery evidence is captured when reachable, or a precise `DEVICE_URL` blocker is recorded without promoting release-sensitive claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| operator-provided URL to local helper | `DEVICE_URL` controls the target of HTTP probes and can point at the wrong host. |
| device HTTP responses to committed evidence | HTTP headers and bodies may contain private network details or unexpected data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-13-03-01 | Spoofing | `DEVICE_URL` | mitigate | Require explicit URL input, no network scanning, record sanitized target and identity consistency before passing evidence per D-06. |
| T-13-03-02 | Tampering | route response evidence | mitigate | Record status, selected headers, body markers, and snippets for each route rather than hand-written conclusions. |
| T-13-03-03 | Information Disclosure | HTTP logs and snippets | mitigate | Sanitize URL host details where private and include all HTTP artifacts in redaction review before commit. |
| T-13-03-04 | Elevation of Privilege | OTAWWW route | mitigate | Probe only the documented fail-closed response and keep `/api/system/OTAWWW` as the REL-03 gap per D-11. |
</threat_model>

<verification>
Run after tasks:

- `bash -n scripts/phase13-http-static-smoke.sh`
- `bazel test //scripts:phase13_http_static_smoke_test`
- `just parity`
- `git diff -- reference/esp-miner --exit-code`
- Full Rust pre-commit sequence before commit because this is a Rust repository with build graph changes.
</verification>

<success_criteria>
The repo has a tested HTTP/static/recovery evidence helper, and Phase 13 records either live route evidence from the just-flashed device or a precise `DEVICE_URL` blocker without overclaiming.
</success_criteria>

<output>
After completion, create `.planning/phases/13-final-ultra-205-release-evidence/13-03-SUMMARY.md`.
</output>
