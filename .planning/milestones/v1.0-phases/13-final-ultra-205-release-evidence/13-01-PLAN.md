---
phase: 13-final-ultra-205-release-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/parity/evidence/phase-13-final-ultra-205-release-evidence/README.md
  - docs/parity/evidence/phase-13-final-ultra-205-release-evidence/package-release-gate.md
  - docs/parity/evidence/phase-13-final-ultra-205-release-evidence/redaction-review.md
autonomous: true
requirements:
  - REL-04
  - REL-01
  - EVD-05
must_haves:
  truths:
    - "Release-candidate package identity is recorded before any Phase 13 hardware or network evidence is trusted."
    - "Manifest-backed release-gate validation passes or the phase records a package blocker."
    - "Evidence directory and redaction contract exist before generated artifacts are cited."
  artifacts:
    - path: "docs/parity/evidence/phase-13-final-ultra-205-release-evidence/README.md"
      provides: "Phase 13 evidence contract and blocker policy"
      contains: "Evidence Contract"
    - path: "docs/parity/evidence/phase-13-final-ultra-205-release-evidence/package-release-gate.md"
      provides: "Package manifest and release-gate evidence"
      contains: "release_gate"
    - path: "docs/parity/evidence/phase-13-final-ultra-205-release-evidence/redaction-review.md"
      provides: "Redaction review checklist for generated evidence"
      contains: "Secret Redaction Review"
  key_links:
    - from: "just package"
      to: "bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json"
      via: "canonical package command"
      pattern: "bitaxe-ultra205-package\\.json"
    - from: "tools/parity release-gate"
      to: "package-release-gate.md"
      via: "manifest-backed release gate command output"
      pattern: "release_gate: passed"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13-2026-06-30T14-53-46
generated_at: 2026-06-30T15:21:33Z
---

<objective>
Establish the Phase 13 release-candidate package identity and evidence scaffold.

Purpose: Phase 13 must prove that later hardware, HTTP, OTA, and recovery evidence belongs to the same source commit and package manifest cited by release documents.
Output: Phase 13 evidence directory contract, redaction checklist, and package/release-gate baseline.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-final-ultra-205-release-evidence/13-CONTEXT.md
@.planning/phases/13-final-ultra-205-release-evidence/13-RESEARCH.md
@.planning/phases/13-final-ultra-205-release-evidence/13-VALIDATION.md
@docs/release/ultra-205.md
@docs/release/license-inventory.md
@docs/release/provenance-manifest.md
@tools/xtask/src/package_manifest.rs
@tools/parity/src/release_gate.rs

<interfaces>
From `tools/xtask/src/package_manifest.rs`:
```rust
pub(crate) struct PackageManifestV2 {
    pub(crate) schema_version: u32,
    pub(crate) release_name: String,
    pub(crate) source_commit: String,
    pub(crate) reference_commit: String,
    pub(crate) default_flash_image: String,
    pub(crate) image_metadata: ImageMetadata,
    pub(crate) tool_versions: ToolVersions,
    pub(crate) install_notes: ReleaseNotes,
    pub(crate) license_inventory: String,
    pub(crate) provenance_manifest: String,
    pub(crate) otadata_source: String,
    pub(crate) artifacts: Vec<ReleaseArtifact>,
}
```

From `tools/parity/src/release_gate.rs`:
```rust
pub(crate) fn validate_release_gate(documents: &ReleaseGateDocuments) -> ReleaseGateReport;
pub(crate) fn render_release_gate_report(report: &ReleaseGateReport) -> String;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 13 evidence scaffold and redaction contract</name>
  <files>docs/parity/evidence/phase-13-final-ultra-205-release-evidence/README.md, docs/parity/evidence/phase-13-final-ultra-205-release-evidence/package-release-gate.md, docs/parity/evidence/phase-13-final-ultra-205-release-evidence/redaction-review.md</files>
  <read_first>
    - `.planning/phases/13-final-ultra-205-release-evidence/13-CONTEXT.md`
    - `.planning/phases/13-final-ultra-205-release-evidence/13-RESEARCH.md`
    - `docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/redaction-review.md`
    - `docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence.md`
    - `docs/release/ultra-205.md`
  </read_first>
  <action>Create the evidence directory `docs/parity/evidence/phase-13-final-ultra-205-release-evidence/`. Create `README.md` with these exact section headings: `# Phase 13 Final Ultra 205 Release Evidence`, `## Evidence Contract`, `## Required Inputs`, `## Blocker Handling`, `## Generated Artifacts`, and `## Non-Claims`. State that `DEVICE_URL status: blocked` is a valid recorded outcome when the URL is missing or wrong per D-06, and that no checklist row may be promoted from blocker evidence per D-17. Create `redaction-review.md` with a checklist covering Wi-Fi credentials, pool URLs, pool usernames, pool passwords, API tokens, private endpoints, NVS secret values, private `DEVICE_URL` values, raw terminal secrets, and local private IP disclosure beyond necessary bench evidence per D-20. Create `package-release-gate.md` with headings for command log, manifest fields, required artifacts, release-gate result, blocker status, and conclusion; leave fields marked `not run` only until Task 2 fills them.</action>
  <verify>
    <automated>test -f docs/parity/evidence/phase-13-final-ultra-205-release-evidence/README.md && test -f docs/parity/evidence/phase-13-final-ultra-205-release-evidence/package-release-gate.md && test -f docs/parity/evidence/phase-13-final-ultra-205-release-evidence/redaction-review.md && rg -n "Evidence Contract|Blocker Handling|Secret Redaction Review|private DEVICE_URL|package manifest" docs/parity/evidence/phase-13-final-ultra-205-release-evidence</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` names `DEVICE_URL status: blocked` and says blocker evidence must not promote checklist rows.
    - `redaction-review.md` has checklist lines for every secret-bearing surface named in D-20.
    - `package-release-gate.md` exists and is ready for Task 2 command evidence.
  </acceptance_criteria>
  <done>Phase 13 has a committed evidence scaffold and redaction checklist before any generated package, hardware, HTTP, OTA, or recovery artifacts are cited.</done>
</task>

<task type="auto">
  <name>Task 2: Record package manifest identity and release-gate result</name>
  <files>docs/parity/evidence/phase-13-final-ultra-205-release-evidence/package-release-gate.md</files>
  <read_first>
    - `Justfile`
    - `scripts/package-firmware.sh`
    - `tools/xtask/src/package_manifest.rs`
    - `tools/parity/src/release_gate.rs`
    - `docs/release/license-inventory.md`
    - `docs/release/provenance-manifest.md`
  </read_first>
  <action>Run `just package` from the repository root per D-01. Then run `bazel run //tools/parity:report -- release-gate --manifest bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json` per D-02. Update `package-release-gate.md` with the exact commands, pass/fail result, package manifest path, manifest `source_commit`, `reference_commit`, `image_metadata.esp_idf_version`, `image_metadata.rust_target`, `default_flash_image`, `license_inventory`, `provenance_manifest`, and `install_notes.path`. Record the required artifacts from D-03 by filename and kind: `bitaxe-ultra205.elf`, `esp-miner.bin`, `www.bin`, `bitaxe-ultra205-factory.bin`, `otadata-initial.bin`, `docs/release/license-inventory.md`, and `docs/release/provenance-manifest.md`. If a release-critical artifact is missing, write `package_status: blocked - missing <artifact>` and stop before any hardware or network command.</action>
  <verify>
    <automated>just package && bazel run //tools/parity:report -- release-gate --manifest bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json && rg -n "release_gate: passed|source_commit|reference_commit|esp_idf_version|rust_target|bitaxe-ultra205\\.elf|esp-miner\\.bin|www\\.bin|bitaxe-ultra205-factory\\.bin|otadata-initial\\.bin|license-inventory\\.md|provenance-manifest\\.md" docs/parity/evidence/phase-13-final-ultra-205-release-evidence/package-release-gate.md</automated>
  </verify>
  <acceptance_criteria>
    - `package-release-gate.md` records the exact `just package` and `release-gate` commands.
    - The manifest identity includes source commit, reference commit, ESP-IDF version, Rust target, install notes, license inventory, and provenance manifest.
    - Every required D-03 artifact is cited from the manifest, or the task records a package blocker and no downstream hardware/network evidence is trusted.
  </acceptance_criteria>
  <done>Release-candidate package identity is established and release-gate validation passes before Phase 13 hardware or network evidence can proceed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| package manifest to release evidence | Build artifacts and manifest metadata become evidence cited by release docs and checklist rows. |
| local repository to release gate | Local docs, manifest JSON, and generated package outputs are validated before hardware evidence is trusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-13-01-01 | Tampering | `bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json` | mitigate | Run manifest-backed release-gate and record required artifact names/checksums before downstream evidence per D-01 to D-03. |
| T-13-01-02 | Repudiation | `package-release-gate.md` | mitigate | Record exact commands, source commit, reference commit, and release-gate result so evidence identity is auditable. |
| T-13-01-03 | Information Disclosure | `redaction-review.md` and generated package evidence | mitigate | Require D-20 redaction checklist before generated logs, JSON, or Markdown evidence are committed. |
| T-13-01-04 | Elevation of Privilege | release checklist claims | mitigate | Do not update checklist rows in this plan; final promotion is gated by Plan 06 `just parity` and exact evidence classes. |
</threat_model>

<verification>
Run after tasks:

- `just package`
- `bazel run //tools/parity:report -- release-gate --manifest bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json`
- `just parity`
- `git diff -- reference/esp-miner --exit-code`

Before any commit in this Rust repository, run the repo pre-commit sequence: `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`.
</verification>

<success_criteria>
The Phase 13 evidence directory exists, package manifest identity is recorded, release-gate validation passes, and missing release-critical artifacts block later trusted evidence instead of being papered over.
</success_criteria>

<output>
After completion, create `.planning/phases/13-final-ultra-205-release-evidence/13-01-SUMMARY.md`.
</output>
