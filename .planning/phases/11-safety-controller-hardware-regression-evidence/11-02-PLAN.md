---
phase: 11
plan: 02
type: execute
status: planned
wave: 1
depends_on: []
files_modified:
  - tools/parity/src/main.rs
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-04, SAFE-05, SAFE-06, SAFE-08, EVD-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-06-29T20-23-34
generated_at: 2026-06-29T20:56:05.022Z
must_haves:
  truths:
    - "Parity tooling rejects active safety-control rows marked verified without hardware-regression evidence"
    - "Read-only or safe-unavailable hardware-smoke evidence remains allowed only for exact narrow observations"
    - "Phase 11 checklist updates cannot accidentally verify voltage writes, fan actuation, self-test hardware, runtime input, or overheat/fault paths from broad smoke evidence"
  artifacts:
    - path: "tools/parity/src/main.rs"
      provides: "Safety-critical and active-control verified-row validation"
      contains: "hardware-regression"
  key_links:
    - from: "tools/parity/src/main.rs"
      to: "docs/parity/checklist.md"
      via: "Checklist row id validation"
      pattern: "PWR-003"
---

<objective>
Refine the parity guard so Phase 11 cannot promote active safety-control parity from weak or overly broad evidence.

Purpose: The existing guard blocks safety-critical `verified` rows without hardware evidence; Phase 11 also needs active-control and failure-path rows to require `hardware-regression`, while allowing narrow read-only hardware smoke to remain useful.
Output: TDD-backed parity validation in `tools/parity/src/main.rs`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/11-safety-controller-hardware-regression-evidence/11-CONTEXT.md
@.planning/phases/11-safety-controller-hardware-regression-evidence/11-RESEARCH.md
@docs/parity/checklist.md
@tools/parity/src/main.rs
@.planning/phases/06-safety-controllers-and-self-test/06-10-SUMMARY.md
@.planning/phases/10-route-manifest-and-api-compare-unification/10-02-SUMMARY.md
</context>

<interfaces>
From `tools/parity/src/main.rs`:
```rust
#[derive(Debug, Eq, PartialEq, Serialize)]
struct ChecklistRow {
    id: String,
    surface: String,
    reference_breadcrumb: String,
    rust_owned_target: String,
    status: String,
    evidence: String,
    notes: String,
}

#[derive(Debug, Eq, PartialEq, Serialize)]
struct ValidationError {
    id: String,
    message: String,
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing parity tests for active safety-control overclaims</name>
  <requirements>SAFE-01, SAFE-02, SAFE-04, SAFE-05, SAFE-06, SAFE-08, EVD-05</requirements>
  <read_first>
    <file>tools/parity/src/main.rs</file>
    <file>docs/parity/checklist.md</file>
    <file>.planning/phases/11-safety-controller-hardware-regression-evidence/11-CONTEXT.md</file>
    <file>.planning/phases/11-safety-controller-hardware-regression-evidence/11-RESEARCH.md</file>
  </read_first>
  <files>tools/parity/src/main.rs</files>
  <scope>Parity validation tests only. Do not edit `docs/parity/checklist.md` in this plan.</scope>
  <behavior>
    - Test 1: a `PWR-003` row with `status: verified` and `evidence: hardware-smoke` is rejected because core voltage control is an active DS4432U/VCORE behavior.
    - Test 2: a `THR-002` row with `status: verified` and `evidence: hardware-smoke` is rejected because fan controller task parity includes active fan duty behavior.
    - Test 3: a `SELF-001` row with `status: verified` and `evidence: hardware-smoke` is rejected because self-test hardware submodes touch fan, voltage, ASIC work, or reset behavior.
    - Test 4: a `PWR-006` row with `status: verified` and `evidence: hardware-smoke` remains allowed when the notes describe read-only INA260 telemetry freshness and do not claim voltage writes.
  </behavior>
  <action>
    Add focused unit tests beside the existing safety-critical validation tests in `tools/parity/src/main.rs`. Use helper rows matching the local test style already present in the file. The expected validation message for active-control rows must contain `requires hardware-regression evidence`.

    Do not commit failing RED states. Run the tests once before implementation and record in the summary that the RED failure was observed, but only commit or leave final code after verification passes, per repo Rust commit rules.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-parity --all-features active_safety_control</automated>
    <automated>bazel test //tools/parity:tests --test_filter=active_safety_control</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`tools/parity/src/main.rs` contains a test name with `active_safety_control`.</criterion>
    <criterion>The new tests mention `PWR-003`, `THR-002`, `SELF-001`, and `PWR-006`.</criterion>
    <criterion>The failing condition expects the phrase `requires hardware-regression evidence`.</criterion>
  </acceptance_criteria>
  <done>Parity tests express the Phase 11 distinction between read-only hardware-smoke and active-control hardware-regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement active-control hardware-regression validation</name>
  <requirements>SAFE-01, SAFE-02, SAFE-04, SAFE-05, SAFE-06, SAFE-08, EVD-05</requirements>
  <read_first>
    <file>tools/parity/src/main.rs</file>
    <file>.planning/phases/11-safety-controller-hardware-regression-evidence/11-CONTEXT.md</file>
    <file>docs/parity/checklist.md</file>
  </read_first>
  <files>tools/parity/src/main.rs</files>
  <scope>Parity validation logic only. Do not change checklist rows in this plan.</scope>
  <behavior>
    - Verified active-control rows require the evidence token `hardware-regression`.
    - Verified read-only telemetry rows still satisfy the existing safety-critical guard when they carry `hardware-smoke`.
    - Non-verified rows are not blocked by the active-control rule.
  </behavior>
  <action>
    Implement a small pure helper in `tools/parity/src/main.rs` that classifies broad active-control or fault-path checklist row ids. Include this exact initial row set: `PWR-001`, `PWR-002`, `PWR-003`, `PWR-005`, `THR-001`, `THR-002`, `SELF-001`, and `UI-003`.

    Wire the helper into existing row validation so that when `row.status == "verified"` and the row id is in the active-control set, `row.evidence` must include `hardware-regression`. If it lacks that token, return a `ValidationError` whose message includes `requires hardware-regression evidence`. Preserve the existing safety-critical `hardware-smoke` or `hardware-regression` rule for other safety rows, including `PWR-006` read-only telemetry.

    Keep parsing simple and local: evidence tokens are the existing comma-separated or text field. Do not add a new manifest format or dependency.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-parity --all-features active_safety_control</automated>
    <automated>cargo test -p bitaxe-parity --all-features safety_critical</automated>
    <automated>bazel test //tools/parity:tests --test_filter='active_safety_control|safety_critical'</automated>
    <automated>just parity</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`tools/parity/src/main.rs` contains the row ids `PWR-001`, `PWR-002`, `PWR-003`, `PWR-005`, `THR-001`, `THR-002`, `SELF-001`, and `UI-003` in the active-control validation path.</criterion>
    <criterion>`cargo test -p bitaxe-parity --all-features active_safety_control` exits 0.</criterion>
    <criterion>`just parity` exits 0 against the current checklist.</criterion>
  </acceptance_criteria>
  <done>Parity validation prevents Phase 11 from verifying broad active safety rows without `hardware-regression` evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Checklist Markdown to parity validator | Human-edited evidence strings become machine-enforced release posture. |
| Evidence token to safety claim | A weak token can overstate the safety claim if validation is too broad. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-02-01 | Tampering | `validate_rows` in `tools/parity/src/main.rs` | mitigate | Verified active-control row ids require `hardware-regression`; broad `hardware-smoke` evidence no longer verifies voltage/fan/self-test/fault/runtime-input behavior. |
| T-11-02-02 | Repudiation | Checklist evidence tokens | mitigate | Validation errors name the row id and required evidence class so unsafe promotions are auditable. |
| T-11-02-03 | Information Disclosure | Parity tests | accept | Tests use synthetic row data only and do not read hardware logs or secrets. |
| T-11-02-04 | Denial of Service | Parity report strictness | mitigate | `PWR-006` read-only hardware-smoke remains allowed so the guard blocks active overclaims without blocking narrow telemetry evidence. |
</threat_model>

<verification>
Run targeted parity tests, then `just parity`. If Rust source is changed and a commit is made later, also run the repo Rust pre-commit sequence: `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`.
</verification>

<success_criteria>
Active safety-control rows cannot be marked verified with only `hardware-smoke`, read-only telemetry rows can still cite narrow board-named smoke evidence, and current checklist validation remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/11-safety-controller-hardware-regression-evidence/11-02-SUMMARY.md`.
</output>
