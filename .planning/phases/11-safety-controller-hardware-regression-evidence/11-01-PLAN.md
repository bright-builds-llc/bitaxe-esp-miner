---
phase: 11
plan: 01
type: execute
status: planned
wave: 1
depends_on: []
files_modified:
  - docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md
  - docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/README.md
  - docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/redaction-review.md
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-03, SAFE-04, SAFE-05, SAFE-06, SAFE-07, SAFE-08, SAFE-09, EVD-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-06-29T20-23-34
generated_at: 2026-06-29T20:56:05.022Z
must_haves:
  truths:
    - "Executor has a board-205-only Phase 11 runbook before live safety evidence work starts"
    - "Every safety surface has an exact claim type, allowed probe, pass criteria, failure criteria, evidence token, and promotion boundary"
    - "Actuation, stress, destructive, fault-injection, and self-test hardware paths stop unless bounded steps, abort conditions, and recovery are documented"
    - "Evidence artifacts require board, port, source commit, reference commit, command/probe, logs, conclusion, and secret-redaction review"
  artifacts:
    - path: "docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md"
      provides: "Phase 11 ledger, runbook, surface matrix, recovery protocol, and conclusion table"
      contains: "Phase 11 Safety Controller Hardware Regression Evidence"
    - path: "docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/README.md"
      provides: "Component evidence pack index and artifact contract"
      contains: "Component Evidence Packs"
    - path: "docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/redaction-review.md"
      provides: "Secret-redaction checklist for captured logs and JSON"
      contains: "Secret Redaction Review"
  key_links:
    - from: "docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md"
      to: "AGENTS.md"
      via: "Ultra 205 detector and evidence rules"
      pattern: "just detect-ultra205"
    - from: "docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md"
      to: "docs/parity/checklist.md"
      via: "Surface matrix checklist row ids"
      pattern: "PWR-003"
---

<objective>
Create the Phase 11 Wave 0 evidence ledger, runbook, recovery protocol, and component evidence-pack contract before any live Ultra 205 hardware evidence work.

Purpose: Phase 11 must close safety evidence gaps without enabling unsafe actuator paths or overclaiming parity.
Output: A board-205-only evidence ledger plus artifact/redaction templates that downstream executors can fill from repo-owned commands.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/verification.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-safety-controller-hardware-regression-evidence/11-CONTEXT.md
@.planning/phases/11-safety-controller-hardware-regression-evidence/11-RESEARCH.md
@.planning/phases/11-safety-controller-hardware-regression-evidence/11-VALIDATION.md
@docs/parity/evidence/phase-06-safety-controllers-and-self-test.md
@docs/parity/evidence/phase-06-ultra-205-safety-hardware-smoke.md
@docs/parity/evidence/phase-06-display-input-runtime-gap.md
@docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening.md
@docs/parity/checklist.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Phase 11 runbook and tiered evidence matrix</name>
  <requirements>SAFE-01, SAFE-02, SAFE-03, SAFE-04, SAFE-05, SAFE-06, SAFE-07, SAFE-08, SAFE-09, EVD-05</requirements>
  <read_first>
    <file>AGENTS.md</file>
    <file>.planning/phases/11-safety-controller-hardware-regression-evidence/11-CONTEXT.md</file>
    <file>.planning/phases/11-safety-controller-hardware-regression-evidence/11-RESEARCH.md</file>
    <file>.planning/phases/11-safety-controller-hardware-regression-evidence/11-VALIDATION.md</file>
    <file>docs/parity/evidence/phase-06-ultra-205-safety-hardware-smoke.md</file>
    <file>docs/parity/checklist.md</file>
  </read_first>
  <files>docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md</files>
  <scope>Planning and evidence documentation only. Do not run hardware commands in this task.</scope>
  <action>
    Create `docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md` with these exact top-level sections:
    `# Phase 11 Safety Controller Hardware Regression Evidence`, `## Scope`, `## Hardware Gate`, `## Recovery Protocol`, `## Allowed Command Set`, `## Prohibited Commands Unless A Later Task Adds Bounded Recovery`, `## Surface Evidence Matrix`, `## Component Evidence Packs`, `## Execution Log`, `## Checklist Promotion Rules`, `## Secret Redaction Review`, `## Residual Risks`, and `## Conclusion`.

    The `Hardware Gate` section must implement D-01 exactly: the first live command is `just detect-ultra205`, continuation is allowed only when exactly one likely ESP USB serial port is found and the detector's `espflash board-info --chip esp32s3 --port <port> --non-interactive` check succeeds. The stop cases must be explicit: zero likely ports, multiple likely ports, board-info failure, target other than board `205`, missing recovery path, missing evidence procedure, or redaction uncertainty.

    The `Recovery Protocol` section must implement D-02 and D-04. It must say that this plan set permits only observe-only and wrapper-owned flash-monitor capture unless a later executor adds a bounded probe with explicit input limits, stop conditions, recovery steps, and fail-closed output. Recovery commands are limited to repo-owned non-destructive commands: `just detect-ultra205`, `just flash-monitor board=205 port=<port> evidence-dir=docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence`, `just monitor port=<port>`, `just parity`, and `just test`. It must direct executors to stop and record `hardware evidence pending` when recovery prerequisites are incomplete.

    The `Prohibited Commands Unless A Later Task Adds Bounded Recovery` section must name these blocked surfaces exactly per D-02, D-03, D-06, D-09, D-11, and D-13: active DS4432U voltage writes, fan duty actuation, overheat stimulus, fault injection, self-test hardware submodes that touch voltage/fan/ASIC work, mining stress, raw I2C writes, raw flash writes, erase, rollback, and interrupted-update procedures. State that observe-only evidence cannot verify these active behaviors.

    The `Surface Evidence Matrix` must include one row for each checklist row: `PWR-001`, `PWR-002`, `PWR-003`, `PWR-005`, `PWR-006`, `THR-001`, `THR-002`, `THR-003`, `IO-001`, `UI-001`, `UI-002`, `UI-003`, `SELF-001`, `STAT-002`, `API-002`, `API-006`, plus an `EVD-05` evidence-governance row. Each row must have columns: `Requirement`, `Checklist Row`, `Component`, `Claim Type`, `Allowed Probe`, `Required Metadata`, `Pass Criteria`, `Failure Criteria`, `Evidence Artifact`, `Allowed Evidence Token`, and `Promotion Boundary`. Per D-08, do not let a black-box flash/monitor smoke row verify active voltage, fan, thermal, power, self-test hardware, or failure-path parity.

    Use these exact claim-type distinctions per D-05 through D-14:
    read-only telemetry for `PWR-006`, thermal/RPM observation parts of `THR-001` and `THR-002`, and safe-unavailable API/log/WebSocket status may use `hardware-smoke` only for the exact observed board-named claim;
    active DS4432U voltage writes, fan duty effects, overheat/fault handling, self-test hardware submodes, ASIC reset/power sequencing, and runtime input/display hardware behavior require `hardware-regression`;
    pure PID logic for `THR-003` remains `unit` and must not verify fan hardware;
    startup SSD1306 evidence supports only startup display observations and must not verify runtime display/input parity per D-10.
    Per D-12, if self-test, display/input, watchdog, or load responsiveness cannot be safely run, the ledger must record owner/follow-up and `hardware evidence pending` instead of leaving stale Phase 6 wording.
    Per D-16, every evidence artifact contract must require board `205`, selected port, source commit, reference commit, exact command or probe, package manifest or firmware identity when applicable, logs, observed behavior, conclusion, and secret-redaction review.

    The `Checklist Promotion Rules` section must implement D-13, D-14, and D-15: broad mixed rows stay below `verified` unless every active subclaim has matching evidence, narrow observed subclaims can be cited in notes without verifying the whole row, and `tools/parity` remains the source of truth for rejecting unsafe verified rows.
  </action>
  <verify>
    <automated>rg -n "Phase 11 Safety Controller Hardware Regression Evidence|just detect-ultra205|active DS4432U voltage writes|Surface Evidence Matrix|PWR-003|THR-002|SELF-001|Secret Redaction Review|hardware evidence pending" docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md</automated>
    <automated>python - <<'PY'
from pathlib import Path
p = Path('docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md')
text = p.read_text()
assert '\n---\n' not in text, 'standalone body separator found'
for row in ['PWR-001','PWR-002','PWR-003','PWR-005','PWR-006','THR-001','THR-002','THR-003','IO-001','UI-001','UI-002','UI-003','SELF-001','STAT-002','API-002','API-006','EVD-05']:
    assert row in text, row
PY</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The ledger contains `just detect-ultra205` and all listed stop cases.</criterion>
    <criterion>The ledger contains every required checklist row id named in the action.</criterion>
    <criterion>The ledger states active voltage, fan, overheat/fault, self-test hardware, raw writes, erase, rollback, interrupted-update, and mining stress are not allowed without bounded recovery.</criterion>
    <criterion>The ledger contains `Secret Redaction Review` and the phrase `hardware evidence pending`.</criterion>
    <criterion>The ledger body contains no standalone `---` separator.</criterion>
  </acceptance_criteria>
  <done>Phase 11 has a runbook and tiered matrix that fully covers D-01 through D-16 without permitting unsafe live hardware work.</done>
</task>

<task type="auto">
  <name>Task 2: Add component evidence pack and redaction templates</name>
  <requirements>SAFE-01, SAFE-02, SAFE-04, SAFE-05, SAFE-06, SAFE-07, SAFE-09, EVD-05</requirements>
  <read_first>
    <file>docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md</file>
    <file>docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening.md</file>
    <file>tools/flash/src/main.rs</file>
    <file>AGENTS.md</file>
  </read_first>
  <files>docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/README.md, docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/redaction-review.md</files>
  <scope>Planning and evidence template files only. Do not create generated JSON/log evidence by hand.</scope>
  <action>
    Create `docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/README.md` with `# Phase 11 Safety Evidence Artifacts`, `## Component Evidence Packs`, `## Generated By Repo Commands`, `## Required Metadata`, `## Pending Evidence Semantics`, and `## Do Not Hand Edit Generated Artifacts`.

    The README must define these component packs per D-07: `safe-baseline`, `power-telemetry`, `voltage-control`, `thermal-fan`, `self-test-watchdog-load`, `display-input`, and `parity-guard`. For each pack, list the expected artifact names and conclusion rules:
    `flash-command-evidence.json` and `flash-monitor.log` are generated only by `just flash-monitor board=205 port=<port> evidence-dir=docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence`;
    `redaction-review.md` is written by the executor after inspecting captured artifacts;
    active-control packs may remain `hardware evidence pending` when bounded probes or recovery are absent.

    Create `docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/redaction-review.md` with `# Phase 11 Secret Redaction Review`, `## Artifact Scope`, `## Review Checklist`, `## Findings`, and `## Conclusion`.
    The review checklist must require explicit checks for Wi-Fi SSIDs/passwords, pool URLs/usernames/passwords, private endpoints, NVS secret values, API tokens, local private IP disclosure beyond necessary bench evidence, and pasted raw terminal secrets. The conclusion must start as `Conclusion: pending - no Phase 11 live artifacts reviewed yet`.
  </action>
  <verify>
    <automated>rg -n "safe-baseline|power-telemetry|voltage-control|thermal-fan|self-test-watchdog-load|display-input|parity-guard|flash-command-evidence\\.json|flash-monitor\\.log|hardware evidence pending" docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/README.md</automated>
    <automated>rg -n "Wi-Fi|pool URLs|pool usernames|pool passwords|private endpoints|NVS secret values|API tokens|Conclusion: pending - no Phase 11 live artifacts reviewed yet" docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/redaction-review.md</automated>
    <automated>python - <<'PY'
from pathlib import Path
for path in [
    Path('docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/README.md'),
    Path('docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/redaction-review.md'),
]:
    assert path.exists(), path
    assert '\n---\n' not in path.read_text(), f'standalone body separator found in {path}'
PY</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The artifact README names all seven component packs.</criterion>
    <criterion>The artifact README says JSON/log artifacts are generated by `just flash-monitor board=205 port=<port> evidence-dir=docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence`.</criterion>
    <criterion>The redaction template includes every secret category listed in the action.</criterion>
    <criterion>Both new Markdown files contain no standalone `---` body separator.</criterion>
  </acceptance_criteria>
  <done>The evidence artifact directory has a concrete pack contract and redaction procedure before live artifacts are captured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Executor to hardware runbook | Human or agent may attempt unsafe hardware actions from documentation instructions. |
| Hardware logs to committed evidence | Serial/API logs may contain secrets or private endpoint data before redaction. |
| Evidence ledger to checklist promotion | Narrow observations may be misread as verified parity for broad safety surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-01-01 | Information Disclosure | `redaction-review.md` and captured logs | mitigate | Require explicit redaction review for Wi-Fi, pool credentials, private endpoints, NVS secrets, API tokens, and pasted terminal secrets before evidence is cited. |
| T-11-01-02 | Denial of Service | Safety runbook actuator paths | mitigate | Ledger permits only detector-gated observe-only or wrapper flash-monitor capture; active DS4432U writes, fan duty actuation, overheat/fault injection, self-test hardware, erase, rollback, raw writes, and mining stress stop without bounded recovery. |
| T-11-01-03 | Tampering | Checklist promotion rules | mitigate | Surface matrix separates `hardware-smoke` from `hardware-regression` and states broad mixed rows stay below `verified` unless every active subclaim is evidenced. |
| T-11-01-04 | Spoofing | USB hardware identity | mitigate | Runbook requires `just detect-ultra205` and successful board-info before any live evidence command. |
| T-11-01-05 | Repudiation | Manual observations | mitigate | Component packs require board `205`, selected port, source commit, reference commit, exact command/probe, logs, observed behavior, conclusion, and redaction review. |
</threat_model>

<verification>
Run the task-level `rg` and Python checks. Then run `just parity` to confirm the new documentation does not introduce invalid checklist claims.
</verification>

<success_criteria>
Phase 11 has a complete Wave 0 evidence contract. No live hardware command is run by this plan, no unsafe actuator path is authorized, and all SAFE-01 through SAFE-09 plus EVD-05 have a ledger row and promotion boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/11-safety-controller-hardware-regression-evidence/11-01-SUMMARY.md`.
</output>
