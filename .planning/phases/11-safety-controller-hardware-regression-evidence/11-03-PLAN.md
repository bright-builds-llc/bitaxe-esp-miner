---
phase: 11
plan: 03
type: execute
status: planned
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  - docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md
  - docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/README.md
  - docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/redaction-review.md
  - docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/flash-command-evidence.json
  - docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/flash-monitor.log
  - docs/parity/checklist.md
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-03, SAFE-04, SAFE-05, SAFE-06, SAFE-07, SAFE-08, SAFE-09, EVD-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 11-2026-06-29T20-23-34
generated_at: 2026-06-29T20:56:05.022Z
must_haves:
  truths:
    - "Executor records detector-gated Ultra 205 safety evidence or explicit pending status"
    - "Evidence files cite only claims actually supported by captured logs, command output, and redaction review"
    - "Checklist rows remain below verified unless their required hardware evidence class is present"
    - "Final verification proves safety tests, parity tests, full tests, and parity report remain green"
  artifacts:
    - path: "docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md"
      provides: "Executed Phase 11 evidence ledger and final conclusions"
      contains: "Execution Log"
    - path: "docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/flash-command-evidence.json"
      provides: "Wrapper-generated machine evidence when detector-gated flash-monitor runs"
      contains: "trusted_output"
    - path: "docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/flash-monitor.log"
      provides: "Wrapper-generated serial log when detector-gated flash-monitor runs"
      contains: "safe_state:"
    - path: "docs/parity/checklist.md"
      provides: "Phase 11 checklist citations and conservative status posture"
      contains: "phase-11-safety-controller-hardware-regression-evidence"
  key_links:
    - from: "docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md"
      to: "docs/parity/checklist.md"
      via: "Checklist notes cite exact evidence boundaries"
      pattern: "phase-11-safety-controller-hardware-regression-evidence"
    - from: "docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md"
      to: "just flash-monitor"
      via: "Wrapper-generated evidence artifacts"
      pattern: "flash-command-evidence\\.json"
---

<objective>
Capture detector-gated Phase 11 evidence when the Ultra 205 gate succeeds, otherwise record precise pending status, then update the parity checklist without overclaiming active safety-control behavior.

Purpose: Phase 11 should close the audit gap as far as current safe surfaces allow while preserving explicit gaps for unavailable hardware, missing recovery, or unimplemented active probes.
Output: Updated evidence ledger, optional wrapper-generated JSON/log artifacts, redaction review, checklist citations, and final verification record.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/11-safety-controller-hardware-regression-evidence/11-CONTEXT.md
@.planning/phases/11-safety-controller-hardware-regression-evidence/11-RESEARCH.md
@docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md
@docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/README.md
@docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/redaction-review.md
@docs/parity/checklist.md
@scripts/detect-ultra205.sh
@tools/flash/src/main.rs
@tools/parity/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run safe verification and detector-gated evidence capture</name>
  <requirements>SAFE-01, SAFE-02, SAFE-05, SAFE-06, SAFE-07, SAFE-09, EVD-05</requirements>
  <read_first>
    <file>AGENTS.md</file>
    <file>docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md</file>
    <file>docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/README.md</file>
    <file>docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/redaction-review.md</file>
    <file>scripts/detect-ultra205.sh</file>
    <file>tools/flash/src/main.rs</file>
  </read_first>
  <files>docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md, docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/redaction-review.md, docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/flash-command-evidence.json, docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/flash-monitor.log</files>
  <scope>Run existing repo commands only. Do not run active DS4432U voltage writes, fan duty actuation, overheat/fault injection, self-test hardware submodes, mining stress, raw writes, erase, rollback, or interrupted-update commands.</scope>
  <action>
    First run host-side safety checks:
    `cargo test -p bitaxe-safety --all-features`
    `cargo test -p bitaxe-parity --all-features`

    Then run the hardware gate exactly once:
    `just detect-ultra205`

    If `just detect-ultra205` does not find exactly one likely Ultra 205 port or its board-info command fails, do not run any flash, monitor, raw hardware, or probe command. Append an `Execution Log` entry to the Phase 11 ledger with: command `just detect-ultra205`, result `blocked`, reason copied from detector output without secrets, conclusion `hardware evidence pending - detector gate did not pass`, and affected component packs `safe-baseline`, `power-telemetry`, `thermal-fan`, `self-test-watchdog-load`, and `display-input`.

    If the detector succeeds, copy the printed `port=<path>` value and run:
    `just flash-monitor board=205 port=<path> evidence-dir=docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence`

    For a successful or fail-closed wrapper run, update the Phase 11 ledger from the generated `flash-command-evidence.json` and `flash-monitor.log`. Per D-16, record board `205`, selected port, source commit, reference commit, exact commands, package manifest or firmware identity when available, wrapper capture status, trusted-output status, observed log markers, redaction review status, and conclusion. Use only exact observed markers. At minimum classify these if present: `safe_state: mining=disabled asic_work_submission=disabled hardware_control=disabled`, `safety_supervisor=started thread=bitaxe-safety-supervisor cadence_ms=100`, `safety_supervisor_step=yield reason=yield_interval_reached`, and `display_input_status=runtime_gap reason=hardware_evidence_pending startup_only=true`.

    Update `redaction-review.md` after inspecting generated artifacts. If no generated artifacts exist because the detector failed, set conclusion to `Conclusion: passed - no live Phase 11 artifacts were generated; detector output reviewed for secrets`. If artifacts exist, list each artifact and mark every secret category from the template as `not present` or `redacted`.

    Do not infer read-only INA260, EMC2101, fan RPM, API, WebSocket, self-test, runtime display/input, active voltage, fan actuation, overheat/fault, ASIC reset, or watchdog-load parity unless the captured log or a safe existing route explicitly proves that exact claim. When a claim is absent, write `hardware evidence pending` for that component pack.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-safety --all-features</automated>
    <automated>cargo test -p bitaxe-parity --all-features</automated>
    <automated>rg -n "Detector Gate Result|hardware evidence pending - detector gate did not pass|selected port: /dev/|selected port: COM|flash-command-evidence\\.json" docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md</automated>
    <automated>if rg -q "selected port: (/dev/|COM)[^ ]+" docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md; then test -f docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/flash-command-evidence.json && test -f docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/flash-monitor.log; else rg -n "hardware evidence pending - detector gate did not pass" docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md; fi</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The ledger records the `just detect-ultra205` result before any `just flash-monitor` entry.</criterion>
    <criterion>If detection failed, the ledger contains `hardware evidence pending - detector gate did not pass` and no generated JSON/log artifact is required.</criterion>
    <criterion>If detection passed, wrapper-generated `flash-command-evidence.json` and `flash-monitor.log` exist under the Phase 11 artifact directory.</criterion>
    <criterion>The redaction review has a non-pending conclusion after this task.</criterion>
    <criterion>No action in the ledger uses active voltage writes, fan actuation, overheat/fault injection, self-test hardware submodes, mining stress, raw writes, erase, rollback, or interrupted-update commands.</criterion>
  </acceptance_criteria>
  <done>Phase 11 has either detector-gated wrapper evidence or explicit hardware-unavailable pending status, with redaction reviewed.</done>
</task>

<task type="auto">
  <name>Task 2: Update checklist conclusions and run final verification</name>
  <requirements>SAFE-01, SAFE-02, SAFE-03, SAFE-04, SAFE-05, SAFE-06, SAFE-07, SAFE-08, SAFE-09, EVD-05</requirements>
  <read_first>
    <file>docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md</file>
    <file>docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence/redaction-review.md</file>
    <file>docs/parity/checklist.md</file>
    <file>tools/parity/src/main.rs</file>
    <file>.planning/phases/11-safety-controller-hardware-regression-evidence/11-VALIDATION.md</file>
  </read_first>
  <files>docs/parity/checklist.md, docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md</files>
  <scope>Checklist and evidence documentation only. Do not edit `reference/esp-miner`.</scope>
  <action>
    Update `docs/parity/checklist.md` rows affected by Phase 11: `PWR-001`, `PWR-002`, `PWR-003`, `PWR-005`, `PWR-006`, `THR-001`, `THR-002`, `THR-003`, `IO-001`, `UI-001`, `UI-002`, `UI-003`, `SELF-001`, `STAT-002`, `API-002`, and `API-006`.

    Each updated note must cite `docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md` and state the exact conclusion from the ledger. Keep rows below `verified` when evidence is pending, observe-only, safe-unavailable, startup-only, or read-only but the row also includes active behavior. Do not mark `PWR-001`, `PWR-002`, `PWR-003`, `PWR-005`, `THR-001`, `THR-002`, `SELF-001`, or `UI-003` as `verified` unless the ledger contains matching `hardware-regression` evidence for that row.

    `THR-003` may remain `implemented | unit` because PID behavior is pure host-tested. `PWR-006` may be promoted only if the ledger contains board-205 read-only INA260 telemetry freshness evidence; otherwise leave it below `verified` and cite the pending or safe-unavailable conclusion. `UI-001`, `UI-002`, and `UI-003` must continue to distinguish startup SSD1306 support from runtime display/input parity.

    Append a `Final Verification` section to the Phase 11 ledger with command results for:
    `cargo test -p bitaxe-safety --all-features`
    `cargo test -p bitaxe-parity --all-features`
    `just parity`
    `just test`
    Include `git diff -- reference/esp-miner --exit-code` as the reference read-only check. If hardware was unavailable, state that hardware evidence is pending by detector gate and that no safety-critical active-control row was promoted.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-safety --all-features</automated>
    <automated>cargo test -p bitaxe-parity --all-features</automated>
    <automated>just parity</automated>
    <automated>just test</automated>
    <automated>git diff -- reference/esp-miner --exit-code</automated>
    <automated>rg -n "phase-11-safety-controller-hardware-regression-evidence" docs/parity/checklist.md</automated>
    <automated>rg -n "Final Verification|Secret Redaction Review|hardware evidence pending|hardware-regression|just parity|just test" docs/parity/evidence/phase-11-safety-controller-hardware-regression-evidence.md</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Every affected checklist row listed in the action cites the Phase 11 evidence ledger or is intentionally unchanged with a reason recorded in the plan summary.</criterion>
    <criterion>`just parity` exits 0 after checklist updates.</criterion>
    <criterion>`just test` exits 0 after checklist updates.</criterion>
    <criterion>`reference/esp-miner` has no diff.</criterion>
    <criterion>The ledger's final conclusion states whether Phase 11 captured hardware evidence or recorded hardware-unavailable pending status.</criterion>
  </acceptance_criteria>
  <done>Checklist and evidence posture match the captured Phase 11 proof without overclaiming safety-critical parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| USB host to Ultra 205 | Live hardware commands can affect a physical board. |
| Serial logs to repository evidence | Captured logs can include private data or stale identity markers. |
| Evidence ledger to checklist | Documentation changes can promote or withhold parity claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-11-03-01 | Spoofing | `just detect-ultra205` gate | mitigate | Use only the detector-provided `port=<path>` after exactly one candidate and successful board-info; stop otherwise. |
| T-11-03-02 | Information Disclosure | `flash-monitor.log`, `flash-command-evidence.json`, redaction review | mitigate | Review generated artifacts for Wi-Fi, pool, private endpoint, NVS, API token, and pasted terminal secrets before citing them. |
| T-11-03-03 | Denial of Service | Live hardware action set | mitigate | This plan permits no active voltage writes, fan duty actuation, overheat/fault injection, self-test hardware submodes, raw writes, erase, rollback, interrupted update, or mining stress. |
| T-11-03-04 | Tampering | `docs/parity/checklist.md` | mitigate | Final `just parity` plus active-control `hardware-regression` validation prevents unsafe `verified` promotions. |
| T-11-03-05 | Repudiation | Manual and automated evidence conclusions | mitigate | Ledger records exact commands, output-derived markers, board, port, commits, artifacts, redaction status, and final conclusion. |
</threat_model>

<verification>
Final verification for this plan is `cargo test -p bitaxe-safety --all-features`, `cargo test -p bitaxe-parity --all-features`, `just parity`, `just test`, and `git diff -- reference/esp-miner --exit-code`.
</verification>

<success_criteria>
Phase 11 produces evidence or precise pending status for every SAFE-01 through SAFE-09 surface, keeps active-control claims below verified without `hardware-regression`, records redaction review, and leaves the reference tree read-only.
</success_criteria>

<output>
After completion, create `.planning/phases/11-safety-controller-hardware-regression-evidence/11-03-SUMMARY.md`.
</output>
