---
phase: 13-final-ultra-205-release-evidence
plan: "04"
type: execute
wave: 4
depends_on:
  - "13-03"
files_modified:
  - scripts/phase13-monitor-capture.sh
  - scripts/phase13-monitor-capture-test.sh
  - scripts/phase13-firmware-ota-smoke.sh
  - scripts/phase13-firmware-ota-smoke-test.sh
  - scripts/BUILD.bazel
  - docs/parity/evidence/phase-13-final-ultra-205-release-evidence/firmware-ota.md
  - docs/parity/evidence/phase-13-final-ultra-205-release-evidence/firmware-ota/firmware-ota-smoke.log
  - docs/parity/evidence/phase-13-final-ultra-205-release-evidence/firmware-ota/post-ota-monitor.log
  - docs/parity/evidence/phase-13-final-ultra-205-release-evidence/redaction-review.md
autonomous: true
requirements:
  - REL-02
  - REL-08
  - EVD-05
must_haves:
  truths:
    - "Firmware OTA evidence uses the manifest `esp-miner.bin` from the same release package."
    - "Invalid image rejection is recorded separately and is not treated as rollback proof."
    - "Valid OTA success is followed by reboot/boot-validation identity evidence, or the exact blocker is recorded."
  artifacts:
    - path: "scripts/phase13-firmware-ota-smoke.sh"
      provides: "Repo-owned firmware OTA evidence helper"
      contains: "/api/system/OTA"
    - path: "scripts/phase13-monitor-capture.sh"
      provides: "Bounded noninteractive post-OTA monitor capture"
      contains: "espflash monitor --chip esp32s3"
    - path: "docs/parity/evidence/phase-13-final-ultra-205-release-evidence/firmware-ota.md"
      provides: "Firmware OTA evidence summary"
      contains: "firmware_ota_status"
  key_links:
    - from: "bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json"
      to: "/api/system/OTA"
      via: "manifest `esp-miner.bin` upload"
      pattern: "esp-miner\\.bin"
    - from: "firmware OTA response"
      to: "post-ota-monitor.log"
      via: "bounded monitor capture after reboot"
      pattern: "ota_boot_validation="
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13-2026-06-30T14-53-46
generated_at: 2026-06-30T15:21:33Z
---

<objective>
Add and run firmware OTA evidence helpers covering invalid rejection, valid upload, reboot identity, and boot-validation logs.

Purpose: Firmware OTA and rollback-related claims require live upload plus post-reboot evidence, not just implementation, package output, or invalid rejection.
Output: OTA helper scripts, tests, OTA log artifacts, post-OTA monitor log, and evidence summary.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-final-ultra-205-release-evidence/13-CONTEXT.md
@docs/parity/evidence/phase-13-final-ultra-205-release-evidence/package-release-gate.md
@docs/parity/evidence/phase-13-final-ultra-205-release-evidence/hardware-detection.md
@docs/parity/evidence/phase-13-final-ultra-205-release-evidence/http-static-recovery.md
@crates/bitaxe-api/src/update_plan.rs
@firmware/bitaxe/src/http_api.rs
@firmware/bitaxe/src/ota_update.rs
@firmware/bitaxe/src/boot_validation.rs
@scripts/BUILD.bazel

<interfaces>
From `crates/bitaxe-api/src/update_plan.rs`:
```rust
FirmwareOtaDecision {
    filename: "esp-miner.bin",
    success_response: PublicHttpResponse { status: 200, body: "Firmware update complete, rebooting now!", .. },
    validation_error_response: PublicHttpResponse { status: 500, body: "Validation / Activation Error", .. },
}
```

From `firmware/bitaxe/src/boot_validation.rs`:
```rust
log::info!("ota_boot_validation=not_pending state={}", state.as_str());
log::info!("ota_boot_validation=marked_valid");
log::info!("ota_boot_validation=marked_invalid_reboot");
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement bounded monitor and firmware OTA smoke helpers</name>
  <files>scripts/phase13-monitor-capture.sh, scripts/phase13-monitor-capture-test.sh, scripts/phase13-firmware-ota-smoke.sh, scripts/phase13-firmware-ota-smoke-test.sh, scripts/BUILD.bazel</files>
  <read_first>
    - `scripts/BUILD.bazel`
    - `scripts/phase13-http-static-smoke.sh`
    - `crates/bitaxe-api/src/update_plan.rs`
    - `firmware/bitaxe/src/http_api.rs`
    - `firmware/bitaxe/src/ota_update.rs`
    - `firmware/bitaxe/src/boot_validation.rs`
  </read_first>
  <action>Create `scripts/phase13-monitor-capture.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. It must accept `--port <path>`, `--out <log>`, `--seconds <n>` defaulting to 35, and optional `--no-reset`; it must run exactly `espflash monitor --chip esp32s3 --port <path> --non-interactive` plus `--no-reset` when requested, terminate the monitor after the bounded duration, and write `capture_status=completed`, `capture_status=timed_out_after_capture`, or `capture_status=failed` to the log. This is a repo-owned wrapper around raw monitor only because `just monitor` is interactive; do not add raw serial writes or raw flash commands. Create `scripts/phase13-firmware-ota-smoke.sh` with `--device-url`, `--manifest`, `--ota-image`, `--port`, `--out-dir`, and `--monitor-seconds`. If `DEVICE_URL` is missing or blocked by Plan 03, write `firmware_ota_status: blocked - DEVICE_URL unavailable` and exit 0 per D-06. When URL and port are available, compute the SHA-256 of `esp-miner.bin`, create a fixed invalid image file under the output dir, POST the invalid image to `/api/system/OTA`, record public status/body as invalid image rejection per D-09, then POST the manifest `esp-miner.bin` to `/api/system/OTA`. On valid OTA success, run `phase13-monitor-capture.sh` and require `firmware_commit=`, `reference_commit=`, and `ota_boot_validation=` markers in `post-ota-monitor.log` before recording OTA evidence as passed. The helper may create log artifacts because they reduce manual transcription risk per D-19; it must not replace the prose ledger required by Plan 06. Register `phase13_monitor_capture`, `phase13_monitor_capture_test`, `phase13_firmware_ota_smoke`, and `phase13_firmware_ota_smoke_test` in `scripts/BUILD.bazel`.</action>
  <verify>
    <automated>bash -n scripts/phase13-monitor-capture.sh && bash -n scripts/phase13-monitor-capture-test.sh && bash -n scripts/phase13-firmware-ota-smoke.sh && bash -n scripts/phase13-firmware-ota-smoke-test.sh && bazel test //scripts:phase13_monitor_capture_test //scripts:phase13_firmware_ota_smoke_test</automated>
  </verify>
  <acceptance_criteria>
    - Monitor helper wraps only `espflash monitor --chip esp32s3 --port <path> --non-interactive` and never writes serial data.
    - OTA helper records missing `DEVICE_URL` as blocked, not as success or failure-by-shortcut.
    - OTA helper records invalid image rejection separately from valid OTA and states that invalid rejection is not rollback proof per D-10.
    - Helper tests cover missing URL, fake invalid rejection, fake valid success, and post-OTA marker requirements.
  </acceptance_criteria>
  <done>Firmware OTA evidence can be collected with tested repo-owned helpers and bounded post-reboot monitor capture.</done>
</task>

<task type="auto">
  <name>Task 2: Run firmware OTA smoke or record blocker</name>
  <files>docs/parity/evidence/phase-13-final-ultra-205-release-evidence/firmware-ota.md, docs/parity/evidence/phase-13-final-ultra-205-release-evidence/firmware-ota/firmware-ota-smoke.log, docs/parity/evidence/phase-13-final-ultra-205-release-evidence/firmware-ota/post-ota-monitor.log, docs/parity/evidence/phase-13-final-ultra-205-release-evidence/redaction-review.md</files>
  <read_first>
    - `docs/parity/evidence/phase-13-final-ultra-205-release-evidence/package-release-gate.md`
    - `docs/parity/evidence/phase-13-final-ultra-205-release-evidence/hardware-detection.md`
    - `docs/parity/evidence/phase-13-final-ultra-205-release-evidence/http-static-recovery.md`
    - `scripts/phase13-firmware-ota-smoke.sh`
  </read_first>
  <action>If Plan 02 detector passed and Plan 03 did not block `DEVICE_URL`, run `scripts/phase13-firmware-ota-smoke.sh --device-url "${DEVICE_URL:-}" --manifest bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json --ota-image bazel-bin/firmware/bitaxe/esp-miner.bin --port <selected-port> --out-dir docs/parity/evidence/phase-13-final-ultra-205-release-evidence/firmware-ota --monitor-seconds 45`. If prerequisites are missing, write `firmware-ota.md` with `firmware_ota_status: blocked - <reason>` and keep OTA rows below verified. On success, write `firmware-ota.md` with artifact checksum, invalid upload request/status/body, valid upload request/status/body, reboot observation, post-reboot firmware commit, reference commit, `ota_boot_validation=` line, selected partition or explicit unavailable state, and final conclusion per D-09 and D-10. Update redaction review for OTA logs, invalid image bytes, valid upload command, post-OTA monitor log, and any sanitized `DEVICE_URL` values per D-20.</action>
  <verify>
    <automated>test -f docs/parity/evidence/phase-13-final-ultra-205-release-evidence/firmware-ota.md && test -f docs/parity/evidence/phase-13-final-ultra-205-release-evidence/firmware-ota/firmware-ota-smoke.log && rg -n "firmware_ota_status: (passed|blocked)|esp-miner\\.bin|invalid image rejection|valid OTA|Firmware update complete, rebooting now!|ota_boot_validation=|not rollback proof|redaction" docs/parity/evidence/phase-13-final-ultra-205-release-evidence/firmware-ota.md docs/parity/evidence/phase-13-final-ultra-205-release-evidence/firmware-ota/firmware-ota-smoke.log</automated>
  </verify>
  <acceptance_criteria>
    - Successful evidence names the manifest `esp-miner.bin` checksum and the upload route `/api/system/OTA`.
    - Invalid image rejection is recorded and explicitly says it is not rollback proof.
    - Valid OTA success is followed by post-reboot identity and boot-validation markers, or a blocker is recorded.
    - Redaction review includes OTA request/response logs and post-OTA monitor output.
  </acceptance_criteria>
  <done>Firmware OTA accepted/rejected behavior and boot-validation status are recorded from live hardware when available, or blocked without overclaiming.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| local package artifact to firmware OTA route | A binary artifact is uploaded to a live device and can trigger reboot. |
| live device reboot to evidence | Post-OTA monitor logs determine whether boot validation and identity claims are supported. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-13-04-01 | Tampering | `/api/system/OTA` upload image | mitigate | Upload only manifest `esp-miner.bin`, record checksum, and reject artifact/commit mismatch per D-07 and D-09. |
| T-13-04-02 | Denial of Service | valid OTA reboot | mitigate | Require detector-approved port, package identity, post-reboot monitor capture, and recovery-ready evidence before running valid OTA. |
| T-13-04-03 | Repudiation | OTA evidence | mitigate | Record invalid and valid request route, body/status, checksum, reboot observation, boot-validation line, and conclusion. |
| T-13-04-04 | Information Disclosure | OTA logs and device URL | mitigate | Include all request/response and monitor artifacts in redaction review before commit. |
</threat_model>

<verification>
Run after tasks:

- `bash -n scripts/phase13-monitor-capture.sh scripts/phase13-firmware-ota-smoke.sh`
- `bazel test //scripts:phase13_monitor_capture_test //scripts:phase13_firmware_ota_smoke_test`
- `just parity`
- `git diff -- reference/esp-miner --exit-code`
- Full Rust pre-commit sequence before commit.
</verification>

<success_criteria>
Firmware OTA evidence proves invalid rejection, valid upload, reboot identity, and boot-validation where reachable; otherwise it records a specific blocker and keeps release-sensitive rows below verified.
</success_criteria>

<output>
After completion, create `.planning/phases/13-final-ultra-205-release-evidence/13-04-SUMMARY.md`.
</output>
