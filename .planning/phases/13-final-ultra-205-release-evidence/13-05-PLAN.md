---
phase: 13-final-ultra-205-release-evidence
plan: "05"
type: execute
wave: 4
depends_on:
  - "13-03"
files_modified:
  - scripts/phase13-monitor-capture.sh
  - scripts/phase13-monitor-capture-test.sh
  - scripts/phase13-recovery-regression.sh
  - scripts/phase13-recovery-regression-test.sh
  - scripts/BUILD.bazel
  - docs/parity/evidence/phase-13-final-ultra-205-release-evidence/recovery-runbook.md
  - docs/parity/evidence/phase-13-final-ultra-205-release-evidence/recovery-regression.md
  - docs/parity/evidence/phase-13-final-ultra-205-release-evidence/recovery-regression/recovery-regression.log
  - docs/parity/evidence/phase-13-final-ultra-205-release-evidence/recovery-regression/large-erase.log
  - docs/parity/evidence/phase-13-final-ultra-205-release-evidence/recovery-regression/large-erase-post-restore-monitor.log
  - docs/parity/evidence/phase-13-final-ultra-205-release-evidence/recovery-regression/interrupted-ota.log
  - docs/parity/evidence/phase-13-final-ultra-205-release-evidence/redaction-review.md
autonomous: true
requirements:
  - REL-08
  - REL-02
  - REL-01
  - EVD-05
must_haves:
  truths:
    - "Rollback, recovery, large erase, failed-update, and interrupted-update evidence run only after the current factory image recovery path is documented."
    - "Large erase uses an exact phase-approved erase command and restores with the current manifest factory image."
    - "If destructive or fault-injection prerequisites are missing, pending evidence is written and release parity is not overclaimed."
  artifacts:
    - path: "docs/parity/evidence/phase-13-final-ultra-205-release-evidence/recovery-runbook.md"
      provides: "Current manifest recovery and destructive stop conditions"
      contains: "Recovery Procedure"
    - path: "scripts/phase13-recovery-regression.sh"
      provides: "Repo-owned recovery/destructive evidence wrapper"
      contains: "espflash erase-flash --chip esp32s3"
    - path: "scripts/phase13-monitor-capture.sh"
      provides: "Bounded noninteractive monitor capture used by recovery and OTA evidence"
      contains: "espflash monitor --chip esp32s3"
    - path: "docs/parity/evidence/phase-13-final-ultra-205-release-evidence/recovery-regression.md"
      provides: "Recovery regression evidence summary"
      contains: "recovery_regression_status"
  key_links:
    - from: "bitaxe-ultra205-factory.bin"
      to: "large erase recovery"
      via: "factory reflash after exact erase command"
      pattern: "bitaxe-ultra205-factory\\.bin"
    - from: "interrupted OTA command"
      to: "post-interruption operability"
      via: "HTTP/static helper rerun"
      pattern: "interrupted-update"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13-2026-06-30T14-53-46
generated_at: 2026-06-30T15:21:33Z
---

<objective>
Document and execute, where safe, rollback/recovery/destructive evidence for final release parity.

Purpose: REL-08 claims are high risk and require exact recovery procedures, current factory image identity, stop conditions, and redaction before any verified claim.
Output: Recovery runbook, tested recovery regression helper, recovery/destructive logs, and conservative evidence summary.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-final-ultra-205-release-evidence/13-CONTEXT.md
@docs/release/ultra-205.md
@docs/parity/evidence/phase-13-final-ultra-205-release-evidence/package-release-gate.md
@docs/parity/evidence/phase-13-final-ultra-205-release-evidence/hardware-detection.md
@docs/parity/evidence/phase-13-final-ultra-205-release-evidence/http-static-recovery.md
@scripts/phase13-http-static-smoke.sh
@scripts/BUILD.bazel

<interfaces>
Exact destructive command permitted only inside this plan's helper after recovery prerequisites:
```bash
espflash erase-flash --chip esp32s3 --port <path> --non-interactive
```

Factory restore command must route through repo-owned flash tooling:
```bash
just flash board=205 port=<path> image=bazel-bin/firmware/bitaxe/bitaxe-ultra205-factory.bin manifest=bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json evidence-dir=<out-dir>/large-erase-restore
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create recovery runbook and destructive evidence helper</name>
  <files>docs/parity/evidence/phase-13-final-ultra-205-release-evidence/recovery-runbook.md, scripts/phase13-monitor-capture.sh, scripts/phase13-monitor-capture-test.sh, scripts/phase13-recovery-regression.sh, scripts/phase13-recovery-regression-test.sh, scripts/BUILD.bazel</files>
  <read_first>
    - `AGENTS.md`
    - `docs/release/ultra-205.md`
    - `docs/parity/evidence/phase-13-final-ultra-205-release-evidence/package-release-gate.md`
    - `scripts/phase13-http-static-smoke.sh`
    - `scripts/BUILD.bazel`
  </read_first>
  <action>Create `recovery-runbook.md` with exact sections: `# Phase 13 Recovery Runbook`, `## Current Package Identity`, `## Recovery Procedure`, `## Stop Conditions`, `## Large Erase Procedure`, `## Failed Update Procedure`, `## Interrupted Update Procedure`, `## OTAWWW Gap Procedure`, and `## Evidence Artifacts`. It must name the current manifest path, factory image path `bazel-bin/firmware/bitaxe/bitaxe-ultra205-factory.bin`, OTA image path `bazel-bin/firmware/bitaxe/esp-miner.bin`, exact large erase command `espflash erase-flash --chip esp32s3 --port <path> --non-interactive`, factory restore command through `just flash`, monitor command through `scripts/phase13-monitor-capture.sh`, and HTTP recovery check through `scripts/phase13-http-static-smoke.sh` per D-13 to D-16. Create `scripts/phase13-monitor-capture.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. It must accept `--port <path>`, `--out <log>`, `--seconds <n>` defaulting to 35, and optional `--no-reset`; it must run exactly `espflash monitor --chip esp32s3 --port <path> --non-interactive` plus `--no-reset` when requested, terminate the monitor after the bounded duration, and write `capture_status=completed`, `capture_status=timed_out_after_capture`, or `capture_status=failed` to the log. This is a repo-owned wrapper around raw monitor only because `just monitor` is interactive; do not add raw serial writes or raw flash commands. Create `scripts/phase13-recovery-regression.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. It must accept `--device-url`, `--manifest`, `--factory-image`, `--ota-image`, `--port`, `--out-dir`, `--allow-failed-update`, `--allow-large-erase`, and `--allow-interrupted-ota`. Without an allow flag, it must write `failed_update_status: pending - allow flag not provided`, `large_erase_status: pending - allow flag not provided`, or `interrupted_update_status: pending - allow flag not provided` and exit 0. With `--allow-failed-update`, it must create a fixed invalid firmware image, compute its checksum, POST it to `/api/system/OTA`, record route, artifact checksum, failure point, public status/body, post-failure partition/static/API state, recovery steps taken or explicitly not needed, and conclusion. With `--allow-large-erase`, it must verify the factory image exists, record `espflash --version`, run the exact erase command, restore with the `just flash ... image=... manifest=...` command, run bounded monitor capture, and rerun HTTP static smoke when `DEVICE_URL` is available. With `--allow-interrupted-ota`, it must run a bounded interrupted firmware upload to `/api/system/OTA` using `curl --max-time 1 --limit-rate 1024 --data-binary @<ota-image>`, record the interruption point/status, then rerun HTTP/static recovery checks. Create `scripts/phase13-monitor-capture-test.sh` and `scripts/phase13-recovery-regression-test.sh` with fake `espflash`, `just`, `curl`, monitor, and HTTP helper commands proving monitor command safety, default pending behavior, failed-update evidence fields, and exact command rendering. Register `phase13_monitor_capture`, `phase13_monitor_capture_test`, `phase13_recovery_regression`, and `phase13_recovery_regression_test` in `scripts/BUILD.bazel`.</action>
  <verify>
    <automated>bash -n scripts/phase13-monitor-capture.sh && bash -n scripts/phase13-monitor-capture-test.sh && bash -n scripts/phase13-recovery-regression.sh && bash -n scripts/phase13-recovery-regression-test.sh && bazel test //scripts:phase13_monitor_capture_test //scripts:phase13_recovery_regression_test && rg -n "espflash erase-flash --chip esp32s3 --port <path> --non-interactive|bitaxe-ultra205-factory\\.bin|Stop Conditions|Interrupted Update Procedure|OTAWWW Gap Procedure" docs/parity/evidence/phase-13-final-ultra-205-release-evidence/recovery-runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - Runbook names current package, factory image, OTA image, exact destructive command, restore command, monitor command, stop conditions, and artifacts.
    - Monitor helper wraps only `espflash monitor --chip esp32s3 --port <path> --non-interactive` and never writes serial data.
    - Helper defaults to pending for large erase and interrupted update without explicit allow flags.
    - Helper tests prove the exact `espflash erase-flash --chip esp32s3 --port <path> --non-interactive` command and `just flash ... image=... manifest=...` restore command are rendered.
    - No raw serial writes, raw flash writes, voltage/fan/mining stress, or unbounded fault-injection commands are added.
  </acceptance_criteria>
  <done>Destructive and fault-recovery procedures are documented with exact commands, recovery path, stop conditions, and tested helper behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Run recovery regression or record pending evidence</name>
  <files>docs/parity/evidence/phase-13-final-ultra-205-release-evidence/recovery-regression.md, docs/parity/evidence/phase-13-final-ultra-205-release-evidence/recovery-regression/recovery-regression.log, docs/parity/evidence/phase-13-final-ultra-205-release-evidence/recovery-regression/large-erase.log, docs/parity/evidence/phase-13-final-ultra-205-release-evidence/recovery-regression/large-erase-post-restore-monitor.log, docs/parity/evidence/phase-13-final-ultra-205-release-evidence/recovery-regression/interrupted-ota.log, docs/parity/evidence/phase-13-final-ultra-205-release-evidence/redaction-review.md</files>
  <read_first>
    - `docs/parity/evidence/phase-13-final-ultra-205-release-evidence/recovery-runbook.md`
    - `docs/parity/evidence/phase-13-final-ultra-205-release-evidence/hardware-detection.md`
    - `docs/parity/evidence/phase-13-final-ultra-205-release-evidence/http-static-recovery.md`
    - `scripts/phase13-recovery-regression.sh`
  </read_first>
  <action>If detector, package identity, factory image, selected port, `DEVICE_URL`, and recovery runbook are present, run `scripts/phase13-recovery-regression.sh --device-url "${DEVICE_URL:-}" --manifest bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json --factory-image bazel-bin/firmware/bitaxe/bitaxe-ultra205-factory.bin --ota-image bazel-bin/firmware/bitaxe/esp-miner.bin --port <selected-port> --out-dir docs/parity/evidence/phase-13-final-ultra-205-release-evidence/recovery-regression --allow-failed-update --allow-large-erase --allow-interrupted-ota`. If any prerequisite is missing, run the helper without the unsafe allow flag that lacks prerequisites so it writes pending evidence per D-16. Write `recovery-regression.md` with `recovery_regression_status: passed`, `blocked`, or `pending`, and include failed-update route `/api/system/OTA`, invalid artifact checksum, failure point, public status/body, post-failure partition/static/API state, recovery steps taken or explicitly not needed, large erase exact command/result, factory reflash result, post-restore monitor markers, interrupted-update request route/artifact checksum/failure point/status, OTAWWW gap response, rollback/boot-validation status as `pending - Plan 04 OTA evidence not run yet` unless already present, redaction result, and final conclusion per D-14 and D-15. Do not mark OTAWWW verified; keep public response `Wrong API input` as the REL-03 gap unless whole-`www` interrupted-update hardware-regression evidence is actually present.</action>
  <verify>
    <automated>test -f docs/parity/evidence/phase-13-final-ultra-205-release-evidence/recovery-regression.md && test -f docs/parity/evidence/phase-13-final-ultra-205-release-evidence/recovery-regression/recovery-regression.log && rg -n "recovery_regression_status: (passed|blocked|pending)|failed update|/api/system/OTA|artifact checksum|failure point|post-failure|recovery steps|large erase|interrupted-update|rollback|boot-validation|Wrong API input|factory reflash|redaction" docs/parity/evidence/phase-13-final-ultra-205-release-evidence/recovery-regression.md docs/parity/evidence/phase-13-final-ultra-205-release-evidence/recovery-regression/recovery-regression.log</automated>
  </verify>
  <acceptance_criteria>
    - Failed-update evidence records route, artifact checksum, failure point, status/body, post-failure state, recovery steps, and conclusion, or remains explicitly pending/blocking.
    - Passed evidence records exact large erase, factory reflash, bounded monitor, post-restore route state, failed-update, interrupted-update, and rollback/boot-validation observations or the pre-OTA pending status.
    - Pending evidence records the exact missing prerequisite and keeps corresponding checklist rows below verified.
    - OTAWWW remains an explicit REL-03 gap unless the helper records whole-`www` interrupted-update hardware-regression evidence, which this plan does not assume.
    - Redaction review includes all recovery/destructive logs before citation.
  </acceptance_criteria>
  <done>Recovery and destructive evidence is either safely captured under the documented runbook or recorded as pending/blocking without unsafe shortcuts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| destructive local command to physical flash | Large erase can remove all firmware and data from the connected device. |
| interrupted upload to firmware state | Fault-injection OTA can leave the device in a partially updated or rebooting state. |
| generated recovery logs to release claims | Recovery artifacts can promote or block REL-08 checklist claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-13-05-01 | Denial of Service | `espflash erase-flash` | mitigate | Run only after current factory image recovery path exists; restore through `just flash` and capture post-restore monitor evidence per D-13 and D-14. |
| T-13-05-02 | Tampering | factory image recovery | mitigate | Use factory image from Plan 01 manifest and record checksum/path before recovery commands. |
| T-13-05-03 | Repudiation | interrupted update evidence | mitigate | Record route, checksum, interruption point, public/internal status, post-action state, recovery steps, and conclusion per D-15. |
| T-13-05-04 | Information Disclosure | recovery logs | mitigate | Include erase, flash, monitor, HTTP, and interrupted-upload artifacts in redaction review before commit. |
| T-13-05-05 | Elevation of Privilege | OTAWWW claim | mitigate | Preserve OTAWWW as fail-closed REL-03 gap with `Wrong API input` unless exact whole-`www` interrupted-update evidence exists per D-11. |
</threat_model>

<verification>
Run after tasks:

- `bash -n scripts/phase13-recovery-regression.sh`
- `bazel test //scripts:phase13_recovery_regression_test`
- `just parity`
- `git diff -- reference/esp-miner --exit-code`
- Full Rust pre-commit sequence before commit.
</verification>

<success_criteria>
REL-08 evidence is captured only with a documented recovery path and exact commands, or missing prerequisites are recorded as pending/blocking evidence without widening scope or overclaiming release parity.
</success_criteria>

<output>
After completion, create `.planning/phases/13-final-ultra-205-release-evidence/13-05-SUMMARY.md`.
</output>
