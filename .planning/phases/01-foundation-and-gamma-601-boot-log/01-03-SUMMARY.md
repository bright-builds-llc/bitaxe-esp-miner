---
phase: 01-foundation-and-gamma-601-boot-log
plan: "03"
subsystem: foundation
tags: [rust, cargo, bazel, pure-crates, safe-state, gamma-601]
requires:
  - "01-02 Cargo and Bazel Rust foundation"
provides:
  - "Pure bitaxe-core crate with typed Gamma 601, BM1370, and Phase 1 safe-state contracts"
  - "Pure bitaxe-config crate with identity-only Gamma 601 board selection"
  - "Pure bitaxe-test-support crate with exact Phase 1 safe-state assertion helpers"
  - "Cargo.lock generated for the first workspace package members"
affects: [foundation, firmware, config, testing, parity]
tech-stack:
  added: [bitaxe-core, bitaxe-config, bitaxe-test-support]
  patterns:
    - "Pure crates expose Cargo packages and Bazel rust_test targets"
    - "Gamma 601 and BM1370 identity flows through shared bitaxe-core domain types"
    - "Phase 1 safe-state helpers depend only on bitaxe-core"
key-files:
  created:
    - Cargo.lock
    - crates/bitaxe-core/Cargo.toml
    - crates/bitaxe-core/BUILD.bazel
    - crates/bitaxe-core/src/lib.rs
    - crates/bitaxe-config/Cargo.toml
    - crates/bitaxe-config/BUILD.bazel
    - crates/bitaxe-config/src/lib.rs
    - crates/bitaxe-test-support/Cargo.toml
    - crates/bitaxe-test-support/BUILD.bazel
    - crates/bitaxe-test-support/src/lib.rs
  modified:
    - .gitignore
    - Cargo.toml
key-decisions:
  - "Represent Gamma 601, BM1370, and Phase 1 disabled mining/work/hardware-control state as pure domain values in bitaxe-core."
  - "Keep Phase1BoardSelection identity-only; NVS, Wi-Fi, mining, ASIC behavior, voltage, fan, thermal, and power stay outside the pure config crate."
  - "Honor the AGENTS.md Rust pre-commit rule by recording TDD RED failures without committing failing intermediate states."
patterns-established:
  - "Each pure crate has a local Cargo package plus a matching //crates/...:tests Bazel target."
  - "Exact safe-state log line is centralized in bitaxe-core and reused by test-support helpers."
requirements-completed: [FND-04, FND-05]
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 1-2026-06-21T00-30-20
generated_at: 2026-06-21T02:31:34Z
duration: 7 min
completed: 2026-06-21
---

# Phase 01 Plan 03: Foundational Pure Crates Summary

**Gamma 601/BM1370 identity and Phase 1 safe-state contracts in Cargo and Bazel tested pure Rust crates**

## Performance

- **Duration:** 7 min
- **Started:** 2026-06-21T02:24:34Z
- **Completed:** 2026-06-21T02:31:34Z
- **Tasks:** 2
- **Files modified:** 11

## Accomplishments

- Added `bitaxe-core` with typed `BoardTarget::Gamma601`, `AsicTarget::Bm1370`, disabled Phase 1 state enums, and the exact required safe-state log line.
- Added `bitaxe-config` with `Phase1BoardSelection::gamma_601()` wired to the shared core board and ASIC types while staying free of hardware/storage side effects.
- Added `bitaxe-test-support` with helpers that assert the exact Phase 1 safe-state string without depending on ESP-IDF.
- Added Cargo workspace members, generated `Cargo.lock`, and created Bazel test targets for all three pure crates.

## Task Commits

Each task was committed atomically:

1. **Task 1: Create bitaxe-core safe-state contract** - `0b6f30f` (feat)
2. **Task 2: Create config and test-support pure crates** - `d25fc38` (feat)

## Files Created/Modified

- `.gitignore` - Ignores Cargo `target/` build output generated by Rust verification.
- `Cargo.toml` - Adds `bitaxe-core`, `bitaxe-config`, and `bitaxe-test-support` workspace members.
- `Cargo.lock` - Locks the new local workspace package graph.
- `crates/bitaxe-core/Cargo.toml` - Defines the pure `bitaxe-core` package.
- `crates/bitaxe-core/BUILD.bazel` - Adds Bazel library and test targets for `bitaxe-core`.
- `crates/bitaxe-core/src/lib.rs` - Defines board, ASIC, safe-state domain types, exact log line, and focused unit tests.
- `crates/bitaxe-config/Cargo.toml` - Defines the pure `bitaxe-config` package with a path dependency on `bitaxe-core`.
- `crates/bitaxe-config/BUILD.bazel` - Adds Bazel library and test targets for `bitaxe-config`.
- `crates/bitaxe-config/src/lib.rs` - Defines the identity-only Gamma 601 board selection contract and tests.
- `crates/bitaxe-test-support/Cargo.toml` - Defines the pure `bitaxe-test-support` package with a path dependency on `bitaxe-core`.
- `crates/bitaxe-test-support/BUILD.bazel` - Adds Bazel library and test targets for `bitaxe-test-support`.
- `crates/bitaxe-test-support/src/lib.rs` - Defines Phase 1 safe-state assertion helpers and tests.

## Decisions Made

- The first pure core model only represents allowed Phase 1 states, so mining, ASIC work submission, and hardware control all default to disabled.
- `Phase1BoardSelection` deliberately carries identity only. Mutable settings, persistence, Wi-Fi, mining, ASIC behavior, voltage, fan, thermal, and power remain later-phase concerns.
- TDD RED failures were captured by test runs, but not committed as failing revisions because the repo-level Rust commit rule requires format, Clippy, build, and tests to pass before every commit.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Preserved Rust passing-commit rule during TDD**
- **Found during:** Task 1 and Task 2
- **Issue:** The plan's `tdd="true"` flow called for committing failing RED tests, but `AGENTS.md` requires `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features` to pass before any Rust-project commit.
- **Fix:** Wrote RED tests and ran them to confirm the expected unresolved API failures, then committed only the passing task states after GREEN implementation and verification.
- **Files modified:** None beyond the planned task files.
- **Verification:** RED failures were observed for unresolved `bitaxe-core` types in Task 1 and unresolved `Phase1BoardSelection` / test-support helpers in Task 2; both task commits passed the Rust pre-commit sequence before commit.
- **Committed in:** `0b6f30f`, `d25fc38`

**2. [Rule 3 - Blocking] Tracked intentional Cargo outputs and ignored generated build output**
- **Found during:** Task 1
- **Issue:** Running Cargo verification after the first package member existed generated `Cargo.lock` and untracked `target/` output. The task cleanup protocol forbids leaving generated files untracked.
- **Fix:** Tracked `Cargo.lock` as the workspace dependency lock and added `target/` to `.gitignore`.
- **Files modified:** `.gitignore`, `Cargo.lock`
- **Verification:** `git status --short` after Task 1 showed only the pre-existing `.planning/config.json` change.
- **Committed in:** `0b6f30f`

---

**Total deviations:** 2 auto-fixed (1 missing critical, 1 blocking)
**Impact on plan:** The deviations preserved repo-level commit guarantees and kept generated Rust build output out of git. No product scope was added.

## Issues Encountered

None beyond the deviations documented above.

## User Setup Required

None - no external service configuration required.

## Verification

Passed:

- Lifecycle validation: `node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs verify lifecycle 01 --require-plans --raw`
- `cargo test -p bitaxe-core`
- `bazel test //crates/bitaxe-core:tests`
- `cargo metadata --format-version=1 --no-deps`
- `cargo test -p bitaxe-config -p bitaxe-test-support`
- `bazel test //crates/bitaxe-config:tests //crates/bitaxe-test-support:tests`
- `cargo fmt --all`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo build --all-targets --all-features`
- `cargo test --all-features`
- `cargo test -p bitaxe-core -p bitaxe-config -p bitaxe-test-support`
- `bazel test //crates/bitaxe-core:tests //crates/bitaxe-config:tests //crates/bitaxe-test-support:tests`

Acceptance checks passed:

- `grep -R "pub enum BoardTarget" crates/bitaxe-core/src/lib.rs`
- `grep -R "Gamma601" crates/bitaxe-core/src/lib.rs`
- `grep -R "safe_state: mining=disabled asic_work_submission=disabled hardware_control=disabled" crates/bitaxe-core/src/lib.rs`
- `grep -R "esp-idf" crates/bitaxe-core/Cargo.toml` returned no matches.
- `grep -R "Phase1BoardSelection" crates/bitaxe-config/src/lib.rs`
- `grep -R "BoardTarget::Gamma601" crates/bitaxe-config/src/lib.rs`
- `grep -R "AsicTarget::Bm1370" crates/bitaxe-config/src/lib.rs`
- `grep -R "safe_state: mining=disabled asic_work_submission=disabled hardware_control=disabled" crates/bitaxe-test-support/src/lib.rs`
- `grep -R "esp-idf" crates/bitaxe-config/Cargo.toml crates/bitaxe-test-support/Cargo.toml` returned no matches.

## Known Stubs

None.

## Threat Flags

None - the new pure crate boundaries, board/ASIC identity, and safe-state contract were covered by the plan threat model.

## Next Phase Readiness

Ready for `01-04-PLAN.md`. The pure core, config, and test-support contracts are now available for later crate skeletons and the safe Gamma 601 boot/log firmware path.

*Phase: 01-foundation-and-gamma-601-boot-log*
*Completed: 2026-06-21*

## Self-Check: PASSED

- Verified created summary and key crate files exist on disk.
- Verified task commits exist: `0b6f30f`, `d25fc38`.
