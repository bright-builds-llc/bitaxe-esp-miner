---
phase: 01-foundation-and-gamma-601-boot-log
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - .gitmodules
  - reference/esp-miner
  - .bazelversion
  - MODULE.bazel
  - BUILD.bazel
  - scripts/verify-reference-clean.sh
  - scripts/verify-reference-clean-test.sh
  - scripts/BUILD.bazel
autonomous: true
requirements:
  - FND-01
  - FND-02
  - FND-03
  - FND-10
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 1-2026-06-21T00-30-20
generated_at: "2026-06-21T00:55:00Z"
must_haves:
  truths:
    - "Developer can prove reference/esp-miner is present, pinned, initialized, and clean."
    - "Developer can run a Bazel-visible reference guard before trusting parity or package evidence."
    - "No normal Phase 1 workflow modifies files inside reference/esp-miner."
  artifacts:
    - path: ".gitmodules"
      provides: "Submodule path and upstream URL for reference/esp-miner"
    - path: "reference/esp-miner"
      provides: "Pinned upstream ESP-Miner submodule at c1915b0a63bfabebdb95a515cedfee05146c1d50"
    - path: "scripts/verify-reference-clean.sh"
      provides: "Fail-fast reference cleanliness guard"
    - path: "scripts/BUILD.bazel"
      provides: "Bazel-visible verify_reference_clean target"
  key_links:
    - from: "scripts/verify-reference-clean.sh"
      to: "reference/esp-miner"
      via: "git submodule status, rev-parse, and status --porcelain checks"
      pattern: "reference/esp-miner"
    - from: "scripts/BUILD.bazel"
      to: "scripts/verify-reference-clean.sh"
      via: "sh_binary target named verify_reference_clean"
      pattern: "verify_reference_clean"
---

<objective>
Create the Phase 1 reference trust root and minimal Bazel graph.

Purpose: Later package, parity, and fixture work is meaningless unless the upstream reference tree is exact, clean, and reachable through the canonical graph.
Output: Pinned `reference/esp-miner` submodule, reference guard script, and Bazel targets for the guard.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-RESEARCH.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-VALIDATION.md
@docs/project/seed-layout.md
@docs/project/first-milestone.md
@docs/parity/checklist.md
@PROVENANCE.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/verification.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the upstream ESP-Miner reference submodule</name>
  <read_first>
    - .gitmodules if it exists
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-RESEARCH.md
    - docs/project/seed-layout.md
    - PROVENANCE.md
  </read_first>
  <files>.gitmodules, reference/esp-miner</files>
  <action>
    Add `https://github.com/bitaxeorg/ESP-Miner.git` as the git submodule at `reference/esp-miner` per D-01 and FND-01. Pin it to exact commit `c1915b0a63bfabebdb95a515cedfee05146c1d50`; do not edit any file under `reference/esp-miner`. Ensure `.gitmodules` contains `path = reference/esp-miner` and `url = https://github.com/bitaxeorg/ESP-Miner.git`. If the submodule directory already exists, inspect it instead of deleting it, then converge it to the exact commit with a clean worktree.
  </action>
  <verify>
    <automated>git submodule status --recursive reference/esp-miner && test "$(git -C reference/esp-miner rev-parse HEAD)" = "c1915b0a63bfabebdb95a515cedfee05146c1d50" && test -z "$(git -C reference/esp-miner status --porcelain --untracked-files=all)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "path = reference/esp-miner" .gitmodules` succeeds.
    - `grep -q "url = https://github.com/bitaxeorg/ESP-Miner.git" .gitmodules` succeeds.
    - `git -C reference/esp-miner rev-parse HEAD` prints `c1915b0a63bfabebdb95a515cedfee05146c1d50`.
    - `git -C reference/esp-miner status --porcelain --untracked-files=all` prints nothing.
  </acceptance_criteria>
  <done>The parent repo records the submodule pointer and the reference tree is clean at the accepted upstream commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the Bazel-visible reference guard</name>
  <read_first>
    - scripts/verify-reference-clean.sh if it exists
    - scripts/BUILD.bazel if it exists
    - .bazelversion if it exists
    - MODULE.bazel if it exists
    - BUILD.bazel if it exists
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-VALIDATION.md
    - docs/project/seed-layout.md
    - standards/core/code-shape.md
    - standards/core/verification.md
  </read_first>
  <files>.bazelversion, MODULE.bazel, BUILD.bazel, scripts/verify-reference-clean.sh, scripts/verify-reference-clean-test.sh, scripts/BUILD.bazel</files>
  <behavior>
    - Missing `reference/esp-miner` fails with a message containing `reference missing or not initialized`.
    - A dirty reference tree fails with a message containing `reference dirty`.
    - A clean tree at `c1915b0a63bfabebdb95a515cedfee05146c1d50` prints `reference clean: c1915b0a63bfabebdb95a515cedfee05146c1d50`.
    - A submodule at any other commit fails with a message containing `reference commit mismatch`.
  </behavior>
  <action>
    Create `.bazelversion` with exactly `9.1.1`. Create `MODULE.bazel` with module name `bitaxe_esp_miner`, version `0.1.0`, and a `bazel_dep(name = "rules_rust", version = "0.70.0")` entry per D-04; later plans may extend it but must not replace Bzlmod with WORKSPACE-first setup. Create root `BUILD.bazel` as the minimal package anchor. Add `scripts/verify-reference-clean.sh` with `#!/usr/bin/env bash`, `set -euo pipefail`, `EXPECTED_REFERENCE_COMMIT=${EXPECTED_REFERENCE_COMMIT:-c1915b0a63bfabebdb95a515cedfee05146c1d50}`, and `reference_dir=${REFERENCE_DIR:-reference/esp-miner}`. The script must check parent `git submodule status --recursive "$reference_dir"` unless `SKIP_PARENT_SUBMODULE_CHECK=1`, reject status prefixes `-`, `+`, or `U`, compare `git -C "$reference_dir" rev-parse HEAD` to the expected commit, reject non-empty `git -C "$reference_dir" status --porcelain --untracked-files=all`, and print the pinned commit on success per D-02 and D-03. Add `scripts/verify-reference-clean-test.sh` with temporary git repositories covering the behavior cases above. Add `scripts/BUILD.bazel` with public `sh_binary(name = "verify_reference_clean", srcs = ["verify-reference-clean.sh"])` and `sh_test(name = "verify_reference_clean_test", srcs = ["verify-reference-clean-test.sh"], data = ["verify-reference-clean.sh"], tags = ["local"])`.
  </action>
  <verify>
    <automated>bash scripts/verify-reference-clean-test.sh && bazel test //scripts:verify_reference_clean_test && bazel run //scripts:verify_reference_clean</automated>
  </verify>
  <acceptance_criteria>
    - `cat .bazelversion` prints exactly `9.1.1`.
    - `grep -q 'bazel_dep(name = "rules_rust", version = "0.70.0")' MODULE.bazel` succeeds.
    - `grep -q 'EXPECTED_REFERENCE_COMMIT=.*c1915b0a63bfabebdb95a515cedfee05146c1d50' scripts/verify-reference-clean.sh` succeeds.
    - `grep -q 'status --porcelain --untracked-files=all' scripts/verify-reference-clean.sh` succeeds.
    - `grep -q 'name = "verify_reference_clean"' scripts/BUILD.bazel` succeeds.
  </acceptance_criteria>
  <done>`bazel run //scripts:verify_reference_clean` fails closed for untrusted reference state and passes only for the pinned clean submodule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent repo -> submodule | The parent repo trusts `reference/esp-miner` as read-only behavioral evidence. |
| developer shell -> scripts | Local shell execution can be influenced by environment variables and current worktree state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering/Repudiation | `reference/esp-miner` | mitigate | Pin commit `c1915b0a63bfabebdb95a515cedfee05146c1d50`; fail on missing, uninitialized, dirty, or mismatched submodule state. |
| T-01-02 | Tampering/Elevation of Privilege | Shell command arguments | accept | This plan adds no user-supplied `port`, image, or path execution surface; later flash plan must use typed args and `Command` vectors. |
| T-01-03 | Repudiation | Package provenance | transfer | Reference commit output is created here; package manifest enforcement is handled in Plan 04. |
| T-01-04 | Spoofing/Repudiation | Parity evidence | mitigate | Guard output prints the exact reference commit and does not update checklist rows to `verified`. |
| T-01-05 | Safety | Firmware boot logs | transfer | No firmware runtime is created in this plan; safe boot log wording is enforced in Plan 03. |
</threat_model>

<verification>
Run `git submodule status --recursive reference/esp-miner`, `bash scripts/verify-reference-clean-test.sh`, `bazel test //scripts:verify_reference_clean_test`, and `bazel run //scripts:verify_reference_clean`.
</verification>

<success_criteria>
FND-01, FND-02, and the reference-trust portion of FND-03/FND-10 are complete when the submodule is pinned and the guard passes only for a clean reference tree.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-gamma-601-boot-log/01-01-SUMMARY.md`.
</output>
