---
phase: 01-foundation-and-gamma-601-boot-log
plan: "05"
type: execute
wave: 4
depends_on:
  - "04"
files_modified:
  - Cargo.toml
  - Cargo.lock
  - firmware/bitaxe/Cargo.toml
  - firmware/bitaxe/src/main.rs
  - tools/flash/Cargo.toml
  - tools/flash/src/main.rs
  - tools/parity/Cargo.toml
  - tools/parity/src/main.rs
  - tools/xtask/Cargo.toml
  - tools/xtask/src/main.rs
autonomous: true
requirements:
  - FND-03
  - FND-04
  - FND-05
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 1-2026-06-21T00-30-20
generated_at: "2026-06-21T00:55:00Z"
must_haves:
  truths:
    - "Firmware and host tool package names exist in the Cargo workspace before implementation plans replace their entrypoints."
    - "Compile-only entrypoint contracts do not perform flashing, packaging, parity mutation, Wi-Fi, mining, ASIC, voltage, fan, thermal, or power behavior."
    - "The root workspace remains valid after firmware and host tool members are added."
  artifacts:
    - path: "firmware/bitaxe/Cargo.toml"
      provides: "Firmware package contract consumed by Plan 06"
    - path: "tools/flash/Cargo.toml"
      provides: "Flash tool package contract consumed by Plan 08"
    - path: "tools/parity/Cargo.toml"
      provides: "Parity tool package contract consumed by Plan 07"
    - path: "tools/xtask/Cargo.toml"
      provides: "Packaging tool package contract consumed by Plan 08"
  key_links:
    - from: "Cargo.toml"
      to: "firmware/bitaxe/Cargo.toml"
      via: "workspace member"
      pattern: "firmware/bitaxe"
    - from: "Cargo.toml"
      to: "tools/flash/Cargo.toml"
      via: "workspace member"
      pattern: "tools/flash"
    - from: "Cargo.toml"
      to: "tools/parity/Cargo.toml"
      via: "workspace member"
      pattern: "tools/parity"
    - from: "Cargo.toml"
      to: "tools/xtask/Cargo.toml"
      via: "workspace member"
      pattern: "tools/xtask"
---

<objective>
Create compile-only package contracts for the firmware and host tool entrypoints.

Purpose: Downstream firmware, parity, package, and flash implementation plans need stable package names and workspace membership without mixing their implementation scope into pure-crate setup.
Output: Workspace members for `firmware/bitaxe`, `tools/flash`, `tools/parity`, and `tools/xtask` with inert entrypoints ready for replacement by their owning plans.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-RESEARCH.md
@docs/project/seed-layout.md
@docs/parity/checklist.md
@AGENTS.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/verification.md
@standards/languages/rust.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add firmware package entrypoint contract</name>
  <read_first>
    - Cargo.toml
    - firmware/bitaxe/Cargo.toml if it exists
    - firmware/bitaxe/src/main.rs if it exists
    - docs/project/seed-layout.md
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
  </read_first>
  <files>Cargo.toml, firmware/bitaxe/Cargo.toml, firmware/bitaxe/src/main.rs</files>
  <action>
    Add `firmware/bitaxe` to root workspace members and create package `bitaxe-firmware` with workspace edition/license. Create `src/main.rs` as a compile-only entrypoint contract with a source comment stating `Plan 06 replaces this entrypoint with the safe ESP-IDF boot/log firmware`. The entrypoint must return without side effects and must not import ESP-IDF, Wi-Fi, ASIC, Stratum, NVS mutation, HTTP, OTA, voltage, fan, thermal, power, or mining modules. Do not add `firmware/bitaxe/BUILD.bazel`; Plan 06 owns the firmware Bazel target.
  </action>
  <verify>
    <automated>cargo metadata --format-version=1 --no-deps && grep -q 'Plan 06 replaces this entrypoint' firmware/bitaxe/src/main.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'firmware/bitaxe' Cargo.toml` succeeds.
    - `grep -q 'name = "bitaxe-firmware"' firmware/bitaxe/Cargo.toml` succeeds.
    - `grep -q 'Plan 06 replaces this entrypoint' firmware/bitaxe/src/main.rs` succeeds.
    - `rg 'esp_idf|wifi|stratum|asic|voltage|fan|thermal|power|mine' firmware/bitaxe/src/main.rs` returns no active runtime behavior.
  </acceptance_criteria>
  <done>The firmware package exists in the workspace and remains effect-free until Plan 06 implements the safe boot/log firmware.</done>
</task>

<task type="auto">
  <name>Task 2: Add host tool package entrypoint contracts</name>
  <read_first>
    - Cargo.toml
    - Cargo.lock
    - tools/flash/Cargo.toml if it exists
    - tools/parity/Cargo.toml if it exists
    - tools/xtask/Cargo.toml if it exists
    - docs/project/seed-layout.md
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
  </read_first>
  <files>Cargo.toml, Cargo.lock, tools/flash/Cargo.toml, tools/flash/src/main.rs, tools/parity/Cargo.toml, tools/parity/src/main.rs, tools/xtask/Cargo.toml, tools/xtask/src/main.rs</files>
  <action>
    Add `tools/flash`, `tools/parity`, and `tools/xtask` to root workspace members. Create packages `bitaxe-flash`, `bitaxe-parity`, and `xtask` with workspace edition/license. Each `src/main.rs` must be a compile-only entrypoint contract with a source comment naming its owning implementation plan: Plan 08 for `tools/flash`, Plan 07 for `tools/parity`, and Plan 08 for `tools/xtask`. These entrypoints must not run `cargo`, `bazel`, `espflash`, shell commands, package generation, parity mutation, flashing, monitoring, Wi-Fi, mining, ASIC, voltage, fan, thermal, or power behavior. Run `cargo generate-lockfile` after the new members exist.
  </action>
  <verify>
    <automated>cargo metadata --format-version=1 --no-deps && cargo check -p bitaxe-firmware -p bitaxe-flash -p bitaxe-parity -p xtask</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'tools/flash' Cargo.toml` succeeds.
    - `grep -q 'tools/parity' Cargo.toml` succeeds.
    - `grep -q 'tools/xtask' Cargo.toml` succeeds.
    - `grep -q 'Plan 08' tools/flash/src/main.rs` succeeds.
    - `grep -q 'Plan 07' tools/parity/src/main.rs` succeeds.
    - `grep -q 'Plan 08' tools/xtask/src/main.rs` succeeds.
    - `rg 'Command::new|espflash|bazel|cargo|package|flash|monitor|verified|wifi|mine|asic|voltage|fan|thermal|power' tools/flash/src tools/parity/src tools/xtask/src` returns no active runtime behavior.
  </acceptance_criteria>
  <done>Host tool packages exist in the workspace, compile, and remain effect-free until their owning implementation plans replace them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package entrypoints -> future implementations | Package names and paths are created before implementation plans add side effects. |
| host tools -> shell/process execution | Phase 1 contracts must not execute external commands until typed tool implementations exist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering/Elevation of Privilege | Host tool entrypoints | mitigate | Entry contracts perform no command execution; Plan 08 owns typed flash/package process execution. |
| T-05-02 | Spoofing/Repudiation | Parity entrypoint | mitigate | Entry contract performs no parity mutation or verification claim; Plan 07 owns reporting semantics. |
| T-05-03 | Safety | Firmware entrypoint | mitigate | Entry contract performs no ESP-IDF, mining, ASIC, voltage, fan, thermal, or power behavior. |
| T-05-04 | Repudiation | Workspace membership | mitigate | Cargo metadata/check verifies all package names and workspace members resolve before downstream implementation. |
</threat_model>

<verification>
Run `cargo metadata --format-version=1 --no-deps` and `cargo check -p bitaxe-firmware -p bitaxe-flash -p bitaxe-parity -p xtask`.
</verification>

<success_criteria>
FND-03/FND-04/FND-05 package-contract coverage is complete when firmware and host tool workspace members exist, compile, and remain side-effect-free until their owning plans implement behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-gamma-601-boot-log/01-05-SUMMARY.md`.
</output>
