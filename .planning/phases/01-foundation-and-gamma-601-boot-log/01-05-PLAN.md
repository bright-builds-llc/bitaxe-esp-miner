---
phase: 01-foundation-and-gamma-601-boot-log
plan: "05"
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - tools/parity/Cargo.toml
  - tools/parity/BUILD.bazel
  - tools/parity/src/main.rs
  - docs/parity/checklist.md
autonomous: true
requirements:
  - FND-03
  - FND-10
  - FND-11
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 1-2026-06-21T00-30-20
generated_at: "2026-06-21T00:55:00Z"
must_haves:
  truths:
    - "Developer can run a parity report that lists checklist statuses, evidence gaps, implementation pointers, and reference breadcrumbs."
    - "The report does not treat implementation status as verification."
    - "Safety-critical rows cannot be reported as verified without hardware-smoke or hardware-regression evidence."
  artifacts:
    - path: "tools/parity/src/main.rs"
      provides: "Parity checklist report CLI"
    - path: "tools/parity/BUILD.bazel"
      provides: "Bazel-visible parity report target"
    - path: "docs/parity/checklist.md"
      provides: "Evidence ledger with Phase 1 implementation pointers"
  key_links:
    - from: "tools/parity/src/main.rs"
      to: "docs/parity/checklist.md"
      via: "Markdown checklist table parser"
      pattern: "docs/parity/checklist.md"
    - from: "tools/parity/src/main.rs"
      to: "reference/esp-miner"
      via: "reference commit lookup"
      pattern: "reference_commit"
---

<objective>
Wire Phase 1 parity and provenance reporting.

Purpose: The project needs an evidence ledger report that distinguishes implementation, missing evidence, and verified parity before later phases add hardware behavior.
Output: `tools/parity` CLI, Bazel target, and updated checklist implementation pointers for Phase 1 surfaces.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-RESEARCH.md
@docs/project/seed-layout.md
@docs/parity/checklist.md
@PROVENANCE.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement parity report CLI</name>
  <read_first>
    - tools/parity/Cargo.toml
    - tools/parity/src/main.rs
    - docs/parity/checklist.md
    - PROVENANCE.md
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-RESEARCH.md
  </read_first>
  <files>tools/parity/Cargo.toml, tools/parity/BUILD.bazel, tools/parity/src/main.rs</files>
  <behavior>
    - `tools/parity report --checklist docs/parity/checklist.md` prints each row ID, status, evidence, breadcrumb, and Rust-owned target.
    - `tools/parity report --format json` emits valid JSON with `reference_commit`.
    - A row with `Status` = `verified` and `Evidence` = `pending` fails.
    - Safety-critical rows containing voltage, fan, thermal, power, or ASIC initialization fail if `verified` evidence is not `hardware-smoke` or `hardware-regression`.
  </behavior>
  <action>
    Replace the Plan 02 `tools/parity` package contract with a real CLI using `clap`, `serde`, `serde_json`, `camino`, and `anyhow` from workspace dependencies per D-14 and D-16. Add subcommand `report` with flags `--checklist docs/parity/checklist.md`, `--format text|json`, and `--fail-on-invalid-verified`. Parse the local Markdown tables in `docs/parity/checklist.md` into typed rows with fields `id`, `surface`, `reference_breadcrumb`, `rust_owned_target`, `status`, `evidence`, and `notes`. Read `git -C reference/esp-miner rev-parse HEAD` and include `reference_commit`, using `Unavailable` only when the reference is absent and returning a nonzero exit when `--fail-on-invalid-verified` is set. Add validation that `verified` rows need non-pending evidence and that safety-critical surfaces `voltage`, `fan`, `thermal`, `power`, and `ASIC initialization` require `hardware-smoke` or `hardware-regression`. Add unit tests with Arrange/Act/Assert for parsing, JSON output, invalid verified rows, and safety-critical evidence rejection. Add `tools/parity/BUILD.bazel` with public `rust_binary(name = "report")` and `rust_test(name = "tests")`.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-parity && bazel test //tools/parity:tests && bazel run //tools/parity:report -- report --checklist docs/parity/checklist.md --fail-on-invalid-verified</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'fail-on-invalid-verified' tools/parity/src/main.rs` succeeds.
    - `grep -q 'hardware-smoke' tools/parity/src/main.rs` succeeds.
    - `grep -q 'hardware-regression' tools/parity/src/main.rs` succeeds.
    - `grep -q 'reference_commit' tools/parity/src/main.rs` succeeds.
    - `grep -q 'name = "report"' tools/parity/BUILD.bazel` succeeds.
  </acceptance_criteria>
  <done>`bazel run //tools/parity:report -- report --checklist docs/parity/checklist.md --fail-on-invalid-verified` reports evidence status and fails on false verified claims.</done>
</task>

<task type="auto">
  <name>Task 2: Update Phase 1 checklist pointers without false verification</name>
  <read_first>
    - docs/parity/checklist.md
    - PROVENANCE.md
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
    - .planning/REQUIREMENTS.md
    - docs/project/first-milestone.md
  </read_first>
  <files>docs/parity/checklist.md</files>
  <action>
    Update only Phase 1-relevant checklist rows in `docs/parity/checklist.md`: WF-001 through WF-005 and SYS-001 through SYS-004. Add or refine Rust-owned targets so they point to `scripts/verify-reference-clean.sh`, `MODULE.bazel`, `Justfile`, `//firmware/bitaxe:firmware_image`, `tools/flash`, `firmware/bitaxe`, `crates/bitaxe-core`, and `tools/parity` where applicable. Set rows implemented by this phase to `implemented` only after their code exists; leave `Evidence` as `pending` until command or hardware evidence is recorded. Do not set SYS-001, SYS-002, ASIC, voltage, fan, thermal, power, or mining rows to `verified` in Phase 1 unless actual Gamma 601 hardware-smoke evidence exists per D-15. Keep `reference/esp-miner` breadcrumbs intact and do not copy upstream source expression into the checklist.
  </action>
  <verify>
    <automated>bazel run //tools/parity:report -- report --checklist docs/parity/checklist.md --fail-on-invalid-verified && grep -q 'tools/parity' docs/parity/checklist.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'scripts/verify-reference-clean.sh' docs/parity/checklist.md` succeeds.
    - `grep -q '//firmware/bitaxe:firmware_image' docs/parity/checklist.md` succeeds.
    - `grep -q 'tools/flash' docs/parity/checklist.md` succeeds.
    - `grep -q 'tools/parity' docs/parity/checklist.md` succeeds.
    - No Phase 1 safety-critical hardware-control row is changed to `verified` with `pending` evidence.
  </acceptance_criteria>
  <done>The checklist names Phase 1 implementation pointers while preserving evidence-backed `verified` semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checklist markdown -> parity report | Local evidence ledger data is parsed and surfaced as report output. |
| reference repo -> report provenance | Reference commit is included in reports as comparison baseline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering/Repudiation | `reference/esp-miner` | mitigate | Report includes reference commit and fails closed with `--fail-on-invalid-verified` when reference provenance is unavailable. |
| T-05-02 | Tampering/Elevation of Privilege | CLI args | mitigate | Parse checklist path and format through `clap`/`camino`; this plan does not execute user-supplied paths as commands. |
| T-05-03 | Repudiation | Provenance reporting | mitigate | Report includes reference commit, row breadcrumbs, Rust targets, statuses, evidence, and missing evidence. |
| T-05-04 | Spoofing/Repudiation | False parity claims | mitigate | `verified` with pending evidence fails; safety-critical verified rows require hardware evidence types. |
| T-05-05 | Safety | Hardware-control parity | mitigate | Phase 1 checklist rows keep ASIC init, voltage, fan, thermal, power, and mining below `verified` absent hardware evidence. |
</threat_model>

<verification>
Run `cargo test -p bitaxe-parity`, `bazel test //tools/parity:tests`, and `bazel run //tools/parity:report -- report --checklist docs/parity/checklist.md --fail-on-invalid-verified`.
</verification>

<success_criteria>
FND-10 and FND-11 are complete when parity tooling reports status/evidence/breadcrumbs and rejects false verified claims.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-gamma-601-boot-log/01-05-SUMMARY.md`.
</output>
