---
phase: 01-foundation-and-gamma-601-boot-log
plan: "08"
type: execute
wave: 6
depends_on:
  - "06"
files_modified:
  - firmware/bitaxe/BUILD.bazel
  - scripts/package-firmware.sh
  - tools/xtask/Cargo.toml
  - tools/xtask/BUILD.bazel
  - tools/xtask/src/main.rs
  - tools/flash/Cargo.toml
  - tools/flash/BUILD.bazel
  - tools/flash/src/main.rs
autonomous: true
requirements:
  - FND-03
  - FND-08
  - FND-09
  - FND-10
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 1-2026-06-21T00-30-20
generated_at: "2026-06-21T00:55:00Z"
must_haves:
  truths:
    - "Developer can run the package target and receive a machine-readable manifest with image, checksum, tool, firmware, reference, and default flash input metadata."
    - "The package target declares and produces `bitaxe-gamma601.elf` as the default flash input consumed by `tools/flash` when no image override is provided."
    - "The package target runs the reference cleanliness guard before manifest generation or trusted package evidence output."
    - "Developer can run a flash dry-run for board 601 and see the exact espflash command before execution."
    - "Port and image/path arguments are parsed as typed CLI values and passed to processes without shell concatenation."
  artifacts:
    - path: "tools/xtask/src/main.rs"
      provides: "Package manifest generator"
    - path: "tools/flash/src/main.rs"
      provides: "Typed flash, monitor, and flash-monitor CLI"
    - path: "scripts/package-firmware.sh"
      provides: "Bazel-visible package wrapper producing declared flash artifacts"
    - path: "firmware/bitaxe/BUILD.bazel"
      provides: "Public firmware_image target with manifest and default flash image outputs"
  key_links:
    - from: "firmware/bitaxe/BUILD.bazel"
      to: "tools/xtask"
      via: "firmware_image packaging action"
      pattern: "firmware_image"
    - from: "firmware/bitaxe/BUILD.bazel"
      to: "//scripts:verify_reference_clean"
      via: "guard dependency or tool executed before package manifest generation"
      pattern: "verify_reference_clean"
    - from: "tools/xtask/src/main.rs"
      to: "tools/flash/src/main.rs"
      via: "manifest field `default_flash_image` pointing to `bitaxe-gamma601.elf`"
      pattern: "default_flash_image"
    - from: "tools/flash/src/main.rs"
      to: "espflash"
      via: "std::process::Command argument vector"
      pattern: "Command::new"
---

<objective>
Create package manifest generation and USB flash/monitor tooling.

Purpose: Phase 1 must produce auditable image metadata and a safe Gamma 601 USB workflow before users flash hardware.
Output: `//firmware/bitaxe:firmware_image`, `tools/xtask` manifest generation, and `tools/flash` CLI.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-RESEARCH.md
@docs/project/seed-layout.md
@docs/project/first-milestone.md
@docs/parity/checklist.md
@PROVENANCE.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/languages/rust.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate firmware image manifest</name>
  <read_first>
    - tools/xtask/Cargo.toml
    - tools/xtask/src/main.rs
    - firmware/bitaxe/BUILD.bazel
    - scripts/package-firmware.sh if it exists
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-RESEARCH.md
    - PROVENANCE.md
  </read_first>
  <files>tools/xtask/Cargo.toml, tools/xtask/BUILD.bazel, tools/xtask/src/main.rs, scripts/package-firmware.sh, firmware/bitaxe/BUILD.bazel</files>
  <behavior>
    - Manifest JSON contains `schema_version`, `board`, `device_model`, `asic`, `firmware_commit`, `reference_commit`, `esp_idf_version`, `rust_target`, `tool_versions`, and `artifacts`.
    - Manifest records `reference_commit` as `c1915b0a63bfabebdb95a515cedfee05146c1d50`.
    - Manifest top-level `default_flash_image` points to `bitaxe-gamma601.elf`, not `bitaxe-gamma601-factory.bin`.
    - Manifest may include `bitaxe-gamma601-factory.bin` as an additional package artifact with offset `0x0`.
    - Every artifact entry contains `kind`, `path`, `offset`, and `sha256`; unavailable values are the literal string `Unavailable`.
    - `//firmware/bitaxe:firmware_image` runs `//scripts:verify_reference_clean` before manifest generation; missing, dirty, or unpinned reference state blocks package evidence and leaves no trusted manifest.
    - Package generation uses SHA-256 from `sha2`, not a custom hash implementation.
  </behavior>
  <action>
    Replace the Plan 05 `tools/xtask` package entrypoint contract with a real CLI containing subcommand `package-firmware` per D-13. Use `clap`, `serde`, `serde_json`, `sha2`, `camino`, and `anyhow` from workspace dependencies. The subcommand must accept `--board 601`, `--firmware-elf <path>`, `--default-flash-image <path>`, `--out-dir <path>`, `--manifest <path>`, and optional `--factory-image <path>`. It must reject any board except `601`. It must read `git rev-parse HEAD` for `firmware_commit`, read `git -C reference/esp-miner rev-parse HEAD` for `reference_commit` only after the package wrapper or Bazel target has run `//scripts:verify_reference_clean`, and read tool versions for `cargo`, `rustc`, `bazel`, and `espflash`, using `Unavailable` when a non-critical version command is absent. It must write a deterministic JSON manifest with `schema_version = 1`, `board = "601"`, `device_model = "Gamma 601"`, `asic = "BM1370"`, `esp_idf_version = "v5.5.4"`, `rust_target = "xtensa-esp32s3-espidf"`, top-level `default_flash_image` pointing to existing declared package output `bitaxe-gamma601.elf`, optional artifact `bitaxe-gamma601-factory.bin` with offset `"0x0"`, and SHA-256 for every existing artifact file. The merged factory `.bin` is not the default input for `espflash flash`; it is an additional package artifact only. Add focused unit tests for manifest serialization, `default_flash_image` validation requiring the default to be `bitaxe-gamma601.elf`, rejection of `bitaxe-gamma601-factory.bin` as the manifest default, unavailable non-critical fields, checksum calculation, board rejection, missing-reference guard failure, and dirty-reference guard failure. Add `scripts/package-firmware.sh` with `#!/usr/bin/env bash`, `set -euo pipefail`, a first-step invocation of the guard provided by `//scripts:verify_reference_clean`, a call to `bazel build //firmware/bitaxe:firmware`, a copy of the produced firmware ELF to the declared package output `bitaxe-gamma601.elf`, an `espflash save-image --chip esp32s3 --merge "$firmware_elf" "$factory_image"` call to produce declared output `bitaxe-gamma601-factory.bin`, and a call to `cargo run -p xtask -- package-firmware --board 601 --firmware-elf "$package_elf" --factory-image "$factory_image" --default-flash-image "$package_elf" --manifest "$manifest" ...`. Support installed `espflash` versions that provide `save-image`; if `espflash save-image` is missing or exits unexpectedly, fail with install/upgrade guidance rather than silently omitting the additional factory artifact. Extend `firmware/bitaxe/BUILD.bazel` with public target `firmware_image` that declares and produces `bitaxe-gamma601.elf`, `bitaxe-gamma601-factory.bin`, and `bitaxe-gamma601-package.json`, and that includes `//scripts:verify_reference_clean` as a tool/dependency so the guard runs before manifest generation.
  </action>
  <verify>
    <automated>cargo test -p xtask && bash -n scripts/package-firmware.sh && bazel test //tools/xtask:tests && bazel query 'deps(//firmware/bitaxe:firmware_image)' | grep '//scripts:verify_reference_clean' && bazel build //firmware/bitaxe:firmware_image && test -f bazel-bin/firmware/bitaxe/bitaxe-gamma601-package.json && grep -q '"default_flash_image".*bitaxe-gamma601.elf' bazel-bin/firmware/bitaxe/bitaxe-gamma601-package.json && grep -q 'bitaxe-gamma601-factory.bin' bazel-bin/firmware/bitaxe/bitaxe-gamma601-package.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'package-firmware' tools/xtask/src/main.rs` succeeds.
    - `grep -q 'schema_version' tools/xtask/src/main.rs` succeeds.
    - `grep -q 'default_flash_image' tools/xtask/src/main.rs` succeeds.
    - `grep -q 'c1915b0a63bfabebdb95a515cedfee05146c1d50' tools/xtask/src/main.rs` succeeds.
    - `grep -q 'sha2' tools/xtask/Cargo.toml` succeeds.
    - `grep -q 'verify_reference_clean' firmware/bitaxe/BUILD.bazel scripts/package-firmware.sh` succeeds.
    - `grep -q 'bitaxe-gamma601.elf' tools/xtask/src/main.rs scripts/package-firmware.sh firmware/bitaxe/BUILD.bazel` succeeds.
    - `grep -q 'name = "firmware_image"' firmware/bitaxe/BUILD.bazel` succeeds.
    - `grep -q 'bitaxe-gamma601-factory.bin' firmware/bitaxe/BUILD.bazel scripts/package-firmware.sh` succeeds.
    - Tests prove missing or dirty `reference/esp-miner` blocks package manifest generation before trusted evidence is written.
  </acceptance_criteria>
  <done>`bazel build //firmware/bitaxe:firmware_image` first verifies the reference guard, then creates declared package artifacts and an auditable JSON manifest whose `default_flash_image` points to `bitaxe-gamma601.elf`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement safe flash and monitor CLI</name>
  <read_first>
    - tools/flash/Cargo.toml
    - tools/flash/src/main.rs
    - firmware/bitaxe/BUILD.bazel
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-RESEARCH.md
    - docs/project/seed-layout.md
    - docs/adr/0011-usb-flashing-ergonomics.md
  </read_first>
  <files>tools/flash/Cargo.toml, tools/flash/BUILD.bazel, tools/flash/src/main.rs</files>
  <behavior>
    - `tools/flash flash --board 601 --dry-run --port /dev/cu.usbmodem101 --image path.elf` prints an `espflash flash` command using the explicit image and does not execute it.
    - `tools/flash flash --board 601 --dry-run --port /dev/cu.usbmodem101` builds guarded `//firmware/bitaxe:firmware_image`, reads the manifest, resolves `default_flash_image` to `bitaxe-gamma601.elf`, prints the manifest path and resolved ELF path, and does not execute flashing.
    - `tools/flash flash board=601 dry-run=true port=/dev/cu.usbmodem101 image=path.elf` is accepted for `just flash board=601 port=...` compatibility.
    - `tools/flash flash --board 205` fails because Phase 1 only supports `board=601`.
    - Missing `--port` runs `espflash list-ports`; zero ports produces a message containing `No serial ports found` and an exact `--port /dev/...` example.
    - Multiple likely ports produce a message containing `Ambiguous serial ports` and one `--port <value>` example per candidate.
    - Process execution uses `std::process::Command` with `.arg(...)` calls, never `sh -c` or concatenated shell strings.
  </behavior>
  <action>
    Replace the Plan 05 `tools/flash` package entrypoint contract with a real `clap` CLI exposing subcommands `flash`, `monitor`, and `flash-monitor`. Use typed `BoardId` accepting only `601`, `camino::Utf8PathBuf` for image, manifest, and evidence paths, and an optional `--dry-run` on all subcommands. Accept both standard flags such as `--board 601 --port /dev/cu.usbmodem101 --image path.elf --dry-run` and Just-compatible key/value aliases such as `board=601 port=/dev/cu.usbmodem101 image=path.elf dry-run=true`. `flash` must build/package first by invoking guarded `bazel build //firmware/bitaxe:firmware_image` unless `--image <path>` or `image=<path>` is provided. When no image override is provided, it must locate the package manifest at `$(bazel info bazel-bin)/firmware/bitaxe/bitaxe-gamma601-package.json` unless `--manifest <path>` or `manifest=<path>` is passed, parse the top-level `default_flash_image`, verify the referenced file exists, verify the manifest default resolves to `bitaxe-gamma601.elf` and not `bitaxe-gamma601-factory.bin`, and use that ELF path as the flash input. It must print the manifest path, resolved default ELF path, and exact `espflash flash --chip esp32s3 --port <port> <elf>` command before executing. `monitor` must print and run `espflash monitor --port <port>`. `flash-monitor` must run flash then monitor with the same parsed port. If `--port` is omitted, call `espflash list-ports`, choose a single likely USB serial port when exactly one exists, fail clearly on zero or multiple likely ports, and include exact `--port` examples. Add `--evidence-dir <path>` and `evidence-dir=<path>` that write command, board, port, firmware commit, reference commit, manifest path, default flash image path, timestamp, and log path when provided. Add `tools/flash/BUILD.bazel` with `rust_binary(name = "flash")` and `rust_test(name = "tests")`; tests must cover board parsing, key/value alias parsing, dry-run command rendering with explicit image, dry-run command rendering from manifest `default_flash_image` pointing to `bitaxe-gamma601.elf`, rejection of a manifest whose default points to `bitaxe-gamma601-factory.bin`, zero-port error text, ambiguous-port error text, and that rendered commands are vectors rather than shell strings.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-flash && bazel test //tools/flash:tests && bazel run //tools/flash:flash -- flash --board 601 --dry-run --port /dev/cu.usbmodem101 --image /tmp/bitaxe-gamma601.elf && bazel run //tools/flash:flash -- flash --board 601 --dry-run --port /dev/cu.usbmodem101</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'enum BoardId' tools/flash/src/main.rs` succeeds.
    - `grep -q 'default_flash_image' tools/flash/src/main.rs` succeeds.
    - `grep -q 'Command::new("espflash")' tools/flash/src/main.rs` succeeds.
    - `grep -q 'bazel build //firmware/bitaxe:firmware_image' tools/flash/src/main.rs` succeeds.
    - `grep -q 'bitaxe-gamma601.elf' tools/flash/src/main.rs` succeeds.
    - `grep -q 'No serial ports found' tools/flash/src/main.rs` succeeds.
    - `grep -q 'Ambiguous serial ports' tools/flash/src/main.rs` succeeds.
    - `! grep -R 'sh -c\\|bash -c' tools/flash/src` succeeds.
  </acceptance_criteria>
  <done>`tools/flash` safely supports Phase 1 flash, monitor, and flash-monitor workflows for Gamma 601 with dry-run and actionable port handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI -> flash tool | User-supplied board, port, image, and evidence paths enter process execution. |
| package artifacts -> manifest | Build outputs become evidence and future flash inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering/Repudiation | `reference/esp-miner` | mitigate | `//firmware/bitaxe:firmware_image` runs `//scripts:verify_reference_clean` before manifest generation; manifests record the pinned reference commit only after the guard passes. |
| T-04-02 | Tampering/Elevation of Privilege | `tools/flash` | mitigate | Parse `port` and image args with `clap`/`camino`, use `Command::new` with `.arg(...)`, and reject `sh -c`/`bash -c`. |
| T-04-03 | Repudiation | Package manifest | mitigate | Manifest includes image paths, offsets, SHA-256, tool versions, firmware commit, reference commit, `default_flash_image = bitaxe-gamma601.elf`, and optional factory `.bin` metadata. |
| T-04-04 | Spoofing/Repudiation | Parity status | mitigate | Package/flash evidence is written separately and does not mark hardware behavior verified. |
| T-04-05 | Safety | Flash workflow | mitigate | Board parser accepts only `601`, prints commands before execution, and requires a resolved port before flashing. |
</threat_model>

<verification>
Run `cargo test -p xtask`, `bazel query 'deps(//firmware/bitaxe:firmware_image)' | grep '//scripts:verify_reference_clean'`, `bazel build //firmware/bitaxe:firmware_image`, confirm the generated manifest contains `default_flash_image` pointing to `bitaxe-gamma601.elf` and records `bitaxe-gamma601-factory.bin` only as an additional artifact, run `cargo test -p bitaxe-flash`, `bazel test //tools/flash:tests`, and dry-run flash both with an explicit ELF image and with the manifest default ELF path.
</verification>

<success_criteria>
FND-08 and FND-09 are complete when guarded package manifests are generated, the manifest exposes `default_flash_image` as `bitaxe-gamma601.elf`, and flash/monitor tooling handles board 601, ports, dry-run, manifest-default ELF resolution, and command printing without shell injection.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-gamma-601-boot-log/01-08-SUMMARY.md`.
</output>
