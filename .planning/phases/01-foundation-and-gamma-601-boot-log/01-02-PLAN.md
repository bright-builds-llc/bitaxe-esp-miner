---
phase: 01-foundation-and-gamma-601-boot-log
plan: "02"
type: execute
wave: 1
depends_on:
  - "01"
files_modified:
  - Cargo.toml
  - Cargo.lock
  - rust-toolchain.toml
  - .cargo/config.toml
  - MODULE.bazel
  - crates/bitaxe-core/Cargo.toml
  - crates/bitaxe-core/BUILD.bazel
  - crates/bitaxe-core/src/lib.rs
  - crates/bitaxe-config/Cargo.toml
  - crates/bitaxe-config/BUILD.bazel
  - crates/bitaxe-config/src/lib.rs
  - crates/bitaxe-asic/Cargo.toml
  - crates/bitaxe-asic/BUILD.bazel
  - crates/bitaxe-asic/src/lib.rs
  - crates/bitaxe-stratum/Cargo.toml
  - crates/bitaxe-stratum/BUILD.bazel
  - crates/bitaxe-stratum/src/lib.rs
  - crates/bitaxe-api/Cargo.toml
  - crates/bitaxe-api/BUILD.bazel
  - crates/bitaxe-api/src/lib.rs
  - crates/bitaxe-test-support/Cargo.toml
  - crates/bitaxe-test-support/BUILD.bazel
  - crates/bitaxe-test-support/src/lib.rs
  - firmware/bitaxe/Cargo.toml
  - firmware/bitaxe/src/main.rs
  - tools/flash/Cargo.toml
  - tools/flash/src/main.rs
  - tools/parity/Cargo.toml
  - tools/parity/src/main.rs
  - tools/xtask/Cargo.toml
  - tools/xtask/src/main.rs
autonomous: true
requirements:
  - FND-03
  - FND-04
  - FND-05
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 1-2026-06-21T00-30-20
generated_at: "2026-06-21T00:55:00Z"
must_haves:
  truths:
    - "Developer can inspect one Cargo workspace that owns Rust dependency versions."
    - "Developer can run pure crate tests through Bazel."
    - "The planned core, config, ASIC, Stratum, API, and test-support crates exist without ESP-IDF side effects."
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace members and pinned workspace dependencies"
    - path: "rust-toolchain.toml"
      provides: "ESP Rust toolchain pin for firmware-capable builds"
    - path: ".cargo/config.toml"
      provides: "ESP32-S3 target linker/env configuration without forcing host tools to Xtensa"
    - path: "crates/bitaxe-core/src/lib.rs"
      provides: "Phase 1 board, ASIC, and safe-state domain contracts"
  key_links:
    - from: "MODULE.bazel"
      to: "Cargo.lock"
      via: "rules_rust crate_universe mirror"
      pattern: "crate_universe"
    - from: "crates/bitaxe-config/src/lib.rs"
      to: "crates/bitaxe-core/src/lib.rs"
      via: "shared BoardTarget and AsicTarget types"
      pattern: "BoardTarget"
---

<objective>
Create the Rust workspace and pure-crate foundation for Phase 1.

Purpose: Firmware and host tools need stable Rust package contracts, while future logic must start in pure crates rather than ESP-IDF adapters.
Output: Root Cargo/Bazel Rust configuration and six planned crates with testable Phase 1 contracts.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-RESEARCH.md
@docs/project/seed-layout.md
@AGENTS.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/languages/rust.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Cargo workspace, ESP toolchain, and Bazel Rust mirror</name>
  <read_first>
    - Cargo.toml if it exists
    - Cargo.lock if it exists
    - rust-toolchain.toml if it exists
    - .cargo/config.toml if it exists
    - MODULE.bazel
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-RESEARCH.md
    - docs/project/seed-layout.md
    - standards/languages/rust.md
  </read_first>
  <files>Cargo.toml, Cargo.lock, rust-toolchain.toml, .cargo/config.toml, MODULE.bazel</files>
  <action>
    Create root `Cargo.toml` with `[workspace] resolver = "2"` and members `crates/bitaxe-core`, `crates/bitaxe-config`, `crates/bitaxe-asic`, `crates/bitaxe-stratum`, `crates/bitaxe-api`, `crates/bitaxe-test-support`, `tools/flash`, `tools/parity`, `tools/xtask`, and `firmware/bitaxe` per D-05 and D-06. Set `[workspace.package] edition = "2021"` and `license = "MIT"`. Add `[workspace.dependencies]` pins: `anyhow = "1.0.102"`, `camino = "1.2.3"`, `clap = { version = "4.6.1", features = ["derive"] }`, `embuild = "0.33.1"`, `esp-idf-svc = "0.52.1"`, `esp-idf-sys = "0.37.2"`, `log = "0.4"`, `serde = { version = "1.0.228", features = ["derive"] }`, `serde_json = "1.0.150"`, `sha2 = "0.11.0"`, `tempfile = "3.27.0"`, and `thiserror = "2.0.18"`. Create `rust-toolchain.toml` with `[toolchain] channel = "esp"` and `components = ["rust-src"]` per D-07. Create `.cargo/config.toml` with `[target.xtensa-esp32s3-espidf] linker = "ldproxy"` and `[env] MCU = "esp32s3"`; do not add a global `[build] target`, because host tools must remain host-targeted unless a firmware script passes `--target xtensa-esp32s3-espidf`. Extend `MODULE.bazel` from Plan 01 with rules_rust `crate_universe` wired to the root Cargo manifests and `Cargo.lock`; Task 2 generates `Cargo.lock` after all workspace members exist.
  </action>
  <verify>
    <automated>grep -q 'resolver = "2"' Cargo.toml && grep -q 'channel = "esp"' rust-toolchain.toml && grep -q 'xtensa-esp32s3-espidf' .cargo/config.toml && grep -q 'crate_universe' MODULE.bazel</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'resolver = "2"' Cargo.toml` succeeds.
    - `grep -q 'esp-idf-svc = "0.52.1"' Cargo.toml` succeeds.
    - `grep -q 'esp-idf-sys = "0.37.2"' Cargo.toml` succeeds.
    - `grep -q 'channel = "esp"' rust-toolchain.toml` succeeds.
    - `grep -q 'linker = "ldproxy"' .cargo/config.toml` succeeds.
    - `.cargo/config.toml` does not contain `[build]` followed by `target = "xtensa-esp32s3-espidf"`.
  </acceptance_criteria>
  <done>The workspace pins the Phase 1 Rust, ESP-IDF, target, and dependency baseline without creating a second Rust dependency truth outside Cargo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create pure crate contracts and tests</name>
  <read_first>
    - crates/bitaxe-core/Cargo.toml if it exists
    - crates/bitaxe-core/src/lib.rs if it exists
    - docs/project/seed-layout.md
    - docs/parity/checklist.md
    - standards/core/architecture.md
    - standards/core/testing.md
    - standards/languages/rust.md
  </read_first>
  <files>crates/bitaxe-core/Cargo.toml, crates/bitaxe-core/BUILD.bazel, crates/bitaxe-core/src/lib.rs, crates/bitaxe-config/Cargo.toml, crates/bitaxe-config/BUILD.bazel, crates/bitaxe-config/src/lib.rs, crates/bitaxe-asic/Cargo.toml, crates/bitaxe-asic/BUILD.bazel, crates/bitaxe-asic/src/lib.rs, crates/bitaxe-stratum/Cargo.toml, crates/bitaxe-stratum/BUILD.bazel, crates/bitaxe-stratum/src/lib.rs, crates/bitaxe-api/Cargo.toml, crates/bitaxe-api/BUILD.bazel, crates/bitaxe-api/src/lib.rs, crates/bitaxe-test-support/Cargo.toml, crates/bitaxe-test-support/BUILD.bazel, crates/bitaxe-test-support/src/lib.rs, firmware/bitaxe/Cargo.toml, firmware/bitaxe/src/main.rs, tools/flash/Cargo.toml, tools/flash/src/main.rs, tools/parity/Cargo.toml, tools/parity/src/main.rs, tools/xtask/Cargo.toml, tools/xtask/src/main.rs</files>
  <behavior>
    - `bitaxe_core::BoardTarget::Gamma601.display_name()` returns `Gamma 601`.
    - `bitaxe_core::AsicTarget::Bm1370.display_name()` returns `BM1370`.
    - `bitaxe_core::Phase1SafeState::default().log_line()` returns `safe_state: mining=disabled asic_work_submission=disabled hardware_control=disabled`.
    - ASIC, Stratum, and API crates expose explicit deferred status values for later phases without active mining, networking, or HTTP behavior.
  </behavior>
  <action>
    Create each planned crate from `docs/project/seed-layout.md` per D-05. In `bitaxe-core`, define `BoardTarget::Gamma601`, `AsicTarget::Bm1370`, `Phase1SafeState`, `MiningState::Disabled`, `AsicWorkSubmissionState::Disabled`, and `HardwareControlState::Disabled`; add focused unit tests with `// Arrange`, `// Act`, `// Assert` comments. In `bitaxe-config`, export a `Phase1BoardSelection` value that uses `BoardTarget::Gamma601` and `AsicTarget::Bm1370` but does not implement NVS or mutable settings. In `bitaxe-asic`, expose `AsicRuntimeStatus::DeferredUntilPhase3`; in `bitaxe-stratum`, expose `StratumRuntimeStatus::DeferredUntilPhase4`; in `bitaxe-api`, expose `ApiRuntimeStatus::DeferredUntilPhase5`; in `bitaxe-test-support`, add small helper functions for asserting Phase 1 safe-state strings. Each crate must have a `BUILD.bazel` target for library and tests using `rules_rust`, and pure crates must not depend on `esp-idf-svc`, `esp-idf-sys`, or hardware adapters. Also create compiling package contract entrypoints for `firmware/bitaxe`, `tools/flash`, `tools/parity`, and `tools/xtask` so the root workspace is valid; those entrypoints must state their owning later plan in a source comment and must not perform flashing, packaging, parity mutation, Wi-Fi, mining, ASIC, voltage, fan, thermal, or power behavior. Run `cargo generate-lockfile` after all workspace members in this task exist.
  </action>
  <verify>
    <automated>cargo metadata --format-version=1 --no-deps && cargo test -p bitaxe-core -p bitaxe-config -p bitaxe-asic -p bitaxe-stratum -p bitaxe-api -p bitaxe-test-support && bazel test //crates/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "pub enum BoardTarget" crates/bitaxe-core/src/lib.rs` succeeds.
    - `grep -R "Gamma601" crates/bitaxe-core/src/lib.rs` succeeds.
    - `grep -R "safe_state: mining=disabled asic_work_submission=disabled hardware_control=disabled" crates/bitaxe-core crates/bitaxe-test-support` succeeds.
    - `grep -R "DeferredUntilPhase3" crates/bitaxe-asic/src/lib.rs` succeeds.
    - `grep -R "esp-idf" crates/bitaxe-*/Cargo.toml` finds no ESP-IDF dependency in pure crates.
  </acceptance_criteria>
  <done>The pure crate layout exists, builds through Cargo and Bazel, and encodes the Phase 1 safe-state contract used by firmware.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace manifests -> Bazel graph | Cargo dependency truth is mirrored into Bazel. |
| pure crates -> firmware adapters | Pure crates must not gain ESP-IDF or hardware side effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering/Repudiation | `reference/esp-miner` | transfer | Plan 01 owns dirty/missing reference enforcement; this plan does not trust reference data directly. |
| T-02-02 | Tampering/Elevation of Privilege | CLI args | accept | No flash or image command execution is added in this plan. |
| T-02-03 | Repudiation | Package manifest provenance | mitigate | Workspace dependencies and target pins are explicit so later manifests can report exact toolchain and crate versions. |
| T-02-04 | Spoofing/Repudiation | Parity status | mitigate | Later-phase ASIC, Stratum, and API crates expose deferred status values and no `verified` claims. |
| T-02-05 | Safety | Safe boot state | mitigate | `Phase1SafeState` makes mining, ASIC work submission, and hardware control disabled in a test-covered pure contract. |
</threat_model>

<verification>
Run `cargo metadata --format-version=1 --no-deps`, `cargo test -p bitaxe-core -p bitaxe-config -p bitaxe-asic -p bitaxe-stratum -p bitaxe-api -p bitaxe-test-support`, and `bazel test //crates/...`.
</verification>

<success_criteria>
FND-04 and FND-05 are complete when the workspace, toolchain pins, and pure crate contracts exist and pass Cargo plus Bazel tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-gamma-601-boot-log/01-02-SUMMARY.md`.
</output>
