---
phase: 01-foundation-and-gamma-601-boot-log
plan: "04"
type: execute
wave: 3
depends_on:
  - "03"
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/bitaxe-asic/Cargo.toml
  - crates/bitaxe-asic/BUILD.bazel
  - crates/bitaxe-asic/src/lib.rs
  - crates/bitaxe-stratum/Cargo.toml
  - crates/bitaxe-stratum/BUILD.bazel
  - crates/bitaxe-stratum/src/lib.rs
  - crates/bitaxe-api/Cargo.toml
  - crates/bitaxe-api/BUILD.bazel
  - crates/bitaxe-api/src/lib.rs
autonomous: true
requirements:
  - FND-05
  - FND-10
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 1-2026-06-21T00-30-20
generated_at: "2026-06-21T00:55:00Z"
must_haves:
  truths:
    - "ASIC, Stratum, and API crates exist as pure Rust packages."
    - "Each deferred Phase 1 surface reports an explicit deferred status instead of active behavior."
    - "No Phase 1 crate starts mining, networking, HTTP serving, ASIC initialization, voltage, fan, thermal, or power behavior."
  artifacts:
    - path: "crates/bitaxe-asic/src/lib.rs"
      provides: "ASIC runtime status contract"
    - path: "crates/bitaxe-stratum/src/lib.rs"
      provides: "Stratum runtime status contract"
    - path: "crates/bitaxe-api/src/lib.rs"
      provides: "API runtime status contract"
  key_links:
    - from: "crates/bitaxe-asic/src/lib.rs"
      to: "docs/parity/checklist.md"
      via: "deferred Phase 3 ASIC status"
      pattern: "DeferredUntilPhase3"
    - from: "crates/bitaxe-stratum/src/lib.rs"
      to: "docs/parity/checklist.md"
      via: "deferred Phase 4 Stratum status"
      pattern: "DeferredUntilPhase4"
    - from: "crates/bitaxe-api/src/lib.rs"
      to: "docs/parity/checklist.md"
      via: "deferred Phase 5 API status"
      pattern: "DeferredUntilPhase5"
---

<objective>
Create the remaining pure crate contracts for deferred ASIC, Stratum, and API surfaces.

Purpose: The monorepo shape must include the planned pure crates while making deferred behavior explicit and non-active in Phase 1.
Output: `bitaxe-asic`, `bitaxe-stratum`, and `bitaxe-api` crates with Cargo/Bazel test targets and deferred-status contracts.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-RESEARCH.md
@docs/project/seed-layout.md
@docs/parity/checklist.md
@PROVENANCE.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ASIC and Stratum deferred-status crates</name>
  <read_first>
    - Cargo.toml
    - crates/bitaxe-core/src/lib.rs
    - docs/project/seed-layout.md
    - docs/parity/checklist.md
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
    - standards/core/testing.md
    - standards/languages/rust.md
  </read_first>
  <files>Cargo.toml, crates/bitaxe-asic/Cargo.toml, crates/bitaxe-asic/BUILD.bazel, crates/bitaxe-asic/src/lib.rs, crates/bitaxe-stratum/Cargo.toml, crates/bitaxe-stratum/BUILD.bazel, crates/bitaxe-stratum/src/lib.rs</files>
  <behavior>
    - `bitaxe_asic::AsicRuntimeStatus::DeferredUntilPhase3` is exposed and test-covered.
    - `bitaxe_stratum::StratumRuntimeStatus::DeferredUntilPhase4` is exposed and test-covered.
    - The crates do not implement ASIC initialization, work submission, pool sockets, or mining behavior.
  </behavior>
  <action>
    Add `crates/bitaxe-asic` and `crates/bitaxe-stratum` to root workspace members. In `bitaxe-asic`, expose `AsicRuntimeStatus::DeferredUntilPhase3`; in `bitaxe-stratum`, expose `StratumRuntimeStatus::DeferredUntilPhase4`. Add focused unit tests with Arrange/Act/Assert comments and `BUILD.bazel` library/test targets. Keep both crates free of ESP-IDF dependencies and active hardware, serial, pool, mining, voltage, fan, thermal, and power behavior per D-08 and deferred scope.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-asic -p bitaxe-stratum && bazel test //crates/bitaxe-asic:tests //crates/bitaxe-stratum:tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "DeferredUntilPhase3" crates/bitaxe-asic/src/lib.rs` succeeds.
    - `grep -R "DeferredUntilPhase4" crates/bitaxe-stratum/src/lib.rs` succeeds.
    - `grep -R "esp-idf" crates/bitaxe-asic/Cargo.toml crates/bitaxe-stratum/Cargo.toml` finds no ESP-IDF dependency.
    - `rg 'init|send_work|socket|mine|voltage|fan|thermal|power' crates/bitaxe-asic/src crates/bitaxe-stratum/src` returns no active runtime behavior.
  </acceptance_criteria>
  <done>ASIC and Stratum package contracts exist and clearly defer active behavior to later phases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create API deferred-status crate and refresh lockfile</name>
  <read_first>
    - Cargo.toml
    - Cargo.lock
    - docs/project/seed-layout.md
    - docs/parity/checklist.md
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
    - standards/core/testing.md
    - standards/languages/rust.md
  </read_first>
  <files>Cargo.toml, Cargo.lock, crates/bitaxe-api/Cargo.toml, crates/bitaxe-api/BUILD.bazel, crates/bitaxe-api/src/lib.rs</files>
  <behavior>
    - `bitaxe_api::ApiRuntimeStatus::DeferredUntilPhase5` is exposed and test-covered.
    - The API crate does not implement HTTP routes, WebSockets, mutable settings, OTA, or static assets.
    - `Cargo.lock` includes all pure crate members created through this plan.
  </behavior>
  <action>
    Add `crates/bitaxe-api` to root workspace members. In `bitaxe-api`, expose `ApiRuntimeStatus::DeferredUntilPhase5`. Add focused unit tests and `BUILD.bazel` library/test targets. Keep the crate free of ESP-IDF dependencies and active HTTP/WebSocket/static asset behavior. Run `cargo generate-lockfile` after the API crate is added so `Cargo.lock` reflects all pure crates created in Plans 03 and 04.
  </action>
  <verify>
    <automated>cargo metadata --format-version=1 --no-deps && cargo test -p bitaxe-api && bazel test //crates/bitaxe-api:tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "DeferredUntilPhase5" crates/bitaxe-api/src/lib.rs` succeeds.
    - `grep -R "esp-idf" crates/bitaxe-api/Cargo.toml` finds no ESP-IDF dependency.
    - `grep -q 'crates/bitaxe-api' Cargo.toml` succeeds.
    - `test -f Cargo.lock` succeeds.
  </acceptance_criteria>
  <done>API package contract exists, pure-crate workspace metadata resolves, and the lockfile is current.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pure crates -> later firmware/adapters | Deferred crates must not start side-effecting services in Phase 1. |
| parity status -> user trust | Deferred status must not be confused with verified parity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing/Repudiation | Deferred runtime status | mitigate | Expose explicit deferred status enums and test the exact variants. |
| T-04-02 | Tampering/Elevation of Privilege | Pure crate boundaries | mitigate | Reject ESP-IDF dependencies and active side-effect behavior in acceptance criteria. |
| T-04-03 | Safety | ASIC/mining behavior | mitigate | ASIC and Stratum crates contain no initialization, work submission, sockets, or mining logic in Phase 1. |
| T-04-04 | Repudiation | GPL provenance | mitigate | Keep crates independently authored and avoid copying upstream C expression into MIT-first Rust files. |
</threat_model>

<verification>
Run `cargo metadata --format-version=1 --no-deps`, crate-scoped Cargo tests, and Bazel tests for `bitaxe-asic`, `bitaxe-stratum`, and `bitaxe-api`.
</verification>

<success_criteria>
FND-05 remaining pure crate coverage is complete when ASIC, Stratum, and API crates exist, build, test, and explicitly defer active behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-gamma-601-boot-log/01-04-SUMMARY.md`.
</output>
