---
phase: 01-foundation-and-gamma-601-boot-log
plan: "03"
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/bitaxe-core/Cargo.toml
  - crates/bitaxe-core/BUILD.bazel
  - crates/bitaxe-core/src/lib.rs
  - crates/bitaxe-config/Cargo.toml
  - crates/bitaxe-config/BUILD.bazel
  - crates/bitaxe-config/src/lib.rs
  - crates/bitaxe-test-support/Cargo.toml
  - crates/bitaxe-test-support/BUILD.bazel
  - crates/bitaxe-test-support/src/lib.rs
autonomous: true
requirements:
  - FND-04
  - FND-05
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 1-2026-06-21T00-30-20
generated_at: "2026-06-21T00:55:00Z"
must_haves:
  truths:
    - "Developer can run focused tests for the core safe-state contract."
    - "Gamma 601 and BM1370 identities are typed domain values instead of scattered strings."
    - "Test-support helpers can assert the Phase 1 safe-state string without depending on ESP-IDF."
  artifacts:
    - path: "crates/bitaxe-core/src/lib.rs"
      provides: "Phase 1 board, ASIC, and safe-state domain contracts"
    - path: "crates/bitaxe-config/src/lib.rs"
      provides: "Gamma 601 board-selection contract"
    - path: "crates/bitaxe-test-support/src/lib.rs"
      provides: "Pure test helpers for safe-state assertions"
    - path: "Cargo.lock"
      provides: "Generated Cargo dependency lock after first package members exist"
  key_links:
    - from: "crates/bitaxe-config/src/lib.rs"
      to: "crates/bitaxe-core/src/lib.rs"
      via: "shared BoardTarget and AsicTarget types"
      pattern: "BoardTarget"
    - from: "crates/bitaxe-test-support/src/lib.rs"
      to: "crates/bitaxe-core/src/lib.rs"
      via: "Phase1SafeState helper assertions"
      pattern: "Phase1SafeState"
---

<objective>
Create the foundational pure crates for board identity, safe state, config selection, and test support.

Purpose: Firmware and host tools need a tested pure Rust contract for Gamma 601/BM1370 identity and the explicit no-mining/no-control Phase 1 state.
Output: `bitaxe-core`, `bitaxe-config`, and `bitaxe-test-support` crates with Cargo/Bazel test targets.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-RESEARCH.md
@docs/project/seed-layout.md
@docs/parity/checklist.md
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create bitaxe-core safe-state contract</name>
  <read_first>
    - Cargo.toml
    - crates/bitaxe-core/Cargo.toml if it exists
    - crates/bitaxe-core/src/lib.rs if it exists
    - docs/project/seed-layout.md
    - docs/parity/checklist.md
    - standards/core/architecture.md
    - standards/core/testing.md
    - standards/languages/rust.md
  </read_first>
  <files>Cargo.toml, crates/bitaxe-core/Cargo.toml, crates/bitaxe-core/BUILD.bazel, crates/bitaxe-core/src/lib.rs</files>
  <behavior>
    - `bitaxe_core::BoardTarget::Gamma601.display_name()` returns `Gamma 601`.
    - `bitaxe_core::AsicTarget::Bm1370.display_name()` returns `BM1370`.
    - `bitaxe_core::Phase1SafeState::default().log_line()` returns `safe_state: mining=disabled asic_work_submission=disabled hardware_control=disabled`.
  </behavior>
  <action>
    Add `crates/bitaxe-core` to `Cargo.toml` workspace members and create package `bitaxe-core` per D-05. In `src/lib.rs`, define `BoardTarget::Gamma601`, `AsicTarget::Bm1370`, `Phase1SafeState`, `MiningState::Disabled`, `AsicWorkSubmissionState::Disabled`, and `HardwareControlState::Disabled`. Implement display/log helpers with exact strings required by D-08 and D-09. Add focused unit tests with `// Arrange`, `// Act`, and `// Assert` comments. Add `crates/bitaxe-core/BUILD.bazel` with `rust_library` and `rust_test` targets using `rules_rust`. Do not depend on `esp-idf-svc`, `esp-idf-sys`, or hardware adapters.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-core && bazel test //crates/bitaxe-core:tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "pub enum BoardTarget" crates/bitaxe-core/src/lib.rs` succeeds.
    - `grep -R "Gamma601" crates/bitaxe-core/src/lib.rs` succeeds.
    - `grep -R "safe_state: mining=disabled asic_work_submission=disabled hardware_control=disabled" crates/bitaxe-core/src/lib.rs` succeeds.
    - `grep -R "esp-idf" crates/bitaxe-core/Cargo.toml` finds no ESP-IDF dependency.
  </acceptance_criteria>
  <done>`bitaxe-core` owns tested pure Phase 1 identity and safe-state contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create config and test-support pure crates</name>
  <read_first>
    - Cargo.toml
    - Cargo.lock if it exists
    - crates/bitaxe-core/src/lib.rs
    - docs/project/seed-layout.md
    - standards/core/testing.md
    - standards/languages/rust.md
  </read_first>
  <files>Cargo.toml, Cargo.lock, crates/bitaxe-config/Cargo.toml, crates/bitaxe-config/BUILD.bazel, crates/bitaxe-config/src/lib.rs, crates/bitaxe-test-support/Cargo.toml, crates/bitaxe-test-support/BUILD.bazel, crates/bitaxe-test-support/src/lib.rs</files>
  <behavior>
    - `bitaxe_config::Phase1BoardSelection::gamma_601()` uses `BoardTarget::Gamma601` and `AsicTarget::Bm1370`.
    - `bitaxe_test_support` exposes helpers that assert the exact Phase 1 safe-state log line.
    - Neither crate performs NVS mutation, Wi-Fi setup, ASIC behavior, or hardware control.
  </behavior>
  <action>
    Add `crates/bitaxe-config` and `crates/bitaxe-test-support` to root workspace members. In `bitaxe-config`, export `Phase1BoardSelection` using `BoardTarget::Gamma601` and `AsicTarget::Bm1370` while intentionally excluding NVS, mutable settings, Wi-Fi, mining, ASIC, voltage, fan, thermal, and power behavior per D-08 and deferred scope. In `bitaxe-test-support`, add small helper functions for asserting Phase 1 safe-state strings. Add `BUILD.bazel` library/test targets for both crates. Run `cargo generate-lockfile` after these package members exist.
  </action>
  <verify>
    <automated>cargo metadata --format-version=1 --no-deps && cargo test -p bitaxe-config -p bitaxe-test-support && bazel test //crates/bitaxe-config:tests //crates/bitaxe-test-support:tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "Phase1BoardSelection" crates/bitaxe-config/src/lib.rs` succeeds.
    - `grep -R "BoardTarget::Gamma601" crates/bitaxe-config/src/lib.rs` succeeds.
    - `grep -R "AsicTarget::Bm1370" crates/bitaxe-config/src/lib.rs` succeeds.
    - `grep -R "safe_state: mining=disabled asic_work_submission=disabled hardware_control=disabled" crates/bitaxe-test-support/src/lib.rs` succeeds.
    - `grep -R "esp-idf" crates/bitaxe-config/Cargo.toml crates/bitaxe-test-support/Cargo.toml` finds no ESP-IDF dependency.
  </acceptance_criteria>
  <done>`bitaxe-config` and `bitaxe-test-support` build through Cargo and Bazel while preserving pure-crate boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pure crates -> firmware adapters | Pure crates must not gain ESP-IDF or hardware side effects. |
| config contract -> later persistence | Phase 1 config identity must not imply NVS or mutable settings are implemented. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering/Repudiation | `reference/esp-miner` | transfer | Plan 01 owns dirty/missing reference enforcement. |
| T-03-02 | Spoofing/Repudiation | Board/ASIC identity | mitigate | Encode Gamma 601 and BM1370 as typed enum variants with focused tests. |
| T-03-03 | Safety | Safe boot state | mitigate | `Phase1SafeState` makes mining, ASIC work submission, and hardware control disabled in a test-covered pure contract. |
| T-03-04 | Tampering/Elevation of Privilege | Pure crate boundaries | mitigate | Acceptance criteria reject ESP-IDF dependencies in these pure crates. |
</threat_model>

<verification>
Run `cargo metadata --format-version=1 --no-deps`, crate-scoped Cargo tests, and Bazel tests for `bitaxe-core`, `bitaxe-config`, and `bitaxe-test-support`.
</verification>

<success_criteria>
FND-05 foundational crate coverage is complete when the board/ASIC identity, safe-state contract, config selection, and test helpers are typed and testable without ESP-IDF side effects.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-gamma-601-boot-log/01-03-SUMMARY.md`.
</output>
