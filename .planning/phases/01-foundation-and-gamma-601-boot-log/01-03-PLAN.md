---
phase: 01-foundation-and-gamma-601-boot-log
plan: "03"
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - firmware/bitaxe/Cargo.toml
  - firmware/bitaxe/BUILD.bazel
  - firmware/bitaxe/build.rs
  - firmware/bitaxe/sdkconfig.defaults
  - firmware/bitaxe/src/main.rs
  - scripts/build-firmware.sh
autonomous: true
requirements:
  - FND-03
  - FND-04
  - FND-06
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 1-2026-06-21T00-30-20
generated_at: "2026-06-21T00:55:00Z"
must_haves:
  truths:
    - "Firmware builds for xtensa-esp32s3-espidf through a Bazel-visible target."
    - "Boot logs identify Gamma 601 and BM1370 while explicitly disabling mining, ASIC work submission, and hardware control."
    - "Firmware startup does not initialize Wi-Fi, Stratum, ASIC init, voltage, fan, thermal, or power control."
  artifacts:
    - path: "firmware/bitaxe/src/main.rs"
      provides: "Safe Gamma 601 boot/log firmware entrypoint"
    - path: "firmware/bitaxe/sdkconfig.defaults"
      provides: "ESP-IDF defaults for the ESP32-S3 Phase 1 firmware"
    - path: "scripts/build-firmware.sh"
      provides: "Bazel-owned wrapper around Cargo/ESP-IDF firmware build"
    - path: "firmware/bitaxe/BUILD.bazel"
      provides: "Public firmware build target"
  key_links:
    - from: "firmware/bitaxe/src/main.rs"
      to: "crates/bitaxe-core/src/lib.rs"
      via: "Phase1SafeState log contract"
      pattern: "Phase1SafeState"
    - from: "firmware/bitaxe/BUILD.bazel"
      to: "scripts/build-firmware.sh"
      via: "Bazel target //firmware/bitaxe:firmware"
      pattern: "build-firmware.sh"
---

<objective>
Build the first safe Gamma 601 firmware entrypoint.

Purpose: Phase 1 must prove the accepted ESP-IDF Rust target can produce a boot/log image while keeping all mining and hardware-control behavior disabled.
Output: ESP-IDF Rust firmware package, safe boot logs, and Bazel-visible firmware build target.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-RESEARCH.md
@.planning/phases/01-foundation-and-gamma-601-boot-log/01-VALIDATION.md
@docs/project/first-milestone.md
@docs/project/seed-layout.md
@docs/parity/checklist.md
@standards/core/architecture.md
@standards/languages/rust.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement safe boot/log firmware entrypoint</name>
  <read_first>
    - firmware/bitaxe/Cargo.toml
    - firmware/bitaxe/src/main.rs
    - crates/bitaxe-core/src/lib.rs
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-CONTEXT.md
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-RESEARCH.md
    - docs/project/first-milestone.md
    - docs/parity/checklist.md
  </read_first>
  <files>firmware/bitaxe/Cargo.toml, firmware/bitaxe/build.rs, firmware/bitaxe/sdkconfig.defaults, firmware/bitaxe/src/main.rs</files>
  <behavior>
    - Firmware initializes ESP-IDF patches and `EspLogger`.
    - Firmware logs `bitaxe-rust boot: board=Gamma 601 asic=BM1370`.
    - Firmware logs `safe_state: mining=disabled asic_work_submission=disabled hardware_control=disabled`.
    - Firmware logs `reference_commit=c1915b0a63bfabebdb95a515cedfee05146c1d50`.
    - Firmware does not start mining, ASIC work submission, Wi-Fi, Stratum, voltage, fan, thermal, power, OTA, or HTTP services.
  </behavior>
  <action>
    Replace the Plan 02 firmware package contract with a real Phase 1 ESP-IDF Rust app per D-08 and D-09. In `firmware/bitaxe/Cargo.toml`, set package name `bitaxe-firmware`, edition from workspace, license from workspace, dependencies `anyhow.workspace = true`, `bitaxe-core = { path = "../../crates/bitaxe-core" }`, `esp-idf-svc.workspace = true`, `esp-idf-sys.workspace = true`, and `log.workspace = true`, plus build-dependency `embuild.workspace = true` pinned by Plan 02 to `0.33.1`. Add `[package.metadata.esp-idf-sys] esp_idf_version = "tag:v5.5.4"`, `esp_idf_tools_install_dir = "workspace"`, `esp_idf_sdkconfig = "sdkconfig"`, and `esp_idf_sdkconfig_defaults = ["sdkconfig.defaults"]`. Add `build.rs` using the esp-rs build-script pattern with `embuild::espidf::sysenv::output()`. Create `sdkconfig.defaults` for ESP32-S3 with serial logging and no Wi-Fi credentials. In `src/main.rs`, call `esp_idf_svc::sys::link_patches()`, initialize `esp_idf_svc::log::EspLogger`, read `Phase1SafeState::default()`, and log exact identity/status lines: `firmware_name=bitaxe-rust`, `board=Gamma 601`, `asic=BM1370`, `esp_idf_version=v5.5.4`, `rust_target=xtensa-esp32s3-espidf`, `reference_commit=c1915b0a63bfabebdb95a515cedfee05146c1d50`, and the safe-state string. Use ESP-IDF APIs for reset reason, app/partition identity, and PSRAM/platform status when available; log `Unavailable` for values that cannot be read without guessing. Do not import or call ASIC init, Stratum, Wi-Fi, HTTP, OTA, NVS mutation, voltage, fan, thermal, or power-control code.
  </action>
  <verify>
    <automated>cargo check -p bitaxe-firmware --target xtensa-esp32s3-espidf && grep -R "safe_state: mining=disabled asic_work_submission=disabled hardware_control=disabled" firmware/bitaxe/src/main.rs crates/bitaxe-core/src/lib.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'esp_idf_version = "tag:v5.5.4"' firmware/bitaxe/Cargo.toml` succeeds.
    - `grep -q 'xtensa-esp32s3-espidf' firmware/bitaxe/src/main.rs` succeeds.
    - `grep -q 'board=Gamma 601' firmware/bitaxe/src/main.rs` succeeds.
    - `grep -q 'asic=BM1370' firmware/bitaxe/src/main.rs` succeeds.
    - `grep -q 'safe_state: mining=disabled asic_work_submission=disabled hardware_control=disabled' firmware/bitaxe/src/main.rs` succeeds.
    - `rg 'BM1370_init|send_work|stratum|wifi|fan|thermal|voltage|power' firmware/bitaxe/src/main.rs` returns no active runtime calls.
  </acceptance_criteria>
  <done>The firmware entrypoint builds for ESP32-S3 and emits only Phase 1 identity/status/safe-state boot logs.</done>
</task>

<task type="auto">
  <name>Task 2: Add Bazel firmware build wrapper</name>
  <read_first>
    - firmware/bitaxe/BUILD.bazel if it exists
    - scripts/build-firmware.sh if it exists
    - firmware/bitaxe/Cargo.toml
    - .cargo/config.toml
    - docs/project/seed-layout.md
    - .planning/phases/01-foundation-and-gamma-601-boot-log/01-RESEARCH.md
    - standards/core/code-shape.md
    - standards/core/verification.md
  </read_first>
  <files>firmware/bitaxe/BUILD.bazel, scripts/build-firmware.sh</files>
  <action>
    Add `scripts/build-firmware.sh` with `#!/usr/bin/env bash`, `set -euo pipefail`, and breadcrumb logs that print `MCU=esp32s3`, `target=xtensa-esp32s3-espidf`, `esp_idf_version=tag:v5.5.4`, and the cargo command before running it. If `$HOME/export-esp.sh` exists, source it; otherwise fail with an actionable message naming `espup install --targets esp32s3 --std`. The script must run `cargo build -p bitaxe-firmware --release --target xtensa-esp32s3-espidf` and copy declared ELF/output paths into the Bazel output directory passed by the target. Add `firmware/bitaxe/BUILD.bazel` with public target `firmware` that invokes the script through a repo-owned Bazel action or `sh_binary`/`genrule` wrapper. Keep the target name exactly `//firmware/bitaxe:firmware` per D-04, D-06, and D-11.
  </action>
  <verify>
    <automated>bash -n scripts/build-firmware.sh && bazel build //firmware/bitaxe:firmware</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'set -euo pipefail' scripts/build-firmware.sh` succeeds.
    - `grep -q 'xtensa-esp32s3-espidf' scripts/build-firmware.sh` succeeds.
    - `grep -q 'espup install --targets esp32s3 --std' scripts/build-firmware.sh` succeeds.
    - `grep -q 'name = "firmware"' firmware/bitaxe/BUILD.bazel` succeeds.
    - `bazel query //firmware/bitaxe:firmware` succeeds.
  </acceptance_criteria>
  <done>`bazel build //firmware/bitaxe:firmware` is the canonical firmware build entrypoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware runtime -> hardware | Firmware can eventually affect hardware, so Phase 1 must stay safe-no-op. |
| Bazel -> Cargo/ESP-IDF | Bazel delegates to Cargo/ESP-IDF through a script wrapper. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering/Repudiation | `reference/esp-miner` | transfer | Plan 01 verifies the reference tree; this plan only logs the pinned commit string. |
| T-03-02 | Tampering/Elevation of Privilege | Build script args | mitigate | `scripts/build-firmware.sh` accepts no untrusted port/image args and uses fixed cargo target values. |
| T-03-03 | Repudiation | Firmware provenance | mitigate | Firmware logs ESP-IDF version, target, firmware identity, and reference commit; package manifest is completed in Plan 04. |
| T-03-04 | Spoofing/Repudiation | Parity status | mitigate | Build success is not marked as `verified`; hardware smoke remains required in Plan 06. |
| T-03-05 | Safety | Boot log safe state | mitigate | Exact safe-state log line is enforced by code and grep acceptance criteria, and active mining/hardware-control imports are rejected. |
</threat_model>

<verification>
Run `cargo check -p bitaxe-firmware --target xtensa-esp32s3-espidf`, `bash -n scripts/build-firmware.sh`, and `bazel build //firmware/bitaxe:firmware`.
</verification>

<success_criteria>
FND-06 is implementation-ready when the firmware builds for ESP32-S3 and logs safe identity/status without starting any mining or hardware-control path.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-gamma-601-boot-log/01-03-SUMMARY.md`.
</output>
