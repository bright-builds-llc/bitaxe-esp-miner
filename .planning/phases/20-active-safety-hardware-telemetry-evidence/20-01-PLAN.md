---
phase: 20-active-safety-hardware-telemetry-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/parity/src/safety_allow.rs
  - docs/parity/evidence/phase-20-active-safety-hardware-telemetry-evidence/evidence-contract.md
  - docs/parity/evidence/phase-20-active-safety-hardware-telemetry-evidence/redaction-review.md
  - docs/parity/evidence/phase-20-active-safety-hardware-telemetry-evidence/summary.md
  - .planning/phases/20-active-safety-hardware-telemetry-evidence/20-VALIDATION.md
autonomous: true
requirements:
  - SAFE-04
  - SAFE-08
  - EVD-05
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 20-2026-07-03T20-48-00
generated_at: 2026-07-03T21:10:01Z
nyquist_compliant: true
must_haves:
  truths:
    - "Failure-path probes can be represented by a first-class `failure-paths` safety allow surface instead of being hidden under unrelated surfaces."
    - "The Phase 20 evidence tree exists before hardware, network, or checklist citations are added."
    - "Committed Phase 20 evidence has a redaction contract covering serial logs, manifests, API bodies, WebSocket frames, detector output, board-info output, command output, URLs, IP/MAC/SSID values, credentials, tokens, NVS secrets, and terminal secrets."
  artifacts:
    - path: tools/parity/src/safety_allow.rs
      provides: "Safety allow manifest validator with `failure-paths` surface coverage"
      contains: "\"failure-paths\""
    - path: docs/parity/evidence/phase-20-active-safety-hardware-telemetry-evidence/evidence-contract.md
      provides: "Phase 20 evidence-pack and claim-tier contract"
      contains: "failure-paths"
    - path: docs/parity/evidence/phase-20-active-safety-hardware-telemetry-evidence/redaction-review.md
      provides: "Redaction checklist before citation"
      contains: "redaction_status: pending"
    - path: docs/parity/evidence/phase-20-active-safety-hardware-telemetry-evidence/summary.md
      provides: "Draft final exact-claim ledger"
      contains: "phase20_status: draft"
  key_links:
    - from: docs/parity/evidence/phase-20-active-safety-hardware-telemetry-evidence/evidence-contract.md
      to: tools/parity/src/safety_allow.rs
      via: "documented allowed surfaces"
      pattern: "failure-paths"
    - from: .planning/phases/20-active-safety-hardware-telemetry-evidence/20-VALIDATION.md
      to: tools/parity/src/safety_allow.rs
      via: "Wave 0 validator coverage"
      pattern: "20-W0-01"
---

<objective>
Create the Phase 20 evidence foundation: a first-class `failure-paths` safety-allow surface, a claim/evidence contract, a pending redaction gate, a draft summary ledger, and validation rows that downstream plans can consume.

Purpose: Phase 20 must handle active/fault evidence without broad unsafe scripts or checklist overclaim. This plan establishes the machine-checkable and documentation scaffolding before any surface evidence is created.

Output: Updated safety-allow validator/tests, Phase 20 evidence contract, pending redaction review, draft summary, and Wave 0 validation status.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-active-safety-hardware-telemetry-evidence/20-CONTEXT.md
@.planning/phases/20-active-safety-hardware-telemetry-evidence/20-RESEARCH.md
@.planning/phases/20-active-safety-hardware-telemetry-evidence/20-VALIDATION.md
@tools/parity/src/safety_allow.rs
@docs/parity/evidence/phase-14-safety-hardware-evidence-completion.md

<interfaces>
From `tools/parity/src/safety_allow.rs`:
```rust
const ALLOWED_SURFACES: &[&str] = &[
    "safe-baseline",
    "power-telemetry",
    "voltage-control",
    "thermal-fan",
    "self-test-watchdog-load",
    "display-input",
    "live-api-websocket-telemetry",
    "parity-redaction",
];
const ACTIVE_CLAIM_TIERS: &[&str] = &[
    "bounded-actuation",
    "fault-stimulus",
    "self-test-hardware",
    "load-stress",
    "runtime-display-input",
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `failure-paths` to safety allow validation</name>
  <files>tools/parity/src/safety_allow.rs</files>
  <read_first>
    - tools/parity/src/safety_allow.rs
    - .planning/phases/20-active-safety-hardware-telemetry-evidence/20-CONTEXT.md
    - .planning/phases/20-active-safety-hardware-telemetry-evidence/20-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: a manifest with `surface: "failure-paths"`, `claim_tier: "fault-stimulus"`, and `evidence_class: "hardware-regression"` passes when the active abort, recovery, package, board, and safe-state fields are valid.
    - Test 2: a manifest with `surface: "failure-paths"`, `claim_tier: "fault-stimulus"`, and `evidence_class: "hardware-smoke"` fails with the existing hardware-regression error.
    - Test 3: a manifest with `surface: "failure-paths"`, `claim_tier: "unsupported-pending"`, and `evidence_class: "deferred"` passes without recovery steps because it records blocked evidence, not active stimulus.
  </behavior>
  <action>Add `"failure-paths"` to `ALLOWED_SURFACES` in `tools/parity/src/safety_allow.rs`. Add focused unit tests using the existing `manifest_with_change` helper; do not add new dependencies, do not alter accepted claim-tier semantics, and do not weaken active-tier recovery, abort, or safe-state requirements. This implements D-01, D-03, D-12, and D-13 while preserving the existing hardware-regression guard for active `fault-stimulus` claims.</action>
  <acceptance_criteria>
    - `rg -n '"failure-paths"' tools/parity/src/safety_allow.rs` finds the allowed surface and at least one test case.
    - `rg -n 'fault-stimulus.*hardware-regression|hardware-regression.*fault-stimulus' tools/parity/src/safety_allow.rs` finds test coverage or expected-class coverage.
    - `cargo test -p bitaxe-parity --all-features safety_allow` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p bitaxe-parity --all-features safety_allow</automated>
    <automated>bazel test //tools/parity:tests --test_filter=safety_allow</automated>
  </verify>
  <done>`failure-paths` is a validated surface, active fault-stimulus claims still require `hardware-regression`, and blocked failure-path evidence can be represented as `unsupported-pending` plus `deferred`.</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 20 evidence scaffold and redaction contract</name>
  <files>docs/parity/evidence/phase-20-active-safety-hardware-telemetry-evidence/evidence-contract.md, docs/parity/evidence/phase-20-active-safety-hardware-telemetry-evidence/redaction-review.md, docs/parity/evidence/phase-20-active-safety-hardware-telemetry-evidence/summary.md, .planning/phases/20-active-safety-hardware-telemetry-evidence/20-VALIDATION.md</files>
  <read_first>
    - docs/parity/evidence/phase-20-active-safety-hardware-telemetry-evidence/evidence-contract.md
    - docs/parity/evidence/phase-20-active-safety-hardware-telemetry-evidence/redaction-review.md
    - docs/parity/evidence/phase-20-active-safety-hardware-telemetry-evidence/summary.md
    - .planning/phases/20-active-safety-hardware-telemetry-evidence/20-VALIDATION.md
    - docs/parity/evidence/phase-14-safety-hardware-evidence-completion.md
    - docs/parity/evidence/phase-14-safety-hardware-evidence-completion/redaction-review.md
    - .planning/phases/20-active-safety-hardware-telemetry-evidence/20-CONTEXT.md
  </read_first>
  <action>Create `docs/parity/evidence/phase-20-active-safety-hardware-telemetry-evidence/` if missing. Add `evidence-contract.md` naming these packs exactly: `safe-baseline`, `active-power-voltage`, `active-thermal-fan`, `self-test-watchdog-load`, `runtime-display-input`, `failure-paths`, `live-api-websocket-telemetry`, and `parity-redaction`. For each pack, state the allowed claim tiers, minimum metadata, non-claims, and whether hardware-smoke or hardware-regression is required. Add `redaction-review.md` with `redaction_status: pending`, `reviewer: pending`, `raw_artifacts_committed: no`, and checks for serial logs, JSON manifests, API bodies, WebSocket frames, detector output, board-info output, package logs, command output, manual observations, `DEVICE_URL`, IP addresses, MAC addresses, SSIDs, Wi-Fi credentials, pool credentials, worker secrets, API tokens, NVS secret values, and local terminal secrets per D-15. Add `summary.md` with `phase20_status: draft`, `redaction_status: pending`, `checklist_status: not-updated`, and an exact-claim matrix initialized to `pending`. Update `20-VALIDATION.md` Wave 0 rows so `20-W0-01` points to this plan and mentions `failure-paths` surface coverage.</action>
  <acceptance_criteria>
    - `rg -n 'safe-baseline|active-power-voltage|active-thermal-fan|self-test-watchdog-load|runtime-display-input|failure-paths|live-api-websocket-telemetry|parity-redaction' docs/parity/evidence/phase-20-active-safety-hardware-telemetry-evidence/evidence-contract.md` finds every pack name.
    - `rg -n 'redaction_status: pending|raw_artifacts_committed: no|DEVICE_URL|Wi-Fi credentials|pool credentials|NVS secret values' docs/parity/evidence/phase-20-active-safety-hardware-telemetry-evidence/redaction-review.md` finds every required redaction marker.
    - `rg -n 'phase20_status: draft|checklist_status: not-updated|SAFE-01|SAFE-09|EVD-05' docs/parity/evidence/phase-20-active-safety-hardware-telemetry-evidence/summary.md` finds the draft status and requirement coverage.
    - `rg -n '20-W0-01|failure-paths' .planning/phases/20-active-safety-hardware-telemetry-evidence/20-VALIDATION.md` finds the updated validation mapping.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'failure-paths|redaction_status: pending|phase20_status: draft|20-W0-01' docs/parity/evidence/phase-20-active-safety-hardware-telemetry-evidence .planning/phases/20-active-safety-hardware-telemetry-evidence/20-VALIDATION.md</automated>
  </verify>
  <done>Phase 20 has a committed evidence scaffold and redaction contract before any hardware, network, or checklist citation work begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| plan executor -> safety allow validator | Manifest values decide whether hardware evidence can support claims. |
| evidence artifacts -> committed repo | Local hardware and network identifiers can leak into Markdown, JSON, and logs. |
| checklist claims -> release readers | Overclaiming can represent unsupported safety behavior as verified parity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-20-01-01 | Tampering / Denial of Service | `tools/parity/src/safety_allow.rs` | mitigate | Add `failure-paths` without weakening active-tier recovery, abort, and safe-state checks. |
| T-20-01-02 | Information Disclosure | Phase 20 evidence scaffold | mitigate | Require redaction review for logs, JSON, API bodies, WebSocket frames, device URLs, IP/MAC/SSID values, credentials, tokens, and NVS secrets before citation. |
| T-20-01-03 | Repudiation / Integrity | `docs/parity/checklist.md` future citations | mitigate | Document exact claim tiers and require `just parity` before any final checklist promotion. |
| T-20-01-04 | Elevation of Privilege | Future diagnostics | mitigate | Evidence contract states any new diagnostic route must be compile-gated, bounded, tested, and non-production-exposed per D-07. |
</threat_model>

<verification>
Run `cargo test -p bitaxe-parity --all-features safety_allow`, `bazel test //tools/parity:tests --test_filter=safety_allow`, and the scaffold `rg` checks. Do not touch `reference/esp-miner`.
</verification>

<success_criteria>
Plan 01 is complete when `failure-paths` is a first-class allowed safety surface, Phase 20 evidence and redaction scaffolds exist, validation references are updated, and the parity validator tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/20-active-safety-hardware-telemetry-evidence/20-01-SUMMARY.md`.
</output>
