---
phase: 36-substantive-evidence-admission-and-exact-re-promotion
plan: "08"
type: execute
wave: 7
depends_on:
  - "36-06"
autonomous: true
gap_closure: true
phase_lifecycle_id: 36-2026-07-23T15-20-53
lifecycle_mode: yolo
generated_by: gsd-plan-phase
generated_at: "2026-07-25T02:19:04.334Z"
requirements:
  - SYS-02
  - EVD-11
  - EVD-12
  - EVD-14
  - EVD-15
files_modified:
  - scripts/phase36-substantive-evidence-test.sh
  - target/private-evidence/phase36-gap-attempt-02/
  - .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-GAP-HARDWARE-CANDIDATE.json
  - .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-08-SUMMARY.md
must_haves:
  truths:
    - "A verified deployed-adapter repair and exact clean-current-HEAD board-205 package pass every software-only gate before any detector, hardware, or credential-content access."
    - "Exactly one repo-owned Phase 36 hardware command owns exactly one detector call and either seals eligible evidence or seals a typed non-promotion without retry."
    - "Eligible evidence preserves one immutable private classifier input and produces one distinct commit-redacted candidate; a blocker or non-promotion produces no candidate."
    - "The earliest typed failure, bounded recovery, restoration, cleanup, and terminal disposition remain ordered and immutable."
    - "Only closed categories, bounded counts and durations, booleans, and public digests reach shareable output."
  artifacts:
    - path: scripts/phase36-substantive-evidence-test.sh
      provides: "Exact remaining-plan graph assertion and deployed adapter/fake-detector/replay regressions"
    - path: target/private-evidence/phase36-gap-attempt-02/
      provides: "Fresh mode-0700 protected parent, fixed handle, immutable attempt child, and private sibling records"
    - path: .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-GAP-HARDWARE-CANDIDATE.json
      provides: "Distinct commit-redacted candidate only when the attempt is sealed eligible"
    - path: .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-08-SUMMARY.md
      provides: "Truthful eligible or blocked/non-promotional handoff to offline Plan 36-07"
  key_links:
    - from: scripts/phase36-substantive-evidence-test.sh
      to: .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-08-PLAN.md
      via: "exact incomplete graph `36-08@7`, `36-07@8`, `36-04@9` with every entry gap_closure"
      pattern: "36-08@7"
    - from: target/private-evidence/phase36-gap-attempt-02/attempt.handle
      to: target/private-evidence/phase36-gap-attempt-02/
      via: "single-use exact-package capability resolves only the supervisor-owned child under the fixed protected parent"
    - from: target/private-evidence/phase36-gap-attempt-02/
      to: .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-GAP-HARDWARE-CANDIDATE.json
      via: "private-first classification followed by distinct commit-redacted derivation without changing the private digest"
    - from: .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-08-SUMMARY.md
      to: .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-07-PLAN.md
      via: "only `sealed_eligible` plus the distinct candidate unblocks the offline Wave 8 consumer"
---

<objective>
Exercise the verified deployed-adapter repair through one fresh progress-gated Ultra 205 attempt and create a truthful offline handoff.

Purpose: close the Plan 36-06 pre-effect software boundary without reusing its sealed root, weakening privacy, or authorizing an unchanged retry.
Output: an exact graph regression, one fresh immutable attempt disposition, an eligible redacted candidate or exact non-promotion, and `36-08-SUMMARY.md`.
</objective>

<execution_context>
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-06-SUMMARY.md
@scripts/phase36-substantive-evidence-test.sh
@Justfile
@docs/hardware/hardware-attempt-policy.md
@docs/parity/evidence-policy.md
@AGENTS.md
</execution_context>

<context>
Plan 36-06 stopped before detector access with `effect_adapter_unavailable`, then committed and software-verified the deployed-runfiles repair at exact commit `68b43bcd78ee158582d60e73580023884c2738de`. Its private root remains immutable with public digest `44c102722d9098bb17d67715cb41fd119500cd67c4129b1c03a2107a43f974c0`; do not resolve, search, reuse, rewrite, or splice that root.

The Phase 35 root remains immutable with digest `0401e7b485df2d1ccfc67e63845f98b6217816a184901bf0595d03af3219757d`.

This plan permits only board `205`/BM1366 over the provided USB and barrel-power connectors through the existing repo-owned broker. It prohibits direct UART, pins, pads, headers, GPIO, test points, probes, jumpers, solder, injected signals, archived Phase 28.1.1, Phase 30 mutation, mining, pool inputs, OTA, every recovery path except the predeclared same-image partial-flash restoration, other boards, self-test effects, active control, discovery scans, ad hoc device commands, secret output, prior-root mutation, and Plan 36-04 canonical edits.
</context>

<threat-model>

## ASVS Level

OWASP ASVS Level 1. All high-severity threats are blocking.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Current source to exact package capability | Clean current HEAD, reference, evaluator, ELF, executable, factory, manifest, and package identities enter a single-use preflight handle. |
| Broker to detector and device | One repo-owned hardware process owns the sole detector call and every permitted USB/barrel effect. |
| Sensitive runtime input to private capture | Wi-Fi input may enter memory only after detector admission and NeverPersistRaw values are removed before the first write or emission. |
| Immutable private input to redacted candidate | Authorized private classification precedes distinct lossy redaction and revalidates the unchanged private digest. |
| Plan 36-08 to offline Plan 36-07 | Only a sealed-eligible summary plus a distinct redacted candidate crosses the handoff boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-36-08-01 | Spoofing | Board and exact-package identity | mitigate | Bind clean current HEAD, pinned reference, evaluator, target, board 205/BM1366, ELF, executable, factory, manifest, and package digests before detector; require the broker's one detector result. |
| T-36-08-02 | Tampering | Prior roots, fresh parent, and candidate | mitigate | Never access or mutate the Plan 36-06 root, fingerprint Phase 35, create a fresh mode-0700 parent, give the supervisor exclusive child creation, and recheck immutable private/candidate digests. |
| T-36-08-03 | Repudiation | One-shot chronology | mitigate | Append the complete ordered ledger, preserve the earliest typed failure, record recovery and cleanup separately, seal one closed disposition, and report invocation/detector counts. |
| T-36-08-04 | Information Disclosure | Credentials, operational identifiers, and output | mitigate | Check credentials only with `test -s` in software preflight, defer broker credential access until detector admission, remove NeverPersistRaw before write, keep ProtectedOperational values private, and emit closed shareable fields only. |
| T-36-08-05 | Denial of Service | Flash, capture, recovery, and cleanup | mitigate | Use a 360-second capture bound with at least 420 seconds of adapter wall clock, allow only predeclared bounded same-image recovery, reap all descendants, close holders/descriptors, and retain cleanup evidence. |
| T-36-08-06 | Elevation of Privilege | Hardware scope and downstream promotion | mitigate | Restrict effects to USB/barrel board 205 and the broker allowlist; reject UART/pins, mining, OTA, other boards, active control, archived work, discovery scans, and promotion from any non-eligible disposition. |

</threat-model>

<tasks>

<task type="auto" id="36-08-01">
  <name>Commit the exact graph regression and freeze a clean-current-HEAD preflight</name>
  <read_first>
    - AGENTS.md
    - .codex/tasks/lessons.md
    - .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-04-PLAN.md
    - .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-07-PLAN.md
    - .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-08-PLAN.md
    - .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-06-SUMMARY.md
    - scripts/phase36-substantive-evidence-test.sh
    - scripts/phase36-substantive-evidence.sh
    - Justfile
    - docs/hardware/hardware-attempt-policy.md
    - docs/parity/evidence-policy.md
    - .gitignore
  </read_first>
  <files>
    - scripts/phase36-substantive-evidence-test.sh
    - target/private-evidence/phase36-gap-attempt-02/
  </files>
  <action>
Update only the Phase 36 process test's remaining-plan graph contract:

- declare `plan_08` as `${phase_dir}/36-08-PLAN.md`;
- set the exact sorted incomplete graph to `36-08@7`, `36-07@8`, `36-04@9`;
- assert `gap_closure: true` for exactly those three incomplete plans;
- retain the deployed supervisor, deployed effect-adapter, fake-detector single-call, no-later-effect, earliest-failure/recovery/cleanup ordering, and replay-rejection regressions.

Run the complete software-only gate in this order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`; `just test` and require all 75 targets; fresh no-cache `//tools/parity:phase36_broker_tests`, `//tools/parity:phase36_evidence_tests`, and `//scripts:phase36_substantive_evidence_test`; `just parity`; `just verify-reference`; `just verify-redaction`; `git diff --check`. The named suites must cross the deployed runfiles adapter, fake detector, and capability replay boundaries.

Commit only `scripts/phase36-substantive-evidence-test.sh` with message `test(36-08): bind second gap attempt graph`, then require a clean current `HEAD`. Rebuild the exact board-205 package with `just package`. Check the local prerequisite only as `test -s wifi-credentials.json`; do not read or inspect credential contents, metadata, values, or digests.

Create only the fixed ignored parent `target/private-evidence/phase36-gap-attempt-02` as mode 0700 after `git check-ignore -q target/private-evidence/phase36-gap-attempt-02` passes. Require fixed handle `target/private-evidence/phase36-gap-attempt-02/attempt.handle`, distinct mode-0600 sibling stdout/stderr records, and absent candidate `.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-GAP-HARDWARE-CANDIDATE.json`. Use this task's `<verify>` command as the sole software-gate/package/preflight execution; do not separately replay preflight before or after it. Its one preflight invocation is exactly:

`just phase36-substantive-evidence mode=preflight board=205 private-parent=target/private-evidence/phase36-gap-attempt-02 attempt-handle-file=target/private-evidence/phase36-gap-attempt-02/attempt.handle candidate-output=.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-GAP-HARDWARE-CANDIDATE.json capture-timeout-seconds=360`

Require the handle to bind the clean current HEAD, pinned reference, current evaluator, `xtensa-esp32s3-espidf`, board `205`, BM1366, and exact ELF/executable/factory/manifest/package identities. Preserve without access or mutation the Plan 36-06 root digest `44c102722d9098bb17d67715cb41fd119500cd67c4129b1c03a2107a43f974c0` and preserve Phase 35 root `0401e7b485df2d1ccfc67e63845f98b6217816a184901bf0595d03af3219757d`.

This task must invoke zero detector, board-info, USB, serial, DEVICE_URL, HTTP/WebSocket, flash, monitor, reboot, hardware, or credential-content operation.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain)" &amp;&amp; test -s wifi-credentials.json &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; just test &amp;&amp; bazel test --nocache_test_results //tools/parity:phase36_broker_tests //tools/parity:phase36_evidence_tests //scripts:phase36_substantive_evidence_test &amp;&amp; just parity &amp;&amp; just verify-reference &amp;&amp; just verify-redaction &amp;&amp; git diff --check &amp;&amp; just package &amp;&amp; just phase36-substantive-evidence mode=preflight board=205 private-parent=target/private-evidence/phase36-gap-attempt-02 attempt-handle-file=target/private-evidence/phase36-gap-attempt-02/attempt.handle candidate-output=.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-GAP-HARDWARE-CANDIDATE.json capture-timeout-seconds=360</automated>
  </verify>
  <done>The graph regression is committed, every software boundary is fresh and green, and one clean-current-HEAD single-use preflight is ready with zero detector, hardware, or credential-content access.</done>
  <acceptance_criteria>
    - The process test expects exactly `36-08@7`, `36-07@8`, `36-04@9`, and all three incomplete plans contain `gap_closure: true`.
    - The ordered Rust gate, all 75 Just targets, fresh no-cache broker/evidence/deployed-process tests, parity, reference, redaction, and diff checks exit 0.
    - The graph regression is committed before `just package`; packaging and preflight use the resulting clean current HEAD.
    - Deployed adapter availability, exactly one fake detector call, no later fake effect, earliest-failure/recovery/cleanup order, and replay rejection are proven by fresh tests.
    - The fixed parent is ignored and mode 0700; handle and sibling records are mode 0600; the supervisor-owned child and candidate are absent before hardware launch.
    - Credential prerequisite is checked only by `test -s wifi-credentials.json`; no credential content, metadata, value, or digest is read or emitted.
    - Detector, board-info, USB, serial, network, flash, monitor, reboot, and hardware invocation counts are all zero.
    - The exact Plan 36-06 and Phase 35 public root digests remain unchanged.
  </acceptance_criteria>
</task>

<task type="auto" id="36-08-02">
  <name>Invoke and seal exactly one broker-owned Ultra 205 attempt</name>
  <read_first>
    - .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-06-SUMMARY.md
    - scripts/phase36-substantive-evidence.sh
    - scripts/phase36-hardware-effect.sh
    - tools/parity/src/phase36_broker.rs
    - tools/parity/src/phase36_broker/hardware.rs
    - tools/parity/src/phase36_broker/hardware_process.rs
    - tools/parity/src/phase36_evidence/capture.rs
    - docs/hardware/hardware-attempt-policy.md
    - docs/parity/evidence-policy.md
    - AGENTS.md
  </read_first>
  <files>
    - target/private-evidence/phase36-gap-attempt-02/
    - .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-GAP-HARDWARE-CANDIDATE.json
    - .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-08-SUMMARY.md
  </files>
  <action>
Invoke exactly once, with an adapter/shell wall-clock bound of at least 420 seconds, this existing repo command:

`just phase36-substantive-evidence mode=hardware board=205 private-parent=target/private-evidence/phase36-gap-attempt-02 attempt-handle-file=target/private-evidence/phase36-gap-attempt-02/attempt.handle candidate-output=.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-GAP-HARDWARE-CANDIDATE.json capture-timeout-seconds=360 wifi-credentials=wifi-credentials.json`

Do not run `just detect-ultra205` or any standalone detector before or after the command. The broker owns exactly one detector call and all authorized USB/barrel device work. Do not retry the command for any exit status, category, timeout, missing candidate, cleanup result, timing change, or ordinal change.

The broker must preserve the append-only effect ledger and earliest typed failure; distinguish authorization, invocation, completion/failure, bounded same-image recovery, restoration, cleanup, and terminal sealing; retain all children/process groups/sockets/serial-holder cleanup facts; and seal the fresh child immutable. A typed partial-flash boundary may use only the predeclared same-image recovery needed to restore bootability and can never make evidence eligible. No other recovery, ad hoc device command, or second attempt is allowed.

After the one command returns, perform offline inspection only through the fixed handle and explicit files. Do not scan ignored roots, rediscover a device, or print a private path, operational value, command, raw document, free-form error, credential fact, or secret. Shareable output is limited to closed categories, booleans, bounded counts and durations, and public digests.

If the seal is `sealed_eligible`, require one immutable secret-sanitized private classifier input, run authorized private classification before redaction, recheck its digest unchanged, and require the distinct candidate at `.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-GAP-HARDWARE-CANDIDATE.json` to contain only ShareableFact and PublicProvenance. Record the eligible handoff in `36-08-SUMMARY.md`; this is the only disposition that unblocks offline Plan 36-07.

If the attempt reaches any typed blocker or `sealed_non_promotion`, require an immutable typed seal, complete recovery/restoration/cleanup accounting, and no candidate. Record the exact closed category and truthful non-promotion in `36-08-SUMMARY.md`; leave Plan 36-07 blocked. If no valid typed seal exists, fail this plan and still do not retry.

Preserve the Plan 36-06 root/digest `44c102722d9098bb17d67715cb41fd119500cd67c4129b1c03a2107a43f974c0`, Phase 35 root/digest `0401e7b485df2d1ccfc67e63845f98b6217816a184901bf0595d03af3219757d`, every prior byte, and Plan 36-04 canonical files. Prohibit direct UART/pins/pads/probes/GPIO/jumpers/solder, archived Phase 28.1.1, mining/pool, OTA, other boards, self-test/active control, discovery scans, and secret output.
  </action>
  <verify>
    <automated>umask 077 &amp;&amp; child_name="$(jq -er '.child_name' target/private-evidence/phase36-gap-attempt-02/attempt.handle)" &amp;&amp; seal="target/private-evidence/phase36-gap-attempt-02/${child_name}/seal.json" &amp;&amp; test -f "$seal" &amp;&amp; status="$(jq -er '.status' "$seal")" &amp;&amp; case "$status" in sealed_eligible) test -s .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-GAP-HARDWARE-CANDIDATE.json &amp;&amp; just phase36-substantive-evidence mode=inspect-candidate board=205 private-parent=target/private-evidence/phase36-gap-attempt-02 attempt-handle-file=target/private-evidence/phase36-gap-attempt-02/attempt.handle candidate-output=.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-GAP-HARDWARE-CANDIDATE.json &gt;target/private-evidence/phase36-gap-attempt-02/inspect.stdout 2&gt;target/private-evidence/phase36-gap-attempt-02/inspect.stderr ;; sealed_non_promotion) test ! -e .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-GAP-HARDWARE-CANDIDATE.json ;; *) exit 1 ;; esac &amp;&amp; test -f .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-08-SUMMARY.md &amp;&amp; just verify-redaction &amp;&amp; just verify-reference &amp;&amp; git diff --check</automated>
  </verify>
  <done>One and only one fresh broker-owned attempt has an immutable eligible or non-promotional typed disposition, complete cleanup, and a truthful offline handoff without retry.</done>
  <acceptance_criteria>
    - Hardware-command invocation count is exactly one and broker-owned detector invocation count is exactly one.
    - Every device-capable operation is ledgered in order; the earliest failure is never replaced by recovery or cleanup.
    - Capture timeout is 360 seconds and the adapter/shell wall-clock budget is at least 420 seconds.
    - The fresh child is immutable and contains one typed `sealed_eligible` or `sealed_non_promotion` disposition with complete restoration and cleanup evidence.
    - `sealed_eligible` has an unchanged immutable private-input digest plus a distinct commit-redacted candidate; `sealed_non_promotion` or a blocker has no candidate.
    - `36-08-SUMMARY.md` records only the actual closed disposition and leaves Plan 36-07 blocked unless the candidate is eligible.
    - Shareable output contains only closed categories, booleans, bounded counts/durations, and public digests.
    - No retry, standalone detector, direct UART/pin/pad/probe/GPIO/jumper/solder action, mining/pool input, OTA, other-board action, self-test/active control, discovery scan, prior-root mutation, Plan 36-04 edit, or secret output occurs.
    - Plan 36-06 and Phase 35 roots and bytes remain unchanged; reference, redaction, and diff checks exit 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>

- Confirm exactly two Plan 36-08 tasks exist and the only hardware-mode invocation appears in Task 36-08-02.
- Require the exact graph `36-08@7`, `36-07@8`, `36-04@9`, with every remaining plan marked `gap_closure: true`.
- Require the ordered Rust gate, all 75 Just targets, fresh no-cache Phase 36 broker/evidence/deployed-process tests, deployed adapter/fake-detector/replay regressions, exact clean-current-HEAD package, software-only preflight, parity, reference, redaction, and diff checks.
- Require one broker-owned hardware invocation, one broker-owned detector invocation, at least 420 seconds of wall-clock budget for the 360-second capture, no retry, and offline-only inspection afterward.
- Require either eligible immutable-private-plus-distinct-redacted-candidate handoff or typed sealed non-promotion with no candidate and blocked Plan 36-07.
- Compare the exact Plan 36-06 and Phase 35 public roots and prohibit any Plan 36-04 canonical reconciliation.

</verification>

<success_criteria>

- The deployed adapter fix is exercised from a committed clean current HEAD through the real deployed boundary.
- The fresh attempt has exactly one truthful, immutable, privacy-safe disposition with complete chronology and cleanup.
- SYS-02, EVD-11, EVD-12, EVD-14, and EVD-15 receive an eligible offline handoff or remain explicitly blocked without overclaim.
- Plan 36-07 is runnable only for an eligible candidate; Plan 36-04 remains the sole Wave 9 independent checkpoint and reconciliation owner.
- Prior Plan 36-06 and Phase 35 roots, every non-claim, and all prohibited boundaries remain unchanged.

</success_criteria>

<output>
After Task 36-08-02, create `36-08-SUMMARY.md`. If the disposition is `sealed_eligible`, hand the immutable private input plus distinct redacted candidate to offline Wave 8 Plan 36-07. If it is a typed blocker or `sealed_non_promotion`, record the exact truthful non-promotion, create no candidate, and leave Plan 36-07 blocked. Do not push.
</output>
