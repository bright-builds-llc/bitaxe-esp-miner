---
phase: 36-substantive-evidence-admission-and-exact-re-promotion
plan: "09"
type: execute
wave: 8
depends_on:
  - "36-08"
autonomous: true
gap_closure: true
phase_lifecycle_id: 36-2026-07-23T15-20-53
lifecycle_mode: yolo
generated_by: gsd-plan-phase
generated_at: "2026-07-25T18:28:31Z"
requirements:
  - SYS-02
  - EVD-11
  - EVD-12
  - EVD-14
  - EVD-15
files_modified:
  - tools/parity/src/phase36_broker/contract.rs
  - tools/parity/src/phase36_broker/hardware.rs
  - tools/parity/src/phase36_broker/hardware_process.rs
  - tools/parity/src/phase36_broker/ledger.rs
  - tools/parity/src/phase36_broker/tests.rs
  - scripts/phase36-hardware-effect.sh
  - scripts/phase36-substantive-evidence-test.sh
  - tools/flash/src/main.rs
must_haves:
  truths:
    - "Recovery or restoration is authorized only after the exact-package flash boundary confirms a partial or completed device effect; parse, admission, authentication, invocation-construction, detector, and other pre-effect failures run no recovery."
    - "A no-device-effect transaction records recovery as not authorized or not required, performs bounded cleanup, and preserves the earliest typed failure."
    - "A confirmed partial exact-package flash failure may perform exactly one bounded same-image typed recovery before cleanup, while recovery failure remains secondary to the original flash failure."
    - "The real flash parser and the deployed runfiles/process boundary reproduce the Plan 36-08 pre-device CLI rejection and prove zero recovery."
    - "The software repair is transitively bound to commit `df9cb90008bf47f94434545021a47e237e0c5739` and passes the full ordered Rust and repository gates without hardware access."
  artifacts:
    - path: tools/parity/src/phase36_broker/contract.rs
      provides: "Closed device-effect boundary and recovery-disposition contracts"
    - path: tools/parity/src/phase36_broker/hardware.rs
      provides: "Transaction policy that authorizes recovery only after confirmed partial or completed device effect"
    - path: tools/parity/src/phase36_broker/ledger.rs
      provides: "Earliest-failure-preserving state machine with no-effect cleanup path"
    - path: tools/parity/src/phase36_broker/hardware_process.rs
      provides: "Deployed process classification of pre-effect and confirmed partial-flash outcomes"
    - path: scripts/phase36-substantive-evidence-test.sh
      provides: "Real parser and deployed runfiles/process regressions"
  key_links:
    - from: tools/parity/src/phase36_broker/hardware_process.rs
      to: scripts/phase36-hardware-effect.sh
      via: "closed effect-result contract rather than inference from a generic nonzero exit"
    - from: tools/parity/src/phase36_broker/hardware.rs
      to: tools/parity/src/phase36_broker/ledger.rs
      via: "confirmed partial-flash or completed-device-effect boundary is the sole transition that permits TypedRecovery"
      pattern: "TypedRecovery"
    - from: scripts/phase36-substantive-evidence-test.sh
      to: tools/flash/src/main.rs
      via: "real parser regression for the retired Plan 36-08 dual-plus-redaction arguments"
      pattern: "parse_cli"
    - from: df9cb90008bf47f94434545021a47e237e0c5739
      to: scripts/phase36-hardware-effect.sh
      via: "ancestry and deployed argument-shape regression"
---

<objective>
Close the residual Plan 36-08 recovery-authority defect entirely in software before another hardware attempt.

Purpose: ensure a pre-device software failure cannot trigger a restoration flash, while retaining one bounded same-image recovery path after a confirmed partial or completed device effect.
Output: a typed broker/effect contract, pure and deployed regressions, and `36-09-SUMMARY.md` for Wave 9 Plan 36-10.
</objective>

<execution_context>
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-CONTEXT.md
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-RESEARCH.md
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-VERIFICATION.md
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-08-SUMMARY.md
@.planning/debug/resolved/phase36-plan08-flash-and-recovery-failed.md
@docs/hardware/hardware-attempt-policy.md
@docs/parity/evidence-policy.md
@AGENTS.md
</execution_context>

<context>
Plan 36-08 is immutable non-promotion history: its primary failure was `flash_failed`, its recovery also failed, cleanup completed, no candidate exists, and device restoration remains unresolved. Do not edit Plan 36-08, its summary, its private root, or infer that recovery occurred on-device; the exact failure happened before device access because the deployed adapter constructed a CLI shape rejected by the real parser.

Commit `df9cb90008bf47f94434545021a47e237e0c5739` repaired that unsupported argument shape. This plan must retain and transitively test that repair while closing the separate authority defect that currently sends every failed operation through `TypedRecovery`.

This plan is software-only. Do not run detector, board-info, flash, monitor, serial, HTTP/WebSocket against a device, credentials, DEVICE_URL, network discovery, or any hardware command. Do not access or mutate private evidence. Preserve the Phase 35 root `0401e7b485df2d1ccfc67e63845f98b6217816a184901bf0595d03af3219757d`, the Plan 36-08 non-promotion, its absent candidate, and all existing non-claims.

No direct UART, pins, pads, headers, GPIO, test points, probes, jumpers, solder, injected signals, Phase 28.1.1 work, mining/pool work, OTA, other boards, active self-test/control, scans, security/signing/xattr/AMFI workaround, or prior-root mutation is authorized.
</context>

<threat-model>

## ASVS Level

OWASP ASVS Level 1. All high-severity threats are blocking.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Broker to effect adapter | A typed operation crosses from authorization/state control into a deployed process. |
| Flash process to broker | A closed device-effect result crosses back without trusting free-form stderr or exit status as proof of partial flash. |
| Failure state to recovery authority | Only a trusted partial-flash or completed-device-effect boundary may enter the recovery-required state. |
| Private operational result to tests/seal | Operational details remain private while public outcomes expose only closed categories and dispositions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-36-09-01 | Spoofing | Partial-flash confirmation | mitigate | Construct the recovery-authorizing type only from a trusted flash-stage boundary; generic exit status, parser text, shell text, and caller assertion cannot create it. |
| T-36-09-02 | Tampering | Ledger transition graph | mitigate | Pure transition tests reject recovery after every pre-effect failure and permit it only after confirmed partial flash or completed device effect. |
| T-36-09-03 | Repudiation | Recovery decision | mitigate | Seal a closed `not_authorized`, `not_required`, attempted-success, or attempted-failure disposition while preserving the first failure. |
| T-36-09-04 | Information Disclosure | Adapter/process diagnostics | mitigate | Keep ProtectedOperational values in owner-only files, never persist NeverPersistRaw, and expose only typed public categories without paths or command output. |
| T-36-09-05 | Denial of Service | Recovery loop | mitigate | At most one bounded same-image recovery is authorized, followed by cleanup; no retry or recursive restoration exists. |
| T-36-09-06 | Elevation of Privilege | Pre-effect failure recovery | mitigate | Parser, admission, authentication, invocation-construction, detector, and capture failures transition directly to cleanup and cannot construct recovery authority. |

</threat-model>

<tasks>

<task type="auto" id="36-09-01" tdd="true">
  <name>Make confirmed device effect the sole recovery authority</name>
  <read_first>
    - .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-08-SUMMARY.md
    - .planning/debug/resolved/phase36-plan08-flash-and-recovery-failed.md
    - tools/parity/src/phase36_broker/contract.rs
    - tools/parity/src/phase36_broker/hardware.rs
    - tools/parity/src/phase36_broker/ledger.rs
    - tools/parity/src/phase36_broker/tests.rs
    - tools/parity/src/phase36_broker/hardware_process.rs
    - docs/hardware/hardware-attempt-policy.md
    - docs/parity/evidence-policy.md
  </read_first>
  <files>
    - tools/parity/src/phase36_broker/contract.rs
    - tools/parity/src/phase36_broker/hardware.rs
    - tools/parity/src/phase36_broker/ledger.rs
    - tools/parity/src/phase36_broker/tests.rs
  </files>
  <behavior>
    - "Admission, authentication/capability, invocation-construction, parser, detector, and any other failure before a confirmed device effect produce no recovery operation and record recovery as not authorized or not required."
    - "An exact-package flash failure with no confirmed partial device effect produces no recovery operation."
    - "Only an exact-package flash failure carrying an unforgeable confirmed-partial-effect result permits exactly one TypedRecovery using the same admitted package/image identity."
    - "A later passive/read-only failure may permit exactly one same-image TypedRecovery only when the ledger already contains a trusted completed-device-effect boundary."
    - "The earliest failure remains primary when recovery or cleanup also fails; later failures are secondary only."
    - "Successful flash and passive observation complete without recovery; cleanup remains mandatory on every terminal path."
  </behavior>
  <action>
Per D-02, D-07 through D-10, D-13, and D-16, begin with failing Arrange/Act/Assert tests for the behavior above. Introduce the smallest closed contract that distinguishes no device effect, confirmed partial exact-package flash, and completed device effect, and records the recovery disposition. Keep the recovery-authorizing constructors private to the trusted execution boundary; a caller, generic `Result`, arbitrary string, nonzero exit, or failed operation must not be able to claim device effect.

Replace the ledger's current “every failed operation requires recovery” transition with explicit effect-aware routes: pre/no-effect failures go directly to cleanup with recovery `not_authorized` or `not_required`; confirmed partial exact-package flash and failures after a trusted completed-device-effect boundary may go to one recovery-required state, then cleanup. Enforce same admitted manifest/package/factory-image identity for the recovery call. Preserve the first failure through recovery and cleanup, and reject duplicate, reordered, post-close, or recovery-without-authority records.

Keep all effect ownership inside the broker. Do not broaden the operation allowlist, add a general command runner, use free-form errors as policy, or weaken single-use capability, append-only ledger, cleanup, privacy, timeout, board-205, or package-identity invariants. This task creates no hardware command and makes no restoration claim.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-parity --all-features phase36_broker &amp;&amp; bazel test --nocache_test_results //tools/parity:phase36_broker_tests &amp;&amp; git diff --check</automated>
  </verify>
  <done>The pure broker state machine can authorize recovery only after a confirmed partial or completed exact-package device effect and records every pre/no-effect path as closed non-authority/non-requirement.</done>
  <acceptance_criteria>
    - A table-driven test covers every operation and failure boundary, including admission, capability/authentication, detector, invocation construction, parser, exact flash before effect, passive observation, three read-only captures, recovery, and cleanup.
    - No failure before a trusted device-effect boundary appends an authorized/invoked TypedRecovery ledger record.
    - Confirmed partial flash appends exactly one TypedRecovery record set bound to the original package/image identity.
    - A passive/read-only failure after confirmed completed flash may append exactly one same-image TypedRecovery, while the same failure without the effect boundary cannot.
    - Primary and secondary failure assertions prove the earliest failure cannot be overwritten.
    - Invalid recovery authorization fails closed before process invocation.
  </acceptance_criteria>
</task>

<task type="auto" id="36-09-02" tdd="true">
  <name>Prove the real parser and deployed process boundary cannot recover before device effect</name>
  <read_first>
    - .planning/debug/resolved/phase36-plan08-flash-and-recovery-failed.md
    - scripts/phase36-hardware-effect.sh
    - scripts/phase36-substantive-evidence.sh
    - scripts/phase36-substantive-evidence-test.sh
    - tools/parity/src/phase36_broker/hardware_process.rs
    - tools/parity/src/phase36_broker/hardware.rs
    - tools/flash/src/main.rs
    - tools/parity/BUILD.bazel
    - scripts/BUILD.bazel
    - AGENTS.md
    - standards/core/verification.md
    - standards/core/testing.md
    - standards/languages/rust.md
  </read_first>
  <files>
    - tools/parity/src/phase36_broker/hardware_process.rs
    - scripts/phase36-hardware-effect.sh
    - scripts/phase36-substantive-evidence-test.sh
    - tools/flash/src/main.rs
  </files>
  <behavior>
    - "The exact retired Plan 36-08 `flash --evidence-mode dual --redact-evidence` shape is rejected by the real parser before device effect and the deployed broker records zero recovery invocations."
    - "The repaired argument shape from `df9cb90008bf47f94434545021a47e237e0c5739` remains accepted by the real parser."
    - "Admission, detector, parser, and adapter invocation-construction failures at the deployed runfiles boundary record recovery not authorized/not required and perform cleanup only."
    - "An injected closed confirmed-partial-flash result, or a later failure after an injected completed-device-effect boundary, authorizes exactly one same-image recovery and one cleanup without touching real hardware."
    - "Free-form stderr, exit codes, and fake adapter output cannot forge confirmed partial effect."
  </behavior>
  <action>
Per D-06 through D-10, D-13, D-16, D-17, D-23, and D-25, carry the pure contract through the deployed process shell. Bind the regression to repair commit `df9cb90008bf47f94434545021a47e237e0c5739` with an ancestry assertion and retain the supported argument-shape test. Add a real-parser regression for the exact retired dual-plus-redaction Plan 36-08 arguments and route that parser rejection through the deployed runfiles supervisor/broker/effect adapter. Assert detector ownership remains literal and single, no device-capable child is invoked after the parser/pre-effect failure, TypedRecovery count is zero, cleanup is complete, the earliest category remains the parser/invocation failure, and the closed recovery disposition is not-authorized/not-required.

Extend the closed adapter/process result so only a trusted, bounded flash-stage observation can report confirmed partial or completed device effect. Do not parse human stderr or equate “process launched” with device effect. Use synthetic runfiles/process doubles to prove one valid confirmed-partial exact flash invokes exactly one same-image typed recovery and cleanup, and that a later failure after a confirmed completed flash has the same bounded restoration authority; assert a mismatched image/package or forged output is rejected before recovery. Preserve the repaired exact/recovery command shape: board 205, admitted manifest and factory image, redacted evidence directory, Wi-Fi credentials only on exact flash, and no flash-monitor-only evidence-mode option.

Keep the supervisor child absent, the private parent `0700`, handle/siblings `0600`, NeverPersistRaw unwritten/unemitted, ProtectedOperational private, and public output limited to closed ShareableFact/PublicProvenance categories. Run no hardware, credentials, detector, network, or private-evidence command. Finish with the repository's ordered Rust gates and deployed no-cache tests.
  </action>
  <verify>
    <automated>git merge-base --is-ancestor df9cb90008bf47f94434545021a47e237e0c5739 HEAD &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; bazel test --nocache_test_results //tools/flash:tests //tools/parity:phase36_broker_tests //scripts:phase36_substantive_evidence_test &amp;&amp; just test &amp;&amp; just parity &amp;&amp; just verify-reference &amp;&amp; just verify-redaction &amp;&amp; git diff --check</automated>
  </verify>
  <done>The real parser and deployed runfiles/process tests prove Plan 36-08's pre-device failure performs zero recovery while a confirmed partial flash retains one bounded same-image recovery path.</done>
  <acceptance_criteria>
    - The regression uses the real `parse_cli` path for both the retired and repaired argument shapes.
    - Deployed process assertions distinguish parse/admission/invocation failures from confirmed partial or completed device effect without inspecting human output as authority.
    - Pre-effect scenarios have zero recovery invocations and a closed not-authorized/not-required disposition.
    - The synthetic confirmed-partial scenario has exactly one same-image recovery, one cleanup, and preserved primary failure.
    - No test reaches a real detector, USB device, serial port, credential file, DEVICE_URL, HTTP/WebSocket device origin, or network.
    - Ordered Rust gates, all named no-cache targets, parity, reference, redaction, and diff checks exit 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>

- Confirm `git merge-base --is-ancestor df9cb90008bf47f94434545021a47e237e0c5739 HEAD` succeeds.
- Confirm pure and deployed tests cover no-effect, confirmed-partial, completed-effect/later-failure, recovery-success, recovery-failure, and cleanup-failure paths while preserving the first failure.
- Confirm tests use the real parser and deployed runfiles/process boundaries, not a duplicated fake parser.
- Confirm no hardware/private-evidence/credential/network command ran and Plan 36-08 plus Phase 35 history remain unchanged.
- Run the mandatory Rust sequence in order, named no-cache Bazel/process tests, full repository test, parity, reference, redaction, and diff checks.

</verification>

<success_criteria>

- Recovery authority is impossible before a confirmed partial or completed exact-package device effect.
- No-effect failures record recovery as not authorized/not required and clean up without restoration.
- Confirmed partial flash retains one bounded same-image recovery path and earliest-failure preservation.
- The repair commit is transitively guarded at pure, real-parser, and deployed process boundaries.
- Plan 36-09 remains software-only and hands a clean committed source to Wave 9 Plan 36-10.

</success_criteria>

<output>
After both tasks complete, create `36-09-SUMMARY.md`. Use atomic task commits, do not push, and return control so Wave 9 Plan 36-10 can run from a clean HEAD.
</output>
