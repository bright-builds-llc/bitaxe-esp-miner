---
phase: 36-substantive-evidence-admission-and-exact-re-promotion
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
phase_lifecycle_id: 36-2026-07-23T15-20-53
lifecycle_mode: yolo
generated_by: gsd-plan-phase
generated_at: "2026-07-23T15:45:00Z"
requirements:
  - EVD-11
  - EVD-12
  - EVD-14
files_modified:
  - tools/parity/src/phase36_evidence.rs
  - tools/parity/src/phase36_evidence/
  - tools/parity/src/main.rs
  - tools/parity/fixtures/phase36/
  - tools/parity/BUILD.bazel
  - scripts/phase36-evidence-test.sh
  - scripts/BUILD.bazel
must_haves:
  truths:
    - "Phase 36 has a successor schema that fingerprints but never rewrites or reinterprets the Phase 35 v1 root."
    - "Every missing, contradictory, self-attested, or privacy-unsafe input has a named fail-closed fixture before production admission is implemented."
    - "Protected-root process tests use synthetic values and prove terminal/shareable output cannot expose protected inputs."
    - "One aggregate immutable_artifacts_insufficient result closes the hermetic classification outcome while preserving every component insufficiency category."
    - "The shareable successor includes a privacy-safe versioned typed fact projection sufficient for independent reconstruction, not digests alone."
  artifacts:
    - path: tools/parity/src/phase36_evidence.rs
      provides: "Successor contract entrypoint and closed insufficiency vocabulary"
    - path: tools/parity/fixtures/phase36/
      provides: "Eligible-shape and one-field mutation fixtures using synthetic values"
    - path: scripts/phase36-evidence-test.sh
      provides: "Real-process protected-input, permission, redaction, and no-hardware regression harness"
  key_links:
    - from: tools/parity/src/phase36_evidence.rs
      to: tools/parity/src/phase35_evidence.rs
      via: "typed immutable Phase 35 root fingerprint and supersession reference"
    - from: scripts/phase36-evidence-test.sh
      to: tools/parity/src/main.rs
      via: "read-only explicit-root CLI exercised with synthetic protected canaries"
---

<objective>
Create Wave 0 validation scaffolding and the minimal versioned Phase 36 successor contract.

Purpose: make shallow, privacy-unsafe, history-rewriting, and hardware-fallback implementations impossible to pass later plans.
Output: closed domain skeleton, fixtures, named Cargo/Bazel targets, and a real-process synthetic protected-root harness.
</objective>

<execution_context>
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-CONTEXT.md
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-RESEARCH.md
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-VALIDATION.md
</execution_context>

<threat-model>

| ID | Threat | Severity | Required mitigation and verification |
| --- | --- | --- | --- |
| T36-01 | Protected values or local paths escape through fixtures, errors, terminal output, or public projections. | high | Synthetic canaries, explicit protected-root permissions, closed outputs, and real-process negative assertions. |
| T36-02 | The successor silently rewrites Phase 35 v1 semantics or mutates Attempt 31. | high | A distinct schema, typed immutable root fingerprint, no write surface, and byte-identity fixtures. |
| T36-06 | A failed classifier invokes hardware or target discovery. | high | The initial CLI is read-only and structural tests reject detector, credential, serial, HTTP mutation, and flash tokens. |

</threat-model>

<tasks>

<task type="auto" id="36-01-01">
  <name>Define the Phase 36 successor envelope and closed insufficiency types</name>
  <read_first>
    - .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-CONTEXT.md
    - .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-RESEARCH.md
    - tools/parity/src/phase35_evidence.rs
    - tools/parity/src/phase35_evidence/contract.rs
    - tools/parity/src/phase35_evidence/inventory.rs
    - tools/parity/src/main.rs
    - tools/parity/BUILD.bazel
  </read_first>
  <files>
    - tools/parity/src/phase36_evidence.rs
    - tools/parity/src/phase36_evidence/contract.rs
    - tools/parity/src/phase36_evidence/tests.rs
    - tools/parity/src/main.rs
    - tools/parity/BUILD.bazel
  </files>
  <action>
Create a new `phase36_evidence` module without changing `phase35-evidence-v1`. Define:

- schema constant `phase36-evidence-v1`;
- `Phase35RootReference` containing the immutable Phase 35 root digest, evidence-source commit, and Phase 35 generation digest;
- `Phase36EvaluationIdentity` containing current evaluator commit and successor contract digest;
- `Attempt31Sufficiency` as separate typed results for snapshot substance, runtime health, runtime identity observation, and independent effect observation;
- exact insufficiency variants `snapshot_substance_insufficient`, `runtime_health_insufficient`, `runtime_identity_observation_insufficient`, and `independent_effect_observation_insufficient`;
- `Phase36EvidenceEnvelope` that references immutable inputs and cannot embed or overwrite a Phase 35 root;
- `ImmutableArtifactAssessment` that emits aggregate `immutable_artifacts_insufficient` exactly when any requirement-bearing component is insufficient and retains the ordered component insufficiency categories;
- a versioned `ShareablePhase36FactsV1` containing the typed non-secret sensor states/values/stamps/reasons, runtime-health/checkpoint facts, provenance joins, observed runtime-identity categories, and independent effect-ledger categories needed to reconstruct every claim decision, plus claim digests. It must omit protected operational values and paths rather than replacing substantive facts with digests.

Add a read-only `classify-phase36-evidence --root <explicit-path>` CLI skeleton. It must reject missing/non-directory/symlink/world-readable protected roots, never discover other roots, never write below the input, never invoke detector/credentials/hardware/network mutations, and render no input paths or values in success/errors. Keep parsing and decisions pure; keep filesystem reads in a thin adapter.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-parity phase36_contract --all-features &amp;&amp; bazel test //tools/parity:phase36_evidence_tests &amp;&amp; git diff --check</automated>
  </verify>
  <done>The Phase 36 successor can describe immutable evidence sufficiency without changing Phase 35 or invoking effects.</done>
  <acceptance_criteria>
    - `phase36_evidence.rs` contains `phase36-evidence-v1`, `Phase35RootReference`, `Phase36EvaluationIdentity`, `Attempt31Sufficiency`, and `Phase36EvidenceEnvelope`.
    - Exact tests assert all four named insufficiency strings.
    - Tests assert aggregate `immutable_artifacts_insufficient` for each component insufficiency and assert no aggregate insufficiency only when every required component is sufficient.
    - `ShareablePhase36FactsV1` round-trips every non-secret claim field and one-field mutation tests prove each field changes the corresponding claim digest or eligibility outcome.
    - A test proves the Phase 35 root bytes and digest remain unchanged after classification.
    - The CLI source contains no detector, credential, flash, monitor, serial-control, HTTP mutation, or hardware-run invocation.
    - `bazel test //tools/parity:phase36_evidence_tests` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" id="36-01-02">
  <name>Add synthetic mutation fixtures and protected-root real-process guards</name>
  <read_first>
    - tools/parity/src/phase36_evidence.rs
    - tools/parity/src/phase35_evidence/tests/fixtures.rs
    - scripts/phase35-correlated-evidence-test.sh
    - docs/parity/evidence-policy.md
    - .planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-VALIDATION.md
    - scripts/BUILD.bazel
  </read_first>
  <files>
    - tools/parity/fixtures/phase36/
    - tools/parity/src/phase36_evidence/tests.rs
    - scripts/phase36-evidence-test.sh
    - tools/parity/BUILD.bazel
    - scripts/BUILD.bazel
  </files>
  <action>
Create a fixture factory and table-driven mutation suite with only synthetic digests, sessions, observations, and canaries. Cover unsupported schema, changed Phase 35 root/generation, changed evaluator identity, missing/extra/duplicate roles, unsafe paths, symlinks, wrong permissions, mixed evidence-source commits, package-derived runtime observation, supervisor-authored no-actuation, incomplete effect interval, contradictory sensor/health states, missing provenance joins, and partial public output.

Add `scripts/phase36-evidence-test.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. It must create one mode-0700 parent, a mode-0700 synthetic child, mode-0600 inputs, invoke exactly the read-only classifier, and assert protected canaries/paths never appear in stdout, stderr, or shareable files. Add a structural denylist for detector, credential, flash, monitor, serial-control, curl mutation, archived Phase 28.1.1, and hardware-run calls. Register named Bazel targets used by `36-VALIDATION.md`.
  </action>
  <verify>
    <automated>bazel test //tools/parity:phase36_evidence_tests //scripts:phase36_evidence_test &amp;&amp; just verify-redaction &amp;&amp; git diff --check</automated>
  </verify>
  <done>Wave 0 covers every reopened admission boundary with synthetic, privacy-safe, effect-free regressions.</done>
  <acceptance_criteria>
    - `tools/parity/fixtures/phase36/` contains no IP, MAC, URL, SSID, device path, account, worker, credential, token, or secret-shaped value.
    - The mutation table has an exact expected error/insufficiency category for every listed boundary.
    - `scripts/phase36-evidence-test.sh` contains `set -euo pipefail` and verifies modes `0700` and `0600`.
    - The real-process test proves no synthetic protected canary or input path reaches stdout, stderr, or public output.
    - Both named Bazel targets exit 0 without hardware.
  </acceptance_criteria>
</task>

</tasks>

<verification>

- Run both task commands.
- Run `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`.
- Run `just verify-redaction`, `just verify-reference`, and `git diff --check`.
- Review the diff for any Phase 35 mutation or effectful hardware path; either fails the plan.

</verification>

<success_criteria>

- EVD-11 has a distinct immutable successor envelope.
- EVD-12 and EVD-14 have named fixture boundaries before production admission work.
- Protected inputs cannot escape through the CLI or fixtures.
- No hardware authority is introduced.

</success_criteria>

<output>
After completion, create `36-01-SUMMARY.md`; use atomic task commits and do not push.
</output>
