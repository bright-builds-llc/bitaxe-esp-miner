---
phase: 36-substantive-evidence-admission-and-exact-re-promotion
plan: "05"
type: execute
wave: 5
depends_on:
  - "36-03"
autonomous: true
gap_closure: true
phase_lifecycle_id: 36-2026-07-23T15-20-53
lifecycle_mode: yolo
generated_by: gsd-plan-phase
generated_at: "2026-07-24T20:02:44Z"
requirements:
  - SYS-02
  - EVD-11
  - EVD-12
  - EVD-14
files_modified:
  - tools/parity/src/phase36_broker.rs
  - tools/parity/src/phase36_broker/
  - tools/parity/src/phase36_evidence/capture.rs
  - tools/parity/src/phase36_evidence/tests/
  - tools/parity/src/main.rs
  - tools/parity/fixtures/phase36-broker/
  - tools/parity/BUILD.bazel
  - scripts/phase36-substantive-evidence.sh
  - scripts/phase36-substantive-evidence-test.sh
  - scripts/BUILD.bazel
  - Justfile
must_haves:
  truths:
    - "Every device-capable operation in a Phase 36 attempt is constructible and executable only by one exclusive broker capability."
    - "The broker appends a typed, hash-chained authorization and completion record before and after each operation, and the supervisor cannot author or bypass that ledger."
    - "The capture contract obtains substantive sensor, runtime-health, runtime-identity, and same-snapshot API/WebSocket/retained facts without active control or claim inference."
    - "Pure mutation tests and real fresh-process/runfiles tests pass before any Phase 36 hardware attempt is permitted."
    - "Attempt 31, the immutable Phase 35 root, and every existing non-claim remain byte-identical."
  artifacts:
    - path: tools/parity/src/phase36_broker.rs
      provides: "Closed broker capabilities, effect state machine, and append-only ledger contract"
    - path: tools/parity/src/phase36_evidence/capture.rs
      provides: "Exact substantive capture and admission contract"
    - path: scripts/phase36-substantive-evidence.sh
      provides: "Thin protected-root supervisor with preflight and hardware modes"
    - path: scripts/phase36-substantive-evidence-test.sh
      provides: "Real-process, socket-framing, runfiles, privacy, and bypass regressions"
  key_links:
    - from: scripts/phase36-substantive-evidence.sh
      to: tools/parity/src/phase36_broker.rs
      via: "one fresh-process broker capability; the shell never receives a port, DEVICE_URL, or effect adapter"
      pattern: "phase36.*broker"
    - from: tools/parity/src/phase36_broker/ledger.rs
      to: tools/parity/src/phase36_evidence/effects.rs
      via: "sealed typed ledger admitted as ValidatedIndependentEffectInterval"
      pattern: "ValidatedIndependentEffectInterval"
    - from: tools/parity/src/phase36_evidence/capture.rs
      to: tools/parity/src/phase36_evidence/substance.rs
      via: "exact API, WebSocket, and retained fact bundle with one boot/revision join"
      pattern: "SubstantiveSnapshotJoin"
    - from: tools/parity/src/phase36_evidence/capture.rs
      to: tools/parity/src/phase36_evidence/runtime_identity.rs
      via: "independently observed same-device runtime identity and exact-package join input"
      pattern: "ValidatedObservedRuntimeIdentity"
---

<objective>
Build and software-verify the exclusive capability broker, append-only typed effect ledger, and exact Phase 36 substantive capture/admission harness.

Purpose: close the mechanism gaps identified by independent verification before consuming another hardware attempt, while making unledgered effects and unsupported claims structurally impossible.
Output: pure typed broker/capture contracts, a thin repo-owned supervisor, and real cross-process/runfiles tests that pass without USB, credentials, target discovery, or hardware.
</objective>

<execution_context>
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-CONTEXT.md
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-RESEARCH.md
@.planning/phases/36-substantive-evidence-admission-and-exact-re-promotion/36-VERIFICATION.md
@docs/parity/evidence-policy.md
@docs/hardware/hardware-attempt-policy.md
@AGENTS.md
</execution_context>

<context>
The immutable Phase 35 root digest is `0401e7b485df2d1ccfc67e63845f98b6217816a184901bf0595d03af3219757d`. Plans 36-01 through 36-03 already implement fail-closed substantive, runtime-identity, independent-effect, and claim-specific admission, but the authoritative generation lacks eligible facts. This plan creates the only future effect path; it does not run it.

Plan 36-04 remains paused at independent `gaps_found` verification. Do not edit or resume it in this plan.
</context>

<threat-model>

## ASVS Level

OWASP ASVS Level 1. All high-severity threats are blocking.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Supervisor to broker | The low-authority shell crosses into the sole process allowed to own detector, serial, flash, HTTP, WebSocket, and cleanup adapters. |
| Broker to child adapter | Typed capability and operation data cross into an effect-capable subprocess; raw capabilities must not be inherited by unrelated descendants. |
| Private capture to classifier | Mode-0600 secret-sanitized facts cross into pure admission without terminal or shareable disclosure. |
| Private classifier to projection | Only ShareableFact and PublicProvenance cross into a distinct commit-redacted candidate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-36-05-01 | Spoofing | Broker capability socket | mitigate | Use a one-time typed capability bound to broker PID/process group, exact evaluator identity, attempt nonce, and protected-root inode; reject replay, wrong peer, or fresh-process parsing drift. |
| T-36-05-02 | Tampering | Append-only effect ledger | mitigate | Broker alone opens a mode-0600 `O_APPEND` ledger, hash-chains records, fsyncs transitions, and seals the final digest; children receive no writable ledger descriptor. |
| T-36-05-03 | Repudiation | Effect interval | mitigate | Record authorization before invocation and result/closure after invocation with monotonic sequence, effect ID, bounded category, and earliest-failure preservation. |
| T-36-05-04 | Information Disclosure | Protected inputs and capture | mitigate | Remove NeverPersistRaw before first write, keep ProtectedOperational only in 0600 files below a 0700 ignored root, and emit only closed categories/digests. |
| T-36-05-05 | Denial of Service | Cross-process framing and cleanup | mitigate | Bound frames, payloads, deadlines, descendants, and file descriptors; test coalesced/fragmented socket writes and process-group cleanup. |
| T-36-05-06 | Elevation of Privilege | Supervisor or child bypass | mitigate | Closed effect enum, broker-exclusive adapters, structural source/runfiles denylist, and real-process tests reject direct espflash, curl, serial, detector, or nested build-runner execution outside the broker. |

</threat-model>

<tasks>

<task type="auto" id="36-05-01" tdd="true">
  <name>Define the exclusive broker capability and append-only typed ledger</name>
  <read_first>
    - AGENTS.md
    - standards/core/architecture.md
    - standards/core/testing.md
    - standards/languages/rust.md
    - .codex/tasks/lessons.md
    - tools/parity/src/phase36_evidence/effects.rs
    - tools/parity/src/phase36_evidence/contract.rs
    - tools/parity/src/main.rs
    - docs/parity/evidence-policy.md
    - docs/hardware/hardware-attempt-policy.md
  </read_first>
  <files>
    - tools/parity/src/phase36_broker.rs
    - tools/parity/src/phase36_broker/contract.rs
    - tools/parity/src/phase36_broker/ledger.rs
    - tools/parity/src/phase36_broker/tests.rs
    - tools/parity/src/main.rs
    - tools/parity/BUILD.bazel
  </files>
  <behavior>
    - "A capability is single-use, attempt-bound, exact-evaluator-bound, and accepted only from the expected fresh-process peer."
    - "Allowed operations are closed to exact package admission, one board-205 detector probe, exact-package flash, passive serial observation, read-only system-info, read-only WebSocket, read-only retained-fact capture, typed recovery, and cleanup."
    - "Active control, self-test execution, watchdog intervention, mining, credential inspection/promotion, OTA/recovery update flows, other boards, direct UART, pins, probes, and archived Phase 28.1.1 cannot be represented."
    - "Every operation has ordered Authorized, Invoked, Completed or Failed, and Closed records; missing, duplicate, reordered, unknown, unclosed, or post-close records fail admission."
    - "The first typed failure cannot be overwritten by recovery, cleanup, sealing, or reporting failures."
  </behavior>
  <action>
Per D-14 through D-18 and D-27, write failing unit tests first, then implement a pure broker contract and ledger reducer. Define `Phase36BrokerCapability`, `Phase36AllowedOperation`, `Phase36LedgerRecord`, `Phase36LedgerState`, `Phase36EffectInterval`, and closed failure/signature enums. Bind the capability to attempt ordinal, exact source/evaluator/package identities, broker process identity, protected-root identity, and expiry. Do not put any port, URL, credential, USB identity, PID, path, or free-form error in a shareable type.

The broker process must be the sole owner of detector, flash, serial, HTTP, WebSocket, recovery, and cleanup adapters. Open the private ledger mode 0600 with append-only semantics; append and fsync authorization before an operation, then append and fsync its typed result and closure. Hash-chain exact canonical bytes and seal one final digest after cleanup. Never give the supervisor or children a writable ledger handle. Preserve the earliest typed failure separately from secondary recovery/cleanup failures.

Implement illegal states as enums/newtypes, keep the reducer pure, and keep process/filesystem effects in thin adapters. Preserve `tools/parity/src/phase36_evidence/effects.rs` compatibility so a sealed complete ledger can construct `ValidatedIndependentEffectInterval`. Do not change Attempt 31, Phase 35 evidence, the current checklist, or any non-claim.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-parity phase36_broker --all-features &amp;&amp; bazel test //tools/parity:phase36_broker_tests &amp;&amp; git diff --check</automated>
  </verify>
  <done>The type system and ledger reducer permit only the closed passive Phase 36 operation set and authenticate one complete independently owned interval.</done>
  <acceptance_criteria>
    - `phase36_broker.rs` exports `Phase36BrokerCapability`, `Phase36AllowedOperation`, `Phase36LedgerRecord`, and `Phase36EffectInterval`.
    - Unit tests use Arrange/Act/Assert and cover one concern per test.
    - Tests reject replayed, expired, wrong-peer, wrong-attempt, wrong-source, wrong-evaluator, and wrong-package capabilities.
    - Tests reject every missing, duplicate, out-of-order, unknown, unclosed, and post-close ledger mutation.
    - Tests prove cleanup failure cannot replace an earlier detector, flash, capture, or admission failure.
    - No enum variant or adapter permits active control, self-test, watchdog intervention, mining, credentials, OTA, other boards, UART, pins, probes, or archived-lineage work.
    - `bazel test //tools/parity:phase36_broker_tests` exits 0 without hardware.
  </acceptance_criteria>
</task>

<task type="auto" id="36-05-02" tdd="true">
  <name>Implement the exact substantive capture and admission harness</name>
  <read_first>
    - tools/parity/src/phase36_broker.rs
    - tools/parity/src/phase36_evidence/substance.rs
    - tools/parity/src/phase36_evidence/runtime_identity.rs
    - tools/parity/src/phase36_evidence/effects.rs
    - tools/parity/src/operator_snapshot_evidence.rs
    - tools/device-session/src/model.rs
    - docs/hardware/esp-device-session.md
    - docs/parity/evidence-policy.md
    - scripts/phase35-correlated-evidence-root.sh
    - scripts/phase35-correlated-evidence-effects.sh
  </read_first>
  <files>
    - tools/parity/src/phase36_evidence/capture.rs
    - tools/parity/src/phase36_evidence/tests/capture.rs
    - tools/parity/src/main.rs
    - scripts/phase36-substantive-evidence.sh
    - Justfile
  </files>
  <behavior>
    - "API system-info, live WebSocket, and retained facts must share one exact boot session and operator-snapshot revision."
    - "Power current/voltage/wattage, temperature, and tachometer retain their independent state/value/sequence/stamp/reason semantics."
    - "Runtime health contains lifecycle, supervisor availability, checkpoint category/sequence/age, health/staleness, and unproved watchdog participation."
    - "Runtime identity is derived from observed device-session facts and agrees with exact source, reference, ELF, factory image, executable, manifest, and package identities."
    - "Zero or multiple fresh origin-only DEVICE_URL candidates stop before HTTP/WebSocket use."
    - "`inspect-candidate` is read-only, while `classify-candidate` is a distinct offline private-classification boundary."
  </behavior>
  <action>
Per D-04 through D-13 and D-16 through D-18, write failing pure capture/admission tests, then add the exact capture contract and a thin repo-owned `scripts/phase36-substantive-evidence.sh` supervisor with exactly five closed modes: `preflight`, `synthetic`, `hardware`, `inspect-candidate`, and `classify-candidate`. Register a `just phase36-substantive-evidence` recipe that exposes all five and rejects missing, duplicate, or unknown arguments.

All modes require `board=205`, `private-parent=<explicit ignored 0700 path>`, `attempt-handle-file=<explicit 0600 file below private-parent>`, and `candidate-output=<explicit commit-redacted path>`. Hardware additionally requires `capture-timeout-seconds>=360` and `wifi-credentials=<opaque path>`. Classification additionally requires `classification-output=<explicit 0600 path below private-parent>`.

`preflight` and `synthetic` remain effect-free. `inspect-candidate` resolves only the explicit handle, sealed child, and candidate; writes nothing; and emits only `candidate_eligible`, `candidate_non_promotable`, or `candidate_invalid` plus public digests. `classify-candidate` resolves those same explicit inputs, writes exactly the requested 0600 classification output, preserves private/candidate bytes, and emits only `classification_complete`, `classification_insufficient`, or `classification_invalid` plus public digests. Both offline modes perform zero detector, credential, USB, serial, target discovery, flash, monitor, DEVICE_URL, device-network, reboot, restoration, or hardware effects and never search for an attempt child.

The supervisor creates one mode-0700 parent below a path proven ignored by `git check-ignore`, proves the requested broker-owned child does not exist immediately before launch, and creates distinct mode-0600 wrapper stdout/stderr siblings outside that child. It passes one opaque capability to a fresh broker process and never receives a port, DEVICE_URL, credential content, device identity, or raw response.

Define one canonical private capture bundle containing secret-sanitized API system-info, live-WebSocket, retained correlation/runtime-health facts, device-session ledger/result, exact package manifest references, and broker ledger. The private classifier must validate actual sensor state/value/sequence/stamp/reason fields; the exact boot/revision join; runtime-health/checkpoint facts; independently observed source/reference/ELF/same-device facts; and the complete effect interval. Only after private classification succeeds may a separate commit-redacted candidate be derived. Never rewrite the private classifier input.

In hardware mode only, DEVICE_URL may be derived inside the broker from the same fresh detector and corresponding monitor session when exactly one origin-only candidate exists. It remains memory-only runtime input. Do not use mDNS, ARP, router state, scans, stale logs, or unrelated evidence. The harness must have no active-control, self-test, watchdog, mining, pool, credential-inspection, OTA, other-board, direct-UART, pin, probe, or archived-lineage path.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-parity phase36_capture --all-features &amp;&amp; bazel test //tools/parity:phase36_evidence_tests //tools/parity:phase36_broker_tests &amp;&amp; git diff --check</automated>
  </verify>
  <done>The harness can collect and privately admit every exact substantive fact required by SYS-02, EVD-11, EVD-12, and EVD-14 without exposing protected inputs or widening effects.</done>
  <acceptance_criteria>
    - `capture.rs` parses exact system-info, WebSocket, retained, device-session, package, and broker-ledger roles.
    - One-field mutation tests cover every sensor, health, checkpoint, watchdog, boot, revision, source, reference, ELF, package, device, interval, and ledger field.
    - Mixed boot/revision, package-derived runtime identity, supervisor-authored no-actuation, and compatibility-zero freshness each fail with a closed typed category.
    - The supervisor proves mode 0700/0600 permissions and child absence before broker launch.
    - The hardware branch invokes device effects only through the broker.
    - The private capture and commit-redacted candidate are distinct files and a test proves private input digest stability across derivation.
    - Justfile/Bazel runfiles tests invoke all five exact modes and reject missing or unknown arguments.
    - Tests prove `inspect-candidate` is read-only and `classify-candidate` writes only the explicit 0600 classification output with the stated closed categories.
  </acceptance_criteria>
</task>

<task type="auto" id="36-05-03">
  <name>Prove the broker and harness across real process, socket, runfiles, and cleanup boundaries</name>
  <read_first>
    - .codex/tasks/lessons.md
    - tools/parity/src/phase36_broker.rs
    - tools/parity/src/phase36_evidence/capture.rs
    - scripts/phase36-substantive-evidence.sh
    - scripts/phase36-evidence-test.sh
    - scripts/BUILD.bazel
    - tools/parity/BUILD.bazel
    - docs/parity/evidence-policy.md
  </read_first>
  <files>
    - tools/parity/fixtures/phase36-broker/
    - tools/parity/src/phase36_broker/tests.rs
    - tools/parity/src/phase36_evidence/tests/capture.rs
    - scripts/phase36-substantive-evidence-test.sh
    - scripts/BUILD.bazel
    - tools/parity/BUILD.bazel
  </files>
  <action>
Per D-09, D-17, and D-18, create synthetic-only fixtures and a real fresh-process harness. Exercise all five modes; Unix-socket frames delivered coalesced, fragmented, short, oversized, duplicated, reordered, and after close; fresh-process capability parsing; broker/client death; process-group descendants; file-descriptor inheritance; mode enforcement; exact runfiles helper resolution; and deployed Bazel execution. Production children must not invoke nested Bazel, Cargo, Just, or another build runner.

Use synthetic PTY/loopback HTTP/WebSocket/retained adapters only. Prove the supervisor cannot call `espflash`, detector, serial, curl/HTTP mutation, WebSocket, or device-session effects directly and that the broker rejects any undeclared adapter. Prove the broker ledger is append-only, child-inaccessible, hash-stable, and complete after cleanup. Prove stdout/stderr/public candidates contain no synthetic ProtectedOperational canary, NeverPersistRaw canary, private path, or raw command. Prove inspect changes no byte/mtime and classify writes only the explicit 0600 output while private/candidate digests remain unchanged.

Run all pure and cross-process targets before declaring the hardware precondition satisfied. Parse actual `phase-plan-index 36`, filter incomplete plans, sort by wave, and require exactly `36-05@5`, `36-06@6`, `36-07@7`, `36-04@8`; direct frontmatter reads must report `gap_closure: true` for all four. This verifies no lower incomplete non-gap plan without asserting unsupported `--gaps-only` fields. This task performs no detector call, USB/serial access, credential access, target discovery, flash, monitor, hardware, or network call outside synthetic loopback.
  </action>
  <verify>
    <automated>bazel test --nocache_test_results //tools/parity:phase36_broker_tests //tools/parity:phase36_evidence_tests //scripts:phase36_substantive_evidence_test &amp;&amp; cargo test -p bitaxe-parity phase36_broker --all-features &amp;&amp; cargo test -p bitaxe-parity phase36_capture --all-features &amp;&amp; just verify-redaction &amp;&amp; just verify-reference &amp;&amp; git diff --check</automated>
  </verify>
  <done>All pure and actual operating-system boundaries are green, making Plan 36-06 eligible to evaluate its exact preflight without asserting hardware success.</done>
  <acceptance_criteria>
    - The named Bazel targets execute fresh and exit 0.
    - Tests cross real Unix sockets, fresh processes, process groups, files, permissions, and Bazel runfiles rather than injecting production objects in process.
    - Structural tests prove only the broker owns effect-capable executable names and adapters.
    - Tests prove all descendants are reaped and no unexpected file descriptor or socket holder survives cleanup.
    - Real-process Justfile/runfiles tests exercise `mode=inspect-candidate` and `mode=classify-candidate` with their exact read/write/effect/output contracts.
    - Actual incomplete graph output sorted by wave is exactly `36-05@5,36-06@6,36-07@7,36-04@8`, and all four frontmatters have `gap_closure: true`.
    - No test invokes detector, USB, serial, credentials, target discovery, flash, monitor, or real hardware.
    - `just verify-redaction`, `just verify-reference`, and `git diff --check` exit 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>

- Run the three task verification commands.
- Run the Rust pre-commit sequence in order: `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`.
- Run `just test`, `just verify-redaction`, `just verify-reference`, `just parity`, and `git diff --check`.
- Compare the Phase 35 evidence directory against HEAD and require no byte change; verify the root digest remains `0401e7b485df2d1ccfc67e63845f98b6217816a184901bf0595d03af3219757d`.
- Confirm no hardware, detector, credential, USB, serial, target discovery, direct UART/pin, archived Phase 28.1.1, or canonical reconciliation action occurred.

</verification>

<success_criteria>

- The only effect-capable Phase 36 path is an exclusive broker with a typed append-only ledger.
- Exact substantive capture/admission is complete and privacy-preserving.
- Pure and cross-process tests pass before hardware.
- Attempt 31, Phase 35 bytes, Plan 36-04, checklist truth, and all non-claims remain unchanged.

</success_criteria>

<output>
After completion, create `36-05-SUMMARY.md`; use atomic task commits, do not push, and do not run Plan 36-06 hardware automatically from this plan.
</output>
