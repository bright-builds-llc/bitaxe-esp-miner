---
generated_by: gsd-discuss-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1.6-2026-07-09T13-37-00
generated_at: 2026-07-09T13:37:00Z
---

# Phase 28.1.1.6: BM1366 Version-Rolling Negotiation Nonce-Production Diagnosis - Context

**Gathered:** 2026-07-09
**Status:** Ready for planning
**Mode:** YOLO recommended decisions accepted

<domain>
## Phase Boundary

This phase consumes `next_hypothesis: version_rolling_negotiation` from Phase 28.1.1.5. Enumerate/count/address markers match; ASIC-256 ticket mask and RX-loop wire parity are retained; cores remain UART-alive but idle (`power_delta` falling; no `result_correlated` / fake-pool submit).

`mining.configure` already lands and the negotiated mask is stored in `LiveStratumRuntime`, but work-field construction discards `maybe_version_mask`. The 11/11 job-field match covers **base notify `version` bytes only**, not rolled `version_bits` / negotiated-mask semantics.

Out of scope: Phase 29/30 checklist `verified` promotion; job-byte patches; reopening falsified levers; ReadChipId `0x0A` byte patch; implementing ASIC mask reload as a second speculative patch in the same phase.

</domain>

<decisions>
## Implementation Decisions

### Evidence ladder
- **D-01:** Retain 28.1.1.1–28.1.1.5 facts: job 11/11 (base version); below-job 12/12; register-read poll / long-block RX / ticket-mask sole-blocker / chip-enumerate RX-loop sole-blocker falsified; ASIC-256 + RX-loop wire retained; no Rust `result_correlated` / fake-pool submit.
- **D-02:** Re-scope: configure sent + mask stored is insufficient; work build must apply negotiated mask; 11/11 does not prove rolled-version semantics.
- **D-03:** Ultra 205 detector-gated; hardware A/B unblocked.

### Primary hypothesis
- **D-04:** First investigation is `negotiated_version_mask_work_field_parity` — stop discarding `maybe_version_mask` in `crates/bitaxe-stratum/src/v1/mining.rs` and apply it to work-field / midstate path with pure-crate fixtures.
- **D-05:** If A/B unchanged after mask is applied to work fields, do not invent a second patch; set `next_hypothesis: pool_negotiated_mask_asic_reload` (or evidence-named narrower).
- **D-06:** Fast feedback: `asic_probe=power_delta` → `power_delta_class`. Phase gate remains `result_correlated` + `fake_pool_submit_observed`.

### Comparator (first deliverable)
- **D-07:** Redaction-safe version-rolling comparator: configure observed, mask_stored, mask_applied_to_work, version_mask_tx class, version_bits_on_submit_path, power_delta, correlate/submit; no raw hex in commits.
- **D-08:** `recommended_investigation` closed set: `negotiated_version_mask_work_field_parity` | `pool_negotiated_mask_asic_reload` | `none`. HARD BAN: never emit `post_max_baud_delay_2000`, `match_upstream_register_read_poll`, `upstream_like_long_block_receive`, `ticket_mask_asic_difficulty`, `count_asic_chips_rx_loop_parity`.
- **D-09:** Prefer composing existing scripts over a parallel capture stack.
- **D-10:** Optional compact firmware/stratum markers (counts only, no hex); skip if existing logs suffice.

### A/B and patch policy
- **D-11:** Exactly one focused patch for negotiated-mask work-field application + one detector-gated ≥360 s fake-pool A/B labeled `negotiated_version_mask_work_field_parity`.
- **D-12:** Promote default only if `ab_outcome: improved` with `result_correlated` and/or `fake_pool_submit_observed: true`.
- **D-13:** One confirmed divergence at a time; investigation flags off-by-default; no speculative flag spam.
- **D-14:** Bans: no job-byte patches; no falsified investigation defaults; retain RX-loop + ASIC-256; no Phase 30 checklist `verified` edits; no ReadChipId `0x0A` patch; no second speculative ASIC-reload patch this phase.

### Hardware and verification
- **D-15:** `just detect-ultra205`, `just verify-reference`, board 205, ≥360 s, wall clock ≥420 s; fake-pool preferred (already returns version-rolling.mask).
- **D-16:** Never modify `reference/esp-miner`; scratch-only upstream helpers.
- **D-17:** `passed` only with `result_correlated` + share-submission marker; else `gaps_found` / `blocked_safe_prerequisite`. Phase 30 promotion deferred.

### the agent's Discretion
- Exact comparator field names and whether compact markers ship in Plan 01 vs 02.
- How negotiated mask maps onto BM1366 job `version` / midstate bytes — must match upstream semantics with fixtures, not invent a new encoding.
- If hardware blocked, record `blocked_safe_prerequisite` precisely.

</decisions>

<specifics>
## Specific Ideas

- Primary discard site: `let _ = maybe_version_mask;` in `crates/bitaxe-stratum/src/v1/mining.rs`.
- Mask already flows from configure / `mining.set_version_mask` into `LiveStratumRuntime` and into the work builder.
- Fake pool fixture returns `version-rolling.mask` — use source-work-aligned capture path.
- Keep raw logs under ignored `scratch/` or `.planning/phases/**/hardware-runs/`.

</specifics>

<canonical_refs>
## Canonical References

### Phase truth
- `.planning/ROADMAP.md` — Phase 28.1.1.6 scope
- `.planning/STATE.md` — handoff from 28.1.1.5
- `.planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-final-evidence-redacted.md`
- `.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/NEXT-AGENT-PROMPT.md`
- `.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-wire-diff-summary.md`

### Tooling
- `scripts/phase28.1.1.5-chip-enumerate-compare.mjs`
- `scripts/phase28.1.1.4-init-sequencing-compare.mjs`
- `scripts/phase28.1.1.1-source-work-aligned-capture.sh`
- `scripts/phase28.1.1.1-fake-stratum-pool.mjs`
- `scripts/phase27-live-hardware-bridge-package.sh`

### Code
- `crates/bitaxe-stratum/src/v1/mining.rs` — mask discard
- `crates/bitaxe-stratum/src/v1/live_runtime.rs` — configure + mask store
- `crates/bitaxe-asic/src/bm1366/init_plan.rs` — hardcoded ASIC mask
- `crates/bitaxe-asic/src/bm1366/result.rs` — version_bits
- `firmware/bitaxe/src/live_stratum_runtime.rs` — power_delta

### Policy
- `AGENTS.md` — detector gate, redaction, timeouts
- `docs/adr/0012-parity-verification-evidence.md`
- `docs/parity/checklist.md` — no verified promotion here

</canonical_refs>
