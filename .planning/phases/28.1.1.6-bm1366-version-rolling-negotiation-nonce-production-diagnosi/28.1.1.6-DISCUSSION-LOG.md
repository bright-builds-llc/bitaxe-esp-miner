---
generated_by: gsd-discuss-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1.6-2026-07-09T13-37-00
generated_at: 2026-07-09T13:37:00Z
---

# Phase 28.1.1.6 Discussion Log

**Mode:** YOLO — recommended decisions auto-accepted (no interactive review).

## Gray areas considered

1. Is `mining.configure` already enough, or is work-field application the gap?
2. Does 11/11 job match already cover version rolling?
3. Forced first A/B label?
4. Fail-closed next hypothesis if falsified?

## Recommended answers (accepted)

1. **Work-field application is the gap** — configure + store already land; `mining.rs` discards `maybe_version_mask`.
2. **No** — 11/11 covers base notify `version` bytes only, not rolled `version_bits` / negotiated-mask semantics.
3. **`negotiated_version_mask_work_field_parity`** as forced first A/B.
4. **`pool_negotiated_mask_asic_reload`** as default next_hypothesis if falsified after mask is applied to work fields (unless evidence points elsewhere). Do not implement ASIC reload in this phase.

## Carry-forward bans

- Job-byte field patches
- `post_max_baud_delay_2000`
- `match_upstream_register_read_poll`
- `upstream_like_long_block_receive`
- `ticket_mask_asic_difficulty` as sole blocker (ASIC-256 retained)
- `count_asic_chips_rx_loop_parity` as sole blocker (RX-loop retained)
- ReadChipId `0x0A` byte patch
- Phase 30 checklist `verified` edits
- Second speculative patch (ASIC reload) in the same phase

## Hardware policy

Detector-gated Ultra 205; ≥360 s fake-pool preferred; wall clock ≥420 s; redaction-safe commits only.
