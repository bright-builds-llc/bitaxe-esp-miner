# Phase 28.1.1.6 Patch Disposition

ab_label: negotiated_version_mask_work_field_parity
ab_outcome: unchanged
result_correlated: false
fake_pool_submit_observed: false
configure_observed: false
mask_stored_class: unavailable
mask_applied_to_work: true
job_version_field_class: base_notify
power_delta_class: falling
wire_parity_ticket_mask_retained: true
wire_parity_rx_loop_retained: true
patch_disposition: falsified_negotiated_version_mask_work_field_parity_as_sole_blocker
wire_parity_mask_on_work_retained: true
next_hypothesis: pool_negotiated_mask_asic_reload
second_speculative_patch_applied: false
asic_mask_reload_applied: false
job_byte_patch_applied: false
post_max_baud_delay_2000_reintroduced: false
register_read_poll_defaulted: false
upstream_like_long_block_receive_defaulted: false
ticket_mask_asic_difficulty_reopened_as_sole_blocker: false
count_asic_chips_rx_loop_parity_reopened_as_sole_blocker: false
phase30_promotion_input: pending
raw_bytes_committed: false
credential_contents_read: false
files_changed: []
default_promotion_skipped: true

## Gate application

Plan 02 A/B reported `ab_outcome: unchanged` with `result_correlated: false` and `fake_pool_submit_observed: false`.
Per D-12 / Plan 03 decision gate, `negotiated_version_mask_work_field_parity` is **not** promoted as a sole-blocker fix (`patch_disposition: falsified_negotiated_version_mask_work_field_parity_as_sole_blocker`).

Mask attach succeeded as wire-correct work metadata: `mask_applied_to_work: true`, `job_version_field_class: base_notify`. Cores remain idle (`power_delta_class: falling`). Per D-05, route `next_hypothesis: pool_negotiated_mask_asic_reload` without implementing ASIC mask reload in this phase.

Task 2 keep/revert: **KEEP** mask-on-MiningWork (`wire_parity_mask_on_work_retained: true`). A/B did not `regressed` correlate/submit relative to baseline, so Plan 02 revert is not warranted. Sole-blocker claim is falsified; upstream-like work-metadata parity retained. No second speculative patch.

## Next hypothesis (deferred)

`pool_negotiated_mask_asic_reload` — after negotiated mask is applied to MiningWork and hashing remains idle, runtime ASIC `SetVersionMask` / mask reload is the next highest remaining suspect (D-05).
Do **not** implement ASIC mask reload in this phase.

## Task 2 code policy verification

- Policy applied: keep mask-on-MiningWork (`wire_parity_mask_on_work_retained: true`); no revert.
- Code churn: none — Plan 02 `MiningWork.maybe_version_mask` attach retained as wire-correct default.
- ASIC-256 ticket-mask wire retained (`wire_parity_ticket_mask_retained: true`).
- RX-loop wire retained (`wire_parity_rx_loop_retained: true`).
- No ASIC mask reload / runtime `SetVersionMask` added this plan (`asic_mask_reload_applied: false`).
- No job-byte patch (`job_byte_patch_applied: false`).
- `git diff -- crates/bitaxe-stratum/src/v1/mining.rs` → empty (keep-only).
- `cargo test -p bitaxe-stratum mining` → 26 passed, 0 failed.

## Non-claims

- No job-byte patch (D-14).
- No ASIC mask reload implementation (D-05/D-14); `asic_mask_reload_applied: false`.
- No second speculative patch (`second_speculative_patch_applied: false`).
- No `post_max_baud_delay_2000` reintroduction (D-14).
- No register-read poll default-on (D-14).
- No upstream-like long-block RX default-on (D-14).
- No ticket-mask ASIC-difficulty reopened as sole blocker (D-14); ASIC-256 retained.
- No count-asic-chips RX-loop parity reopened as sole blocker (D-14); RX-loop retained.
- No Phase 30 checklist `verified` edits (D-14/D-17); `phase30_promotion_input: pending`.
- Sole-blocker claim for `negotiated_version_mask_work_field_parity` is falsified; wire-parity mask-on-MiningWork retained.
