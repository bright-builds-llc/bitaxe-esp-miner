---
phase: 27-live-hardware-asic-and-stratum-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firmware/bitaxe/src/mining_evidence_mode.rs
  - firmware/bitaxe/src/main.rs
  - firmware/bitaxe/src/asic_adapter.rs
  - firmware/bitaxe/src/asic_adapter/production.rs
  - firmware/bitaxe/src/live_stratum_runtime.rs
autonomous: true
requirements: [STR-08, ASIC-10]
user_setup: []
must_haves:
  truths:
    - "Phase 27 live hardware bridge is a distinct compile-time opt-in and remains fail-closed when mode or ack is missing or mismatched."
    - "Production UART peripherals survive boot gate and are available to the live socket runtime without re-routing through controlled_mining_runtime."
    - "Live socket runtime executes Bm1366ProductionCommand::SendProductionWork and ReadProductionResult only after pool-derived WorkQueued events and Phase 22 prerequisite readiness."
    - "Live path publishes Phase 24 redaction-safe asic_production_status markers and never reaches SendDiagnosticWork."
  artifacts:
    - path: "firmware/bitaxe/src/mining_evidence_mode.rs"
      provides: "Phase 27 mode/ack pair and fail-closed tests"
    - path: "firmware/bitaxe/src/asic_adapter/production.rs"
      provides: "Retained production UART executor for Bm1366ProductionCommand"
    - path: "firmware/bitaxe/src/live_stratum_runtime.rs"
      provides: "ASIC bridge sub-step with guarded dispatch execution"
    - path: "firmware/bitaxe/src/main.rs"
      provides: "Peripheral retention and Phase 27 startup branch"
  key_links:
    - from: "firmware/bitaxe/src/live_stratum_runtime.rs"
      to: "firmware/bitaxe/src/asic_adapter/production.rs"
      via: "maybe_production_command execution after GuardedMiningLoopInputs::plan"
      pattern: "execute_production_command|maybe_production_command"
    - from: "firmware/bitaxe/src/live_stratum_runtime.rs"
      to: "crates/bitaxe-stratum/src/v1/mining_loop.rs"
      via: "GuardedMiningLoopInputs with production registry from LiveStratumRuntime"
      pattern: "GuardedMiningLoopInputs"
    - from: "firmware/bitaxe/src/main.rs"
      to: "firmware/bitaxe/src/live_stratum_runtime.rs"
      via: "Phase 27 mode gate before maybe_start_after_network_setup bridge entry"
      pattern: "is_phase27_live_hardware_bridge"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 27-2026-07-05T14-51-50
generated_at: 2026-07-05T15:00:00Z
---

<objective>
Establish Phase 27 opt-in gates, retained production UART execution, and live-runtime BM1366 work dispatch from pool-derived notify state.

Purpose: Phase 24 production commands and Phase 25 socket shell exist but are not connected on hardware. This plan closes the dispatch half of the integration gap per D-01 through D-06 without routing live mining through controlled_mining_runtime.
Output: Phase 27 evidence mode, production UART executor, and live ASIC bridge dispatch step with redaction-safe status markers.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-live-hardware-asic-and-stratum-bridge/27-CONTEXT.md
@.planning/phases/27-live-hardware-asic-and-stratum-bridge/27-RESEARCH.md
@.planning/phases/24-bm1366-production-work-path/24-03-SUMMARY.md
@.planning/phases/25-live-stratum-runtime-and-safe-stop/25-02-SUMMARY.md
@firmware/bitaxe/src/controlled_mining_runtime.rs
@firmware/bitaxe/src/asic_adapter/status.rs
@crates/bitaxe-stratum/src/v1/mining_loop.rs
@crates/bitaxe-asic/src/bm1366/production.rs
@AGENTS.md
@standards/core/architecture.md
@standards/languages/rust.md

<interfaces>
From `crates/bitaxe-stratum/src/v1/mining_loop.rs`:
```rust
pub struct GuardedMiningLoopInputs {
    pub gate: MiningLoopGate,
    pub pool_defaults: Ultra205Defaults,
    pub source: GuardedMiningLoopSource,
    pub production_registry: ProductionWorkRegistry,
    pub runtime_state: MiningRuntimeState,
    pub maybe_nonce_observation: Option<ProductionNonceObservation>,
}
pub struct GuardedBm1366DispatchPlan {
    pub maybe_production_command: Option<Bm1366ProductionCommand>,
}
```

From `crates/bitaxe-asic/src/bm1366/production.rs`:
```rust
pub enum Bm1366ProductionCommand {
    SendProductionWork(ProductionWorkPayload),
    ReadProductionResult,
}
```

From `firmware/bitaxe/src/asic_adapter/status.rs`:
```rust
pub fn publish_production_asic_status(status: ProductionAsicStatus);
pub fn publish_production_asic_blocked_status(reason: ProductionAsicBlocker);
// Logs: asic_production_status=initialized|work_dispatched|result_correlated|fail_closed
```

Phase 25 `live_stratum_runtime.rs` already emits `LiveRuntimeEvent::WorkQueued` on notify; pump loop is socket-only today with `StepKind::Asic` label present but unused for UART effects.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 27-01-01: Add Phase 27 mode gate and production UART executor</name>
  <files>firmware/bitaxe/src/mining_evidence_mode.rs, firmware/bitaxe/src/main.rs, firmware/bitaxe/src/asic_adapter.rs, firmware/bitaxe/src/asic_adapter/production.rs</files>
  <read_first>
    - .planning/phases/27-live-hardware-asic-and-stratum-bridge/27-CONTEXT.md
    - .planning/phases/27-live-hardware-asic-and-stratum-bridge/27-RESEARCH.md
    - firmware/bitaxe/src/mining_evidence_mode.rs
    - firmware/bitaxe/src/main.rs
    - firmware/bitaxe/src/asic_adapter.rs
    - firmware/bitaxe/src/asic_adapter/uart.rs
    - crates/bitaxe-asic/src/bm1366/production.rs
    - crates/bitaxe-asic/src/bm1366/result.rs
  </read_first>
  <behavior>
    - Test 1: `MiningEvidenceMode::from_compile_env(Some(PHASE27_LIVE_HARDWARE_BRIDGE_MODE), Some(PHASE27_LIVE_HARDWARE_BRIDGE_ACK))` returns distinct `Phase27LiveHardwareBridge`; missing or mismatched values return `FailClosed`.
    - Test 2: Phase 27 mode does not match Phase 25 or Phase 21 mode pairs; only one live bridge variant is active per build.
    - Test 3: `ProductionAsicExecutor::execute(Bm1366ProductionCommand::SendProductionWork(..))` calls UART write_frame for each `adapter_actions()` entry and publishes `asic_production_status=work_dispatched`.
    - Test 4: `ProductionAsicExecutor::execute(Bm1366ProductionCommand::ReadProductionResult)` uses bounded read_exact + `parse_bm1366_result_frame` without logging raw frame bytes.
    - Test 5: `SendDiagnosticWork` is unreachable from production executor module (type-level boundary).
  </behavior>
  <action>
    Add per D-10 constants `PHASE27_LIVE_HARDWARE_BRIDGE_MODE: "phase27-live-hardware-asic-stratum-bridge"` and `PHASE27_LIVE_HARDWARE_BRIDGE_ACK: "ultra205-phase27-live-hardware-bridge-safe-stop"` plus enum variant `Phase27LiveHardwareBridge` and `is_phase27_live_hardware_bridge()`. Preserve existing Phase 21 and Phase 25 behavior; mismatched mode/ack must remain `FailClosed` with no silent fallback to Phase 25-only runtime.

    Refactor boot path per RESEARCH Pitfall 1: after `run_boot_gate_with_peripherals`, retain UART/reset/GPIO in a `ProductionAsicHandle` (static `OnceLock` or app-owned struct) instead of dropping `AsicUart` when boot gate returns. Export `production_asic_handle()` or equivalent accessor used only when Phase 27 mode is active.

    Create `firmware/bitaxe/src/asic_adapter/production.rs` with `ProductionAsicExecutor` that:
    - Accepts `Bm1366ProductionCommand` and `valid_jobs` slice for result parsing.
    - Maps commands through `adapter_actions()` and reuses `uart::AsicUart` write/read patterns from diagnostic path.
    - Publishes `ProductionAsicStatus::Initialized` only when boot gate reached production-ready boundary; otherwise fail-closed with `ProductionAsicBlocker::AsicInitFailed` per RESEARCH Assumption A1.
    - Never logs raw BM1366 frames, targets, extranonces, or share payloads.

    Wire `main.rs`: call Phase 27 starter `live_stratum_runtime::maybe_start_phase27_bridge_after_network_setup` only when `is_phase27_live_hardware_bridge()`; keep Phase 25 starter mutually exclusive. Do not route live socket mining through `controlled_mining_runtime`.
  </action>
  <verify>
    <automated>bazel test //crates/bitaxe-asic:tests && bazel build //firmware/bitaxe:firmware</automated>
  </verify>
  <acceptance_criteria>
    - `rg "PHASE27_LIVE_HARDWARE_BRIDGE_MODE|PHASE27_LIVE_HARDWARE_BRIDGE_ACK|Phase27LiveHardwareBridge|is_phase27_live_hardware_bridge" firmware/bitaxe/src/mining_evidence_mode.rs firmware/bitaxe/src/main.rs` finds mode gate symbols.
    - `rg "ProductionAsicExecutor|execute_production_command|asic_adapter/production" firmware/bitaxe/src/asic_adapter` finds production executor.
    - `! rg -n "SendDiagnosticWork" firmware/bitaxe/src/asic_adapter/production.rs` succeeds.
    - `bazel build //firmware/bitaxe:firmware` passes.
  </acceptance_criteria>
  <done>Phase 27 builds with distinct mode/ack, retained UART handle, and typed production command executor separate from diagnostic work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 27-01-02: Wire live ASIC bridge dispatch into socket pump loop</name>
  <files>firmware/bitaxe/src/live_stratum_runtime.rs</files>
  <read_first>
    - firmware/bitaxe/src/live_stratum_runtime.rs
    - firmware/bitaxe/src/controlled_mining_runtime.rs
    - crates/bitaxe-stratum/src/v1/live_runtime.rs
    - crates/bitaxe-stratum/src/v1/mining_loop.rs
    - crates/bitaxe-safety/src/mining_preconditions.rs
    - firmware/bitaxe/src/asic_adapter/status.rs
  </read_first>
  <behavior>
    - Test 1: On `WorkQueued`, firmware runs `GuardedMiningLoopInputs::plan` with `GuardedMiningLoopSource::Notify`, `maybe_nonce_observation: None`, and executes `maybe_production_command` when gate is Ready.
    - Test 2: Blocked `MiningLoopGate` produces no UART execution and publishes `asic_production_status=fail_closed reason={label} mining=disabled work_submission=disabled`.
    - Test 3: Dispatch emits only `Bm1366ProductionCommand::SendProductionWork`; live path never references `SendDiagnosticWork`.
    - Test 4: `publish_watchdog_checkpoint(StepKind::Asic, ..)` runs on each ASIC bridge iteration.
    - Test 5: Phase 25 safe-stop, prerequisite ordering, and socket-only behavior remain unchanged when Phase 27 mode is inactive.
  </behavior>
  <action>
    Extend `live_stratum_runtime.rs` per D-01, D-02, D-03, D-04, D-05, D-06, D-11:
    - Add `maybe_start_phase27_bridge_after_network_setup` mirroring Phase 25 prerequisite gate: evaluate `ProductionMiningPreconditions::decision()` before socket connect, pool settings access, or ASIC dispatch.
    - Track `maybe_pending_asic_bridge: bool` when `WorkQueued` fires; on next pump iteration run ASIC bridge sub-step before socket read when bridge is pending.
    - Build `GuardedMiningLoopInputs` from live runtime registry/state, `mining_loop_gate(preconditions)`, and `GuardedMiningLoopSource::Notify`.
    - Execute `plan()?.maybe_dispatch.and_then(|d| d.maybe_production_command)` through `ProductionAsicExecutor`; on `SendProductionWork` publish `asic_production_status=work_dispatched`; store dispatch `generation` for observation stamping in Plan 27-02.
    - On bridge blockers, publish `asic_production_status=fail_closed` with exact Phase 24 key format and converge on safe-stop path.
    - Preserve Phase 25 `pump_live_socket_until_cleanup` structure, watchdog yields, and safe-stop markers; ASIC step is additive, not a replacement socket shell.
    - Add firmware `#[cfg(test)]` fakes for production executor counter proving dispatch runs on WorkQueued and is skipped when gate blocked.
  </action>
  <verify>
    <automated>bazel test //crates/bitaxe-stratum:tests //crates/bitaxe-safety:tests && bazel build //firmware/bitaxe:firmware</automated>
  </verify>
  <acceptance_criteria>
    - `rg "maybe_start_phase27_bridge|StepKind::Asic|GuardedMiningLoopInputs|maybe_production_command|WorkQueued" firmware/bitaxe/src/live_stratum_runtime.rs` finds bridge wiring.
    - `rg "asic_production_status=work_dispatched|asic_production_status=fail_closed" firmware/bitaxe/src/live_stratum_runtime.rs firmware/bitaxe/src/asic_adapter/status.rs` finds status keys.
    - `! rg -n "SendDiagnosticWork" firmware/bitaxe/src/live_stratum_runtime.rs` succeeds.
    - `bazel build //firmware/bitaxe:firmware` passes.
  </acceptance_criteria>
  <done>Live socket runtime dispatches production BM1366 work from pool-derived notify inside the Phase 25 shell without controlled-runtime routing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| compile-time mode -> live bridge | Only explicit Phase 27 mode/ack may enable ASIC dispatch on live socket path. |
| Phase 22 gate -> UART dispatch | Production dispatch must not run when prerequisite readiness is blocked. |
| guarded plan -> UART executor | Only typed `Bm1366ProductionCommand` crosses into hardware effects. |
| UART -> retained logs | Firmware publishes category labels only; no raw frames or pool secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-27-01 | Elevation of Privilege | `mining_evidence_mode.rs` | mitigate | Distinct Phase 27 mode/ack; fail-closed on mismatch; mutually exclusive startup with Phase 25. |
| T-27-02 | Tampering | `GuardedMiningLoopInputs::plan` | mitigate | Gate runs before dispatch; blocked paths publish fail-closed ASIC status and disable submission. |
| T-27-03 | Elevation of Privilege | live dispatch path | mitigate | Type-level `Bm1366ProductionCommand` only; static check rejects `SendDiagnosticWork` in live bridge module. |
| T-27-04 | Information Disclosure | production UART logs | mitigate | Reuse Phase 24 status publishers; forbid raw frame/target/extranonce log patterns in acceptance scans. |
| T-27-05 | Denial of Service | ASIC bridge step | mitigate | `StepKind::Asic` watchdog checkpoints with existing 25 ms budget and yield semantics. |
</threat_model>

<verification>
Run `bazel test //crates/bitaxe-stratum:tests //crates/bitaxe-asic:tests //crates/bitaxe-safety:tests`.
Run `bazel build //firmware/bitaxe:firmware`.
Confirm `! rg -n "SendDiagnosticWork" firmware/bitaxe/src/live_stratum_runtime.rs firmware/bitaxe/src/asic_adapter/production.rs`.
</verification>

<success_criteria>
Phase 27 firmware can opt in with distinct mode/ack, retain production UART after boot, and dispatch `Bm1366ProductionCommand::SendProductionWork` from live notify-driven work inside `live_stratum_runtime` with redaction-safe ASIC status markers.
</success_criteria>

<output>
After completion, create `.planning/phases/27-live-hardware-asic-and-stratum-bridge/27-01-SUMMARY.md`.
</output>
