---
phase: 07-ota-filesystem-and-release-packaging
plan: 07
type: execute
wave: 4
depends_on: ["07-01", "07-04", "07-05"]
files_modified:
  - firmware/bitaxe/src/boot_validation.rs
  - firmware/bitaxe/src/ota_update.rs
  - firmware/bitaxe/src/http_api.rs
  - firmware/bitaxe/src/main.rs
  - firmware/bitaxe/sdkconfig.defaults
  - docs/parity/evidence/phase-07-ota-filesystem-release.md
autonomous: true
requirements: [REL-02, REL-03, REL-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 7-2026-06-28T13-30-15
generated_at: 2026-06-28T14:16:04Z
must_haves:
  truths:
    - "`/api/system/OTA` accepts valid firmware uploads only after access and AP-mode gates pass."
    - "Firmware OTA writes stream to the next OTA partition and schedules reboot only after validation/activation succeeds."
    - "`/api/system/OTAWWW` stays fail-closed with explicit REL-03 evidence until interruption testing exists."
    - "Boot validation logs pending/valid/rollback states for ESP-IDF rollback evidence."
  artifacts:
    - path: "firmware/bitaxe/src/ota_update.rs"
      provides: "ESP-IDF OTA streaming adapter"
    - path: "firmware/bitaxe/src/boot_validation.rs"
      provides: "Rollback-capable boot validation adapter"
    - path: "firmware/bitaxe/src/http_api.rs"
      provides: "Runtime update route handlers"
  key_links:
    - from: "firmware/bitaxe/src/http_api.rs"
      to: "firmware/bitaxe/src/ota_update.rs"
      via: "`handle_firmware_ota_update`"
      pattern: "handle_firmware_ota"
    - from: "firmware/bitaxe/src/main.rs"
      to: "firmware/bitaxe/src/boot_validation.rs"
      via: "startup boot validation call"
      pattern: "validate_boot"
---

<objective>
Implement firmware OTA upload/apply behavior, rollback boot validation, and the OTAWWW explicit gap runtime path.

Purpose: Move firmware OTA from fail-closed placeholder to a guarded ESP-IDF adapter while preventing unverified OTAWWW static-update parity claims.
Output: OTA streaming adapter, boot validation adapter, update route handlers, SDK rollback config, and evidence updates.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ota-filesystem-and-release-packaging/07-CONTEXT.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-RESEARCH.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-01-SUMMARY.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-04-SUMMARY.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-05-SUMMARY.md
@firmware/bitaxe/src/http_api.rs
@firmware/bitaxe/src/main.rs
@firmware/bitaxe/sdkconfig.defaults
@reference/esp-miner/main/http_server/http_server.c lines 1052-1203

<interfaces>
Created by Plan 07-01:
```rust
pub fn plan_update_request(input: UpdateRequestInput) -> UpdateRequestDecision;
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add ESP-IDF rollback boot validation</name>
  <read_first>
    firmware/bitaxe/src/main.rs
    firmware/bitaxe/sdkconfig.defaults
    .planning/phases/07-ota-filesystem-and-release-packaging/07-RESEARCH.md Pitfall 1
  </read_first>
  <files>firmware/bitaxe/src/boot_validation.rs, firmware/bitaxe/src/main.rs, firmware/bitaxe/sdkconfig.defaults, docs/parity/evidence/phase-07-ota-filesystem-release.md</files>
  <behavior>
    - At boot, log `ota_boot_validation=not_pending` when the running image is not pending verification.
    - When pending verification and startup diagnostics pass, call ESP-IDF valid-without-rollback API and log `ota_boot_validation=marked_valid`.
    - When diagnostics fail, call ESP-IDF invalid-and-reboot API and log `ota_boot_validation=marked_invalid_reboot`.
    - Enable rollback-capable SDK config explicitly.
  </behavior>
  <action>Create `boot_validation.rs` around `esp_ota_get_running_partition`, `esp_ota_get_state_partition`, `esp_ota_mark_app_valid_cancel_rollback`, and `esp_ota_mark_app_invalid_rollback_and_reboot` through `esp_idf_svc::sys`. Call the adapter during startup after core safe-state diagnostics and before normal route serving. Add SDK config for rollback support, including `CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE=y` if accepted by the pinned ESP-IDF configuration. Record host/compile evidence and hardware evidence status in `docs/parity/evidence/phase-07-ota-filesystem-release.md`; do not mark rollback verified without live evidence.</action>
  <verify>
    <automated>cargo build -p bitaxe-firmware --target xtensa-esp32s3-espidf</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "esp_ota_get_state_partition|esp_ota_mark_app_valid_cancel_rollback|esp_ota_mark_app_invalid_rollback_and_reboot|ota_boot_validation=marked_valid|ota_boot_validation=marked_invalid_reboot|ota_boot_validation=not_pending" firmware/bitaxe/src/boot_validation.rs firmware/bitaxe/src/main.rs`
    `rg -n "CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE" firmware/bitaxe/sdkconfig.defaults`
    `cargo build -p bitaxe-firmware --target xtensa-esp32s3-espidf` passes.
  </acceptance_criteria>
  <done>Rollback-capable boot validation is compiled and logged, with evidence kept below verified until hardware proof exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace firmware OTA fail-closed handler with guarded streaming adapter</name>
  <read_first>
    crates/bitaxe-api/src/update_plan.rs
    firmware/bitaxe/src/http_api.rs
    reference/esp-miner/main/http_server/http_server.c lines 1131-1203
  </read_first>
  <files>firmware/bitaxe/src/ota_update.rs, firmware/bitaxe/src/http_api.rs, firmware/bitaxe/src/main.rs</files>
  <action>Create `ota_update.rs` with `FirmwareOtaStatus`, `FirmwareOtaApplyResult`, and `stream_firmware_ota(request, status_sink)` around `esp_ota_get_next_update_partition`, `esp_ota_begin`, streaming `httpd_req_recv` chunks no larger than 1000 bytes, `esp_ota_write`, `esp_ota_end`, and `esp_ota_set_boot_partition`. In `http_api.rs`, replace `/api/system/OTA` use of `handle_unsupported_update` with a handler that calls `plan_update_request`, rejects unauthorized/AP-mode decisions with exact public bodies, records statuses `Starting...`, `Working ({percent}%)`, `Protocol Error`, `Write Error`, `Validation Error`, and `Rebooting...`, returns text/plain `Firmware update complete, rebooting now!`, and schedules `esp_restart()` after the response. Abort OTA on write/protocol failures. Do not buffer the full firmware image in RAM.</action>
  <verify>
    <automated>cargo build -p bitaxe-firmware --target xtensa-esp32s3-espidf</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "esp_ota_get_next_update_partition|esp_ota_begin|esp_ota_write|esp_ota_end|esp_ota_set_boot_partition|esp_ota_abort|Firmware update complete, rebooting now!|Validation / Activation Error|Rebooting" firmware/bitaxe/src/ota_update.rs firmware/bitaxe/src/http_api.rs`
    `cargo build -p bitaxe-firmware --target xtensa-esp32s3-espidf` passes.
  </acceptance_criteria>
  <done>`/api/system/OTA` is a guarded streaming ESP-IDF adapter matching D-11 through D-13 public behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire OTAWWW explicit gap route and evidence</name>
  <read_first>
    crates/bitaxe-api/src/update_plan.rs
    firmware/bitaxe/src/http_api.rs
    docs/parity/evidence/phase-07-ota-filesystem-release.md
    .planning/phases/07-ota-filesystem-and-release-packaging/07-CONTEXT.md D-14 through D-17
  </read_first>
  <files>firmware/bitaxe/src/http_api.rs, docs/parity/evidence/phase-07-ota-filesystem-release.md</files>
  <action>Replace `/api/system/OTAWWW` use of the Phase 5 generic unsupported handler with a Phase 7-specific handler that calls `plan_update_request(UpdateRouteKind::AxeOsStaticOtaWww)`, preserves unauthorized and AP-mode rejection strings, and otherwise returns the explicit D-16 gap public response status 400 body `Wrong API input`. Log `otawww_update=gap reason=interruption_evidence_missing owner=phase-07-release`. Update evidence with REL-03 status: explicit V1 parity gap, owner `phase-07-release`, release impact, and follow-up path. Record that the D-15 full whole-`www` update path is not claimed because interruption/recovery hardware evidence is not scheduled in this plan set. Do not implement whole-partition SPIFFS erase/write in this plan.</action>
  <verify>
    <automated>cargo test -p bitaxe-api --all-features update_plan && cargo build -p bitaxe-firmware --target xtensa-esp32s3-espidf</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "otawww_update=gap|interruption_evidence_missing|Wrong API input|AxeOsStaticOtaWww|Not allowed in AP mode" firmware/bitaxe/src/http_api.rs docs/parity/evidence/phase-07-ota-filesystem-release.md`
    `cargo test -p bitaxe-api --all-features update_plan` passes.
    `cargo build -p bitaxe-firmware --target xtensa-esp32s3-espidf` passes.
  </acceptance_criteria>
  <done>REL-03 is satisfied as an explicit fail-closed gap under D-16, without claiming OTAWWW verified parity.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser-upload-to-ota | Untrusted firmware bytes stream into ESP-IDF OTA APIs. |
| bootloader-to-app | ESP-IDF rollback state controls whether a new app remains active. |
| browser-upload-to-otawww | Static update upload attempt crosses into fail-closed policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | T/D | Firmware OTA activation | mitigate | Use `esp_ota_end`, `esp_ota_set_boot_partition`, rollback boot validation, and error statuses before reboot. |
| T-07-07-02 | S/E | OTA route access | mitigate | Preserve private-network/origin gate and AP/APSTA rejection before streaming. |
| T-07-07-03 | D | OTAWWW interrupted write | mitigate | Keep route fail-closed with explicit REL-03 gap until interruption evidence exists. |
</threat_model>

<verification>
Run `cargo test -p bitaxe-api --all-features update_plan`, `cargo build -p bitaxe-firmware --target xtensa-esp32s3-espidf`, `bazel build //firmware/bitaxe:firmware`, `just build`, and `git diff --check`.
</verification>

<success_criteria>
Firmware OTA is implemented with access/AP gates, streaming write, validation/activation, reboot scheduling, and rollback boot validation; OTAWWW remains a documented explicit gap with runtime logging.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ota-filesystem-and-release-packaging/07-07-SUMMARY.md`.
</output>
