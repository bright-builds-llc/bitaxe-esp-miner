---
phase: 07-ota-filesystem-and-release-packaging
plan: 04
type: execute
wave: 1
depends_on: ["07-01", "07-02"]
files_modified:
  - firmware/bitaxe/src/filesystem.rs
  - firmware/bitaxe/src/static_files.rs
  - firmware/bitaxe/src/main.rs
  - firmware/bitaxe/src/http_api.rs
  - firmware/bitaxe/static/www/index.html
  - firmware/bitaxe/static/www/assets/release.json
  - firmware/bitaxe/static/www/assets/app.css
  - firmware/bitaxe/static/recovery_page.html
  - firmware/bitaxe/sdkconfig.defaults
  - firmware/bitaxe/BUILD.bazel
autonomous: true
requirements: [REL-01, REL-07, REL-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 7-2026-06-28T13-30-15
generated_at: 2026-06-28T14:16:04Z
must_haves:
  truths:
    - "Firmware mounts the `www` SPIFFS partition and logs success or failure."
    - "Static serving preserves root, gzip, cache, missing redirect, and `/recovery` behavior."
    - "API routes are not captured by static wildcard routing."
  artifacts:
    - path: "firmware/bitaxe/src/filesystem.rs"
      provides: "ESP-IDF SPIFFS mount/status adapter"
    - path: "firmware/bitaxe/src/static_files.rs"
      provides: "ESP-IDF HTTP static and recovery handlers"
    - path: "firmware/bitaxe/static/recovery_page.html"
      provides: "Embedded recovery upload page"
  key_links:
    - from: "firmware/bitaxe/src/static_files.rs"
      to: "crates/bitaxe-api/src/static_plan.rs"
      via: "pure resolver decision"
      pattern: "resolve_static_request"
    - from: "firmware/bitaxe/src/http_api.rs"
      to: "firmware/bitaxe/src/static_files.rs"
      via: "route registration after API routes"
      pattern: "register_static"
---

<objective>
Implement firmware SPIFFS mount, static file serving, and embedded recovery page behavior.

Purpose: Make the Phase 7 filesystem and recovery surface real in firmware while preserving Phase 5 API route behavior and the upstream-visible static route contract.
Output: SPIFFS/status adapter, static/recovery HTTP handlers, Rust-owned static assets, and firmware build wiring.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ota-filesystem-and-release-packaging/07-CONTEXT.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-UI-SPEC.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-RESEARCH.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-01-SUMMARY.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-02-SUMMARY.md
@firmware/bitaxe/src/http_api.rs
@firmware/bitaxe/src/main.rs
@firmware/bitaxe/sdkconfig.defaults
@firmware/bitaxe/BUILD.bazel
@reference/esp-miner/main/filesystem.c
@reference/esp-miner/main/http_server/http_server.c
@reference/esp-miner/main/http_server/recovery_page.html

<interfaces>
Created by Plan 07-01:
```rust
pub fn resolve_static_request(request: StaticRequest<'_>, catalog: &impl StaticFileCatalog) -> StaticDecision;
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add SPIFFS mount/status adapter and SDK config</name>
  <read_first>
    firmware/bitaxe/sdkconfig.defaults
    reference/esp-miner/main/filesystem.c
    reference/esp-miner/sdkconfig.defaults
    firmware/bitaxe/partitions-ultra205.csv
  </read_first>
  <files>firmware/bitaxe/src/filesystem.rs, firmware/bitaxe/src/main.rs, firmware/bitaxe/sdkconfig.defaults, firmware/bitaxe/BUILD.bazel</files>
  <behavior>
    - Register SPIFFS with base path `/www`, partition label `www`, max files `5`, and `format_if_mount_failed=false`.
    - Log `spiffs_mount=available partition=www total_bytes={...} used_bytes={...}` on success.
    - Log `spiffs_mount=unavailable partition=www reason={mount_failed|partition_not_found|info_failed|esp_err}` on failure.
    - Do not format SPIFFS or silently serve static assets when mount fails.
  </behavior>
  <action>Create `filesystem.rs` with a thin ESP-IDF adapter around `esp_vfs_spiffs_register` and `esp_spiffs_info`. Add a `FilesystemStatus` enum and `mount_www_spiffs() -> FilesystemStatus`. Call it during firmware startup before HTTP route registration and pass or store the status for static serving. Add SDK config lines `CONFIG_PARTITION_TABLE_CUSTOM=y`, `CONFIG_PARTITION_TABLE_CUSTOM_FILENAME="partitions-ultra205.csv"`, `CONFIG_ESPTOOLPY_FLASHSIZE_16MB=y`, `CONFIG_HTTPD_MAX_REQ_HDR_LEN=1024`, `CONFIG_HTTPD_MAX_URI_LEN=2048`, and `CONFIG_SPIFFS_OBJ_NAME_LEN=64` unless already present. Add non-Rust static/partition files to the firmware Bazel genrule `srcs` for dependency tracking.</action>
  <verify>
    <automated>cargo build -p bitaxe-firmware --target xtensa-esp32s3-espidf</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "esp_vfs_spiffs_register|partition_label.*www|format_if_mount_failed.*false|spiffs_mount=available|spiffs_mount=unavailable" firmware/bitaxe/src/filesystem.rs`
    `rg -n "CONFIG_PARTITION_TABLE_CUSTOM|CONFIG_PARTITION_TABLE_CUSTOM_FILENAME|CONFIG_SPIFFS_OBJ_NAME_LEN|CONFIG_HTTPD_MAX_URI_LEN|CONFIG_HTTPD_MAX_REQ_HDR_LEN|CONFIG_ESPTOOLPY_FLASHSIZE_16MB" firmware/bitaxe/sdkconfig.defaults`
    `cargo build -p bitaxe-firmware --target xtensa-esp32s3-espidf` passes.
  </acceptance_criteria>
  <done>Firmware has visible SPIFFS mount status and explicit partition/filesystem SDK settings per D-06 through D-08.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add static and recovery HTTP handlers</name>
  <read_first>
    firmware/bitaxe/src/http_api.rs
    crates/bitaxe-api/src/static_plan.rs
    reference/esp-miner/main/http_server/http_server.c lines 382-494 and 1260-1411
  </read_first>
  <files>firmware/bitaxe/src/static_files.rs, firmware/bitaxe/src/http_api.rs, firmware/bitaxe/src/main.rs, firmware/bitaxe/BUILD.bazel</files>
  <action>Create `static_files.rs` that registers `/recovery` explicitly and registers a static wildcard after `/api/*`, `/api/ws`, `/api/ws/live`, `/api/system/OTA`, and `/api/system/OTAWWW`. Use the pure `resolve_static_request` decision from `bitaxe-api`. Serve files from `/www`, prefer `.gz` when the resolver selects gzip, set `Content-Encoding: gzip`, set `Cache-Control: max-age=2592000` for non-directory static assets, and return 302 `Location: /` with body `Redirect to the captive portal` for missing files. When SPIFFS is unavailable, serve embedded recovery behavior and log `static_files=spiffs_unavailable recovery=available`. Reject traversal decisions with 400 text/plain `Wrong API input`. Do not capture `/api/*` with static routes.</action>
  <verify>
    <automated>cargo build -p bitaxe-firmware --target xtensa-esp32s3-espidf</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "register_recovery|register_static|Content-Encoding|gzip|Cache-Control|max-age=2592000|Redirect to the captive portal|spiffs_unavailable|Wrong API input" firmware/bitaxe/src/static_files.rs firmware/bitaxe/src/http_api.rs`
    `rg -n '"/api/system/OTA"|"/api/system/OTAWWW"|"/api/ws"|"/recovery"' firmware/bitaxe/src/http_api.rs firmware/bitaxe/src/static_files.rs`
    `cargo build -p bitaxe-firmware --target xtensa-esp32s3-espidf` passes.
  </acceptance_criteria>
  <done>Firmware route registration preserves API/update routes and adds upstream-compatible static/recovery behavior per D-08 and D-09.</done>
</task>

<task type="auto">
  <name>Task 3: Add Rust-owned static compatibility and recovery assets</name>
  <read_first>
    .planning/phases/07-ota-filesystem-and-release-packaging/07-UI-SPEC.md
    PROVENANCE.md
    reference/esp-miner/main/http_server/recovery_page.html
  </read_first>
  <files>firmware/bitaxe/static/www/index.html, firmware/bitaxe/static/www/assets/release.json, firmware/bitaxe/static/www/assets/app.css, firmware/bitaxe/static/recovery_page.html</files>
  <action>Add a Rust-owned static source directory for `www.bin` generation. Include `index.html`, `assets/release.json`, and `assets/app.css` with no Angular rewrite and no copied upstream expression. The normal static page must show `AxeOS unavailable`, `Open /recovery and upload www.bin to restore the web interface.`, and links/forms that do not falsely claim full AxeOS UI parity. Add `recovery_page.html` as a re-authored recovery page preserving UI-SPEC labels: `AxeOS Recovery`, `Please upload www.bin to recover AxeOS`, `After clicking upload, please wait 60 seconds`, `DO NOT restart the device until response is received`, `Upload`, `uploading...`, `Upload successful!`, `Error uploading!`, `Restart`, and `Response:`. Keep asset paths and comments explicit enough for Plan 07-06 to record license/provenance; do not edit `docs/release/provenance-manifest.md` in this plan.</action>
  <verify>
    <automated>test -f firmware/bitaxe/static/www/index.html && test -f firmware/bitaxe/static/recovery_page.html</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "AxeOS unavailable|Open /recovery and upload www.bin|AxeOS Recovery|Please upload www.bin|DO NOT restart|Upload successful|Error uploading|/api/system/OTAWWW|/api/system/restart|Rust-owned|not copied" firmware/bitaxe/static`
  </acceptance_criteria>
  <done>Static and recovery assets are source-controlled with asset-local provenance signals and no Angular replacement or copied upstream HTML.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP path to filesystem | Browser path maps to SPIFFS file access. |
| SPIFFS mount to route handling | Firmware decides whether static assets are available or recovery should be explicit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | T/I | `static_files.rs` path handling | mitigate | Use `bitaxe-api` pure resolver to reject traversal before file open. |
| T-07-04-02 | D | SPIFFS mount failure | mitigate | Log unavailable status and keep `/recovery` available instead of silently serving broken files. |
| T-07-04-03 | E/T | Route registration order | mitigate | Register API and update routes before static wildcard; add grep checks for route registration. |
</threat_model>

<verification>
Run `cargo test -p bitaxe-api --all-features static_plan`, `cargo build -p bitaxe-firmware --target xtensa-esp32s3-espidf`, `bazel build //firmware/bitaxe:firmware`, and `git diff --check`.
</verification>

<success_criteria>
SPIFFS status is visible, `/recovery` is always explicitly available, static paths are safe, and API/update routes remain reachable.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ota-filesystem-and-release-packaging/07-04-SUMMARY.md`.
</output>
