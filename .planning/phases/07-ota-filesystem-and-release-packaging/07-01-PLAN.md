---
phase: 07-ota-filesystem-and-release-packaging
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/bitaxe-api/src/update_plan.rs
  - crates/bitaxe-api/src/static_plan.rs
  - crates/bitaxe-api/src/route_shell.rs
  - crates/bitaxe-api/src/lib.rs
  - crates/bitaxe-api/BUILD.bazel
autonomous: true
requirements: [REL-01, REL-02, REL-03, REL-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 7-2026-06-28T13-30-15
generated_at: 2026-06-28T14:16:04Z
must_haves:
  truths:
    - "OTA and OTAWWW request decisions are host-testable before firmware effects run."
    - "Static route resolution rejects traversal, preserves recovery, and describes missing-file fallback."
    - "OTAWWW remains fail-closed with an explicit REL-03 gap when interruption evidence is absent."
  artifacts:
    - path: "crates/bitaxe-api/src/update_plan.rs"
      provides: "Pure OTA/OTAWWW access, AP-mode, size, status, and gap decisions"
    - path: "crates/bitaxe-api/src/static_plan.rs"
      provides: "Pure SPIFFS/static/recovery route resolution decisions"
    - path: "crates/bitaxe-api/src/lib.rs"
      provides: "Public exports for firmware adapters"
  key_links:
    - from: "firmware/bitaxe/src/http_api.rs"
      to: "bitaxe_api::plan_update_request"
      via: "Plan 07-07 firmware adapter import"
      pattern: "plan_update_request"
    - from: "firmware/bitaxe/src/static_files.rs"
      to: "bitaxe_api::resolve_static_request"
      via: "Plan 07-04 static adapter import"
      pattern: "resolve_static_request"
---

<objective>
Create the Wave 0 pure API contracts for Phase 7 update and static-file behavior.

Purpose: Keep route access, AP-mode rejection, static path normalization, status labels, and OTAWWW gap policy unit-tested before ESP-IDF OTA, SPIFFS, or partition effects are added.
Output: `bitaxe-api` update/static planner modules with focused unit tests and public exports.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-CONTEXT.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-RESEARCH.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-VALIDATION.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-UI-SPEC.md
@crates/bitaxe-api/src/route_shell.rs
@crates/bitaxe-api/src/lib.rs
@reference/esp-miner/main/http_server/http_server.c
@reference/esp-miner/main/filesystem.c
@reference/esp-miner/main/http_server/recovery_page.html

<interfaces>
From `crates/bitaxe-api/src/route_shell.rs`:
```rust
pub struct PublicHttpResponse {
    pub status: u16,
    pub body: &'static str,
    pub content_type: Option<&'static str>,
}
pub struct RouteAccessInput {
    pub ap_mode_enabled: bool,
    pub request_ip: Ipv4Addr,
    pub origin: OriginGate,
}
pub fn plan_http_access(input: RouteAccessInput) -> HttpAccessDecision;
pub const fn unsupported_update_response() -> PublicHttpResponse; // body: "Wrong API input"
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add pure OTA and OTAWWW update planner</name>
  <read_first>
    crates/bitaxe-api/src/route_shell.rs
    crates/bitaxe-api/src/lib.rs
    .planning/phases/07-ota-filesystem-and-release-packaging/07-CONTEXT.md
    reference/esp-miner/main/http_server/http_server.c lines 1052-1203
  </read_first>
  <files>crates/bitaxe-api/src/update_plan.rs, crates/bitaxe-api/src/lib.rs, crates/bitaxe-api/BUILD.bazel</files>
  <behavior>
    - Private network/origin denial returns status 401 body `Unauthorized`.
    - AP/APSTA update attempts return status 500 body `Not allowed in AP mode` for both `/api/system/OTA` and `/api/system/OTAWWW` per D-12 and D-14.
    - Firmware OTA accept decision sets filename `esp-miner.bin`, start status `Starting...`, progress template `Working ({percent}%)`, success response `Firmware update complete, rebooting now!`, validation error status `Validation Error`, and validation error response `Validation / Activation Error`.
    - OTAWWW decision defaults to explicit REL-03 gap per D-16 with public response status 400 body `Wrong API input`, gap owner `phase-07-release`, release impact `AxeOS static update unavailable until interruption evidence exists`, and follow-up `implement whole-www partition update with interruption/recovery evidence`.
  </behavior>
  <action>Create `update_plan.rs` with typed enums `UpdateRouteKind::{FirmwareOta,AxeOsStaticOtaWww}`, `UpdateRequestInput`, `UpdateRequestDecision`, `UpdateStatusLabel`, and `OtaWwwGapDecision`. Implement `plan_update_request(input: UpdateRequestInput) -> UpdateRequestDecision`. Reuse `plan_http_access` for network/origin checks, then reject AP mode before accepting upload work. Do not perform ESP-IDF calls, partition writes, file reads, sleeps, or reboot effects in this crate. Export the new types and function from `lib.rs`. Add Arrange/Act/Assert unit tests for the exact public strings above and for the D-16 gap path.</action>
  <verify>
    <automated>cargo test -p bitaxe-api --all-features update_plan</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "pub fn plan_update_request|Firmware update complete, rebooting now!|Not allowed in AP mode|Validation / Activation Error|OtaWwwGapDecision|Wrong API input" crates/bitaxe-api/src/update_plan.rs`
    `cargo test -p bitaxe-api --all-features update_plan` passes.
  </acceptance_criteria>
  <done>Firmware OTA and OTAWWW public decisions are represented as pure tested data, with OTAWWW explicitly fail-closed under D-16 rather than silently unsupported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add pure static and recovery route resolver</name>
  <read_first>
    crates/bitaxe-api/src/route_shell.rs
    .planning/phases/07-ota-filesystem-and-release-packaging/07-UI-SPEC.md
    reference/esp-miner/main/http_server/http_server.c lines 382-494
    reference/esp-miner/main/filesystem.c
  </read_first>
  <files>crates/bitaxe-api/src/static_plan.rs, crates/bitaxe-api/src/lib.rs, crates/bitaxe-api/BUILD.bazel</files>
  <behavior>
    - `/` resolves to `/index.html`.
    - `/recovery` resolves to embedded recovery HTML even when SPIFFS is unavailable.
    - `.gz` variants are preferred when the candidate exists.
    - Non-directory static assets set `Cache-Control: max-age=2592000`.
    - Missing files return HTTP 302 to `/` with body `Redirect to the captive portal`.
    - Paths containing `..`, NUL, backslash, or absolute path escapes are rejected before filesystem access.
  </behavior>
  <action>Create `static_plan.rs` with typed inputs `StaticRequest`, `StaticFileCatalog`, `FilesystemAvailability` and decisions `ServeStatic`, `ServeRecovery`, `RedirectToRoot`, `RejectPathTraversal`, and `RecoveryFallback`. Use catalog membership passed in by the firmware adapter; do not read real files in the pure crate. Preserve D-06 through D-10 and the UI spec copy contract. Export resolver types/functions from `lib.rs`.</action>
  <verify>
    <automated>cargo test -p bitaxe-api --all-features static_plan</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "resolve_static_request|Redirect to the captive portal|max-age=2592000|Content-Encoding|gzip|RejectPathTraversal|ServeRecovery" crates/bitaxe-api/src/static_plan.rs`
    `cargo test -p bitaxe-api --all-features static_plan` passes.
  </acceptance_criteria>
  <done>Static/recovery behavior is testable without SPIFFS, and traversal protection is encoded before the firmware file adapter exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose Phase 7 route ownership without breaking Phase 5 API compare</name>
  <read_first>
    crates/bitaxe-api/src/route_shell.rs
    tools/parity/src/api_compare.rs
    .planning/phases/05-axeos-api-logs-and-telemetry/05-07-SUMMARY.md
  </read_first>
  <files>crates/bitaxe-api/src/route_shell.rs, crates/bitaxe-api/src/lib.rs, crates/bitaxe-api/BUILD.bazel</files>
  <action>Add a Phase 7 route manifest helper, `phase07_routes()`, that includes the existing Phase 5 routes plus explicit ownership for `/api/system/OTA`, `/api/system/OTAWWW`, `/recovery`, and the static wildcard owner. Keep `phase05_routes()` available for the Phase 5 API compare contract. Add tests proving `/api/system/OTA` and `/api/system/OTAWWW` no longer depend on the old `SafeUnsupportedUpdate` meaning for Phase 7 while Phase 5 route tests still pass. Do not add new frontend framework code, Angular rewrites, or A/B static partition concepts.</action>
  <verify>
    <automated>cargo test -p bitaxe-api --all-features route_shell</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "phase07_routes|/api/system/OTA|/api/system/OTAWWW|/recovery|static" crates/bitaxe-api/src/route_shell.rs`
    `cargo test -p bitaxe-api --all-features route_shell` passes.
  </acceptance_criteria>
  <done>Downstream firmware and parity plans can consume Phase 7 route ownership without regressing the Phase 5 route manifest.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser-to-update-route | Untrusted upload request metadata crosses into update planning. |
| browser-to-static-route | Untrusted path strings cross into static file resolution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | S/E | `plan_update_request` | mitigate | Preserve private-network/origin denial and add OTA-specific AP/APSTA rejection before any accept decision. |
| T-07-01-02 | T/I | `resolve_static_request` | mitigate | Reject `..`, NUL, backslash, absolute path escapes, and catalog misses before firmware file open. |
| T-07-01-03 | D | OTAWWW policy | mitigate | Default OTAWWW to explicit REL-03 gap until whole-partition interruption/recovery evidence exists. |
</threat_model>

<verification>
Run `cargo test -p bitaxe-api --all-features update_plan`, `cargo test -p bitaxe-api --all-features static_plan`, `cargo test -p bitaxe-api --all-features route_shell`, and `git diff --check`.
</verification>

<success_criteria>
`bitaxe-api` exports pure update/static contracts; all Phase 7 update/static public strings are unit-tested; OTAWWW has a typed explicit gap path under D-16.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ota-filesystem-and-release-packaging/07-01-SUMMARY.md`.
</output>
