---
phase: 07-ota-filesystem-and-release-packaging
plan: 06
type: execute
wave: 1
depends_on: ["07-03"]
files_modified:
  - tools/parity/src/release_gate.rs
  - tools/parity/src/main.rs
  - tools/parity/BUILD.bazel
  - docs/release/license-inventory.md
  - docs/release/provenance-manifest.md
autonomous: true
requirements: [REL-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 7-2026-06-28T13-30-15
generated_at: 2026-06-28T14:16:04Z
must_haves:
  truths:
    - "Release gate fails when license or provenance sections are missing."
    - "Cargo-only license output cannot satisfy the full release inventory."
    - "GPL-derived or upstream-generated materials require explicit review status."
  artifacts:
    - path: "tools/parity/src/release_gate.rs"
      provides: "Release inventory/provenance validation"
    - path: "docs/release/license-inventory.md"
      provides: "Human-readable dependency and asset inventory"
    - path: "docs/release/provenance-manifest.md"
      provides: "Source/reference/static/recovery provenance review"
  key_links:
    - from: "tools/parity/src/main.rs"
      to: "tools/parity/src/release_gate.rs"
      via: "`release-gate` CLI subcommand"
      pattern: "ReleaseGate"
---

<objective>
Implement release license and provenance validation.

Purpose: Make REL-05 enforceable by tooling and documentation instead of relying on prose-only review.
Output: `tools/parity release-gate` plus populated license/provenance inventories.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ota-filesystem-and-release-packaging/07-CONTEXT.md
@.planning/phases/07-ota-filesystem-and-release-packaging/07-03-SUMMARY.md
@PROVENANCE.md
@docs/adr/0013-mit-first-with-gpl-guardrails.md
@tools/parity/src/main.rs
@docs/release/license-inventory.md
@docs/release/provenance-manifest.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add release-gate validator to parity tooling</name>
  <read_first>
    tools/parity/src/main.rs
    docs/release/license-inventory.md
    docs/release/provenance-manifest.md
    PROVENANCE.md
  </read_first>
  <files>tools/parity/src/release_gate.rs, tools/parity/src/main.rs, tools/parity/BUILD.bazel</files>
  <behavior>
    - Fails if license inventory lacks sections `Cargo crates`, `Bazel and rules`, `ESP-IDF and esp-rs`, `Flashing tools`, `Static assets`, and `Release artifacts`.
    - Fails if provenance manifest lacks `Source commit`, `Reference commit`, `Static asset source`, `Recovery page source`, `GPL review status`, and `Release artifact review`.
    - Fails if any section says `unknown` without an explicit owner/follow-up.
  </behavior>
  <action>Add `release_gate.rs` and a `release-gate` subcommand to `bitaxe-parity`. The command should accept `--license-inventory docs/release/license-inventory.md`, `--provenance docs/release/provenance-manifest.md`, and `--manifest <package-json>` as optional inputs. Implement deterministic text output with `release_gate: passed` or listed validation errors. Keep parsing simple and section-based for Markdown, but do not use ad hoc shell greps inside the tool. Add unit tests with inline Markdown fixtures.</action>
  <verify>
    <automated>cargo test -p bitaxe-parity --all-features release_gate</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "release-gate|release_gate: passed|Cargo crates|Bazel and rules|GPL review status|Release artifact review" tools/parity/src/main.rs tools/parity/src/release_gate.rs`
    `cargo test -p bitaxe-parity --all-features release_gate` passes.
  </acceptance_criteria>
  <done>Release inventory/provenance review is tool-checkable.</done>
</task>

<task type="auto">
  <name>Task 2: Populate non-Cargo license and provenance inventory</name>
  <read_first>
    docs/release/license-inventory.md
    docs/release/provenance-manifest.md
    Cargo.lock
    MODULE.bazel
    MODULE.bazel.lock
    firmware/bitaxe/static/www/index.html
    firmware/bitaxe/static/recovery_page.html
    PROVENANCE.md
  </read_first>
  <files>docs/release/license-inventory.md, docs/release/provenance-manifest.md</files>
  <action>Update `docs/release/license-inventory.md` with concrete rows for workspace Rust crates, Bazel/rules dependencies, ESP-IDF Rust bindings, ESP-IDF components, `espflash`, `esptool.py` if used for packaging, and Rust-owned static/recovery assets. Update `docs/release/provenance-manifest.md` with current source commit command, pinned reference commit `c1915b0a63bfabebdb95a515cedfee05146c1d50`, Rust-owned static asset source paths, recovery page source path, and GPL review status. For any included upstream-generated assets, mark them GPL-reviewed and attributed; if none are included, state `No upstream-generated static assets included in Phase 7 package source`.</action>
  <verify>
    <automated>bazel run //tools/parity:report -- release-gate</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "Cargo crates|Bazel and rules|ESP-IDF and esp-rs|Flashing tools|Static assets|Release artifacts|c1915b0a63bfabebdb95a515cedfee05146c1d50|No upstream-generated static assets included" docs/release/license-inventory.md docs/release/provenance-manifest.md`
    `bazel run //tools/parity:report -- release-gate` exits 0 with `release_gate: passed`.
  </acceptance_criteria>
  <done>REL-05 inventory and provenance records are populated and gate-checked.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release inputs to published artifacts | Dependencies, tools, and assets become part of a release claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | R/T | `release_gate.rs` | mitigate | Gate release claims on required license/provenance sections and explicit owners for unknowns. |
| T-07-06-02 | R/I | Static asset provenance | mitigate | Require source paths and GPL review status before static assets are included in release docs. |
| T-07-06-03 | R | Cargo-only inventory | mitigate | Release gate requires non-Cargo sections so cargo-about output cannot satisfy REL-05 alone. |
</threat_model>

<verification>
Run `cargo test -p bitaxe-parity --all-features release_gate`, `bazel run //tools/parity:report -- release-gate`, `just parity`, and `git diff --check`.
</verification>

<success_criteria>
License/provenance records are populated and `release-gate` passes without treating Cargo dependency output as complete release compliance.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ota-filesystem-and-release-packaging/07-06-SUMMARY.md`.
</output>
