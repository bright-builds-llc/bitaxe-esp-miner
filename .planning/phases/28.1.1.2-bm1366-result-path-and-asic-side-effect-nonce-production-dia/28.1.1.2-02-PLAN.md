---
phase: 28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia
plan: 02
type: execute
wave: 2
depends_on:
  - 28.1.1.2-01
files_modified:
  - scripts/phase28.1.1.2-result-path-diagnostic.sh
  - scripts/phase28.1.1.2-result-path-diagnostic-test.sh
  - .planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-result-path-redacted.md
  - .planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-ab-match_upstream_register_read_poll-redacted.md
autonomous: true
requirements: [STR-09, CFG-07, ASIC-11]
user_setup:
  - service: ultra205-hardware
    why: "Detector-gated 360s fake-pool A/B requires connected Ultra 205 + local wifi-credentials.json"
    env_vars: []
    dashboard_config: []
must_haves:
  truths:
    - "Diagnostic wrapper dry-run passes without printing credentials"
    - "Baseline result-path compare runs against existing 28.1.1.1 source-work logs when present"
    - "Exactly one hardware A/B uses investigation label match_upstream_register_read_poll with ≥360s capture after detect-ultra205 and verify-reference"
  artifacts:
    - path: "scripts/phase28.1.1.2-result-path-diagnostic.sh"
      provides: "Orchestrator for baseline compare + forced register-read-poll A/B"
      contains: "match_upstream_register_read_poll"
    - path: "scripts/phase28.1.1.2-result-path-diagnostic-test.sh"
      provides: "Dry-run / syntax / fixture-path automated checks"
      contains: "dry_run"
    - path: ".planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-result-path-redacted.md"
      provides: "Committed baseline D-05 result-path report"
      contains: "recommended_investigation"
    - path: ".planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-ab-match_upstream_register_read_poll-redacted.md"
      provides: "Committed A/B redacted evidence for first register-read poll experiment"
      contains: "match_upstream_register_read_poll"
  key_links:
    - from: "scripts/phase28.1.1.2-result-path-diagnostic.sh"
      to: "scripts/phase28.1.1.1-source-work-aligned-capture.sh"
      via: "reuse source-work fake-pool capture path"
      pattern: "phase28.1.1.1-source-work-aligned-capture"
    - from: "scripts/phase28.1.1.2-result-path-diagnostic.sh"
      to: "scripts/phase28.1.1.2-result-path-compare.mjs"
      via: "baseline + A/B report generation"
      pattern: "result-path-compare"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1.2-2026-07-09T02-18-49
generated_at: 2026-07-09T02:27:00Z
---

<objective>
Add the result-path diagnostic orchestrator, produce a baseline D-05 compare from existing Phase 28.1.1.1 source-work logs, and run exactly one detector-gated Ultra 205 A/B for `match_upstream_register_read_poll` (not job bytes, not `post_max_baud_delay_2000`).

Purpose: D-07 item (1) + RESEARCH discretion resolve the first A/B to register-read poll cadence. D-12/D-13 require detect/verify/fake-pool/≥360s.
Output: diagnostic scripts + `28.1.1.2-result-path-redacted.md` + `28.1.1.2-ab-match_upstream_register_read_poll-redacted.md`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-CONTEXT.md
@.planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-RESEARCH.md
@scripts/phase28.1.1.1-below-job-byte-diagnostic.sh
@scripts/phase28.1.1.1-source-work-aligned-capture.sh
@scripts/phase28.1.1-wire-parity-capture.sh
@scripts/phase27-live-hardware-bridge-package.sh
@firmware/bitaxe/src/asic_adapter/work_result_investigation.rs

<interfaces>
First A/B label (locked by orchestrator + RESEARCH): `match_upstream_register_read_poll`

Upstream register-read side effect to match (reference only — do not edit reference/):
- `hashrate_monitor_task` → `ASIC_read_registers` → `BM1366_read_registers` (~1 Hz × REGISTER_MAP entries `0x4C,0x88,0x89,0x8A,0x8B,0x8C`)

Rust today: one post-dispatch `ReadChipId` probe (`maybe_send_post_dispatch_register_probe`) + init chip-id reads — not a steady-state register poll.

A/B packaging (D-10/D-11):
- If firmware does not yet implement `match_upstream_register_read_poll`, Plan 02 MUST add the **investigation-only** compile-time mode (off by default) via existing `BITAXE_WORK_RESULT_INVESTIGATION` / `--investigation` wiring before the hardware A/B. Do not change default bridge behavior in Plan 02 (default patch is Plan 03 only if A/B confirms).
- Wire through `scripts/phase27-live-hardware-bridge-package.sh --investigation match_upstream_register_read_poll` and `scripts/phase28.1.1-wire-parity-capture.sh --investigation match_upstream_register_read_poll`.

Capture reuse (D-06/D-13):
- Prefer latest valid `scratch/phase28.1.1.1-source-work/**` upstream+rust logs for baseline compare.
- A/B may reuse upstream log from that capture and only reflash Rust with the investigation mode + fake-pool (same pattern as `run_rust_ab_capture` in below-job diagnostic).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add result-path diagnostic wrapper + dry-run test</name>
  <files>scripts/phase28.1.1.2-result-path-diagnostic.sh, scripts/phase28.1.1.2-result-path-diagnostic-test.sh, firmware/bitaxe/src/asic_adapter/work_result_investigation.rs, firmware/bitaxe/src/live_stratum_runtime.rs, firmware/bitaxe/src/asic_adapter/production.rs</files>
  <read_first>
    - scripts/phase28.1.1.1-below-job-byte-diagnostic.sh
    - scripts/phase28.1.1.1-below-job-byte-diagnostic-test.sh
    - scripts/phase28.1.1-wire-parity-capture.sh
    - scripts/phase27-live-hardware-bridge-package.sh
    - firmware/bitaxe/src/asic_adapter/work_result_investigation.rs
    - reference/esp-miner/components/asic/bm1366.c (BM1366_read_registers + REGISTER_MAP)
    - reference/esp-miner/main/tasks/hashrate_monitor_task.c
  </read_first>
  <action>
    1. Create `scripts/phase28.1.1.2-result-path-diagnostic.sh` modeled on `scripts/phase28.1.1.1-below-job-byte-diagnostic.sh` with args: `--port`, `--wifi-credentials`, `--duration-seconds` (≥360), `--capture-dir` (must be gitignored), `--dry-run`.

    Behavior:
    - Always run `just verify-reference` then `just detect-ultra205`; require detected port == `--port` (D-12).
    - Baseline: locate latest valid Phase 28.1.1.1 source-work aligned capture under `scratch/phase28.1.1.1-source-work/` (same find logic as below-job diagnostic). If missing, run `scripts/phase28.1.1.1-source-work-aligned-capture.sh` once (D-13).
    - Run `node scripts/phase28.1.1.2-result-path-compare.mjs --upstream … --rust … --out $capture_dir/result-path-redacted.md` and copy to phase dir as `28.1.1.2-result-path-redacted.md`.
    - **Force first A/B** to investigation mode `match_upstream_register_read_poll` (do not auto-select `post_max_baud_delay_2000` even if an older comparator recommends it). Run one Rust fake-pool capture via `scripts/phase28.1.1-wire-parity-capture.sh` with `--investigation match_upstream_register_read_poll`, duration ≥360, wall clock budget ≥420s for the agent shell.
    - Re-compare A/B rust log vs baseline upstream; write `28.1.1.2-ab-match_upstream_register_read_poll-redacted.md` with D-05 fields plus `ab_label: match_upstream_register_read_poll`, `fake_pool_submit_observed`, `result_correlated`, `ab_outcome: improved|unchanged|regressed|blocked_safe_prerequisite`.
    - Never print wifi/pool credential file contents. Raw logs stay under ignored `capture-dir` / `scratch/`.

    2. If `has_investigation_mode("match_upstream_register_read_poll")` is not already implemented: add investigation-only mode in `work_result_investigation.rs` and a steady-state register-read poll path that mirrors upstream cadence (~1 Hz × the six REGISTER_MAP addresses `0x4C,0x88,0x89,0x8A,0x8B,0x8C` as CMD_READ/`0x52` frames) gated behind that mode only (D-10/D-11). Keep default (no investigation env) unchanged. Log a redaction-safe marker such as `asic_probe=register_read_poll mode=match_upstream_register_read_poll` without hex dumps.

    3. Create `scripts/phase28.1.1.2-result-path-diagnostic-test.sh` that runs `bash -n` on the diagnostic script and `--dry-run` asserting stdout contains `dry_run_status=passed`, `credential_values_printed=false`, and does not contain `poolPassword` / `wifipass`.
  </action>
  <acceptance_criteria>
    - `test -f scripts/phase28.1.1.2-result-path-diagnostic.sh`
    - `bash -n scripts/phase28.1.1.2-result-path-diagnostic.sh`
    - `bash scripts/phase28.1.1.2-result-path-diagnostic-test.sh` exits 0
    - `rg -n "match_upstream_register_read_poll" scripts/phase28.1.1.2-result-path-diagnostic.sh`
    - `rg -n "post_max_baud_delay_2000" scripts/phase28.1.1.2-result-path-diagnostic.sh` does not appear as the forced A/B label (forced label must be `match_upstream_register_read_poll`)
    - `rg -n 'match_upstream_register_read_poll' firmware/bitaxe/src/asic_adapter/work_result_investigation.rs` OR equivalent gate file if mode lives elsewhere
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase28.1.1.2-result-path-diagnostic-test.sh</automated>
  </verify>
  <done>Diagnostic wrapper + dry-run test exist; investigation mode for register-read poll is available off-by-default.</done>
</task>

<task type="auto">
  <name>Task 2: Run baseline compare + hardware A/B match_upstream_register_read_poll</name>
  <files>.planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-result-path-redacted.md, .planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-ab-match_upstream_register_read_poll-redacted.md</files>
  <read_first>
    - scripts/phase28.1.1.2-result-path-diagnostic.sh
    - AGENTS.md (Autonomous Ultra 205, Flash And Monitor Timeouts, Evidence Workflow)
    - .planning/phases/28.1.1.1-bm1366-upstream-golden-comparator-and-nonce-production-gap-r/28.1.1.1-below-job-byte-sequence-redacted.md
  </read_first>
  <action>
    Hardware-first (AGENTS.md):
    1. `just detect-ultra205` — require exactly one port + successful board-info.
    2. Confirm `wifi-credentials.json` exists (do not read/print contents). Prefer fake-pool; do not require live `pool-credentials*.json` (D-13).
    3. `just verify-reference`.
    4. Run diagnostic with `--duration-seconds 360` (or higher), `--capture-dir` under gitignored `scratch/phase28.1.1.2-result-path/` or phase `hardware-runs/`, agent wall clock ≥420s.
    5. Promote only redacted Markdown into the phase directory:
       - `28.1.1.2-result-path-redacted.md`
       - `28.1.1.2-ab-match_upstream_register_read_poll-redacted.md`
    6. Record in A/B report: board `205`, port path category only if needed (prefer `port_selected: detector_gated`), source/reference commit SHAs, commands as category labels, `fake_pool_submit_observed`, `result_correlated`, `ab_outcome`.
    7. If detection fails: write blocked evidence with `blocked_safe_prerequisite` and stop — do not invent share outcomes.

    Do not patch default firmware behavior in this task even if A/B improves (Plan 03 owns that). Do not promote checklist rows (D-15).
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-result-path-redacted.md`
    - `test -f .planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-ab-match_upstream_register_read_poll-redacted.md`
    - `rg -n "recommended_investigation:|chip_id_read_tx|result_read_attempt|fake_pool_submit_observed|result_correlated" .planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-result-path-redacted.md`
    - `rg -n "ab_label: match_upstream_register_read_poll|ab_outcome:" .planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-ab-match_upstream_register_read_poll-redacted.md`
    - `rg -n "poolPassword|stratum\\+tcp://|wifipass" .planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-*-redacted.md` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>rg -n "ab_label: match_upstream_register_read_poll" .planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-ab-match_upstream_register_read_poll-redacted.md</automated>
  </verify>
  <done>Baseline + one register-read-poll A/B redacted artifacts exist with CFG-07-safe content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local wifi-credentials.json → flash tool | Secrets stay runtime-only; scripts must not cat/print them |
| Hardware UART logs → phase Markdown | Only redacted counts/booleans cross into git |
| Investigation firmware package → device | Compile-time mode must stay opt-in until Plan 03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28112-01 | I | diagnostic.sh + redacted md | mitigate | Fake-pool synthetic creds; dry-run asserts `credential_values_printed=false`; redaction sweep before commit |
| T-28112-02 | I | flash-monitor raw logs | mitigate | Capture dir must be `git check-ignore`; never commit `*.raw.log` / hex UART dumps |
| T-28112-03 | S | ab_outcome / submit markers | mitigate | Boolean `fake_pool_submit_observed` only; no accepted/rejected live-share claim; Phase 30 owns promotion |
| T-28112-04 | T | investigation mode | mitigate | Mode off by default; only enabled via `--investigation match_upstream_register_read_poll` for A/B package |
| T-28112-05 | D | destructive flash | accept | Detector-gated board 205 flash only; no erase/rollback outside phase docs |
</threat_model>

<verification>
- `bash scripts/phase28.1.1.2-result-path-diagnostic-test.sh`
- `node scripts/phase28.1.1.2-result-path-compare-test.mjs`
- Redaction sweep on committed `28.1.1.2-*-redacted.md`
</verification>

<success_criteria>
- Baseline result-path report committed.
- Exactly one A/B for `match_upstream_register_read_poll` with ≥360s detector-gated capture evidence (or explicit blocked_safe_prerequisite).
</success_criteria>

<output>
After completion, create `.planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-02-SUMMARY.md`
</output>
