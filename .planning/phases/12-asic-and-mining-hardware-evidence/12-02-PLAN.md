---
phase: 12
plan: 02
type: execute
status: planned
wave: 1
depends_on: []
files_modified:
  - tools/parity/src/main.rs
autonomous: true
requirements: [ASIC-07, STR-06, STR-07, EVD-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-06-30T00-13-19
generated_at: 2026-06-30T00:14:49.245Z
must_haves:
  truths:
    - "Parity tooling rejects verified live ASIC/mining rows that lack the exact hardware-smoke, hardware-regression, or soak evidence class required by the row"
    - "Implemented/unit/fake-pool rows below verified remain allowed"
    - "Non-205 or deferred ASIC/protocol rows cannot reuse Ultra 205 evidence"
  artifacts:
    - path: "tools/parity/src/main.rs"
      provides: "ASIC/mining verified-row validation"
      contains: "ASIC-002"
  key_links:
    - from: "tools/parity/src/main.rs"
      to: "docs/parity/checklist.md"
      via: "Checklist row id validation"
      pattern: "STR-008"
---

<objective>
Add parity tests and validation so Phase 12 cannot mark live ASIC or mining parity verified from weak evidence.

Purpose: The checklist must reject live BM1366 init/work/result/mining overclaims before final release evidence.
Output: TDD-backed validation in `tools/parity/src/main.rs`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@tools/parity/src/main.rs
@docs/parity/checklist.md
@.planning/phases/12-asic-and-mining-hardware-evidence/12-CONTEXT.md
@.planning/phases/12-asic-and-mining-hardware-evidence/12-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for ASIC/mining verified-row overclaims</name>
  <requirements>ASIC-07, STR-06, STR-07, EVD-05</requirements>
  <read_first>
    <file>tools/parity/src/main.rs</file>
    <file>docs/parity/checklist.md</file>
    <file>.planning/phases/12-asic-and-mining-hardware-evidence/12-CONTEXT.md</file>
  </read_first>
  <files>tools/parity/src/main.rs</files>
  <scope>Parity tests only.</scope>
  <action>
    Add focused unit tests beside the existing verified-row guard tests in `tools/parity/src/main.rs`.

    Test behavior:
    - `ASIC-002` with `status: verified` and `evidence: unit,workflow` is rejected because BM1366 initialization needs hardware evidence.
    - `ASIC-003` with `status: verified` and `evidence: unit,golden` is rejected because live work-send needs hardware evidence.
    - `ASIC-004` with `status: verified` and `evidence: unit,golden` is rejected because live result-receive needs hardware evidence.
    - `STR-006` with `status: verified` and `evidence: unit,workflow` is rejected because first live mining loop needs hardware evidence.
    - `STR-008` with `status: verified` and `evidence: hardware-smoke` is rejected unless the notes include either `controlled no-share condition` or an accepted/rejected share observation and required metadata.
    - `STR-007` below verified with `evidence: workflow` remains allowed because criteria documentation is not live proof.

    Expected validation messages must include `requires hardware-smoke or soak evidence` for ASIC/live-loop rows and `requires mining smoke or soak details` for `STR-008`.
  </action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; if cargo test -p bitaxe-parity --all-features asic_mining_verified 2>&1 | tee /tmp/phase12-asic-mining-expected-failure.log; then echo "expected asic_mining_verified tests to fail before validation implementation" >&2; exit 1; fi; rg -n "requires hardware-smoke or soak evidence|requires mining smoke or soak details" /tmp/phase12-asic-mining-expected-failure.log'</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`tools/parity/src/main.rs` contains a test name with `asic_mining_verified`.</criterion>
    <criterion>The tests mention `ASIC-002`, `ASIC-003`, `ASIC-004`, `STR-006`, `STR-007`, and `STR-008`.</criterion>
    <criterion>The expected-failure command exits 0 only when the tests fail for the intended missing validation messages.</criterion>
  </acceptance_criteria>
  <done>Tests express the Phase 12 verified-row guard before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ASIC/mining verified-row validation</name>
  <requirements>ASIC-07, STR-06, STR-07, EVD-05</requirements>
  <read_first>
    <file>tools/parity/src/main.rs</file>
    <file>docs/parity/checklist.md</file>
  </read_first>
  <files>tools/parity/src/main.rs</files>
  <scope>Parity validation logic only.</scope>
  <action>
    Add a small pure helper in `tools/parity/src/main.rs` for live ASIC/mining row validation.

    Initial live ASIC row set: `ASIC-002`, `ASIC-003`, `ASIC-004`, and `ASIC-005`.
    Initial live mining row set: `STR-006` and `STR-008`.

    For rows in either set where `row.status == "verified"`, require `row.evidence` to include `hardware-smoke`, `hardware-regression`, or `soak`. If the token is missing, return a `ValidationError` whose message includes `requires hardware-smoke or soak evidence`.

    For `STR-008` where `row.status == "verified"`, additionally require row notes to include one of `accepted share`, `rejected share`, or `controlled no-share condition`, and require notes to mention `board`, `port`, `firmware commit`, `reference commit`, `redaction`, and `conclusion`. If missing, return a `ValidationError` whose message includes `requires mining smoke or soak details`.

    Preserve existing guards for safety-critical, active safety-control, release/OTA, and deferred-scope rows. Do not block implemented rows below `verified`.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-parity --all-features asic_mining_verified</automated>
    <automated>cargo test -p bitaxe-parity --all-features safety_critical</automated>
    <automated>just parity</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`tools/parity/src/main.rs` contains row ids `ASIC-002`, `ASIC-003`, `ASIC-004`, `ASIC-005`, `STR-006`, and `STR-008` in the live ASIC/mining validation path.</criterion>
    <criterion>`cargo test -p bitaxe-parity --all-features asic_mining_verified` exits 0.</criterion>
    <criterion>`just parity` exits 0 against the current checklist.</criterion>
  </acceptance_criteria>
  <done>Parity validation prevents Phase 12 ASIC/mining overclaims while allowing current below-verified rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Checklist Markdown to release gate | Human-edited evidence strings can promote parity claims. |
| Evidence token to live mining claim | Weak tokens can overstate work/result/mining behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-02-01 | Tampering | `tools/parity/src/main.rs` | mitigate | Verified live ASIC/mining rows require hardware-smoke, hardware-regression, or soak evidence. |
| T-12-02-02 | Repudiation | `STR-008` notes | mitigate | Verified live mining requires board, port, commits, share/no-share result, redaction, and conclusion details. |
| T-12-02-03 | Denial of Service | Strict validation | mitigate | Implemented rows below verified remain allowed so current known gaps do not block parity reports. |
</threat_model>

<verification>
Run targeted parity tests and `just parity`. If Rust source changed, later final verification must include the repo Rust pre-commit sequence.
</verification>

<success_criteria>
Current checklist remains green, but future verified ASIC/mining overclaims are rejected unless they carry exact live hardware or soak evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/12-asic-and-mining-hardware-evidence/12-02-SUMMARY.md`.
</output>
