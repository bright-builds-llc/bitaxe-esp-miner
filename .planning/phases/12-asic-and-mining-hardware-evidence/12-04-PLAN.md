---
phase: 12
plan: 04
type: execute
status: planned
wave: 3
depends_on: [12-03]
files_modified:
  - docs/parity/checklist.md
  - docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence.md
  - .planning/phases/12-asic-and-mining-hardware-evidence/12-VALIDATION.md
autonomous: true
requirements: [ASIC-07, STR-06, STR-07, EVD-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-06-30T00-13-19
generated_at: 2026-06-30T00:14:49.245Z
must_haves:
  truths:
    - "Checklist rows cite Phase 12 evidence without promoting unsupported ASIC or mining claims"
    - "Final validation records host tests, parity, reference cleanliness, hardware outcome, and residual risks"
    - "Phase 12 validation strategy is marked passed only if executed evidence and checks satisfy the contract"
  artifacts:
    - path: "docs/parity/checklist.md"
      provides: "Conservative ASIC/mining checklist status and Phase 12 citations"
      contains: "phase-12-asic-and-mining-hardware-evidence"
    - path: "docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence.md"
      provides: "Final Phase 12 evidence conclusions"
      contains: "Final Verification"
  key_links:
    - from: "docs/parity/checklist.md"
      to: "docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence.md"
      via: "Checklist evidence citations"
      pattern: "phase-12-asic-and-mining-hardware-evidence"
---

<objective>
Update checklist and validation conclusions from Phase 12 evidence without overclaiming unsupported hardware or mining parity.

Purpose: The phase must leave a clear release-readiness posture whether live evidence passed, was safe-blocked, or remained pending.
Output: Updated checklist notes, final evidence verification section, and validation sign-off.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence.md
@docs/parity/checklist.md
@tools/parity/src/main.rs
@.planning/phases/12-asic-and-mining-hardware-evidence/12-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update checklist rows from exact Phase 12 conclusions</name>
  <requirements>ASIC-07, STR-06, STR-07, EVD-05</requirements>
  <read_first>
    <file>docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence.md</file>
    <file>docs/parity/checklist.md</file>
    <file>tools/parity/src/main.rs</file>
  </read_first>
  <files>docs/parity/checklist.md, docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence.md</files>
  <scope>Checklist and evidence documentation only.</scope>
  <action>
    Update `docs/parity/checklist.md` rows `ASIC-002`, `ASIC-003`, `ASIC-004`, `ASIC-005`, `ASIC-007`, `STR-006`, `STR-007`, `STR-008`, `API-002`, `API-006`, and `STAT-002`.

    Every changed note must cite `docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence.md`.
    Keep rows below `verified` unless the ledger contains exact evidence for the row's claim and the needed evidence token. If the ledger only contains fail-closed, safe-blocked, detector-blocked, or wrapper safe boot evidence, keep ASIC-002/003/004/005/007 and STR-006/008 below `verified` with `hardware evidence pending`.

    `STR-007` may stay `implemented | workflow` when criteria are documented but no live smoke/soak ran.
    `API-002`, `API-006`, and `STAT-002` must mention whether Phase 12 observed mining/ASIC status through live firmware, or whether API/telemetry mining status remains pending for Phase 13/future live evidence.

    Append a `Final Verification` section to the ledger with command results for `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, `cargo test --all-features`, `just parity`, `just test`, and `git diff -- reference/esp-miner --exit-code`.
  </action>
  <verify>
    <automated>cargo fmt --all</automated>
    <automated>cargo clippy --all-targets --all-features -- -D warnings</automated>
    <automated>cargo build --all-targets --all-features</automated>
    <automated>cargo test --all-features</automated>
    <automated>just parity</automated>
    <automated>just test</automated>
    <automated>git diff -- reference/esp-miner --exit-code</automated>
    <automated>rg -n "phase-12-asic-and-mining-hardware-evidence" docs/parity/checklist.md</automated>
    <automated>rg -n "Final Verification|just parity|just test|hardware evidence pending|Conclusion" docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence.md</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`just parity` exits 0 after checklist updates.</criterion>
    <criterion>`just test` exits 0 after checklist updates.</criterion>
    <criterion>`reference/esp-miner` has no diff.</criterion>
    <criterion>The ledger final conclusion states whether Phase 12 captured live ASIC/mining evidence or recorded pending status.</criterion>
  </acceptance_criteria>
  <done>Checklist and evidence conclusions match Phase 12 proof without overclaiming.</done>
</task>

<task type="auto">
  <name>Task 2: Update validation sign-off from executed evidence</name>
  <requirements>EVD-05</requirements>
  <read_first>
    <file>.planning/phases/12-asic-and-mining-hardware-evidence/12-VALIDATION.md</file>
    <file>docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence.md</file>
  </read_first>
  <files>.planning/phases/12-asic-and-mining-hardware-evidence/12-VALIDATION.md</files>
  <scope>Validation artifact only.</scope>
  <action>
    Update `12-VALIDATION.md` from draft to the actual final state.
    If all automated checks passed and evidence conclusions are correctly scoped, set frontmatter `status: passed`, `nyquist_compliant: true`, and `wave_0_complete: true`.
    In the per-requirement table, set each status to either `passed`, `passed with hardware evidence pending`, or `blocked` based on the final evidence ledger. Do not mark ASIC-07, STR-06, or STR-07 simply `passed` unless live evidence satisfies the matching requirement.
    Change `Approval: pending` to `Approval: approved 2026-06-30` only when the validation contract is satisfied without overclaiming.
  </action>
  <verify>
    <automated>python3 -c "from pathlib import Path; text = Path('.planning/phases/12-asic-and-mining-hardware-evidence/12-VALIDATION.md').read_text(); required = ['status: passed', 'nyquist_compliant: true', 'wave_0_complete: true', 'Approval: approved 2026-06-30', 'ASIC-07', 'STR-06', 'STR-07', 'EVD-05']; missing = [item for item in required if item not in text]; assert not missing, f'missing validation sign-off fields: {missing}'; rows = {req: [line for line in text.splitlines() if req in line] for req in ['ASIC-07', 'STR-06', 'STR-07']}; empty = [req for req, lines in rows.items() if not lines]; assert not empty, f'missing rows: {empty}'; overclaims = [req for req, lines in rows.items() if any('| passed |' in line and 'hardware evidence pending' not in line for line in lines)]; assert not overclaims, f'overclaimed live evidence: {overclaims}'"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`12-VALIDATION.md` frontmatter reflects the actual executed validation state.</criterion>
    <criterion>ASIC-07, STR-06, and STR-07 statuses do not overclaim when live hardware or mining evidence is pending.</criterion>
  </acceptance_criteria>
  <done>Validation sign-off accurately reflects Phase 12 evidence and residual risk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Evidence ledger to checklist | Documentation changes can promote or defer release claims. |
| Validation sign-off to lifecycle gate | A passed validation file can hide pending live evidence if worded incorrectly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-12-04-01 | Tampering | `docs/parity/checklist.md` | mitigate | `just parity` and Phase 12 guard tests reject unsupported verified rows. |
| T-12-04-02 | Repudiation | final ledger conclusions | mitigate | Final Verification section records exact commands and results. |
| T-12-04-03 | Information Disclosure | evidence citations | mitigate | Checklist cites only redacted ledger conclusions, not raw secrets. |
</threat_model>

<verification>
Run targeted host tests, full `just test`, `just parity`, and reference cleanliness check.
</verification>

<success_criteria>
Phase 12 closes all evidence it can safely support, keeps unsupported live ASIC/mining claims pending, and leaves parity tooling green.
</success_criteria>

<output>
After completion, create `.planning/phases/12-asic-and-mining-hardware-evidence/12-04-SUMMARY.md`.
</output>
