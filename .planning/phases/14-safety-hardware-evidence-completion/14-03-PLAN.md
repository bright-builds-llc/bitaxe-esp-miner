---
phase: 14-safety-hardware-evidence-completion
plan: "03"
type: execute
wave: 2
depends_on:
  - "14-01"
  - "14-02"
files_modified:
  - scripts/phase14-power-voltage.sh
  - scripts/phase14-power-voltage-test.sh
  - scripts/phase14-thermal-fan.sh
  - scripts/phase14-thermal-fan-test.sh
  - scripts/BUILD.bazel
  - docs/parity/evidence/phase-14-safety-hardware-evidence-completion/power-telemetry.md
  - docs/parity/evidence/phase-14-safety-hardware-evidence-completion/voltage-control.md
  - docs/parity/evidence/phase-14-safety-hardware-evidence-completion/thermal-fan.md
  - docs/parity/evidence/phase-14-safety-hardware-evidence-completion/power-telemetry/power-voltage.log
  - docs/parity/evidence/phase-14-safety-hardware-evidence-completion/thermal-fan/thermal-fan.log
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03
  - SAFE-04
  - SAFE-07
  - SAFE-08
  - EVD-05
must_haves:
  truths:
    - "Power/current/voltage telemetry evidence is not conflated with DS4432U voltage actuation"
    - "Thermal and fan evidence separates read-only temperature/RPM observations from fan duty effects"
    - "Voltage control and fan duty claims remain below verified unless a bounded hardware-regression procedure passes"
  artifacts:
    - path: "scripts/phase14-power-voltage.sh"
      provides: "Power telemetry and voltage-control evidence wrapper"
      contains: "phase14_power_voltage_status"
    - path: "scripts/phase14-thermal-fan.sh"
      provides: "Thermal/fan evidence wrapper"
      contains: "phase14_thermal_fan_status"
    - path: "docs/parity/evidence/phase-14-safety-hardware-evidence-completion/power-telemetry.md"
      provides: "Power/current telemetry conclusion"
      contains: "PWR-006"
    - path: "docs/parity/evidence/phase-14-safety-hardware-evidence-completion/thermal-fan.md"
      provides: "Thermal/fan conclusion"
      contains: "THR-002"
  key_links:
    - from: "scripts/phase14-power-voltage.sh"
      to: "tools/parity safety-allow"
      via: "preflight manifest"
      pattern: "safety-allow --manifest"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 14-2026-06-30T23-56-34
generated_at: 2026-07-01T00:24:20.624Z
---

<objective>
Add and run Phase 14 power, voltage, thermal, and fan evidence wrappers.

Purpose: D-03, D-05 through D-08, and D-13 require per-surface wrappers that record read-only observations, bounded actuation blockers, recovery gates, logs, and exact claim conclusions without performing ad hoc voltage or fan actions.
Output: Tested power/voltage and thermal/fan shell wrappers, generated evidence logs, and human-readable evidence summaries.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/14-safety-hardware-evidence-completion/14-CONTEXT.md
@.planning/phases/14-safety-hardware-evidence-completion/14-RESEARCH.md
@.planning/phases/14-safety-hardware-evidence-completion/14-VALIDATION.md
@docs/parity/evidence/phase-14-safety-hardware-evidence-completion/README.md
@docs/parity/evidence/phase-14-safety-hardware-evidence-completion/redaction-review.md
@tools/parity/src/safety_allow.rs
@scripts/BUILD.bazel
@scripts/detect-ultra205.sh
@scripts/phase13-http-static-smoke.sh
@firmware/bitaxe/src/safety_adapter/power.rs
@firmware/bitaxe/src/safety_adapter/thermal.rs
@crates/bitaxe-safety/src/power.rs
@crates/bitaxe-safety/src/thermal.rs
@docs/parity/checklist.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add power/voltage and thermal/fan wrapper scripts with tests</name>
  <requirements>SAFE-01, SAFE-02, SAFE-03, SAFE-04, SAFE-07, SAFE-08, EVD-05</requirements>
  <files>scripts/phase14-power-voltage.sh, scripts/phase14-power-voltage-test.sh, scripts/phase14-thermal-fan.sh, scripts/phase14-thermal-fan-test.sh, scripts/BUILD.bazel</files>
  <read_first>
    <file>scripts/BUILD.bazel</file>
    <file>scripts/phase13-http-static-smoke.sh</file>
    <file>scripts/phase13-http-static-smoke-test.sh</file>
    <file>scripts/detect-ultra205.sh</file>
    <file>tools/parity/src/safety_allow.rs</file>
    <file>firmware/bitaxe/src/safety_adapter/power.rs</file>
    <file>firmware/bitaxe/src/safety_adapter/thermal.rs</file>
  </read_first>
  <action>Create `scripts/phase14-power-voltage.sh` and `scripts/phase14-thermal-fan.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. Each wrapper must accept `--manifest <allow.json>`, `--out-dir <dir>`, `--surface <surface>`, and optional `--serial-log <path>`. The power wrapper accepts surfaces `power-telemetry` and `voltage-control`; the thermal wrapper accepts surface `thermal-fan`. Before any probe output is trusted, run `bazel run //tools/parity:report -- safety-allow --manifest "$manifest" --surface "$surface" --allowed-command "scripts/<wrapper-name>.sh --manifest $manifest --surface $surface --out-dir $out_dir"`. If allow validation fails, detector fails, board-info is missing, package identity is stale, the serial log is unavailable, or redaction is pending, write a log line `phase14_<surface>_status: pending - <reason>` and exit 0. The wrappers must never run voltage writes, fan duty writes, raw I2C writes, overheat stimulus, fault injection, mining stress, erase, rollback, interrupted-update, or raw flash writes. `phase14-power-voltage.sh` must record `power_telemetry_claim: read-only-observation` for `PWR-006` and `voltage_control_claim: unsupported-pending` for `PWR-003` and `PWR-005` unless a validated manifest plus safe production route provides bounded actuation evidence; absent that route, record `voltage_control_status: pending - no production-safe bounded voltage route exists`. `phase14-thermal-fan.sh` must record `thermal_claim: read-only-observation` for `THR-001`, `fan_rpm_claim: read-only-observation` for `THR-002` when observed, and `fan_duty_status: pending - no production-safe bounded fan-duty route exists` for active fan duty. Add test scripts that use fake manifest validator and fake serial logs to prove: missing manifest blocks, failed validator blocks, power wrapper records `hardware_evidence_pending`, voltage control stays pending, thermal wrapper records `thermal_hardware_evidence_pending`, and fan duty stays pending. Register `phase14_power_voltage`, `phase14_power_voltage_test`, `phase14_thermal_fan`, and `phase14_thermal_fan_test` in `scripts/BUILD.bazel`.</action>
  <verify>
    <automated>bash -n scripts/phase14-power-voltage.sh && bash -n scripts/phase14-power-voltage-test.sh && bash -n scripts/phase14-thermal-fan.sh && bash -n scripts/phase14-thermal-fan-test.sh && bazel test //scripts:phase14_power_voltage_test //scripts:phase14_thermal_fan_test</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`scripts/phase14-power-voltage.sh` contains `phase14_power-telemetry_status` or `phase14_power_telemetry_status`, `voltage_control_status`, `PWR-006`, `PWR-003`, and `PWR-005`.</criterion>
    <criterion>`scripts/phase14-thermal-fan.sh` contains `phase14_thermal_fan_status`, `THR-001`, `THR-002`, and `fan_duty_status: pending`.</criterion>
    <criterion>Neither wrapper contains `erase-flash`, `write-bin`, `i2cset`, `rollback`, `interrupted`, or `fan duty write` command execution paths.</criterion>
    <criterion>`scripts/BUILD.bazel` exposes both wrapper tests.</criterion>
  </acceptance_criteria>
  <done>Power/voltage and thermal/fan wrappers are tested and fail closed to pending evidence when prerequisites are absent.</done>
</task>

<task type="auto">
  <name>Task 2: Run power/voltage and thermal/fan wrappers or record pending evidence</name>
  <requirements>SAFE-01, SAFE-02, SAFE-03, SAFE-04, SAFE-07, SAFE-08, EVD-05</requirements>
  <files>docs/parity/evidence/phase-14-safety-hardware-evidence-completion/power-telemetry.md, docs/parity/evidence/phase-14-safety-hardware-evidence-completion/voltage-control.md, docs/parity/evidence/phase-14-safety-hardware-evidence-completion/thermal-fan.md, docs/parity/evidence/phase-14-safety-hardware-evidence-completion/power-telemetry/power-voltage.log, docs/parity/evidence/phase-14-safety-hardware-evidence-completion/thermal-fan/thermal-fan.log</files>
  <read_first>
    <file>docs/parity/evidence/phase-14-safety-hardware-evidence-completion/README.md</file>
    <file>docs/parity/evidence/phase-14-safety-hardware-evidence-completion/redaction-review.md</file>
    <file>scripts/phase14-power-voltage.sh</file>
    <file>scripts/phase14-thermal-fan.sh</file>
    <file>docs/parity/checklist.md</file>
  </read_first>
  <action>Run the wrappers only through validated Phase 14 allow manifests. Start with `just detect-ultra205`; if it does not print exactly one `port=<path>` and pass board-info, create pending evidence instead of probing. If detector passes, use `just package` and `bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json` as the package identity source, create one allow manifest per wrapper under the corresponding out-dir, validate it with `safety-allow`, then run the wrapper. Write `power-telemetry.md` with board `205`, selected port or `unavailable`, source commit or `unavailable`, reference commit or `unavailable`, package manifest, exact command, allow manifest path, observed INA260 status or `power_telemetry_status: pending`, `PWR-006` conclusion, and non-claims for DS4432U writes and ASIC power sequencing. Write `voltage-control.md` with `PWR-003` and `PWR-005`, `voltage_control_status: pending - no production-safe bounded voltage route exists` unless a safe bounded route actually exists and the allow manifest proves hardware-regression; do not invent that route. Write `thermal-fan.md` with `THR-001`, `THR-002`, `THR-003`, observed thermal/fan markers or `thermal_fan_status: pending`, `fan_duty_status: pending - no production-safe bounded fan-duty route exists`, and a note that pure PID unit coverage remains `unit` evidence only. Per D-06 and D-08, read-only observations may support exact subclaims but must not verify active voltage writes, fan duty effects, overheat/fault behavior, or stale cached values.</action>
  <verify>
    <automated>test -f docs/parity/evidence/phase-14-safety-hardware-evidence-completion/power-telemetry.md && test -f docs/parity/evidence/phase-14-safety-hardware-evidence-completion/voltage-control.md && test -f docs/parity/evidence/phase-14-safety-hardware-evidence-completion/thermal-fan.md && rg -n "PWR-006|power_telemetry_status|read-only-observation|PWR-003|PWR-005|voltage_control_status: pending|THR-001|THR-002|THR-003|fan_duty_status: pending|hardware-regression" docs/parity/evidence/phase-14-safety-hardware-evidence-completion/power-telemetry.md docs/parity/evidence/phase-14-safety-hardware-evidence-completion/voltage-control.md docs/parity/evidence/phase-14-safety-hardware-evidence-completion/thermal-fan.md</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Evidence docs record board `205`, selected port or `unavailable`, source commit or `unavailable`, reference commit or `unavailable`, package manifest, exact command, and conclusion.</criterion>
    <criterion>`voltage-control.md` contains `voltage_control_status: pending - no production-safe bounded voltage route exists` unless actual hardware-regression artifacts exist.</criterion>
    <criterion>`thermal-fan.md` contains `fan_duty_status: pending - no production-safe bounded fan-duty route exists` unless actual hardware-regression artifacts exist.</criterion>
    <criterion>No checklist row is promoted by this task.</criterion>
  </acceptance_criteria>
  <done>Power, voltage, thermal, fan, and PID evidence is either captured narrowly or recorded as pending with explicit blockers and non-claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Allow manifest to surface wrappers | Procedure metadata controls whether power, voltage, thermal, or fan evidence can run. |
| Serial/API observations to evidence docs | Runtime logs may contain stale, unavailable, or secret-bearing values. |
| Read-only observation to active-control claim | Sensor observations can be misused to verify actuators. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-14-03-01 | Elevation of Privilege | `scripts/phase14-power-voltage.sh` | mitigate | Validate allow manifest, run detector first, and never execute DS4432U/raw I2C/flash/write commands outside a safe production route. |
| T-14-03-02 | Tampering | power and thermal logs | mitigate | Record exact command, manifest path, source/reference commits, observed marker, and pending reason in generated logs and summaries. |
| T-14-03-03 | Information Disclosure | generated logs | mitigate | Defer citation until Plan 14-06 redaction review covers logs and copied outputs. |
| T-14-03-04 | Repudiation | active voltage/fan status | mitigate | Evidence docs must contain explicit pending strings when bounded actuation routes are absent. |
</threat_model>

<verification>
Plan-level checks:
- `bazel test //scripts:phase14_power_voltage_test //scripts:phase14_thermal_fan_test`
- `cargo test -p bitaxe-safety --all-features power`
- `cargo test -p bitaxe-safety --all-features thermal`
- `just parity`
</verification>

<success_criteria>
Phase 14 has tested per-surface wrappers and evidence records for power/current/voltage and thermal/fan surfaces, with active voltage and fan duty claims left below verified unless real hardware-regression evidence exists.
</success_criteria>

<output>
After completion, create `.planning/phases/14-safety-hardware-evidence-completion/14-03-SUMMARY.md`.
</output>
