---
phase: 14-safety-hardware-evidence-completion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/parity/src/safety_allow.rs
  - tools/parity/src/main.rs
  - tools/parity/BUILD.bazel
autonomous: true
requirements:
  - SAFE-01
  - SAFE-04
  - SAFE-08
  - EVD-05
must_haves:
  truths:
    - "Active Ultra 205 safety probes cannot run without a machine-validated allow manifest"
    - "Allow manifests reject non-205 boards, detector mismatch, board-info failure, stale package identity, missing recovery, missing safe-state markers, and missing redaction reviewer"
    - "Active claim tiers require `hardware-regression` before they can support checklist promotion"
  artifacts:
    - path: "tools/parity/src/safety_allow.rs"
      provides: "Typed Phase 14 allow-manifest parser and validator"
      contains: "SafetyAllowManifest"
    - path: "tools/parity/src/main.rs"
      provides: "`safety-allow` CLI dispatch"
      contains: "SafetyAllow"
    - path: "tools/parity/BUILD.bazel"
      provides: "Bazel source registration for allow-manifest module"
      contains: "src/safety_allow.rs"
  key_links:
    - from: "scripts/phase14-*.sh"
      to: "tools/parity:report safety-allow"
      via: "manifest validation before probes"
      pattern: "safety-allow --manifest"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 14-2026-06-30T23-56-34
generated_at: 2026-07-01T00:24:20.624Z
---

<objective>
Add the Wave 0 machine-enforced allow-manifest gate required before any Phase 14 active safety hardware probe can run.

Purpose: D-01 through D-04 require a typed preflight gate that binds board `205`, detector-selected port, board-info success, package identity, bounded procedure, recovery, safe-state markers, evidence destination, redaction reviewer, and exact checklist rows before active evidence can support `hardware-regression`. This includes the D-02 rule that every live run starts with `just detect-ultra205` and stops to pending evidence when detector or board-info prerequisites fail.
Output: `tools/parity` gains a tested `safety-allow` subcommand used by later Phase 14 surface wrappers.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/verification.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-safety-hardware-evidence-completion/14-CONTEXT.md
@.planning/phases/14-safety-hardware-evidence-completion/14-RESEARCH.md
@.planning/phases/14-safety-hardware-evidence-completion/14-VALIDATION.md
@tools/parity/src/main.rs
@tools/parity/BUILD.bazel
@docs/parity/checklist.md

<interfaces>
From `tools/parity/src/main.rs`:
```rust
#[derive(Debug, Subcommand)]
enum CliCommand {
    Report(ReportArgs),
    ApiCompare(ApiCompareArgs),
    ReleaseGate(ReleaseGateArgs),
}

#[derive(Debug, Eq, PartialEq, Serialize)]
struct ValidationError {
    id: String,
    message: String,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing allow-manifest validation tests</name>
  <requirements>SAFE-01, SAFE-04, SAFE-08, EVD-05</requirements>
  <files>tools/parity/src/safety_allow.rs, tools/parity/src/main.rs, tools/parity/BUILD.bazel</files>
  <read_first>
    <file>tools/parity/src/main.rs</file>
    <file>tools/parity/BUILD.bazel</file>
    <file>.planning/phases/14-safety-hardware-evidence-completion/14-CONTEXT.md</file>
    <file>.planning/phases/14-safety-hardware-evidence-completion/14-VALIDATION.md</file>
    <file>docs/parity/checklist.md</file>
  </read_first>
  <behavior>
    - Test 1: board `601` is rejected with a message containing `board must be 205`.
    - Test 2: `detector_port` different from `port` is rejected with `detector port mismatch`.
    - Test 3: `board_info_status` other than `passed` is rejected with `board-info must pass`.
    - Test 4: package manifest `source_commit` or `reference_commit` mismatch is rejected with `package identity mismatch`.
    - Test 5: active `claim_tier` values `bounded-actuation`, `fault-stimulus`, `self-test-hardware`, `load-stress`, and `runtime-display-input` require `evidence_class` exactly `hardware-regression`.
    - Test 6: missing `recovery_steps`, missing `post_action_safe_state_markers`, missing `redaction_reviewer`, or empty `checklist_rows` is rejected.
  </behavior>
  <action>Create `tools/parity/src/safety_allow.rs` with tests first. Define `SafetyAllowManifest` with these exact JSON fields: `board`, `port`, `detector_command`, `detector_port`, `board_info_command`, `board_info_status`, `package_manifest`, `source_commit`, `reference_commit`, `surface`, `claim_tier`, `evidence_class`, `allowed_command`, `allowed_inputs`, `abort_conditions`, `recovery_steps`, `post_action_safe_state_markers`, `evidence_dir`, `redaction_reviewer`, and `checklist_rows`. Use `serde::Deserialize`, `serde_json::Value`, and `camino::Utf8PathBuf`; do not add a new dependency. Add tests in the module using temporary directories and checked-in-style JSON strings. Register `mod safety_allow;` and a `SafetyAllow(SafetyAllowArgs)` CLI variant in `tools/parity/src/main.rs`, but leave the implementation absent or failing until Task 2 so the RED failure can be observed. Add `src/safety_allow.rs` to the `rust_binary` `srcs` in `tools/parity/BUILD.bazel`. Do not commit a failing RED state; record the RED command output in the summary only.</action>
  <verify>
    <automated>cargo test -p bitaxe-parity --all-features safety_allow</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`tools/parity/src/safety_allow.rs` contains `struct SafetyAllowManifest` and every field named in the action.</criterion>
    <criterion>Tests mention `board must be 205`, `detector port mismatch`, `board-info must pass`, `package identity mismatch`, and `hardware-regression`.</criterion>
    <criterion>`tools/parity/BUILD.bazel` contains `src/safety_allow.rs`.</criterion>
  </acceptance_criteria>
  <done>RED tests define the Phase 14 allow-manifest invariants before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `safety-allow` parser, validator, and CLI output</name>
  <requirements>SAFE-01, SAFE-04, SAFE-08, EVD-05</requirements>
  <files>tools/parity/src/safety_allow.rs, tools/parity/src/main.rs, tools/parity/BUILD.bazel</files>
  <read_first>
    <file>tools/parity/src/safety_allow.rs</file>
    <file>tools/parity/src/main.rs</file>
    <file>tools/parity/BUILD.bazel</file>
    <file>tools/parity/src/release_gate.rs</file>
    <file>.planning/phases/14-safety-hardware-evidence-completion/14-RESEARCH.md</file>
  </read_first>
  <behavior>
    - Valid manifests render `safety_allow_status: passed`.
    - Invalid manifests render all validation failures and cause the CLI to exit nonzero.
    - Active claim tiers cannot pass without `hardware-regression`, recovery steps, abort conditions, and safe-state markers.
    - Read-only, safe-unavailable, API/WebSocket projection, unsupported-pending, and parity-redaction tiers may pass without active actuation when their evidence class matches the exact claim.
  </behavior>
  <action>Implement a pure validator in `tools/parity/src/safety_allow.rs` and expose it through `bazel run //tools/parity:report -- safety-allow --manifest <path>`. The validator must require `board == "205"`, `detector_command == "just detect-ultra205"`, `detector_port == port`, `board_info_status == "passed"`, and `board_info_command == format!("espflash board-info --chip esp32s3 --port {port} --non-interactive")` per D-02. Read `package_manifest` as JSON and require its `source_commit` and `reference_commit` to equal the manifest fields. Accept only these `surface` values: `safe-baseline`, `power-telemetry`, `voltage-control`, `thermal-fan`, `self-test-watchdog-load`, `display-input`, `live-api-websocket-telemetry`, and `parity-redaction`. Accept only these `claim_tier` values: `safe-baseline`, `read-only-observation`, `bounded-actuation`, `fault-stimulus`, `self-test-hardware`, `load-stress`, `runtime-display-input`, `api-websocket-projection`, `safe-unavailable`, `unsupported-pending`, and `parity-redaction`. Active tiers are exactly `bounded-actuation`, `fault-stimulus`, `self-test-hardware`, `load-stress`, and `runtime-display-input`; they require `evidence_class == "hardware-regression"`, nonempty `recovery_steps`, abort conditions containing `detector_mismatch`, `board_info_failure`, and `missing_safe_state_marker`, and post-action safe-state markers containing `safe_state: mining=disabled` and `hardware_control=disabled`. Require nonempty `allowed_command`, nonempty `evidence_dir`, `redaction_reviewer` not equal to `pending` or `required-before-citation`, and nonempty `checklist_rows`. Treat each manifest as procedure-scoped per D-07 by validating the exact command, allowed inputs, abort conditions, recovery steps, safe-state markers, evidence directory, and checklist rows together rather than as independent optional fields. Add optional CLI filters `--surface <surface>` and `--allowed-command <exact>` that fail when the manifest differs. Render text output with `safety_allow_status: passed`, `surface: <surface>`, `claim_tier: <claim_tier>`, `evidence_class: <evidence_class>`, and `checklist_rows: <comma-separated rows>` when valid.</action>
  <verify>
    <automated>cargo test -p bitaxe-parity --all-features safety_allow</automated>
    <automated>bazel test //tools/parity:tests --test_filter=safety_allow</automated>
    <automated>just parity</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`bazel run //tools/parity:report -- safety-allow --manifest <valid-test-manifest>` is documented in tests and returns `safety_allow_status: passed` for the valid fixture.</criterion>
    <criterion>`cargo test -p bitaxe-parity --all-features safety_allow` exits 0.</criterion>
    <criterion>`just parity` exits 0 and still rejects unsafe verified checklist rows.</criterion>
  </acceptance_criteria>
  <done>Later Phase 14 wrappers can machine-check their procedure manifest before running any active or evidence-producing probe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| JSON allow manifest to repo-owned hardware wrappers | Human-authored manifest data controls whether a hardware probe may run. |
| Package manifest to allow manifest | Package identity must match the firmware image being cited as evidence. |
| Checklist evidence class to verified safety claim | Weak evidence tokens could otherwise promote active safety behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-14-01-01 | Tampering | `tools/parity/src/safety_allow.rs` | mitigate | Parse JSON into typed domain values and reject non-205 board, detector mismatch, board-info failure, package identity mismatch, missing recovery, and missing safe-state markers. |
| T-14-01-02 | Elevation of Privilege | active probe authorization | mitigate | Active tiers require `hardware-regression`, exact command filtering, abort conditions, recovery steps, and post-action safe-state markers before later wrappers can proceed. |
| T-14-01-03 | Repudiation | evidence-class validation | mitigate | CLI output lists surface, claim tier, evidence class, checklist rows, and every validation failure. |
| T-14-01-04 | Information Disclosure | tests and sample manifests | accept | Tests use synthetic package manifests and no hardware logs, credentials, private endpoints, or NVS values. |
</threat_model>

<verification>
Plan-level checks:
- `cargo test -p bitaxe-parity --all-features safety_allow`
- `bazel test //tools/parity:tests --test_filter=safety_allow`
- `just parity`
- If committing Rust changes: `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`
</verification>

<success_criteria>
`tools/parity` exposes a tested `safety-allow` gate that blocks unsafe Phase 14 active hardware procedures before they can run or support checklist promotion.
</success_criteria>

<output>
After completion, create `.planning/phases/14-safety-hardware-evidence-completion/14-01-SUMMARY.md`.
</output>
