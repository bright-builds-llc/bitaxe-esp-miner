---
phase: 08-parity-evidence-and-ultra-205-release-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/parity/src/main.rs
  - tools/parity/src/release_gate.rs
autonomous: true
requirements: [REL-08, EVD-01, EVD-02, EVD-03, EVD-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-06-28T21-51-32
generated_at: 2026-06-28T22:16:47.573Z
must_haves:
  truths:
    - "Verified checklist rows fail validation when their evidence class does not support the claim."
    - "Release-sensitive rows cannot be verified from package, unit, workflow, or API-compare evidence alone."
    - "Non-205 boards, deferred ASICs, deferred protocol expansion, BAP, all-board images, and UI rewrite rows cannot inherit Ultra 205 evidence."
    - "Release-gate validation checks package manifest evidence when `--manifest bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json` is supplied."
  artifacts:
    - path: "tools/parity/src/main.rs"
      provides: "Checklist verified-claim and deferred-scope guards"
    - path: "tools/parity/src/release_gate.rs"
      provides: "Manifest-backed release-gate validation"
  key_links:
    - from: "docs/parity/checklist.md"
      to: "tools/parity/src/main.rs"
      via: "parsed checklist row validation"
      pattern: "validate_rows"
    - from: "bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json"
      to: "tools/parity/src/release_gate.rs"
      via: "optional `--manifest` release-gate input"
      pattern: "validate_manifest_if_provided"
---

<objective>
Harden the existing parity and release-gate tooling so Phase 8 cannot accidentally turn incomplete evidence into current-release claims.

Purpose: Implement D-01, D-02, D-03, D-04, D-09, D-11, and D-13 by keeping `docs/parity/checklist.md` as the audit ledger and enforcing evidence classes in `tools/parity`.
Output: Focused tests and guard logic in the existing `bitaxe-parity` CLI path.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-parity-evidence-and-ultra-205-release-gate/08-CONTEXT.md
@.planning/phases/08-parity-evidence-and-ultra-205-release-gate/08-RESEARCH.md
@.planning/phases/08-parity-evidence-and-ultra-205-release-gate/08-VALIDATION.md
@docs/adr/0006-parity-checklist-as-audit-evidence.md
@docs/adr/0012-parity-verification-evidence.md
@docs/adr/0014-pivot-to-ultra-205-bm1366-first-parity.md
@docs/parity/checklist.md
@docs/parity/evidence/phase-07-ota-filesystem-release.md
@docs/parity/evidence/phase-07-ultra-205-ota-hardware-smoke.md
@docs/release/provenance-manifest.md
@tools/parity/src/main.rs
@tools/parity/src/release_gate.rs
</context>

<interfaces>
From `tools/parity/src/main.rs`:

```rust
fn validate_rows(rows: &[ChecklistRow]) -> Vec<ValidationError>;
fn validate_release_ota_verified_row(row: &ChecklistRow) -> Vec<ValidationError>;
fn has_evidence_token(row: &ChecklistRow, token: &str) -> bool;
fn row_haystack(row: &ChecklistRow) -> String;
```

From `tools/parity/src/release_gate.rs`:

```rust
pub(crate) struct ReleaseGateDocuments {
    pub(crate) maybe_manifest_path: Option<Utf8PathBuf>,
    pub(crate) maybe_manifest_json: Option<String>,
}
pub(crate) fn validate_release_gate(documents: &ReleaseGateDocuments) -> ReleaseGateReport;
```
</interfaces>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Tighten checklist verified-claim guards</name>
  <read_first>
    tools/parity/src/main.rs
    docs/parity/checklist.md
    docs/parity/evidence/phase-07-ota-filesystem-release.md
    docs/parity/evidence/phase-07-ultra-205-ota-hardware-smoke.md
    .planning/phases/08-parity-evidence-and-ultra-205-release-gate/08-CONTEXT.md
  </read_first>
  <files>tools/parity/src/main.rs</files>
  <behavior>
    - `REL-003` with status `verified` must require row notes/evidence containing all five REL-08 terms: `rollback`, `recovery`, `large erase`, `failed update`, and `interrupted-update`.
    - `OTA-001` with status `verified` must require hardware evidence plus notes/evidence containing `valid OTA`, `invalid image rejection`, and `boot-validation`.
    - `FS-001` with status `verified` must require hardware evidence plus notes/evidence containing `live static`, `/assets/app.css.gz`, `missing static redirect`, and `/recovery`.
    - Non-205 or deferred-scope rows must fail if marked `verified` from Ultra 205 evidence. Guard terms: `CFG-002`, `ASIC-008`, `ASIC-009`, `ASIC-010`, `STR-005`, `BAP`, `all-board`, and `Angular`.
  </behavior>
  <action>Add failing tests first in `tools/parity/src/main.rs` using the existing inline checklist-table test style and Arrange/Act/Assert comments. Test names must include `release_image_verified_requires_rel08_evidence`, `firmware_ota_verified_requires_valid_invalid_and_boot_validation`, `filesystem_verified_requires_live_static_recovery_surfaces`, and `deferred_scope_verified_rows_reject_ultra205_evidence`. Implement pure helper functions in `tools/parity/src/main.rs` without adding dependencies. Keep existing safety-critical and Phase 7 release/OTA guard behavior intact per D-02, D-03, D-04, and D-13.</action>
  <verify>
    <automated>cargo test -p bitaxe-parity --all-features</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "release_image_verified_requires_rel08_evidence|firmware_ota_verified_requires_valid_invalid_and_boot_validation|filesystem_verified_requires_live_static_recovery_surfaces|deferred_scope_verified_rows_reject_ultra205_evidence" tools/parity/src/main.rs`
    `rg -n "rollback|large erase|failed update|interrupted-update|invalid image rejection|boot-validation|/assets/app.css.gz|missing static redirect|CFG-002|ASIC-008|STR-005|BAP|Angular" tools/parity/src/main.rs`
    `cargo test -p bitaxe-parity --all-features` exits 0.
  </acceptance_criteria>
  <done>Checklist validation blocks release-sensitive, OTA, filesystem, safety, and deferred-scope overclaims before `just parity` can pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate manifest-backed release-gate evidence</name>
  <read_first>
    tools/parity/src/release_gate.rs
    docs/release/provenance-manifest.md
    docs/release/license-inventory.md
    tools/xtask/src/package_manifest.rs
    .planning/phases/07-ota-filesystem-and-release-packaging/07-06-SUMMARY.md
  </read_first>
  <files>tools/parity/src/release_gate.rs</files>
  <behavior>
    - When `ReleaseGateDocuments.maybe_manifest_json` is present, release-gate validation parses it as JSON and requires `"schema_version": 2`.
    - Manifest-backed validation requires non-empty `source_commit`, `reference_commit`, `license_inventory`, `provenance_manifest`, `image_metadata.board`, `image_metadata.device_model`, `image_metadata.asic`, and `tool_versions.espflash`.
    - Manifest-backed validation requires artifact paths containing `esp-miner.bin`, `www.bin`, `bitaxe-ultra205-factory.bin`, `otadata-initial.bin`, and `bitaxe-ultra205-package.json` evidence via the manifest path itself.
    - Every required artifact entry must contain a 64-character lowercase hex `sha256`.
    - If the provenance manifest still contains `Awaiting package output evidence` while a manifest is supplied, release-gate validation fails.
  </behavior>
  <action>Add release-gate tests first in `tools/parity/src/release_gate.rs`: `release_gate_manifest_requires_schema_two`, `release_gate_manifest_requires_named_ultra205_artifacts`, `release_gate_manifest_rejects_bad_sha256`, and `release_gate_manifest_requires_artifact_review_closure`. Implement the manifest parser with `serde_json::Value`, not ad hoc string splitting. Keep the existing optional manifest behavior: default `bazel run //tools/parity:report -- release-gate` must still validate docs and cargo-about without requiring a package manifest, while `bazel run //tools/parity:report -- release-gate --manifest bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json` must enforce the new manifest-backed checks per D-09 and D-11.</action>
  <verify>
    <automated>cargo test -p bitaxe-parity --all-features release_gate_manifest</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "release_gate_manifest_requires_schema_two|release_gate_manifest_requires_named_ultra205_artifacts|release_gate_manifest_rejects_bad_sha256|release_gate_manifest_requires_artifact_review_closure" tools/parity/src/release_gate.rs`
    `rg -n "schema_version|esp-miner.bin|www.bin|bitaxe-ultra205-factory.bin|otadata-initial.bin|sha256|Awaiting package output evidence|serde_json::Value" tools/parity/src/release_gate.rs`
    The automated Cargo test command exits 0.
  </acceptance_criteria>
  <done>Release-gate validation ties supplied package manifests to concrete Ultra 205 artifacts, checksums, provenance, and artifact-review closure.</done>
</task>

<task type="auto">
  <name>Task 3: Prove canonical guard commands still work</name>
  <read_first>
    tools/parity/src/main.rs
    tools/parity/src/release_gate.rs
    tools/parity/BUILD.bazel
    Justfile
  </read_first>
  <files>tools/parity/src/main.rs, tools/parity/src/release_gate.rs</files>
  <action>Run the focused parity tests through Bazel and then run the canonical command evidence paths from D-09. Do not change checklist rows in this plan. If default `release-gate` fails because a previous local edit left release docs invalid, fix only the guard-code bug if the failure points to the code; otherwise record the external doc blocker in the plan summary and leave docs to Plan 08-04.</action>
  <verify>
    <automated>cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features && bazel test //tools/parity:tests && just parity && bazel run //tools/parity:report -- release-gate</automated>
  </verify>
  <acceptance_criteria>
    `bazel test //tools/parity:tests` exits 0.
    `just parity` exits 0 and prints `validation_errors: none`.
    `bazel run //tools/parity:report -- release-gate` exits 0 and prints `release_gate: passed`.
  </acceptance_criteria>
  <done>The hardened guard path is reachable through the repo's canonical Bazel and Just commands.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| checklist-to-release-claim | Markdown checklist rows become public parity and release-readiness claims. |
| manifest-to-release-gate | JSON package metadata influences whether artifacts are considered releasable. |
| deferred-scope-to-release | Deferred board, ASIC, protocol, accessory, and UI scope can be accidentally treated as current-release evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-08-01-01 | Repudiation | `tools/parity/src/main.rs` | mitigate | Reject verified rows lacking required evidence tokens and REL-08 notes. |
| T-08-01-02 | Tampering | `tools/parity/src/release_gate.rs` | mitigate | Parse manifest JSON and require schema/artifact/checksum fields when `--manifest` is supplied. |
| T-08-01-03 | Repudiation | Deferred-scope checklist rows | mitigate | Reject verified non-205/deferred rows that try to reuse Ultra 205 evidence. |
| T-08-01-04 | Information Disclosure | Evidence validation | accept | This plan adds no raw hardware logs or secrets; secret redaction is enforced in later evidence plans. |
</threat_model>

<verification>
Run `cargo test -p bitaxe-parity --all-features`, `cargo test -p bitaxe-parity --all-features release_gate_manifest`, then the Rust pre-commit sequence required by `AGENTS.md` in this order: `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`. Then run `bazel test //tools/parity:tests`, `just parity`, and `bazel run //tools/parity:report -- release-gate`.
</verification>

<success_criteria>
`tools/parity` rejects unsupported verified claims for release, OTA, filesystem, safety-critical, and deferred-scope rows. Default release-gate remains canonical, and manifest-backed release-gate checks concrete Ultra 205 package artifacts when a manifest is supplied.
</success_criteria>

<output>
After completion, create `.planning/phases/08-parity-evidence-and-ultra-205-release-gate/08-01-SUMMARY.md`.
</output>
