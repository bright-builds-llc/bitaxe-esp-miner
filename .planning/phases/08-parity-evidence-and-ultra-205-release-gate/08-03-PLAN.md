---
phase: 08-parity-evidence-and-ultra-205-release-gate
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - docs/parity/evidence/phase-08-ultra-205-release-gate.md
autonomous: true
requirements: [REL-08, EVD-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-06-28T21-51-32
generated_at: 2026-06-28T22:16:47.573Z
must_haves:
  truths:
    - "Live static, recovery, OTA, OTAWWW, failed-update recovery, and boot-validation evidence runs only after hardware detection and DEVICE_URL discovery succeed."
    - "If the board is not reachable over HTTP, every live HTTP/OTA/recovery surface records a concrete not-run blocker."
    - "Destructive or fault-injection evidence runs only after the recovery path, factory checksum, exact commands, expected observations, and stop criteria are documented."
    - "OTAWWW remains an explicit REL-03 gap unless whole-www update and interrupted-update hardware-regression evidence exists."
  artifacts:
    - path: "docs/parity/evidence/phase-08-ultra-205-release-gate.md"
      provides: "Live Ultra 205 HTTP/OTA/recovery/recovery-path evidence"
  key_links:
    - from: "docs/parity/evidence/phase-08-ultra-205-release-gate.md"
      to: "docs/release/ultra-205.md"
      via: "documented release evidence procedures"
      pattern: "Large Erase"
    - from: "docs/parity/evidence/phase-08-ultra-205-release-gate.md"
      to: "tools/parity/src/main.rs"
      via: "evidence strings consumed by checklist row guards"
      pattern: "interrupted-update"
---

<objective>
Capture or explicitly defer live Ultra 205 release evidence for static, recovery, firmware OTA, invalid OTA, failed-update recovery, OTAWWW gap, rollback/boot validation, large erase, and interrupted update behavior.

Purpose: Implement D-06, D-07, D-08, and D-14 without adding feature scope beyond the current release gate.
Output: Filled Phase 8 evidence sections with command results, sanitized HTTP observations, hardware conclusions, and documented blockers when evidence cannot safely run.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-parity-evidence-and-ultra-205-release-gate/08-CONTEXT.md
@.planning/phases/08-parity-evidence-and-ultra-205-release-gate/08-RESEARCH.md
@.planning/phases/08-parity-evidence-and-ultra-205-release-gate/08-VALIDATION.md
@.planning/phases/08-parity-evidence-and-ultra-205-release-gate/08-01-SUMMARY.md
@.planning/phases/08-parity-evidence-and-ultra-205-release-gate/08-02-SUMMARY.md
@docs/release/ultra-205.md
@docs/parity/evidence/phase-08-ultra-205-release-gate.md
@crates/bitaxe-api/src/update_plan.rs
@crates/bitaxe-api/src/static_plan.rs
@firmware/bitaxe/src/http_api.rs
@firmware/bitaxe/src/ota_update.rs
@firmware/bitaxe/src/boot_validation.rs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Capture live static, recovery, and OTAWWW gap responses</name>
  <read_first>
    docs/parity/evidence/phase-08-ultra-205-release-gate.md
    docs/release/ultra-205.md
    crates/bitaxe-api/src/static_plan.rs
    crates/bitaxe-api/src/update_plan.rs
    firmware/bitaxe/src/static_files.rs
    firmware/bitaxe/src/http_api.rs
  </read_first>
  <files>docs/parity/evidence/phase-08-ultra-205-release-gate.md</files>
  <action>If `DEVICE_URL status:` is not `established`, do not run curl. If the status is `blocked - hardware detection failed`, update `Static And Recovery HTTP Smoke` and `OTAWWW Gap Response` with `not run - hardware detection blocked` and the detector blocker from Plan 08-02. If the status is `blocked - no reachable DEVICE_URL`, update those sections with `not run - no reachable DEVICE_URL` and the exact URL blocker from Plan 08-02. If `DEVICE_URL status: established`, run these local probes using the uncommitted exact URL: `curl --include --max-time 10 "$DEVICE_URL/"`, `curl --include --compressed --max-time 10 "$DEVICE_URL/assets/app.css.gz"`, `curl --include --max-time 10 "$DEVICE_URL/phase8-missing-static-smoke"`, `curl --include --max-time 10 "$DEVICE_URL/recovery"`, and `curl --include --max-time 20 --request POST --data-binary @bazel-bin/firmware/bitaxe/www.bin "$DEVICE_URL/api/system/OTAWWW"`. Record sanitized command templates, HTTP status, key headers, body snippets without private endpoint data, and exact expected OTAWWW body `Wrong API input`. Record missing static behavior as either redirect to `/` or the actual observed status/body. Do not mark OTAWWW verified; keep it as REL-03 gap unless evidence contains hardware-regression plus interrupted-update proof.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; f=docs/parity/evidence/phase-08-ultra-205-release-gate.md; section_has() { local heading="## $1"; shift; local content; content=$(awk -v heading="$heading" "BEGIN{inside=0} \\$0==heading{inside=1; next} inside && /^## /{exit} inside{print}" "$f"); for term in "$@"; do printf "%s\n" "$content" | rg -q --fixed-strings "$term"; done; }; if rg -q --fixed-strings "DEVICE_URL status: established" "$f"; then section_has "Static And Recovery HTTP Smoke" "curl --include --max-time 10" "/assets/app.css.gz" "phase8-missing-static-smoke" "/recovery" "HTTP status"; section_has "OTAWWW Gap Response" "/api/system/OTAWWW" "Wrong API input" "HTTP status"; rg -q --fixed-strings "sanitized URL evidence: redacted-local-device-url" "$f"; elif rg -q --fixed-strings "DEVICE_URL status: blocked - hardware detection failed" "$f"; then section_has "Static And Recovery HTTP Smoke" "not run - hardware detection blocked"; section_has "OTAWWW Gap Response" "not run - hardware detection blocked"; rg -q --fixed-strings "detector result: blocked - zero, multiple, or invalid Ultra 205 USB candidates" "$f"; elif rg -q --fixed-strings "DEVICE_URL status: blocked - no reachable DEVICE_URL" "$f"; then section_has "Static And Recovery HTTP Smoke" "not run - no reachable DEVICE_URL"; section_has "OTAWWW Gap Response" "not run - no reachable DEVICE_URL"; rg -q --fixed-strings "no IP, DHCP, Wi-Fi association, AP address, mDNS, hostname, or operator-supplied reachable URL" "$f"; else printf "missing supported DEVICE_URL status in %s\n" "$f" >&2; exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    Evidence contains `/`, `/assets/app.css.gz`, `phase8-missing-static-smoke`, `/recovery`, and `/api/system/OTAWWW` observations or the exact hardware-detection/DEVICE_URL not-run blocker for each relevant section.
    If OTAWWW was probed, evidence contains `Wrong API input`.
    Evidence does not contain the exact private `DEVICE_URL`.
  </acceptance_criteria>
  <done>Static/recovery/OTAWWW live surfaces are either observed on Ultra 205 or explicitly blocked without promotion.</done>
</task>

<task type="auto">
  <name>Task 2: Capture firmware OTA, failed-update recovery, and boot validation</name>
  <read_first>
    docs/parity/evidence/phase-08-ultra-205-release-gate.md
    docs/release/ultra-205.md
    crates/bitaxe-api/src/update_plan.rs
    firmware/bitaxe/src/http_api.rs
    firmware/bitaxe/src/ota_update.rs
    firmware/bitaxe/src/boot_validation.rs
    Justfile
  </read_first>
  <files>docs/parity/evidence/phase-08-ultra-205-release-gate.md</files>
  <action>If `DEVICE_URL status:` is not `established`, update `Firmware OTA Accepted Upload`, `Invalid Image Rejection`, `Failed Update Recovery`, and `Rollback And Boot Validation` with either `not run - hardware detection blocked` or `not run - no reachable DEVICE_URL`, matching the exact blocker from Plan 08-02, and stop. If established, read the `esp-miner.bin` SHA-256 from `bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json`, then run `curl --include --max-time 180 --request POST --data-binary @bazel-bin/firmware/bitaxe/esp-miner.bin "$DEVICE_URL/api/system/OTA"`. Record expected success body `Firmware update complete, rebooting now!`, actual HTTP status/body, artifact checksum, reboot observation, and post-reboot monitor evidence from `just monitor port=<port>`. After the device is reachable again or explicitly not reachable, create an invalid image in a temporary path outside the repo, upload it with `curl --include --max-time 60 --request POST --data-binary @<tmp-invalid-image> "$DEVICE_URL/api/system/OTA"`, and record actual HTTP status/body plus exact evidence label `invalid image rejection` and expected public failure body `Validation / Activation Error` or actual `Protocol Error`/`Write Error` if the adapter rejects earlier. Then record `failed update recovery` evidence under `Failed Update Recovery`: `post-failed-update operability` from monitor/boot-validation state plus `/` or `/recovery` HTTP status, `recovery outcome` as passed or blocked, and the concrete recovery command `just flash board=205 port=<port>` if the board must be reflashed to return to service. Record boot-validation log lines containing `ota_boot_validation=marked_valid`, `ota_boot_validation=marked_invalid_reboot`, or `ota_boot_validation=not_pending state=<state>`. If any established-device step cannot run, record the exact not-run blocker in the affected section: `not run - firmware OTA blocked: <reason>`, `not run - invalid image rejection blocked: <reason>`, `not run - failed update recovery blocked: <reason>`, or `not run - boot validation blocked: <reason>`. Do not claim rollback verified when any affected blocker is present.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; f=docs/parity/evidence/phase-08-ultra-205-release-gate.md; section_text() { local heading="## $1"; awk -v heading="$heading" "BEGIN{inside=0} \\$0==heading{inside=1; next} inside && /^## /{exit} inside{print}" "$f"; }; section_has() { local heading="$1"; shift; local content; content=$(section_text "$heading"); for term in "$@"; do printf "%s\n" "$content" | rg -q --fixed-strings "$term"; done; }; section_contains() { local heading="$1"; local term="$2"; section_text "$heading" | rg -q --fixed-strings "$term"; }; if rg -q --fixed-strings "DEVICE_URL status: established" "$f"; then if section_contains "Firmware OTA Accepted Upload" "not run - firmware OTA blocked:"; then section_has "Firmware OTA Accepted Upload" "not run - firmware OTA blocked:"; else section_has "Firmware OTA Accepted Upload" "Firmware update complete, rebooting now!" "esp-miner.bin" "sha256" "HTTP status"; fi; if section_contains "Invalid Image Rejection" "not run - invalid image rejection blocked:"; then section_has "Invalid Image Rejection" "not run - invalid image rejection blocked:"; else section_has "Invalid Image Rejection" "/api/system/OTA" "invalid image rejection" "HTTP status"; fi; if section_contains "Failed Update Recovery" "not run - failed update recovery blocked:"; then section_has "Failed Update Recovery" "not run - failed update recovery blocked:"; else section_has "Failed Update Recovery" "failed update recovery" "post-failed-update operability" "recovery outcome" "HTTP status"; fi; if section_contains "Rollback And Boot Validation" "not run - boot validation blocked:"; then section_has "Rollback And Boot Validation" "not run - boot validation blocked:"; else section_has "Rollback And Boot Validation" "ota_boot_validation="; fi; rg -q --fixed-strings "sanitized URL evidence: redacted-local-device-url" "$f"; elif rg -q --fixed-strings "DEVICE_URL status: blocked - hardware detection failed" "$f"; then for section in "Firmware OTA Accepted Upload" "Invalid Image Rejection" "Failed Update Recovery" "Rollback And Boot Validation"; do section_has "$section" "not run - hardware detection blocked"; done; elif rg -q --fixed-strings "DEVICE_URL status: blocked - no reachable DEVICE_URL" "$f"; then for section in "Firmware OTA Accepted Upload" "Invalid Image Rejection" "Failed Update Recovery" "Rollback And Boot Validation"; do section_has "$section" "not run - no reachable DEVICE_URL"; done; else printf "missing supported DEVICE_URL status in %s\n" "$f" >&2; exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    Evidence contains valid OTA result, invalid OTA result, failed update recovery, post-failed-update operability, recovery outcome, and `ota_boot_validation=` observations, or each section contains the exact not-run blocker from Plan 08-02.
    Evidence contains `esp-miner.bin` and a SHA-256 value when OTA ran.
    Evidence does not contain Wi-Fi credentials, pool credentials, NVS secret values, or exact private endpoint URLs.
  </acceptance_criteria>
  <done>Firmware OTA, failed-update recovery, and boot-validation evidence is recorded without treating upload success alone as rollback proof.</done>
</task>

<task type="auto">
  <name>Task 3: Document and conditionally execute destructive recovery checks</name>
  <read_first>
    docs/parity/evidence/phase-08-ultra-205-release-gate.md
    docs/release/ultra-205.md
    AGENTS.md
    Justfile
    tools/flash/src/main.rs
  </read_first>
  <files>docs/parity/evidence/phase-08-ultra-205-release-gate.md</files>
  <action>Before any destructive command, write a `Destructive Procedure Gate` subsection under `Large Erase Recovery` with these exact recovery facts: manifest path `bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json`, factory artifact `bitaxe-ultra205-factory.bin`, exact recovery command `just flash board=205 port=<port>`, exact monitor command `just monitor port=<port>`, erase command `espflash erase-flash --chip esp32s3 --port <port> --non-interactive`, expected recovery observations `partition table present`, `spiffs_mount=available`, `ota_boot_validation=`, and `axeos_api_route_shell=started`, and stop criteria `no detected port`, `missing factory artifact checksum`, `no reachable DEVICE_URL after non-destructive smoke`, `valid OTA did not return to an operable state`, or `operator recovery instructions missing`. If any stop criterion is true, set `Large Erase Recovery` and `Interrupted Update Recovery` to `not run - recovery gate blocked: <criterion>`. If the gate passes, run the erase command, then immediately run `just flash board=205 port=<port>` and `just monitor port=<port>`, recording `recovery outcome` plus the observed partition table, SPIFFS, boot-validation, and HTTP route status. For interrupted firmware update, only run `curl --include --limit-rate 1024 --max-time 2 --request POST --data-binary @bazel-bin/firmware/bitaxe/esp-miner.bin "$DEVICE_URL/api/system/OTA"` if the exact interruption point and recovery command are recorded first; otherwise set `Interrupted Update Recovery` to `not run - no deterministic interruption point`. After any interruption attempt, recover with `just flash board=205 port=<port>` and record `recovery outcome` plus `final boot/static/recovery status`.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; f=docs/parity/evidence/phase-08-ultra-205-release-gate.md; section_text() { local heading="## $1"; awk -v heading="$heading" "BEGIN{inside=0} \\$0==heading{inside=1; next} inside && /^## /{exit} inside{print}" "$f"; }; section_has() { local heading="$1"; shift; local content; content=$(section_text "$heading"); for term in "$@"; do printf "%s\n" "$content" | rg -q --fixed-strings "$term"; done; }; section_contains() { local heading="$1"; local term="$2"; section_text "$heading" | rg -q --fixed-strings "$term"; }; section_has "Large Erase Recovery" "Destructive Procedure Gate" "bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json" "bitaxe-ultra205-factory.bin" "just flash board=205 port=" "just monitor port=" "espflash erase-flash --chip esp32s3 --port" "partition table present" "spiffs_mount=available" "ota_boot_validation=" "axeos_api_route_shell=started"; if section_contains "Large Erase Recovery" "not run - recovery gate blocked:"; then section_has "Large Erase Recovery" "not run - recovery gate blocked:"; else section_has "Large Erase Recovery" "espflash erase-flash --chip esp32s3" "recovery outcome" "just flash board=205 port=" "just monitor port="; fi; if section_contains "Interrupted Update Recovery" "not run - recovery gate blocked:"; then section_has "Interrupted Update Recovery" "not run - recovery gate blocked:"; elif section_contains "Interrupted Update Recovery" "not run - no deterministic interruption point"; then section_has "Interrupted Update Recovery" "not run - no deterministic interruption point"; else section_has "Interrupted Update Recovery" "curl --include --limit-rate 1024" "recovery outcome" "just flash board=205 port=" "final boot/static/recovery status"; fi'</automated>
  </verify>
  <acceptance_criteria>
    Evidence contains the exact erase, flash, and monitor command strings before any destructive conclusion.
    Evidence contains either large erase pass/recovery observations or `not run - recovery gate blocked: <criterion>`.
    Evidence contains either interrupted-update pass/recovery observations or `not run - no deterministic interruption point`.
  </acceptance_criteria>
  <done>Destructive/fault-injection release evidence is captured only when recovery is explicit; otherwise pending evidence is concrete and visible.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| local-network-to-update-routes | HTTP upload requests can trigger OTA or static-update behavior. |
| firmware-to-bootloader | OTA activation and rollback state cross from app firmware into ESP-IDF bootloader decisions. |
| destructive-host-command-to-flash | `espflash erase-flash` can erase the physical Ultra 205 and deny service until recovered. |
| evidence-to-repo | Live logs and HTTP output can contain private local details. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-08-03-01 | Elevation of Privilege | `/api/system/OTA` | mitigate | Exercise OTA only through existing access-planned route; record responses without weakening `plan_http_access`. |
| T-08-03-02 | Denial of Service | `espflash erase-flash` and interrupted OTA | mitigate | Require a documented factory recovery path, checksum, exact commands, and stop criteria before destructive actions. |
| T-08-03-03 | Repudiation | OTA evidence | mitigate | Record valid upload, invalid upload, reboot, running/boot-validation state, and recovery outcome; upload success alone is insufficient. |
| T-08-03-04 | Information Disclosure | Hardware evidence | mitigate | Commit sanitized command templates/statuses only; omit exact private URL, Wi-Fi credentials, pool credentials, NVS secret values, and private endpoints. |
| T-08-03-05 | Tampering | Static path probes | mitigate | Probe existing routes only; do not modify static assets or `reference/esp-miner`. |
</threat_model>

<verification>
Run the three branch-aware evidence checks above. When `DEVICE_URL status: established`, also run the listed `curl`, `just monitor`, `espflash erase-flash`, and recovery commands according to the documented gates. Finish with `just parity` to confirm no unsupported verified claims were introduced.
</verification>

<success_criteria>
Every Phase 8 live release surface named in D-06 contains either observed Ultra 205 evidence or a concrete blocker, including failed update recovery, post-failed-update operability, and recovery outcome. Destructive and fault-injection cases are either recovered through documented commands or explicitly pending because the recovery/interruption gate did not pass.
</success_criteria>

<output>
After completion, create `.planning/phases/08-parity-evidence-and-ultra-205-release-gate/08-03-SUMMARY.md`.
</output>
