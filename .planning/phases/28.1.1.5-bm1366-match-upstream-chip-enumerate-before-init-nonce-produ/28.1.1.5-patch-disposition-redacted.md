# Phase 28.1.1.5 Patch Disposition

ab_label: count_asic_chips_rx_loop_parity
ab_outcome: unchanged
result_correlated: false
fake_pool_submit_observed: false
read_chip_id_tx_match: true
address_interval_match: true
chip_count_source_class_rust: counted_rx
enumerate_to_mining_ready_gap_class: drain_idle_like
power_delta_class: falling
wire_parity_ticket_mask_retained: true
patch_disposition: falsified_count_asic_chips_rx_loop_parity_as_sole_blocker
wire_parity_rx_loop_retained: true
next_hypothesis: version_rolling_negotiation
second_speculative_patch_applied: false
read_chip_id_byte_patch_applied: false
job_byte_patch_applied: false
post_max_baud_delay_2000_reintroduced: false
register_read_poll_defaulted: false
upstream_like_long_block_receive_defaulted: false
ticket_mask_asic_difficulty_reopened_as_sole_blocker: false
version_rolling_implemented: false
phase30_promotion_input: pending
raw_bytes_committed: false
credential_contents_read: false
files_changed: []
default_promotion_skipped: true

## Gate application

Plan 02 A/B reported `ab_outcome: unchanged` with `result_correlated: false` and `fake_pool_submit_observed: false`.
Per D-13 / Plan 03 decision gate, `count_asic_chips_rx_loop_parity` is **not** promoted as a sole-blocker fix (`patch_disposition: falsified_count_asic_chips_rx_loop_parity_as_sole_blocker`).

Enumerate markers otherwise match after the patch: `read_chip_id_tx_match: true`, `address_interval_match: true`, `chip_count_source_class_rust: counted_rx`, `enumerate_to_mining_ready_gap_class: drain_idle_like`. Cores remain idle (`power_delta_class: falling`). Per D-07, route `next_hypothesis: version_rolling_negotiation` without implementing version-rolling in this phase.

Task 2 keep/revert: **KEEP** RX-loop / counted-handoff control-flow parity (`wire_parity_rx_loop_retained: true`). A/B did not `regressed` correlate/submit relative to baseline, so single-shot detect revert is not warranted. Sole-blocker claim is falsified; wire/control-flow parity retained. No second speculative patch.

## Next hypothesis (deferred)

`version_rolling_negotiation` — after enumerate/count/address markers match and hashing remains idle, version-rolling / `mining.configure` negotiation is the next highest remaining suspect (D-07).
Do **not** implement version-rolling in this phase.

## Task 2 code policy verification

- Policy applied: keep RX-loop + counted handoff (`wire_parity_rx_loop_retained: true`); no revert.
- Code churn: none — Plan 02 `chip_detect` drain-until-idle / soft-retry / counted handoff retained as wire-correct default.
- ASIC-256 ticket-mask wire retained (`wire_parity_ticket_mask_retained: true`).
- No version-rolling / `mining.configure` negotiation code added.

## Non-claims

- No job-byte patch (D-15).
- No ReadChipId `0x0A` byte patch (D-02/D-15).
- No `post_max_baud_delay_2000` reintroduction (D-15).
- No register-read poll default-on (D-15).
- No upstream-like long-block RX default-on (D-15).
- No ticket-mask ASIC-difficulty reopened as sole blocker (D-15); ASIC-256 retained.
- No Phase 30 checklist `verified` edits (D-15/D-18); `phase30_promotion_input: pending`.
- No version-rolling implementation this plan (`version_rolling_implemented: false`).
- Sole-blocker claim for `count_asic_chips_rx_loop_parity` is falsified; wire-parity RX-loop retained.
