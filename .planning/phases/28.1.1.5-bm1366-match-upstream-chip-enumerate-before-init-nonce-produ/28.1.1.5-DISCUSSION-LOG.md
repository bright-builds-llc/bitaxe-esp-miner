---
generated_by: gsd-discuss-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1.5-2026-07-09T11-47-00
generated_at: 2026-07-09T11:47:00Z
---

# Phase 28.1.1.5 Discussion Log

**Mode:** YOLO — recommended decisions auto-accepted (no interactive review).

## Gray areas considered

1. Is the lever still the raw `0x0A` chip-ID frame?
2. What does “chip-enumerate before init” mean given Phase 27 frame match?
3. Which single A/B label to force first?
4. What is the fail-closed next hypothesis if falsified?

## Recommended answers (accepted)

1. **No** — Phase 27 already records TX frame match for `{55 AA 52 05 00 00 0A}` / `ReadChipId`. Do not reopen as a byte-mismatch patch.
2. **Sequencing / count / address-split** — upstream `count_asic_chips` + `address_interval = 256 / chip_counter` vs Rust config-driven `SetChipAddress` after chip-detect + mining-ready; also inter-command order/delays and multi-frame RX count semantics.
3. **Defer exact A/B label to Plan 02** after Wave 0 comparator names one of: counted-chip address interval, count_asic_chips RX loop parity, or inter-command delay/order — one only.
4. **`version_rolling_negotiation`** as default next_hypothesis if falsified and markers otherwise match (unless evidence points elsewhere).

## Carry-forward bans

- Job-byte field patches
- `post_max_baud_delay_2000`
- `match_upstream_register_read_poll`
- `upstream_like_long_block_receive`
- `ticket_mask_asic_difficulty` as sole blocker (ASIC-256 wire parity retained)
- Phase 30 checklist `verified` edits
- Second speculative patch in the same phase

## Hardware policy

Detector-gated Ultra 205; ≥360 s fake-pool preferred; wall clock ≥420 s; redaction-safe commits only.
