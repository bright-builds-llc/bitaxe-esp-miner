---
phase: 28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase28.1.1.5-chip-enumerate-compare.mjs
  - scripts/phase28.1.1.5-chip-enumerate-compare-test.mjs
autonomous: true
requirements: [CFG-07, ASIC-11, STR-09]
user_setup: []
must_haves:
  truths:
    - "A repo-owned chip-enumerate comparator emits version_mask prelude class, ReadChipId/enumerate TX present, chip_detected / chip_count_class, address_assignment vs expected, enumerate_to_mining_ready_gap_class, power_delta_class, result_correlated, and fake_pool_submit_observed without raw UART hex or credentials"
    - "When ReadChipId TX matches and Ultra 205 address_interval is interval_256 but Rust chip_count_source is config_expected / RX loop is single-shot (gap immediate), forced_ab_label is count_asic_chips_rx_loop_parity"
    - "recommended_investigation never emits post_max_baud_delay_2000, match_upstream_register_read_poll, upstream_like_long_block_receive, or ticket_mask_asic_difficulty"
  artifacts:
    - path: "scripts/phase28.1.1.5-chip-enumerate-compare.mjs"
      provides: "Redaction-safe chip-enumerate / count / address category report composing 28.1.1.4/2 parsers"
      contains: "count_asic_chips_rx_loop_parity"
    - path: "scripts/phase28.1.1.5-chip-enumerate-compare-test.mjs"
      provides: "Fixture tests for ReadChipId TX match, RX-loop diverge, Ultra 205 address_interval match, falsified-knob ban"
      contains: "forced_ab_label"
  key_links:
    - from: "scripts/phase28.1.1.5-chip-enumerate-compare.mjs"
      to: "scripts/phase28.1.1.4-init-sequencing-compare.mjs"
      via: "compose/reuse lineEvents classifyFrame summarizeLog / power_delta_class"
      pattern: "phase28.1.1.4-init-sequencing-compare|phase28.1.1.2-result-path-compare"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1.5-2026-07-09T11-47-00
generated_at: 2026-07-09T12:00:00Z
---

<objective>
Ship Wave 0 redaction-safe chip-enumerate comparator so version-mask prelude, ReadChipId TX, chip-count source, address assignment, and enumerate→mining-ready gap are category-compared against upstream SERIALTX, with `power_delta_class` as fast feedback, before any RX-loop patch or hardware A/B.

Purpose: D-02/D-04/D-08 require golden category diff of enumerate/count/handoff (not `0x0A` byte mismatch); RESEARCH forces first A/B label `count_asic_chips_rx_loop_parity` unless Wave 0 evidence renames per Pattern 2.
Output: `scripts/phase28.1.1.5-chip-enumerate-compare.mjs` + test; optional compact firmware marker deferred (skip if uart_trace suffices — D-11).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-CONTEXT.md
@.planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-RESEARCH.md
@.planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-VALIDATION.md
@scripts/phase28.1.1.4-init-sequencing-compare.mjs
@scripts/phase28.1.1.2-result-path-compare.mjs
@crates/bitaxe-asic/src/bm1366/chip_detect.rs
@crates/bitaxe-asic/src/bm1366/command.rs

<interfaces>
<!-- Compose 28.1.1.4 / 28.1.1.2 — do not invent a parallel taxonomy (D-10). -->

From scripts/phase28.1.1.2-result-path-compare.mjs:
- `classifyFrame`, `lineEvents`, `summarizeLog`
- Events: `job_tx`, `result_correlated`, `fake_pool_submit`, chip-detect / enable markers when present

From scripts/phase28.1.1.4-init-sequencing-compare.mjs:
- Reuse `power_delta_class` rules and last-pre-job window selection helpers where practical
- Do **not** reopen ticket-mask as sole-blocker recommender

Confirmed RESEARCH facts (encode in fixtures, not guess):
- Chip-ID TX frame already matches Rust `Bm1366Command::ReadChipId` — `read_chip_id_tx_match: true` is a precondition, not a patch target (D-02)
- Ultra 205 `address_interval = 256` when chip_count=1 on both sides — numeric match does **not** mean enumerate RX parity
- Rust today: single `read_exact(11)` + `validate_single_chip_detect_response` hardcodes `detected_chips = 1`; mining-ready uses catalog `expected_chips()`
- Upstream: `count_asic_chips` drain-until-idle + soft preamble/CRC retry; `address_interval = 256 / chip_counter`

D-08 required report fields (exact keys):
- `version_mask_prelude_count_class_rust: 0|3|other`
- `version_mask_prelude_match: true|false`
- `read_chip_id_tx_present_upstream: true|false`
- `read_chip_id_tx_present_rust: true|false`
- `read_chip_id_tx_match: true|false`
- `chip_detected_rust: true|false`
- `chip_count_class_upstream: counted_N|unavailable`
- `chip_count_class_rust: config_expected_N|counted_N|unavailable`
- `chip_count_source_class_rust: config_expected|counted_rx`
- `address_assignment_count_rust: N`
- `address_interval_class_rust: interval_256|other`
- `address_interval_match: true|false`
- `enumerate_to_mining_ready_gap_class: immediate|drain_idle_like|delayed_other|unavailable`
- `power_delta_class: rising_hashing|flat|falling|unavailable`
- `result_correlated: true|false`
- `fake_pool_submit_observed: true|false`
- `difficulty_mask_class_rust: diff_256|other`
- `wire_parity_ticket_mask_retained: true|false`
- `forced_ab_label: count_asic_chips_rx_loop_parity|counted_chip_address_interval|enumerate_to_mining_ready_gap|none`
- `recommended_investigation:` closed enum below
- `raw_bytes_committed: false`
- `credential_contents_read: false`
- `phase30_promotion_input: pending`
- `read_chip_id_byte_patch_recommended: false` (always false — D-02)

Closed `recommended_investigation` set for this phase (D-09):
`match_upstream_chip_enumerate_before_init` | `version_rolling_negotiation` | `none`

HARD BAN (fail tests if emitted as recommended_investigation or forced_ab_label):
`post_max_baud_delay_2000`, `match_upstream_register_read_poll`, `upstream_like_long_block_receive`, `ticket_mask_asic_difficulty`

`forced_ab_label` default (RESEARCH Pattern 2 / D-05):
1. If blocked → `none`
2. Else if address_interval mismatch on Ultra 205 (unexpected) → `counted_chip_address_interval`
3. Else if RX loop already drain_idle_like on Rust and only timing gap remains → `enumerate_to_mining_ready_gap`
4. Else (expected Ultra 205 path: TX match + interval_256 + config_expected / immediate gap) → `count_asic_chips_rx_loop_parity`
5. **Never** emit HARD BAN labels

power_delta_class rules (D-06; fast feedback only — not phase pass gate):
- `unavailable` if marker missing or `unavailable=true`
- `rising_hashing` if `delta_mw >= 2000`
- `falling` if `delta_mw <= -200`
- `flat` otherwise
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement chip-enumerate comparator CLI with D-08 fields</name>
  <files>scripts/phase28.1.1.5-chip-enumerate-compare.mjs</files>
  <read_first>
    - scripts/phase28.1.1.4-init-sequencing-compare.mjs
    - scripts/phase28.1.1.2-result-path-compare.mjs
    - .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-CONTEXT.md (D-02, D-04, D-06, D-08, D-09, D-10)
    - .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-RESEARCH.md (Patterns 1–2, Pitfalls 1/3/4, Already Matched table)
  </read_first>
  <behavior>
    - Missing upstream or rust log → `comparison_status: blocked_safe_prerequisite`, `recommended_investigation: none`, `forced_ab_label: none`
    - Fixture: ReadChipId TX match true, address_interval_match true (interval_256), rust chip_count_source config_expected, gap immediate, power_delta falling, no correlate → `forced_ab_label: count_asic_chips_rx_loop_parity`, `recommended_investigation: match_upstream_chip_enumerate_before_init`
    - Fixture: both sides drain_idle_like + counted_rx + still no correlate → `recommended_investigation: version_rolling_negotiation` (or `none` if correlate true)
    - Never recommend falsified knobs or ticket_mask_asic_difficulty; never recommend ReadChipId `0x0A` byte patch (`read_chip_id_byte_patch_recommended: false`)
    - Report has no multi-byte hex dumps or credential sentinels
  </behavior>
  <action>
    Create `scripts/phase28.1.1.5-chip-enumerate-compare.mjs` (Node ESM, stdlib only) accepting `--upstream`, `--rust`, `--out` (per D-10 compose with 28.1.1.4/2 — import `lineEvents` / `classifyFrame` / `summarizeLog` from `scripts/phase28.1.1.2-result-path-compare.mjs`; reuse power_delta / window helpers from 28.1.1.4 where practical; do not invent a parallel capture stack).

    Classify enumerate/count/address categories from markers and frame events only (no committed hex). Detect ReadChipId / chip-detect TX presence via existing event taxonomy or length/class markers already emitted by uart_trace / SERIALTX parsers — never require re-encoding `0x0A` as a patch target (D-02).

    Derive `enumerate_to_mining_ready_gap_class` from timestamps/markers between chip-detect complete and first mining-ready write when available; otherwise `unavailable`.

    Recommender (D-09 / D-04 / D-05):
    1. If blocked → `none`
    2. Else if enumerate/count/RX-source diverge (config_expected vs counted, or gap immediate vs drain_idle_like, or missing multi-frame RX markers) AND still no `result_correlated` → `match_upstream_chip_enumerate_before_init`
    3. Else if enumerate markers match AND still no `result_correlated` → `version_rolling_negotiation`
    4. Else → `none`
    5. **Never** emit HARD BAN strings

    Emit `forced_ab_label` per RESEARCH Pattern 2 rules in `<interfaces>` (default `count_asic_chips_rx_loop_parity` for Ultra 205 expected fixture).

    Emit Markdown with all D-08 keys listed in `<interfaces>`. Keep `phase30_promotion_input: pending` (D-15/D-18). Assert `wire_parity_ticket_mask_retained` from difficulty_mask class when present (status only — not a recommender).
  </action>
  <acceptance_criteria>
    - `test -f scripts/phase28.1.1.5-chip-enumerate-compare.mjs`
    - `rg -n "count_asic_chips_rx_loop_parity" scripts/phase28.1.1.5-chip-enumerate-compare.mjs`
    - `rg -n "forced_ab_label" scripts/phase28.1.1.5-chip-enumerate-compare.mjs`
    - `rg -n "enumerate_to_mining_ready_gap_class" scripts/phase28.1.1.5-chip-enumerate-compare.mjs`
    - `rg -n "read_chip_id_tx_match" scripts/phase28.1.1.5-chip-enumerate-compare.mjs`
    - `rg -n "match_upstream_chip_enumerate_before_init|version_rolling_negotiation" scripts/phase28.1.1.5-chip-enumerate-compare.mjs`
    - `rg -n "phase28.1.1.2-result-path-compare" scripts/phase28.1.1.5-chip-enumerate-compare.mjs`
    - `rg -n "read_chip_id_byte_patch_recommended: false" scripts/phase28.1.1.5-chip-enumerate-compare.mjs`
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/phase28.1.1.5-chip-enumerate-compare.mjs && rg -n "forced_ab_label|count_asic_chips_rx_loop_parity|HARD_BAN|ticket_mask_asic_difficulty" scripts/phase28.1.1.5-chip-enumerate-compare.mjs</automated>
  </verify>
  <done>Comparator CLI exists, composes prior parsers, emits D-08 category fields, closed recommender, and forced_ab_label. Behavioral recommender/ban fixtures are gated by Task 2 compare-test (Wave 0 behavioral gate).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 fixture tests locking recommender and bans</name>
  <files>scripts/phase28.1.1.5-chip-enumerate-compare-test.mjs</files>
  <read_first>
    - scripts/phase28.1.1.4-init-sequencing-compare-test.mjs
    - scripts/phase28.1.1.5-chip-enumerate-compare.mjs
    - .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-VALIDATION.md
  </read_first>
  <behavior>
    - Unit fixtures cover: TX match + interval_256 + config_expected/immediate → `forced_ab_label: count_asic_chips_rx_loop_parity` + recommend `match_upstream_chip_enumerate_before_init`; enumerate matched + no correlate → `version_rolling_negotiation`; blocked missing logs; `power_delta_class` falling/rising/unavailable
    - Assert report body never matches multi-byte hex dump patterns like `55 AA 52` / `55 AA 51` sequences as committed content, and never contains `poolPassword` / `poolURL` / credential JSON keys
    - Assert HARD BAN strings never appear as `recommended_investigation` or `forced_ab_label` values
    - Assert `read_chip_id_byte_patch_recommended: false` always
    - No firmware marker required in this plan (D-11: skip if uart_trace suffices)
  </behavior>
  <action>
    Create `scripts/phase28.1.1.5-chip-enumerate-compare-test.mjs` using `node:assert` (mirror 28.1.1.4 test style). Include synthetic upstream/rust log fixtures (inline strings, not committed scratch hex dumps) that encode:
    1. Expected Ultra 205 diverge: ReadChipId TX match, address_interval_match true, rust `chip_count_source_class_rust: config_expected`, gap `immediate`, `delta_mw=-390` → falling, `forced_ab_label: count_asic_chips_rx_loop_parity`, recommend `match_upstream_chip_enumerate_before_init`
    2. Enumerate matched path: both drain_idle_like + counted_rx + interval match, still no correlate → recommend `version_rolling_negotiation`
    3. Missing log → blocked / `forced_ab_label: none`
    4. Falsified-knob ban: after running recommender on gap fixtures, assert recommendation and forced_ab_label ∉ HARD BAN set (including `ticket_mask_asic_difficulty`)
    5. Never recommend `0x0A` byte patch

    Do not patch `chip_detect.rs` / adapter RX loop in this plan (Plan 02 owns the patch). Do not edit firmware markers unless comparator cannot classify from existing traces — prefer skip (D-11).
  </action>
  <acceptance_criteria>
    - `test -f scripts/phase28.1.1.5-chip-enumerate-compare-test.mjs`
    - `node scripts/phase28.1.1.5-chip-enumerate-compare-test.mjs` exits 0
    - `rg -n "count_asic_chips_rx_loop_parity" scripts/phase28.1.1.5-chip-enumerate-compare-test.mjs`
    - `rg -n "post_max_baud_delay_2000|match_upstream_register_read_poll|upstream_like_long_block_receive|ticket_mask_asic_difficulty" scripts/phase28.1.1.5-chip-enumerate-compare-test.mjs` (ban assertions present)
    - `rg -n "forced_ab_label" scripts/phase28.1.1.5-chip-enumerate-compare-test.mjs`
    - `rg -n "read_chip_id_tx_match" scripts/phase28.1.1.5-chip-enumerate-compare-test.mjs`
  </acceptance_criteria>
  <verify>
    <automated>node scripts/phase28.1.1.5-chip-enumerate-compare-test.mjs</automated>
  </verify>
  <done>Wave 0 compare-test green; recommender, forced_ab_label, and redaction bans locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| USB monitor logs → comparator → committed Markdown | Untrusted device logs may contain UART hex, SSIDs, IPs; committed reports must stay category/boolean only |
| Local credential files → flash/monitor (not this plan) | Credentials must never be read into comparator inputs or reports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28115-01 | I | chip-enumerate-compare.mjs report | mitigate | Emit field categories/counts/booleans only; unit tests fail on hex dumps and credential key leakage (CFG-07) |
| T-28115-02 | I | synthetic fixtures in compare-test | mitigate | Inline category markers only; never commit raw UART hex dumps |
| T-28115-03 | E | phase30_promotion_input / checklist | mitigate | Hard-code `phase30_promotion_input: pending`; no checklist edits in this plan (D-15) |
| T-28115-04 | E | overclaim share outcomes | mitigate | Comparator records correlate/submit booleans only; does not claim STR-09 verified |
| T-28115-05 | T | reference/esp-miner | accept | This plan does not touch reference; later plans use scratch-only helpers + verify-reference |
</threat_model>

<verification>
- `node scripts/phase28.1.1.5-chip-enumerate-compare-test.mjs` passes
- Sample CLI run on synthetic fixtures writes report with D-08 keys and `raw_bytes_committed: false`
</verification>

<success_criteria>
- Comparator detects expected Ultra 205 enumerate RX-loop diverge and sets `forced_ab_label: count_asic_chips_rx_loop_parity`
- Falsified investigation knobs and ticket-mask-as-sole-blocker cannot appear as recommendations
- No raw UART hex or credentials in committed report output; no `0x0A` byte-patch recommendation
</success_criteria>

<output>
After completion, create `.planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-01-SUMMARY.md`
</output>
