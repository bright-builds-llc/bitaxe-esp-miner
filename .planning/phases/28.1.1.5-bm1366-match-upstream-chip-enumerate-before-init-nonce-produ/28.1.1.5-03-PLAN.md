---
phase: 28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ
plan: 03
type: execute
wave: 3
depends_on:
  - 28.1.1.5-02
files_modified:
  - .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-patch-disposition-redacted.md
  - crates/bitaxe-asic/src/bm1366/chip_detect.rs
  - firmware/bitaxe/src/asic_adapter.rs
  - firmware/bitaxe/src/asic_adapter/chip_detect_investigation.rs
autonomous: true
requirements: [ASIC-11, STR-09, CFG-07]
user_setup: []
must_haves:
  truths:
    - "Default RX-loop / counted-handoff remains only if Plan 02 ab_outcome is improved with result_correlated and/or fake_pool_submit_observed true"
    - "If A/B unchanged/regressed after enumerate markers otherwise match, disposition falsifies count_asic_chips_rx_loop_parity as sole blocker and sets next_hypothesis version_rolling_negotiation without a second speculative patch"
    - "No job-byte, 0x0A frame-byte, post_max_baud, register-read poll, long-block, or version-rolling default changes; ASIC-256 ticket mask retained; no Phase 30 checklist verified edits"
  artifacts:
    - path: ".planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-patch-disposition-redacted.md"
      provides: "Single-divergence patch decision record"
      contains: "patch_disposition:"
  key_links:
    - from: ".planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-ab-count_asic_chips_rx_loop_parity-redacted.md"
      to: ".planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-patch-disposition-redacted.md"
      via: "ab_outcome gates keep_default vs falsify"
      pattern: "ab_outcome:"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1.5-2026-07-09T11-47-00
generated_at: 2026-07-09T12:00:00Z
---

<objective>
Promote the `count_asic_chips_rx_loop_parity` change as the lasting default **only** if Plan 02 A/B improved with correlate and/or fake-pool submit; otherwise falsify this lever as sole blocker, keep or revert per evidence rules below, and set `next_hypothesis: version_rolling_negotiation` without implementing version-rolling or any second speculative patch.

Purpose: Enforce D-07/D-13/D-14 — one confirmed divergence at a time; default promotion gated on real nonce/submit evidence; if enumerate markers match after the patch but hashing still idle, route to version-rolling for a later phase.
Output: `28.1.1.5-patch-disposition-redacted.md` plus conditional keep/revert of the Plan 02 RX-loop / handoff change.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-CONTEXT.md
@.planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-RESEARCH.md
@.planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-ab-count_asic_chips_rx_loop_parity-redacted.md
@crates/bitaxe-asic/src/bm1366/chip_detect.rs
@firmware/bitaxe/src/asic_adapter.rs

Decision gate (read A/B report first — D-13):
- `ab_outcome: improved` AND (`result_correlated: true` OR `fake_pool_submit_observed: true`) → `patch_disposition: applied_default_promotion`; **keep** RX-loop + counted handoff as default; `next_hypothesis: none` (or secondary note only); `phase30_promotion_input: ready` only if both correlate and submit observed (still no checklist edit).
- `ab_outcome: unchanged|regressed` → `patch_disposition: falsified_count_asic_chips_rx_loop_parity_as_sole_blocker`:
  - If A/B report shows ReadChipId TX match + address_interval match + enumerate markers otherwise aligned (or gap class moved toward drain_idle_like) → set `next_hypothesis: version_rolling_negotiation` (D-07). **Do not implement version-rolling.** Prefer **keeping** the RX-loop / counted-handoff as wire/control-flow parity even when it did not unblock hashing — document `wire_parity_rx_loop_retained: true` and that sole-blocker claim is falsified. Only revert the Plan 02 change if A/B clearly **regressed** correlate/submit relative to baseline **and** retaining the loop is unsafe; default judgment: keep RX-loop parity, falsify sole-blocker claim.
  - If markers still show residual enumerate diverge → document residual mismatch; still no second speculative patch this phase; `next_hypothesis` may remain `match_upstream_chip_enumerate_before_init` only if evidence points there — otherwise prefer `version_rolling_negotiation` per D-07.
- `ab_outcome: blocked_safe_prerequisite` → `patch_disposition: blocked_safe_prerequisite`; no further code churn beyond documenting block; leave Plan 02 patch state as-is with explicit note.

Bans (D-15/D-07): no job-byte patches; no ReadChipId `0x0A` byte patches; no reintroducing falsified investigation defaults; no version-rolling implementation; keep ASIC-256 ticket-mask wire parity; no Phase 30 checklist `verified` edits.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record patch disposition from A/B evidence</name>
  <files>.planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-patch-disposition-redacted.md</files>
  <read_first>
    - .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-ab-count_asic_chips_rx_loop_parity-redacted.md
    - .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-CONTEXT.md (D-07, D-13, D-14, D-15, D-18)
    - .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-RESEARCH.md (Pattern 5 outcomes)
  </read_first>
  <action>
    Create `28.1.1.5-patch-disposition-redacted.md` with exact fields:
    - `ab_label: count_asic_chips_rx_loop_parity` (or documented Wave 0 rename used in Plan 02)
    - `ab_outcome:` copied from Plan 02 A/B report
    - `result_correlated:` boolean
    - `fake_pool_submit_observed:` boolean
    - `read_chip_id_tx_match:` boolean
    - `address_interval_match:` boolean
    - `chip_count_source_class_rust:`
    - `enumerate_to_mining_ready_gap_class:`
    - `power_delta_class:`
    - `wire_parity_ticket_mask_retained: true|false`
    - `patch_disposition:` one of `applied_default_promotion` | `falsified_count_asic_chips_rx_loop_parity_as_sole_blocker` | `blocked_safe_prerequisite`
    - `wire_parity_rx_loop_retained: true|false` (true when RX-loop / counted handoff kept for upstream control-flow parity)
    - `next_hypothesis:` `none` | `version_rolling_negotiation` | `match_upstream_chip_enumerate_before_init` (prefer `version_rolling_negotiation` when falsified and markers otherwise match — D-07; never implement here)
    - `second_speculative_patch_applied: false`
    - `read_chip_id_byte_patch_applied: false`
    - `job_byte_patch_applied: false`
    - `post_max_baud_delay_2000_reintroduced: false`
    - `register_read_poll_defaulted: false`
    - `upstream_like_long_block_receive_defaulted: false`
    - `ticket_mask_asic_difficulty_reopened_as_sole_blocker: false`
    - `version_rolling_implemented: false`
    - `phase30_promotion_input: pending|ready` (`ready` only if result_correlated + submit observed; still no checklist edit)
    - `raw_bytes_committed: false`
    - `credential_contents_read: false`

    Do not edit `docs/parity/checklist.md` verified rows (D-15/D-18).
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-patch-disposition-redacted.md`
    - `rg -n "^patch_disposition: (applied_default_promotion|falsified_count_asic_chips_rx_loop_parity_as_sole_blocker|blocked_safe_prerequisite)$" .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-patch-disposition-redacted.md`
    - `rg -n "second_speculative_patch_applied: false" .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-patch-disposition-redacted.md`
    - `rg -n "version_rolling_implemented: false" .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-patch-disposition-redacted.md`
    - `rg -n "read_chip_id_byte_patch_applied: false" .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-patch-disposition-redacted.md`
    - `rg -n "ticket_mask_asic_difficulty_reopened_as_sole_blocker: false" .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-patch-disposition-redacted.md`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "^patch_disposition:" .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-patch-disposition-redacted.md</automated>
  </verify>
  <done>Disposition Markdown records keep-vs-falsify decision with D-07 next_hypothesis when applicable.</done>
</task>

<task type="auto">
  <name>Task 2: Apply keep/revert code policy from disposition</name>
  <files>crates/bitaxe-asic/src/bm1366/chip_detect.rs, firmware/bitaxe/src/asic_adapter.rs, firmware/bitaxe/src/asic_adapter/chip_detect_investigation.rs</files>
  <read_first>
    - .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-patch-disposition-redacted.md
    - .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-ab-count_asic_chips_rx_loop_parity-redacted.md
    - crates/bitaxe-asic/src/bm1366/chip_detect.rs
    - firmware/bitaxe/src/asic_adapter.rs
  </read_first>
  <action>
    Execute code policy from Task 1 disposition (D-13 / D-07):

    **If `applied_default_promotion`:** keep RX-loop + counted handoff as-is. Ensure unit tests still pass. Document in disposition that default promotion is evidence-backed.

    **If `falsified_count_asic_chips_rx_loop_parity_as_sole_blocker`:**
    - Default: **retain** RX-loop / counted-handoff control-flow parity (`wire_parity_rx_loop_retained: true`) and do **not** add a second patch (version-rolling deferred).
    - Revert Plan 02 change **only** if A/B `ab_outcome: regressed` with clear correlate/submit regression attributable to the RX-loop change; if reverting, restore prior single-shot detect path and document why control-flow parity was sacrificed.
    - Set `next_hypothesis: version_rolling_negotiation` when markers otherwise match after the A/B (D-07). Do not implement version-rolling / `mining.configure` changes in this phase.

    **If `blocked_safe_prerequisite`:** leave Plan 02 code state unchanged; no second patch.

    Never touch ReadChipId frame bytes, job bytes, post_max_baud defaults, register-read poll, long-block RX, or ticket-mask ASIC-256. Never edit checklist verified rows.

    Re-run `cargo test -p bitaxe-asic chip_detect` after any keep/revert.
  </action>
  <acceptance_criteria>
    - `rg -n "second_speculative_patch_applied: false" .planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-patch-disposition-redacted.md`
    - Disposition `next_hypothesis` is `none`, `version_rolling_negotiation`, or `match_upstream_chip_enumerate_before_init` (never a falsified lever name)
    - `rg -n "mining\\.configure|version_rolling" firmware/bitaxe/src crates/bitaxe-stratum/src` shows no new version-rolling implementation added this plan (deferred) — or disposition explicitly records `version_rolling_implemented: false` with no new configure negotiation code
    - `git diff -- crates/bitaxe-asic/src/bm1366/command.rs` empty
    - `cargo test -p bitaxe-asic chip_detect` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p bitaxe-asic chip_detect</automated>
  </verify>
  <done>Code state matches disposition; no second speculative patch; version-rolling deferred to next_hypothesis only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| A/B evidence → default firmware behavior | Over-promotion without correlate/submit would ship a false “fixed” claim |
| Disposition Markdown → Phase 30 | Must not imply checklist verified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28115-11 | E | default promotion gate | mitigate | Require ab_outcome improved + correlate and/or submit (D-13); else falsify sole-blocker |
| T-28115-12 | I | patch-disposition Markdown | mitigate | Categories/booleans only; no hex/credentials |
| T-28115-13 | E | phase30 / checklist / share overclaim | mitigate | `phase30_promotion_input` pending unless both gates true; never edit checklist verified rows; do not claim STR-09 verified |
| T-28115-14 | T | speculative second patch (version-rolling) | mitigate | Hard `second_speculative_patch_applied: false` and `version_rolling_implemented: false`; version-rolling deferred (D-07) |
</threat_model>

<verification>
- Disposition file exists with closed patch_disposition enum
- chip_detect tests pass under chosen keep/revert policy
- No checklist verified edits; no version-rolling implementation; no ReadChipId byte patch
</verification>

<success_criteria>
- Default promotion occurs only on improved correlate/submit evidence
- Otherwise sole-blocker falsified with next_hypothesis version_rolling_negotiation when markers otherwise match
- No second speculative patch and no reopened falsified levers
</success_criteria>

<output>
After completion, create `.planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-03-SUMMARY.md`
</output>
