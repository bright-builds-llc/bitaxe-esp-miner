---
generated_by: gsd-discuss-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1.5-2026-07-09T11-47-00
generated_at: 2026-07-09T11:47:00Z
---

# Phase 28.1.1.5: BM1366 Match Upstream Chip-Enumerate Before Init Nonce-Production Diagnosis - Context

**Gathered:** 2026-07-09
**Status:** Ready for planning
**Mode:** YOLO recommended decisions accepted

<domain>
## Phase Boundary

This phase consumes `next_hypothesis: match_upstream_chip_enumerate_before_init` from Phase 28.1.1.4. Dynamic mining-ready finals (`0x14`/`0x08`/`0x10`) are wire-aligned after ASIC-256 ticket-mask retention; cores remain UART-alive but idle (`power_delta` falling; no `result_correlated` / fake-pool submit).

**Important prior fact:** the chip-ID TX frame `{55 AA 52 05 00 00 0A}` already matches Rust `Bm1366Command::ReadChipId` (Phase 27 `chip-detect-upstream-diff.md`). This phase is **not** “fix the 0x0A bytes.” It isolates **enumerate → count → address-split → init** sequencing and chip-count sourcing that may leave cores idle despite a matching ReadChipId frame.

Out of scope: Phase 29/30 checklist `verified` promotion; job-byte patches; reopening falsified levers (`post_max_baud_delay_2000`, `match_upstream_register_read_poll`, `upstream_like_long_block_receive`, `ticket_mask_asic_difficulty` as sole blocker); claiming enable GPIO missing; implementing version-rolling as a second speculative patch in the same phase.

</domain>

<decisions>
## Implementation Decisions

### Evidence ladder
- **D-01:** Retain 28.1.1.1–28.1.1.4 facts: job 11/11; below-job 12/12; register-read poll falsified; long-block RX falsified; ticket-mask sole-blocker falsified with ASIC-256 `wire_parity_retained: true`; no Rust `result_correlated` / fake-pool submit.
- **D-02:** Chip-ID TX frame bytes already match; do not treat raw `0x0A` hex mismatch as the primary lever.
- **D-03:** Ultra 205 detector-gated; hardware A/B unblocked.

### Primary hypothesis
- **D-04:** First investigation is upstream `BM1366_init` enumerate sequencing: 3× version mask → ReadChipId TX → `count_asic_chips` (loop RX) → address_interval from **counted** chips → `_set_chip_address` → remaining init, versus Rust production `run_chip_detect_actions` + `run_mining_ready_init_actions` (config `chip_count` / expected chips, address assignment in `mining_ready_commands`).
- **D-05:** Highest-signal confirmed divergence candidates (pick one for the single A/B): (a) address interval / SetChipAddress driven by config expected count instead of counted responses; (b) missing or weaker `count_asic_chips`-style multi-frame RX accumulate/retry before mining-ready; (c) inter-command delay/order between enumerate and first mining-ready write. Prefer the candidate with redacted wire/marker evidence.
- **D-06:** Fast feedback: `asic_probe=power_delta` → `power_delta_class`. Phase gate remains `result_correlated` + `fake_pool_submit_observed`.
- **D-07:** If the chosen lever is falsified after frames/sequencing markers match, set `next_hypothesis` to the next highest remaining suspect (prefer `version_rolling_negotiation` unless evidence points elsewhere). Do not invent a second speculative patch in this phase.

### Comparator (first deliverable)
- **D-08:** Redaction-safe chip-enumerate comparator composing prior parsers: version_mask prelude count class, ReadChipId/enumerate TX present, chip_detected / chip_count_class, address_assignment_count vs expected, `power_delta_class`, correlate/submit booleans; no raw hex in commits.
- **D-09:** `recommended_investigation` closed set must never re-emit falsified knobs (`post_max_baud_delay_2000`, `match_upstream_register_read_poll`, `upstream_like_long_block_receive`, `ticket_mask_asic_difficulty` as sole blocker). Prefer `match_upstream_chip_enumerate_before_init` | `version_rolling_negotiation` | `none`.
- **D-10:** Prefer composing existing scripts (`phase28.1.1.4-init-sequencing-compare.mjs`, wire-field/RX comparators, source-work capture) over a parallel stack.
- **D-11:** Optional compact firmware marker for enumerate/address counts only (no hex); skip if uart_trace already sufficient.

### A/B and patch policy
- **D-12:** Exactly one focused investigation/default-candidate patch for the confirmed enumerate/count/address divergence, with pure-crate or firmware fixture coverage, then one detector-gated ≥360 s fake-pool A/B labeled for that lever.
- **D-13:** Promote default only if `ab_outcome: improved` with `result_correlated` and/or `fake_pool_submit_observed: true`.
- **D-14:** One confirmed divergence at a time; investigation flags off-by-default; no speculative flag spam.
- **D-15:** Bans: no job-byte patches; no falsified investigation defaults; keep ASIC-256 ticket-mask wire parity unless A/B clearly regresses and retaining it is unsafe; no Phase 30 checklist `verified` edits.

### Hardware and verification
- **D-16:** `just detect-ultra205`, `just verify-reference`, board 205, ≥360 s, wall clock ≥420 s; fake-pool preferred.
- **D-17:** Never modify `reference/esp-miner`; scratch-only upstream helpers.
- **D-18:** `passed` only with `result_correlated` + share-submission marker; else `gaps_found` / `blocked_safe_prerequisite`. Phase 30 promotion deferred.

### the agent's Discretion
- Exact comparator field names and whether compact firmware markers ship in Plan 01 vs 02.
- Which of D-05 (a)/(b)/(c) is the forced A/B label after Wave 0 evidence — must be named in Plan 02 from comparator output, not guessed mid-flash.
- If hardware blocked, record `blocked_safe_prerequisite` precisely.

</decisions>

<specifics>
## Specific Ideas

- Upstream anchors: `reference/esp-miner/components/asic/bm1366.c` (`BM1366_init` init3 + address_interval), `asic_common.c` (`count_asic_chips`).
- Rust anchors: `crates/bitaxe-asic/src/bm1366/init_plan.rs`, `mining_ready.rs` (`SetChipAddress` loop), `firmware/bitaxe/src/asic_adapter.rs` (`run_chip_detect_actions` → `run_mining_ready_init_actions`), `chip_detect_investigation.rs` (upstream_aligned when Phase 27 bridge).
- Phase 27 already documented frame match + RX accumulate / version-mask prelude — reuse; do not re-litigate H1–H5 as open unknowns without new redacted evidence.
- Keep raw logs under ignored `scratch/` or `.planning/phases/**/hardware-runs/`.

</specifics>

<canonical_refs>
## Canonical References

### Phase truth
- `.planning/ROADMAP.md` — Phase 28.1.1.5 scope
- `.planning/STATE.md` — handoff from 28.1.1.4
- `.planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-final-evidence-redacted.md`
- `.planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-patch-disposition-redacted.md`
- `.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/NEXT-AGENT-PROMPT.md`
- `.planning/phases/27-live-hardware-asic-and-stratum-bridge/chip-detect-upstream-diff.md`

### Tooling
- `scripts/phase28.1.1.4-init-sequencing-compare.mjs`
- `scripts/phase28.1.1.4-init-sequencing-diagnostic.sh`
- `scripts/phase28.1.1.1-source-work-aligned-capture.sh`
- `scripts/phase28.1.1.1-upstream-golden-capture.sh`
- `scripts/phase27-live-hardware-bridge-package.sh`

### Code
- `crates/bitaxe-asic/src/bm1366/init_plan.rs`
- `crates/bitaxe-asic/src/bm1366/mining_ready.rs`
- `crates/bitaxe-asic/src/bm1366/command.rs` — `ReadChipId`
- `firmware/bitaxe/src/asic_adapter.rs`
- `firmware/bitaxe/src/asic_adapter/chip_detect_investigation.rs`
- `firmware/bitaxe/src/live_stratum_runtime.rs` — power_delta probe
- `reference/esp-miner/components/asic/bm1366.c` — read-only
- `reference/esp-miner/components/asic/asic_common.c` — read-only

### Policy
- `AGENTS.md` — detector gate, redaction, timeouts
- `docs/adr/0012-parity-verification-evidence.md`
- `docs/parity/checklist.md` — no verified promotion here

</canonical_refs>

<code_contexts>
## Code Contexts

- Production path already runs chip-detect then mining-ready; Phase 27 bridge defaults to `upstream_aligned_after_safety_bring_up` (3× version mask, skip second reset, wait TX).
- `mining_ready_commands` assigns addresses from `config.chip_count` and `address_interval()` — compare to upstream `256 / chip_counter` after `count_asic_chips`.
- Keep ASIC-256 ticket mask from 28.1.1.4 unless disposition requires otherwise.

</code_contexts>
