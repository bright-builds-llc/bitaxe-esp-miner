---
phase: 33-confirmed-settings-durability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/bitaxe-api/src/lib.rs
  - crates/bitaxe-api/src/settings.rs
  - crates/bitaxe-api/src/v12_settings.rs
  - crates/bitaxe-config/src/persistence.rs
autonomous: true
requirements:
  - CFG-09
  - CFG-10
  - CFG-13
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 33-2026-07-14T01-50-49
generated_at: 2026-07-14T02:20:35Z
must_haves:
  truths:
    - Every known field is compatibility-validated before exact hostname-only authority is decided.
    - Only an exact valid hostname-only object can construct a persistence transaction or live effect.
    - PATCH success follows write, commit, independent reload, typed reconciliation, and confirmed publication in that order.
    - Same-value hostname requests use the same proof chain and concurrent writers cannot interleave through publication.
  artifacts:
    - crates/bitaxe-api/src/v12_settings.rs owns one exhaustive validation-and-authority decision.
    - crates/bitaxe-api/src/settings.rs owns the pure confirmation protocol and typed failure taxonomy.
    - crates/bitaxe-config/src/persistence.rs exposes only narrow typed snapshot reconciliation helpers if required.
  key_links:
    - The authorized decision carries the validated Hostname newtype into the confirmation protocol.
    - Public success and best-effort live effect become reachable only after exact reloaded-value reconciliation and publication.
---

<objective>
Build the pure hostname authority and storage-confirmation contract.

Purpose: Make invalid, compatibility-only, authorized, and post-commit-uncertain paths deterministic before ESP-IDF integration.
Output: An exhaustive request decision, a typed ordered transaction protocol, and failure/concurrency regressions for CFG-09, CFG-10, and CFG-13.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-confirmed-settings-durability/33-CONTEXT.md
@.planning/phases/33-confirmed-settings-durability/33-RESEARCH.md
@.planning/phases/33-confirmed-settings-durability/33-VALIDATION.md
@crates/bitaxe-api/src/settings.rs
@crates/bitaxe-api/src/v12_settings.rs
@crates/bitaxe-config/src/persistence.rs
</context>

<threat_model>
- **T-33-01 (high):** Broad compatibility or a mixed request could gain write authority. Mitigate with full known-field validation followed by a closed exact hostname-only decision and exhaustive field-set tests.
- **T-33-02 (high):** Requested values could become public truth without storage confirmation. Make reload return independent evidence and gate publication and success on typed exact reconciliation.
- **T-33-03 (high):** Concurrent requests could interleave write, reload, or publication. Define one transaction boundary through publication and prove deterministic serialized outcomes.
- **T-33-04 (high):** Raw request bodies or secret values could reach diagnostics. Retain only stable categories, step names, and counts.
- **T-33-05 (high):** A failure after commit could be falsely described as rollback. Represent post-commit uncertainty explicitly and never schedule a compensating write.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Compose compatibility validation with exact hostname authority</name>
  <files>crates/bitaxe-api/src/lib.rs, crates/bitaxe-api/src/v12_settings.rs, crates/bitaxe-api/src/settings.rs</files>
  <read_first>crates/bitaxe-api/src/v12_settings.rs, crates/bitaxe-api/src/settings.rs, crates/bitaxe-api/src/lib.rs, reference/esp-miner/main/http_server.c, .planning/phases/33-confirmed-settings-durability/33-CONTEXT.md, .planning/phases/33-confirmed-settings-durability/33-RESEARCH.md</read_first>
  <action>Create one pure Phase 33 request decision that first parses an object and applies every existing known-field compatibility validator, then grants authority only when the complete field set is exactly one valid `hostname`. Carry the existing validated `Hostname` newtype in the authorized variant. Preserve current public semantics exactly: malformed/non-object returns `Invalid JSON`; any invalid known field, including one inside a mixed body, returns `Wrong API input`; empty, unknown-only, unsupported-known-only, and fully valid mixed objects return empty success with no write, publication, or effect. Record only stable compatibility categories and counts, never body text or values. Remove or make unreachable any composition that extracts hostname from a mixed object.</action>
  <verify><automated>cargo test -p bitaxe-api v12_settings &amp;&amp; cargo test -p bitaxe-api settings</automated></verify>
  <acceptance_criteria>
    - Tests cover malformed JSON, non-object JSON, empty object, exact valid hostname, exact invalid hostname, unknown-only, unsupported-known-only, valid hostname plus another valid field, invalid hostname plus another field, and another invalid known field mixed with hostname.
    - Only the exact valid hostname case returns an authorized variant containing `Hostname`.
    - Every error/no-op test proves no persistence command or live effect can be constructed.
    - No diagnostic structure stores a raw body, hostname value, credential, endpoint, worker, password, token, or NVS secret.
  </acceptance_criteria>
  <done>The pure API boundary preserves compatibility while closing write authority to one validated exact hostname object.</done>
</task>

<task type="auto">
  <name>Task 2: Deepen the confirmation protocol through typed reconciliation and publication</name>
  <files>crates/bitaxe-api/src/settings.rs, crates/bitaxe-config/src/persistence.rs</files>
  <read_first>crates/bitaxe-api/src/settings.rs, crates/bitaxe-api/src/v12_settings.rs, crates/bitaxe-config/src/persistence.rs, .planning/phases/33-confirmed-settings-durability/33-VALIDATION.md</read_first>
  <action>Refactor the persistence contract so an authorized hostname always performs write, commit, independent reload, typed hostname reconciliation, and confirmed publication before `PublicSuccess`. Make reload return a candidate or typed evidence rather than mutating public state invisibly; add a narrow typed snapshot helper in `bitaxe-config` only if it keeps parsing/reconciliation pure. Define stable failures for validate/write/commit/reload/reconcile/publish, including a distinct post-commit uncertainty classification. Do not attempt rollback after commit. Keep same-value requests on the identical write-and-confirm path. Model transaction serialization through publication in the protocol and add a deterministic fake adapter/coordinator test in which two writers cannot interleave or publish another request's reload. Schedule the best-effort live hostname effect only after public success becomes eligible.</action>
  <verify><automated>cargo test -p bitaxe-api settings &amp;&amp; cargo test -p bitaxe-config persistence</automated></verify>
  <acceptance_criteria>
    - A success test proves exact event order: write, commit, independent reload, reconcile, publish, public success, then live effect eligibility.
    - Separate tests inject write, commit, reload, mismatch, and publication failures and prove no public success or live effect is produced.
    - Reload/mismatch/publication failures after a successful commit are classified as post-commit uncertainty without rollback or an unchanged-storage claim.
    - A same-value test proves write, commit, reload, reconcile, and publish still occur.
    - A deterministic two-writer test proves one transaction owns the entire mutation-through-publication interval and each success reconciles its own typed hostname.
  </acceptance_criteria>
  <done>The pure protocol makes storage confirmation and serialization necessary conditions for PATCH success.</done>
</task>

</tasks>

<verification>
- [ ] `cargo test -p bitaxe-api v12_settings` passes.
- [ ] `cargo test -p bitaxe-api settings` passes.
- [ ] `cargo test -p bitaxe-config persistence` passes.
- [ ] `cargo fmt --all` passes before commit.
- [ ] `cargo clippy --all-targets --all-features -- -D warnings` passes before commit.
- [ ] `cargo build --all-targets --all-features` passes before commit.
- [ ] `cargo test --all-features` passes before commit.
</verification>

<success_criteria>
- All request classes have explicit public semantics and effect authority.
- Public success cannot occur before typed reloaded hostname equality and confirmed publication.
- Concurrent and same-value requests follow one uniform serialized proof chain.
- The core adds no ESP-IDF dependency and stores no secret-bearing diagnostics.
</success_criteria>

<output>
After completion, create `.planning/phases/33-confirmed-settings-durability/33-01-SUMMARY.md`.
</output>
