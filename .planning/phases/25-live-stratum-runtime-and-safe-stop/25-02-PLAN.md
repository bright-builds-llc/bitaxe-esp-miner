---
phase: 25-live-stratum-runtime-and-safe-stop
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - firmware/bitaxe/src/live_stratum_runtime.rs
  - firmware/bitaxe/src/main.rs
  - firmware/bitaxe/src/mining_evidence_mode.rs
  - firmware/bitaxe/src/runtime_snapshot.rs
  - crates/bitaxe-safety/src/watchdog.rs
autonomous: true
requirements:
  - STR-08
  - STR-09
  - SAFE-12
  - SAFE-13
user_setup: []
must_haves:
  truths:
    - "Firmware live Stratum mode is a distinct compile-time opt-in and remains fail-closed when the Phase 25 mode and acknowledgment are absent."
    - "Firmware live Stratum startup evaluates Phase 22 typed prerequisite readiness before any `TcpStream` connect attempt or secret-bearing pool configuration access."
    - "Firmware owns real TCP socket connect/read/write/shutdown with bounded timeouts and feeds typed observations into the pure runtime from Plan 25-01."
    - "Safe stop stops socket activity, invalidates work, disables mining and hardware-control surfaces, blocks submission, and refreshes API/WebSocket-visible post-stop state without implementing Phase 26 telemetry projection."
    - "Watchdog checkpoints cover socket, ASIC, API/WebSocket, and evidence-capture load categories."
  artifacts:
    - path: "firmware/bitaxe/src/live_stratum_runtime.rs"
      provides: "ESP-IDF `std::net::TcpStream` adapter, redaction-safe lifecycle markers, and safe-stop execution."
    - path: "firmware/bitaxe/src/mining_evidence_mode.rs"
      provides: "Distinct Phase 25 live-socket evidence mode and fail-closed tests."
    - path: "firmware/bitaxe/src/main.rs"
      provides: "Runtime startup wiring after Wi-Fi/network setup."
    - path: "firmware/bitaxe/src/runtime_snapshot.rs"
      provides: "Minimal post-stop mining state refresh for SAFE-12."
    - path: "crates/bitaxe-safety/src/watchdog.rs"
      provides: "Watchdog step kinds and tests for socket/ASIC/API/WebSocket/evidence bounded work."
  key_links:
    - from: "firmware/bitaxe/src/live_stratum_runtime.rs"
      to: "crates/bitaxe-stratum/src/v1/live_runtime.rs"
      via: "Socket lines become typed runtime observations; outbound actions become JSON lines."
      pattern: "LiveStratumRuntime"
    - from: "firmware/bitaxe/src/live_stratum_runtime.rs"
      to: "crates/bitaxe-safety/src/mining_preconditions.rs"
      via: "Phase 22 `ProductionMiningPreconditions::decision()` is evaluated before `TcpStream::connect` or pool credential/settings access."
      pattern: "ProductionMiningPreconditions"
    - from: "firmware/bitaxe/src/live_stratum_runtime.rs"
      to: "crates/bitaxe-stratum/src/v1/submit_response.rs"
      via: "Pool submit responses classify only through SubmitIntent-tied pure classifier."
      pattern: "classify_submit_response"
    - from: "firmware/bitaxe/src/live_stratum_runtime.rs"
      to: "firmware/bitaxe/src/runtime_snapshot.rs"
      via: "Safe stop writes post-stop `MiningRuntimeState` for API/WebSocket-visible refresh."
      pattern: "replace_.*mining_runtime_state"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 25-2026-07-05T01-55-45
generated_at: 2026-07-05T02:13:00Z
---

<objective>
Wire the pure Phase 25 runtime into a thin firmware socket shell that can start only under explicit local evidence gates, remain watchdog-responsive, and stop into the bounded SAFE-12 state.

Purpose: STR-08 needs real TCP socket I/O, while SAFE-12 and SAFE-13 require the shell to stop safely and avoid starving watchdog-sensitive firmware work. This plan preserves D-01 by keeping parsing/classification decisions in the pure crate.
Output: Firmware live Stratum adapter, Phase 25 opt-in mode, watchdog step coverage, and minimal post-stop state refresh.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-live-stratum-runtime-and-safe-stop/25-CONTEXT.md
@.planning/phases/25-live-stratum-runtime-and-safe-stop/25-RESEARCH.md
@.planning/phases/25-live-stratum-runtime-and-safe-stop/25-VALIDATION.md
@.planning/phases/22-claim-ladder-and-safety-preconditions/22-02-SUMMARY.md
@.planning/phases/24-bm1366-production-work-path/24-03-SUMMARY.md
@crates/bitaxe-stratum/src/v1/live_runtime.rs
@crates/bitaxe-stratum/src/v1/submit_response.rs
@crates/bitaxe-stratum/src/v1/messages.rs
@crates/bitaxe-stratum/src/v1/production_work.rs
@crates/bitaxe-safety/src/watchdog.rs
@crates/bitaxe-safety/src/effects.rs
@firmware/bitaxe/src/controlled_mining_runtime.rs
@firmware/bitaxe/src/mining_evidence_mode.rs
@firmware/bitaxe/src/runtime_snapshot.rs
@firmware/bitaxe/src/main.rs
@firmware/bitaxe/src/http_api.rs
@firmware/bitaxe/src/websocket_api.rs
@firmware/bitaxe/BUILD.bazel

<interfaces>
Existing and planned contracts the executor must use:

- `MiningEvidenceMode::current()` currently fails closed unless compile-time mode and acknowledgment match.
- `runtime_snapshot::replace_mining_runtime_state_for_evidence(mining)` replaces API-visible mining state; Phase 25 may add a more specifically named post-stop wrapper but must not implement Phase 26 statistics or scoreboard semantics.
- `http_api` and `websocket_api` derive live frames from `runtime_snapshot::collect_api_snapshot()`; Phase 25 work is limited to post-stop refresh.
- Plan 25-01 creates `LiveStratumRuntime`, `LiveRuntimeAction`, `SafeStopPostconditions`, and `classify_submit_response`.
- Phase 22 created `ProductionMiningPreconditions`, `ProductionMiningPrerequisite`, and `ProductionMiningPreconditionDecision`; `ProductionMiningPreconditions::decision()` returns `Ready` or `Blocked { reason, plan }`, and `MiningLoopGate::decision()` consumes that decision before BM1366 dispatch.
- `StepSupervisor::decision(StepProgress)` uses a 25 ms step budget, 100 ms yield interval, and 4-step consecutive limit.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 25-02-01: Add firmware live socket adapter</name>
  <files>firmware/bitaxe/src/live_stratum_runtime.rs, firmware/bitaxe/src/main.rs, firmware/bitaxe/src/mining_evidence_mode.rs</files>
  <read_first>
    - .planning/phases/25-live-stratum-runtime-and-safe-stop/25-CONTEXT.md
    - .planning/phases/25-live-stratum-runtime-and-safe-stop/25-RESEARCH.md
    - AGENTS.md
    - firmware/bitaxe/src/mining_evidence_mode.rs
    - firmware/bitaxe/src/controlled_mining_runtime.rs
    - firmware/bitaxe/src/main.rs
    - firmware/bitaxe/src/network_stack.rs
    - firmware/bitaxe/src/settings_adapter.rs
    - crates/bitaxe-stratum/src/v1/live_runtime.rs
    - crates/bitaxe-stratum/src/v1/messages.rs
    - crates/bitaxe-stratum/src/v1/submit_response.rs
    - crates/bitaxe-safety/src/mining_preconditions.rs
    - crates/bitaxe-stratum/src/v1/mining_loop.rs
  </read_first>
  <behavior>
    - Test 1: `MiningEvidenceMode::from_compile_env(Some("phase25-live-stratum-runtime"), Some("ultra205-phase25-live-stratum-safe-stop"))` returns a distinct Phase 25 live Stratum variant; missing or mismatched values return `FailClosed`.
    - Test 2: blocked `ProductionMiningPreconditionDecision` values for missing, stale, unavailable, unsafe, ambiguous, or undocumented Phase 22 prerequisites emit a stable blocker reason, leave `TcpStream::connect` uncalled, leave pool endpoint/user/password/settings access uncalled, and keep live socket mining disabled.
    - Test 3: ready `ProductionMiningPreconditionDecision` is required before the adapter may construct a `TcpStream`, parse secret-bearing pool settings, or call into the live socket loop.
    - Test 4: firmware adapter build path uses `std::net::TcpStream`, `set_read_timeout(Some(Duration::from_millis(100)))`, `set_write_timeout(Some(Duration::from_millis(100)))`, and `shutdown(Shutdown::Both)` on safe stop.
    - Test 5: adapter logs only fixed category markers: `phase25_live_stratum_status=blocked|connecting|subscribed|authorized|active|reconnecting|stopped`, `phase25_prerequisite_status=<stable_reason>`, `redacted=true`, and no raw pool values.
    - Test 6: adapter reads pool settings from existing settings/NVS snapshot only after the Phase 25 mode gate and Phase 22 prerequisite readiness gate pass; it never reads `wifi-credentials.json` or `pool-credentials*.json`.
  </behavior>
  <action>
    Modify `mining_evidence_mode.rs` per D-03 and D-16 to add constants `PHASE25_LIVE_STRATUM_MODE: "phase25-live-stratum-runtime"` and `PHASE25_LIVE_STRATUM_ACK: "ultra205-phase25-live-stratum-safe-stop"` plus a distinct enum variant such as `Phase25LiveStratumRuntime`. Preserve the existing Phase 21 `LiveMiningRuntime` behavior and tests.

    Before any `TcpStream::connect`, pool endpoint parsing, pool username/password access, `LiveStratumRuntime` startup, or credential/settings-derived live socket action, evaluate Phase 22 readiness per D-03 using `ProductionMiningPreconditions::decision()` and the existing `ProductionMiningPreconditionDecision` / `MiningLoopGate` contracts. When the decision is `Blocked { reason, .. }`, emit only stable category markers such as `phase25_live_stratum_status=blocked`, `phase25_prerequisite_status=<reason>`, `redacted=true`; block work submission in runtime state; converge on the safe-stop/non-start path; and return without opening a socket or touching secret-bearing pool configuration. Add regression tests with fake connect and fake pool-settings access counters proving both counters stay zero for blocked prerequisites.

    Create `firmware/bitaxe/src/live_stratum_runtime.rs` per D-01, D-02, D-04, D-05, D-06, D-11, and D-13. Use a single-owner socket loop around `std::net::TcpStream`; parse each complete JSON line through `bitaxe_stratum::v1::messages::parse_server_message`; drive Plan 25-01 `LiveStratumRuntime`; serialize outbound `StratumV1ClientMessage` values through `to_json_line()`. Use 100 ms read/write timeouts and one bounded read or write operation per loop step.

    Keep all pool endpoints, ports, users, workers, passwords, targets, extranonces, share payloads, socket errors, device URLs, IPs, MACs, Wi-Fi values, tokens, NVS secrets, and raw BM1366 frames out of retained logs. Emit only category labels and `redacted=true`.

    Wire `mod live_stratum_runtime;` in `main.rs`, and call the Phase 25 starter only after Wi-Fi/network setup has completed or has explicitly failed into a category label. The starter must return immediately when `MiningEvidenceMode` is not the Phase 25 variant. Do not add broad API, WebSocket, statistics, scoreboard, display, input, Stratum v2, voltage-control, fan-control, erase, rollback, or recovery behavior.
  </action>
  <verify>
    <automated>bazel test //crates/bitaxe-safety:tests //crates/bitaxe-stratum:tests && bazel build //firmware/bitaxe:firmware</automated>
  </verify>
  <acceptance_criteria>
    - `rg "PHASE25_LIVE_STRATUM_MODE|PHASE25_LIVE_STRATUM_ACK|Phase25LiveStratumRuntime" firmware/bitaxe/src/mining_evidence_mode.rs` returns matches.
    - `rg "ProductionMiningPreconditions|ProductionMiningPreconditionDecision|MiningLoopGate|phase25_prerequisite_status" firmware/bitaxe/src/live_stratum_runtime.rs` returns matches.
    - `rg "connect_counter|pool_settings_access_counter|blocked.*precondition|prerequisite.*before.*TcpStream" firmware/bitaxe/src/live_stratum_runtime.rs firmware/bitaxe/src/mining_evidence_mode.rs` returns regression-test matches proving blocked prerequisites prevent connect and credential/settings access.
    - `rg "TcpStream|set_read_timeout\\(Some\\(Duration::from_millis\\(100\\)\\)\\)|set_write_timeout\\(Some\\(Duration::from_millis\\(100\\)\\)\\)|shutdown\\(Shutdown::Both\\)" firmware/bitaxe/src/live_stratum_runtime.rs` returns matches.
    - `rg "parse_server_message|LiveStratumRuntime|to_json_line|classify_submit_response" firmware/bitaxe/src/live_stratum_runtime.rs` returns matches.
    - `rg "phase25_live_stratum_status|redacted=true" firmware/bitaxe/src/live_stratum_runtime.rs` returns matches.
    - `! rg -n "wifi-credentials|pool-credentials|stratum[+]tcp://|poolURL|poolUser|poolPassword|target=|extranonce=|share_payload|socket_error=|device_url=|raw_bm1366_frame|erase-flash|voltage-control|fan-control" firmware/bitaxe/src/live_stratum_runtime.rs firmware/bitaxe/src/mining_evidence_mode.rs firmware/bitaxe/src/main.rs` returns no matches.
  </acceptance_criteria>
  <done>Firmware has a gated real Stratum socket shell for STR-08 and STR-09 without broadening credential handling or evidence claims.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 25-02-02: Implement safe-stop snapshot and watchdog proof</name>
  <files>firmware/bitaxe/src/live_stratum_runtime.rs, firmware/bitaxe/src/runtime_snapshot.rs, crates/bitaxe-safety/src/watchdog.rs</files>
  <read_first>
    - .planning/phases/25-live-stratum-runtime-and-safe-stop/25-CONTEXT.md
    - .planning/phases/25-live-stratum-runtime-and-safe-stop/25-VALIDATION.md
    - crates/bitaxe-safety/src/watchdog.rs
    - crates/bitaxe-safety/src/effects.rs
    - crates/bitaxe-stratum/src/v1/live_runtime.rs
    - crates/bitaxe-stratum/src/v1/state.rs
    - firmware/bitaxe/src/live_stratum_runtime.rs
    - firmware/bitaxe/src/runtime_snapshot.rs
    - firmware/bitaxe/src/http_api.rs
    - firmware/bitaxe/src/websocket_api.rs
  </read_first>
  <behavior>
    - Test 1: watchdog `StepKind` covers `Socket`, `Asic`, `Api`, `WebSocket`, and `EvidenceCapture`; each kind follows the existing 25 ms budget, 100 ms yield interval, and 4 consecutive-step limit.
    - Test 2: firmware safe stop emits exactly `phase25_safe_stop_status=complete socket=stopped work_queue=invalidated active_work=invalidated mining=disabled hardware_control=disabled work_submission=disabled post_stop_snapshot=updated`.
    - Test 3: post-stop snapshot state has `MiningActivityStatus::SafeBlocked` or `Paused`, `WorkSubmissionGate::Blocked`, zero advancement after stop, and `maybe_blocked_reason=Some("phase25_safe_stop")` or an equally stable category label.
    - Test 4: safe-stop path is called from normal stop, reconnect/fallback exhaustion, prerequisite failure, operator cancellation, and verification cleanup paths.
  </behavior>
  <action>
    Extend `crates/bitaxe-safety/src/watchdog.rs` per D-13 and D-14 with `StepKind::Socket`, `StepKind::Asic`, `StepKind::Api`, `StepKind::WebSocket`, and `StepKind::EvidenceCapture`. Add focused unit tests that each new kind can return `Continue`, `YieldNow`, and `ResetOrFeedWatchdog` under the existing thresholds.

    In `runtime_snapshot.rs`, add a narrowly named helper such as `replace_mining_runtime_state_after_phase25_safe_stop(mining: MiningRuntimeState)` that delegates to the existing state replacement. Do not add Phase 26 statistics, scoreboard, counter, or API/WebSocket projection semantics.

    In `live_stratum_runtime.rs`, implement SAFE-12 post-stop execution per D-10, D-11, and D-12: call pure `safe_stop`, stop socket reads/writes, call `shutdown(Shutdown::Both)`, invalidate work state, disable mining, disable hardware-control surfaces through existing safety effect/category semantics, block work submission, refresh the runtime snapshot through the new post-stop helper, and publish the exact safe-stop status marker. Normal stop, reconnect/fallback exhaustion, prerequisite failure, operator cancellation, and verification cleanup must all converge on this path.
  </action>
  <verify>
    <automated>bazel test //crates/bitaxe-safety:tests && bazel build //firmware/bitaxe:firmware</automated>
  </verify>
  <acceptance_criteria>
    - `rg "Socket|Asic|Api|WebSocket|EvidenceCapture" crates/bitaxe-safety/src/watchdog.rs` returns matches.
    - `rg "replace_mining_runtime_state_after_phase25_safe_stop" firmware/bitaxe/src/runtime_snapshot.rs` returns exactly one public helper.
    - `rg "phase25_safe_stop_status=complete socket=stopped work_queue=invalidated active_work=invalidated mining=disabled hardware_control=disabled work_submission=disabled post_stop_snapshot=updated" firmware/bitaxe/src/live_stratum_runtime.rs` returns a match.
    - `rg "normal_stop|fallback_exhausted|prerequisite_failure|operator_cancelled|verification_cleanup" firmware/bitaxe/src/live_stratum_runtime.rs` returns matches or equivalent stable `SafeStopReason` variants.
    - `! rg -n "statistics|scoreboard|accepted_shares|rejected_shares|stratum[+]tcp://|poolURL|poolUser|poolPassword|device_url=|raw_bm1366_frame" firmware/bitaxe/src/runtime_snapshot.rs firmware/bitaxe/src/live_stratum_runtime.rs` returns no unauthorized Phase 26 or secret-bearing matches; existing type names from imports are acceptable only if not used to implement Phase 26 counters.
  </acceptance_criteria>
  <done>SAFE-12 safe stop and SAFE-13 watchdog proof exist in code/buildable firmware, with only the minimal post-stop API/WebSocket-visible state refresh allowed in Phase 25.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| local NVS/settings -> socket adapter | Runtime pool configuration becomes socket connection input but must not be logged or committed. |
| TCP socket -> pure runtime | Untrusted bytes become typed Stratum messages through existing parser only. |
| stop request -> hardware effects | Stop must disable mining and hardware-control categories without adding unsafe voltage/fan routes. |
| firmware loop -> watchdog | Socket, ASIC, API/WebSocket, and evidence work must yield or checkpoint before starvation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-25-01 | Information Disclosure | `live_stratum_runtime.rs`, retained logs | mitigate | Category-only logs, forbidden-pattern acceptance scans, no credential-file reads, and no raw pool/socket/share/BM1366 values in retained markers. |
| T-25-02 | Repudiation / Integrity | submit response handling | mitigate | Firmware delegates accepted/rejected classification to Plan 25-01 classifier requiring `SubmitIntent` and matching request id; no synthetic response promotion. |
| T-25-03 | Tampering / Safety | safe stop and reconnect paths | mitigate | All stop/reconnect/fallback/prerequisite paths converge on work invalidation, submission block, mining disablement, and snapshot refresh. |
| T-25-04 | Denial of Service | socket loop and evidence load | mitigate | 100 ms socket timeouts, one bounded operation per loop step, expanded `StepKind` coverage, and explicit checkpoint/yield decisions. |
| T-25-05 | Elevation of Privilege / Safety | hardware enablement | mitigate | Distinct Phase 25 compile-time mode and ack are required; no voltage-control, fan-control, erase, rollback, or destructive recovery path is added. |
</threat_model>

<verification>
Run `bazel test //crates/bitaxe-safety:tests`.
Run `bazel build //firmware/bitaxe:firmware`.
Run `rg "phase25_live_stratum_status|phase25_safe_stop_status" firmware/bitaxe/src/live_stratum_runtime.rs`.
Run `! rg -n "wifi-credentials|pool-credentials|stratum[+]tcp://|poolURL|poolUser|poolPassword|target=|extranonce=|share_payload|socket_error=|device_url=|raw_bm1366_frame" firmware/bitaxe/src/live_stratum_runtime.rs firmware/bitaxe/src/runtime_snapshot.rs`.
</verification>

<success_criteria>
Plan 25-02 succeeds when firmware can compile a distinct, gated live Stratum socket adapter, socket operations are bounded and watchdog-aware, all stop paths converge on SAFE-12 postconditions, and post-stop state refresh remains limited to Phase 25 safe-stop visibility.
</success_criteria>

<output>
After completion, create `.planning/phases/25-live-stratum-runtime-and-safe-stop/25-02-SUMMARY.md`.
</output>
