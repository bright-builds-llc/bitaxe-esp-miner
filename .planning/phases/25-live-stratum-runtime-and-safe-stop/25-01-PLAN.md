---
phase: 25-live-stratum-runtime-and-safe-stop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/bitaxe-stratum/src/v1/live_runtime.rs
  - crates/bitaxe-stratum/src/v1/submit_response.rs
  - crates/bitaxe-stratum/src/v1/fake_pool.rs
  - crates/bitaxe-stratum/src/v1.rs
  - crates/bitaxe-stratum/BUILD.bazel
autonomous: true
requirements:
  - STR-08
  - STR-09
  - STR-11
  - SAFE-12
user_setup: []
must_haves:
  truths:
    - "Pure Stratum runtime can model connect, subscribe, authorize, difficulty/extranonce, notify, submit, reconnect, and safe stop without socket I/O."
    - "Accepted or rejected share classification requires a matching SubmitIntent and request id."
    - "Fake-pool coverage proves deterministic accepted, rejected, blocked, timeout, reconnect, clean-jobs, malformed, and fallback behavior without making live STR-09 claims."
  artifacts:
    - path: "crates/bitaxe-stratum/src/v1/live_runtime.rs"
      provides: "Pure live runtime state machine, outbound action queue, production registry integration, and safe-stop postconditions."
    - path: "crates/bitaxe-stratum/src/v1/submit_response.rs"
      provides: "SubmitIntent-tied response classifier and redaction-safe classification labels."
    - path: "crates/bitaxe-stratum/src/v1/fake_pool.rs"
      provides: "Deterministic fake-pool tests for STR-11 lifecycle and fail-closed cases."
    - path: "crates/bitaxe-stratum/src/v1.rs"
      provides: "Public module exports for live_runtime and submit_response."
    - path: "crates/bitaxe-stratum/BUILD.bazel"
      provides: "Bazel source registration for new pure modules."
  key_links:
    - from: "crates/bitaxe-stratum/src/v1/live_runtime.rs"
      to: "crates/bitaxe-stratum/src/v1/production_work.rs"
      via: "ProductionWorkRegistry invalidation and SubmitIntent ownership."
      pattern: "ProductionWorkRegistry"
    - from: "crates/bitaxe-stratum/src/v1/live_runtime.rs"
      to: "crates/bitaxe-stratum/src/v1/messages.rs"
      via: "StratumV1ClientMessage outbound JSON-RPC and StratumV1ServerMessage inbound parsing boundary."
      pattern: "StratumV1(Server|Client)Message"
    - from: "crates/bitaxe-stratum/src/v1/submit_response.rs"
      to: "crates/bitaxe-stratum/src/v1/production_work.rs"
      via: "Classifier accepts SubmitIntent and request id, not raw pool text alone."
      pattern: "SubmitIntent"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 25-2026-07-05T01-55-45
generated_at: 2026-07-05T02:12:00Z
---

<objective>
Create the Wave 0 pure Stratum v1 runtime and deterministic test surface required before firmware socket I/O can make any live claim.

Purpose: Phase 25 must keep D-01 and D-02 intact by putting lifecycle state, fake-pool behavior, submit mapping, and response classification in `crates/bitaxe-stratum`, while firmware later owns ESP-IDF sockets and timing.
Output: New pure `live_runtime` and `submit_response` modules, expanded fake-pool coverage, and Bazel/module exports.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-live-stratum-runtime-and-safe-stop/25-CONTEXT.md
@.planning/phases/25-live-stratum-runtime-and-safe-stop/25-RESEARCH.md
@.planning/phases/25-live-stratum-runtime-and-safe-stop/25-VALIDATION.md
@.planning/phases/22-claim-ladder-and-safety-preconditions/22-02-SUMMARY.md
@.planning/phases/24-bm1366-production-work-path/24-02-SUMMARY.md
@.planning/phases/24-bm1366-production-work-path/24-03-SUMMARY.md
@crates/bitaxe-stratum/src/v1/messages.rs
@crates/bitaxe-stratum/src/v1/mining.rs
@crates/bitaxe-stratum/src/v1/mining_loop.rs
@crates/bitaxe-stratum/src/v1/production_work.rs
@crates/bitaxe-stratum/src/v1/state.rs
@crates/bitaxe-stratum/src/v1/fake_pool.rs
@crates/bitaxe-stratum/src/v1.rs
@crates/bitaxe-stratum/BUILD.bazel

<interfaces>
Existing contracts the executor must preserve:

- `StratumV1ClientMessage::subscribe`, `authorize`, `submit_share`, and `to_json_line()` are the outbound JSON-RPC boundary.
- `parse_server_message()` returns typed `StratumV1ServerMessage` variants for `Notify`, `SetDifficulty`, `SetExtranonce`, `SetVersionMask`, `Response`, `ClientReconnect`, and related pool messages.
- `MiningWorkBuilder::new(notify, extranonce).with_pool_difficulty(...).build(Bm1366JobId)` creates pool-derived `MiningWork`.
- `ProductionWorkRegistry` owns queued/active pool-derived BM1366 work and has `invalidate_for_clean_jobs`, `invalidate_for_reconnect`, `invalidate_for_authorization_reset`, and `invalidate_for_session_replacement`.
- `SubmitIntent::submission().to_client_message(request_id, username)` is the only path from live ASIC-derived result correlation to `mining.submit`.
- `MiningRuntimeState` owns lifecycle, counters, pool difficulty, work-submission gate, mining activity, and `maybe_blocked_reason`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 25-01-01: Add pure live runtime state machine</name>
  <files>crates/bitaxe-stratum/src/v1/live_runtime.rs, crates/bitaxe-stratum/src/v1.rs, crates/bitaxe-stratum/BUILD.bazel</files>
  <read_first>
    - .planning/phases/25-live-stratum-runtime-and-safe-stop/25-CONTEXT.md
    - .planning/phases/25-live-stratum-runtime-and-safe-stop/25-VALIDATION.md
    - crates/bitaxe-stratum/src/v1/messages.rs
    - crates/bitaxe-stratum/src/v1/mining.rs
    - crates/bitaxe-stratum/src/v1/mining_loop.rs
    - crates/bitaxe-stratum/src/v1/production_work.rs
    - crates/bitaxe-stratum/src/v1/state.rs
    - crates/bitaxe-stratum/src/v1.rs
    - crates/bitaxe-stratum/BUILD.bazel
  </read_first>
  <behavior>
    - Test 1: starting a ready runtime queues `mining.subscribe` with request id `1`, sets lifecycle to `Connecting`, and does not log or expose pool endpoint, username, password, worker, target, extranonce, or share payload in `Debug`.
    - Test 2: subscribe response with extranonce queues `mining.authorize` request id `2`; authorize success sets lifecycle `Authorized`; set-difficulty stores `PoolDifficulty`.
    - Test 3: notify with `clean_jobs=false` builds `MiningWork`, enqueues it in `ProductionWorkRegistry`, allows work submission, and sets lifecycle `Active`.
    - Test 4: notify with `clean_jobs=true`, `client.reconnect`, authorization reset, and session replacement invalidate queued, active, and valid-job state before another submit intent can be produced.
    - Test 5: `safe_stop("operator_cancelled")` returns postconditions with `socket_stopped=true`, `active_work_invalidated=true`, `mining_disabled=true`, `hardware_control_disabled=true`, `work_submission_blocked=true`, `post_stop_snapshot_required=true`, and prevents later inbound messages from advancing mining state.
  </behavior>
  <action>
    Create `crates/bitaxe-stratum/src/v1/live_runtime.rs` per D-01, D-02, D-03, D-10, D-11, and D-13. Define pure types with redaction-safe `Debug`: `LiveStratumRuntime`, `LiveRuntimeAction`, `LiveRuntimeEvent`, `LivePoolCredentials`, `LiveRuntimeConfig`, `SafeStopReason`, and `SafeStopPostconditions`.

    Implement lifecycle methods that use existing typed contracts: queue `StratumV1ClientMessage::subscribe(StratumRequestId::new(1), model, version)`, queue `authorize(StratumRequestId::new(2), username, password)` only after `StratumResponse.maybe_extranonce` is present, update `MiningRuntimeState` on authorize/set-difficulty/notify, build pool work through `MiningWorkBuilder`, and store pool-derived work in `ProductionWorkRegistry`.

    Implement invalidation paths for clean-jobs, `StratumV1ServerMessage::ClientReconnect`, authorization reset, session replacement, fallback exhaustion, and `safe_stop`. Do not add socket types, sleeps, ESP-IDF calls, credential-file reads, `std::net`, or firmware-only effects to this pure module. Export `pub mod live_runtime;` from `v1.rs` and add `"src/v1/live_runtime.rs"` to `crates/bitaxe-stratum/BUILD.bazel`.
  </action>
  <verify>
    <automated>bazel test //crates/bitaxe-stratum:tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg "pub struct LiveStratumRuntime|pub enum LiveRuntimeAction|pub struct SafeStopPostconditions" crates/bitaxe-stratum/src/v1/live_runtime.rs` returns matches.
    - `rg "socket_stopped|active_work_invalidated|mining_disabled|hardware_control_disabled|work_submission_blocked|post_stop_snapshot_required" crates/bitaxe-stratum/src/v1/live_runtime.rs` returns matches.
    - `rg "invalidate_for_clean_jobs|invalidate_for_reconnect|invalidate_for_authorization_reset|invalidate_for_session_replacement" crates/bitaxe-stratum/src/v1/live_runtime.rs` returns matches.
    - `! rg -n "TcpStream|std::net|esp_idf|sleep|poolURL|poolUser|poolPassword|stratum[+]tcp://|device_url|raw_bm1366_frame" crates/bitaxe-stratum/src/v1/live_runtime.rs` returns no matches.
    - `rg "pub mod live_runtime;" crates/bitaxe-stratum/src/v1.rs` returns exactly one match, and `rg "\"src/v1/live_runtime.rs\"" crates/bitaxe-stratum/BUILD.bazel` returns one match.
  </acceptance_criteria>
  <done>Pure live runtime covers STR-08 lifecycle and SAFE-12 safe-stop postconditions without owning firmware socket I/O or leaking secret-bearing values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 25-01-02: Add SubmitIntent-tied response classifier</name>
  <files>crates/bitaxe-stratum/src/v1/submit_response.rs, crates/bitaxe-stratum/src/v1.rs, crates/bitaxe-stratum/BUILD.bazel</files>
  <read_first>
    - .planning/phases/25-live-stratum-runtime-and-safe-stop/25-CONTEXT.md
    - docs/parity/evidence/phase-24-bm1366-production-work-path/result-correlation.md
    - crates/bitaxe-stratum/src/v1/messages.rs
    - crates/bitaxe-stratum/src/v1/production_work.rs
    - crates/bitaxe-stratum/src/v1/mining.rs
    - crates/bitaxe-stratum/src/v1/state.rs
    - crates/bitaxe-stratum/src/v1.rs
    - crates/bitaxe-stratum/BUILD.bazel
  </read_first>
  <behavior>
    - Test 1: matching `StratumResponse { maybe_id: Some(request_id), success: true }` plus a live `SubmitIntent` classifies as `Accepted`.
    - Test 2: matching failed response classifies as `Rejected` with a redaction-safe label such as `pool_rejected_share`, not the raw pool error string.
    - Test 3: mismatched id, absent id, absent SubmitIntent, fake-only response, and stale generation classify as `NoObservedShare` or `Blocked`, never accepted or rejected.
    - Test 4: timeout, reconnect, malformed JSON, blocked prerequisite, and socket-stopped observations classify distinctly as `Timeout`, `Reconnect`, `Malformed`, `Blocked`, and `Stopped`.
  </behavior>
  <action>
    Create `crates/bitaxe-stratum/src/v1/submit_response.rs` per D-04, D-05, D-06, and D-09. Define `SubmitResponseObservation`, `SubmitClassification`, and `RedactedSubmitRejectReason` with redaction-safe `Debug` and stable lower-snake labels.

    Implement `classify_submit_response(intent: &SubmitIntent, request_id: StratumRequestId, observation: SubmitResponseObservation) -> SubmitClassification`. The function must require a current `SubmitIntent`, matching request id, and typed `StratumResponse` before returning `Accepted` or `Rejected`. It must never inspect or render raw share payloads, extranonces, targets, pool endpoints, usernames, workers, passwords, socket errors, or raw BM1366 frames.

    Export `pub mod submit_response;` from `v1.rs` and add `"src/v1/submit_response.rs"` to `crates/bitaxe-stratum/BUILD.bazel`.
  </action>
  <verify>
    <automated>bazel test //crates/bitaxe-stratum:tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg "pub enum SubmitResponseObservation|pub enum SubmitClassification|pub enum RedactedSubmitRejectReason|classify_submit_response" crates/bitaxe-stratum/src/v1/submit_response.rs` returns matches.
    - `rg "Accepted|Rejected|Timeout|Reconnect|Malformed|NoObservedShare|Blocked|Stopped" crates/bitaxe-stratum/src/v1/submit_response.rs` returns matches.
    - `! rg -n "record_accepted_share|record_rejected_share" crates/bitaxe-stratum/src/v1/submit_response.rs` returns no matches unless the call is guarded by a matching `SubmitIntent` and request-id test in the same module.
    - `! rg -n "stratum[+]tcp://|poolUser|poolPassword|target=|extranonce=|share_payload|socket_error=|raw_bm1366_frame" crates/bitaxe-stratum/src/v1/submit_response.rs` returns no matches.
    - `rg "pub mod submit_response;" crates/bitaxe-stratum/src/v1.rs` returns exactly one match, and `rg "\"src/v1/submit_response.rs\"" crates/bitaxe-stratum/BUILD.bazel` returns one match.
  </acceptance_criteria>
  <done>STR-09 classification can only advance from a live ASIC-derived submit intent plus matching pool response, preserving exact non-claims for fake or stale evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 25-01-03: Expand deterministic fake-pool coverage</name>
  <files>crates/bitaxe-stratum/src/v1/fake_pool.rs, crates/bitaxe-stratum/src/v1/live_runtime.rs, crates/bitaxe-stratum/src/v1/submit_response.rs</files>
  <read_first>
    - .planning/phases/25-live-stratum-runtime-and-safe-stop/25-CONTEXT.md
    - .planning/phases/25-live-stratum-runtime-and-safe-stop/25-VALIDATION.md
    - crates/bitaxe-stratum/src/v1/fake_pool.rs
    - crates/bitaxe-stratum/src/v1/live_runtime.rs
    - crates/bitaxe-stratum/src/v1/submit_response.rs
    - crates/bitaxe-stratum/src/v1/messages.rs
    - crates/bitaxe-stratum/src/v1/production_work.rs
  </read_first>
  <behavior>
    - Test 1: fake pool covers subscribe, authorize, set-difficulty, extranonce assignment, notify, and submit response sequencing through the new pure runtime.
    - Test 2: fake pool accepted and rejected submit responses produce `SubmitClassification::Accepted` and `SubmitClassification::Rejected` only in deterministic STR-11 test scope.
    - Test 3: clean-jobs and reconnect advance generation and invalidate queued/active work before stale submit classification.
    - Test 4: blocked prerequisite, fallback activation, timeout, malformed response, disconnect, and no-response paths produce redaction-safe non-accepted outcomes.
  </behavior>
  <action>
    Extend the existing `FakePoolTranscript` in `fake_pool.rs` per D-07, D-08, and D-09. Do not create a second simulator, daemon, socket listener, external mock process, or fixture service.

    Add events or helpers only as needed to drive the new `LiveStratumRuntime` and `submit_response` classifier: clean-jobs notify, client reconnect, malformed response, no-response timeout, blocked prerequisite, fallback activation, accepted submit response, and rejected submit response. Keep fake-pool evidence explicitly deterministic and below live STR-09 proof in comments and test names.
  </action>
  <verify>
    <automated>bazel test //crates/bitaxe-stratum:tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg "clean_jobs|reconnect|malformed|timeout|blocked|accepted|rejected" crates/bitaxe-stratum/src/v1/fake_pool.rs` returns Phase 25 test coverage matches.
    - `rg "classify_submit_response|SubmitClassification|LiveStratumRuntime" crates/bitaxe-stratum/src/v1/fake_pool.rs` returns matches.
    - `! rg -n "TcpStream|std::net|listener|daemon|external simulator|stratum[+]tcp://|poolUser|poolPassword|share_payload|raw_bm1366_frame" crates/bitaxe-stratum/src/v1/fake_pool.rs` returns no matches.
    - `bazel test //crates/bitaxe-stratum:tests` passes with the new fake-pool tests included.
  </acceptance_criteria>
  <done>STR-11 deterministic fake-pool coverage proves the Phase 25 lifecycle and fail-closed matrix without claiming live accepted/rejected share evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| pool response -> pure parser | Untrusted Stratum JSON becomes typed `StratumV1ServerMessage`. |
| ASIC result -> submit intent | Parsed BM1366 result becomes a `SubmitIntent` only after current-generation active-work correlation. |
| fake-pool fixture -> evidence claim | Deterministic tests can satisfy STR-11 but must not be promoted as live STR-09 proof. |
| stop request -> runtime state | Safe-stop input must prevent later runtime state advancement and stale work submission. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-25-01 | Information Disclosure | `live_runtime.rs`, `submit_response.rs`, `fake_pool.rs` | mitigate | Redaction-safe `Debug`, no raw pool endpoint/user/password/worker/target/extranonce/share/socket/BM1366 values, and static forbidden-pattern checks in acceptance criteria. |
| T-25-02 | Repudiation / Integrity | `submit_response.rs`, `fake_pool.rs` | mitigate | Accepted/rejected classification requires `SubmitIntent` plus matching request id; fake-pool tests remain STR-11-only and never live STR-09 evidence. |
| T-25-03 | Tampering / Safety | `live_runtime.rs`, `ProductionWorkRegistry` | mitigate | Clean-jobs, reconnect, authorization reset, session replacement, fallback exhaustion, and safe stop invalidate queued, active, and valid-job state. |
| T-25-04 | Denial of Service | pure runtime loop contracts | mitigate | Pure runtime emits bounded actions only; firmware blocking I/O is deferred to Plan 25-02 with watchdog checkpoints. |
| T-25-05 | Elevation of Privilege / Safety | live runtime startup | mitigate | Runtime remains fail-closed behind typed Phase 22 readiness and does not add hardware enablement or socket startup in this plan. |
</threat_model>

<verification>
Run `bazel test //crates/bitaxe-stratum:tests`.
Run `rg "pub mod live_runtime;|pub mod submit_response;" crates/bitaxe-stratum/src/v1.rs`.
Run `rg "\"src/v1/live_runtime.rs\"|\"src/v1/submit_response.rs\"" crates/bitaxe-stratum/BUILD.bazel`.
Run `! rg -n "stratum[+]tcp://|poolUser|poolPassword|target=|extranonce=|share_payload|socket_error=|device_url=|raw_bm1366_frame" crates/bitaxe-stratum/src/v1/live_runtime.rs crates/bitaxe-stratum/src/v1/submit_response.rs crates/bitaxe-stratum/src/v1/fake_pool.rs`.
</verification>

<success_criteria>
Plan 25-01 succeeds when pure Stratum modules model the full Phase 25 lifecycle, submit response classification is tied to live ASIC-derived submit intent, fake-pool tests cover STR-11, and no pure module owns socket I/O or leaks sensitive runtime values.
</success_criteria>

<output>
After completion, create `.planning/phases/25-live-stratum-runtime-and-safe-stop/25-01-SUMMARY.md`.
</output>
