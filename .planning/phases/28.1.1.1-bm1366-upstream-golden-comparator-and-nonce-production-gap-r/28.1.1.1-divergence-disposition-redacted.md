# Phase 28.1.1.1 Divergence Disposition

divergence_disposition: gaps_found
rust_patch_status: not_permitted_because_aligned_job_fields_match
next_diagnostic_layer: below_job_bytes
source_work_alignment_status: matched
comparison_status: match
matched_field_count: 11/11
upstream_fake_pool_submit_observed: true
rust_fake_pool_submit_observed: false
raw_bytes_committed: false
credential_contents_read: false

## Basis

The redacted comparator evidence report records:

- `comparison_status: match`
- `upstream_capture_status: job_frame_found`
- `rust_capture_status: job_frame_found`
- `source_work_alignment_status: matched`
- `comparison_pair: best_field_match`
- `matched_field_count: 11/11`
- `mismatched_fields: none`

The deterministic source-work run proves both sides emitted BM1366 job frames from the same synthetic Stratum fixture. It also proves the previously observed `ntime` and merkle-root mismatch was a live source-work alignment artifact, not a confirmed Rust job-construction divergence.

## Patch Policy Result

No Rust job construction patch is justified from this run. Under aligned source work, the best upstream/Rust job-frame pair matches across all compared fields.

The aligned upstream run produced fake-pool submit responses; the aligned Rust run did not submit a share and did not produce result-correlation evidence. The remaining valid action is below job bytes.

## Diagnostic Routing

Route diagnosis below job construction:

- ASIC enable or power sequencing
- reset timing
- PLL or baud timing
- missing upstream side effect outside the job-frame bytes
