# Phase 28.1.1.1 Upstream Capture Attempt

attempt_status: captured_with_gaps
hardware_detection_status: passed_before_upstream_flash
post_upstream_detection_status: passed_after_board_reboot
scratch_checkout_status: reused_or_recreated_as_local_clone
upstream_debug_flags_status: prepared_in_scratch_only_with_job_work_debug
docker_manifest_status: passed
docker_pull_status: passed
docker_image_status: built
upstream_build_status: passed
upstream_flash_status: passed
upstream_nvs_flash_status: passed
upstream_monitor_status: app_logs_and_bm1366_job_frames_found
rust_recovery_status: passed_after_upstream_capture
raw_capture_ignored: true
credential_files_used_as_runtime_inputs: true
credential_values_printed: false
credential_values_committed: false
reference_tree_modified: false

## Attempt

The Docker registry-cache blocker was cleared by retrying the manifest, pull, and local Docker build path. After the board reboot restored `espflash` connectivity, the helper built the upstream debug image in ignored scratch, generated the local NVS image under ignored scratch using runtime-only credential files, flashed the upstream merged image plus NVS image, and completed the bounded monitor capture.

The previous bootloader-only capture was fixed by cleaning stale scratch build/config state, forcing a scratch-only USB Serial/JTAG console override, and enabling BM1366 job-work debug in scratch. The successful capture recorded app markers, BM1366 debug frames, and BM1366 job frames. The helper wrote only count/status classification outside the ignored raw log.

## Recovery Result

After the successful upstream capture, Rust recovery passed through the existing detector-gated `just flash-monitor` path and produced a fresh 360-second Rust log. The Rust run produced BM1366 job TX/RX traces and applied the pool-input bridge, but did not produce `result_correlated` or share-submission evidence.

## Evidence Boundaries

Raw logs and generated NVS data remain under ignored `scratch/` and `hardware-runs/` paths. This redacted artifact records only status categories and does not include raw serial bytes, Wi-Fi values, pool endpoints, pool users, worker strings, credential secrets, device URLs, IPs, or MAC addresses.
