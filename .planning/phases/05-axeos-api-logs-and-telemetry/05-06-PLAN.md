---
phase: 05-axeos-api-logs-and-telemetry
plan: 06
type: execute
wave: 5
depends_on: ["05-04"]
files_modified:
  - crates/bitaxe-api/BUILD.bazel
  - crates/bitaxe-api/src/lib.rs
  - crates/bitaxe-api/src/commands.rs
  - crates/bitaxe-api/fixtures/api/command-responses.json
autonomous: true
requirements: [API-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 5-2026-06-27T18-02-49
generated_at: 2026-06-27T18:51:56Z
must_haves:
  truths:
    - "Pause, resume, restart, identify, and block-found dismiss produce upstream-compatible public responses."
    - "Command plans return response JSON separately from side-effect enums so firmware can send responses before effects."
    - "Pause and resume update visible mining activity state without forcing work submission ready."
  artifacts:
    - path: "crates/bitaxe-api/src/commands.rs"
      provides: "Pure command response/effect planners"
    - path: "crates/bitaxe-api/fixtures/api/command-responses.json"
      provides: "Captured public command response fixtures"
  key_links:
    - from: "crates/bitaxe-api/src/commands.rs"
      to: "crates/bitaxe-stratum/src/v1/state.rs"
      via: "MiningActivityStatus mutation plans only"
    - from: "crates/bitaxe-api/src/commands.rs"
      to: "firmware/bitaxe/src/display_adapter.rs"
      via: "identify effect consumed by firmware"
---

<objective>
Implement pure command route planners for non-OTA AxeOS actions.

Purpose: Commands have visible side effects and safety implications. This plan makes response shape and side-effect ordering testable before firmware executes effects.
Output: `bitaxe-api::commands` with public JSON responses, side-effect plans, and fixture tests for API-08.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-axeos-api-logs-and-telemetry/05-CONTEXT.md
@.planning/phases/05-axeos-api-logs-and-telemetry/05-UI-SPEC.md
@crates/bitaxe-api/src/commands.rs
@crates/bitaxe-stratum/src/v1/state.rs
@crates/bitaxe-stratum/src/v1/mining_loop.rs
@firmware/bitaxe/src/display_adapter.rs
@reference/esp-miner/main/http_server/http_server.c
@reference/esp-miner/main/screen.c
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add command response and effect planners</name>
  <files>crates/bitaxe-api/BUILD.bazel, crates/bitaxe-api/src/lib.rs, crates/bitaxe-api/src/commands.rs, crates/bitaxe-api/fixtures/api/command-responses.json</files>
  <read_first>
    - `.planning/phases/05-axeos-api-logs-and-telemetry/05-CONTEXT.md` decisions D-21 through D-24.
    - `.planning/phases/05-axeos-api-logs-and-telemetry/05-UI-SPEC.md` Copywriting Contract and command route contract.
    - `reference/esp-miner/main/http_server/http_server.c` command handlers.
    - `reference/esp-miner/main/screen.c` identify mode behavior.
  </read_first>
  <behavior>
    - Test 1: Pause plan returns `{"message":"Mining paused"}` and a mining activity state update that does not alter work submission.
    - Test 2: Resume plan returns `{"message":"Mining resumed"}` and preserves existing mining-loop gate status.
    - Test 3: Restart plan returns `{"message":"System will restart shortly."}` plus `RestartAfterResponse`.
    - Test 4: Identify toggles on for 30000 ms and off with exact upstream-compatible messages.
    - Test 5: Block-found dismiss clears `showNewBlock`, preserves `blockFound`, and returns the expected message.
  </behavior>
  <action>
    Create and wire the `commands` module in `lib.rs` and Bazel, then implement a `CommandPlan` with `response` and `effect` fields per D-21 through D-24. Effects should include pause, resume, restart-after-response, identify on/off with 30000 ms duration, and block-found dismiss state update. Keep command behavior pure and host-testable. Do not implement OTA commands here. Preserve exact public response copy from the UI spec.
  </action>
  <acceptance_criteria>
    - Fixture tests prove exact JSON response bodies for every non-OTA command route.
    - Restart effect is represented as after-response and cannot be executed by the pure planner.
    - Pause/resume effects cannot set `WorkSubmissionGate::Ready`.
  </acceptance_criteria>
  <verify>
    <automated>bazel test //crates/bitaxe-api:tests --test_filter=commands</automated>
    <quick>bazel test //crates/bitaxe-api:tests</quick>
    <full>just test</full>
  </verify>
  <done>Command planners produce compatible responses and safe side-effect plans for firmware route wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add command state transition and safety regression tests</name>
  <files>crates/bitaxe-api/src/commands.rs</files>
  <read_first>
    - `crates/bitaxe-stratum/src/v1/state.rs` for mining activity and work submission fields.
    - `crates/bitaxe-stratum/src/v1/mining_loop.rs` for fail-closed mining-loop gate decisions.
    - `.planning/phases/04-stratum-v1-and-first-mining-loop/04-04-SUMMARY.md` for blocked runtime state decisions.
  </read_first>
  <behavior>
    - Test 1: Applying pause to an active or safe-blocked state only changes visible paused state and leaves work submission unchanged.
    - Test 2: Applying resume from paused state restores visible active/safe-blocked state according to the prior gate result, not by assuming readiness.
    - Test 3: Block-found dismiss is idempotent for repeated requests.
  </behavior>
  <action>
    Add focused state-transition helpers and tests around the pure command plans. Use typed state from `MiningRuntimeState` and avoid booleans that can represent impossible combinations. The command layer should not decide ASIC readiness, safety readiness, or hardware evidence readiness; it only plans visible state transitions consumed by the firmware shell.
  </action>
  <acceptance_criteria>
    - Tests cover active, paused, and safe-blocked mining states.
    - `commands.rs` has no raw ASIC commands, no UART calls, no ESP-IDF imports, and no NVS writes.
    - Repeated block-found dismiss calls remain deterministic.
  </acceptance_criteria>
  <verify>
    <automated>bazel test //crates/bitaxe-api:tests --test_filter=commands</automated>
    <quick>bazel test //crates/bitaxe-api:tests</quick>
    <full>just test</full>
  </verify>
  <done>Command state transitions are safe, typed, idempotent where expected, and ready for Plan 05 firmware effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| HTTP command route -> command planner | Untrusted user actions become planned device effects. |
| Command planner -> firmware effects | Planned effects will be executed by firmware after response send. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-05-20 | Tampering | pause/resume planners | mitigate | Typed state tests prove commands cannot force work submission ready or bypass gates. |
| T-05-21 | Denial of Service | restart planner | mitigate | Restart is represented as delayed after-response effect; firmware route can log and schedule predictably. |
| T-05-22 | Repudiation | command responses | mitigate | Exact response fixtures and typed effects make accepted commands auditable by firmware logs. |
| T-05-23 | Safety impact | identify/display/mining state | mitigate | Identify is time-bounded to 30000 ms; mining effects stay visible-state only. |

High-risk block: stop if a command planner can directly execute restart, write NVS, send ASIC work, or set work submission ready.
</threat_model>

<verification>
Quick: `bazel test //crates/bitaxe-api:tests`

Full: `just test`
</verification>

<simplification_pass>
Before completing, remove any command helper that mixes public response construction with firmware effect execution. The root-cause safety boundary is response plan first, effect execution in the firmware shell after the response has been sent.
</simplification_pass>

<success_criteria>
- API-08 command response/effect behavior is host-tested.
- D-21 through D-24 are implemented without OTA behavior or mining-gate bypass.
- Plan 05 can consume command plans directly in firmware routes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-axeos-api-logs-and-telemetry/05-06-SUMMARY.md`.
</output>
