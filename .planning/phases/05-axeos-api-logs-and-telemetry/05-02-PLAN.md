---
phase: 05-axeos-api-logs-and-telemetry
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - crates/bitaxe-api/BUILD.bazel
  - crates/bitaxe-api/src/lib.rs
  - crates/bitaxe-api/src/settings.rs
  - crates/bitaxe-api/fixtures/api/settings-patch-cases.json
autonomous: true
requirements: [API-02, API-03]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 5-2026-06-27T18-02-49
generated_at: 2026-06-27T18:51:56Z
must_haves:
  truths:
    - "PATCH /api/system request planning validates all known fields before any write is emitted."
    - "Unknown fields are ignored and invalid known fields reject the whole patch with upstream-compatible public errors."
    - "Accepted settings expose an internal persist-then-reload contract without adding public rich errors."
  artifacts:
    - path: "crates/bitaxe-api/src/settings.rs"
      provides: "Public PATCH parser, accepted settings plan, public error mapping, and fake adapter tests"
    - path: "crates/bitaxe-api/fixtures/api/settings-patch-cases.json"
      provides: "Representative success, unknown-field, invalid-field, and no-write rejection fixtures"
  key_links:
    - from: "crates/bitaxe-api/src/settings.rs"
      to: "crates/bitaxe-config/src/settings.rs"
      via: "SettingsPatch and apply_settings_patch"
    - from: "crates/bitaxe-api/src/settings.rs"
      to: "crates/bitaxe-config/src/persistence.rs"
      via: "NvsSnapshot reload semantics in tests"
---

<objective>
Implement the host-testable settings PATCH planning layer for AxeOS compatibility.

Purpose: `PATCH /api/system` is a high-impact settings boundary. This plan ensures public behavior matches upstream while all validation and write decisions reuse the existing pure config model.
Output: A tested `bitaxe-api::settings` module that emits accepted write/reload plans or generic public errors.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-axeos-api-logs-and-telemetry/05-CONTEXT.md
@.planning/phases/05-axeos-api-logs-and-telemetry/05-RESEARCH.md
@.planning/phases/05-axeos-api-logs-and-telemetry/05-VALIDATION.md
@crates/bitaxe-api/src/settings.rs
@crates/bitaxe-config/src/settings.rs
@crates/bitaxe-config/src/persistence.rs
@crates/bitaxe-config/src/nvs.rs
@reference/esp-miner/main/http_server/http_server.c
@reference/esp-miner/main/nvs_config.c

<interfaces>
```rust
// crates/bitaxe-config/src/settings.rs
pub enum RawSettingValue { String(String), Number(i64), Float(f64), Bool(bool) }
pub struct SettingsPatch { /* REST-name keyed patch */ }
pub enum SettingsUpdateDecision {
    Accepted { writes: Vec<NvsWrite> },
    Rejected { errors: Vec<ConfigValidationError> },
}
pub fn apply_settings_patch(patch: &SettingsPatch) -> SettingsUpdateDecision;
```

```rust
// crates/bitaxe-config/src/persistence.rs
pub fn apply_patch_to_snapshot(snapshot: &NvsSnapshot, patch: &SettingsPatch) -> PersistenceDecision;
pub fn reload_snapshot(snapshot: &NvsSnapshot) -> PersistenceDecision;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse PATCH JSON into schema-driven settings decisions</name>
  <files>crates/bitaxe-api/BUILD.bazel, crates/bitaxe-api/src/lib.rs, crates/bitaxe-api/src/settings.rs, crates/bitaxe-api/fixtures/api/settings-patch-cases.json</files>
  <read_first>
    - `.planning/phases/05-axeos-api-logs-and-telemetry/05-CONTEXT.md` decisions D-06 through D-10.
    - `reference/esp-miner/main/http_server/http_server.c` PATCH handling and public error strings.
    - `crates/bitaxe-config/src/settings.rs` for the existing update authority.
    - `crates/bitaxe-config/src/nvs.rs` for REST names and stored types.
  </read_first>
  <behavior>
    - Test 1: Valid known REST fields produce expected `NvsWrite` values and legacy mirror writes.
    - Test 2: Unknown JSON fields are ignored and do not appear in emitted writes.
    - Test 3: Any invalid known field rejects the whole patch and emits no writes.
    - Test 4: Malformed or non-object JSON maps to public `Invalid JSON` without write decisions.
  </behavior>
  <action>
    Create and wire the `settings` module in `lib.rs` and Bazel, then implement a `SettingsPatchRequest` or equivalent parser that accepts `serde_json::Value`, requires a JSON object, converts known REST field values into `bitaxe_config::RawSettingValue`, ignores unknown fields per D-06, and calls `apply_settings_patch` per D-07. Define public error mapping for `Invalid JSON` and `Wrong API input` only; retain typed internal diagnostics for tests and firmware logs without exposing a new public schema per D-08 and D-09. Keep frequency, voltage, fan, thermal, pool, and display validation pure and side-effect-free per D-10.
  </action>
  <acceptance_criteria>
    - Invalid known fields reject atomically and tests assert the original `NvsSnapshot` remains unchanged.
    - Unknown fields are ignored even when mixed with valid known fields.
    - Public error bodies are exactly `Invalid JSON` or `Wrong API input` where applicable.
    - No setting value containing password, cert, key, or user fields is logged by new test helpers.
  </acceptance_criteria>
  <verify>
    <automated>bazel test //crates/bitaxe-api:tests //crates/bitaxe-config:tests --test_filter=settings</automated>
    <quick>bazel test //crates/bitaxe-api:tests //crates/bitaxe-config:tests</quick>
    <full>just test</full>
  </verify>
  <done>PATCH request planning is schema-driven, all-or-nothing for known fields, unknown-field tolerant, and public-error compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove persist-then-reload adapter ordering with fake storage</name>
  <files>crates/bitaxe-api/src/settings.rs, crates/bitaxe-api/fixtures/api/settings-patch-cases.json</files>
  <read_first>
    - `crates/bitaxe-config/src/persistence.rs` for `NvsSnapshot`, `apply_patch_to_snapshot`, and `reload_snapshot`.
    - `.planning/phases/02-ultra-205-config-and-nvs-model/02-04-SUMMARY.md` for pure persistence scope decisions.
    - `.planning/phases/05-axeos-api-logs-and-telemetry/05-UI-SPEC.md` PATCH contract.
  </read_first>
  <behavior>
    - Test 1: An accepted patch produces a plan whose adapter sequence is validate, write all, commit, reload, then public success.
    - Test 2: An adapter write, commit, or reload failure maps to a firmware-visible failure while preserving the upstream public response shape.
    - Test 3: Hostname changes are represented as a best-effort live-apply effect after persistence success.
  </behavior>
  <action>
    Add an internal adapter contract such as `SettingsPersistencePlan`, `SettingsPersistenceEffect`, and a fake storage test harness. The contract must require persistence and reload before the route returns success when the adapter can prove it, while keeping the public success body empty and public errors generic per D-08 and D-09. Represent live hostname apply as an effect that firmware can attempt after accepted persistence; do not add Wi-Fi, NVS I/O, or hardware side effects in this crate.
  </action>
  <acceptance_criteria>
    - Fake adapter tests prove response success is impossible before write, commit, and reload success.
    - Firmware-visible failures carry typed internal reasons but public mapping stays compatible.
    - Hostname live-apply is a best-effort effect and cannot make validation or persistence succeed by itself.
  </acceptance_criteria>
  <verify>
    <automated>bazel test //crates/bitaxe-api:tests --test_filter=settings</automated>
    <quick>bazel test //crates/bitaxe-api:tests //crates/bitaxe-config:tests</quick>
    <full>just test</full>
  </verify>
  <done>Settings planning satisfies API-03 pure semantics and is ready for firmware route wiring in Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| HTTP body -> settings parser | Untrusted JSON controls persisted device settings. |
| Settings plan -> firmware storage adapter | Validated settings become NVS write/commit/reload effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-05-03 | Tampering | `settings.rs` parser | mitigate | Parse JSON object at the boundary, validate every known field through `bitaxe-config`, reject whole patch on any invalid known field. |
| T-05-04 | Information Disclosure | settings diagnostics | mitigate | Public errors stay generic and tests ensure secrets are not logged by Phase 05 settings code. |
| T-05-05 | Denial of Service | oversized or malformed JSON | mitigate | Route plan must pair this parser with request-size caps in Plan 05; parser rejects non-object/malformed values visibly. |
| T-05-06 | Elevation of Privilege | frequency/voltage/fan fields | mitigate | Accepted settings emit data writes only; Phase 5 must not enable hardware effects. |

High-risk block: stop if the implementation writes any known setting before all known fields validate, logs secret-bearing settings, or adds hardware-control effects.
</threat_model>

<verification>
Quick: `bazel test //crates/bitaxe-api:tests //crates/bitaxe-config:tests`

Full: `just test`
</verification>

<simplification_pass>
Before completing, remove duplicate validation branches that mirror `bitaxe-config`. The root cause of PATCH drift is split validation authority; the final code must call the existing settings schema rather than maintaining a second HTTP table.
</simplification_pass>

<success_criteria>
- API-03 has host-testable validation, persistence planning, rejection, reload, and public error coverage.
- D-06 through D-10 are implemented without enabling voltage, fan, thermal, power, or unsafe mining effects.
- Settings code remains reusable by the firmware route shell in Plan 05.
</success_criteria>

<output>
After completion, create `.planning/phases/05-axeos-api-logs-and-telemetry/05-02-SUMMARY.md`.
</output>
