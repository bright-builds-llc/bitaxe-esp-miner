---
phase: 05-axeos-api-logs-and-telemetry
plan: 07
type: execute
wave: 7
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05", "05-06"]
files_modified:
  - tools/parity/Cargo.toml
  - tools/parity/BUILD.bazel
  - tools/parity/src/main.rs
  - tools/parity/src/api_compare.rs
  - tools/parity/fixtures/api/phase05-required-routes.json
  - tools/parity/fixtures/api/axeos-route-usage.json
  - docs/parity/checklist.md
  - docs/parity/evidence/phase-05-axeos-api-logs-and-telemetry.md
autonomous: true
requirements: [API-01, API-09, API-10]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 5-2026-06-27T18-02-49
generated_at: 2026-06-27T18:51:56Z
must_haves:
  truths:
    - "API comparison evidence distinguishes schema compatibility, captured response compatibility, and firmware smoke."
    - "Existing AxeOS service route usage remains administrable without a new frontend framework or Angular rewrite."
    - "`/recovery` and static fallback behavior are covered separately from API-route usage, without claiming Phase 7 packaging success."
    - "Parity checklist rows advance only to evidence-supported statuses and do not mark safety-critical hardware behavior verified."
  artifacts:
    - path: "tools/parity/src/api_compare.rs"
      provides: "Route/property/captured-response/static-route compatibility checks"
    - path: "tools/parity/fixtures/api/axeos-route-usage.json"
      provides: "Static AxeOS route usage plus separate `/recovery` and static fallback expectations extracted from upstream services/filesystem behavior"
    - path: "docs/parity/evidence/phase-05-axeos-api-logs-and-telemetry.md"
      provides: "Phase 05 evidence record and residual hardware/static packaging boundaries"
  key_links:
    - from: "tools/parity/src/api_compare.rs"
      to: "reference/esp-miner/main/http_server/openapi.yaml"
      via: "required V1 Phase 05 route/property checks"
    - from: "tools/parity/fixtures/api/axeos-route-usage.json"
      to: "reference/esp-miner/main/http_server/axe-os/src/app/services"
      via: "service-call fixture scan"
---

<objective>
Close Phase 05 with API comparison, static AxeOS compatibility evidence, and parity checklist updates.

Purpose: Implemented routes are not enough for parity. This plan proves schema/fixture/static compatibility and records what remains Phase 6 or Phase 7-owned without overclaiming.
Output: `tools/parity` API compare checks, AxeOS route usage fixtures, Phase 05 evidence, and checklist updates.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-axeos-api-logs-and-telemetry/05-CONTEXT.md
@.planning/phases/05-axeos-api-logs-and-telemetry/05-RESEARCH.md
@.planning/phases/05-axeos-api-logs-and-telemetry/05-VALIDATION.md
@.planning/phases/05-axeos-api-logs-and-telemetry/05-UI-SPEC.md
@tools/parity/src/main.rs
@tools/parity/BUILD.bazel
@docs/parity/checklist.md
@reference/esp-miner/main/http_server/openapi.yaml
@reference/esp-miner/main/http_server/axe-os/src/app/services/system.service.ts
@reference/esp-miner/main/http_server/axe-os/src/app/services/live-data.service.ts
@reference/esp-miner/main/http_server/axe-os/src/app/services/web-socket.service.ts
@reference/esp-miner/main/http_server/filesystem.c
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add API compare and static AxeOS route usage checks</name>
  <files>tools/parity/Cargo.toml, tools/parity/BUILD.bazel, tools/parity/src/main.rs, tools/parity/src/api_compare.rs, tools/parity/fixtures/api/phase05-required-routes.json, tools/parity/fixtures/api/axeos-route-usage.json</files>
  <read_first>
    - `.planning/phases/05-axeos-api-logs-and-telemetry/05-CONTEXT.md` decisions D-25 through D-27.
    - `.planning/phases/05-axeos-api-logs-and-telemetry/05-UI-SPEC.md` Static Asset Compatibility Evidence and API Comparison Evidence Contract.
    - `reference/esp-miner/main/http_server/openapi.yaml`.
    - `reference/esp-miner/main/http_server/axe-os/src/app/services/system.service.ts`, `live-data.service.ts`, and `web-socket.service.ts`.
  </read_first>
  <behavior>
    - Test 1: API compare fails if any Phase 05 route is missing from the upstream OpenAPI contract or Rust route manifest.
    - Test 2: API compare checks representative required properties for info, ASIC, statistics, scoreboard, settings PATCH, logs, WebSocket live update, and commands.
    - Test 3: Static route usage fixture includes info, ASIC, statistics, scoreboard, logs, PATCH settings, pause, resume, restart, identify, block-found dismiss, `/api/ws`, and `/api/ws/live`.
    - Test 4: OTA and OTAWWW appear only as Phase 7-owned unsafe-success-blocked routes.
    - Test 5: `/recovery` and static fallback expectations are represented separately from ordinary API route usage and cannot be counted as Phase 5 static/update packaging success.
  </behavior>
  <action>
    Extend `tools/parity` with an `api-compare` subcommand or report mode that checks Phase 05 route/property coverage and captured response fixtures per D-27. Use structured JSON manifests for expected route/property assertions; do not add a deprecated YAML dependency. For `openapi.yaml`, keep checks narrow and explicit if a maintained OpenAPI 3.1 parser is not already available. Add an AxeOS static route usage fixture extracted from upstream services per D-25; this fixture proves administration compatibility without introducing shadcn, Tailwind, React, Solid, Vite, or an Angular build pipeline. Add explicit fixture fields for `/recovery` and static fallback behavior from `reference/esp-miner/main/filesystem.c`: Phase 5 must either prove the route/fallback returns safe compatible content in a fixture/smoke, or mark static/recovery packaging evidence as Phase 7 pending. Keep OTA and OTAWWW represented only as safe Phase 7-owned unsupported/deferred route evidence per D-26.
  </action>
  <acceptance_criteria>
    - `bazel test //tools/parity:tests` fails when a required Phase 05 route is removed from the fixture.
    - `api-compare` output labels evidence as schema, captured-response, static-route, or firmware-smoke.
    - The fixture records OTA/OTAWWW as Phase 7-owned and cannot treat them as successful Phase 05 update routes.
    - The fixture records `/recovery` and static fallback as separate checks and fails if Phase 5 evidence labels them as release/static packaging success.
  </acceptance_criteria>
  <verify>
    <automated>bazel test //tools/parity:tests</automated>
    <quick>bazel test //tools/parity:tests && bazel test //crates/bitaxe-api:tests</quick>
    <full>just test</full>
  </verify>
  <done>API compare and static AxeOS route usage checks provide automated API-01, API-09, and API-10 evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Record Phase 05 parity evidence and checklist status</name>
  <files>docs/parity/checklist.md, docs/parity/evidence/phase-05-axeos-api-logs-and-telemetry.md</files>
  <read_first>
    - `docs/parity/checklist.md` API, LOG, STAT, UI, FS, OTA, PWR, and THR rows.
    - `docs/adr/0012-parity-verification-evidence.md`.
    - `.planning/phases/05-axeos-api-logs-and-telemetry/05-VALIDATION.md`.
    - Summaries from plans 05-01 through 05-06.
  </read_first>
  <action>
    Create the Phase 05 evidence document with command output sections for focused API tests, parity API compare, firmware build, and full `just test`. Update checklist rows for API/log/WebSocket/statistics/scoreboard/static compatibility with statuses supported by actual evidence. Mark schema/golden/API-compare proof separately from real firmware smoke. If no live Ultra 205 API smoke runs, record it as not run with the reason. Add a dedicated static/recovery subsection that states whether `/recovery` and static fallback were fixture/smoke-proved in Phase 5; if not, mark static/recovery packaging evidence as Phase 7 pending and explain that Phase 5 only proves the API/log/WebSocket administration surface cannot falsely claim update/static success. Do not mark voltage, fan, thermal, power, ASIC initialization, OTA, OTAWWW, SPIFFS, filesystem release packaging, recovery update behavior, or release artifacts verified from Phase 05 work.
  </action>
  <acceptance_criteria>
    - Evidence doc includes exact commands run, result, firmware commit/reference commit when available, and conclusion.
    - `just parity` reports `validation_errors: none`.
    - Checklist rows cite Phase 05 evidence and preserve Phase 6/Phase 7 boundaries.
    - Static/recovery evidence is either proved by fixture/smoke or explicitly labeled Phase 7 pending; neither case can count OTA, OTAWWW, SPIFFS, recovery update, or static-asset packaging as Phase 5 success.
    - No deferred idea from 05-CONTEXT.md appears as implemented Phase 05 scope.
  </acceptance_criteria>
  <verify>
    <automated>just parity</automated>
    <quick>bazel test //tools/parity:tests && just parity</quick>
    <full>just test</full>
  </verify>
  <done>Phase 05 evidence is recorded, checklist statuses are accurate, and parity tooling rejects unsupported verified claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Reference OpenAPI/assets -> parity fixtures | Upstream GPL reference data informs compatibility checks. |
| Parity evidence -> release/readiness claims | Documentation affects what users believe is verified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-05-24 | Tampering | API compare fixtures | mitigate | Tests fail on missing routes/properties and fixtures carry provenance to the pinned reference. |
| T-05-25 | Repudiation | evidence document | mitigate | Record exact commands, results, and evidence type labels. |
| T-05-26 | Information Disclosure | captured response fixtures | mitigate | Use synthetic/public values; redact or exclude secrets from captured data. |
| T-05-27 | Safety impact | checklist status | mitigate | `just parity --fail-on-invalid-verified` blocks safety-critical verified rows without hardware evidence. |
| T-05-28 | Malicious Code | static assets and `/recovery` | mitigate | Fixture-test existing pinned AxeOS assets plus `/recovery`/static fallback expectations; do not introduce unmanaged frontend build output or claim Phase 7 packaging success. |

High-risk block: stop if captured fixtures include real credentials, if parity rows mark safety-critical surfaces verified without hardware evidence, or if the plan starts an Angular rewrite/build pipeline.
</threat_model>

<verification>
Quick: `bazel test //tools/parity:tests && just parity`

Wave: `bazel test //...`

Full: `just test`
</verification>

<simplification_pass>
Before completing, remove checks that duplicate the same proof under different labels. The root-cause evidence model must stay clear: schema compatibility, captured response compatibility, static route compatibility, and firmware smoke are separate evidence types.
</simplification_pass>

<success_criteria>
- API-01 route/schema coverage exists in `tools/parity`.
- API-09 static AxeOS compatibility is proved by route usage fixtures without a frontend rewrite, and `/recovery` plus static fallback behavior is either fixture/smoke-proved or explicitly recorded as Phase 7 pending.
- API-10 representative success/error comparison evidence is automated.
- Phase 6 safety and Phase 7 OTA/filesystem/release packaging remain scoped out except for safe unsupported route compatibility, and Phase 5 evidence cannot falsely claim update/static success.
</success_criteria>

<output>
After completion, create `.planning/phases/05-axeos-api-logs-and-telemetry/05-07-SUMMARY.md`.
</output>
