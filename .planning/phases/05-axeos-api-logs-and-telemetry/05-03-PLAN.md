---
phase: 05-axeos-api-logs-and-telemetry
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - crates/bitaxe-api/BUILD.bazel
  - crates/bitaxe-api/src/lib.rs
  - crates/bitaxe-api/src/system.rs
  - crates/bitaxe-api/src/asic.rs
  - crates/bitaxe-api/src/statistics.rs
  - crates/bitaxe-api/src/scoreboard.rs
  - crates/bitaxe-api/src/mining.rs
  - crates/bitaxe-api/fixtures/api/statistics-empty-compatible.json
  - crates/bitaxe-api/fixtures/api/scoreboard-empty.json
autonomous: true
requirements: [API-02, API-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 5-2026-06-27T18-02-49
generated_at: 2026-06-27T18:51:56Z
must_haves:
  truths:
    - "System info, ASIC, statistics, scoreboard, and mining-state responses derive from typed Rust runtime data."
    - "Statistics can be empty or minimal without schema failure and without fake historical mining data."
    - "Scoreboard returns an array shape and never adds authoritative server-side rank or since fields."
  artifacts:
    - path: "crates/bitaxe-api/src/system.rs"
      provides: "Full-state mapper from ApiSnapshot to AxeOS system info wire response"
    - path: "crates/bitaxe-api/src/statistics.rs"
      provides: "Statistics labels, optional columns, timestamps, and empty-series handling"
    - path: "crates/bitaxe-api/src/scoreboard.rs"
      provides: "Scoreboard wire array shape"
  key_links:
    - from: "crates/bitaxe-api/src/mining.rs"
      to: "crates/bitaxe-stratum/src/v1/state.rs"
      via: "MiningRuntimeState counters, lifecycle, fallback, and work gate mapping"
    - from: "crates/bitaxe-api/src/asic.rs"
      to: "crates/bitaxe-config/src/catalog.rs"
      via: "Ultra 205 catalog and BM1366 options"
---

<objective>
Implement pure response mappers for the main AxeOS read endpoints: full system info, ASIC settings, statistics, scoreboard, and mining-state values.

Purpose: AxeOS compatibility requires stable JSON shapes while runtime data remains typed and testable. This plan bridges existing config, Stratum, and ASIC state into public responses without hardware-control claims.
Output: Host-tested mappers and fixtures for API-02 and API-04.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-axeos-api-logs-and-telemetry/05-CONTEXT.md
@.planning/phases/05-axeos-api-logs-and-telemetry/05-RESEARCH.md
@.planning/phases/05-axeos-api-logs-and-telemetry/05-UI-SPEC.md
@crates/bitaxe-api/src/snapshot.rs
@crates/bitaxe-api/src/wire.rs
@crates/bitaxe-config/src/defaults.rs
@crates/bitaxe-config/src/catalog.rs
@crates/bitaxe-stratum/src/v1/state.rs
@crates/bitaxe-stratum/src/v1/mining_loop.rs
@crates/bitaxe-asic/src/bm1366/observation.rs
@reference/esp-miner/main/http_server/system_api_json.c
@reference/esp-miner/main/http_server/axe-os/api/system/asic_settings.c
@reference/esp-miner/main/tasks/statistics_task.h
@reference/esp-miner/main/http_server/axe-os/src/models/ISystemScoreboard.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map system info, ASIC settings, and mining state from ApiSnapshot</name>
  <files>crates/bitaxe-api/BUILD.bazel, crates/bitaxe-api/src/lib.rs, crates/bitaxe-api/src/system.rs, crates/bitaxe-api/src/asic.rs, crates/bitaxe-api/src/mining.rs</files>
  <read_first>
    - `.planning/phases/05-axeos-api-logs-and-telemetry/05-CONTEXT.md` decisions D-11, D-12, and D-15.
    - `.planning/phases/04-stratum-v1-and-first-mining-loop/04-04-SUMMARY.md` for mining-loop blocked state.
    - `reference/esp-miner/main/http_server/system_api_json.c` for full-state fields.
    - `crates/bitaxe-config/src/catalog.rs` and `crates/bitaxe-stratum/src/v1/state.rs`.
  </read_first>
  <behavior>
    - Test 1: Ultra 205 safe snapshot maps to `ASICModel = "BM1366"`, `deviceModel = "ultra"`, `asicCount = 1`, expected frequency and voltage options.
    - Test 2: Share counters, rejected reasons, pool difficulty, fallback-active status, and hashrate inputs come from `MiningRuntimeState`.
    - Test 3: `hardware_evidence_ack_missing` remains visible as safe blocked state and never maps to active work submission.
  </behavior>
  <action>
    Create and wire the `system`, `asic`, and `mining` modules in `lib.rs` and Bazel, then implement pure mappers from `ApiSnapshot` to system info, ASIC settings, and mining-state wire DTOs per D-11, D-12, and D-15. Use `bitaxe-config` for defaults/catalog data, `MiningRuntimeState` for pool/share/hashrate/activity data, and `AsicInitStatus` for safe ASIC state. Use safe unavailable or blocked values for Phase 6-owned voltage, fan, thermal, current, and power readings per D-05. Do not serialize internal structs directly.
  </action>
  <acceptance_criteria>
    - Tests assert exact field names and value sources for representative system, ASIC, and mining fields.
    - Mapping code contains no hardcoded duplicate Ultra 205 option tables when `bitaxe-config` exports the data.
    - Safe blocked mining cannot become active unless `MiningRuntimeState` is already active and work submission is ready.
  </acceptance_criteria>
  <verify>
    <automated>bazel test //crates/bitaxe-api:tests --test_filter='system|asic|mining'</automated>
    <quick>bazel test //crates/bitaxe-api:tests //crates/bitaxe-stratum:tests //crates/bitaxe-asic:tests</quick>
    <full>just test</full>
  </verify>
  <done>System, ASIC, and mining mappers are pure, typed-source backed, and preserve safe blocked/unavailable Phase 6-owned values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement statistics and scoreboard response shapes</name>
  <files>crates/bitaxe-api/BUILD.bazel, crates/bitaxe-api/src/lib.rs, crates/bitaxe-api/src/statistics.rs, crates/bitaxe-api/src/scoreboard.rs, crates/bitaxe-api/fixtures/api/statistics-empty-compatible.json, crates/bitaxe-api/fixtures/api/scoreboard-empty.json</files>
  <read_first>
    - `.planning/phases/05-axeos-api-logs-and-telemetry/05-CONTEXT.md` decisions D-13 and D-14.
    - `.planning/phases/05-axeos-api-logs-and-telemetry/05-UI-SPEC.md` statistics and scoreboard contracts.
    - `reference/esp-miner/main/tasks/statistics_task.h`.
    - `reference/esp-miner/main/http_server/http_server.c` `GET_system_statistics` and `GET_scoreboard`.
    - `reference/esp-miner/main/http_server/axe-os/src/models/ISystemScoreboard.ts`.
  </read_first>
  <behavior>
    - Test 1: Statistics accepts optional `columns` selection, always includes `timestamp`, and returns compatible `currentTimestamp`, `labels`, and `statistics` arrays.
    - Test 2: Empty history produces a compatible empty or minimal series with documented evidence and no fake mining data.
    - Test 3: Empty scoreboard serializes as `[]`; populated entries include only `difficulty`, `job_id`, `extranonce2`, `ntime`, `nonce`, and `version_bits`.
  </behavior>
  <action>
    Create and wire the `statistics` and `scoreboard` modules in `lib.rs` and Bazel, then add statistics and scoreboard mappers per D-13 and D-14. Implement typed labels matching AxeOS expectations, route query planning for optional columns, timestamp handling, and an empty-history representation that is documented in fixtures. Implement scoreboard entries with the upstream server-owned fields only; do not add authoritative `rank` or `since` fields because AxeOS derives display values.
  </action>
  <acceptance_criteria>
    - Fixture tests compare structured JSON for empty statistics and empty scoreboard.
    - `scoreboard.rs` rejects or omits server-side `rank` and `since` fields.
    - Statistics tests prove missing historical samples do not invent hashrate, fan, power, or temperature history.
  </acceptance_criteria>
  <verify>
    <automated>bazel test //crates/bitaxe-api:tests --test_filter='statistics|scoreboard'</automated>
    <quick>bazel test //crates/bitaxe-api:tests</quick>
    <full>just test</full>
  </verify>
  <done>API-04 read endpoint mappers produce compatible statistics and scoreboard JSON without fake history or client-derived fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Runtime state -> public API | Mining, ASIC, and platform state become user-visible JSON. |
| Historical samples -> statistics response | Absent or sparse runtime history must not be fabricated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-05-07 | Tampering | mining state mapper | mitigate | Derive share counters and gates from `MiningRuntimeState`; tests assert blocked state remains blocked. |
| T-05-08 | Information Disclosure | system info response | mitigate | Do not expose secrets from settings; map only public fields needed by AxeOS. |
| T-05-09 | Repudiation | statistics evidence | mitigate | Empty/minimal statistics must be documented in fixtures and parity evidence, not silently treated as live history. |
| T-05-10 | Safety impact | hardware telemetry fields | mitigate | Phase 6-owned voltage/fan/thermal/power fields use safe unavailable/default values and cannot be marked verified. |

High-risk block: stop if any mapper invents live safety telemetry, bypasses mining-loop gates, or duplicates mutable config validation tables.
</threat_model>

<verification>
Quick: `bazel test //crates/bitaxe-api:tests`

Full: `just test`
</verification>

<simplification_pass>
Before completing, remove any response-field mapping that cannot be traced to an upstream field, an existing Rust typed source, or an explicit safe unavailable value. The root-cause goal is to prevent stale duplicate state models.
</simplification_pass>

<success_criteria>
- API-02 system/settings-visible read values have exact wire coverage for the fields touched here.
- API-04 endpoint mappers derive ASIC/statistics/scoreboard/mining values from typed runtime state.
- Empty and blocked states remain client-compatible and evidence-scoped.
</success_criteria>

<output>
After completion, create `.planning/phases/05-axeos-api-logs-and-telemetry/05-03-SUMMARY.md`.
</output>
