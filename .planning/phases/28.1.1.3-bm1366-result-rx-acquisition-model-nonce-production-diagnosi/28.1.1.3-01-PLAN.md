---
phase: 28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase28.1.1.3-rx-acquisition-compare.mjs
  - scripts/phase28.1.1.3-rx-acquisition-compare-test.mjs
  - firmware/bitaxe/src/asic_adapter/uart.rs
  - firmware/bitaxe/src/asic_adapter/production.rs
autonomous: true
requirements: [STR-09, CFG-07, ASIC-11]
user_setup: []
must_haves:
  truths:
    - "A repo-owned RX-acquisition comparator emits first-class partial_frame, clear_rx, rx_idle, rx_complete counts plus result_correlated and fake_pool_submit_observed without raw UART hex or credentials"
    - "When rust has job_tx + result_read_attempt without result_correlated, recommended_investigation is result_rx_acquisition_model (not match_upstream_register_read_poll and not post_max_baud_delay_2000)"
    - "Optional compact asic_rx_acquisition_summary markers (no hex) exist so flood-safe counts can be parsed when raw uart_trace markers are unreliable"
  artifacts:
    - path: "scripts/phase28.1.1.3-rx-acquisition-compare.mjs"
      provides: "Redaction-safe RX-acquisition counts/order report composing 28.1.1.2 parsers"
      contains: "result_rx_acquisition_model"
    - path: "scripts/phase28.1.1.3-rx-acquisition-compare-test.mjs"
      provides: "Fixture tests for D-06 fields, recommender, and redaction sentinels"
      contains: "partial_frame"
  key_links:
    - from: "scripts/phase28.1.1.3-rx-acquisition-compare.mjs"
      to: "scripts/phase28.1.1.2-result-path-compare.mjs"
      via: "compose/reuse lineEvents classifyFrame summarizeLog"
      pattern: "partial_frame|clear_rx|rx_idle|rx_complete"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1.3-2026-07-09T03-02-32
generated_at: 2026-07-09T03:10:00Z
---

<objective>
Ship Wave 0 flood-safe redaction-safe RX-acquisition comparator metrics (and optional compact firmware summary markers) so `partial_frame` / `clear_rx` / `rx_idle` / `rx_complete` are first-class evidence before any firmware A/B or default patch.

Purpose: D-06/D-07 require composing 28.1.1.2 tooling; RESEARCH Pitfall 1 shows raw uart_trace counts are unreliable under phase27 bridge flood. D-05: `result_read_attempt` remains an RX-poll marker, not a TX probe.
Output: `scripts/phase28.1.1.3-rx-acquisition-compare.mjs` + test; optional compact `asic_rx_acquisition_summary` markers in uart/production path.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-CONTEXT.md
@.planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-RESEARCH.md
@.planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-VALIDATION.md
@scripts/phase28.1.1.2-result-path-compare.mjs
@scripts/phase28.1.1.2-result-path-compare-test.mjs

<interfaces>
<!-- Compose 28.1.1.2 — do not invent a parallel taxonomy (D-07). -->

From scripts/phase28.1.1.2-result-path-compare.mjs:
- Events already classified: `partial_frame`, `clear_rx`, `rx_idle`, `rx_result_frame`, `result_read_attempt`, `result_correlated`, `fake_pool_submit`, `job_tx`, …
- CLI shape: `--upstream <log> --rust <log> --out <report.md>`
- Pitfall: recommender still prefers `match_upstream_register_read_poll` when chip_id asymmetry remains, and requires `partialFrameRust >= 5` before `result_rx_acquisition_model` — **must update for 28.1.1.3** so jobs+result_read+!correlate → `result_rx_acquisition_model` without requiring partial_frame≥5, and must NOT recommend falsified register-read as this phase's primary A/B (RESEARCH Pitfall 5).

D-06 required report fields (exact keys):
- `partial_frame_rust` / `partial_frame_upstream` (integer counts; prefer summary markers when present)
- `clear_rx_rust` / `clear_rx_upstream`
- `rx_idle_rust` / `rx_idle_upstream`
- `rx_complete_rust` / `rx_complete_upstream` (map from `rx_result_frame` and/or `asic_rx_acquisition_summary complete=N`)
- `result_read_attempt_rust` / `result_read_attempt_upstream`
- `result_correlated: true|false`
- `fake_pool_submit_observed: true|false`
- `recommended_investigation` (closed enum)
- `raw_bytes_committed: false`
- `credential_contents_read: false`
- `phase30_promotion_input: pending`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement RX-acquisition comparator CLI with first-class D-06 fields</name>
  <files>scripts/phase28.1.1.3-rx-acquisition-compare.mjs</files>
  <read_first>
    - scripts/phase28.1.1.2-result-path-compare.mjs
    - scripts/phase28.1.1.2-result-path-compare-test.mjs
    - .planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-CONTEXT.md (D-05, D-06, D-07, D-08)
    - .planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-RESEARCH.md (Pattern 1, Pitfalls 1/5)
  </read_first>
  <behavior>
    - Missing upstream or rust log → `comparison_status: blocked_safe_prerequisite`
    - Fixture with rust job_tx>0, result_read_attempt>0, result_correlated=false (partial_frame may be 0) → `recommended_investigation: result_rx_acquisition_model`
    - Same gap shape must NOT recommend `match_upstream_register_read_poll` or `post_max_baud_delay_2000`
    - Report Markdown contains D-06 keys and never contains multi-byte hex frame dumps or credential sentinels
    - Lines matching `asic_rx_acquisition_summary idle=N partial=N clear=N complete=N` increment the corresponding counts (flood-safe path)
  </behavior>
  <action>
    Create `scripts/phase28.1.1.3-rx-acquisition-compare.mjs` (Node ESM, stdlib only) accepting `--upstream`, `--rust`, `--out` (per D-07 compose with 28.1.1.2 — reuse/import or carefully duplicate `lineEvents`/`classifyFrame`/`summarizeLog` from `scripts/phase28.1.1.2-result-path-compare.mjs`; do not invent a parallel capture stack).

    Emit a Markdown report with at least these exact fields (D-06):
    - `partial_frame_upstream` / `partial_frame_rust`
    - `clear_rx_upstream` / `clear_rx_rust`
    - `rx_idle_upstream` / `rx_idle_rust`
    - `rx_complete_upstream` / `rx_complete_rust` (from `rx_result_frame` event counts and/or summary `complete=`)
    - `result_read_attempt_upstream` / `result_read_attempt_rust` (D-05: document as RX-poll marker, not TX)
    - `result_correlated: true|false` (true iff rust log contains `asic_production_status=result_correlated` or event `result_correlated`)
    - `fake_pool_submit_observed: true|false`
    - `recommended_investigation:` one of `result_rx_acquisition_model` | `asic_enable_power_sequencing` | `none` | (blocked → `none`). Do **not** emit `match_upstream_register_read_poll` or `post_max_baud_delay_2000` as recommendations from this script (both falsified prior phases; D-02/D-12).
    - `raw_bytes_committed: false`
    - `credential_contents_read: false`
    - `phase30_promotion_input: pending` (D-15)

    Recommender rules for 28.1.1.3 (RESEARCH Pitfall 5 / D-04):
    1. If blocked → `none`
    2. Else if rust `job_tx > 0` AND rust `result_read_attempt > 0` AND rust `result_correlated == false` → `result_rx_acquisition_model` (**do not require** `partial_frame >= 5`)
    3. Else if rust still gapped and enable/power markers missing → `asic_enable_power_sequencing`
    4. Else → `none`
    5. **Never** return `post_max_baud_delay_2000` or `match_upstream_register_read_poll`

    Parse compact summary markers when present:
    `asic_rx_acquisition_summary idle=(\\d+) partial=(\\d+) clear=(\\d+) complete=(\\d+)` — add those integers into the corresponding rust/upstream counts (prefer max of event-count vs summary totals so flood-dropped uart_trace does not zero out evidence).

    Do not write raw UART hex frames into `--out`. Counts and booleans only (D-08).
  </action>
  <acceptance_criteria>
    - `test -f scripts/phase28.1.1.3-rx-acquisition-compare.mjs`
    - `rg -n "partial_frame_rust|clear_rx_rust|rx_idle_rust|rx_complete_rust" scripts/phase28.1.1.3-rx-acquisition-compare.mjs`
    - `rg -n "result_rx_acquisition_model" scripts/phase28.1.1.3-rx-acquisition-compare.mjs`
    - `rg -n "asic_rx_acquisition_summary" scripts/phase28.1.1.3-rx-acquisition-compare.mjs`
    - `rg -n "raw_bytes_committed: false" scripts/phase28.1.1.3-rx-acquisition-compare.mjs`
    - `node scripts/phase28.1.1.3-rx-acquisition-compare.mjs --help >/dev/null`
  </acceptance_criteria>
  <verify>
    <automated>node scripts/phase28.1.1.3-rx-acquisition-compare.mjs --help >/dev/null</automated>
  </verify>
  <done>Comparator CLI exists, documents usage, and encodes D-06 fields plus the post-28.1.1.2 recommender.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fixture tests + optional compact firmware RX summary markers</name>
  <files>scripts/phase28.1.1.3-rx-acquisition-compare-test.mjs, firmware/bitaxe/src/asic_adapter/uart.rs, firmware/bitaxe/src/asic_adapter/production.rs</files>
  <read_first>
    - scripts/phase28.1.1.2-result-path-compare-test.mjs
    - scripts/phase28.1.1.3-rx-acquisition-compare.mjs
    - firmware/bitaxe/src/asic_adapter/uart.rs (try_read_exact / partial_frame / clear_rx)
    - firmware/bitaxe/src/asic_adapter/production.rs (result_read_attempt / Pending)
    - .planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-VALIDATION.md
    - .planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-RESEARCH.md (Pattern 2)
  </read_first>
  <behavior>
    - Test A: rust log with job_tx, many result_read_attempt, zero result_correlated, partial_frame=0 → `recommended_investigation: result_rx_acquisition_model` and D-06 count keys present
    - Test B: rust log with `asic_rx_acquisition_summary idle=10 partial=3 clear=3 complete=1` → report `partial_frame_rust >= 3`, `clear_rx_rust >= 3`, `rx_idle_rust >= 10`, `rx_complete_rust >= 1`
    - Test C: rust log with `asic_production_status=result_correlated` + fake_pool_submit → both booleans true
    - Test D: missing upstream file → `comparison_status: blocked_safe_prerequisite`
    - Test E: report must not match credential/hex dump sentinels (`poolPassword`, `stratum+tcp://`, long hex runs)
    - Test F: gap shape from Test A must not contain `recommended_investigation: match_upstream_register_read_poll` or `post_max_baud_delay_2000`
  </behavior>
  <action>
    1. Create `scripts/phase28.1.1.3-rx-acquisition-compare-test.mjs` using `node:assert` and temp fixture files (same pattern as `phase28.1.1.2-result-path-compare-test.mjs`). Cover STR-09 submit boolean, CFG-07 redaction, ASIC-11 `result_correlated` derivation, and D-06 RX-acquisition fields.

    2. Add optional compact flood-safe firmware markers (RESEARCH Pattern 2 / D-06): in `uart.rs` / `production.rs`, maintain per-poll or rate-limited counters for idle/partial/clear/complete and emit a single log line shaped exactly:
       `asic_rx_acquisition_summary idle=N partial=N clear=N complete=N`
       with **no hex**. Prefer emitting every N polls (e.g. every 50–100 `result_read_attempt`) or on a timer — not per-byte uart_trace. Do not change default RX timeout or clear_rx policy in this plan (Plan 02 owns investigation long-block; Plan 03 owns default patch). Keep existing `partial_frame`/`clear_rx`/`rx_idle` traces if already present; summary is additive.
  </action>
  <acceptance_criteria>
    - `node scripts/phase28.1.1.3-rx-acquisition-compare-test.mjs` exits 0
    - `rg -n "result_rx_acquisition_model" scripts/phase28.1.1.3-rx-acquisition-compare-test.mjs`
    - `rg -n "partial_frame_rust|fake_pool_submit_observed|result_correlated" scripts/phase28.1.1.3-rx-acquisition-compare-test.mjs`
    - `rg -n "asic_rx_acquisition_summary" firmware/bitaxe/src/asic_adapter/uart.rs firmware/bitaxe/src/asic_adapter/production.rs`
  </acceptance_criteria>
  <verify>
    <automated>node scripts/phase28.1.1.3-rx-acquisition-compare-test.mjs</automated>
  </verify>
  <done>Wave 0 unit path is green; compact summary markers exist for flood-safe counts; VALIDATION.md Wave 0 comparator/test boxes can be checked during Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ignored capture logs → committed Markdown report | Untrusted UART/monitor text crosses into redacted evidence |
| Synthetic fixture logs → CI assertions | Test inputs must not teach the script to echo secrets |
| Firmware summary markers → USB serial | Compact counters must never include hex payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28113-01 | I | rx-acquisition-compare.mjs | mitigate | Emit counts/booleans only; assert `raw_bytes_committed: false`; unit test rejects credential/hex dump sentinels (CFG-07) |
| T-28113-02 | S | recommended_investigation / share markers | mitigate | `fake_pool_submit_observed` and `result_correlated` are booleans only; never claim accepted/rejected live share; `phase30_promotion_input: pending` |
| T-28113-03 | I | asic_rx_acquisition_summary | mitigate | Summary line carries integers only — no hex, no credentials, no endpoints |
| T-28113-04 | T | comparator CLI args | accept | Local developer paths only; no network; malformed logs → blocked_safe_prerequisite |
</threat_model>

<verification>
- `node scripts/phase28.1.1.3-rx-acquisition-compare-test.mjs`
- `node scripts/phase28.1.1.3-rx-acquisition-compare.mjs --help >/dev/null`
</verification>

<success_criteria>
- D-06 comparator exists with fixture coverage for RX-acquisition first-class fields.
- Recommender prefers `result_rx_acquisition_model` for the known gap shape and never defaults to falsified register-read or `post_max_baud_delay_2000`.
- Compact no-hex summary markers are available for flood-safe counting.
</success_criteria>

<output>
After completion, create `.planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-01-SUMMARY.md`
</output>
