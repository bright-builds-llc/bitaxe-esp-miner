---
phase: 28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi
plan: 02
type: execute
wave: 2
depends_on:
  - 28.1.1.3-01
files_modified:
  - scripts/phase28.1.1.3-rx-acquisition-diagnostic.sh
  - scripts/phase28.1.1.3-rx-acquisition-diagnostic-test.sh
  - firmware/bitaxe/src/asic_adapter/work_result_investigation.rs
  - firmware/bitaxe/src/live_stratum_runtime.rs
  - firmware/bitaxe/src/asic_adapter.rs
  - .planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-rx-acquisition-redacted.md
  - .planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-ab-upstream_like_long_block_receive-redacted.md
autonomous: true
requirements: [STR-09, CFG-07, ASIC-11]
user_setup:
  - service: ultra205-hardware
    why: "Detector-gated 360s fake-pool A/B requires connected Ultra 205 + local wifi-credentials.json"
    env_vars: []
    dashboard_config: []
must_haves:
  truths:
    - "Diagnostic wrapper dry-run passes without printing credentials"
    - "Baseline RX-acquisition compare runs against existing 28.1.1.1/28.1.1.2 source-work logs when present"
    - "Exactly one hardware A/B uses investigation label upstream_like_long_block_receive with RESULT_WORK_TIMEOUT_MS=10000 and ≥360s capture after detect-ultra205 and verify-reference"
  artifacts:
    - path: "scripts/phase28.1.1.3-rx-acquisition-diagnostic.sh"
      provides: "Orchestrator for baseline compare + forced long-block A/B"
      contains: "upstream_like_long_block_receive"
    - path: "scripts/phase28.1.1.3-rx-acquisition-diagnostic-test.sh"
      provides: "Dry-run / syntax / fixture-path automated checks"
      contains: "dry_run"
    - path: ".planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-rx-acquisition-redacted.md"
      provides: "Committed baseline D-06 RX-acquisition report"
      contains: "recommended_investigation"
    - path: ".planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-ab-upstream_like_long_block_receive-redacted.md"
      provides: "Committed A/B redacted evidence for long-block receive experiment"
      contains: "upstream_like_long_block_receive"
  key_links:
    - from: "scripts/phase28.1.1.3-rx-acquisition-diagnostic.sh"
      to: "scripts/phase28.1.1.1-source-work-aligned-capture.sh"
      via: "reuse source-work fake-pool capture path"
      pattern: "phase28.1.1.1-source-work-aligned-capture"
    - from: "scripts/phase28.1.1.3-rx-acquisition-diagnostic.sh"
      to: "scripts/phase28.1.1.3-rx-acquisition-compare.mjs"
      via: "baseline + A/B report generation"
      pattern: "rx-acquisition-compare"
    - from: "firmware/bitaxe/src/live_stratum_runtime.rs"
      to: "firmware/bitaxe/src/asic_adapter/uart.rs"
      via: "poll_timeout_ms uses RESULT_WORK_TIMEOUT_MS when investigation mode enabled"
      pattern: "upstream_like_long_block_receive|RESULT_WORK_TIMEOUT_MS"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1.3-2026-07-09T03-02-32
generated_at: 2026-07-09T03:10:00Z
---

<objective>
Add the RX-acquisition diagnostic orchestrator, produce a baseline D-06 compare from existing Phase 28.1.1.1/28.1.1.2 source-work logs, and run exactly one detector-gated Ultra 205 A/B for `upstream_like_long_block_receive` using `RESULT_WORK_TIMEOUT_MS=10000` (not raising `MAX_POLL_SLICE` alone).

Purpose: D-09 forces one highest-signal RX-acquisition A/B; RESEARCH locks the lever to long-block because `SOCKET_TIMEOUT_MS=100` clamps slice-only changes. D-13/D-14 require detect/verify/fake-pool/≥360s.
Output: diagnostic scripts + `28.1.1.3-rx-acquisition-redacted.md` + `28.1.1.3-ab-upstream_like_long_block_receive-redacted.md`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-CONTEXT.md
@.planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-RESEARCH.md
@scripts/phase28.1.1.2-result-path-diagnostic.sh
@scripts/phase28.1.1.1-source-work-aligned-capture.sh
@scripts/phase28.1.1-wire-parity-capture.sh
@scripts/phase27-live-hardware-bridge-package.sh
@firmware/bitaxe/src/asic_adapter/work_result_investigation.rs
@firmware/bitaxe/src/live_stratum_runtime.rs
@firmware/bitaxe/src/asic_adapter/uart.rs

<interfaces>
Forced A/B label (locked by CONTEXT + RESEARCH): `upstream_like_long_block_receive`

Why not raise MAX_POLL_SLICE alone (RESEARCH Pitfall 2):
```rust
// live_stratum_runtime.rs
const SOCKET_TIMEOUT_MS: u64 = 100;
let poll_timeout_ms = u64::from(slice_ms).min(SOCKET_TIMEOUT_MS).max(1) as u32;
```

Long-block behavior when investigation enabled:
- Continuous `poll_continuous_asic_result` uses `uart::RESULT_WORK_TIMEOUT_MS` (10000) instead of `slice_ms.min(SOCKET_TIMEOUT_MS)`.
- Log `poll_timeout_ms=10000` on `result_read_attempt` so A/B evidence is greppable.
- Off-by-default via existing `BITAXE_WORK_RESULT_INVESTIGATION` / `--investigation` (D-11: one new mode only).

A/B packaging (D-10/D-11):
- Add `upstream_like_long_block_receive_enabled()` in `work_result_investigation.rs` mirroring `match_upstream_register_read_poll_enabled`.
- Wire through `scripts/phase27-live-hardware-bridge-package.sh --investigation upstream_like_long_block_receive` and `scripts/phase28.1.1-wire-parity-capture.sh --investigation upstream_like_long_block_receive`.
- Do **not** change default bridge behavior in Plan 02 (default patch is Plan 03 only if A/B confirms).

Capture reuse (D-07/D-14):
- Prefer latest valid `scratch/phase28.1.1.1-source-work/**` or prior 28.1.1.2 capture upstream+rust logs for baseline compare.
- A/B may reuse upstream log from that capture and only reflash Rust with the investigation mode + fake-pool (same pattern as 28.1.1.2 diagnostic).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add diagnostic wrapper + long-block investigation mode + dry-run test</name>
  <files>scripts/phase28.1.1.3-rx-acquisition-diagnostic.sh, scripts/phase28.1.1.3-rx-acquisition-diagnostic-test.sh, firmware/bitaxe/src/asic_adapter/work_result_investigation.rs, firmware/bitaxe/src/live_stratum_runtime.rs, firmware/bitaxe/src/asic_adapter.rs</files>
  <read_first>
    - scripts/phase28.1.1.2-result-path-diagnostic.sh
    - scripts/phase28.1.1.2-result-path-diagnostic-test.sh
    - scripts/phase28.1.1-wire-parity-capture.sh
    - scripts/phase27-live-hardware-bridge-package.sh
    - firmware/bitaxe/src/asic_adapter/work_result_investigation.rs
    - firmware/bitaxe/src/live_stratum_runtime.rs (poll_continuous_asic_result ~980–1000)
    - firmware/bitaxe/src/asic_adapter/uart.rs (RESULT_WORK_TIMEOUT_MS)
    - reference/esp-miner/components/asic/asic_common.c (receive_work / SERIAL_rx 10000)
  </read_first>
  <action>
    1. Create `scripts/phase28.1.1.3-rx-acquisition-diagnostic.sh` modeled on `scripts/phase28.1.1.2-result-path-diagnostic.sh` with args: `--port`, `--wifi-credentials`, `--duration-seconds` (≥360), `--capture-dir` (must be gitignored), `--dry-run`.

    Behavior:
    - Always run `just verify-reference` then `just detect-ultra205`; require detected port == `--port` (D-13).
    - Baseline: locate latest valid Phase 28.1.1.1 source-work aligned capture under `scratch/phase28.1.1.1-source-work/` (same find logic as 28.1.1.2 diagnostic). If missing, run `scripts/phase28.1.1.1-source-work-aligned-capture.sh` once (D-14).
    - Run `node scripts/phase28.1.1.3-rx-acquisition-compare.mjs --upstream … --rust … --out $capture_dir/rx-acquisition-redacted.md` and copy to phase dir as `28.1.1.3-rx-acquisition-redacted.md`.
    - **Force first A/B** to investigation mode `upstream_like_long_block_receive` (do not auto-select `match_upstream_register_read_poll` or `post_max_baud_delay_2000`). Run one Rust fake-pool capture via `scripts/phase28.1.1-wire-parity-capture.sh` with `--investigation upstream_like_long_block_receive`, duration ≥360, wall clock budget ≥420s for the agent shell.
    - Re-compare A/B rust log vs baseline upstream; write `28.1.1.3-ab-upstream_like_long_block_receive-redacted.md` with D-06 fields plus `ab_label: upstream_like_long_block_receive`, `poll_timeout_ms_observed:` (expect 10000 in A/B log), `fake_pool_submit_observed`, `result_correlated`, `ab_outcome: improved|unchanged|regressed|blocked_safe_prerequisite`.
    - `ab_outcome: improved` iff `result_correlated: true` and/or `fake_pool_submit_observed: true` relative to baseline gap (D-10).
    - Never print wifi/pool credential file contents. Raw logs stay under ignored `capture-dir` / `scratch/`.

    2. Add investigation-only mode:
       - `pub fn upstream_like_long_block_receive_enabled() -> bool { has_investigation_mode("upstream_like_long_block_receive") }` in `work_result_investigation.rs`.
       - Re-export from `asic_adapter.rs` like other investigation gates.
       - In `poll_continuous_asic_result` (`live_stratum_runtime.rs`): when `upstream_like_long_block_receive_enabled()`, set `poll_timeout_ms = uart::RESULT_WORK_TIMEOUT_MS` (10000), **bypassing** `slice_ms.min(SOCKET_TIMEOUT_MS)`. When disabled, keep existing clamp. Log the chosen `poll_timeout_ms` on the existing `result_read_attempt` line.
       - Keep default (no investigation env) unchanged. Do not raise `MAX_POLL_SLICE_MS` alone. Do not default-on register-read poll. Do not reopen job bytes. Do not reintroduce `post_max_baud_delay_2000` (D-12).

    3. Create `scripts/phase28.1.1.3-rx-acquisition-diagnostic-test.sh` that runs `bash -n` on the diagnostic script and `--dry-run` asserting stdout contains `dry_run_status=passed`, `credential_values_printed=false`, forced label `upstream_like_long_block_receive`, and does not contain `poolPassword` / `wifipass`.
  </action>
  <acceptance_criteria>
    - `test -f scripts/phase28.1.1.3-rx-acquisition-diagnostic.sh`
    - `bash -n scripts/phase28.1.1.3-rx-acquisition-diagnostic.sh`
    - `bash scripts/phase28.1.1.3-rx-acquisition-diagnostic-test.sh` exits 0
    - `rg -n "upstream_like_long_block_receive" scripts/phase28.1.1.3-rx-acquisition-diagnostic.sh`
    - `rg -n "upstream_like_long_block_receive_enabled" firmware/bitaxe/src/asic_adapter/work_result_investigation.rs`
    - `rg -n "RESULT_WORK_TIMEOUT_MS" firmware/bitaxe/src/live_stratum_runtime.rs`
    - Forced A/B label in diagnostic is `upstream_like_long_block_receive` (not `match_upstream_register_read_poll`, not `post_max_baud_delay_2000`)
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase28.1.1.3-rx-acquisition-diagnostic-test.sh</automated>
  </verify>
  <done>Diagnostic wrapper + dry-run test exist; long-block investigation mode is available off-by-default and uses RESULT_WORK_TIMEOUT_MS.</done>
</task>

<task type="auto">
  <name>Task 2: Run baseline compare + hardware A/B upstream_like_long_block_receive</name>
  <files>.planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-rx-acquisition-redacted.md, .planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-ab-upstream_like_long_block_receive-redacted.md</files>
  <read_first>
    - scripts/phase28.1.1.3-rx-acquisition-diagnostic.sh
    - AGENTS.md (Autonomous Ultra 205, Flash And Monitor Timeouts, Evidence Workflow)
    - .planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-final-evidence-redacted.md
  </read_first>
  <action>
    Hardware-first (AGENTS.md / D-13/D-14):
    1. `just detect-ultra205` — require exactly one port + successful board-info.
    2. Confirm `wifi-credentials.json` exists (do not read/print contents). Prefer fake-pool; do not require live `pool-credentials*.json`.
    3. `just verify-reference`.
    4. Run diagnostic with `--duration-seconds 360` (or higher), `--capture-dir` under gitignored `scratch/phase28.1.1.3-rx-acquisition/` or phase `hardware-runs/`, agent wall clock ≥420s.
    5. Promote only redacted Markdown into the phase directory:
       - `28.1.1.3-rx-acquisition-redacted.md`
       - `28.1.1.3-ab-upstream_like_long_block_receive-redacted.md`
    6. Record in A/B report: board `205`, `port_selected: detector_gated`, source/reference commit SHAs, commands as category labels, D-06 counts, `poll_timeout_ms_observed`, `fake_pool_submit_observed`, `result_correlated`, `ab_outcome`.
    7. If detection fails: write blocked evidence with `blocked_safe_prerequisite` and stop — do not invent share outcomes.
    8. If long-block starves fake-pool session (RESEARCH Pitfall 7): keep the same mode label; optionally reduce investigation timeout toward 1000–2000 ms within the same mode (still one mode, still ≫100 ms clamp) and document in A/B report — do not add a second investigation flag (D-11).

    Do not patch default firmware behavior in this task even if A/B improves (Plan 03 owns that). Do not promote checklist rows (D-15). Do not reopen job bytes / post_max_baud / register-read default (D-12).
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-rx-acquisition-redacted.md`
    - `test -f .planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-ab-upstream_like_long_block_receive-redacted.md`
    - `rg -n "partial_frame_rust|clear_rx_rust|rx_idle_rust|rx_complete_rust|recommended_investigation|fake_pool_submit_observed|result_correlated" .planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-rx-acquisition-redacted.md`
    - `rg -n "ab_label: upstream_like_long_block_receive|ab_outcome:" .planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-ab-upstream_like_long_block_receive-redacted.md`
    - `rg -n "poolPassword|stratum\\+tcp://|wifipass" .planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-*-redacted.md` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>rg -n "ab_label: upstream_like_long_block_receive" .planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-ab-upstream_like_long_block_receive-redacted.md</automated>
  </verify>
  <done>Baseline + one long-block A/B redacted artifacts exist with CFG-07-safe content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local wifi-credentials.json → flash tool | Secrets stay runtime-only; scripts must not cat/print them |
| Hardware UART logs → phase Markdown | Only redacted counts/booleans cross into git |
| Investigation firmware package → device | Compile-time mode must stay opt-in until Plan 03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28113-01 | I | diagnostic.sh + redacted md | mitigate | Fake-pool synthetic creds; dry-run asserts `credential_values_printed=false`; redaction sweep before commit (CFG-07) |
| T-28113-02 | I | flash-monitor raw logs | mitigate | Capture dir must be `git check-ignore`; never commit `*.raw.log` / hex UART dumps |
| T-28113-03 | S | ab_outcome / submit markers | mitigate | Boolean `fake_pool_submit_observed` only; no accepted/rejected live-share claim; Phase 30 owns promotion (STR-09 blocker input only) |
| T-28113-04 | T | investigation mode | mitigate | Mode off by default; only enabled via `--investigation upstream_like_long_block_receive` for A/B package |
| T-28113-05 | D | destructive flash | accept | Detector-gated board 205 flash only; no erase/rollback outside phase docs |
| T-28113-06 | D | 10s block on shared task | accept | Investigation-only tradeoff; document fake-pool session health; may shorten within same mode if session dies |
</threat_model>

<verification>
- `bash scripts/phase28.1.1.3-rx-acquisition-diagnostic-test.sh`
- `node scripts/phase28.1.1.3-rx-acquisition-compare-test.mjs`
- Redaction sweep on committed `28.1.1.3-*-redacted.md`
</verification>

<success_criteria>
- Baseline RX-acquisition report committed with D-06 fields.
- Exactly one A/B for `upstream_like_long_block_receive` with ≥360s detector-gated capture evidence (or explicit blocked_safe_prerequisite).
</success_criteria>

<output>
After completion, create `.planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-02-SUMMARY.md`
</output>
