---
phase: 04-stratum-v1-and-first-mining-loop
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - crates/bitaxe-stratum/src/v1.rs
  - crates/bitaxe-stratum/src/v1/fake_pool.rs
  - crates/bitaxe-stratum/src/v1/state.rs
  - crates/bitaxe-stratum/fixtures/v1/fake-pool-transcripts.json
autonomous: true
requirements: [STR-02, STR-04, STR-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 04-2026-06-27T13-17-33
generated_at: 2026-06-27T13:45:00Z
must_haves:
  truths:
    - "Developer can run deterministic fake-pool tests covering subscribe, authorize, notify, set-difficulty, submit accepted, submit rejected, fallback, reconnect, and error behavior."
    - "Pool lifecycle state and share counters update from typed protocol outcomes, not raw JSON or firmware socket side effects."
    - "Runtime state includes API/telemetry-ready fields without implementing Phase 5 HTTP/WebSocket handlers."
  artifacts:
    - path: "crates/bitaxe-stratum/src/v1/fake_pool.rs"
      provides: "Deterministic fake-pool transcript runner"
    - path: "crates/bitaxe-stratum/src/v1/state.rs"
      provides: "Typed pool lifecycle, share counters, rejected reasons, fallback, and work-submission gate state"
    - path: "crates/bitaxe-stratum/fixtures/v1/fake-pool-transcripts.json"
      provides: "Metadata-rich fake-pool scenarios"
  key_links:
    - from: "crates/bitaxe-stratum/src/v1.rs"
      to: "crates/bitaxe-stratum/src/v1/state.rs"
      via: "pub mod state"
      pattern: "pub mod state"
    - from: "crates/bitaxe-stratum/src/v1/fake_pool.rs"
      to: "crates/bitaxe-stratum/src/v1/state.rs"
      via: "typed lifecycle, counter, fallback, hashrate, and mining-activity updates"
      pattern: "MiningRuntimeState|record_accepted_share|MiningActivityStatus"
    - from: "crates/bitaxe-stratum/fixtures/v1/fake-pool-transcripts.json"
      to: "crates/bitaxe-stratum/src/v1/fake_pool.rs"
      via: "fixture-named transcript scenarios covered by fake-pool tests"
      pattern: "subscribe_authorize_notify_submit_accepted|fallback_pool_active"
---

<objective>
Add deterministic fake-pool and runtime-state coverage for Stratum v1 lifecycle behavior.

Purpose: This plan proves STR-02, STR-04, and STR-05 without requiring a real pool or firmware sockets.
Output: A pure fake-pool transcript runner and typed mining runtime state that later firmware/API code can reuse.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-stratum-v1-and-first-mining-loop/04-CONTEXT.md
@.planning/phases/04-stratum-v1-and-first-mining-loop/04-RESEARCH.md
@.planning/phases/04-stratum-v1-and-first-mining-loop/04-VALIDATION.md
@.planning/phases/04-stratum-v1-and-first-mining-loop/04-01-PLAN.md
@reference/esp-miner/components/stratum/stratum_socket.c
@reference/esp-miner/main/system.c
@reference/esp-miner/main/system.h
@reference/esp-miner/main/http_server/system_api_json.c
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runtime state and share counters</name>
  <files>crates/bitaxe-stratum/src/v1/state.rs, crates/bitaxe-stratum/src/v1.rs</files>
  <read_first>
    - crates/bitaxe-stratum/src/v1/messages.rs
    - reference/esp-miner/main/system.h
    - reference/esp-miner/main/http_server/system_api_json.c
    - .planning/phases/04-stratum-v1-and-first-mining-loop/04-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: Default `MiningRuntimeState` has `PoolLifecycleStatus::Disconnected`, zero accepted/rejected shares, `fallback_active == false`, and `WorkSubmissionGate::Blocked`.
    - Test 2: Calling `record_accepted_share(ShareDifficulty::new(42.0))` increments accepted shares and records best share difficulty `42.0`.
    - Test 3: Calling `record_rejected_share("low difficulty")` increments rejected shares and stores the rejected reason.
    - Test 4: Setting fallback active changes the status field used by later API/telemetry surfaces.
    - Test 5: Runtime state records `HashrateInputs` and exposes mining activity statuses `Paused`, `Active`, and `SafeBlocked`.
  </behavior>
  <action>Create `src/v1/state.rs` with `PoolLifecycleStatus::{Disconnected, Connecting, Subscribed, Authorized, Active, Reconnecting, FallbackActive, Error}`, `WorkSubmissionGate::{Blocked, Ready}`, `MiningActivityStatus::{Paused, Active, SafeBlocked}`, `HashrateInputs { hashes_done: u64, elapsed_ms: u64, rolling_hashrate_hs: f64 }`, `ShareCounters { accepted: u64, rejected: u64, rejected_reasons: Vec<String>, maybe_best_difficulty: Option<ShareDifficulty> }`, `ShareDifficulty(f64)`, and `MiningRuntimeState { lifecycle, counters, maybe_pool_difficulty: Option<PoolDifficulty>, fallback_active: bool, work_submission: WorkSubmissionGate, hashrate_inputs: HashrateInputs, mining_activity: MiningActivityStatus }`. Implement methods `record_accepted_share`, `record_rejected_share`, `set_lifecycle`, `set_pool_difficulty`, `set_fallback_active`, `record_hashrate_inputs`, `set_mining_activity`, and `allow_work_submission`. Use `maybe_` naming for optional internal fields. Export `state` from `v1.rs`.</action>
  <verify>
    <automated>cargo test -p bitaxe-stratum runtime_state --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "enum PoolLifecycleStatus|enum WorkSubmissionGate|enum MiningActivityStatus|struct HashrateInputs|struct ShareCounters|struct MiningRuntimeState|maybe_pool_difficulty|maybe_best_difficulty" crates/bitaxe-stratum/src/v1/state.rs` finds all state surfaces.
    - `rg -n "record_accepted_share|record_rejected_share|set_fallback_active|record_hashrate_inputs|set_mining_activity|allow_work_submission" crates/bitaxe-stratum/src/v1/state.rs` finds mutation methods.
    - `rg -n "HashrateInputs|MiningActivityStatus|SafeBlocked|Paused|Active" crates/bitaxe-stratum/src/v1/state.rs` finds runtime telemetry and activity status fields.
    - `rg -n "pub mod state" crates/bitaxe-stratum/src/v1.rs` finds the export.
    - `cargo test -p bitaxe-stratum runtime_state --all-features` passes.
  </acceptance_criteria>
  <done>Runtime mining state and counters are typed and unit-tested for Phase 5 reuse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic fake-pool transcript runner</name>
  <files>crates/bitaxe-stratum/src/v1/fake_pool.rs, crates/bitaxe-stratum/src/v1.rs, crates/bitaxe-stratum/fixtures/v1/fake-pool-transcripts.json</files>
  <read_first>
    - crates/bitaxe-stratum/src/v1/messages.rs
    - crates/bitaxe-stratum/src/v1/state.rs
    - reference/esp-miner/components/stratum/stratum_socket.c
    - reference/esp-miner/components/stratum/test/test_stratum_json.c
    - .planning/phases/04-stratum-v1-and-first-mining-loop/04-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: A transcript requiring subscribe, authorize, set-difficulty, notify, and accepted submit finishes with lifecycle `Active`, one accepted share, zero rejected shares.
    - Test 2: A rejected submit transcript finishes with one rejected share and rejected reason text.
    - Test 3: A disconnect transcript sets lifecycle `Reconnecting`.
    - Test 4: A fallback transcript sets `fallback_active == true` and lifecycle `FallbackActive`.
    - Test 5: An unexpected client message returns an error instead of advancing the transcript.
  </behavior>
  <action>Create `src/v1/fake_pool.rs` with `FakePoolEvent::{ExpectClient(StratumV1ClientMessage), SendServer(StratumV1ServerMessage), Disconnect, Timeout}` and `FakePoolTranscript { events: Vec<FakePoolEvent> }`. Implement `run(&self, client_messages: &[StratumV1ClientMessage]) -> Result<MiningRuntimeState, StratumV1Error>` that processes expected sends, applies `SetDifficulty`, `Notify`, `Response(success=true)`, `Response(success=false)`, `ClientReconnect`, `Disconnect`, and `Timeout` to `MiningRuntimeState`, and fails on unexpected client messages. Export `fake_pool` from `v1.rs`. Create `fixtures/v1/fake-pool-transcripts.json` with provenance fields and cases named `subscribe_authorize_notify_submit_accepted`, `submit_rejected`, `disconnect_reconnect`, and `fallback_pool_active`.</action>
  <verify>
    <automated>cargo test -p bitaxe-stratum fake_pool --all-features && cargo test -p bitaxe-stratum pool_lifecycle --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "enum FakePoolEvent|struct FakePoolTranscript|fn run" crates/bitaxe-stratum/src/v1/fake_pool.rs` finds transcript types.
    - `rg -n "Disconnect|Timeout|FallbackActive|Reconnecting|record_accepted_share|record_rejected_share" crates/bitaxe-stratum/src/v1/fake_pool.rs` finds lifecycle handling.
    - `rg -n "pub mod fake_pool" crates/bitaxe-stratum/src/v1.rs` finds the export.
    - `rg -n "subscribe_authorize_notify_submit_accepted|submit_rejected|disconnect_reconnect|fallback_pool_active|reference_commit|STR-002|STR-004" crates/bitaxe-stratum/fixtures/v1/fake-pool-transcripts.json` finds fake-pool fixture cases and provenance.
    - `cargo test -p bitaxe-stratum fake_pool --all-features` passes.
  </acceptance_criteria>
  <done>Fake-pool transcripts deterministically cover core pool lifecycle and counter behavior without live sockets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Fake-pool transcript -> runtime state | Test transcripts simulate untrusted pool behavior and must not bypass parser/state validation. |
| Runtime state -> future API/telemetry | Phase 4 creates typed fields but does not expose HTTP/WebSocket surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-04-04 | Tampering | Fake-pool transcript runner | mitigate | Fail on unexpected client messages and malformed lifecycle ordering instead of advancing state silently. |
| T-04-05 | Repudiation | Share counters | mitigate | Accepted/rejected counters update only from typed response outcomes and retain rejected reasons for later evidence/API review. |
| T-04-06 | Information Disclosure | Fixture transcripts | mitigate | Use synthetic usernames/passwords and no real pool credentials in fixtures. |
</threat_model>

<verification>
Run after this plan:

```bash
cargo test -p bitaxe-stratum runtime_state --all-features
cargo test -p bitaxe-stratum fake_pool --all-features
cargo test -p bitaxe-stratum pool_lifecycle --all-features
```
</verification>

<success_criteria>
- Fake-pool tests cover accepted, rejected, reconnect, fallback, and error paths.
- Runtime state includes counters and lifecycle fields needed by later API/telemetry work.
- No firmware sockets or live pool calls are introduced.
</success_criteria>
