---
phase: 04-stratum-v1-and-first-mining-loop
plan: "04"
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - crates/bitaxe-stratum/src/v1.rs
  - crates/bitaxe-stratum/src/v1/mining_loop.rs
  - firmware/bitaxe/src/main.rs
  - firmware/bitaxe/src/asic_adapter/status.rs
  - docs/parity/checklist.md
  - docs/parity/evidence/phase-04-stratum-v1-mining-loop.md
autonomous: true
requirements: [STR-01, STR-02, STR-03, STR-04, STR-05, STR-06, STR-07]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 04-2026-06-27T13-17-33
generated_at: 2026-06-27T13:45:00Z
must_haves:
  truths:
    - "Firmware-visible mining-loop status remains fail-closed unless explicit ASIC/safety/hardware-evidence gates allow work submission."
    - "Phase 4 parity checklist updates distinguish pure/fake-pool evidence from live hardware mining proof."
    - "A Phase 4 evidence file records the run or skipped status for Ultra 205 mining smoke/soak criteria."
  artifacts:
    - path: "crates/bitaxe-stratum/src/v1/mining_loop.rs"
      provides: "Pure mining-loop gate and status decisions"
    - path: "firmware/bitaxe/src/asic_adapter/status.rs"
      provides: "Visible status logging for mining-loop blocked/ready state"
    - path: "docs/parity/evidence/phase-04-stratum-v1-mining-loop.md"
      provides: "Hardware smoke/soak criteria and evidence conclusion"
  key_links:
    - from: "crates/bitaxe-stratum/src/v1/mining_loop.rs"
      to: "firmware/bitaxe/src/main.rs"
      via: "firmware publishes the same blocked reason exposed by the pure gate"
      pattern: "hardware_evidence_ack_missing"
    - from: "crates/bitaxe-stratum/src/v1/mining_loop.rs"
      to: "crates/bitaxe-asic/src/bm1366/work.rs"
      via: "ready gates may emit typed BM1366 work dispatch, never raw frame bytes"
      pattern: "Bm1366WorkFields|Bm1366Command"
    - from: "docs/parity/checklist.md"
      to: "docs/parity/evidence/phase-04-stratum-v1-mining-loop.md"
      via: "STR-001 through STR-007 and STAT-004 rows link to evidence conclusion"
      pattern: "STR-00[1-7]|STAT-004|phase-04-stratum-v1-mining-loop"
---

<objective>
Connect Phase 4 pure Stratum behavior to a fail-closed first mining-loop gate and parity evidence.

Purpose: This plan satisfies STR-06 and STR-07 without overclaiming live mining parity when hardware evidence is absent.
Output: A pure mining-loop gate, visible firmware status, checklist/evidence updates, and full verification commands.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-stratum-v1-and-first-mining-loop/04-CONTEXT.md
@.planning/phases/04-stratum-v1-and-first-mining-loop/04-RESEARCH.md
@.planning/phases/04-stratum-v1-and-first-mining-loop/04-VALIDATION.md
@crates/bitaxe-asic/src/bm1366/init_plan.rs
@crates/bitaxe-asic/src/bm1366/observation.rs
@firmware/bitaxe/src/main.rs
@firmware/bitaxe/src/asic_adapter/status.rs
@docs/parity/checklist.md
@docs/adr/0012-parity-verification-evidence.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fail-closed mining-loop gate</name>
  <files>crates/bitaxe-stratum/src/v1/mining_loop.rs, crates/bitaxe-stratum/src/v1.rs</files>
  <read_first>
    - crates/bitaxe-stratum/src/v1/state.rs
    - crates/bitaxe-stratum/src/v1/mining.rs
    - crates/bitaxe-asic/src/bm1366/init_plan.rs
    - crates/bitaxe-asic/src/bm1366/observation.rs
    - .planning/phases/04-stratum-v1-and-first-mining-loop/04-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `MiningLoopGate::default().decision()` returns blocked reason `hardware_evidence_ack_missing`.
    - Test 2: A gate with initialized ASIC but missing safety evidence returns blocked reason `safety_preflight_evidence_missing`.
    - Test 3: A gate with initialized ASIC, safety evidence, and hardware evidence ack returns `MiningLoopDecision::Ready`.
    - Test 4: A blocked decision leaves `MiningRuntimeState.work_submission == WorkSubmissionGate::Blocked` and `MiningActivityStatus::SafeBlocked`.
    - Test 5: A ready gate can turn typed fake-pool/mining queue state into a typed BM1366 dispatch plan without creating raw ASIC frame bytes.
    - Test 6: A typed BM1366 nonce/result can be evaluated against valid-job tracking and converted into a `ShareSubmission` only when the job remains valid.
  </behavior>
  <action>Create `src/v1/mining_loop.rs` with `MiningLoopGate { asic_initialized: bool, safety_evidence: bool, hardware_evidence_ack: bool }`, `MiningLoopDecision::{Ready, Blocked { reason: &'static str }}`, constants `HARDWARE_EVIDENCE_ACK_MISSING`, `ASIC_INITIALIZED_GATE_MISSING`, and `SAFETY_PREFLIGHT_EVIDENCE_MISSING`, and method `decision(&self) -> MiningLoopDecision`. Add a guarded pure adapter path such as `GuardedMiningLoopInputs` and `GuardedMiningLoopPlan` that wires pool/config defaults, typed Stratum fake-pool or notify state, `MiningWorkQueue`, `MiningRuntimeState`, `Bm1366WorkFields` or `Bm1366Command`, and optional `Bm1366NonceResult` handling. The adapter must return no dispatch and set `MiningActivityStatus::SafeBlocked` unless `MiningLoopGate::decision()` is `Ready`; when ready, it may emit a typed BM1366 work dispatch and convert valid typed nonce results into `ShareSubmission`. Implement `apply_to_state(&self, state: &mut MiningRuntimeState)` so ready gates call `allow_work_submission()` and set activity `Active`, while blocked gates set lifecycle to `PoolLifecycleStatus::Error`, activity `SafeBlocked`, and preserve blocked work submission. Export `mining_loop` from `v1.rs`.</action>
  <verify>
    <automated>cargo test -p bitaxe-stratum mining_loop --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "struct MiningLoopGate|enum MiningLoopDecision|HARDWARE_EVIDENCE_ACK_MISSING|SAFETY_PREFLIGHT_EVIDENCE_MISSING|ASIC_INITIALIZED_GATE_MISSING" crates/bitaxe-stratum/src/v1/mining_loop.rs` finds gate surfaces.
    - `rg -n "fn decision|fn apply_to_state|WorkSubmissionGate::Blocked|allow_work_submission|MiningActivityStatus::SafeBlocked" crates/bitaxe-stratum/src/v1/mining_loop.rs` finds decision behavior.
    - `rg -n "GuardedMiningLoop|Bm1366Command|Bm1366WorkFields|Bm1366NonceResult|ShareSubmission|MiningWorkQueue" crates/bitaxe-stratum/src/v1/mining_loop.rs` finds the guarded config/Stratum/BM1366/state adapter path.
    - `rg -n "pub mod mining_loop" crates/bitaxe-stratum/src/v1.rs` finds export.
    - `cargo test -p bitaxe-stratum mining_loop --all-features` passes.
  </acceptance_criteria>
  <done>Mining-loop work submission has a pure fail-closed gate that cannot be unlocked by parser/fake-pool tests alone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add visible firmware status without enabling live mining</name>
  <files>firmware/bitaxe/src/asic_adapter/status.rs, firmware/bitaxe/src/main.rs</files>
  <read_first>
    - firmware/bitaxe/src/main.rs
    - firmware/bitaxe/src/asic_adapter/status.rs
    - crates/bitaxe-stratum/src/v1/mining_loop.rs
    - docs/adr/0012-parity-verification-evidence.md
  </read_first>
  <behavior>
    - Test 1: Firmware source contains a visible log line `mining_loop_status=blocked reason=hardware_evidence_ack_missing work_submission=disabled`.
    - Test 2: Firmware does not call a live pool socket or BM1366 work submission path from `main.rs`.
  </behavior>
  <action>Add a function `publish_mining_loop_blocked_status(reason: &'static str)` to `firmware/bitaxe/src/asic_adapter/status.rs` that logs exactly `mining_loop_status=blocked reason={reason} work_submission=disabled`. Call it from `main.rs` after ASIC boot gate status using the reason constant string `hardware_evidence_ack_missing`. Do not add live socket connection, pool credentials, FreeRTOS mining task, or BM1366 work submission in this task.</action>
  <verify>
    <automated>cargo test -p bitaxe-stratum mining_loop --all-features && cargo build -p bitaxe-firmware --target xtensa-esp32s3-espidf</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "publish_mining_loop_blocked_status|mining_loop_status=blocked reason=\\{reason\\} work_submission=disabled" firmware/bitaxe/src/asic_adapter/status.rs` finds status function and log format.
    - `rg -n "hardware_evidence_ack_missing|publish_mining_loop_blocked_status" firmware/bitaxe/src/main.rs` finds the boot-time blocked status call.
    - `! rg -n "esp_transport|STRATUM_V1|submit_share|SendDiagnosticWork" firmware/bitaxe/src/main.rs` succeeds, proving this task did not add live mining dispatch to `main.rs`.
  </acceptance_criteria>
  <done>Firmware exposes a clear mining-loop blocked status while keeping live work submission disabled.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update parity checklist and Phase 4 evidence</name>
  <files>docs/parity/checklist.md, docs/parity/evidence/phase-04-stratum-v1-mining-loop.md</files>
  <read_first>
    - docs/parity/checklist.md
    - docs/adr/0012-parity-verification-evidence.md
    - .planning/phases/04-stratum-v1-and-first-mining-loop/04-VALIDATION.md
    - .planning/phases/04-stratum-v1-and-first-mining-loop/04-CONTEXT.md
  </read_first>
  <behavior>
    - Evidence file records fake-pool/unit evidence as run and live hardware mining smoke/soak as not run unless explicit evidence exists.
    - Checklist STR-001 through STR-004 move from `not-started` to at least `implemented` with unit/golden/fake-pool evidence.
    - Checklist STR-006 and STR-007 do not claim `verified` unless live hardware evidence exists.
  </behavior>
  <action>Create `docs/parity/evidence/phase-04-stratum-v1-mining-loop.md` with sections `Scope`, `Commands`, `Pure/Fake-Pool Evidence`, `Hardware Smoke`, `Soak Criteria`, `Conclusion`, and `Residual Risk`. Include exact command slots for `cargo test -p bitaxe-stratum --all-features`, `bazel test //crates/bitaxe-stratum:tests`, `just parity`, and `just verify-reference`. Set hardware smoke conclusion to `not run - hardware evidence pending` unless this plan actually runs hardware. Update `docs/parity/checklist.md` rows `STR-001` through `STR-007` and `STAT-004` with implementation pointers into `crates/bitaxe-stratum` and evidence pointing at the new evidence file. Keep any live mining row below `verified` if no hardware smoke was run.</action>
  <verify>
    <automated>rg -n "STR-001|STR-002|STR-003|STR-004|STR-006|STAT-004|phase-04-stratum-v1-mining-loop" docs/parity/checklist.md docs/parity/evidence/phase-04-stratum-v1-mining-loop.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/parity/evidence/phase-04-stratum-v1-mining-loop.md` succeeds.
    - `rg -n "Hardware Smoke|not run - hardware evidence pending|cargo test -p bitaxe-stratum --all-features|bazel test //crates/bitaxe-stratum:tests|just parity|just verify-reference" docs/parity/evidence/phase-04-stratum-v1-mining-loop.md` finds required evidence sections and commands.
    - `rg -n "STR-001|STR-002|STR-003|STR-004|STR-005|STR-006|STR-007|STAT-004" docs/parity/checklist.md` finds updated rows.
    - `! rg -n "STR-00[6-7].*verified" docs/parity/checklist.md` succeeds unless a live hardware-smoke section in the evidence file records an actual run.
  </acceptance_criteria>
  <done>Parity checklist and evidence record Phase 4 pure/fake-pool progress without overclaiming live mining verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Pure mining loop -> firmware status | Pure gate decisions become visible firmware logs without enabling sockets or ASIC work. |
| Implementation evidence -> parity checklist | Checklist status must distinguish unit/fake-pool proof from hardware mining proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-04-10 | Elevation of Privilege | Mining-loop gate | mitigate | Work submission is blocked unless ASIC initialized, safety evidence present, and hardware evidence acknowledged. |
| T-04-11 | Repudiation | Parity checklist | mitigate | Evidence file records exact commands and explicitly states hardware smoke/soak status before checklist status changes. |
| T-04-12 | Information Disclosure | Hardware evidence | mitigate | Redact real pool usernames/passwords and avoid committing secrets in smoke/soak logs. |
</threat_model>

<verification>
Run after this plan:

```bash
cargo test -p bitaxe-stratum --all-features
cargo test -p bitaxe-asic --all-features
bazel test //crates/bitaxe-stratum:tests
just verify-reference
just parity
```

Before any final commit in this Rust repo, also run:

```bash
cargo fmt --all
cargo clippy --all-targets --all-features -- -D warnings
cargo build --all-targets --all-features
cargo test --all-features
```
</verification>

<success_criteria>
- Mining-loop work submission remains fail-closed by default.
- Firmware logs an explicit blocked status.
- Parity evidence distinguishes pure/fake-pool proof from live mining smoke/soak proof.
- STR-01 through STR-07 and STAT-004 have implementation/evidence pointers without overclaiming verified live mining parity.
</success_criteria>
