---
phase: 04-stratum-v1-and-first-mining-loop
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/bitaxe-stratum/Cargo.toml
  - crates/bitaxe-stratum/BUILD.bazel
  - crates/bitaxe-stratum/src/lib.rs
  - crates/bitaxe-stratum/src/error.rs
  - crates/bitaxe-stratum/src/jsonrpc.rs
  - crates/bitaxe-stratum/src/v1.rs
  - crates/bitaxe-stratum/src/v1/messages.rs
  - crates/bitaxe-stratum/fixtures/v1/protocol-cases.json
autonomous: true
requirements: [STR-01]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 04-2026-06-27T13-17-33
generated_at: 2026-06-27T13:45:00Z
must_haves:
  truths:
    - "Developer can run Stratum v1 parser and serializer tests covering subscribe, authorize, configure, suggest-difficulty, extranonce-subscribe, notify, set-difficulty, set-extranonce, set-version-mask, submit, result, and error behavior."
    - "Raw JSON is parsed at the crate boundary into typed domain values before it mutates mining state."
    - "Reference-derived protocol fixtures include source path, reference commit, license posture, derivation, and STR checklist IDs."
  artifacts:
    - path: "crates/bitaxe-stratum/src/v1.rs"
      provides: "Stratum v1 module facade using v1.rs plus v1/ child modules"
    - path: "crates/bitaxe-stratum/src/v1/messages.rs"
      provides: "Typed Stratum v1 client and server message parsing/serialization"
    - path: "crates/bitaxe-stratum/fixtures/v1/protocol-cases.json"
      provides: "Metadata-rich protocol fixture cases"
  key_links:
    - from: "crates/bitaxe-stratum/src/lib.rs"
      to: "crates/bitaxe-stratum/src/v1.rs"
      via: "pub mod v1"
      pattern: "pub mod v1"
    - from: "crates/bitaxe-stratum/src/v1/messages.rs"
      to: "crates/bitaxe-stratum/fixtures/v1/protocol-cases.json"
      via: "unit tests cover fixture-described methods"
      pattern: "mining.notify|mining.submit|mining.set_difficulty"
---

<objective>
Replace the Phase 1 deferred Stratum marker with a typed Stratum v1 protocol core.

Purpose: This plan creates the parse/serialize foundation for STR-01 while preserving the pure-core boundary. Firmware sockets and mining-loop side effects stay out of scope until later plans.
Output: `bitaxe-stratum` owns JSON-RPC IDs, client messages, server messages, parser errors, serializers, and protocol fixture metadata.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-stratum-v1-and-first-mining-loop/04-CONTEXT.md
@.planning/phases/04-stratum-v1-and-first-mining-loop/04-RESEARCH.md
@.planning/phases/04-stratum-v1-and-first-mining-loop/04-VALIDATION.md
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@reference/esp-miner/components/stratum/include/stratum_api.h
@reference/esp-miner/components/stratum/stratum_api.c
@reference/esp-miner/components/stratum/test/test_stratum_json.c
@docs/adr/0008-reference-breadcrumb-comments.md
@docs/adr/0013-mit-first-with-gpl-guardrails.md

<interfaces>
Upstream methods that must be represented:
```text
mining.subscribe
mining.authorize
mining.configure
mining.suggest_difficulty
mining.extranonce.subscribe
mining.notify
mining.set_difficulty
mining.set_extranonce
mining.set_version_mask
mining.submit
client.reconnect
client.show_message
client.get_version
mining.ping
```

Client serialization target examples:
```json
{"id":1,"method":"mining.subscribe","params":["bitaxe/ultra/205"]}
{"id":2,"method":"mining.authorize","params":["user","x"]}
{"id":3,"method":"mining.suggest_difficulty","params":[1000]}
{"id":4,"method":"mining.extranonce.subscribe","params":[]}
{"id":5,"method":"mining.submit","params":["user","job","00000000","647025b5","12345678","00000000"]}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish typed Stratum v1 module and error surface</name>
  <files>crates/bitaxe-stratum/Cargo.toml, crates/bitaxe-stratum/BUILD.bazel, crates/bitaxe-stratum/src/lib.rs, crates/bitaxe-stratum/src/error.rs, crates/bitaxe-stratum/src/jsonrpc.rs, crates/bitaxe-stratum/src/v1.rs, crates/bitaxe-stratum/src/v1/messages.rs, crates/bitaxe-stratum/fixtures/v1/protocol-cases.json</files>
  <read_first>
    - crates/bitaxe-stratum/Cargo.toml
    - crates/bitaxe-stratum/BUILD.bazel
    - crates/bitaxe-stratum/src/lib.rs
    - .planning/phases/04-stratum-v1-and-first-mining-loop/04-CONTEXT.md
    - .planning/phases/04-stratum-v1-and-first-mining-loop/04-RESEARCH.md
    - docs/adr/0008-reference-breadcrumb-comments.md
    - docs/adr/0013-mit-first-with-gpl-guardrails.md
  </read_first>
  <behavior>
    - Test 1: `StratumRuntimeStatus::ActiveV1Core` replaces the deferred-only runtime status.
    - Test 2: `StratumV1Error::UnknownMethod { method: "mining.unknown".to_owned() }` renders a string containing `unknown Stratum v1 method`.
    - Test 3: `StratumRequestId::new(7).raw()` returns `7`.
  </behavior>
  <action>Add `thiserror.workspace = true`, `serde.workspace = true`, and `serde_json.workspace = true` to `crates/bitaxe-stratum/Cargo.toml`. Create `src/error.rs` with `#[derive(thiserror::Error)] pub enum StratumV1Error` containing at least `InvalidJson`, `MissingField(&'static str)`, `InvalidField { field: &'static str, reason: &'static str }`, `UnknownMethod { method: String }`, `InvalidParams { method: &'static str }`, and `SerializationFailed`. Create `src/jsonrpc.rs` with `#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)] pub struct StratumRequestId(u64)` and methods `new(raw: u64) -> Self` and `raw(self) -> u64`. Create `src/v1.rs` with module breadcrumbs to `reference/esp-miner/components/stratum/stratum_api.c`, `include/stratum_api.h`, and checklist row `STR-001`; export `pub mod messages;`. Create a minimal `src/v1/messages.rs` with Stratum v1 breadcrumb comments and a public placeholder type so the module graph compiles before Task 2 fills in the protocol model. Update `src/lib.rs` to export `pub mod error;`, `pub mod jsonrpc;`, `pub mod v1;`, re-export `StratumV1Error`, and change the runtime enum variant from `DeferredUntilPhase4` to `ActiveV1Core`. Update `BUILD.bazel` to include the new source files, `compile_data = glob(["fixtures/**"])`, deps `@crates//:serde`, `@crates//:serde_json`, and `@crates//:thiserror`. Create `fixtures/v1/protocol-cases.json` with top-level fields `checklist_ids`, `source_file`, `source_behavior`, `reference_commit`, `license_posture`, `derivation`, and `cases`; include cases for `mining.subscribe`, `mining.authorize`, and `mining.notify`. Do not create `src/v1/mod.rs`.</action>
  <verify>
    <automated>cargo test -p bitaxe-stratum stratum_v1_contract --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub mod error|pub mod jsonrpc|pub mod v1|pub use error::StratumV1Error" crates/bitaxe-stratum/src/lib.rs` finds all exports.
    - `rg -n "ActiveV1Core" crates/bitaxe-stratum/src/lib.rs` finds the active status variant.
    - `rg -n "InvalidJson|MissingField|InvalidField|UnknownMethod|InvalidParams|SerializationFailed" crates/bitaxe-stratum/src/error.rs` finds all error variants.
    - `rg -n "struct StratumRequestId|fn raw" crates/bitaxe-stratum/src/jsonrpc.rs` finds the ID type and raw accessor.
    - `rg -n "pub mod messages|reference/esp-miner/components/stratum/stratum_api.c|STR-001" crates/bitaxe-stratum/src/v1.rs` finds the module and breadcrumbs.
    - `test -f crates/bitaxe-stratum/src/v1/messages.rs` succeeds before `cargo test` runs.
    - `rg -n "thiserror\\.workspace = true|serde\\.workspace = true|serde_json\\.workspace = true" crates/bitaxe-stratum/Cargo.toml` finds the workspace deps.
    - `rg -n "compile_data = glob\\(\\[\"fixtures/\\*\\*\"\\]\\)|@crates//:serde_json|@crates//:thiserror" crates/bitaxe-stratum/BUILD.bazel` finds Bazel wiring.
    - `rg -n "checklist_ids|source_file|source_behavior|reference_commit|license_posture|derivation|mining.notify" crates/bitaxe-stratum/fixtures/v1/protocol-cases.json` finds fixture provenance and notify case.
    - `test ! -e crates/bitaxe-stratum/src/v1/mod.rs` succeeds.
  </acceptance_criteria>
  <done>The Stratum crate has typed module/error/ID scaffolding, dependency wiring, and protocol fixture metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Stratum v1 message parsing and serialization</name>
  <files>crates/bitaxe-stratum/src/v1/messages.rs, crates/bitaxe-stratum/src/v1.rs, crates/bitaxe-stratum/BUILD.bazel, crates/bitaxe-stratum/fixtures/v1/protocol-cases.json</files>
  <read_first>
    - crates/bitaxe-stratum/src/error.rs
    - crates/bitaxe-stratum/src/jsonrpc.rs
    - crates/bitaxe-stratum/src/v1.rs
    - crates/bitaxe-stratum/fixtures/v1/protocol-cases.json
    - reference/esp-miner/components/stratum/include/stratum_api.h
    - reference/esp-miner/components/stratum/stratum_api.c
    - reference/esp-miner/components/stratum/test/test_stratum_json.c
  </read_first>
  <behavior>
    - Test 1: `StratumV1ClientMessage::subscribe(StratumRequestId::new(1), "ultra", "205").to_json_line()` returns a JSON line containing `"method":"mining.subscribe"` and `bitaxe/ultra/205`.
    - Test 2: `StratumV1ClientMessage::authorize(...).to_json_line()` returns a JSON line containing `"method":"mining.authorize"` and the username/password params.
    - Test 3: Parsing `{"id":null,"method":"mining.set_difficulty","params":[4294967295]}` yields `StratumV1ServerMessage::SetDifficulty`.
    - Test 4: Parsing the upstream self-test `mining.notify` JSON yields a `MiningNotify` with `job_id == "0"`, `clean_jobs == true`, `version == 0x20000004`, `nbits == 0x1705ae3a`, and `ntime == 0x647025b5`.
    - Test 5: Parsing `{"id":1,"result":true,"error":null}` yields a successful response, while parsing an unknown method returns `StratumV1Error::UnknownMethod`.
  </behavior>
  <action>Create `src/v1/messages.rs`. Define `StratumV1ClientMessage` variants for `Subscribe { id, user_agent }`, `Authorize { id, username, password }`, `ConfigureVersionRolling { id, mask }`, `SuggestDifficulty { id, difficulty }`, `ExtranonceSubscribe { id }`, `Pong { id }`, `SendVersion { id, version }`, and `SubmitShare { id, username, job_id, extranonce2, ntime, nonce, version_bits }`. Add constructors `subscribe`, `authorize`, `suggest_difficulty`, `extranonce_subscribe`, and `submit_share`, and a `to_json_line(&self) -> Result<String, StratumV1Error>` method that matches upstream method names and appends `\\n`. Define `StratumV1ServerMessage` variants for `Notify(MiningNotify)`, `SetDifficulty(PoolDifficulty)`, `SetExtranonce(ExtranonceAssignment)`, `SetVersionMask(VersionMask)`, `Response(StratumResponse)`, `ClientReconnect`, `ClientShowMessage(String)`, `ClientGetVersion`, and `Ping { maybe_id: Option<StratumRequestId> }`. Define domain types `MiningNotify`, `PoolDifficulty`, `ExtranonceAssignment`, `VersionMask`, and `StratumResponse`. Implement `parse_server_message(input: &str) -> Result<StratumV1ServerMessage, StratumV1Error>` using `serde_json::Value`, parsing hex fields `version`, `nbits`, and `ntime` with `u32::from_str_radix(..., 16)`, capping extranonce2 length at `32`, and rejecting malformed method params with `InvalidParams`. Update `v1.rs` to export `messages`. Update `BUILD.bazel` `srcs` with `src/v1/messages.rs`. Extend `protocol-cases.json` with cases for `mining.set_difficulty`, `mining.set_extranonce`, `mining.set_version_mask`, `mining.submit`, subscribe result, success result, and error result.</action>
  <verify>
    <automated>cargo test -p bitaxe-stratum stratum_v1_protocol --all-features && bazel test //crates/bitaxe-stratum:tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "enum StratumV1ClientMessage|enum StratumV1ServerMessage|struct MiningNotify|fn parse_server_message" crates/bitaxe-stratum/src/v1/messages.rs` finds all protocol surfaces.
    - `rg -n "mining\\.subscribe|mining\\.authorize|mining\\.suggest_difficulty|mining\\.extranonce\\.subscribe|mining\\.submit" crates/bitaxe-stratum/src/v1/messages.rs` finds client method names.
    - `rg -n "mining\\.notify|mining\\.set_difficulty|mining\\.set_extranonce|mining\\.set_version_mask|client\\.reconnect|client\\.show_message" crates/bitaxe-stratum/src/v1/messages.rs` finds server method handling.
    - `rg -n "from_str_radix\\(.*16|MAX_EXTRANONCE_2_LEN|32" crates/bitaxe-stratum/src/v1/messages.rs` finds hex parsing and extranonce cap.
    - `rg -n "src/v1/messages.rs" crates/bitaxe-stratum/BUILD.bazel` finds Bazel source wiring.
    - `rg -n "mining.set_difficulty|mining.set_extranonce|mining.set_version_mask|mining.submit" crates/bitaxe-stratum/fixtures/v1/protocol-cases.json` finds fixture cases.
    - `cargo test -p bitaxe-stratum stratum_v1_protocol --all-features` passes.
  </acceptance_criteria>
  <done>Stratum v1 client serializers and server parser are implemented as pure typed code with fixture coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Pool JSON -> typed Stratum messages | Untrusted pool input is parsed into domain values before state mutation. |
| Reference fixture data -> Rust tests | GPL-covered reference facts are carried as metadata-rich fixtures, not copied as source expression. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-04-01 | Tampering | `parse_server_message` | mitigate | Reject unknown methods, missing params, invalid hex, oversized extranonce2, and malformed results with `StratumV1Error`. |
| T-04-02 | Supply Chain | `protocol-cases.json` | mitigate | Require source path, reference commit, license posture, derivation, and checklist IDs in fixture metadata. |
| T-04-03 | Information Disclosure | `SubmitShare` serializer | mitigate | Tests use synthetic usernames/passwords only; evidence and fixtures must not include real pool secrets. |
</threat_model>

<verification>
Run after this plan:

```bash
cargo test -p bitaxe-stratum stratum_v1_protocol --all-features
bazel test //crates/bitaxe-stratum:tests
git status --short reference/esp-miner
```
</verification>

<success_criteria>
- `bitaxe-stratum` exposes a typed active Stratum v1 protocol core.
- Parser/serializer unit tests cover all STR-01 method classes planned for Phase 4.
- Fixture metadata records reference/provenance without editing `reference/esp-miner`.
- No firmware socket or live mining behavior is introduced in this plan.
</success_criteria>
