---
phase: 09-flash-monitor-evidence-wrapper-hardening
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening.md
  - docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening/flash-command-evidence.json
  - docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening/flash-monitor.log
  - docs/parity/checklist.md
  - docs/release/ultra-205.md
autonomous: true
requirements: [FND-07, FND-08, REL-07, EVD-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: "09-2026-06-29T13-16-47"
generated_at: "2026-06-29T13:38:19.268Z"
must_haves:
  truths:
    - "Fresh Ultra 205 serial evidence is captured through `just flash-monitor ... evidence-dir=...`, not raw `espflash monitor` fallback."
    - "The evidence record contains selected board, port, source commit, reference commit, package manifest, exact flash command, exact monitor command, monitor log, observed behavior, and conclusion."
    - "Release documentation tells operators how to recover from monitor startup failures without treating partial logs as trusted proof."
    - "WF-005 cites wrapper-produced Phase 9 evidence while HTTP/static/recovery/OTA/rollback rows remain below verified."
    - "`reference/esp-miner` remains read-only."
  artifacts:
    - path: "docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening.md"
      provides: "Human-readable Phase 9 wrapper evidence ledger citing generated JSON and log artifacts"
    - path: "docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening/flash-command-evidence.json"
      provides: "Canonical machine-readable wrapper evidence record"
    - path: "docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening/flash-monitor.log"
      provides: "Wrapper-owned noninteractive Ultra 205 serial capture"
    - path: "docs/parity/checklist.md"
      provides: "WF-005 wrapper evidence citation without release-row overclaim"
    - path: "docs/release/ultra-205.md"
      provides: "Operator documentation for wrapper evidence and fail-closed recovery guidance"
  key_links:
    - from: "scripts/detect-ultra205.sh"
      to: "docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening.md"
      via: "detector output recorded before hardware flash"
      pattern: "port="
    - from: "tools/flash/src/main.rs"
      to: "docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening/flash-command-evidence.json"
      via: "enriched `EvidenceRecord`"
      pattern: "monitor_command"
    - from: "docs/parity/checklist.md"
      to: "docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening.md"
      via: "WF-005 evidence citation"
      pattern: "phase-09-flash-monitor-evidence-wrapper-hardening"
---

<objective>
Capture fresh Ultra 205 wrapper-owned flash-monitor evidence, then update workflow/release evidence records so the Phase 8 raw-monitor fallback is no longer the only serial proof.

Purpose: Implement D-12 through D-15, plus documentation and checklist closure for D-09 through D-11, without promoting HTTP/static/recovery/OTA/rollback rows from serial evidence alone.
Output: Phase 9 evidence document, wrapper-generated JSON/log artifacts when hardware is detected, updated WF-005 and release-guide guidance, and passing parity validation.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-flash-monitor-evidence-wrapper-hardening/09-CONTEXT.md
@.planning/phases/09-flash-monitor-evidence-wrapper-hardening/09-RESEARCH.md
@.planning/phases/09-flash-monitor-evidence-wrapper-hardening/09-VALIDATION.md
@.planning/phases/09-flash-monitor-evidence-wrapper-hardening/09-01-SUMMARY.md
@AGENTS.md
@Justfile
@scripts/detect-ultra205.sh
@tools/flash/src/main.rs
@docs/parity/checklist.md
@docs/parity/evidence/phase-08-ultra-205-release-gate.md
@docs/parity/evidence/phase-08-ultra-205-release-summary.md
@docs/parity/evidence/phase-07-ultra-205-ota-hardware-smoke.md
@docs/release/ultra-205.md
@docs/adr/0011-usb-flashing-ergonomics.md
@docs/adr/0012-parity-verification-evidence.md
@docs/adr/0014-pivot-to-ultra-205-bm1366-first-parity.md

<interfaces>
Expected Phase 9 evidence JSON fields produced by Plan 09-01:

```text
command_kind
board
port
firmware_commit
reference_commit
manifest_path
flash_image_path
timestamp
command
log_path
flash_command
monitor_command
monitor_log_path
capture_mode
capture_status
capture_timeout_seconds
trusted_output
conclusion
```

Passing capture status values for Phase 9 serial evidence:

```text
completed
timed_out_after_trusted_output
```

Unsupported release rows that must not be promoted by this plan:

```text
FS-001
OTA-001
OTA-002
REL-001
REL-002
REL-003
```
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Capture fresh wrapper-owned Ultra 205 serial evidence</name>
  <read_first>
    docs/parity/evidence/phase-08-ultra-205-release-gate.md
    docs/parity/evidence/phase-08-ultra-205-release-summary.md
    docs/parity/evidence/phase-07-ultra-205-ota-hardware-smoke.md
    tools/flash/src/main.rs
    scripts/detect-ultra205.sh
    Justfile
    AGENTS.md
    .planning/phases/09-flash-monitor-evidence-wrapper-hardening/09-CONTEXT.md
  </read_first>
  <files>docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening.md, docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening/flash-command-evidence.json, docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening/flash-monitor.log</files>
  <action>Create or update `docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening.md` with H2 sections named exactly `Run Identity`, `Detector Gate`, `Wrapper Evidence Artifacts`, `JSON Evidence Fields`, `Monitor Log Markers`, `Observed Behavior`, `Scope Boundary`, `Secret Redaction Review`, and `Final Conclusion`. Run `just detect-ultra205` before any flash or monitor command. If detection exits nonzero, record `wrapper evidence result: blocked - hardware detection failed`, `detector result: blocked - zero, multiple, or invalid Ultra 205 USB candidates`, `raw espflash fallback: not used`, and `conclusion: not run - hardware detection blocked`, then do not run flash, monitor, curl, OTA, erase, or raw `espflash monitor`. If detection exits 0 and prints exactly one `port=<path>`, record the detector command, selected port, board-info command `espflash board-info --chip esp32s3 --port <path> --non-interactive`, board-info output summary, source commit, reference commit, and package manifest path. Then run exactly `just flash-monitor board=205 port=<path> evidence-dir=docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening`. Do not run `timeout 25 espflash monitor` or any raw monitor fallback. If wrapper evidence succeeds, record `wrapper evidence result: passed - wrapper-owned noninteractive flash-monitor capture trusted`, `raw espflash fallback: not used`, the JSON path `docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening/flash-command-evidence.json`, the log path `docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening/flash-monitor.log`, exact `flash_command`, exact `monitor_command`, `capture_mode`, `capture_status`, `trusted_output`, observed boot markers, and conclusion `passed - wrapper-owned serial boot evidence captured; HTTP/static/recovery/OTA/rollback parity not claimed`. If the wrapper exits nonzero after detector success, record `wrapper evidence result: failed - evidence capture is not trusted`, copy the wrapper's recovery guidance into the evidence document, and treat the task as failed rather than updating checklist/docs claims.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; f=docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening.md; dir=docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening; for term in "## Run Identity" "## Detector Gate" "## Wrapper Evidence Artifacts" "## JSON Evidence Fields" "## Monitor Log Markers" "## Observed Behavior" "## Scope Boundary" "## Secret Redaction Review" "## Final Conclusion" "raw espflash fallback: not used"; do rg -q --fixed-strings "$term" "$f"; done; if rg -q --fixed-strings "wrapper evidence result: passed - wrapper-owned noninteractive flash-monitor capture trusted" "$f"; then test -f "$dir/flash-command-evidence.json"; test -f "$dir/flash-monitor.log"; for term in "\"command_kind\": \"flash-monitor\"" "\"board\": \"205\"" "\"firmware_commit\"" "\"reference_commit\"" "\"manifest_path\"" "\"flash_image_path\"" "\"flash_command\"" "\"monitor_command\"" "\"monitor_log_path\"" "\"capture_mode\": \"noninteractive\"" "\"capture_status\"" "\"capture_timeout_seconds\"" "\"trusted_output\": true" "\"conclusion\""; do rg -q --fixed-strings "$term" "$dir/flash-command-evidence.json"; done; rg -q "espflash monitor --chip esp32s3 --port .+ --non-interactive" "$dir/flash-command-evidence.json"; rg -q "\"capture_status\": \"(completed|timed_out_after_trusted_output)\"" "$dir/flash-command-evidence.json"; for marker in "bitaxe-rust boot: board=Ultra 205 asic=BM1366" "safe_state:" "ota_boot_validation=" "spiffs_mount=available" "axeos_api_route_shell=started" "firmware_commit=" "reference_commit="; do rg -q --fixed-strings "$marker" "$dir/flash-monitor.log"; done; elif rg -q --fixed-strings "wrapper evidence result: blocked - hardware detection failed" "$f"; then rg -q --fixed-strings "detector result: blocked - zero, multiple, or invalid Ultra 205 USB candidates" "$f"; rg -q --fixed-strings "conclusion: not run - hardware detection blocked" "$f"; else printf "missing supported wrapper evidence result in %s\n" "$f" >&2; exit 1; fi; ! rg -q --fixed-strings "timeout 25 espflash monitor" "$f"'</automated>
  </verify>
  <acceptance_criteria>
    Evidence document contains every required H2 heading.
    Evidence document contains `raw espflash fallback: not used`.
    If hardware detection passed, `flash-command-evidence.json` and `flash-monitor.log` exist under `docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening/`.
    If hardware detection passed, JSON contains `monitor_command` with `espflash monitor --chip esp32s3 --port <port> --non-interactive`, `capture_mode` value `noninteractive`, `trusted_output` value `true`, and `capture_status` value `completed` or `timed_out_after_trusted_output`.
    If hardware detection passed, `flash-monitor.log` contains all seven trusted serial markers from Plan 09-01.
    If hardware detection failed, no flash, monitor, curl, OTA, erase, or raw `espflash monitor` command is run.
  </acceptance_criteria>
  <done>Fresh Phase 9 evidence is wrapper-owned when hardware is available, or explicitly blocked before hardware effects if the detector fails.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh workflow checklist and operator recovery documentation</name>
  <read_first>
    docs/parity/checklist.md
    docs/release/ultra-205.md
    docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening.md
    docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening/flash-command-evidence.json
    docs/parity/evidence/phase-08-ultra-205-release-gate.md
    docs/parity/evidence/phase-08-ultra-205-release-summary.md
    .planning/phases/09-flash-monitor-evidence-wrapper-hardening/09-CONTEXT.md
  </read_first>
  <files>docs/parity/checklist.md, docs/release/ultra-205.md</files>
  <action>Update only the workflow/release documentation surfaces allowed by D-13 and D-14. In `docs/parity/checklist.md`, update row `WF-005` to cite `docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening.md`, `flash-command-evidence.json`, and `flash-monitor.log` as wrapper-produced Phase 9 evidence. If Task 1 recorded passed wrapper evidence, include the exact phrase `Phase 9 wrapper evidence passed through just flash-monitor with noninteractive capture; raw espflash fallback is historical only`. If Task 1 recorded detector blocked, include `Phase 9 wrapper evidence pending - hardware detection blocked` and do not remove existing historical evidence. Do not add a new checklist row unless `WF-005` cannot hold the citation; if a new row is added, it must be workflow-only and must not change release-sensitive rows. Keep `FS-001`, `OTA-001`, `REL-001`, `REL-002`, and `REL-003` at status `implemented`; keep `OTA-002` at `deferred | deferred` with public response `Wrong API input`. In `docs/release/ultra-205.md`, update `## Flash And Monitor Evidence Capture` to document the exact command `just flash-monitor board=205 port=<port> evidence-dir=docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening`, optional timeout override `capture-timeout-seconds=25`, JSON source of truth `flash-command-evidence.json`, log path `flash-monitor.log`, passing statuses `completed` and `timed_out_after_trusted_output`, and fail-closed text `evidence capture failed and is not trusted`. Add a recovery subsection with exact commands `just detect-ultra205`, `just flash-monitor board=205 port=<port> evidence-dir=<path>`, and diagnostic-only `just monitor port=<port>`. State that raw `espflash monitor` output must not be used as the trusted Phase 9 proof. Preserve the existing Phase 8 residual gap language for live HTTP, static, recovery, firmware OTA, invalid image rejection, rollback, failed update recovery, large erase, interrupted update, and OTAWWW.</action>
  <verify>
    <automated>just parity && bash -lc 'set -euo pipefail; checklist=docs/parity/checklist.md; guide=docs/release/ultra-205.md; rg -q --fixed-strings "phase-09-flash-monitor-evidence-wrapper-hardening.md" "$checklist"; rg -q --fixed-strings "flash-command-evidence.json" "$checklist"; rg -q --fixed-strings "flash-monitor.log" "$checklist"; if rg -q --fixed-strings "wrapper evidence result: passed - wrapper-owned noninteractive flash-monitor capture trusted" docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening.md; then rg -q --fixed-strings "Phase 9 wrapper evidence passed through just flash-monitor with noninteractive capture; raw espflash fallback is historical only" "$checklist"; fi; for pattern in "^\\| FS-001 .* \\| implemented \\|" "^\\| OTA-001 .* \\| implemented \\|" "^\\| REL-001 .* \\| implemented \\|" "^\\| REL-002 .* \\| implemented \\|" "^\\| REL-003 .* \\| implemented \\|"; do rg -q "$pattern" "$checklist"; done; rg -q "^\\| OTA-002 .* \\| deferred \\| deferred \\|" "$checklist"; for term in "just flash-monitor board=205 port=<port> evidence-dir=docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening" "capture-timeout-seconds=25" "flash-command-evidence.json" "flash-monitor.log" "completed" "timed_out_after_trusted_output" "evidence capture failed and is not trusted" "just detect-ultra205" "just flash-monitor board=205 port=<port> evidence-dir=<path>" "diagnostic-only" "just monitor port=<port>" "raw `espflash monitor` output must not be used as the trusted Phase 9 proof" "Wrong API input"; do rg -q --fixed-strings "$term" "$guide"; done; section=$(awk '\''$0=="## Flash And Monitor Evidence Capture"{inside=1; next} inside && /^## /{exit} inside{print}'\'' "$guide"); ! printf "%s\n" "$section" | rg -q --fixed-strings "timeout 25 espflash"'</automated>
  </verify>
  <acceptance_criteria>
    `just parity` exits 0 and prints `validation_errors: none`.
    `docs/parity/checklist.md` row `WF-005` cites `phase-09-flash-monitor-evidence-wrapper-hardening.md`, `flash-command-evidence.json`, and `flash-monitor.log`.
    `FS-001`, `OTA-001`, `REL-001`, `REL-002`, and `REL-003` remain `implemented`.
    `OTA-002` remains `deferred | deferred`.
    `docs/release/ultra-205.md` contains `just flash-monitor board=205 port=<port> evidence-dir=docs/parity/evidence/phase-09-flash-monitor-evidence-wrapper-hardening`.
    `docs/release/ultra-205.md` contains `capture-timeout-seconds=25`, `evidence capture failed and is not trusted`, `just detect-ultra205`, diagnostic-only `just monitor port=<port>`, and `raw \`espflash monitor\` output must not be used as the trusted Phase 9 proof`.
    The `Flash And Monitor Evidence Capture` section does not contain `timeout 25 espflash`.
  </acceptance_criteria>
  <done>Workflow evidence and operator docs point to wrapper-produced Phase 9 artifacts while preserving all later release-evidence blockers.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| hardware detector to flash command | Read-only detector output determines whether autonomous flash-monitor hardware use is allowed. |
| wrapper JSON/log to committed evidence | Generated machine evidence and serial logs become committed parity evidence. |
| evidence to checklist | Workflow evidence can influence checklist wording and status. |
| evidence to operator docs | Release guide recovery instructions influence developer actions after monitor failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-09-02-01 | Spoofing | `just detect-ultra205` | mitigate | Run detector before hardware use and continue only when it prints exactly one `port=<path>` and board-info succeeds. |
| T-09-02-02 | Tampering | `flash-command-evidence.json` and `flash-monitor.log` | mitigate | Accept passed evidence only when generated JSON records noninteractive monitor command, trusted output, passing capture status, and log markers. |
| T-09-02-03 | Information Disclosure | Phase 9 evidence docs/logs | mitigate | Run only serial-scope wrapper evidence; scan and record that Wi-Fi credentials, pool credentials, private endpoints, and NVS secret values are not committed. |
| T-09-02-04 | Repudiation | `docs/parity/checklist.md` | mitigate | Cite exact Phase 9 evidence files in `WF-005`; keep HTTP/static/recovery/OTA/rollback rows below verified unless their own Phase 13 evidence exists. |
| T-09-02-05 | Elevation of Privilege | Release guide recovery commands | mitigate | Recovery guidance points to repo-owned detector, wrapper flash-monitor, and diagnostic-only monitor commands; it states raw `espflash monitor` output is not trusted Phase 9 proof. |
| T-09-02-06 | Tampering | `reference/esp-miner` | mitigate | Do not modify `reference/esp-miner`; verify with `git diff -- reference/esp-miner --exit-code`. |
</threat_model>

<verification>
Run the branch-aware evidence artifact check from Task 1, `just parity`, `git diff -- reference/esp-miner --exit-code`, and the docs/checklist fixed-string checks from Task 2. Before committing Phase 9 completion, run `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, `cargo test --all-features`, `just test`, and `just parity`.
</verification>

<success_criteria>
Fresh Ultra 205 serial evidence is captured through the repo wrapper when the detector passes; generated JSON/log artifacts contain the required fields and trusted markers; WF-005 and the release guide cite wrapper-produced artifacts; failure guidance is fail-closed and repo-owned; `FS-001`, `OTA-001`, `OTA-002`, `REL-001`, `REL-002`, and `REL-003` are not promoted from Phase 9 serial evidence; `reference/esp-miner` is unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/09-flash-monitor-evidence-wrapper-hardening/09-02-SUMMARY.md`.
</output>
