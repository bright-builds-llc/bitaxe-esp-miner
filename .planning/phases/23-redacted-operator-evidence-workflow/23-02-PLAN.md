---
phase: 23-redacted-operator-evidence-workflow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/parity/src/operator_evidence.rs
  - tools/parity/src/main.rs
  - tools/parity/BUILD.bazel
autonomous: true
requirements: [EVD-07, STR-10, CFG-07, EVD-09]
user_setup: []
must_haves:
  truths:
    - "Evidence-root inventory validation fails when any required Phase 23 slot is missing."
    - "Evidence-root inventory validation fails when required redaction-review or raw-artifact fields are absent."
    - "Synthetic forbidden evidence values are rejected before committed artifacts can be cited."
    - "The validator is available through the existing `tools/parity` command surface."
  artifacts:
    - path: "tools/parity/src/operator_evidence.rs"
      provides: "pure Phase 23 evidence-root inventory and redaction validation"
      exports: ["load_operator_evidence_documents", "validate_operator_evidence_documents", "render_operator_evidence_report"]
    - path: "tools/parity/src/main.rs"
      provides: "`operator-evidence` CLI subcommand wiring"
      contains: "OperatorEvidence"
    - path: "tools/parity/BUILD.bazel"
      provides: "Bazel ownership for operator_evidence.rs in report binary and tests"
      contains: "src/operator_evidence.rs"
  key_links:
    - from: "tools/parity/src/main.rs"
      to: "tools/parity/src/operator_evidence.rs"
      via: "operator-evidence subcommand"
      pattern: "OperatorEvidence|operator_evidence"
    - from: "tools/parity/src/operator_evidence.rs"
      to: "docs/parity/evidence/phase-23-redacted-operator-evidence-workflow"
      via: "required slot file validation"
      pattern: "package.md|detector.md|board-info.md|command.md|log.md|api.md|websocket.md|share-outcome.md|safe-stop.md|redaction-review.md|conclusion.md"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 23-2026-07-04T22-53-37
generated_at: 2026-07-04T23:00:00Z
---

<objective>
Add pure validator support for the Phase 23 evidence root and redaction inventory.

Purpose: The workflow must not depend on manual review alone for EVD-07 and EVD-09. Missing slots, missing redaction review, committed raw artifacts, and synthetic forbidden values must fail in a deterministic repo-owned check.

Output: `tools/parity` exposes an `operator-evidence` subcommand backed by tested pure Rust validation.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-redacted-operator-evidence-workflow/23-CONTEXT.md
@.planning/phases/23-redacted-operator-evidence-workflow/23-RESEARCH.md
@.planning/phases/23-redacted-operator-evidence-workflow/23-VALIDATION.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@tools/parity/src/main.rs
@tools/parity/src/mining_allow.rs
@tools/parity/src/release_evidence.rs
@tools/parity/BUILD.bazel
@docs/parity/evidence/phase-21-live-mining-and-soak-evidence/redaction-review.md
</context>

<interfaces>
From `tools/parity/src/main.rs`, new subcommands are declared in `CliCommand` and argument structs use `#[derive(Debug, Parser)]` with `Utf8PathBuf` parsed through `parse_utf8_path`.

From `tools/parity/src/mining_allow.rs`, validator modules expose a load/validate/render shape:

```rust
pub(crate) fn load_mining_allow_documents(...)
pub(crate) fn validate_mining_allow_documents(...)
pub(crate) fn render_mining_allow_report(...)
```

From `tools/parity/src/release_evidence.rs`, redaction status is considered passed only when review content contains `redaction_status: passed`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add operator evidence validator</name>
  <read_first>
    - `.planning/phases/23-redacted-operator-evidence-workflow/23-CONTEXT.md`
    - `.planning/phases/23-redacted-operator-evidence-workflow/23-RESEARCH.md`
    - `.planning/phases/23-redacted-operator-evidence-workflow/23-VALIDATION.md`
    - `tools/parity/src/main.rs`
    - `tools/parity/src/mining_allow.rs`
    - `tools/parity/src/release_evidence.rs`
    - `tools/parity/BUILD.bazel`
    - `tools/parity/src/operator_evidence.rs`
  </read_first>
  <files>tools/parity/src/operator_evidence.rs, tools/parity/src/main.rs, tools/parity/BUILD.bazel</files>
  <behavior>
    - Test 1: A root containing all required slot files, `redaction_status: passed`, and `raw_artifacts_committed: no` validates with no errors.
    - Test 2: A root missing `share-outcome.md` fails with a validation error naming `share-outcome.md`.
    - Test 3: A root whose `redaction-review.md` lacks `redaction_status: passed` fails when `--require-redaction-passed` is set.
    - Test 4: A root whose committed files contain synthetic raw pool URL, worker, owner-address-like username, password, target, extranonce, share payload, socket error, device URL, IP, MAC, Wi-Fi, token, NVS secret, or raw BM1366 frame sentinel fails.
  </behavior>
  <action>Create `tools/parity/src/operator_evidence.rs` using the existing functional-core pattern from `mining_allow.rs` and `release_evidence.rs` per D-13 and D-14. Define constants for required slot files exactly `package.md`, `detector.md`, `board-info.md`, `command.md`, `log.md`, `api.md`, `websocket.md`, `share-outcome.md`, `safe-stop.md`, `redaction-review.md`, and `conclusion.md`. Implement `OperatorEvidenceDocuments`, `OperatorEvidenceReport`, `OperatorEvidenceFilters`, `load_operator_evidence_documents`, `validate_operator_evidence_documents`, and `render_operator_evidence_report`. Validation must fail when the root path is missing, any required slot file is missing, no slot file contains `raw_artifacts_committed: no`, `redaction-review.md` lacks `redaction_status: passed` while `require_redaction_passed` is true, any slot lacks `slot_status: passed|blocked|pending|deferred`, or committed content contains synthetic forbidden values/categories from D-10 and D-11. Add `mod operator_evidence;` to `main.rs`, add a `OperatorEvidence(OperatorEvidenceArgs)` subcommand with `--evidence-root`, `--require-redaction-passed`, and optional `--format text|json` if the existing report pattern makes JSON cheap; otherwise text output is sufficient. Add `src/operator_evidence.rs` to `tools/parity/BUILD.bazel`.</action>
  <acceptance_criteria>
    - `rg -n "mod operator_evidence|OperatorEvidence|operator-evidence|require_redaction_passed" tools/parity/src/main.rs`
    - `rg -n "REQUIRED_SLOT_FILES|package.md|share-outcome.md|raw_artifacts_committed: no|redaction_status: passed" tools/parity/src/operator_evidence.rs`
    - `rg -n "pool|worker|owner|password|target|extranonce|share|socket|device_url|ip|mac|wifi|token|nvs|BM1366" tools/parity/src/operator_evidence.rs`
    - `rg -n "src/operator_evidence.rs" tools/parity/BUILD.bazel`
  </acceptance_criteria>
  <verify>
    <automated>bazel test //tools/parity:tests</automated>
  </verify>
  <done>`tools/parity` has a tested pure validator that rejects missing Phase 23 slots, missing redaction pass status when required, committed raw artifacts, and synthetic forbidden evidence categories.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover blocked and leak scenarios</name>
  <read_first>
    - `.planning/phases/23-redacted-operator-evidence-workflow/23-CONTEXT.md`
    - `.planning/phases/23-redacted-operator-evidence-workflow/23-RESEARCH.md`
    - `.planning/phases/23-redacted-operator-evidence-workflow/23-VALIDATION.md`
    - `tools/parity/src/operator_evidence.rs`
    - `tools/parity/src/main.rs`
    - `tools/parity/BUILD.bazel`
  </read_first>
  <files>tools/parity/src/operator_evidence.rs</files>
  <behavior>
    - Test 1: A blocked `api.md` and blocked `websocket.md` are accepted when both include exact reasons rejecting stale `DEVICE_URL`, mDNS, ARP, router state, scans, and unrelated evidence.
    - Test 2: A pending `share-outcome.md` is accepted only when it names owner `Phase 25` and states accepted/rejected share outcomes remain non-claims.
    - Test 3: A conclusion that lacks `phase23_workflow_claim: redacted_operator_evidence_workflow` fails.
    - Test 4: A conclusion that says Phase 23 verifies trusted BM1366 production work, live Stratum socket success, accepted shares, rejected shares, or Phase 26 telemetry promotion fails.
  </behavior>
  <action>Extend the unit tests inside `operator_evidence.rs` to encode D-02, D-06, D-09, D-10, D-11, D-15, D-16, D-18, and D-19. Use synthetic sentinel values only, with clearly fake strings such as `stratum+tcp://sentinel-pool.invalid:3333`, `bc1qsentinelowneraddress.bitaxe`, `sentinel-password`, `target=00000000sentinel`, `extranonce=sentinel-extra`, `share_payload=sentinel-share`, `socket_error=sentinel-private-host`, `device_url=http://192.0.2.55`, `ip=192.0.2.55`, `mac=aa:bb:cc:dd:ee:ff`, `ssid=SentinelWifi`, `token=sentinel-token`, `nvs_secret=sentinel-nvs`, and `raw_bm1366_frame=aa55sentinel`. Do not use real local credential files or real owner addresses. Ensure tests use Arrange, Act, Assert comments per the Rust testing standard.</action>
  <acceptance_criteria>
    - `rg -n "stale DEVICE_URL|mDNS|ARP|router state|network scan|Phase 25|accepted/rejected share outcomes remain non-claims" tools/parity/src/operator_evidence.rs`
    - `rg -n "sentinel-pool.invalid|sentinel-password|sentinel-extra|sentinel-share|sentinel-token|raw_bm1366_frame" tools/parity/src/operator_evidence.rs`
    - `rg -n "phase23_workflow_claim: redacted_operator_evidence_workflow|trusted BM1366 production work|live Stratum socket success|Phase 26 telemetry" tools/parity/src/operator_evidence.rs`
  </acceptance_criteria>
  <verify>
    <automated>bazel test //tools/parity:tests</automated>
  </verify>
  <done>The validator test suite proves blocked API/WebSocket target handling, pending share-outcome non-claims, required workflow conclusion, and rejection of later-phase overclaim language using synthetic fixtures only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| committed evidence root to parity validator | Markdown slot artifacts become machine-checked input to `tools/parity`; invalid or secret-bearing content must fail before citation. |
| raw runtime values to validator scan | Synthetic tests represent forbidden live values without requiring agents to read or expose real local credentials. |
| operator workflow claims to checklist claims | Validator rejects overclaim phrases so Phase 23 workflow proof does not become Phase 24/25/26 behavior proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-23-01 | Tampering / Repudiation | `operator_evidence.rs` slot inventory | mitigate | Task 1 rejects missing required slot files, missing `slot_status`, missing `raw_artifacts_committed: no`, and missing redaction pass status when required. |
| T-23-02 | Information Disclosure | `operator_evidence.rs` forbidden value scan | mitigate | Task 1 and Task 2 add deterministic synthetic leak checks for pool credentials, owner identity, target, extranonce, share payload, socket error, NVS/settings values, device URL, IP, MAC, Wi-Fi, tokens, and raw BM1366 frames. |
| T-23-03 | Spoofing / Tampering | API/WebSocket blocked slot validation | mitigate | Task 2 requires blocked target-provenance text for stale `DEVICE_URL`, mDNS, ARP, router state, scans, and unrelated evidence. |
| T-23-04 | Elevation of Privilege / Tampering | `tools/parity` command surface | mitigate | Task 1 wires validation through the repo-owned `tools/parity` command instead of ad hoc evidence checks. |
| T-23-05 | Repudiation / Integrity | conclusion overclaim validation | mitigate | Task 2 fails conclusion content that claims Phase 24 BM1366 production work, Phase 25 live Stratum/share behavior, or Phase 26 telemetry promotion. |
</threat_model>

<verification>
Run the Phase 23 targeted validator checks:

```bash
bazel test //tools/parity:tests
bazel run //tools/parity:report -- operator-evidence --evidence-root docs/parity/evidence/phase-23-redacted-operator-evidence-workflow --require-redaction-passed
```

If the evidence root does not exist yet because Plan 01 has not executed in the same workspace, the second command may fail with missing-root errors; that is acceptable before Wave 2 and must pass before closure.
</verification>

<success_criteria>
Plan 02 succeeds when `tools/parity` can validate a Phase 23 evidence root, unit tests cover missing-slot and synthetic leak failures, and the validator rejects stale-target and later-phase overclaim content without touching real local credentials.
</success_criteria>

<output>
After completion, create `.planning/phases/23-redacted-operator-evidence-workflow/23-02-SUMMARY.md`.
</output>
