---
phase: "30"
plan: "02"
type: execute
wave: 2
depends_on:
  - "30-01"
files_modified:
  - tools/parity/src/main.rs
  - docs/parity/evidence/phase-30-live-share-outcome-and-verified-promotion/conclusion.md
  - .planning/phases/30-live-share-outcome-and-verified-promotion/30-VALIDATION.md
autonomous: true
requirements:
  - STR-09
  - CFG-07
  - ASIC-11
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 30-2026-07-13T16-24-26
generated_at: 2026-07-13T16:44:00.000Z
must_haves:
  truths:
    - A verified STR-09, CFG-07, or ASIC-11 row is rejected without explicit row-specific eligible Phase 30 proof.
    - No-promotion, gaps-found, none, blocked, workflow-only, or deterministic fake-pool evidence cannot satisfy Phase 30 admission.
    - Future eligible evidence is checked independently for each exact row rather than enabling all three together.
    - The committed conservative checklist passes parity while all three requirements remain pending.
  artifacts:
    - path: tools/parity/src/main.rs
      provides: Focused Phase 30 promotion-admission guard and regression matrix.
    - path: docs/parity/evidence/phase-30-live-share-outcome-and-verified-promotion/conclusion.md
      provides: Final no-promotion conclusion and exact non-claim ledger.
    - path: .planning/phases/30-live-share-outcome-and-verified-promotion/30-VALIDATION.md
      provides: Executed Nyquist sign-off and mapped green verification results.
  key_links:
    - from: tools/parity/src/main.rs
      to: docs/parity/evidence/phase-30-live-share-outcome-and-verified-promotion/disposition.md
      via: exact Phase 30 path and disposition/admission tokens required in verified-row evidence notes
    - from: tools/parity/src/main.rs
      to: docs/parity/checklist.md
      via: validate_rows invokes the Phase 30 guard for every verified row
---

<objective>
Make the Phase 30 promotion boundary executable and close the phase with verified no-promotion evidence.

Purpose: prevent a later checklist edit from promoting STR-09, CFG-07, or ASIC-11 using the archived Won't Do result, blocked evidence, generic workflow proof, or another row's evidence.
Output: a focused parity validator and regression matrix, final Phase 30 conclusion, completed validation sign-off, and green repository/lifecycle gates.
</objective>

<execution_context>
@.planning/phases/30-live-share-outcome-and-verified-promotion/30-CONTEXT.md
@.planning/phases/30-live-share-outcome-and-verified-promotion/30-RESEARCH.md
@.planning/phases/30-live-share-outcome-and-verified-promotion/30-VALIDATION.md
@.planning/phases/30-live-share-outcome-and-verified-promotion/30-01-PLAN.md
</execution_context>

<threat_model>
## Threat Model

**Assets:** verified checklist status, exact per-row evidence semantics, current conservative checklist, future explicit Phase 30 admission path.

**Trust boundaries:** Markdown checklist cells into typed parsed rows; evidence-note tokens into promotion decisions; shared Phase 30 categories into three independent requirement claims.

| Threat | Severity | Mitigation | Verification |
| --- | --- | --- | --- |
| A path citation self-authenticates promotion | High | Require exact Phase 30 promotion disposition plus row-specific same-chain/redaction/support tokens; reject no-promotion and blocker tokens. | Negative tests add the path without proof and expect a row-specific validation error. |
| One requirement's evidence promotes another | High | Use separate STR-09, ASIC-11, and CFG-07 predicates with non-interchangeable required tokens. | Cross-row fixture matrix proves each evidence bundle fails the other two rows. |
| Existing CFG-07 permanent ban conflicts with future explicit evidence | High | Replace the unconditional Phase 28 rejection only when the full Phase 30 CFG-07 predicate passes; keep all other cases rejected. | Tests cover current conservative row, missing proof, partial proof, and complete future proof. |
| Blocked or gaps-found evidence masquerades as live proof | High | Reject `no_promotion_no_eligible_evidence`, `gaps_found`, `eligible_share_outcome: none`, `blocked_safe_prerequisite`, `workflow-only`, and fake-pool scopes for verified rows. | Negative tests exercise each forbidden token. |
| New guard breaks unrelated verified rows | Medium | Scope the helper exactly to STR-09, CFG-07, and ASIC-11 and retain Phase 28 validators. | Full Cargo/Bazel parity suite and `just parity` pass. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add row-specific Phase 30 promotion admission and regression tests</name>
  <files>tools/parity/src/main.rs</files>
  <read_first>
    - tools/parity/src/main.rs
    - docs/parity/checklist.md
    - docs/parity/evidence/phase-30-live-share-outcome-and-verified-promotion/disposition.md
    - .planning/phases/30-live-share-outcome-and-verified-promotion/30-CONTEXT.md
    - .planning/phases/30-live-share-outcome-and-verified-promotion/30-RESEARCH.md
    - docs/adr/0012-parity-verification-evidence.md
    - docs/adr/0006-parity-checklist-as-audit-evidence.md
  </read_first>
  <action>
Before production edits, add focused `phase30_` Arrange/Act/Assert tests to `tools/parity/src/main.rs`. Record expected red failures locally for: each of STR-09, CFG-07, and ASIC-11 marked `verified` with no Phase 30 reference; a reference paired only with `phase30_disposition: no_promotion_no_eligible_evidence`; a reference paired with `gaps_found`, `eligible_share_outcome: none`, or `blocked_safe_prerequisite`; and each row supplied another row's proof bundle. Do not commit the red state.

Add `validate_phase30_promotion_row(row: &ChecklistRow) -> Vec<ValidationError>` and call it from `validate_rows` after the existing Phase 28 guard. Return immediately for IDs other than `STR-09`, `CFG-07`, and `ASIC-11`. Since `validate_rows` already evaluates only verified rows, require every in-scope verified row's haystack to contain `phase-30-live-share-outcome-and-verified-promotion/`, `phase30_promotion_disposition: promoted`, `new_evidence_input: explicit`, `detector_gate: passed`, `same_chain_gate: passed`, `redaction_status: passed`, and `raw_artifacts_committed: no`. Reject when the haystack contains any of: `no_promotion_no_eligible_evidence`, `gaps_found`, `eligible_share_outcome: none`, `blocked_safe_prerequisite`, `workflow-only`, `fake-pool`, or `deterministic-only`.

Add separate predicates with these exact row-specific tokens. STR-09 requires `eligible_share_outcome: accepted` or `eligible_share_outcome: rejected`, plus `live_submit_response_classified: true`, `asic_correlation: passed`, and `safe_stop_status: complete`. ASIC-11 requires `asic_result_to_active_work: correlated`, `submit_intent_from_correlated_result: true`, `eligible_share_outcome: accepted` or `eligible_share_outcome: rejected`, and `safe_stop_status: complete`. CFG-07 requires `runtime_credentials_input: local-owner-supplied`, `live_mining_credentials_consumed: true`, `committed_credential_values: none`, `eligible_share_outcome: accepted` or `eligible_share_outcome: rejected`, and `safe_stop_status: complete`.

Modify only the `CFG-07` arm in `validate_phase28_hardware_promotion_row`: retain its current error unless the new full Phase 30 CFG-07 predicate passes. Do not weaken any Phase 28 STR-09, bridge, safety, redaction, exact-non-claim, or blocker rule. Ensure complete future-positive fixtures include the existing Phase 28 required evidence tokens as well as Phase 30 tokens; Phase 30 is an additional admission layer, not a bypass.

Add tests named with the `phase30_` prefix covering: the committed conservative rows; three missing-admission cases; all forbidden disposition/blocker tokens; partial required-token matrices; three exact future-positive fixtures; and cross-row non-interchangeability. Error messages must name the row and the missing or forbidden Phase 30 category without echoing secret values.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-parity --all-features phase30_ && bazel test //tools/parity:tests && just parity</automated>
  </verify>
  <acceptance_criteria>
    - `tools/parity/src/main.rs` contains `fn validate_phase30_promotion_row` and invokes it from `validate_rows` after `validate_phase28_hardware_promotion_row`.
    - Every in-scope verified row requires the seven shared exact Phase 30 tokens and rejects all seven forbidden no-proof tokens.
    - STR-09, ASIC-11, and CFG-07 each have a distinct exact predicate and cross-row tests.
    - CFG-07 remains rejected by the Phase 28 guard unless the full Phase 30 CFG-07 predicate passes.
    - Existing Phase 28 tests remain present and pass unchanged in meaning.
    - The targeted Cargo tests, `bazel test //tools/parity:tests`, and `just parity` exit 0.
  </acceptance_criteria>
  <commit_gate>
Before committing, run separately and in exact order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Then rerun the automated verification. Do not commit unless every command passes.
  </commit_gate>
  <done>All three rows require explicit, independent Phase 30 evidence; no-promotion and blocked inputs fail closed; complete future fixtures pass only their exact row; and all parity tests remain green.</done>
</task>

<task type="auto">
  <name>Task 2: Publish final no-promotion conclusion and complete validation sign-off</name>
  <files>docs/parity/evidence/phase-30-live-share-outcome-and-verified-promotion/conclusion.md, .planning/phases/30-live-share-outcome-and-verified-promotion/30-VALIDATION.md</files>
  <read_first>
    - docs/parity/evidence/phase-30-live-share-outcome-and-verified-promotion/disposition.md
    - docs/parity/checklist.md
    - tools/parity/src/main.rs
    - .planning/REQUIREMENTS.md
    - .planning/phases/30-live-share-outcome-and-verified-promotion/30-VALIDATION.md
    - .planning/milestones/v1.1-phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md
    - docs/parity/evidence/phase-29-evidence-workflow-automation-closure/summary.md
  </read_first>
  <action>
Create `conclusion.md` without YAML frontmatter. Repeat the exact disposition fields from Plan 01, record that the Phase 30 contract and parity tests passed, and include a three-row conclusion table with result `not_promoted_pending` for STR-09, CFG-07, and ASIC-11. State that Phase 30 itself completed successfully because it recorded and enforced the conservative decision; this does not satisfy or verify those requirements. Preserve the complete non-claim set and record `hardware_accessed: false`, `credentials_accessed: false`, and `raw_artifacts_committed: no`.

After every mapped command passes, update `30-VALIDATION.md`: set `status: complete` and `wave_0_complete: true`; change the four mapped task rows to `✅ green`; check the Wave 0 and final sign-off boxes; add a `## Final Gate Results` section listing actual passed commands; and set `Approval: approved 2026-07-13`. Keep `nyquist_compliant: true` described as sampling completeness, not evidence promotion.

Run a final targeted truth review: `.planning/REQUIREMENTS.md` traceability for all three remains `Pending (gap closure)`; checklist status cells remain `implemented`; archived verification remains `gaps_found`; Phase 28.1 remains `closed_wont_do_unresolved`; and no diff line changes any requirement or parity status to verified/satisfied/passed. Do not edit REQUIREMENTS to manufacture completion and do not access hardware, credentials, ignored local evidence, or archived diagnostic entrypoints.
  </action>
  <verify>
    <automated>bazel test //scripts:phase30_no_promotion_contract_test //tools/parity:tests && just parity && just verify-reference && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `conclusion.md` contains all three `not_promoted_pending` rows and explicitly separates Phase completion from requirement verification.
    - `30-VALIDATION.md` contains `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`, four green task rows, checked sign-off boxes, and `Approval: approved 2026-07-13`.
    - Requirements traceability still contains `Pending (gap closure)` for STR-09, CFG-07, and ASIC-11.
    - Checklist status cells for all three remain exactly `implemented`.
    - Archived root verification remains `gaps_found` and Phase 28.1 remains `closed_wont_do_unresolved`.
    - Both Bazel targets, `just parity`, `just verify-reference`, and `git diff --check` exit 0.
  </acceptance_criteria>
  <commit_gate>
Before committing, run separately and in exact order: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Then rerun the automated verification. Do not commit unless every command passes.
  </commit_gate>
  <done>The executable Phase 30 guard is green, final no-promotion evidence is committed, validation is signed off, all three requirements remain pending/implemented, and repository/lifecycle truth is ready for final GSD verification.</done>
</task>

</tasks>

<verification>
- `cargo test -p bitaxe-parity --all-features phase30_`
- `bazel test //scripts:phase30_no_promotion_contract_test //tools/parity:tests`
- `just parity`
- `just verify-reference`
- Mandatory Cargo sequence in exact order
- Targeted pending/non-promotion review over REQUIREMENTS, checklist, archived verification, and validation metadata
- `node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 30`
- `git diff --check`
</verification>

<success_criteria>
- Parity validation rejects verified STR-09, CFG-07, or ASIC-11 without explicit row-specific eligible Phase 30 proof.
- Current conservative checklist and no-promotion artifacts pass every repository check.
- Phase 30 has a final conclusion and complete Nyquist sampling record without claiming the three requirements passed.
- No hardware, credentials, ignored local evidence, or archived diagnostic execution occurred.
</success_criteria>
