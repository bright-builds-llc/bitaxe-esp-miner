---
phase: 26-telemetry-and-parity-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/bitaxe-stratum/src/v1/telemetry_projection.rs
  - crates/bitaxe-stratum/src/v1.rs
  - crates/bitaxe-stratum/BUILD.bazel
autonomous: true
requirements:
  - API-11
  - API-13
user_setup: []
must_haves:
  truths:
    - "Production-visible mining counters are derived from typed Phase 26 runtime events."
    - "Accepted and rejected counters advance only from current-generation SubmitIntent plus parsed pool response."
    - "Bounded statistics samples are marked only by runtime-event boundaries, never repeated request-time reads."
    - "Safe-stop events reset active mining, work submission, lifecycle, and stale event eligibility before serialization."
  artifacts:
    - path: "crates/bitaxe-stratum/src/v1/telemetry_projection.rs"
      provides: "Shared runtime-event projection, sequence/session guards, bounded sample marker contract, counter gate, safe-stop reset, and redaction-safe labels."
    - path: "crates/bitaxe-stratum/src/v1.rs"
      provides: "Public module export for the Phase 26 projection."
    - path: "crates/bitaxe-stratum/BUILD.bazel"
      provides: "Bazel source registration for the new projection module."
  key_links:
    - from: "crates/bitaxe-stratum/src/v1/telemetry_projection.rs"
      to: "crates/bitaxe-stratum/src/v1/production_work.rs"
      via: "PoolSessionGeneration and SubmitIntent prove current share context."
      pattern: "PoolSessionGeneration|SubmitIntent"
    - from: "crates/bitaxe-stratum/src/v1/telemetry_projection.rs"
      to: "crates/bitaxe-stratum/src/v1/submit_response.rs"
      via: "SubmitClassification is the only accepted/rejected share outcome gate."
      pattern: "SubmitClassification"
    - from: "crates/bitaxe-stratum/src/v1/telemetry_projection.rs"
      to: "crates/bitaxe-stratum/src/v1/state.rs"
      via: "Projection emits MiningRuntimeState after gate checks and safe-stop reset."
      pattern: "MiningRuntimeState"
    - from: "crates/bitaxe-stratum/src/v1/telemetry_projection.rs"
      to: "crates/bitaxe-api/src/runtime_projection.rs"
      via: "RuntimeProjectionSampleMarker is drained only from runtime-event sample boundaries before API statistics serialization."
      pattern: "RuntimeProjectionSampleMarker|drain_pending_sample_marker"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 26-2026-07-05T03-48-38
generated_at: 2026-07-05T03:57:00Z
---

<objective>
Create the shared Phase 26 runtime-event projection that all API, WebSocket, statistics, scoreboard, and parity-visible mining state must consume.

Purpose: Phase 26 decisions D-01 through D-04 and D-18 require one typed source of truth for counter legitimacy, stale-generation rejection, safe-stop reset, hashrate inputs, mining activity, pool lifecycle, fallback status, and blocker labels.
Output: A pure `telemetry_projection` module in `crates/bitaxe-stratum`, exported through Bazel and Rust modules with focused unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-telemetry-and-parity-closure/26-CONTEXT.md
@.planning/phases/26-telemetry-and-parity-closure/26-RESEARCH.md
@.planning/phases/26-telemetry-and-parity-closure/26-VALIDATION.md
@.planning/phases/24-bm1366-production-work-path/24-03-SUMMARY.md
@.planning/phases/25-live-stratum-runtime-and-safe-stop/25-01-SUMMARY.md
@.planning/phases/25-live-stratum-runtime-and-safe-stop/25-02-SUMMARY.md
@crates/bitaxe-stratum/src/v1/state.rs
@crates/bitaxe-stratum/src/v1/production_work.rs
@crates/bitaxe-stratum/src/v1/submit_response.rs
@crates/bitaxe-stratum/src/v1/live_runtime.rs
@crates/bitaxe-stratum/src/v1.rs
@crates/bitaxe-stratum/BUILD.bazel

<interfaces>
Existing contracts the executor must preserve:

- `MiningRuntimeState` owns `PoolLifecycleStatus`, `ShareCounters`, `HashrateInputs`, `MiningActivityStatus`, `WorkSubmissionGate`, fallback status, pool difficulty, and `maybe_blocked_reason`.
- `PoolSessionGeneration::initial()`, `.next()`, and `.raw()` identify pool-session generations; stale generations must not update production-visible counters.
- `SubmitIntent` carries the generation and ASIC job identity created only after production work result correlation.
- `SubmitClassification::{Accepted, Rejected, Timeout, Reconnect, Malformed, NoObservedShare, Blocked, Stopped}` is the Phase 25 parsed response classification boundary.
- Existing direct `record_accepted_share` and `record_rejected_share` helpers remain available for legacy deterministic tests, but Phase 26 production-visible paths must use the projection gate instead.
- Plan 26-01 must create `RuntimeProjectionSampleMarker` and `RuntimeProjectionSampleSource` as the cross-plan statistics sample contract. Plan 26-02 may consume a drained marker, but must not fabricate statistics rows from repeated request-time reads.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 26-01-01: Add typed telemetry projection contracts</name>
  <files>crates/bitaxe-stratum/src/v1/telemetry_projection.rs, crates/bitaxe-stratum/src/v1.rs, crates/bitaxe-stratum/BUILD.bazel</files>
  <read_first>
    - .planning/phases/26-telemetry-and-parity-closure/26-CONTEXT.md
    - .planning/phases/26-telemetry-and-parity-closure/26-RESEARCH.md
    - .planning/phases/26-telemetry-and-parity-closure/26-VALIDATION.md
    - crates/bitaxe-stratum/src/v1/telemetry_projection.rs
    - crates/bitaxe-stratum/src/v1/state.rs
    - crates/bitaxe-stratum/src/v1/production_work.rs
    - crates/bitaxe-stratum/src/v1/submit_response.rs
    - crates/bitaxe-stratum/src/v1/live_runtime.rs
    - crates/bitaxe-stratum/src/v1.rs
    - crates/bitaxe-stratum/BUILD.bazel
  </read_first>
  <behavior>
    - Test 1: folding `RuntimeTelemetryEvent::LifecycleChanged { sequence: 1, lifecycle: PoolLifecycleStatus::Active }` sets lifecycle active without changing share counters.
    - Test 2: folding `RuntimeTelemetryEvent::HashrateObserved { sequence: 2, inputs }` updates `HashrateInputs` and leaves accepted/rejected counters unchanged.
    - Test 3: folding `RuntimeTelemetryEvent::Blocked { sequence: 3, reason: "phase25_safe_stop" }` sets `work_submission=Blocked`, `mining_activity=SafeBlocked`, and carries the exact blocked reason.
    - Test 4: folding `RuntimeTelemetryEvent::BoundedSampleReady { sequence: 4, timestamp_ms, source }` creates one pending `RuntimeProjectionSampleMarker`, and a second request-time drain without another runtime event returns `None`.
    - Test 5: `Debug` output for projection events and state contains redaction-safe labels only and never raw pool, target, extranonce, share payload, socket, device, Wi-Fi, NVS, token, or BM1366 frame values.
  </behavior>
  <action>
    Create `crates/bitaxe-stratum/src/v1/telemetry_projection.rs` per D-01, D-03, D-04, D-07, D-12, and D-18. Define public types `RuntimeTelemetryProjection`, `RuntimeTelemetryEvent`, `RuntimeTelemetrySequence`, `RuntimeProjectionSampleMarker`, `RuntimeProjectionSampleSource`, and `ProjectionShareOutcome`.

    Use concrete event variants: `LifecycleChanged { sequence, lifecycle }`, `PoolDifficultyObserved { sequence, difficulty }`, `HashrateObserved { sequence, inputs }`, `FallbackChanged { sequence, active }`, `WorkSubmissionReady { sequence }`, `Blocked { sequence, reason }`, `BoundedSampleReady { sequence, timestamp_ms, source }`, `SubmitClassified { sequence, generation, classification }`, and `SafeStopped { sequence, reason }`. Store `last_sequence`, `current_generation`, `maybe_pending_sample_marker`, and a `MiningRuntimeState` inside `RuntimeTelemetryProjection`.

    Implement `RuntimeTelemetryProjection::new(generation: PoolSessionGeneration) -> Self`, `state(&self) -> &MiningRuntimeState`, `current_generation(&self) -> PoolSessionGeneration`, `fold(&mut self, event: RuntimeTelemetryEvent) -> ProjectionShareOutcome`, and `drain_pending_sample_marker(&mut self) -> Option<RuntimeProjectionSampleMarker>`. Ignore events whose sequence is not greater than `last_sequence`. `drain_pending_sample_marker` must return a marker at most once per `BoundedSampleReady` event, so repeated HTTP/statistics request reads cannot create duplicate samples. Export `pub mod telemetry_projection;` from `crates/bitaxe-stratum/src/v1.rs` and add `"src/v1/telemetry_projection.rs"` to `crates/bitaxe-stratum/BUILD.bazel`.
  </action>
  <verify>
    <automated>bazel test //crates/bitaxe-stratum:tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg "pub struct RuntimeTelemetryProjection|pub enum RuntimeTelemetryEvent|pub struct RuntimeTelemetrySequence|pub struct RuntimeProjectionSampleMarker|pub enum RuntimeProjectionSampleSource|pub enum ProjectionShareOutcome" crates/bitaxe-stratum/src/v1/telemetry_projection.rs` returns matches.
    - `rg "LifecycleChanged|PoolDifficultyObserved|HashrateObserved|FallbackChanged|WorkSubmissionReady|Blocked|BoundedSampleReady|SubmitClassified|SafeStopped" crates/bitaxe-stratum/src/v1/telemetry_projection.rs` returns matches.
    - `rg "last_sequence|current_generation|maybe_pending_sample_marker|MiningRuntimeState|drain_pending_sample_marker" crates/bitaxe-stratum/src/v1/telemetry_projection.rs` returns matches.
    - `rg "projection_sample_marker_drains_once_per_runtime_boundary|projection_repeated_request_reads_do_not_create_statistics_samples" crates/bitaxe-stratum/src/v1/telemetry_projection.rs` returns matches.
    - `rg "pub mod telemetry_projection;" crates/bitaxe-stratum/src/v1.rs` returns exactly one match.
    - `rg "\"src/v1/telemetry_projection.rs\"" crates/bitaxe-stratum/BUILD.bazel` returns exactly one match.
    - `rg "stratum[+]tcp://|poolURL|poolUser|poolPassword|target=|extranonce=|share_payload|socket_error|device_url|wifi|NVS|raw_bm1366_frame" crates/bitaxe-stratum/src/v1/telemetry_projection.rs` returns no matches outside redaction-denylist tests.
  </acceptance_criteria>
  <done>The Phase 26 projection contract exists, is exported, and can fold lifecycle, hashrate, bounded sample, blocked, fallback, and safe-stop events without endpoint-local state rules or request-time sample fabrication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 26-01-02: Gate share counters through SubmitClassification</name>
  <files>crates/bitaxe-stratum/src/v1/telemetry_projection.rs</files>
  <read_first>
    - .planning/phases/26-telemetry-and-parity-closure/26-CONTEXT.md
    - .planning/phases/26-telemetry-and-parity-closure/26-VALIDATION.md
    - crates/bitaxe-stratum/src/v1/telemetry_projection.rs
    - crates/bitaxe-stratum/src/v1/state.rs
    - crates/bitaxe-stratum/src/v1/production_work.rs
    - crates/bitaxe-stratum/src/v1/submit_response.rs
    - docs/parity/evidence/phase-25-live-stratum-runtime-and-safe-stop/share-outcome.md
  </read_first>
  <behavior>
    - Test 1: `SubmitClassified` with current generation and `SubmitClassification::Accepted` increments `ShareCounters.accepted` by exactly one and updates `maybe_best_difficulty` only from an explicit projection difficulty value.
    - Test 2: `SubmitClassified` with current generation and `SubmitClassification::Rejected { reason: PoolRejectedShare }` increments `ShareCounters.rejected` by exactly one and records `pool_rejected_share`.
    - Test 3: `NoObservedShare`, `Timeout`, `Reconnect`, `Malformed`, `Blocked`, `Stopped`, fake-pool-only, missing-intent, blocked-safe-prerequisite, and stale-generation classifications do not increment accepted or rejected counters.
    - Test 4: after `SafeStopped`, later lower-sequence or stale-generation accepted/rejected events do not reactivate mining or advance counters.
    - Test 5: share-counter folding does not create a statistics sample marker unless a separate runtime sample-boundary event is folded.
  </behavior>
  <action>
    Implement the counter gate per D-02, D-04, D-16, and D-18. `RuntimeTelemetryEvent::SubmitClassified` must require `generation == current_generation` and a `SubmitClassification::Accepted` or `SubmitClassification::Rejected` before it calls `MiningRuntimeState::record_accepted_share` or `MiningRuntimeState::record_rejected_share`.

    Add a concrete accepted-share difficulty input to the projection event, for example `maybe_share_difficulty: Option<ShareDifficulty>`, and use `ShareDifficulty::new(0.0)` only when a parsed accepted response is present but no better difficulty is available. Do not turn missing intent, fake-pool-only fixtures, stopped sockets, blocked prerequisites, stale generations, or implementation-only tests into production-visible accepted/rejected counters.

    Add focused Arrange/Act/Assert unit tests in `telemetry_projection.rs` for event folding, bounded sample markers, counter gating, stale-generation rejection, safe-stop reset, and redaction-safe rendering. Test names must mention the exact invariant, such as `projection_does_not_advance_counters_for_stale_generation`, `projection_safe_stop_prevents_stale_active_mining`, and `projection_submit_classification_does_not_emit_request_time_sample_marker`.
  </action>
  <verify>
    <automated>bazel test //crates/bitaxe-stratum:tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg "projection_does_not_advance_counters_for_stale_generation|projection_safe_stop_prevents_stale_active_mining|projection_advances_accepted_counter_only_for_current_generation|projection_advances_rejected_counter_with_redacted_reason|projection_submit_classification_does_not_emit_request_time_sample_marker" crates/bitaxe-stratum/src/v1/telemetry_projection.rs` returns matches.
    - `rg "record_accepted_share|record_rejected_share" crates/bitaxe-stratum/src/v1/telemetry_projection.rs` returns matches only inside the current-generation `SubmitClassification::Accepted` or `SubmitClassification::Rejected` gate.
    - `rg "NoObservedShare|Timeout|Reconnect|Malformed|Blocked|Stopped|stale_generation|submit_intent_missing|blocked_safe_prerequisite" crates/bitaxe-stratum/src/v1/telemetry_projection.rs` returns matches in tests or gate logic.
    - `rg "pool_rejected_share|unknown_rejected_share" crates/bitaxe-stratum/src/v1/telemetry_projection.rs` returns matches.
    - `bazel test //crates/bitaxe-stratum:tests` passes.
  </acceptance_criteria>
  <done>API-13 counter invariants are enforced in a pure shared projection before any API, statistics, scoreboard, WebSocket, or parity-visible consumer can serialize mining state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Runtime event producer -> projection core | Socket, ASIC, fake-pool, and firmware shell observations enter the typed pure state fold. |
| Projection core -> API/WebSocket/evidence consumers | Production-visible counters, mining activity, blocker labels, and hashrate fields become serializable state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-26-01 | Tampering | `RuntimeTelemetryProjection::fold` | mitigate | Reject non-monotonic sequences and stale `PoolSessionGeneration` events before state mutation. |
| T-26-02 | Tampering | Share counter gate | mitigate | Increment accepted/rejected counters only for current-generation `SubmitClassification::Accepted` or `Rejected` events. |
| T-26-03 | Repudiation | Safe-stop state reset | mitigate | Unit-test that safe-stop blocks work submission, marks mining safe-blocked, disconnects lifecycle, and prevents stale active-mining projection. |
| T-26-04 | Information Disclosure | Projection debug/log labels | mitigate | Keep `Debug` and labels redaction-safe; tests must reject raw pool, target, extranonce, share payload, socket, device, Wi-Fi, NVS, token, and BM1366 frame values. |
</threat_model>

<verification>
Run `bazel test //crates/bitaxe-stratum:tests`.
</verification>

<success_criteria>
Plan 26-01 is complete when `telemetry_projection.rs` exists, is exported through `v1.rs` and Bazel, all projection tests pass, accepted/rejected counters advance only through current-generation parsed submit classifications, and safe-stop/stale-generation tests prove no stale active mining state survives the projection.
</success_criteria>

<output>
After completion, create `.planning/phases/26-telemetry-and-parity-closure/26-01-SUMMARY.md`.
</output>
