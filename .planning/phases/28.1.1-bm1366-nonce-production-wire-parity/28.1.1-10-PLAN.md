---
phase: 28.1.1-bm1366-nonce-production-wire-parity
plan: 10
type: execute
gap_closure: true
wave: 9
depends_on: ["28.1.1-09"]
files_modified:
  - crates/bitaxe-api/src/logs.rs
  - firmware/bitaxe/src/log_buffer.rs
  - firmware/bitaxe/src/live_stratum_runtime.rs
  - scripts/phase28.1.1-accepted-state-lifecycle-compare.mjs
  - scripts/phase28.1.1-accepted-state-lifecycle-compare-test.mjs
  - scripts/phase28.1.1-accepted-state-diagnostic.sh
  - scripts/phase28.1.1-accepted-state-diagnostic-test.sh
  - .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-UAT.md
  - .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-accepted-state-lifecycle-redacted.md
  - .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md
  - .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-10-SUMMARY.md
  - .planning/debug/phase28.1.1-cold-start-capture-gap.md
  - .planning/STATE.md
autonomous: true
requirements: [STR-09, ASIC-11, CFG-07]
user_setup:
  - "At the hardware checkpoint, disconnect both barrel/DC power and USB, wait at least five seconds, restore barrel power, and reconnect USB immediately when prompted."
must_haves:
  truths:
    - "Cold-start evidence replays already-retained closed categories during a bounded non-blocking window; it never repeats an ASIC read or delays initialization"
    - "After the retained-package checkpoint is armed, the cold-start branch performs no package, flash, reset, board-info, network discovery, or credential read until capture completes"
    - "A lifecycle comparison requires exactly five stage keys, accepts only equivalent duplicates, and promotes no raw, credential, network, or endpoint values"
    - "Phase 30 remains pending unless the existing same-chain result/share gate passes"
  artifacts:
    - path: firmware/bitaxe/src/log_buffer.rs
      provides: "Strict extraction of retained accepted-state category markers"
      contains: "accepted_state"
    - path: crates/bitaxe-api/src/logs.rs
      provides: "Host-tested exact-line selection and bounded replay cadence"
      contains: "replay"
    - path: scripts/phase28.1.1-accepted-state-diagnostic.sh
      provides: "No-flash cold-start reattach capture path"
      contains: "lifecycle"
    - path: .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-accepted-state-lifecycle-redacted.md
      provides: "Category-only reinit versus cold-start comparison"
      contains: "lifecycle_status"
  key_links:
    - from: firmware/bitaxe/src/live_stratum_runtime.rs
      to: firmware/bitaxe/src/log_buffer.rs
      via: "bounded diagnostic replay cadence after production listener readiness"
      pattern: "listener_armed|accepted_state"
    - from: scripts/phase28.1.1-accepted-state-diagnostic.sh
      to: scripts/phase28.1.1-accepted-state-lifecycle-compare.mjs
      via: "five-stage normalized reinit/cold-start inputs"
      pattern: "lifecycle"
generated_by: gsd-plan-phase
lifecycle_mode: gaps
phase_lifecycle_id: 28.1.1-2026-07-10T02-30-00
generated_at: 2026-07-10T02:30:00Z
---

<objective>
Close the Phase 28.1.1 physical cold-start UAT gap without changing ASIC initialization or production behavior.

Purpose: make the existing category-only accepted-state observations observable after native USB re-enumeration, then compare a retained-package cold start with the exact-commit flash/reinit member.

Output: diagnostic-only category replay, a genuinely no-flash lifecycle capture path, deterministic tests, and detector-gated redacted lifecycle evidence.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-UAT.md
@.planning/debug/phase28.1.1-cold-start-capture-gap.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VALIDATION.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md

<interfaces>
Accepted-state stages remain the closed set `post_enumerate`, `post_mining_ready`, `post_max_baud`, `post_mask_reload`, and `post_first_work`.

Replay is transport-only: replay exact retained category markers with `log::info!`, never `info_retained`, so replay does not recursively append or trigger another ASIC read. Use a host-tested bounded cadence from listener readiness for at least 60 seconds at a small fixed interval; drive it from the existing bridge loop without a sleep, task, thread, or initialization delay.

`lifecycle_status: match | mismatch | unavailable`. A valid match requires the stage set to equal the five closed keys on both exact-commit members, `redacted=true` on every marker, byte/field-equivalent duplicates only, no synthetic unavailable marker, and no stack-overflow, panic, or software-reset marker in the cold-start member.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add bounded retained accepted-state category replay</name>
  <files>crates/bitaxe-api/src/logs.rs, firmware/bitaxe/src/log_buffer.rs, firmware/bitaxe/src/live_stratum_runtime.rs</files>
  <read_first>crates/bitaxe-api/src/logs.rs, firmware/bitaxe/src/log_buffer.rs, firmware/bitaxe/src/asic_adapter.rs, firmware/bitaxe/src/live_stratum_runtime.rs, crates/bitaxe-asic/src/bm1366/accepted_state.rs</read_first>
  <action>Add host-testable retained-log utilities in `bitaxe-api`: exact first-token line selection and a timestamp-input bounded replay cadence with a window of at least 60 seconds and a fixed small interval. Tests must prove not-due/due/exhausted behavior, exact token matching, complete-line handling, equivalent duplicate preservation for downstream validation, and secret/noise exclusion. Wire the firmware retained buffer through these utilities. At the existing Phase 27 `listener_armed` transition, and only when `accepted_state_snapshot` investigation mode is enabled, arm the cadence. On due bridge-loop iterations, emit selected retained category lines with `log::info!` only. Do not call `info_retained`, repeat register reads, sleep, add a task/thread, change initialization timing, or enable this in default firmware. Preserve later post-mask/post-first-work observations at their original boundaries.</action>
  <verify><automated>cargo test -p bitaxe-api logs && cargo test -p bitaxe-asic accepted_state && cargo check -p bitaxe-firmware --all-features --target xtensa-esp32s3-espidf</automated></verify>
  <acceptance_criteria>Host tests exercise line selection and every replay-cadence boundary; ESP32 wiring compiles; default mode emits nothing; the replay window exceeds the wrapper's tested reappearance-and-attach timeout; no ASIC read/init/global pthread behavior changes.</acceptance_criteria>
  <done>Boot-owned category observations remain available throughout a bounded post-enumeration attachment window without changing the observed hardware lifecycle.</done>
</task>

<task type="auto">
  <name>Task 2: Add a true retained-package lifecycle capture and comparator</name>
  <files>scripts/phase28.1.1-accepted-state-lifecycle-compare.mjs, scripts/phase28.1.1-accepted-state-lifecycle-compare-test.mjs, scripts/phase28.1.1-accepted-state-diagnostic.sh, scripts/phase28.1.1-accepted-state-diagnostic-test.sh</files>
  <read_first>scripts/phase28.1.1-accepted-state-diagnostic.sh, scripts/phase28.1.1-accepted-state-diagnostic-test.sh, scripts/phase13-monitor-capture.sh, scripts/phase28.1.1-accepted-state-compare.mjs, .planning/debug/phase28.1.1-cold-start-capture-gap.md</read_first>
  <action>Create a lifecycle comparator that imports/reuses the accepted-state parser, requires the stage set to equal the five closed keys exactly, accepts duplicate stage markers only when all parsed fields are equivalent, fails on conflicting duplicates/missing stages/synthetic unavailable observations/`redacted!=true`, compares reinit and cold-start categories, and renders only status/count/boolean/category output. Refactor the diagnostic wrapper so `accepted-state` remains the exact-commit package/flash/reinit member while `lifecycle` becomes a retained-package cold-start member. Define the boundary explicitly: detector/reference/package-identity checks happen before the operator arms the retained-package checkpoint; from arming through capture completion there is no package, flash, reset, board-info, network discovery, or credential access; post-capture detector is allowed. Require the previously detector-gated port to disappear and reappear within a tested timeout shorter than the firmware replay window, attach `espflash monitor --no-reset` for at least 360 seconds, normalize exactly five stages, reject stack-overflow/panic/software-reset loops, run the lifecycle comparator, and write only redacted output. Add injectable shell/Node fixtures proving routing boundaries, disappearance/reappearance timeout/failure, port mismatch, equivalent/conflicting duplicates, exact stage-set enforcement, unavailable-marker rejection, secret-noise exclusion, and no credential access.</action>
  <verify><automated>node scripts/phase28.1.1-accepted-state-lifecycle-compare-test.mjs && bash scripts/phase28.1.1-accepted-state-diagnostic-test.sh && shellcheck scripts/phase28.1.1-accepted-state-diagnostic.sh scripts/phase28.1.1-accepted-state-diagnostic-test.sh</automated></verify>
  <acceptance_criteria>All deterministic tests pass; from arming through capture completion lifecycle hardware routing cannot invoke package/flash/reset/board-info/discovery/credential paths; all promoted output is closed and redacted.</acceptance_criteria>
  <done>The repo can represent the two lifecycle members truthfully: exact-commit reinit by flash, then retained-package physical cold start by no-reset reattach.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: Execute exact-commit reinit and physical cold-start evidence</name>
  <files>.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-UAT.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-accepted-state-lifecycle-redacted.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-10-SUMMARY.md, .planning/debug/phase28.1.1-cold-start-capture-gap.md, .planning/STATE.md</files>
  <read_first>.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-UAT.md, .planning/debug/phase28.1.1-cold-start-capture-gap.md, AGENTS.md, standards/core/verification.md</read_first>
  <action>Run focused tests and the mandatory Rust pre-commit sequence in order. Review the implementation diff and commit the clean implementation locally before packaging so the manifest source commit is exact. Build the accepted-state package, verify reference cleanliness, and pass the detector gate. Capture the reinit member for at least 360 seconds with redaction enabled and leave that exact package retained. Complete the lifecycle preflight, then arm the no-flash checkpoint. Ask the user to remove both barrel/DC and USB power for at least five seconds, then restore barrel power and reconnect USB immediately. Capture at least 360 seconds without reset/reflash; only after capture run post-run `just detect-ultra205`. Require one stable boot, listener readiness, the exact five-stage set with equivalent duplicates only, zero stack-overflow/panic/software-reset markers, matching manifest/source identity, and a category-only lifecycle result. If any prerequisite or stage is absent, record `blocked_safe_prerequisite` or `gaps_found`; do not improvise a reset, erase, scan, or stale target. Update UAT, debug session, lifecycle evidence, VERIFICATION (including Plan 10 lifecycle ID), STATE, and `28.1.1-10-SUMMARY.md`. Run a promoted-evidence denylist, confirm `docs/parity/checklist.md` is unchanged, and run the formal GSD lifecycle validator. Commit evidence atomically, push `main`, then verify a clean worktree and zero ahead/behind. Keep Phase 30 pending unless the separate existing same-chain result/share gate passes.</action>
  <verify><automated>cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo build --all-targets --all-features && cargo test --all-features && node scripts/phase28.1.1-accepted-state-lifecycle-compare-test.mjs && bash scripts/phase28.1.1-accepted-state-diagnostic-test.sh && just verify-reference && ! rg -n -i "pool(url|user|password)|wifi-credentials|pool-credentials|device_url: [^[]|https?://|ssid:|mac address:|password:|token:|(hex|raw_value|register_value|frame_value|frame_hex|uart_hex|nonce_hex|target_hex)[=:][[:space:]]*(0x)?[0-9a-f]{6,}" .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-accepted-state-lifecycle-redacted.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-UAT.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-10-SUMMARY.md .planning/debug/phase28.1.1-cold-start-capture-gap.md .planning/STATE.md && node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 28.1.1 --require-plans --require-verification --raw && git diff --check</automated></verify>
  <acceptance_criteria>Host and firmware gates pass; implementation commit equals manifest source commit; reinit and cold-start members use that same package; cold-start capture is no-flash/no-reset and complete; denylist and lifecycle validator pass; evidence commit is pushed to main; worktree is clean and origin/main has zero ahead/behind.</acceptance_criteria>
  <done>Phase 28.1.1 physical cold-start UAT is passed or conservatively closed with exact evidence, summary and lifecycle validation are complete, and both implementation and evidence commits are pushed cleanly.</done>
</task>
</tasks>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation | Blocking condition |
| --- | --- | --- | --- |
| Replay changes observed ASIC behavior | critical | Replay retained categories only after init; no reads/sleeps/tasks | Any ASIC command or init timing changes |
| Cold-start member silently reflashes or resets | critical | Separate lifecycle branch and negative routing fixtures | Any package/flash/reset/board-info call from arming through capture completion |
| USB race yields partial evidence | high | Reattach state machine plus five-stage requirement | Fewer than five unique stages |
| Secrets or raw values enter evidence | high | Exact marker selection, denylist, ignored raw logs | Any sensitive/raw value is tracked |
| Phase 30 overpromotion | high | Preserve existing same-chain gate | Correlation/share proof is absent |
</threat_model>

<verification>
Review the implementation diff for transport-only scope. Run every Task 3 command. Confirm `docs/parity/checklist.md` remains unchanged, the package manifest names the implementation commit, the cold-start branch contains no flash/reset path, and post-capture board-info runs only after the no-reset evidence window.
</verification>

<success_criteria>
The corrected accepted-state package produces a truthful, redacted, five-stage physical cold-start lifecycle comparison after total power loss, with no observer-induced ASIC timing change and no Phase 30 promotion beyond existing evidence.
</success_criteria>

<output>After completion, create `28.1.1-10-SUMMARY.md` and update UAT, verification, and state with the exact result.</output>
