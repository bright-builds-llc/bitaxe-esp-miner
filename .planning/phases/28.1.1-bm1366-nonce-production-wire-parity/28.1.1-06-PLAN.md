---
phase: 28.1.1-bm1366-nonce-production-wire-parity
plan: 06
type: execute
gap_closure: true
wave: 5
depends_on: []
files_modified:
  - crates/bitaxe-asic/src/bm1366.rs
  - crates/bitaxe-asic/src/bm1366/accepted_state.rs
  - scripts/phase28.1.1-accepted-state-compare.mjs
  - scripts/phase28.1.1-accepted-state-compare-test.mjs
autonomous: true
requirements: [ASIC-11, CFG-07]
user_setup: []
must_haves:
  truths:
    - "Accepted-state classification is pure data-in/data-out logic; firmware and scripts do not own the decision rules"
    - "The comparator reports only counts, booleans, stages, and closed-set categories; it never renders raw register values or wire frames"
    - "Recommendations cannot reopen any sole-blocker hypothesis banned by CONTEXT D-03"
  artifacts:
    - path: crates/bitaxe-asic/src/bm1366/accepted_state.rs
      provides: "Typed stage snapshots and deterministic accepted-state classification"
      contains: "AcceptedStateClassification"
    - path: scripts/phase28.1.1-accepted-state-compare.mjs
      provides: "Redaction-safe upstream/Rust accepted-state comparator"
      contains: "recommended_investigation"
    - path: scripts/phase28.1.1-accepted-state-compare-test.mjs
      provides: "Fixture coverage for match, mismatch, unavailable, and strict routing"
      contains: "accepted_state_compare_test"
  key_links:
    - from: scripts/phase28.1.1-accepted-state-compare.mjs
      to: crates/bitaxe-asic/src/bm1366/accepted_state.rs
      via: "shared stage and classification vocabulary"
      pattern: "post_enumerate|post_mining_ready|post_max_baud|post_mask_reload|post_first_work"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1-2026-07-09T19-24-27
generated_at: 2026-07-09T19:34:00Z
---

<objective>
Build the observation-only functional core for comparing upstream and Rust BM1366 accepted hashing state.

Purpose: after seven wire-level sole-blocker falsifications, classify whether safe-readable BM1366 state transitions and hashing counters show that transmitted commands became an accepted hashing state. Keep raw values local and make the next investigation a closed-set evidence decision.

Output: typed Rust state/classification model plus a deterministic redaction-safe comparator and fixture tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-CONTEXT.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VALIDATION.md
@.planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-VERIFICATION.md
@crates/bitaxe-asic/src/bm1366/registers.rs
@crates/bitaxe-asic/src/bm1366/result.rs
@scripts/phase28.1.1.7-asic-mask-reload-compare.mjs

<interfaces>
Closed stage vocabulary:
`post_enumerate`, `post_mining_ready`, `post_max_baud`, `post_mask_reload`, `post_first_work`.

Closed comparison values:
`match`, `mismatch`, `unavailable`.

Closed recommendation values:
`accepted_state_transition_divergence`, `cold_boot_recovery_lifecycle_parity`, `upstream_init_transcript_prefix_bisection`, `none`.

Required report fields:
`accepted_state_status`, `first_divergent_stage`, `chip_count_class`, `hash_counter_activity_upstream`, `hash_counter_activity_rust`, `power_delta_class`, `result_correlated`, `fake_pool_submit_observed`, `recommended_investigation`, `redacted`.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add typed accepted-state snapshots and classification</name>
  <files>crates/bitaxe-asic/src/bm1366.rs, crates/bitaxe-asic/src/bm1366/accepted_state.rs</files>
  <read_first>crates/bitaxe-asic/src/bm1366.rs, crates/bitaxe-asic/src/bm1366/registers.rs, crates/bitaxe-asic/src/bm1366/result.rs, standards/core/architecture.md, standards/core/testing.md</read_first>
  <action>Create `accepted_state.rs` and export it from `bm1366.rs`. Define public enums for the five stages and `Match | Mismatch | Unavailable`; define a snapshot containing stage plus category/count inputs only (chip-count class, readable-response count, error/domain/total counter activity booleans, power class, result-correlated boolean, submit-observed boolean). Define `AcceptedStateClassification` with first divergent stage and the four-value recommendation. Implement a pure classifier: any upstream-active/Rust-inactive safe-readable counter difference selects `accepted_state_transition_divergence`; missing safe-readable observations select `cold_boot_recovery_lifecycle_parity`; state match with no correlate selects `cold_boot_recovery_lifecycle_parity`; correlate/submit selects `none`. Add focused Arrange/Act/Assert tests for each branch. Do not store credential, network, raw UART, or raw register-value strings in these types.</action>
  <verify><automated>cargo test -p bitaxe-asic accepted_state</automated></verify>
  <acceptance_criteria>`crates/bitaxe-asic/src/bm1366.rs` contains `pub mod accepted_state;`; all four recommendation branches have focused unit tests; `cargo test -p bitaxe-asic accepted_state` exits 0; `rg -n "pool|password|endpoint|raw_frame|raw_register" crates/bitaxe-asic/src/bm1366/accepted_state.rs` returns no sensitive field definition.</acceptance_criteria>
  <done>The typed accepted-state model is exported, focused tests pass, and its public surface contains category/count inputs only.</done>
</task>

<task type="auto">
  <name>Task 2: Add closed-set redacted comparator and fixture tests</name>
  <files>scripts/phase28.1.1-accepted-state-compare.mjs, scripts/phase28.1.1-accepted-state-compare-test.mjs</files>
  <read_first>scripts/phase28.1.1.7-asic-mask-reload-compare.mjs, scripts/phase28.1.1.7-asic-mask-reload-compare-test.mjs, crates/bitaxe-asic/src/bm1366/accepted_state.rs, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-CONTEXT.md</read_first>
  <action>Create a Node comparator that accepts `--upstream-log`, `--rust-log`, and `--out`; parses only named `accepted_state_snapshot` markers and existing power/correlate/submit markers; emits exactly the required report fields and `redacted: true`; never copies source lines or numeric register values. Reject unknown stage/category values. Hard-code the D-03 banned hypothesis set and assert `recommended_investigation` can never equal a banned value. Create fixture-driven tests for active-vs-inactive mismatch, missing observations, matching idle state, correlate/submit success, unknown-category rejection, and a denylist source line whose secret-like text never appears in output.</action>
  <verify><automated>node scripts/phase28.1.1-accepted-state-compare-test.mjs</automated></verify>
  <acceptance_criteria>The comparator output contains every required field and `redacted: true`; test output contains `accepted_state_compare_test: passed`; all six cases exit 0; no output includes raw source lines or any D-03 banned recommendation.</acceptance_criteria>
  <done>The comparator deterministically routes every fixture through the closed recommendation set without copying sensitive source data.</done>
</task>
</tasks>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation | Blocking condition |
| --- | --- | --- | --- |
| Raw register/UART/pool data reaches committed reports | high | Category-only types, no source-line echo, denylist fixture | Any raw value or secret-like source text appears in comparator output |
| Comparator reopens an exhausted hypothesis | high | Closed recommendation enum plus explicit banned-set test | Any D-03 value is emitted |
| Infrastructure owns classification rules | medium | Pure `bitaxe-asic` classifier with focused tests | Script and Rust routing disagree |
</threat_model>

<verification>
Run `cargo test -p bitaxe-asic accepted_state`, the Node comparator test, `cargo clippy -p bitaxe-asic --all-targets --all-features -- -D warnings`, and `git diff --check`.
</verification>

<success_criteria>
The phase has a tested, redaction-safe, closed-set accepted-state classifier and comparator that can name the first architecture-level divergence without another firmware behavior patch.
</success_criteria>

<output>After completion, create `28.1.1-06-SUMMARY.md`.</output>
