# Phase 28.1.1 Wire Diff Summary (Field-Level, Redacted)

Date: 2026-07-07  
Board: Ultra 205  
Reference pin: `c1915b0a63bfabebdb95a515cedfee05146c1d50`  
Hardware runs: `wire-parity-run-04`, `wire-parity-run-05` (local, gitignored raw logs)

## Init Sequence

| Category | Upstream anchor | Rust capture | Verdict |
| --- | --- | --- | --- |
| Chip-detect prelude | 3× version mask before enumerate (`bm1366.c:195-198`) | 3× `55 aa 51 09 00 a4 90 00 ff ff` prelude + `55 aa 52 05 00 00 0a` enumerate | **match** |
| Static init frames | `BM1366_init` register writes | `asic_uart_trace` init TX matches `upstream_init_frames` fixtures | **match** |
| Dynamic difficulty mask (reg 0x14) | `get_difficulty_mask` @ difficulty 1000 | Frame `55 aa 51 09 00 14 00 00 80 ff 04` | **match** |
| Dynamic PLL (reg 0x08) | `BM1366_send_hash_frequency` @ 485 MHz class | Frame `55 aa 51 09 00 08 50 c2 02 40 1c` | **match** |
| Dynamic HCN (reg 0x10) | `BM1366_set_nonce_space` uses post-PLL frequency | Frame payload ends `00 0d 32 24 10` after HCN fix | **match** |
| Post-init register read | Chip identity response | `asic_production_trace=register_read_parsed` post-init and post-dispatch | **match** (parser fix) |

## Job Frame Structure

| Field | Upstream `BM1366_job` | Rust `Bm1366WorkPayload` | Verdict |
| --- | --- | --- | --- |
| Header | `TYPE_JOB \| GROUP_SINGLE \| CMD_WRITE` (`0x21`) | Same (`packet.rs`) | **match** |
| Length | 88 bytes total | `len=88` in UART trace | **match** |
| `num_midstates` | Forced to `0x01` in `BM1366_send_work` | `0x01` | **match** |
| Field order | job_id, midstates, nonce, nbits, ntime, merkle, prev, version | Same layout (`work.rs`) | **match** |
| Merkle/prev transforms | `construct_bm_job` word/endian rules (`mining.c:76-81`) | `reverse_32bit_words` + `reverse_endianness_per_word` | **match** (code review) |

## Job Id Sequencing (Fix Applied)

| Behavior | Upstream `BM1366_send_work` | Rust before fix | Rust after fix |
| --- | --- | --- | --- |
| First production job id | `(0+8)%128 = 8` pre-increment | First notify used id `0` | `next_asic_job_id()` pre-increments (`live_runtime.rs`) |
| Stride | +8 mod 128 | +8 mod 128 | **match** |

Run-05 capture still showed first pool frame with job id `0` because the flashed image predated the host-side rebuild; run-06 reflash blocked on transient USB port busy.

## Stratum Handshake (Fix Applied)

| Step | Upstream | Rust before | Rust after |
| --- | --- | --- | --- |
| Version rolling | `mining.configure` before subscribe | Not sent | `ConfigureVersionRolling` id=1 on `start()` |
| Nonzero version mask in work build | Allowed (base notify version in job packet) | Fail-closed error | Mask stored; work builds with notify version |

## Remaining Divergence / Open Items

1. **No upstream SERIALTX_DEBUG golden from scratch build** — scratch tree prepared under `scratch/upstream-wire-capture/`; operator-local ESP-IDF C build required for byte-level job-field confirmation against live pool notify.
2. **Hardware hashing still absent** on run-05 and the post-fix rerun: the post-fix run recorded 165 work-dispatch markers, 157 redispatch markers, 2 register-read parser proofs, zero `result_correlated`, zero `share_submission_status=accepted|rejected`, and flat/negative power classification after 360 s with pool bridge applied.
3. **No new field-level job divergence identified** after the post-fix rerun. Additional job-field edits need upstream scratch golden job-byte confirmation first.

## Conclusion

Init wire bytes and job frame encoding align with current upstream references. Fixes landed for chip-identity register-read parsing, HCN from actual PLL frequency, upstream-aligned first job id stride, and `mining.configure` negotiation. The post-fix hardware rerun still did not produce a correlated nonce or accepted/rejected share. Live nonce production remains blocked pending upstream scratch golden job-byte confirmation or another concrete field-level divergence.
