---
phase: 28.1.1-bm1366-nonce-production-wire-parity
reviewed: 2026-07-11T02:27:38Z
depth: focused-gap-closure-plus-independent-review
files_reviewed: 13
files_reviewed_list:
  - crates/bitaxe-stratum/src/v1/live_runtime.rs
  - crates/bitaxe-api/src/logs.rs
  - crates/bitaxe-asic/src/bm1366/accepted_state.rs
  - scripts/phase28.1.1-accepted-state-compare.mjs
  - scripts/phase28.1.1-accepted-state-compare-test.mjs
  - scripts/phase28.1.1-promoted-evidence-denylist.sh
  - scripts/phase28.1.1-promoted-evidence-denylist-test.sh
  - scripts/phase28.1.1-accepted-state-diagnostic.sh
  - scripts/phase28.1.1-accepted-state-diagnostic-test.sh
  - scripts/phase13-monitor-capture.sh
  - scripts/phase13-monitor-capture-test.sh
  - scripts/process-group.sh
  - scripts/BUILD.bazel
findings:
  critical: 0
  warning: 0
  info: 0
  total: 0
resolved_findings:
  critical: 1
  warning: 2
  total: 3
status: passed
---

# Phase 28.1.1: Focused Gap-Closure Code Review

**Reviewed:** 2026-07-11T02:27:38Z

**Depth:** focused review of WR-01 through WR-03, Task 1 formatting isolation, and independent review fixes in `ab7f5b9`

**Status:** passed

## Summary

The three Plan 10 review warnings are closed with deterministic regression coverage. The lifecycle wrapper now separates operator attestation from host-observed USB absence, measures at least 5000 ms with an injectable monotonic clock, uses closed response tokens and deadlines, and preserves the no-package/no-flash/no-reset boundary after arming. Replay now has a 180000 ms window with a 2000 ms cadence and a tested replay at 72000 ms after the 60000 ms reattach plus 10000 ms monitor-start budget. Rust and Node classifiers require all five accepted-state stages before they can report `match`.

The later independent review found three additional issues before final closure: an orphaned descendant monitor could outlive direct-child cleanup, a token read could begin before but complete at or after its deadline, and the Rust classifier allowed mismatch or result progress to outrank an unavailable observation. Commit `ab7f5b9` resolves all three with shared process-group cleanup, a post-read monotonic deadline check, and unavailable-first classification/recommendation precedence. Focused regressions exercise timeout, TERM, normal-exit, detached-descendant cancellation, token-at-deadline rejection, and unavailable combined with both mismatch and result progress. No review finding remains open.

The formatting correction remains isolated in commit `1fe4918`, which changes only `crates/bitaxe-stratum/src/v1/live_runtime.rs` and contains the two rustfmt-recorded layout edits. No firmware ASIC read, task, thread, sleep, initialization-order, or default-mode behavior changed in Plans 11 Tasks 1-3.

Material guidance applied: the Ultra 205 detector, timeout, credential-path, redaction, hardware-evidence, and frontmatter rules in `AGENTS.md`; `AGENTS.bright-builds.md`; `standards-overrides.md`; and the Bright Builds architecture, code-shape, verification, testing, and Rust standards.

## Resolved Warnings

### WR-01: Resolved — measured both-power lifecycle handshake

- The wrapper waits for exact `both-power-paths-removed` attestation, then requires the selected USB port to be absent immediately and remain unobserved for at least 5000 monotonic milliseconds.
- Evidence records `operator_attested_both_power_paths_removed: true`, the USB absence measurement, and `barrel_removal_electronically_verified: false` as separate claims.
- Exact fixtures reject 4999 ms, early reappearance, wrong/absent tokens, abandoned waits, and a port still present at attestation; exactly 5000 ms passes.
- Command evidence: `bash scripts/phase28.1.1-accepted-state-diagnostic-test.sh` passed.

### WR-02: Resolved — replay remains observable after latest attachment

- Constants are closed at 180000 ms replay window, 2000 ms cadence, 60000 ms reattach deadline, and 10000 ms monitor-start reserve.
- The host test consumes every cadence through 70000 ms, proves another replay at 72000 ms, continues through the bounded schedule, and proves expiry at 180000 ms.
- Lifecycle fixtures accept selected-port presence at exactly 60000 ms, reject absence at exactly 60000 ms, and reject first presence at 60001 ms.
- Command evidence: `cargo test -p bitaxe-api logs` and `bash scripts/phase28.1.1-accepted-state-diagnostic-test.sh` passed.

### WR-03: Resolved — incomplete accepted-state inputs fail closed

- Rust and Node comparisons mark empty, matching three-of-five, and one-sided incomplete members `unavailable` and identify the first missing stage.
- Complete fixtures preserve the previous match, mismatch, unavailable-observation, result-progress, and closed recommendation behavior.
- The accepted-state wrapper withholds an armable manifest/log unless the reinit member has one boot, one listener-ready marker, zero hazards, and a strict five-stage self-comparison match.
- Command evidence: `cargo test -p bitaxe-asic accepted_state`, `node scripts/phase28.1.1-accepted-state-compare-test.mjs`, and `node scripts/phase28.1.1-accepted-state-lifecycle-compare-test.mjs` passed.

## Related Evidence-Gate Repair

The promoted-evidence denylist now handles search results explicitly: status 0 blocks a forbidden match, status 1 is the only passing state, and malformed regex or injected tool errors fail. Its fixtures cover clean, forbidden-sentinel, malformed-pattern, and tool-error outcomes without real secret values.

Command evidence: `bash scripts/phase28.1.1-promoted-evidence-denylist-test.sh`, `shellcheck` over the four changed shell scripts, and `shfmt -d` over those scripts passed.

## Independent Review Findings Resolved in `ab7f5b9`

### IR-01: Resolved — orphan descendant watcher cleanup

- Prior cleanup tracked the immediate monitor child only; a detached descendant could survive timeout, cancellation, or wrapper exit.
- `scripts/process-group.sh` now starts bounded watchers in isolated process groups and terminates the group with a verified TERM/KILL fallback. Phase 13 also persists descendant-group state so the lifecycle wrapper can terminate a detached monitor descendant before clearing child state.
- Command evidence: `bash scripts/phase13-monitor-capture-test.sh` passed timeout, TERM, and normal-exit descendant cases; `bash scripts/phase28.1.1-accepted-state-diagnostic-test.sh` passed detached-descendant lifecycle cancellation and proved no live child state remained.

### IR-02: Resolved — token crossing the deadline

- A read previously validated the deadline only before blocking, so a token returned at or beyond the deadline could be accepted.
- The token loop now samples the monotonic clock again after a successful read and requires `now_ms < deadline_ms` before validating the token.
- Command evidence: the lifecycle fixture advances from 299999 ms before read to 300000 ms after read and verifies timeout with no accepted attestation.

### IR-03: Resolved — Rust unavailable precedence

- A missing observation could previously be masked by mismatch status or result progress, producing a non-lifecycle recommendation despite incomplete evidence.
- `classify_accepted_state` now gives missing observations first priority for both `AcceptedStateStatus::Unavailable` and `ColdBootRecoveryLifecycleParity`.
- Command evidence: `cargo test -p bitaxe-asic accepted_state` includes separate unavailable-plus-mismatch and unavailable-plus-result-progress regressions.

## Verification

- `cargo fmt --all -- --check` — passed.
- `cargo clippy --all-targets --all-features -- -D warnings` — passed.
- `cargo build --all-targets --all-features` — passed.
- `cargo test --all-features` — passed.
- `cargo test -p bitaxe-api logs` — 17 passed.
- `cargo test -p bitaxe-asic accepted_state` — 11 passed, including the two unavailable-precedence regressions.
- `bash scripts/phase13-monitor-capture-test.sh` — passed, including process-tree timeout, TERM, and exit cleanup.
- `bash scripts/phase28.1.1-accepted-state-diagnostic-test.sh` — passed, including deadline-crossing and detached-descendant cancellation.
- Both accepted-state Node comparator suites — passed.
- Promoted-evidence denylist and lifecycle diagnostic shell suites — passed.
- `git show --format= --name-only 1fe4918` — only `crates/bitaxe-stratum/src/v1/live_runtime.rs`.
- `git diff --check` — passed.

## Task 4 Closure Review

The hardware closure remains conservative. The strict five-stage 360-second reinit candidate belongs to `4e2d165`; commit `d275a0e` changed the lifecycle polling surface, so that candidate could not establish hardware-attempt-head identity. On `d275a0e`, the initial detector failed `board-info`, the USB-replug recovery was consumed, and the both-power recovery checkpoint expired before continuation validation. Independent review fix `ab7f5b9` changed classifier and process-cleanup code again without hardware access, so neither older package/checkpoint identity can establish exact review-fix-head or later documentation-closure identity.

A continuation called the bounded detector once before checking that expiry. Although the command reported exactly one ESP32-S3 and successful `board-info`, its result is invalid because the checkpoint was already stale. The result is disclosed but not used in baseline, lifecycle, UAT, verification, or promotion claims. No package, credential access, flash, reset, monitor capture, or later hardware action followed; cleanup verified no child process. This execution-order deviation does not reopen a code-review warning, but it is recorded as a Plan 11 deviation and reinforces the fail-closed stop required by the finite recovery contract.

No hardware command was run during either focused code-review pass or on `ab7f5b9`. The Ultra 205 is connected after the bounded recovery action, but the expired Plan 11 checkpoint cannot be refreshed or resumed. Phase 30 plus STR-09, ASIC-11, and CFG-07 remain pending.

***

_Reviewed: 2026-07-11T02:27:38Z_

_Reviewer: Codex (Plan 11 focused gap-closure and independent review closure)_
