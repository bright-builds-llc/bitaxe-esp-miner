# Phase 28.1.1: BM1366 Nonce Production Wire Parity - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md; this log preserves the alternatives considered.

**Date:** 2026-07-09T19:24:27.682Z
**Phase:** 28.1.1-bm1366-nonce-production-wire-parity
**Mode:** Yolo
**Areas discussed:** Remaining blocker hypothesis, Experiment and patch policy, Closure and handoff gate

***

## Remaining Blocker Hypothesis

| Option | Description | Selected |
| --- | --- | --- |
| Accepted ASIC state readback | Compare safe-readable BM1366 register/electrical state across initialization and first work dispatch to test transmitted intent versus accepted hashing state. | ✓ |
| Cold-boot versus recovery lifecycle parity | Compare one clean boot and one controlled reinit/recovery lifecycle under aligned work. | |
| Upstream init transcript prefix bisection | Replay or bisect the ordered upstream init transaction, including timing, after narrower state/lifecycle checks. | |

**the agent's choice:** Accepted ASIC state readback first.
**Notes:** This is the narrowest untested boundary consistent with UART-alive, flat-power cores after seven sole-blocker falsifications. Lifecycle comparison is the next fallback; full transcript bisection is escalation-only.

***

## Experiment And Patch Policy

| Option | Description | Selected |
| --- | --- | --- |
| Architecture checkpoint then gated one-patch A/B | Run an observation-only causal-boundary audit, patch one confirmed divergence, then run one detector-gated hardware A/B. | ✓ |
| Diagnostic-only architecture checkpoint | Produce a ranked candidate report and defer the patch/A/B to another inserted phase. | |
| Narrow host-midstate audit then conditional A/B | Start with host midstate/ASIC-roll logic before establishing an ASIC accepted-state divergence. | |

**the agent's choice:** Architecture checkpoint followed by one evidence-backed patch and A/B.
**Notes:** No persistent flag is added unless current typed instrumentation is insufficient. Safe upstream-parity changes may be retained; ineffective speculative behavior changes must be reverted rather than stacked.

***

## Closure And Handoff Gate

| Option | Description | Selected |
| --- | --- | --- |
| Strict passed closure | Require correlated nonce, rising hashing-class power, and a live accepted/rejected share in committed redacted evidence. | ✓ when evidence exists |
| `gaps_found` architecture handoff | Close a completed negative diagnostic attempt while keeping the product blocker and Phase 30 promotion pending. | ✓ current fallback |
| `blocked_safe_prerequisite` | Stop before a valid run when detector, board-info, credentials, or documented recovery prerequisites are unavailable. | ✓ only before a valid run |

**the agent's choice:** Preserve all three exact outcomes with strict routing.
**Notes:** A completed unchanged A/B is `gaps_found`, not blocked. Structural markers and fake-pool-only evidence cannot promote Phase 30.

***

## the agent's Discretion

- Exact safe-readable register set, snapshot type names, comparator filenames, and plan slicing, within the locked evidence and safety constraints.

## Deferred Ideas

- None beyond the already documented Phase 29, Phase 30, and future-milestone boundaries.

***

## Yolo Update — 2026-07-13T04:37:37.407Z

**Lifecycle:** `28.1.1-2026-07-09T19-24-27`

This continuation retains the phase's existing lifecycle because the lifecycle verifier treats every top-level plan and summary as one provenance chain. The session adds gap-closing decisions without rewriting completed historical artifacts.

**Areas discussed:** Authoritative non-invasive cold-start reader, Cold-session evidence semantics, One-shot experiment and closure policy

### Authoritative Non-Invasive Cold-Start Reader

| Option | Description | Selected |
| --- | --- | --- |
| OS-native native-USB plus boot-lifetime replay | Reuse the read-only OS-native reader, stable identity split, and session-tagged replay after automatic cold attachment. | ✓ |
| Dedicated on-device evidence journal | Persist a credential-isolated typed boot record for later managed readback. | Deferred fallback |
| Firmware-owned ASIC-only reinitialization | Keep USB attached and reinitialize only the ASIC lifecycle. | Deferred causal diagnostic |

**the agent's choice:** OS-native native-USB plus boot-lifetime replay.

**Notes:** Connected evidence already showed OS-native heartbeat delivery while passive `espflash` stayed silent. The next gate treats `espflash` as observational only, requires nonzero OS-native delivery, and excludes all dormant UART hardware paths. One silent bounded qualification fails closed without retry; an evidence journal would require a separate later plan.

### Cold-Session Evidence Semantics

| Option | Description | Selected |
| --- | --- | --- |
| Original-byte-only cold proof | Require the native reader to preserve original pre-enumeration boot bytes. | |
| Hybrid monitored originals plus cold session-tagged replay | Prove originals during monitored reinit, then accept same-session boot/listener/accepted-state replay after cold enumeration and nonzero OS-native delivery. | ✓ |
| Typed current-boot evidence ledger | Replace retained replay lines with a dedicated current-boot evidence model. | Deferred fallback |
| Diagnostic-only cold discriminator | Keep cold proof separate from the formal nonce/share gate. | |

**the agent's choice:** Hybrid monitored originals plus cold session-tagged replay.

**Notes:** Native USB cannot authoritatively retain bytes emitted before enumeration. The cold contract instead requires the same stable physical device, a new enumeration identity, one new session distinct from reinit, monotonic heartbeats, listener readiness, complete replay proof, all five accepted-state stages, exact ownership ordering, and category-only public evidence. A zero-byte stream is invalid evidence, not a firmware-failure claim.

### One-Shot Experiment And Closure Policy

| Option | Description | Selected |
| --- | --- | --- |
| Direct formal production chain | Skip a separate cold-transport qualification and run only the product gate. | |
| Separate qualification then fresh formal chain | Qualify OS-native cold delivery once, prove cleanup, destroy authority, then create a fresh formal authority on the same unchanged exact HEAD. | ✓ |
| Atomic qualification and formal chain | Combine transport qualification and production evidence under one long-lived authority. | |

**the agent's choice:** Separate one-shot qualification followed by a fresh formal chain.

**Notes:** The qualification contributes no Phase 30 claim and facts are never stitched across chains. `blocked_safe_prerequisite` applies before valid qualification; a completed formal chain without every product signal is `gaps_found`; only same-chain `result_correlated`, hashing-class rise, and an accepted/rejected live share is `passed`. Both hardware captures use at least 360 seconds with wall clocks of at least 420 seconds.

### the agent's Discretion

- Exact native qualification schema names, validator factoring, private authority filenames, and plan slicing, provided the dormant UART implementation stays inactive and the qualification/formal-chain boundary remains explicit.

### Deferred Ideas

- A credential-isolated on-device evidence partition after a completed silent native-USB qualification.
- Firmware-owned ASIC-only reinitialization as a later causal diagnostic, never as both-power cold proof.
