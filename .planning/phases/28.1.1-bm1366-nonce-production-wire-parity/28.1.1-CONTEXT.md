---
generated_by: gsd-insert-phase
phase: 28.1.1-bm1366-nonce-production-wire-parity
generated_at: 2026-07-07T00:00:00Z
---

# Phase 28.1.1: BM1366 Nonce Production Wire Parity - Context

**Gathered:** 2026-07-07
**Status:** Partial — wire fixes landed; live nonce production still blocked pending post-fix hardware rerun

## Scope Changes (28.1.1-05)

- Implemented upstream `mining.configure` before subscribe (`live_runtime.rs`).
- Removed fail-closed on nonzero negotiated version mask for BM1366 work build (`mining.rs`).
- Fixed ASIC job id sequencing to match upstream pre-increment stride (`bm1366.c:313-314`).

## Fixes Landed This Phase

1. Chip-identity register read (`reg 0x00`) parses instead of `register_read_unparsed`.
2. HCN / nonce-space frame uses actual post-PLL frequency (fixture `00 0d 32 24 10`).
3. Dynamic init golden tests for difficulty mask, PLL, and HCN frames.
4. First pool job id uses upstream pre-increment (+8 before assign); unit test added.
5. Stratum version-rolling configure handshake on session start.

## Hardware Evidence (redacted)

See `28.1.1-run-wire-parity-redacted.md` and `28.1.1-wire-diff-summary.md`. Best capture: run-05 — orchestration healthy, init/register-read match, **zero nonces**, power delta negative.
**Source:** Phase 28.1 disposition `firmware-nonce-production` and `NEXT-AGENT-PROMPT.md`

## Phase Boundary

Close the remaining live-mining gap: the BM1366 is alive and orchestration works, but cores never hash under Rust firmware. Upstream v2.14.0 mines immediately on the same Ultra 205 board.

This phase delivers:
1. Upstream golden UART capture procedure (out-of-tree, gitignored) and field-level diff conclusions.
2. Rust TX trace/diff tooling comparable to upstream init + first-minute job window.
3. Init/job wire-byte fixes with per-hypothesis hardware reruns.
4. Version-rolling fallback only if init/job parity is exhausted without `result_correlated`.
5. Redacted hardware evidence and Nyquist closure when success criteria are met.

Out of scope: non-205 boards, Stratum v2, OTAWWW, full active safety, unbounded soak.

## Established Facts (do not re-litigate)

- Phase 28.1 proved orchestration, 1 MBaud RX, register-read responses, and Stratum session live.
- INA260 delta ~flat; zero nonces in 360 s under Rust; upstream mined 171 nonces / 11 accepted on same board.
- Exhausted: W6/W11 encoding, baud, W10/F1 read window, H1 boot pollution, W8 ramp alone, orchestration/J2c, UART path, board health.

## Suspect Priority

1. Dynamic init frames (difficulty mask, PLL 0x08, HCN 0x10) — never byte-asserted until this phase.
2. Register-read parser — chip-identity frame `aa 55 13 66...` classified malformed.
3. Job frame field content — no upstream golden from live traffic yet.
4. Version rolling — upstream negotiates `mining.configure`; Rust deferred.

## Definition of Done

1. `asic_production_status=result_correlated` with rising INA260 power delta.
2. At least one live `mining.submit` accepted or rejected in redacted committed run doc.
3. Host gates green; fail-closed default build intact; J4 `single_dispatch_bounded_read` still reachable.
4. STR-09/CFG-07 promotion only via parity validator when evidence supports it.
