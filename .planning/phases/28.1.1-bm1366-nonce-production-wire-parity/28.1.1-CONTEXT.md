---
generated_by: gsd-discuss-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1-2026-07-07T15-22-48
generated_at: 2026-07-07T15:22:48.624Z
---

# Phase 28.1.1: BM1366 Nonce Production Wire Parity - Context

**Gathered:** 2026-07-07
**Status:** Ready for planning
**Mode:** Yolo

<domain>
## Phase Boundary

Phase 28.1.1 closes the `firmware-nonce-production` blocker isolated by Phase 28.1. The Ultra 205, BM1366 chip, safety bring-up, Stratum socket session, bridge orchestration, job redispatch cadence, and UART RX path are already proven healthy enough to reach work dispatch. The remaining work is to make Rust-programmed BM1366 init/work content produce a correlated nonce and then a live accepted or rejected Stratum share.

This phase owns upstream-vs-Rust BM1366 wire parity, post-fix Ultra 205 hardware reruns, redacted evidence capture, and exact blocker closure. It does not own Phase 29 evidence-wrapper automation, Phase 30 checklist promotion except by producing the required share-outcome evidence, non-205 boards, non-BM1366 ASICs, Stratum v2, OTA/recovery fault injection, runtime display/input, BAP, or unbounded soak/stress mining.

</domain>

<decisions>
## Implementation Decisions

### Blocker Baseline And Non-Retested Facts

- **D-01:** Treat Phase 28.1 disposition `firmware-nonce-production` as the starting point. Do not re-litigate orchestration starvation, baud domains, UART RX, board health, chip health, static init framing, reset/basic bring-up, or safe-prerequisite gates.
- **D-02:** Treat current Phase 28.1.1 verification as partial. The context must guide a follow-up planning pass, because prior ad hoc fixes landed but hardware still has zero `result_correlated` and zero accepted/rejected share outcomes.
- **D-03:** Preserve the current exact non-claim: Rust firmware has not yet proven live nonce production or share-response classification on hardware, even though upstream v2.14.0 mined on the same board.

### Wire-Parity Investigation Strategy

- **D-04:** Keep upstream capture out-of-tree under gitignored scratch space. `reference/esp-miner` remains read-only, and upstream `BM1366_SERIALTX_DEBUG` or RX debug edits happen only in the scratch copy created by `scripts/phase28.1.1-upstream-wire-capture.sh`.
- **D-05:** Use upstream golden wire capture as the decisive comparator only for field-level conclusions. Raw upstream and Rust byte streams, live job bytes, extranonces, share payloads, pool endpoints, workers, and credentials stay local and uncommitted.
- **D-06:** Prefer one-divergence-at-a-time fixes with fixture or unit coverage at the pure Rust boundary. Init/job parity claims need either upstream-derived fixture provenance or a redacted field-level diff summary.

### Remaining Fix Order

- **D-07:** First rerun hardware with the already-landed post-fix Rust image: `mining.configure` before subscribe, nonzero version mask accepted during work build, upstream-style ASIC job-id pre-increment, chip-identity register-read parsing, HCN from actual PLL frequency, and dynamic init fixtures.
- **D-08:** If the post-fix rerun still produces no power rise and no correlated nonce, prioritize upstream golden job-byte confirmation over more speculative firmware edits. The open item is live job-field parity, not another orchestration redesign.
- **D-09:** Treat version rolling as in-scope only when required by evidence. The runtime may keep `mining.configure` and negotiated mask handling, but broader version-rolling semantics should be expanded only if the wire diff or hardware rerun shows it is required for nonce production.

### Hardware Evidence And Redaction

- **D-10:** Hardware verification must follow the repo-local Ultra 205 detector gate: `just detect-ultra205` first, exactly one likely ESP USB serial port, board `205`, ESP32-S3 board-info success, and repo-owned flash/monitor commands.
- **D-11:** Use at least 360 seconds of monitor capture for flash/monitor evidence and a shell wall clock above the capture budget. Do not treat early silence as failure until the full capture window completes unless the repo-owned tool reports a hard failure.
- **D-12:** Evidence intended for commit must be redacted or category-labeled. Committed artifacts may cite counts and labels such as `pool_config: local-owner-supplied`, `result_correlated`, or accepted/rejected outcome categories, but must not contain raw secrets, endpoints, IPs, MACs, SSIDs, device URLs, NVS secret values, raw pool payloads, raw job bytes, or raw BM1366 frames.
- **D-13:** Local `hardware-runs/` captures remain gitignored scratch evidence unless a later redaction workflow promotes selected field-level summaries into committed artifacts.

### Promotion And Phase Handoff

- **D-14:** STR-09, ASIC-11, and CFG-07 promotion remains deferred until Phase 28.1.1 produces redacted hardware evidence of a live ASIC-derived submit classified accepted or rejected. Phase 30 owns checklist promotion from that evidence.
- **D-15:** The done condition is not "wire bytes look plausible." It is `asic_production_status=result_correlated`, a rising INA260 power delta consistent with hashing, and at least one live accepted or rejected share classification in redacted committed evidence.
- **D-16:** If three evidence-backed fix attempts after a clean wire-diff cycle still fail to produce hashing, stop the fix loop and re-question the architecture instead of accumulating speculative mode flags.

### the agent's Discretion

- Downstream agents may choose exact plan slicing, fixture filenames, helper names, and whether to patch existing capture scripts or add narrower helpers. Those choices must preserve the detector gate, read-only upstream reference, functional-core/imperative-shell split, fail-closed default build, and exact evidence semantics.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase Scope And Current State

- `.planning/ROADMAP.md` - Phase 28.1.1 goal, five intended plan slots, dependencies, and Phase 29/30 ordering.
- `.planning/PROJECT.md` - v1.1 trusted Ultra 205 production mining scope and current `firmware-nonce-production` disposition.
- `.planning/REQUIREMENTS.md` - STR-09, ASIC-11, CFG-07, EVD-07, EVD-08, EVD-09, and REL-09 traceability.
- `.planning/STATE.md` - current focus, partial status, accepted Phase 28.1 decisions, and hardware evidence concerns.
- `.planning/v1.1-MILESTONE-AUDIT.md` - milestone tech debt and gap closure context.

### Phase 28.1 Source Evidence

- `.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-CONTEXT.md` - upstream-parity orchestration decisions and approved A/B fallback.
- `.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-05-SUMMARY.md` - detector-gated runs, upstream A/B outcome, and blocker disposition.
- `.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-blocker-disposition.md` - authoritative `firmware-nonce-production` evidence record.
- `.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/NEXT-AGENT-PROMPT.md` - handoff priorities and non-retested facts.

### Phase 28.1.1 Existing Evidence

- `.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-wire-diff-summary.md` - field-level redacted wire diff summary and remaining divergences.
- `.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-run-wire-parity-redacted.md` - detector-gated redacted run-05 evidence and current blocker status.
- `.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md` - partial verification, host-gate evidence, and unmet done criteria.

### Current Implementation Surfaces

- `scripts/phase28.1.1-upstream-wire-capture.sh` - out-of-tree upstream scratch capture helper.
- `scripts/phase28.1.1-wire-parity-capture.sh` - J3-equivalent Rust flash/monitor capture helper.
- `scripts/phase27-live-hardware-bridge-package.sh` - phase-correct firmware package entrypoint for bridge runs.
- `crates/bitaxe-asic/src/bm1366/upstream_init_frames.rs` - upstream-derived BM1366 init fixture bytes with provenance.
- `crates/bitaxe-asic/src/bm1366/mining_ready.rs` - dynamic difficulty, PLL, and HCN init frame generation/tests.
- `crates/bitaxe-asic/src/bm1366/work.rs` and `crates/bitaxe-asic/src/bm1366/production.rs` - BM1366 job payload layout, job id semantics, and production command boundary.
- `crates/bitaxe-stratum/src/v1/live_runtime.rs` - version-rolling configure handshake, notify-held regeneration, submit queueing, and ASIC job id pre-increment.
- `crates/bitaxe-stratum/src/v1/mining.rs` - Stratum notify to BM1366 work-field construction and negotiated version mask handling.
- `firmware/bitaxe/src/asic_adapter/production.rs` - retained production UART executor, register-read parser outcome, and production result polling.
- `firmware/bitaxe/src/live_stratum_runtime.rs` - bridge pump, post-dispatch register-read probe, and INA260 power-delta marker.
- `tools/parity/src/main.rs` - checklist verified-row guardrails and Phase 28 promotion validation.

### Upstream Reference And Policy

- `reference/esp-miner/components/asic/bm1366.c` - BM1366 init, register read, send work, frequency, nonce-space, and debug flags.
- `reference/esp-miner/components/asic/asic_common.c` - upstream difficulty mask and receive-work behavior.
- `reference/esp-miner/components/stratum/mining.c` - upstream job construction, merkle/prev transforms, extranonce, and submit behavior.
- `reference/esp-miner/main/tasks/create_jobs_task.c` - upstream work regeneration cadence and extranonce2 update.
- `reference/esp-miner/main/tasks/asic_result_task.c` - upstream result polling behavior.
- `reference/esp-miner/config-205.cvs` - Ultra 205 upstream defaults used by A/B procedure.
- `AGENTS.md` - Ultra 205 detector gate, credential handling, redaction rules, timeout budget, evidence workflow, and frontmatter-safe Markdown rule.
- `AGENTS.bright-builds.md`, `standards/core/verification.md`, `standards/core/testing.md`, and `standards/languages/rust.md` - Bright Builds verification, testing, Rust, and workflow expectations.
- `PROVENANCE.md` - GPL/reference provenance, fixture, and release-review policy.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- `LiveStratumRuntime::start` already emits `ConfigureVersionRolling` before subscribe, and response handling stores the negotiated version mask.
- `LiveStratumRuntime::next_asic_job_id` now pre-increments by 8 modulo 128, matching the upstream job id stride decision captured in Phase 28.1.1 evidence.
- `upstream_init_frames.rs` and `mining_ready.rs` already cover the previously skipped dynamic difficulty, PLL, and HCN frames with upstream-derived fixture tests.
- `ProductionAsicExecutor` classifies parsed register-read responses separately from job nonces, letting captures distinguish chip-alive proof from actual nonce production.
- The phase capture scripts already separate upstream scratch capture from Rust hardware reruns and keep raw hardware artifacts under ignored local paths.

### Established Patterns

- Pure ASIC and Stratum byte/layout logic belongs in `crates/bitaxe-asic` and `crates/bitaxe-stratum`; firmware should only execute typed commands and publish redaction-safe markers.
- Hardware claims must be detector-gated, board-specific, command-recorded, redaction-reviewed, and exact. Implementation plus unit tests are not hardware parity proof.
- Local credentials are pass-by-path runtime inputs only. Committed docs use category labels, not raw pool, network, endpoint, worker, or secret values.
- Frontmatter-parsed GSD Markdown may use standalone `---` only for the opening and closing YAML frontmatter delimiters at the top of the file.

### Integration Points

- Planning should start from the current partial state rather than redoing completed 28.1.1 work: post-fix firmware rerun, then upstream golden job-byte capture if needed.
- Any new wire-diff conclusions should update `28.1.1-wire-diff-summary.md` as redacted field-level evidence, not local raw logs.
- Any successful live share outcome should create redacted evidence for Phase 30 to consume instead of directly over-promoting `docs/parity/checklist.md`.

</code_context>

<specifics>
## Specific Ideas

- The fastest next hardware discriminator is a post-fix Rust rerun: if INA260 rises and `result_correlated` appears, proceed directly to share-outcome capture; if not, prioritize upstream golden job-byte capture.
- `result_correlated` requires a real BM1366 nonce correlated to live pool work. Register-read parser success is chip-alive proof only.
- The upstream golden capture should compare ordering/timing and field semantics, but committed outputs should summarize only field-level verdicts.
- Version rolling is a priority observation because upstream mined with version rolling active while Rust previously did not negotiate a version mask.

</specifics>

<deferred>
## Deferred Ideas

- Phase 29 owns evidence workflow automation closure.
- Phase 30 owns STR-09/CFG-07 verified promotion after Phase 28.1.1 share-outcome evidence exists.
- Non-205 boards, non-BM1366 ASIC families, Stratum v2, OTA/recovery fault injection, runtime display/input, BAP, full active safety closure, and unbounded soak/stress mining remain future milestones.

</deferred>

***

*Phase: 28.1.1-bm1366-nonce-production-wire-parity*
*Context gathered: 2026-07-07*
