---
generated_by: gsd-discuss-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1-2026-07-09T19-24-27
generated_at: 2026-07-09T19:24:27.682Z
---

# Phase 28.1.1: BM1366 Nonce Production Wire Parity - Context

**Gathered:** 2026-07-09
**Status:** Ready for planning
**Mode:** Yolo

<domain>
## Phase Boundary

Phase 28.1.1 remains responsible for closing the `firmware-nonce-production` blocker on the Ultra 205 BM1366 path: Rust firmware must move from UART-alive, wire-correct work dispatch to a hashing ASIC result that correlates to active pool work and then produces a live accepted or rejected Stratum share.

The completed 28.1.1.1 through 28.1.1.7 diagnostic lineage materially narrows this attempt. Aligned source work has 11/11 job-field parity and 12/12 below-job semantic-event parity. Register-read cadence, long-block RX, ASIC-256 ticket mask, counted-chip RX handoff, negotiated-mask application to work, and post-configure ASIC mask reload were each falsified as the sole blocker. This attempt therefore owns an architecture-level accepted-state transition checkpoint, at most one evidence-backed fix, detector-gated hardware verification, redacted evidence, and an exact pass or handoff verdict. It does not own Phase 29 evidence-wrapper automation, Phase 30 checklist promotion without eligible evidence, non-205 boards, non-BM1366 ASICs, Stratum v2, OTA/recovery fault injection, or unbounded soak/stress mining.

</domain>

<decisions>
## Implementation Decisions

### Accumulated Evidence Baseline

- **D-01:** Start from the Phase 28.1.1.7 handoff, not the original 28.1.1 hypothesis list. Rust still has flat hashing-class power, zero `result_correlated`, and zero fake-pool or live share submission despite retained wire-correct changes.
- **D-02:** Preserve the proven upstream-parity changes unless new evidence shows they are unsafe or causally regressive: counted-chip RX handoff, ASIC-256 ticket mask, upstream-like RX handling, negotiated mask on `MiningWork`, and post-configure `SetVersionMask` reload.
- **D-03:** Do not reopen job bytes, dispatch cadence, `post_max_baud_delay_2000`, `match_upstream_register_read_poll`, `upstream_like_long_block_receive`, `ticket_mask_asic_difficulty`, `count_asic_chips_rx_loop_parity`, `negotiated_version_mask_work_field_parity`, or `pool_negotiated_mask_asic_reload` as sole blockers without new contradictory evidence.
- **D-04:** Treat the Phase 28.1.1 D-16 architecture-reconsideration threshold as exceeded. Another isolated mode flag is not an acceptable starting point.

### Architecture-Level First Discriminator

- **D-05:** The first discriminator is `accepted_asic_state_readback`: compare safe-readable BM1366 register/electrical state after chip enumeration, mining-ready initialization, max-baud transition, post-configure mask reload, and first work dispatch. The question is whether transmitted upstream-parity intent became a hashing-capable ASIC state.
- **D-06:** The comparator must report redaction-safe state-transition categories and counts, including stage reached, readable-register agreement or unavailability, chip count/source, mask-reload class, UART state, power-delta class, nonce/result observation, and submit observation. It must not commit raw frames, register dumps that expose raw wire data, pool values, or network/device identifiers.
- **D-07:** Diagnostic reads must be bounded and bracketed so their observation effect is distinguishable from the already-falsified continuous register-read cadence lever. State readback is an observation boundary, not a default mining-loop behavior change.
- **D-08:** If safe-readable state is incomplete or matches while Rust remains idle, the next ranked discriminator is controlled cold-boot versus recovery/reinit lifecycle parity. Full ordered upstream init-transcript prefix bisection is escalation-only after state and lifecycle comparison fail to identify a sharper divergence.
- **D-09:** Host midstate/ASIC roll, pool target, result correlation, and submit-path hypotheses are not first-line while power remains flat and no nonce-like result is observed. They may be selected only if the architecture checkpoint produces concrete evidence pointing downstream of hashing state.

### Experiment And Patch Policy

- **D-10:** Wave 0 is observation-only: build the accepted-state/lifecycle comparator and rank the first divergent boundary before changing default firmware behavior.
- **D-11:** After a concrete divergence is named, permit exactly one focused patch with pure-boundary fixture or unit coverage where practical, followed by one detector-gated Ultra 205 A/B capture of at least 360 seconds and a shell wall-clock budget of at least 420 seconds.
- **D-12:** Do not add a persistent investigation flag unless existing typed commands, capture wrappers, and compact markers cannot expose the discriminator. Any new diagnostic control must be off by default, closed-set, redaction-safe, and tested against re-emitting falsified hypotheses.
- **D-13:** Retain the candidate patch only if it independently restores confirmed upstream parity and is safe, or if the A/B improves to `result_correlated` or a share-submit observation. Otherwise revert the behavior change and record the hypothesis as falsified; do not stack a second speculative patch in the same attempt.

### Hardware Evidence And Redaction

- **D-14:** Every hardware run must begin with `just detect-ultra205`, succeed for exactly one ESP32-S3 board-205 port, and run `just verify-reference`. Required local credentials remain pass-by-path runtime inputs only.
- **D-15:** Prefer deterministic fake-pool/source-work alignment for the architecture discriminator. After a correlated nonce or fake-pool submit appears, run the live-pool outcome capture needed by the original phase gate.
- **D-16:** Use repo-owned package/flash/monitor/evidence commands with at least 360 seconds of capture and a wall-clock budget above the full flash plus capture duration. Record the source commit, reference commit, board, port category, exact commands, board-info result, behavior, and conclusion.
- **D-17:** Committed evidence may contain counts, booleans, closed-set categories, and redacted verdicts only. Raw UART/job bytes, register dumps, pool payloads, endpoints, ports, users, workers, owner addresses, passwords, tokens, device URLs, IPs, MACs, SSIDs, Wi-Fi values, and NVS secret values remain local and uncommitted.

### Closure And Handoff Gate

- **D-18:** Verification is `passed` only when one detector-gated evidence chain shows `asic_production_status=result_correlated`, rising/positive hashing-class power, and at least one live `share_submission_status=accepted` or `share_submission_status=rejected` classification in committed redacted evidence.
- **D-19:** If a valid discriminator and A/B complete without the D-18 outcome, close this diagnostic attempt as `gaps_found`, name the first still-open architecture boundary, retain only safe wire-correct changes, and keep the product blocker open.
- **D-20:** Use `blocked_safe_prerequisite` only before a valid discriminator completes because detection, board-info, required local credentials, or documented recovery prerequisites are unavailable. Do not replace a completed negative A/B with a blocked verdict.
- **D-21:** Phase 30 promotion remains pending unless D-18 is met. Do not mark STR-09, ASIC-11, or CFG-07 verified from structural markers, fake-pool-only evidence, or a narrowed blocker.

### the agent's Discretion

- Downstream agents may choose the exact safe-readable register set, typed snapshot model, comparator file names, and plan slicing. They must keep the functional-core/imperative-shell boundary, avoid unsafe or undocumented raw writes, preserve detector/redaction rules, and follow the D-05 through D-21 evidence ladder.

</decisions>

<canonical_refs>

## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Scope, Requirements, And Current Handoff

- `.planning/PROJECT.md` - v1.1 trusted Ultra 205 production-mining scope and exact non-claim policy.
- `.planning/ROADMAP.md` - Phase 28.1.1 lineage, Phase 29/30 ordering, and all inserted diagnostic outcomes.
- `.planning/REQUIREMENTS.md` - STR-09, ASIC-11, CFG-07, EVD-09, and REL-09 evidence constraints.
- `.planning/STATE.md` - current Phase 28.1.1.7 handoff and `remaining_nonce_production_blocker_narrowing` next hypothesis.
- `AGENTS.md` - detector gate, credential handling, hardware-first evidence, timeout, recovery, redaction, and frontmatter rules.
- `AGENTS.bright-builds.md` - repo workflow and verification defaults.
- `standards/core/architecture.md` - functional-core/imperative-shell and domain-type expectations.
- `standards/core/verification.md` - repository sync and pre-commit verification requirements.
- `standards/core/testing.md` - pure business-logic test expectations.
- `standards/languages/rust.md` - Rust code-shape and verification guidance.

### Diagnostic Lineage And Falsified Sole Blockers

- `.planning/phases/28.1.1.1-bm1366-upstream-golden-comparator-and-nonce-production-gap-r/28.1.1.1-VERIFICATION.md` - 11/11 aligned job-field and 12/12 below-job semantic parity; no Rust submit.
- `.planning/phases/28.1.1.2-bm1366-result-path-and-asic-side-effect-nonce-production-dia/28.1.1.2-VERIFICATION.md` - register-read polling A/B disposition.
- `.planning/phases/28.1.1.3-bm1366-result-rx-acquisition-model-nonce-production-diagnosi/28.1.1.3-VERIFICATION.md` - long-block RX acquisition A/B disposition.
- `.planning/phases/28.1.1.4-bm1366-asic-init-content-sequencing-nonce-production-diagnos/28.1.1.4-VERIFICATION.md` - ASIC-256 ticket-mask parity and unchanged A/B.
- `.planning/phases/28.1.1.5-bm1366-match-upstream-chip-enumerate-before-init-nonce-produ/28.1.1.5-VERIFICATION.md` - counted-chip RX/address handoff and unchanged A/B.
- `.planning/phases/28.1.1.6-bm1366-version-rolling-negotiation-nonce-production-diagnosi/28.1.1.6-VERIFICATION.md` - negotiated mask-on-work parity and unchanged A/B.
- `.planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-VERIFICATION.md` - post-configure ASIC mask reload structural success, unchanged A/B, and architecture-level handoff.
- `.planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-final-evidence-redacted.md` - latest redacted evidence and exact product non-claims.

### Current Implementation And Upstream Evidence Surfaces

- `crates/bitaxe-asic/src/bm1366/registers.rs` - typed BM1366 register definitions and safe-readable state candidates.
- `crates/bitaxe-asic/src/bm1366/result.rs` - result-frame parsing and nonce observation semantics.
- `crates/bitaxe-asic/src/bm1366/transcript.rs` - typed initialization transcript and sequencing evidence.
- `crates/bitaxe-asic/src/bm1366/uart_accumulate.rs` - counted RX accumulation boundary.
- `crates/bitaxe-asic/src/bm1366/mining_ready.rs` - mining-ready command/state construction.
- `firmware/bitaxe/src/asic_adapter/production.rs` - retained UART production executor, bounded result reads, and runtime mask reload.
- `firmware/bitaxe/src/asic_adapter/uart.rs` - UART read/clear behavior and compact trace markers.
- `firmware/bitaxe/src/live_stratum_runtime.rs` - dispatch, power-delta, result correlation, and share-submit integration.
- `scripts/phase28.1.1.7-asic-mask-reload-compare.mjs` - latest closed-set comparator taxonomy and falsified-lever bans.
- `scripts/phase28.1.1.7-asic-mask-reload-diagnostic.sh` - current detector-gated hardware diagnostic wrapper pattern.
- `reference/esp-miner/components/asic/bm1366.c` - upstream BM1366 init, reads, state transitions, and work dispatch reference.
- `reference/esp-miner/components/asic/asic_common.c` - upstream receive-work and ASIC common behavior.
- `reference/esp-miner/main/tasks/asic_result_task.c` - upstream runtime result path.
- `PROVENANCE.md` - read-only reference, upstream-derived fixture, and GPL provenance policy.

</canonical_refs>

<code_context>

## Existing Code Insights

### Reusable Assets

- The Phase 28.1.1.7 comparator/diagnostic pair already provides a redaction-safe closed-set taxonomy, detector-gated A/B wrapper, timeout enforcement, power/correlation/submit markers, and explicit bans for exhausted hypotheses.
- `bm1366::registers`, `transcript`, `uart_accumulate`, and `mining_ready` provide typed pure boundaries for a state-transition snapshot and fixture tests.
- `ProductionAsicExecutor` and the retained UART already support typed commands and bounded reads; any readback shell should remain thin around a pure comparison model.
- `live_stratum_runtime` already exposes post-configure reload, power-delta, result-correlation, and share-submit markers needed for the final gate.

### Established Patterns

- Pure state classification and comparison belongs in `crates/bitaxe-asic`; firmware owns bounded I/O and redaction-safe markers only.
- Each diagnostic phase builds a deterministic comparator first, then exactly one named A/B, an evidence-based keep/revert disposition, and final redacted verification.
- Hardware evidence is detector-gated, board-specific, at least 360 seconds, reference-clean, and fail-closed on claims.
- Frontmatter-parsed Markdown uses standalone `---` only for the opening and closing top frontmatter delimiters; body separation uses headings or `***`.

### Integration Points

- Extend the current comparator taxonomy rather than starting another parallel evidence stack.
- Add accepted-state snapshots at bounded initialization/dispatch transitions without making readback part of the normal mining loop.
- Feed any confirmed divergence into exactly one typed patch and reuse the Phase 28.1.1.7 diagnostic wrapper pattern for A/B and redaction.

</code_context>

<specifics>
## Specific Ideas

- The key distinction is transmitted intent versus accepted hashing state: Rust can emit matching commands while the BM1366 remains UART-responsive but electrically idle.
- A safe readback can be `unavailable`; unavailable state should route to lifecycle comparison rather than inventing a register value or raw write.
- A fake-pool correlate/submit is a strong discriminator but does not replace the original live accepted/rejected share gate for Phase 30 promotion.

</specifics>

<deferred>
## Deferred Ideas

- Phase 29 owns evidence workflow automation closure.
- Phase 30 owns STR-09, CFG-07, and ASIC-11 checklist promotion after eligible live share-outcome evidence exists.
- Non-205 boards, non-BM1366 ASICs, Stratum v2, OTA/recovery fault injection, runtime display/input, BAP, broader active-safety closure, and unbounded soak/stress mining remain future scope.

</deferred>

***

*Phase: 28.1.1-bm1366-nonce-production-wire-parity*
*Context gathered: 2026-07-09*
