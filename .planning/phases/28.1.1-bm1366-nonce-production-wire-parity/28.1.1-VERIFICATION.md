---
phase: 28.1.1-bm1366-nonce-production-wire-parity
status: gaps_found
score: 6/12
verified: 2026-07-10
generated_by: gsd-verifier
generated_at: 2026-07-11T02:03:31Z
updated_by: gsd-execute-plan
updated_at: 2026-07-11T02:03:31Z
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1-2026-07-09T19-24-27
lifecycle_validated: true
verification_result: gaps_found
phase30_promotion_input: pending
---

# Phase 28.1.1 Verification

## Verdict

Phase 28.1.1 remains `gaps_found`. Plans 06-11 implement and host-verify category-only accepted-state observation, fail-closed exact-stage comparison, bounded diagnostic replay, measured power-removal semantics, tri-state promoted-evidence denial, and a retained-package no-flash/no-reset lifecycle path. These surfaces close the Plan 10 evidence-correctness warnings, but they do not establish nonce production, hashing, a live share, or the physical lifecycle pair.

Plan 11 produced a strict five-stage 360-second reinit candidate on source commit `4e2d16524d57037e0814b191fed1b87fca4d0623`. Commit `d275a0e7af6a1534df5fca07820066791ae4af19` then changed the lifecycle polling scripts, so the earlier package identity became stale and cannot arm or promote the current-head run. On current head, the first detector failed `board-info`, the one USB-replug recovery was consumed, and the both-power checkpoint response reached continuation after its persisted monotonic deadline. The finite recovery contract therefore ended `blocked_safe_prerequisite` without a current-head package, reinit capture, lifecycle arm, or monitor capture.

One continuation mistakenly invoked `just detect-ultra205` before asserting the expired deadline. It observed exactly one ESP32-S3 and successful `board-info`, but this result is invalid and unpromoted because it occurred after expiry. No credentials, package, flash, reset, monitor, or later hardware command followed. Cleanup verified no live child.

The score remains six of twelve independently verified goal-backward truths. Software correctness improved, but the six absent hardware/goal truths did not become true.

## Goal-Backward Truths

| Truth | Result | Evidence |
| --- | --- | --- |
| Accepted-state observation is off by default, one-shot, and category-only | verified | Plan 07 implementation plus category-only marker tests and wrapper fixtures |
| A detector-validated Ultra 205 baseline ran for at least 360 seconds | verified | `28.1.1-accepted-state-baseline-redacted.md` retains the earlier valid detector-gated 360-second baseline |
| Rust reached readable single-chip accepted-state observation points | verified | The earlier baseline contains all five Rust stage categories with readable observations |
| Rust reached a hashing-capable state | not verified | Hash counters remained inactive and power classified as falling |
| A BM1366 result correlated to active pool work | not verified | `result_correlated: false` |
| A live accepted or rejected share was observed | not verified | The live attempt remained ineligible and no share outcome was observed |
| Upstream and Rust accepted-state parity was established | not verified | Normalized upstream stage observations remain unavailable |
| Retained accepted-state replay is bounded, diagnostic-only, and transport-only | verified | Plan 11 closes the contract at 180000 ms with a 2000 ms cadence and proves a replay at 72000 ms without adding ASIC reads |
| The lifecycle branch has a measured, no-flash/no-reset arming boundary | verified as implementation only | Fixtures require strict five-stage reinit, exact closed tokens, 5000 ms USB absence, deadline validation, and a 360-second `--no-reset` capture |
| A true physical cold-start lifecycle pair completed | not verified | The current-head recovery checkpoint expired; no eligible reinit plus cold-start pair exists |
| Historical D-03 levers remain closed | verified | Disposition preserves the prohibited reopens and closed recommendation set |
| Phase 30 promotion is eligible | not verified | The same-chain result, hashing-rise, and accepted/rejected-share gate remains unsatisfied |

## Plan 11 Must-Have Audit

| Must-have | Result | Verification |
| --- | --- | --- |
| Empty, partial, or one-sided accepted-state inputs never classify as `match` | verified | Rust and Node tests cover empty, three-of-five, and one-sided missing inputs; all return unavailable |
| Replay timing preserves a real cadence after the latest valid reappearance and monitor-start budget | verified | Constants are 180000/2000/60000/10000 ms; the real-schedule test proves the 72000 ms replay and 180000 ms expiry |
| Power semantics distinguish operator attestation from host-observed USB absence | verified as implementation only | Fixtures require at least 5000 ms continuous absence and keep electronic barrel proof false; the expired hardware checkpoint produced no lifecycle member |
| Evidence denial passes only an explicit no-match search result | verified | Clean, forbidden, malformed-pattern, and injected-tool-error fixtures pass their expected outcomes |
| Hardware arming requires an exact-current-head package and strict five-stage reinit | verified fail-closed | The `4e2d165` candidate became stale after `d275a0e`; the current-head run never armed |
| Phase 30 and STR-09/ASIC-11/CFG-07 remain pending without same-chain proof | verified | Checklist and promotion remain unchanged; the invalid post-expiry detector contributes no claim |

## Strict Production Evidence Gate

Source: `28.1.1-final-share-evidence-redacted.md`.

| Required field | Pass value | Observed |
| --- | --- | --- |
| ASIC production | `result_correlated` | `work_dispatched` |
| Power delta | `hashing_class_rise` | `falling` |
| Live share | `accepted` or `rejected` | `not_observed` |

All three fields must occur in one detector-gated evidence chain. None is present, so Phase 30 eligibility is prohibited.

## Hardware Evidence Audit

- The earlier detector-gated baseline remains valid negative evidence and is not replaced by Plan 11.
- The strict five-stage Plan 11 candidate is diagnostic history only because its source identity is `4e2d165`, not current head `d275a0e`.
- The valid current-head detector state remains unavailable: the initial attempt failed `board-info`; the later successful observation was executed after checkpoint expiry and is invalid.
- The operator-reported both-power action was a bounded detector recovery, not an armed lifecycle capture. No lifecycle attestation or physical pair is claimed.
- No current-head credential access, package, flash, reset, or monitor capture followed expiry. Cleanup verified no process remained.
- Raw logs remain ignored; promoted artifacts contain category-only, redacted facts.
- There is no accepted/rejected-share artifact. Phase 30 remains pending.

## Independent Verification Commands

| Command | Result |
| --- | --- |
| `cargo fmt --all` | passed |
| `cargo clippy --all-targets --all-features -- -D warnings` | passed |
| `cargo build --all-targets --all-features` | passed |
| `cargo test --all-features` | passed |
| `cargo fmt --all -- --check` | passed |
| `node scripts/phase28.1.1-accepted-state-compare-test.mjs` | passed |
| `node scripts/phase28.1.1-accepted-state-lifecycle-compare-test.mjs` | passed |
| `bash scripts/phase28.1.1-promoted-evidence-denylist-test.sh` | passed |
| `bash scripts/phase28.1.1-accepted-state-diagnostic-test.sh` | passed |
| `just verify-reference` | passed at the pinned reference identity |
| Promoted-evidence denylist over every Plan 11 closure artifact | passed |
| Formal lifecycle validator with required plans and verification | passed |
| `git diff --exit-code HEAD -- docs/parity/checklist.md` | passed; checklist unchanged |
| `git diff --check` | passed |

## Closed Evidence-Correctness Gaps

Plan 11 resolves the prior verification findings for continuous five-second USB absence, replay observability after latest valid attachment, incomplete accepted-state classification, and malformed/tool-error denylist handling. The focused code review records zero remaining warnings and deterministic coverage for each boundary.

Closing those implementation findings does not close the physical lifecycle or production-share gaps.

## Remaining Gaps

### 1. Physical lifecycle pair remains unavailable

No eligible chain combines the current exact head, a timely detector gate, a strict five-stage reinit member, and the retained-package cold-start capture. The Plan 11 recovery contract is exhausted; its expired checkpoint cannot be refreshed or retried.

### 2. Original nonce/share goal remains incomplete

There is still no same-chain correlated BM1366 result, hashing-class power rise, and accepted or rejected live share. Lifecycle structure cannot substitute for this proof.

## Requirements And Promotion

| Requirement | Phase 28.1.1 result |
| --- | --- |
| STR-09 | not promoted; no real accepted/rejected response to an ASIC-derived submit |
| ASIC-11 | not promoted; no live BM1366 result correlated to active pool work |
| CFG-07 | not promoted by this phase; Phase 30 remains coupled to eligible live evidence |

`docs/parity/checklist.md` and the Phase 30 promotion contract remain unchanged.

## Conservative Next Action

Do not resume or refresh this Plan 11 checkpoint. Any further physical lifecycle attempt requires a newly planned, finite recovery contract that starts with current-head identity and timely checkpoint validation. Re-enter Phase 30 only after one evidence chain independently proves the correlated result, hashing-class rise, and accepted/rejected-share gate.

## Exact Non-Claims

- Firmware nonce production is not verified.
- Hashing-capable accepted state is not verified.
- Upstream/Rust accepted-state or lifecycle parity is not verified.
- A true physical cold-start lifecycle member was not captured.
- The post-expiry detector result is not valid evidence.
- A live accepted/rejected share is not verified.
- STR-09, ASIC-11, and CFG-07 are not promoted by this phase.
- Phase 30 remains pending.

## Final Status

verification_result: gaps_found
implementation_status: complete_review_warnings_resolved
hardware_lifecycle_status: blocked_safe_prerequisite
phase_goal_status: not_achieved
first_open_architecture_boundary: exact_current_head_reinit_and_physical_cold_start_lifecycle
phase30_promotion_input: pending
raw_bytes_committed: false
credential_contents_read: false

***

Material guidance applied: the Ultra 205 detector, timeout, credential-path, finite recovery, true two-power-path lifecycle, redaction, and evidence rules in `AGENTS.md`; `AGENTS.bright-builds.md`; `standards-overrides.md`; and the Bright Builds architecture, code-shape, verification, testing, and Rust standards. Lifecycle mode and ID match Context, Plans 06-11, Summaries 06-11, and this verification artifact.
