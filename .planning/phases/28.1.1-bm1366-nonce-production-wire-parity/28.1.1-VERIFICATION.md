---
phase: 28.1.1-bm1366-nonce-production-wire-parity
verified_at: 2026-07-07T05:05:00Z
verdict: partial
---

# Phase 28.1.1 Verification

## Definition of Done

| Criterion | Status | Evidence |
| --- | --- | --- |
| `result_correlated` + rising power delta | **not met** | run-05: `asic_probe=power_delta delta_mw=-190`, zero `result_correlated` |
| Live accepted/rejected share | **not met** | Zero `share_submission_status=accepted\|rejected` in 360 s capture |
| Host gates green | **met** | `cargo fmt`, `cargo clippy -p bitaxe-stratum -p bitaxe-asic`, `cargo test` (116 stratum + 85 asic) |
| Fail-closed default + J4 lever | **met** (static) | No changes to fail-closed default path; J4 investigation env unchanged |
| Checklist promotion | **not claimed** | No STR-09/CFG-07 verified promotion |

## Plan Completion

| Plan | Status | Notes |
| --- | --- | --- |
| 28.1.1-01 Upstream golden capture | **procedure complete** | Scratch copy + helper script; full upstream ESP-IDF build/capture operator-local |
| 28.1.1-02 Rust TX trace + capture | **complete** | UART trace in bridge builds; `phase28.1.1-wire-parity-capture.sh`; run-05 360 s |
| 28.1.1-03 Dynamic init + parser | **complete** | HCN/PLL/difficulty fixtures; chip-id register parser; hardware `register_read_parsed` |
| 28.1.1-04 Job frame parity | **partial** | Field layout matches upstream; job-id pre-increment fix landed; hardware confirm pending |
| 28.1.1-05 Version rolling + evidence | **partial** | `mining.configure` on start; nonzero mask work build allowed; redacted run doc committed |

## Code Changes (summary)

- `crates/bitaxe-asic/src/bm1366/registers.rs`, `result.rs` — chip identity register parse
- `crates/bitaxe-asic/src/bm1366/frequency_voltage.rs`, `mining_ready.rs` — HCN from actual PLL MHz
- `crates/bitaxe-asic/src/bm1366/upstream_init_frames.rs` — dynamic init golden fixtures
- `crates/bitaxe-stratum/src/v1/live_runtime.rs` — `mining.configure` + upstream job-id pre-increment
- `crates/bitaxe-stratum/src/v1/mining.rs` — allow negotiated version mask in work build
- `scripts/phase28.1.1-*.sh`, `scripts/bm1366-init-tx-dump.sh` — capture/dump tooling

## Verdict

**Partial.** Wire-parity investigation narrowed remaining risk to job-id sequencing (fix landed, hardware rerun blocked on USB) and upstream golden job-byte confirmation. Phase goal (live nonce production) is **not** achieved; blocker `firmware-nonce-production` remains for a follow-up hardware iteration.
