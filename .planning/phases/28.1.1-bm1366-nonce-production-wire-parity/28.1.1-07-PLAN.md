---
phase: 28.1.1-bm1366-nonce-production-wire-parity
plan: 07
type: execute
gap_closure: true
wave: 6
depends_on: ["28.1.1-06"]
files_modified:
  - firmware/bitaxe/src/asic_adapter.rs
  - firmware/bitaxe/src/asic_adapter/production.rs
  - firmware/bitaxe/src/asic_adapter/work_result_investigation.rs
  - firmware/bitaxe/src/live_stratum_runtime.rs
  - scripts/phase28.1.1-upstream-wire-capture.sh
  - scripts/phase28.1.1-accepted-state-diagnostic.sh
  - scripts/phase28.1.1-accepted-state-diagnostic-test.sh
autonomous: true
requirements: [ASIC-11, CFG-07]
user_setup: []
must_haves:
  truths:
    - "Accepted-state reads are one-shot, stage-bracketed, off by default, and never become continuous mining-loop polling"
    - "Firmware emits category/count markers only; raw register values remain local"
    - "The diagnostic wrapper enforces board 205, detector success, reference cleanliness, redaction, and a 360-second hardware minimum"
  artifacts:
    - path: scripts/phase28.1.1-accepted-state-diagnostic.sh
      provides: "Blocked/hardware evidence wrapper for accepted-state and lifecycle attempts"
      contains: "duration-seconds"
    - path: scripts/phase28.1.1-accepted-state-diagnostic-test.sh
      provides: "Deterministic safety and routing coverage"
      contains: "accepted_state_diagnostic_test"
  key_links:
    - from: firmware/bitaxe/src/live_stratum_runtime.rs
      to: crates/bitaxe-asic/src/bm1366/accepted_state.rs
      via: "typed snapshot observation and category-only marker"
      pattern: "accepted_state_snapshot"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1-2026-07-09T19-24-27
generated_at: 2026-07-09T19:34:00Z
---

<objective>
Add the thin firmware and command wrapper needed to observe safe-readable accepted state without changing default mining behavior.

Purpose: collect bounded observations at the locked stage boundaries and feed the pure Plan 06 classifier while preserving the falsified-register-poll non-default.

Output: one-shot diagnostic instrumentation, category-only markers, and a detector/redaction-safe wrapper with tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-CONTEXT.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-06-SUMMARY.md
@firmware/bitaxe/src/asic_adapter.rs
@firmware/bitaxe/src/asic_adapter/production.rs
@firmware/bitaxe/src/asic_adapter/work_result_investigation.rs
@firmware/bitaxe/src/live_stratum_runtime.rs
@scripts/phase28.1.1.7-asic-mask-reload-diagnostic.sh

<interfaces>
New diagnostic token: `accepted_state_snapshot` in `BITAXE_WORK_RESULT_INVESTIGATION`; default false.

Marker shape: `accepted_state_snapshot stage=<closed-stage> observation=<available|unavailable> readable_responses=<count> hash_counter_activity=<active|inactive|unavailable> redacted=true`. Match/mismatch is assigned only by the Plan 06 upstream/Rust comparator.

Wrapper CLI: `--mode blocked|hardware --attempt accepted-state|lifecycle --duration-seconds N --port PATH --wifi-credentials PATH --pool-credentials PATH --evidence-out PATH`.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add bounded one-shot accepted-state instrumentation</name>
  <files>firmware/bitaxe/src/asic_adapter.rs, firmware/bitaxe/src/asic_adapter/production.rs, firmware/bitaxe/src/asic_adapter/work_result_investigation.rs, firmware/bitaxe/src/live_stratum_runtime.rs</files>
  <read_first>firmware/bitaxe/src/asic_adapter.rs, firmware/bitaxe/src/asic_adapter/production.rs, firmware/bitaxe/src/asic_adapter/work_result_investigation.rs, firmware/bitaxe/src/live_stratum_runtime.rs, crates/bitaxe-asic/src/bm1366/accepted_state.rs, reference/esp-miner/components/asic/bm1366.c</read_first>
  <action>Add only the `accepted_state_snapshot` diagnostic token, default false, with parser tests. When enabled, issue one bounded safe-readable burst using the existing chip-id plus upstream hashrate-monitor register set (`0x00`, `0x4c`, `0x88`–`0x8c`) at each reachable locked stage; never issue raw writes. While `asic_adapter.rs` still owns the initialization UART, collect `post_enumerate`, `post_mining_ready`, and `post_max_baud` observations directly through a bounded helper. After peripherals are retained, change `ProductionReadOutcome::RegisterReadProof` to carry the typed `Bm1366RegisterRead` so `post_mask_reload` and `post_first_work` responses feed the same Plan 06 snapshot recorder. Render the exact observation/category-only marker shape without the `value` or `asic_address`; only the offline comparator may assign match/mismatch. Clear each pending stage after its bounded observation window so no continuous register polling occurs. Add tests that the default emits zero accepted-state read bursts, enabled mode emits at most one burst per stage, raw register values are absent from marker format strings, and existing `match_upstream_register_read_poll` remains off and untouched.</action>
  <verify><automated>cargo test -p bitaxe-asic accepted_state && cargo test -p bitaxe-stratum && rg -n "accepted_state_snapshot" firmware/bitaxe/src/asic_adapter.rs firmware/bitaxe/src/asic_adapter/production.rs firmware/bitaxe/src/asic_adapter/work_result_investigation.rs firmware/bitaxe/src/live_stratum_runtime.rs</automated></verify>
  <acceptance_criteria>Default build has `accepted_state_snapshot_enabled() == false`; the safe read set is exactly `0x00,0x4c,0x88,0x89,0x8a,0x8b,0x8c`; each stage is one-shot; no marker includes a register `value` or `asic_address`; existing result correlation tests remain green.</acceptance_criteria>
  <done>Firmware can emit one bounded category-only snapshot per stage only when the new diagnostic token is explicitly enabled.</done>
</task>

<task type="auto">
  <name>Task 2: Add detector-gated accepted-state diagnostic wrapper</name>
  <files>scripts/phase28.1.1-upstream-wire-capture.sh, scripts/phase28.1.1-accepted-state-diagnostic.sh, scripts/phase28.1.1-accepted-state-diagnostic-test.sh</files>
  <read_first>scripts/phase28.1.1-upstream-wire-capture.sh, scripts/phase28.1.1.7-asic-mask-reload-diagnostic.sh, scripts/phase27-live-hardware-bridge-package.sh, scripts/phase28.1.1-accepted-state-compare.mjs, reference/esp-miner/components/asic/bm1366.c, AGENTS.md</read_first>
  <action>Extend the existing upstream helper so it modifies only its gitignored scratch copy to enable upstream register-read/hash-counter observations and emits the same stage/observation/category marker vocabulary; keep upstream raw values in scratch logs and never modify `reference/esp-miner`. Create a rerunnable Bash wrapper with `#!/usr/bin/env bash` and `set -euo pipefail` implementing the exact CLI. Hardware mode must call `just detect-ultra205`, verify exactly one returned port matches `--port`, call `just verify-reference`, require existing credential paths without reading them, reject duration below 360, build via the phase-correct Phase 27 package path with `accepted_state_snapshot`, and call repo-owned flash/monitor with `capture-timeout-seconds` equal to the requested duration and `redact-evidence=true`. Blocked mode writes only prerequisite categories. Both modes call the Plan 06 comparator and write a concise summary/log under a gitignored run path plus the caller-provided redacted evidence path. Create shell tests for scratch-only upstream changes, duration rejection, invalid mode/attempt, detector/port mismatch fixture, missing credential path, command rendering, banned-hypothesis absence, and no secret-path contents in output.</action>
  <verify><automated>bash scripts/phase28.1.1-accepted-state-diagnostic-test.sh</automated></verify>
  <acceptance_criteria>The shell test prints `accepted_state_diagnostic_test: passed`; upstream changes occur only under the scratch root; hardware commands contain board `205`, `capture-timeout-seconds=360` or higher, and `redact-evidence=true`; wrapper never reads credential contents; no D-03 hypothesis is a selectable attempt.</acceptance_criteria>
  <done>The wrapper and its deterministic tests enforce detector, timeout, credential-path, redaction, and closed-attempt constraints.</done>
</task>
</tasks>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation | Blocking condition |
| --- | --- | --- | --- |
| Observation reads perturb default mining | high | Off-by-default token, one burst per stage, bounded pending state | Default emits a burst or any stage repeats continuously |
| Unsafe/ambiguous hardware target | high | Detector and explicit port equality before package/flash | Zero/multiple/mismatched ports or failed board-info |
| Secret/raw value disclosure | high | Path-only inputs, category markers, redacted capture | Credential read or raw register value appears in promoted output |
</threat_model>

<verification>
Run the affected Rust tests, shell diagnostic test, default-off grep/assertion, `cargo clippy --all-targets --all-features -- -D warnings`, and `git diff --check`.
</verification>

<success_criteria>
The repository can collect one-shot accepted-state observations through its normal safe hardware path while the default firmware remains behaviorally unchanged.
</success_criteria>

<output>After completion, create `28.1.1-07-SUMMARY.md`.</output>
