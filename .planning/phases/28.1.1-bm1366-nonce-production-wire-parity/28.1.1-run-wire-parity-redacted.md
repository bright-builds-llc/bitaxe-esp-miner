# Phase 28.1.1 Wire Parity Hardware Run (Redacted)

Date: 2026-07-07  
Board: `205`  
Port: local USB serial (not committed)  
Source commit: working tree at phase execution  
Reference pin: `c1915b0a63bfabebdb95a515cedfee05146c1d50`  
Image: `bazel-bin/firmware/bitaxe/bitaxe-ultra205-factory.bin` (phase27 live-hardware-bridge package)  
Capture timeout: 360 s

## Commands

```bash
just detect-ultra205
bash scripts/phase28.1.1-wire-parity-capture.sh <port> .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/hardware-runs/wire-parity-run-05 360
```

## Board Info

- Chip: esp32s3 (revision v0.2)
- Flash: 16MB
- Features: WiFi, BLE, Embedded Flash

## Observed Markers (run-05)

| Marker | Count / value | Notes |
| --- | --- | --- |
| `bootstrap=initialized_no_mining` | present | Phase27 bring-up complete |
| `asic_production_trace=register_read_parsed` | post-init + post-dispatch | Chip-identity parser fix confirmed |
| `asic_production_status=work_dispatched` | many | Pool-derived 88-byte job frames |
| `asic_bridge=job_redispatched extranonce2_counter=` | 1..28+ | H4 cadence active |
| `pool_config` | local-owner-supplied | Pool bridge applied (`pool_bridge_applied=1`) |
| `asic_probe=power_delta` | `delta_mw=-190` | Cores not hashing (~1 W class, not ~10 W) |
| `result_correlated` | 0 | Blocker persists |
| `share_submission_status=accepted\|rejected` | 0 | No live submit classification |

## Init TX (category labels only)

- Version-mask prelude: 3 frames before chip enumerate
- Chip enumerate: `CMD_SET_ADDRESS` class frame present
- Dynamic frames: difficulty mask, PLL, HCN bytes match committed fixtures
- Baud switch: 1 MBaud class operation completed

## Job TX (category labels only)

- Frame size: 88 bytes
- Header: job write class (`0x21`)
- First observed job id in capture: `0` (pre job-id fix image; host fix landed after this flash)
- Stride: +8 between subsequent dispatches observed
- Field slots present: starting_nonce, nbits, ntime, merkle_root, prev_block_hash, version

## Conclusion

**Blocker status:** `firmware-nonce-production` **still open** on this run. Orchestration, init parity, register-read parsing, and production work dispatch are healthy; INA260 power delta remains flat/negative and no nonces correlate. Follow-up: reflash with post-fix firmware (job id pre-increment + `mining.configure`) and execute upstream scratch SERIALTX_DEBUG capture for job-field golden diff.
