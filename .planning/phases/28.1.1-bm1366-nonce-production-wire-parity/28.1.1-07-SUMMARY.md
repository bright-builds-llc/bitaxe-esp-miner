---
phase: 28.1.1-bm1366-nonce-production-wire-parity
plan: 07
subsystem: firmware-diagnostics
tags: [bm1366, accepted-state, hardware-wrapper, redaction, gap-closure]
requires:
  - phase: 28.1.1-06
    provides: "accepted-state classifier and comparator vocabulary"
provides:
  - "Off-by-default one-shot accepted-state firmware observation"
  - "Category-only markers for five initialization and work stages"
  - "Detector-gated accepted-state/lifecycle evidence wrapper"
affects: [28.1.1-08, 28.1.1-09]
tech-stack:
  added: []
  patterns: [imperative-shell, typed-observation, one-shot-diagnostic, redacted-evidence]
key-files:
  created:
    - scripts/phase28.1.1-accepted-state-diagnostic.sh
    - scripts/phase28.1.1-accepted-state-diagnostic-test.sh
  modified:
    - crates/bitaxe-asic/src/bm1366/accepted_state.rs
    - firmware/bitaxe/src/asic_adapter.rs
    - firmware/bitaxe/src/asic_adapter/production.rs
    - firmware/bitaxe/src/asic_adapter/work_result_investigation.rs
    - firmware/bitaxe/src/live_stratum_runtime.rs
    - scripts/phase28.1.1-upstream-wire-capture.sh
key-decisions:
  - "The accepted_state_snapshot token is the only new firmware lever and remains false by default"
  - "Safe reads are exactly 0x00, 0x4c, and 0x88 through 0x8c, with no diagnostic writes"
  - "Raw values are reduced at the firmware boundary to typed categories before retained logging"
patterns-established:
  - "Boot-owned UART stages use bounded direct reads; retained stages use one bounded normal-RX window"
requirements-completed: [ASIC-11, CFG-07]
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1-2026-07-09T19-24-27
generated_at: 2026-07-09T20:30:00Z
duration: 28 min
completed: 2026-07-09
---

# Phase 28.1.1 Plan 07: Accepted-State Firmware And Wrapper Summary

**Added an opt-in, one-shot accepted-state observation path and a redaction-safe hardware wrapper without changing default mining behavior.**

## Accomplishments

- Added typed register-read outcomes and category-only markers for post-enumerate, post-mining-ready, post-max-baud, post-mask-reload, and post-first-work stages.
- Limited reads to the chip identity and upstream hashrate-monitor registers, with one bounded burst per stage and no raw writes.
- Added a 360-second-minimum wrapper that gates hardware on reference cleanliness, detector success, exact port equality, credential-path presence, Phase 27 packaging, and redacted evidence capture.
- Extended upstream preparation to enable BM1366 TX/RX diagnostics only in a scratch copy and generate an explicit unavailable marker template until captured observations are normalized.
- Added deterministic coverage for invalid attempts, short captures, missing prerequisites, port mismatch, command routing, closed hypotheses, and credential-content redaction.

## Task Commits

1. **Task 1: Add bounded one-shot accepted-state instrumentation** — `c2e2f67`
2. **Task 2: Add detector-gated accepted-state diagnostic wrapper** — `2017b2e`

## Verification

- `cargo fmt --all` — completed; unrelated baseline rustfmt drift was excluded.
- `cargo clippy --all-targets --all-features -- -D warnings` — passed.
- `cargo build --all-targets --all-features` — passed.
- `cargo test --all-features` — passed.
- Shell diagnostic safety test — `accepted_state_diagnostic_test: passed`.
- `shellcheck` on all three changed shell scripts — passed.
- Phase 27 package build with `accepted_state_snapshot` — ESP32-S3 firmware and package built successfully.
- `git diff --check` — passed.

## Deviations From Plan

- Retained post-mask/post-work register responses use the existing continuous result reader during a fixed five-second observation window. This avoids adding a competing UART reader while preserving the required one-shot TX behavior.
- The upstream scratch helper enables raw TX/RX capture and emits an honest unavailable marker template; it does not invent category values before a real upstream capture is normalized.

## Issues Encountered

- The delegated Plan 07 executor left a partial typed-read implementation and then stalled. The useful typed outcome/safe-register work was completed through the execute-phase fallback.

## Next Phase Readiness

- Plan 08 can run the detector-gated accepted-state attempt first, classify the result, and route to the one allowed lifecycle attempt when observations are missing or idle.
- No hardware or parity claim is made by this implementation-only plan.

## Self-Check: PASSED

- Both task commits exist.
- The firmware package compiles with the new token enabled.
- Default behavior remains gated off by exact-token parsing.
- Wrapper outputs contain only closed categories and redacted metadata.

***

*Phase: 28.1.1-bm1366-nonce-production-wire-parity*
*Completed: 2026-07-09*
