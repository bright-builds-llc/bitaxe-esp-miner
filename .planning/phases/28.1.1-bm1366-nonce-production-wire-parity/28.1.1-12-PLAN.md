---
phase: 28.1.1-bm1366-nonce-production-wire-parity
plan: 12
type: execute
gap_closure: true
wave: 11
depends_on: [28.1.1-11]
files_modified:
  - scripts/phase28.1.1-hardware-attempt-state.mjs
  - scripts/phase28.1.1-hardware-attempt-state-test.mjs
  - scripts/phase28.1.1-exact-head-hardware-attempt.sh
  - scripts/phase28.1.1-exact-head-hardware-attempt-test.sh
  - scripts/phase28.1.1-strict-production-evidence.mjs
  - scripts/phase28.1.1-strict-production-evidence-test.mjs
  - scripts/phase28.1.1-roadmap-phase30-immutability.mjs
  - scripts/phase28.1.1-roadmap-phase30-immutability-test.mjs
  - scripts/BUILD.bazel
  - .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-12-SUMMARY.md
autonomous: true
requirements: [STR-09, ASIC-11, CFG-07]
must_haves:
  truths:
    - "A versioned closed state schema contains every identity, fresh boot-session digest, crash-closed effect phase, lifecycle measurement, named sentinel count, evidence digest, blocker reason, and terminal input used for authorization or classification"
    - "Every terminal cleanup atomically replaces the live resume mapping with a minimal private digest tombstone, so a closed handle remains distinguishable from a never-valid handle without retaining live attempt references"
    - "Seven closed effects are authorized only by one validate-persist-dispatch runner; lifecycle_start alone owns a lease-bound subordinate USB/PTY/monitor state machine"
    - "The agent and presence validator never open credentials; only the fixed flash_reinit_runtime repo-owned consumer chain may open them from a mode-0600 ignored capability"
    - "Invalid lifecycle evidence maps to blocked_safe_evidence_invalid with verification_result=gaps_found and can never map to gaps_found_same_chain_production_markers_absent or passed_same_chain_hardware"
    - "The software plan ends on a clean committed HEAD after focused tests and the mandatory Rust gates, without hardware access or push"
  artifacts:
    - path: scripts/phase28.1.1-hardware-attempt-state.mjs
      provides: "Closed v2 attempt schema, boot/effect/transition tables, lifecycle lease model, and invariants"
      contains: "PHASE28_ATTEMPT_SCHEMA_VERSION"
    - path: scripts/phase28.1.1-exact-head-hardware-attempt.sh
      provides: "Short-lock effect runner and lease-bound lifecycle continuation shell"
      contains: "run_validated_effect"
    - path: scripts/phase28.1.1-strict-production-evidence.mjs
      provides: "Exact five-terminal classifier"
      contains: "blocked_safe_evidence_invalid"
    - path: scripts/phase28.1.1-roadmap-phase30-immutability.mjs
      provides: "Exact Phase 30 section/projection guard"
      contains: "Phase 30"
  key_links:
    - from: scripts/phase28.1.1-exact-head-hardware-attempt.sh
      to: scripts/phase28.1.1-hardware-attempt-state.mjs
      via: "imports the sole schema/effect/transition authority"
      pattern: "validate|authorize|transition"
    - from: scripts/phase28.1.1-strict-production-evidence.mjs
      to: scripts/phase28.1.1-hardware-attempt-state.mjs
      via: "accepts only the closed classifier projection"
      pattern: "lifecycle_status|classifier_input_sha256|terminal_outcome"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1-2026-07-09T19-24-27
generated_at: 2026-07-11T04:05:00Z
---

<objective>
Build and commit the deterministic authorization, lifecycle-lease, classification, and Phase 30 guard tooling needed before another Ultra 205 attempt.

Purpose: remove Plan 11's stale-ordering gap without mixing safety-critical tooling implementation with physical checkpoints. This plan is software-only and performs no detector, board-info, package, flash, monitor, credential-content read, hardware action, push, or closure promotion.

Output: a closed v2 schema, seven-effect runner, lifecycle_start composite model, exact terminal classifier, Phase 30 guard, exhaustive injected-adapter tests, and one clean committed tooling HEAD for Plan 13.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/index.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/verification.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-CONTEXT.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-RESEARCH.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VALIDATION.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-UAT.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-11-PLAN.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-11-SUMMARY.md
@.planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-REVIEW.md
@.planning/debug/phase28.1.1-cold-start-capture-gap.md
@scripts/phase28.1.1-accepted-state-diagnostic.sh
@scripts/phase28.1.1-accepted-state-diagnostic-test.sh
@scripts/process-group.sh
@scripts/detect-ultra205.sh

<state_contract>
`PHASE28_ATTEMPT_SCHEMA_VERSION=exact-head-attempt-v2` is one closed object. Unknown fields, missing fields, invalid enum members, nulls outside the listed domains, or inconsistent derived values are fatal and never repaired. Constructors, validators, transitions, runner fixtures, and classifier adapters import this authority rather than copying field lists.

| Group | Required typed fields and closed domains |
| --- | --- |
| Identity | `schema_version=exact-head-attempt-v2`; `phase=28.1.1`; `tooling_plan=12`; `execution_plan=13`; current `phase_lifecycle_id`; fresh `attempt_id=<32hex>`; `exact_head=<40hex>`; `boot_session_sha256=<64hex>` |
| State/effect | `attempt_state=new|connected_entry_waiting|recovery_waiting|detector_passed|credentials_bound|reference_checked|packaged|reinit_captured|reinit_validated|lifecycle_authorized|armed|removal_attested|absence_observing|restore_waiting|restore_attested|reappearance_observing|capture_running|capture_complete|post_capture_validated|classified|terminal`; `effect_id=<effect-id|null>`; `effect_phase=none|authorized|invoked|completed|failed`; `effect_authorization_nonce=<32hex|null>`; `effect_sequence=<u32>` |
| Checkpoint/recovery | `resume_handle_sha256=<64hex>`; `checkpoint_id=null|plan13-connected-entry|plan13-recovery-usb-replug|plan13-recovery-both-power|plan13-recovery-reset|plan13-recovery-boot-reset|plan13-lifecycle-removal|plan13-lifecycle-restore`; `checkpoint_token=null|plan13-connected-entry-v1|plan13-usb-replug-recovery-v1|plan13-both-power-recovery-v1|plan13-reset-recovery-v1|plan13-boot-reset-recovery-v1|plan13-armed-removal-v1|plan13-barrel-usb-restore-v1`; `expected_response_token=null|ultra205-remains-connected|plan13-usb-replugged-v1|plan13-both-power-restored-v1|plan13-reset-pressed-v1|plan13-boot-reset-assisted-v1|plan13-both-power-paths-removed|plan13-barrel-then-usb-restored`; `expected_user_action=null|keep-connected|replug-usb-with-barrel-retained|remove-both-wait-five-seconds-restore-barrel-then-usb|press-reset-once|perform-boot-reset-board-info-assist-once|remove-both-power-paths-and-attest|restore-barrel-then-usb`; `checkpoint_generation=<u32>`; `created_monotonic_ms=<u64>`; `monotonic_deadline_ms=<u64|null>`; `detector_attempt_count=0..5`; each `usb_replug_count|both_power_count|reset_count|boot_reset_count=0|1` |
| Credential binding | `wifi_credential_state|pool_credential_state=not_run|present|absent`; random `wifi_credential_binding_id|pool_credential_binding_id=<32hex|null>`; `credential_capability_status=not_created|sealed|consumed|destroyed`; `credential_capability_sha256=<64hex|null>`; `runtime_credential_consumption=not_run|pass|fail` |
| Reference/device/package | `reference_guard_state=not_run|pass|fail`; `reference_commit=<40hex|null>`; `reference_guard_output_sha256=<64hex|null>`; `selected_port_state=unknown|one_board205|invalid`; `selected_port_fingerprint_sha256=<64hex|null>`; `manifest_state=not_run|pass|fail`; `manifest_source_commit=<40hex|null>`; `manifest_sha256=<64hex|null>`; `factory_image_sha256=<64hex|null>` |
| Reinit capture | `reinit_capture_started_ms|reinit_capture_ended_ms=<u64|null>`; `reinit_capture_duration_ms=<u64|null>`; `reinit_capture_category=not_run|complete_360s|short|failed`; `reinit_raw_log_sha256=<64hex|null>`; `reinit_classifier_input_sha256=<64hex|null>`; `reinit_five_stage_result=not_run|pass|fail` |
| Lifecycle lease | `lifecycle_substate=not_started|lease_issued|owner_attached|removal_waiting|removal_attested|absence_observing|restore_waiting|restore_attested|reappearance_observing|monitoring|complete|failed`; `lifecycle_lease_id=<32hex|null>`; `lifecycle_capability_sha256=<64hex|null>`; `lifecycle_owner_pid=<positive|null>`; `lifecycle_owner_start_fingerprint_sha256=<64hex|null>`; `lifecycle_deadline_ms=<u64|null>` |
| Lifecycle measurements | `attestation_status=not_requested|waiting|accepted|rejected|expired`; `attestation_accepted_ms=<u64|null>`; `usb_absence_started_ms|usb_absence_ended_ms=<u64|null>`; `usb_absence_ms=<u64|null>`; `usb_absence_category=not_observed|continuous_at_least_5000|too_short|interrupted`; `restore_token_status=not_requested|waiting|accepted|rejected|expired`; `restore_accepted_ms=<u64|null>`; `usb_reappearance_ms=<u64|null>`; `reappearance_elapsed_ms=<u64|null>`; `reappearance_category=not_observed|within_60000|late` |
| Armed sentinels | exact-key objects `armed_prohibited_sentinel_counts` and `capture_complete_prohibited_sentinel_counts`, each with exactly `detector_board_info_effect_count`, `credential_presence_bind_effect_count`, `reference_guard_effect_count`, `package_effect_count`, `flash_reinit_runtime_effect_count`, `lifecycle_start_effect_count`, `post_capture_detector_board_info_effect_count`, `reset_action_count`, `discovery_action_count`, and `unrelated_process_action_count`, all `<u32>`; exact-key objects `armed_permitted_lifecycle_counts` and `capture_complete_permitted_lifecycle_counts`, each with exactly `removal_pty_write_count`, `restore_pty_write_count`, `usb_absence_observation_session_count`, `usb_reappearance_observation_session_count`, and `retained_monitor_start_count`, all `<u32>`; `armed_prohibited_sentinel_sha256|capture_complete_prohibited_sentinel_sha256|armed_permitted_lifecycle_sha256|capture_complete_permitted_lifecycle_sha256=<64hex|null>` |
| Lifecycle capture | `capture_started_ms|capture_ended_ms=<u64|null>`; `capture_duration_ms=<u64|null>`; `capture_category=not_started|running|complete_360s|short|failed`; `lifecycle_raw_log_sha256=<64hex|null>`; `same_chain_raw_log_set_sha256=<64hex|null>`; `classifier_input_sha256=<64hex|null>`; `classifier_output_sha256=<64hex|null>`; `classifier_version=not_run|strict-production-v2`; `lifecycle_status=absent|incomplete|mismatch|match` |
| Cleanup/result | `process_running=true|false`; `cleanup_state=not_needed|complete|unresolved`; `result_correlated=true|false|null`; `power_delta_class=rising_hashing|flat|falling|unavailable|null`; `share_submission_status=accepted|rejected|not_observed|null`; `terminal_outcome=<terminal-id|null>`; `blocker_reason=<blocker-reason>` from the closed table below |

Boot session is a fresh local observation, not a caller value. On Linux read `/proc/sys/kernel/random/boot_id`, trim surrounding ASCII whitespace, require one canonical UUID, prefix `linux-boot-id-v1\0`, and SHA-256 the bytes. On macOS run `/usr/sbin/sysctl -n kern.boottime`, normalize its parsed `sec` and `usec` integers as `sec=<u64>;usec=<u32>`, prefix `macos-kern-boottime-v1\0`, and SHA-256 the bytes. Persist only `boot_session_sha256`, never the raw observation. Re-observe and compare at attempt construction, before every effect authorization, before and after every checkpoint continuation input, before every lifecycle lease CAS/socket delivery, and immediately before classification. An unavailable, malformed, or changed observation authorizes no effect/token and uses only `boot_session_observation_unavailable` or `boot_session_mismatch`. Injected Linux/macOS fixtures, unavailable-command/file fixtures, normalization cases, and reboot mismatch tests are mandatory.

Consistency is mandatory: every end is at or after its start and every duration equals `end-start` under the persisted boot-session digest; `complete_360s` requires duration `>=360000`; continuous absence requires `usb_absence_ms>=5000`; `within_60000` requires `reappearance_elapsed_ms<=60000`; reference/manifest/raw-log/classifier digests must exist before a transition that consumes them; manifest source equals exact HEAD; the classifier input commits to exact HEAD, boot-session digest, reference identity, manifest digest, both raw-log digests, capture timestamps, all four named sentinel digests, and result fields. At arm, all five permitted lifecycle counts are zero. At capture completion the removal/restore PTY writes, USB absence/reappearance observation sessions, and retained monitor start counts are each exactly one; all ten prohibited counts and their prohibited digest remain byte-identical. Missing or inconsistent values transition only to a conservative terminal.

The closed `blocker_reason` members and their only terminal routes are:

| blocker_reason members | Terminal route |
| --- | --- |
| `none` | only a non-blocked classifier result; never with a blocked terminal |
| `resume_handle_missing`, `resume_handle_malformed`, `resume_handle_wrong`, `resume_handle_stale`, `resume_handle_ambiguous`, `checkpoint_token_mismatch`, `checkpoint_generation_mismatch`, `checkpoint_state_mismatch`, `checkpoint_expired`, `boot_session_observation_unavailable`, `boot_session_mismatch`, `exact_head_mismatch`, `dirty_head`, `manifest_mismatch`, `reference_mismatch`, `state_malformed`, `state_missing_field`, `state_unknown_field`, `validator_error`, `lock_failure`, `persistence_failure`, `lease_conflict`, `lease_owner_mismatch`, `lease_dead_or_reused_process`, `effect_in_flight_ambiguous`, `detector_failed`, `detector_recovery_exhausted`, `credential_binding_failed`, `private_capability_invalid`, `credential_consumption_failed`, `reference_guard_failed`, `package_failed`, `reinit_capture_failed`, `reinit_capture_short`, `reinit_stage_mismatch`, `lifecycle_attestation_rejected`, `usb_absence_too_short_or_interrupted`, `usb_reappearance_late`, `monitor_failed`, `lifecycle_capture_failed`, `lifecycle_capture_short`, `post_capture_detector_failed`, `cancelled_or_abandoned` | `blocked_safe_attempt_prerequisite` before lifecycle authorization; after lifecycle authorization reasons whose evidence is absent/incomplete/contradictory route to `blocked_safe_evidence_invalid` as specified by the transition table |
| `sentinel_mismatch`, `classifier_input_invalid`, `classification_inconsistent` | `blocked_safe_evidence_invalid` |
| `process_cleanup_unresolved` | `blocked_safe_unresolved_process` |
| `production_markers_absent` | `gaps_found_same_chain_production_markers_absent`, and only when lifecycle is exactly `match` |

No constructor, transition, adapter, checkpoint, cleanup path, or classifier may invent another blocker string. The transition table maps each context-sensitive member in the second row to its one exact pre/post-lifecycle terminal and tests both sides of that boundary.
</state_contract>

<effect_contract>
The only effect IDs are `detector_board_info`, `credential_presence_bind`, `reference_guard`, `package`, `flash_reinit_runtime`, `lifecycle_start`, and `post_capture_detector_board_info`. `run-validated-effect` holds a mode-0700 mkdir lock only across state parse, fresh head/boot observation, pure validation, mode-0600 fsync+atomic persistence, and fixed dispatch. It accepts an effect ID, never a caller command.

The effect lifecycle is closed. Idle state is `effect_id=null`, `effect_phase=none`, and `effect_authorization_nonce=null`. Authorization generates a fresh nonce, increments `effect_sequence`, sets `effect_id`, persists `effect_phase=authorized`, fsyncs file and directory, and only then creates the fixed adapter child behind a private start gate. The child emits a start acknowledgement without touching the external effect; while holding the lock the runner revalidates head, boot session, state, nonce, and sequence, atomically persists `effect_phase=invoked`, then releases that child's gate so the external effect may begin. Immediately when the adapter returns, the runner reacquires the lock and atomically persists `completed` or `failed` plus the closed transition/blocker fields. Only a validated completed/failed record may be normalized in a later atomic transition back to the idle triple. `authorized` or `invoked` observed on restart, or a `completed|failed` record inconsistent with effect output/sequence/nonce, is permanently ambiguous: clean up, set `effect_in_flight_ambiguous`, terminate conservatively, and never redispatch that effect or any successor. Thus a crash cannot convert uncertain detector, flash, or lifecycle activity into a retry.

`lifecycle_start` is the sole composite authorization for the long-lived physical lifecycle. The short lock persists an immutable lease/capability, exact process PID plus start fingerprint, exact HEAD/reference/manifest/boot-session/sentinel snapshot, and deadline before dispatch; after owner attachment is durably acknowledged the global lock is released. The child alone owns the closed subordinate transitions removal-waiting -> attested -> continuous USB absence -> restore-waiting -> restore-attested -> USB reappearance -> retained-package `--no-reset` monitor -> capture-complete. Each subordinate write briefly reacquires the lock and compare-and-swaps the lease ID, owner identity, deadline, fresh boot-session digest, exact identities, and prior substate. Continuations may only deliver the two exact PTY tokens through this active capability; they are not independent hardware effects. Any other effect while the lease is active is denied.

The twelve invalid categories are `expired`, `token_mismatch`, `exact_head_mismatch`, `manifest_mismatch`, `reference_mismatch`, `boot_session_mismatch`, `dirty_head`, `malformed_state`, `validator_error`, `lock_failure`, `persistence_failure`, and `lease_conflict`. Tests execute the full 7x12 Cartesian product and assert every named prohibited/permitted sentinel remains byte-for-byte unchanged. For every one of the seven effects, inject crashes before and after authorized persistence, child creation, start acknowledgement, invoked persistence, start-gate release, adapter return, and completed/failed persistence; assert that any persisted `authorized|invoked` or inconsistent completion terminates without redispatch. Separate subordinate tests cover wrong/late tokens, PID reuse/start mismatch, lease theft, stale deadline/boot session, disappearance interruption, late reappearance, monitor failure, and atomic state-write failure.
</effect_contract>

<resume_ipc_contract>
The repo-known private root is ignored `hardware-runs/phase28.1.1/attempt-control`, created with `umask 077` and mode 0700. Its repo-known `resume-index/` directory is mode 0700, and each SHA-256 handle digest owns exactly one mode-0600 digest-addressed slot. `begin-attempt` generates a random 32-byte hexadecimal `resume_handle`, returns that opaque handle as the sole locator in every public checkpoint, stores only its SHA-256 in state, and creates an `active` slot mapping that digest to exactly one private attempt directory, attempt ID, attempt generation, checkpoint generation, and boot-session digest. Slot creation rejects an occupied active or tombstone digest and regenerates a handle with a bounded retry count; exhaustion fails closed, and no two attempts may share a digest slot. The public handle never embeds or prints a path, lease, PID, port, credential identity, or capability.

Every normal, blocked, crash, or abandoned terminal cleanup removes every live socket, capability, process, lock, attempt-directory, and other live reference, writes and fsyncs a mode-0600 tombstone in the mode-0700 `resume-index/` root, atomically renames that tombstone over the same digest-addressed active mapping, and fsyncs `resume-index/`. This single terminal transition removes/replaces the live active mapping without a delete-only or no-record interval. The exact closed tombstone has only these fields: `schema_version=exact-head-resume-tombstone-v1`, `resume_handle_sha256=<64hex>`, `attempt_generation=<u32>`, `terminal_status=closed`, `terminal_category=<one of the five terminal IDs>`, and `cleanup_time_category=normal|blocked|crash|abandoned`. The digest is retained and never cleared; the clear `resume_handle` is never stored. A tombstone contains no attempt ID/path, socket, PID/start fingerprint, lease/capability/effect identity, port, boot-session digest, credential identity, process reference, secret, timestamp, or additional field.

`resolve-checkpoint` and `deliver-token` use this exact ordered decision: first validate omission and canonical 64-hex syntax (`resume_handle_missing` or `resume_handle_malformed`); next, one valid active mapping proceeds to full validation and resolves; otherwise one valid tombstone with a digest equal to the requested digest returns `resume_handle_stale`; no active mapping and no tombstone returns `resume_handle_wrong`; any active/tombstone collision, duplicate, mismatched digest, or more than one validated match blocks with `resume_handle_ambiguous` as the collision reason. None is repaired or guessed. Tombstones remain through all Plan 13 execution and final verification. Deterministic bounded pruning is allowed only after Plan 13 final verification and closure-commit synchronization have both succeeded, and it must preserve the current run's tombstone/audit result; pruning may remove only older unrelated tombstones under a bounded retention rule.

The only continuation commands are:

- `bash scripts/phase28.1.1-exact-head-hardware-attempt.sh resolve-checkpoint --resume-handle <64hex>`: resolve the private record, lock and validate it, freshly verify clean head and boot session, and emit the exact public checkpoint block without exposing a path.
- `bash scripts/phase28.1.1-exact-head-hardware-attempt.sh deliver-token --resume-handle <64hex> --checkpoint-token <closed-token> --response-token <closed-response>`: resolve and lock the same record; validate handle uniqueness, state, checkpoint token/generation, exact head, fresh boot session, lease identity/owner/liveness when active, and deadline before and after input parsing; CAS checkpoint consumption; then and only then dispatch the closed continuation.

For the two lifecycle responses, the lease owner alone creates and listens on a mode-0600 Unix-domain socket at a private path inside the attempt directory. `deliver-token` must complete the validations above before opening the socket and sends one length-bounded frame containing only handle digest, checkpoint generation, closed token/response, effect authorization nonce, and lifecycle lease ID read from private state. The child revalidates the same frame, fresh boot session, head, deadline, owner start fingerprint, lease, and substate before translating it to its private PTY. No public continuation opens the PTY or observes USB. Missing socket, malformed/wrong/stale/ambiguous handle, wrong lease/nonce/generation/token, unavailable/mismatched boot observation, expired deadline, dead/reused owner, or multiple socket/index matches fails before socket/PTY writes. Terminal cleanup removes every live reference and atomically replaces the active slot with the closed tombstone; `resolve-checkpoint` and `deliver-token` then return `resume_handle_stale`. Injected tests cover stale resolution after normal, blocked, crash, and abandoned cleanup; a never-valid well-formed handle returning `resume_handle_wrong`; malformed input returning `resume_handle_malformed`; duplicate/collision rejection; tombstone mode/schema/retention/pruning; and byte-level proof that tombstones contain no live attempt, socket, process, capability, path, or secret reference.
</resume_ipc_contract>

<credential_contract>
The agent, state tool, presence validator, and classifier never open credential contents. `credential_presence_bind` canonicalizes each supplied path without printing it, checks regular-file presence/ownership/mode without reading it, creates random attempt-scoped opaque IDs, and writes the canonical paths only to an ignored mode-0600 capability file under a mode-0700 run directory. State stores only present/absent categories, random IDs, capability status, and the private capability digest; none of those IDs/digests are printed or promoted.

The only authorized credential-content consumer is the fixed `flash_reinit_runtime` repo-owned consumer chain rooted at `scripts/phase27-live-hardware-bridge-evidence.sh`: `tools/flash` may read the bound Wi-Fi file for NVS seed and `scripts/phase21-pool-input-bridge.sh` plus its JSON helper may read the bound pool file for runtime configuration. The effect verifies capability digest, IDs, mode, owner, exact attempt, and unconsumed status, passes paths directly without tracing them, then destroys the capability file and records only `runtime_credential_consumption=pass|fail`. No lifecycle or post-capture transition may consume credentials.
</credential_contract>

<terminal_contract>
The sole terminal IDs are:

| Terminal | Exact route |
| --- | --- |
| `blocked_safe_attempt_prerequisite` | pre-lifecycle detector/recovery, credential binding, reference, package/reinit, state, boot-session, resume, or authorization failure; `verification_result=gaps_found` |
| `blocked_safe_unresolved_process` | full process-tree death cannot be proved; `verification_result=gaps_found` and no positive evidence |
| `blocked_safe_evidence_invalid` | lifecycle absent/incomplete/mismatch/contradictory/malformed/wrong-head/mixed-chain or any lifecycle consistency failure; `verification_result=gaps_found` |
| `gaps_found_same_chain_production_markers_absent` | lifecycle exactly `match`, identities/digests/sentinels valid, but result/rising-power/share conjunction incomplete; `verification_result=gaps_found` |
| `passed_same_chain_hardware` | lifecycle `match`, `result_correlated=true`, `power_delta_class=rising_hashing`, and exactly one `accepted|rejected`; `verification_result=passed`, `phase30_promotion_input=eligible` |

Invalid lifecycle evidence is allowed to yield the `gaps_found` verification result only through terminal ID `blocked_safe_evidence_invalid`; it must never yield terminal ID `gaps_found_same_chain_production_markers_absent` or `passed_same_chain_hardware`.
</terminal_contract>

<standards_alignment>
Repo-local guidance supplies detector, timeout, finite recovery, credential, hardware, redaction, and frontmatter boundaries. Bright Builds architecture/testing rules require one typed functional core with exhaustive tests around a thin shell; verification rules require fetch/rebase before edits and repo-native gates before commit. No override, UI, or schema migration applies.
</standards_alignment>

<scope_contract>
The ten `files_modified` entries are exactly the documented warning threshold, not an open-ended change: four implementation/test pairs cover state, runner, classifier, and closure integrity; one existing Bazel registry wires those pairs; one summary records the result. Task 1 owns four pure-core/classifier files, Task 2 owns only the two safety-critical runner files, and Task 3 owns only the two guard files plus Bazel wiring. Each task has its own focused plus mandatory-Rust verification and clean commit boundary. Removing a pair would leave a listed authority untested; combining runner and guard work would recreate the flagged mixed-responsibility task.
</scope_contract>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define the closed v2 state and terminal core after upstream alignment</name>
  <files>scripts/phase28.1.1-hardware-attempt-state.mjs, scripts/phase28.1.1-hardware-attempt-state-test.mjs, scripts/phase28.1.1-strict-production-evidence.mjs, scripts/phase28.1.1-strict-production-evidence-test.mjs</files>
  <read_first>AGENTS.md, AGENTS.bright-builds.md, standards-overrides.md, standards/core/architecture.md, standards/core/verification.md, standards/core/testing.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-11-SUMMARY.md, .planning/phases/28.1.1-bm1366-nonce-production-wire-parity/28.1.1-VERIFICATION.md, .planning/debug/phase28.1.1-cold-start-capture-gap.md, scripts/phase28.1.1-accepted-state-diagnostic.sh</read_first>
  <action>Before substantive edits, require an empty `git status --porcelain=v1`, run `git fetch --prune origin`, require branch `main` with an upstream, and inspect `git rev-list --left-right --count HEAD...@{upstream}`. Stop on dirty state, missing upstream, both-ahead-and-behind divergence, or any rebase conflict. If behind-only, rebase onto the upstream; if zero-zero or ahead-only, continue only with behind count zero. Recheck clean state before editing.

Implement `<state_contract>` as closed exported typed tables and constructors, including the exact boot-session derivation, all four effect lifecycle fields/transitions, every named sentinel key/digest, the complete blocker enum/route table, every measurement/capture field, lease identity, credential binding, and consistency invariant above. Implement `<terminal_contract>` as a pure classifier over one closed projection. Reject unknown/missing/inconsistent inputs rather than inferring them. Add focused Arrange/Act/Assert tests for Linux and macOS boot fixtures, unavailable/malformed/change observations, every state/effect transition and invariant, every blocker member/route, all five terminal IDs, exact invalid-lifecycle routing, all digest/timestamp/sentinel consistency rules, and success only for the four-field same-chain conjunction. Run the mandatory Rust sequence before the task commit.</action>
  <verify><automated>node scripts/phase28.1.1-hardware-attempt-state-test.mjs &amp;&amp; node scripts/phase28.1.1-strict-production-evidence-test.mjs &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features</automated></verify>
  <acceptance_criteria>Sync gate records fetch plus behind=0 and stops on dirty/diverged/conflicted state; schema test rejects every missing/unknown/inconsistent field, normalizes injected Linux/macOS boot observations, blocks unavailable or changed boot sessions, proves the closed effect lifecycle and every named sentinel/timing/digest invariant, and exhausts the blocker enum/route table; classifier tests exercise all five exact terminal IDs; lifecycle absent/incomplete/mismatch maps to blocked_safe_evidence_invalid with verification_result=gaps_found and never to production-markers-absent or passed; focused tests and all four mandatory Rust commands exit 0 before commit.</acceptance_criteria>
  <done>The pure state and terminal authority is complete, tested, and committed without hardware access.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the crash-closed runner, private resume IPC, lifecycle lease, and credential capability</name>
  <files>scripts/phase28.1.1-exact-head-hardware-attempt.sh, scripts/phase28.1.1-exact-head-hardware-attempt-test.sh</files>
  <read_first>scripts/phase28.1.1-hardware-attempt-state.mjs, scripts/phase28.1.1-hardware-attempt-state-test.mjs, scripts/phase28.1.1-strict-production-evidence.mjs, scripts/process-group.sh, scripts/phase28.1.1-accepted-state-diagnostic.sh, scripts/phase28.1.1-accepted-state-diagnostic-test.sh, scripts/phase27-live-hardware-bridge-evidence.sh, scripts/phase21-pool-input-bridge.sh, tools/flash/src/main.rs</read_first>
  <action>Implement `<effect_contract>`, `<resume_ipc_contract>`, and `<credential_contract>` with injected adapters only; do not invoke real detector, board-info, credentials, package, flash, monitor, or hardware. Use `umask 077`, the exact ignored mode-0700 private root/resume index, mode-0600 state/capability/socket surfaces, fsync plus atomic rename, fixed effect dispatch, child start gate, process-group cleanup, and immutable lifecycle lease checks. Implement the exact `begin-attempt`, `resolve-checkpoint`, `deliver-token`, and `run-validated-effect` subcommands. The long-lived child must own every USB observation and retained monitor action; release the global lock only after owner identity is persisted, and require fresh boot observation plus lease-bound compare-and-swap for each subordinate transition and both socket-to-PTY writes.

Build tests for the exact 7 effects x 12 invalid categories, checking every named sentinel; all seven effects at every crash boundary in `<effect_contract>`; success trace adjacency; authorized/invoked restart ambiguity and no redispatch; all lifecycle subordinate failures; armed/capture count/digest invariants; 0700/0600 modes; random opaque handle creation and non-path output; exact resolve/deliver commands; missing, malformed, wrong, stale, ambiguous, repeated, and cleaned-up handles; stale after each normal/blocked/crash/abandoned cleanup; never-valid well-formed handles returning wrong; active/tombstone digest duplicate and generation-collision rejection; tombstone schema, atomic replacement, retention, bounded post-verification pruning, and absence of every live reference; validation before socket open; wrong/missing socket, lease, nonce, generation, PID-start, head, boot session, and deadline; private opaque credential binding/destruction; proof that validators never open contents; proof that only the fixed flash_reinit_runtime consumer adapter may do so; finite recovery order/count; Plan 11 non-reuse; 360000/420-second budgets; and full descendant/active-index/socket cleanup with only the minimal tombstone retained.

Run focused Node/shell tests, `shellcheck`, `shfmt -d`, and then the mandatory Rust sequence. Commit this runner/lifecycle/capability boundary only after all pass, require a clean worktree, and record the intermediate runner commit for Task 3. Do not push.</action>
  <verify><automated>bash scripts/phase28.1.1-exact-head-hardware-attempt-test.sh &amp;&amp; node scripts/phase28.1.1-hardware-attempt-state-test.mjs &amp;&amp; node scripts/phase28.1.1-strict-production-evidence-test.mjs &amp;&amp; shellcheck scripts/phase28.1.1-exact-head-hardware-attempt.sh scripts/phase28.1.1-exact-head-hardware-attempt-test.sh &amp;&amp; shfmt -d scripts/phase28.1.1-exact-head-hardware-attempt.sh scripts/phase28.1.1-exact-head-hardware-attempt-test.sh &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; test -z "$(git status --porcelain=v1)"</automated></verify>
  <acceptance_criteria>All 84 invalid cases and every per-effect crash boundary leave unauthorized sentinels untouched and never redispatch ambiguous work; lifecycle tests prove only the lease owner performs disappearance, absence, reappearance, and monitor observations; every continuation resolves exactly one opaque handle and validates state/head/boot/lease/deadline before opening the private socket; malformed, never-valid wrong, terminal stale, and duplicate/collision cases are deterministic; normal/blocked/crash/abandoned cleanup atomically replaces the live slot with the exact mode-0600 minimal tombstone containing no live references; retention survives Plan 13 final verification, and bounded pruning begins only after final-verification/closure-commit synchronization while preserving the current run's audit result; credential consumption remains sole-consumer/private; focused/shell/Rust gates pass at a clean intermediate commit; no hardware or push occurred.</acceptance_criteria>
  <done>The safety-critical runner, resume path, lifecycle capability, and credential boundary are committed and independently verified.</done>
</task>

<task type="auto">
  <name>Task 3: Add closure-integrity guards, Bazel wiring, and the full integration matrix</name>
  <files>scripts/phase28.1.1-roadmap-phase30-immutability.mjs, scripts/phase28.1.1-roadmap-phase30-immutability-test.mjs, scripts/BUILD.bazel</files>
  <read_first>scripts/phase28.1.1-hardware-attempt-state.mjs, scripts/phase28.1.1-exact-head-hardware-attempt.sh, scripts/phase28.1.1-exact-head-hardware-attempt-test.sh, scripts/phase28.1.1-strict-production-evidence.mjs, scripts/BUILD.bazel, .planning/ROADMAP.md</read_first>
  <action>Starting from Task 2's clean intermediate commit, implement the guard helper and Bazel targets/data without changing runner semantics. Preserve exact Phase 30 extraction from `### Phase 30:` through the next `### Phase ` peer or any `## ` heading and separately guard its Requirements, Plans, and Progress-row status. Add `check-frontmatter`, which accepts explicit file arguments and fails unless each named frontmatter-bearing file starts with frontmatter and contains exactly two standalone `---` lines. Add `check-zero-frontmatter`, which accepts explicit file arguments and fails unless each contains exactly zero standalone `---` lines; `.planning/ROADMAP.md` is intentionally frontmatterless and must use only this helper before and after Plan 13, without adding YAML frontmatter. Add `snapshot-protected` and `compare-protected` for the closed protected set `phase28.1.1-prior-evidence-v1`, whose exact members are `28.1.1-accepted-state-baseline-redacted.md`, `28.1.1-accepted-state-disposition-redacted.md`, `28.1.1-accepted-state-lifecycle-redacted.md`, `28.1.1-final-share-evidence-redacted.md`, `28.1.1-init-parser-fixes-redacted.md`, `28.1.1-job-hypothesis-runs-redacted.md`, `28.1.1-live-share-outcome-redacted.md`, `28.1.1-run-postfix-rust-redacted.md`, `28.1.1-run-wire-parity-redacted.md`, `28.1.1-upstream-wire-capture-redacted.md`, and `28.1.1-wire-diff-summary.md` under the Phase 28.1.1 directory. Snapshot writes a mode-0600 digest manifest only to the ignored private attempt root; compare requires every listed path and byte digest to match that pre-edit baseline.

Test exact/adjacent/missing Phase 30 boundaries; missing/extra/misplaced frontmatter delimiters; zero-delimiter ROADMAP acceptance and any ROADMAP delimiter rejection; missing/changed/extra protected-set entries; private manifest mode; and all Bazel targets. Rerun the complete state, classifier, 84-case, crash-boundary, resume/IPC, lifecycle, credential, closure-integrity, and process-cleanup matrix as the intermediate integration boundary. Then run shell checks and the mandatory Rust sequence. Commit only after all pass; require a clean worktree and record this final 40-hex task commit as `tooling_implementation_head` in the Plan 12 summary. The normal GSD metadata commit that adds the summary/STATE/ROADMAP is the later Plan 12 completion commit; Plan 13 captures that clean current HEAD as `plan12_completion_head`. Do not push.</action>
  <verify><automated>bash scripts/phase28.1.1-exact-head-hardware-attempt-test.sh &amp;&amp; node scripts/phase28.1.1-hardware-attempt-state-test.mjs &amp;&amp; node scripts/phase28.1.1-strict-production-evidence-test.mjs &amp;&amp; node scripts/phase28.1.1-roadmap-phase30-immutability-test.mjs &amp;&amp; shellcheck scripts/phase28.1.1-exact-head-hardware-attempt.sh scripts/phase28.1.1-exact-head-hardware-attempt-test.sh &amp;&amp; shfmt -d scripts/phase28.1.1-exact-head-hardware-attempt.sh scripts/phase28.1.1-exact-head-hardware-attempt-test.sh &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; test -z "$(git status --porcelain=v1)"</automated></verify>
  <acceptance_criteria>Exact Phase 30 boundaries, two-delimiter frontmatter-bearing files, zero-delimiter frontmatterless ROADMAP shape, and every explicit prior-evidence path are guarded by deterministic repo-owned commands; neither helper requires nor introduces ROADMAP YAML frontmatter; the private baseline manifest is mode 0600 and absent from promoted output; Bazel exposes all new tests; the complete integration matrix, shell checks, and mandatory Rust gates pass at a clean committed tooling_implementation_head; the later metadata commit leaves a clean Plan 12 completion HEAD for Plan 13; no hardware or push occurred.</acceptance_criteria>
  <done>Plan 13 has a clean committed, fully wired and exhaustively tested software authority for one finite hardware attempt.</done>
</task>
</tasks>

<threat_model>
## ASVS Level 1 Threat Model

| ID | ASVS / STRIDE | Threat | Severity | Mitigation | Blocking condition |
| --- | --- | --- | --- | --- | --- |
| T-12-01 | V4 / Tampering | Missing lifecycle fields or side files silently authorize/classify | critical | Closed schema plus digest/timing/snapshot invariants | Any authorization/classification input exists outside the closed identities |
| T-12-02 | V4 / Tampering | Invalid state reaches an effect | critical | Short-lock validate-persist-fixed-dispatch and 84-case matrix | Any invalid case touches any sentinel |
| T-12-03 | V1 / Spoofing | Long-lived child or PID reuse steals lifecycle authority | critical | Immutable lease, PID-start fingerprint, deadline, CAS transitions | Wrong owner/lease can mutate state or touch hardware |
| T-12-04 | V8 / Information Disclosure | Credential path/content/hash escapes | critical | Opaque binding, private 0600 capability, fixed consumer, no promotion | Any path/content/private digest is logged or promoted |
| T-12-05 | V14 / Denial of Service | Global lock is held through a six-minute lifecycle or descendant survives | high | Release after owner attach; bounded per-transition lock; TERM/KILL verification | Continuations block on global lock or death is unproved |
| T-12-06 | V5 / Repudiation | Invalid lifecycle becomes production-negative or success evidence | high | Exact terminal IDs and classifier tests | Invalid lifecycle reaches production-markers-absent or passed |
| T-12-07 | V1 / Repudiation | Tooling edits Phase 30 or checklist | high | Exact section/projection helper; Plan 13 owns any later closure | Guard permits Phase 30/checklist mutation |
| T-12-08 | V1 / Spoofing | Reboot resets monotonic time while stale state still appears live | critical | Fresh platform boot-session digest before every authorization/CAS/continuation/classification | Observation is unavailable or differs without blocking |
| T-12-09 | V4 / Tampering | Crash leaves detector/flash/lifecycle invocation ambiguous and a retry duplicates it | critical | Authorized/invoked/completed/failed lifecycle, child start gate, terminal no-redispatch rule | Any ambiguous persisted effect is dispatched again |
| T-12-10 | V1 / Spoofing | Fresh continuation cannot locate the attempt, confuses a closed handle with a never-valid handle, or reaches another attempt's PTY | critical | Opaque resume handle, unique 0600 digest slot, atomic minimal terminal tombstone, lease-bound Unix socket, double validation | Missing/malformed/wrong/stale/ambiguous/colliding handle or lease can open socket/PTY, or a tombstone retains a live reference |

All high and critical threats are release-blocking.
</threat_model>

<verification>
Plan 12 is software-only. It validates plan-specific Node/shell surfaces, boot-session fixtures, crash injection, resume/IPC resolution, shell format/lint, exhaustive effect/subordinate fixtures, and the mandatory Rust sequence at each of three cohesive commit boundaries. Completion requires clean committed HEAD and no detector, credential content, package, flash, monitor, hardware, push, or promotion action.
</verification>

<success_criteria>
The plan is complete when one closed schema owns all authorization/classification inputs, seven effects and the lifecycle subordinate machine are exhaustively guarded, credential consumption is executable but privately bound, terminal routing is exact, Phase 30 is immutable, all tests pass, and Plan 13 receives a clean committed tooling_exact_head.
</success_criteria>

<output>After execution, create `28.1.1-12-SUMMARY.md` containing `tooling_implementation_head`, passed gates, zero hardware actions, and zero pushes. After the normal GSD metadata commit, leave the worktree clean; Plan 13 captures that current commit as `plan12_completion_head`.</output>
