# Phase 28.1.1 Job Hypothesis Runs

job_hypothesis_status: no_code_change_no_evidence_backed_hypothesis
raw_bytes_committed: false
credential_contents_read: false
capture_timeout: 360
source_evidence: 28.1.1-run-postfix-rust-redacted.md
evidence_backed_fix_attempts_after_clean_wire_diff: 0
fix_loop_status: continue

## Evidence Review

The known job-frame divergences from earlier Phase 28.1.1 work are already
addressed in the current code: upstream-style ASIC job-id pre-increment,
`mining.configure` before subscribe, and nonzero version-mask acceptance during
work construction.

The post-fix hardware run still produced no correlated nonce, but the committed
redacted evidence does not identify a new concrete job-field mismatch. Without
an upstream scratch SERIALTX_DEBUG golden job-byte comparison, another job-field
code change would be speculative and would violate the phase decision to fix
one evidence-backed divergence at a time.

## Test Results

tests:
- `cargo test -p bitaxe-asic bm1366_work -- --nocapture` passed with 4 passed.
- `cargo test -p bitaxe-stratum mining_job -- --nocapture` passed with 7 passed.
- `cargo test -p bitaxe-stratum live_runtime -- --nocapture` passed with 15 passed.

## Hardware Marker Carry-Forward

| Marker | Count | Source |
| --- | ---: | --- |
| `asic_production_status=result_correlated` | 0 | Plan 02 post-fix run |
| `share_submission_status=accepted` | 0 | Plan 02 post-fix run |
| `share_submission_status=rejected` | 0 | Plan 02 post-fix run |

## Conclusion

No Plan 04 Rust code was changed. The next evidence-backed step remains upstream
scratch golden job-byte confirmation or a narrower field-level comparator that
can identify one concrete job-field mismatch before another hardware rerun.
