---
phase: "29"
plan: "03"
type: execute
wave: 3
depends_on:
  - "29-02"
files_modified:
  - scripts/phase29-doc-redaction-check.sh
  - scripts/phase29-doc-redaction-check-test.sh
  - scripts/BUILD.bazel
  - docs/release/ultra-205.md
  - docs/parity/evidence/phase-29-evidence-workflow-automation-closure/summary.md
  - docs/parity/evidence/phase-29-evidence-workflow-automation-closure/redaction-review.md
  - docs/parity/evidence/phase-29-evidence-workflow-automation-closure/conclusion.md
  - .planning/phases/29-evidence-workflow-automation-closure/29-VALIDATION.md
autonomous: true
requirements:
  - EVD-07
  - EVD-08
  - EVD-09
  - REL-09
generated_by: gsd-planner
lifecycle_mode: yolo
phase_lifecycle_id: 29-2026-07-13T00-19-45
generated_at: 2026-07-13T00:35:00.000Z
must_haves:
  truths:
    - The Ultra 205 operator guide documents automatic strict validation for Phase 25, Phase 27, and Phase 28.
    - Phase 29 evidence records only commands, category labels, test outcomes, and exact non-claims.
    - The parity checklist remains byte-identical; Phase 29 evidence documents automation closure without any status or note mutation.
    - All Rust, Bazel, parity, reference, formatting, diff, and lifecycle gates pass before phase verification.
  artifacts:
    - path: docs/release/ultra-205.md
      provides: Exact repo-owned Phase 25/27/28 operator commands and failure semantics.
    - path: scripts/phase29-doc-redaction-check.sh
      provides: Diff-aware fail-on-match scan for new operator-guide lines and all Phase 29 evidence docs.
    - path: docs/parity/evidence/phase-29-evidence-workflow-automation-closure/summary.md
      provides: Static automation and regression evidence for all four Phase 29 requirements.
    - path: docs/parity/evidence/phase-29-evidence-workflow-automation-closure/redaction-review.md
      provides: Category-only review of generated roots, traces, and documentation.
    - path: docs/parity/evidence/phase-29-evidence-workflow-automation-closure/conclusion.md
      provides: Exact claims and residual non-claims for workflow automation closure.
    - path: .planning/phases/29-evidence-workflow-automation-closure/29-VALIDATION.md
      provides: Completed Nyquist execution map and sign-off.
  key_links:
    - from: docs/release/ultra-205.md
      to: Justfile
      via: exact `just phase25-evidence`, `just phase27-evidence`, and `just phase28-evidence` commands
    - from: docs/parity/evidence/phase-29-evidence-workflow-automation-closure/summary.md
      to: docs/release/ultra-205.md
      via: exact tested Phase 25/27/28 command names and non-claim language
---

<objective>
Publish the tested operator flow and close Phase 29 with redaction-safe evidence, conservative checklist breadcrumbs, and the complete verification gate.

Purpose: make the automated validate/consolidate behavior usable and auditable without claiming new hardware or share outcomes.
Output: updated Ultra 205 instructions, a Phase 29 static evidence set, unchanged conservative checklist statuses, signed-off validation metadata, and clean repo verification.
</objective>

<execution_context>
@.planning/phases/29-evidence-workflow-automation-closure/29-CONTEXT.md
@.planning/phases/29-evidence-workflow-automation-closure/29-RESEARCH.md
@.planning/phases/29-evidence-workflow-automation-closure/29-VALIDATION.md
@.planning/phases/29-evidence-workflow-automation-closure/29-02-SUMMARY.md
</execution_context>

<threat_model>
## Threat Model

**Assets:** operator trust, committed evidence confidentiality, checklist exactness, reproducible command documentation.

**Trust boundaries:** local command output into committed docs; generated evidence summaries into checklist claims; documented paths/flags into operator execution.

| Threat | Severity | Mitigation | Verification |
| --- | --- | --- | --- |
| Secret, endpoint, device, or local identifier copied into docs | High | Record command templates, category labels, booleans, and test target names only; scan new guide lines plus all Phase 29 evidence docs with repo-owned secret and local/network patterns. | The Phase 29 doc-redaction Bazel regression and live baseline comparison both exit 0. |
| Documentation claims validation that wrappers do not perform | High | Copy exact tested command order and flags from scripts/Justfile after Plan 02. | Static tests/grep compare docs to executable command names and required flags. |
| Checklist status promoted without hardware evidence | High | Keep existing statuses unchanged and add only Phase 29 automation breadcrumbs/exact non-claims. | `git diff` status-column review plus `just parity` negative fixtures. |
| Stale or ambiguous source-root instructions | Medium | Require explicit Phase 27 source and dedicated distinct Phase 28 destination in examples. | Docs contain both flags and warn that equal/nested roots fail closed. |
| Formatter corrupts GSD frontmatter | Medium | Do not run mdformat write-mode on frontmatter files; validate exactly two top delimiters and run lifecycle parsing. | Delimiter counts and `gsd-tools verify lifecycle` pass. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Document the automated Phase 25, Phase 27, and Phase 28 operator flow</name>
  <files>scripts/phase29-doc-redaction-check.sh, scripts/phase29-doc-redaction-check-test.sh, scripts/BUILD.bazel, docs/release/ultra-205.md, docs/parity/evidence/phase-29-evidence-workflow-automation-closure/summary.md, docs/parity/evidence/phase-29-evidence-workflow-automation-closure/redaction-review.md, docs/parity/evidence/phase-29-evidence-workflow-automation-closure/conclusion.md</files>
  <read_first>
    - docs/release/ultra-205.md
    - Justfile
    - scripts/phase25-live-stratum-evidence.sh
    - scripts/phase27-live-hardware-bridge-evidence.sh
    - scripts/phase28-evidence.sh
    - scripts/phase28.1.1-promoted-evidence-denylist.sh
    - scripts/phase28.1.1-promoted-evidence-denylist.patterns
    - scripts/BUILD.bazel
    - docs/parity/evidence/phase-23-redacted-operator-evidence-workflow/evidence-contract.md
    - docs/parity/evidence/phase-28-hardware-evidence-and-checklist-promotion/evidence-contract.md
    - AGENTS.md
  </read_first>
  <action>
Before documentation edits, create `scripts/phase29-doc-redaction-check-test.sh` and its Bazel target with fixtures proving clean added lines pass and each of these categories fails: `/Users/`, `/home/`, Windows drive paths, IPv4, IPv6, MAC addresses, `http://`/`https://`, Phase 27 raw sentinel tokens, and the existing pool/Wi-Fi/password/token/raw-value patterns. Run the target and record the expected red failure caused by the missing checker locally; do not commit the red state.

Create `scripts/phase29-doc-redaction-check.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. Require `--baseline-ref COMMIT` and `--evidence-root PATH`. Resolve and validate the baseline commit, collect only added content lines (exclude `+++` diff metadata) from `git diff --unified=0 "$baseline_ref" -- docs/release/ultra-205.md`, and scan those lines plus the full `summary.md`, `redaction-review.md`, and `conclusion.md`. Reuse `phase28.1.1-promoted-evidence-denylist.sh` for existing secret/raw patterns, then apply explicit fail-on-match patterns for `/Users/`, `/home/`, `[A-Za-z]:\\`, IPv4, IPv6, MAC addresses, URLs, and the test-only Phase 27 raw sentinel. Use a mode-0700 temporary directory and mode-0600 aggregate file with trap cleanup; print only category-safe pass/fail labels and never matching content. Add `sh_binary(name = "phase29_doc_redaction_check", ...)` and `sh_test(name = "phase29_doc_redaction_check_test", ...)` targets to `scripts/BUILD.bazel`, including the existing denylist script/pattern file as data.

Update `docs/release/ultra-205.md` so the Phase 25 and Phase 27 sections state that their wrappers complete the canonical eleven-slot root and finish with `operator-evidence --profile phase25|phase27 --require-redaction-passed`. Document that a post-setup blocked/detector failure still writes and validates a complete blocked root but returns nonzero. Preserve hardware-first detection, runtime-only credential, redaction, and 360-second instructions.

Add a Phase 28 section with the exact command shape `just phase28-evidence --phase27-root <committed-phase27-root> --evidence-root <dedicated-phase28-root>`. State that roots must be distinct/non-nested, only committed allowlisted categories and relative cross-links are rendered, accepted/rejected/blocked outcomes retain their exact proof/non-claims, reruns are deterministic, and a failed staging validation leaves the prior destination unchanged.

Create the Phase 29 `summary.md`, `redaction-review.md`, and `conclusion.md`. Record source commit and reference commit as hashes only when the repo-owned evidence convention permits them, exact test/verification command names, observed pass/fail booleans, `raw_artifacts_committed: no`, `pool_config: not-read-by-phase29`, `wifi_config: not-read-by-phase29`, and exact non-claims for new hardware behavior, live share proof, safety promotion, Phase 30 statuses, non-205 boards, recovery/fault injection, Stratum v2, UI/BAP, and unbounded stress. Never paste raw test logs or local paths.
  </action>
  <verify>
    <automated>bazel test //scripts:phase29_doc_redaction_check_test</automated>
    <automated>bazel run //scripts:phase29_doc_redaction_check -- --baseline-ref "$(git log -1 --format=%H -- .planning/phases/29-evidence-workflow-automation-closure/29-02-SUMMARY.md)" --evidence-root docs/parity/evidence/phase-29-evidence-workflow-automation-closure</automated>
    <automated>rg -n "phase28-evidence|operator-evidence|require-redaction-passed|distinct|non-nested|raw_artifacts_committed: no" docs/release/ultra-205.md docs/parity/evidence/phase-29-evidence-workflow-automation-closure</automated>
  </verify>
  <acceptance_criteria>
    - The operator guide names automatic strict validation for Phase 25 and Phase 27.
    - The operator guide contains the exact `just phase28-evidence --phase27-root ... --evidence-root ...` shape and fail-closed rerun semantics.
    - Phase 29 summary maps EVD-07, EVD-08, EVD-09, and REL-09 to exact commands/tests/artifacts.
    - Redaction review declares `raw_artifacts_committed: no`, `pool_config: not-read-by-phase29`, and `wifi_config: not-read-by-phase29`.
    - Conclusion lists exact workflow claims and all required residual non-claims without promoting Phase 30 outcomes.
    - The Phase 29 doc-redaction Bazel test exits 0 and the live baseline scan covers newly added Ultra 205 guide lines plus all three Phase 29 evidence documents.
    - The scanner rejects `/Users/`, `/home/`, Windows drive paths, IPv4, IPv6, MAC addresses, URLs, raw Phase 27 sentinels, and the existing secret/raw denylist categories without printing matched content.
  </acceptance_criteria>
  <commit_gate>
Before committing this task, run these commands separately and in this exact order, stopping on the first failure: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Then run the Phase 29 doc-redaction Bazel test, the live baseline scan, and the content-presence check separately. Do not commit unless every command passes.
  </commit_gate>
  <done>The operator guide and three Phase 29 evidence documents contain exact tested commands/non-claims, the denylist and local-identifier scans pass, and the full ordered Rust gate passes before commit.</done>
</task>

<task type="auto">
  <name>Task 2: Prove checklist immutability and complete the verification contract</name>
  <files>.planning/phases/29-evidence-workflow-automation-closure/29-VALIDATION.md</files>
  <read_first>
    - docs/parity/checklist.md
    - tools/parity/src/main.rs
    - docs/parity/evidence/phase-29-evidence-workflow-automation-closure/summary.md
    - docs/parity/evidence/phase-29-evidence-workflow-automation-closure/redaction-review.md
    - docs/parity/evidence/phase-29-evidence-workflow-automation-closure/conclusion.md
    - .planning/phases/29-evidence-workflow-automation-closure/29-VALIDATION.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
Do not edit `docs/parity/checklist.md`. Mechanically compare it to the commit that created `29-02-SUMMARY.md` using `git diff --quiet "$(git log -1 --format=%H -- .planning/phases/29-evidence-workflow-automation-closure/29-02-SUMMARY.md)" -- docs/parity/checklist.md`. This whole-file comparison is stronger than checking only EVD-07, EVD-08, EVD-09, REL-09, STR-09, CFG-07, and ASIC-11 status fields; any note, pointer, or status drift fails.

Run the complete verification matrix. Update `29-VALIDATION.md` task statuses to green only for commands actually run, set `wave_0_complete: true` only after the new Phase 28 test target and profile fixtures exist, set `nyquist_compliant: true` only when every task has an automated command and no sampling gap remains, and record approval date `2026-07-12` only after all mapped commands pass. If any command fails, leave status draft/pending and stop before a completion claim.

Review the final diff for raw/local identifiers, status-column changes, unrelated edits, generated frontmatter corruption, and copied Phase 27 artifacts. Use lifecycle validation with the same `29-2026-07-13T00-19-45` ID; phase verification itself will be produced by the execute-phase verifier after plan completion.
  </action>
  <verify>
    <automated>git diff --quiet "$(git log -1 --format=%H -- .planning/phases/29-evidence-workflow-automation-closure/29-02-SUMMARY.md)" -- docs/parity/checklist.md</automated>
    <automated>just parity</automated>
  </verify>
  <acceptance_criteria>
    - The whole-file checklist comparison against the Plan 02 summary commit exits 0, proving every checklist row and status is byte-for-byte unchanged.
    - `29-VALIDATION.md` contains no provisional target names and every listed automated command exists or creates its target in Plan 02.
    - `cargo fmt --all`, clippy with `-D warnings`, all-target build, and all-feature tests exit 0 in that order.
    - All affected Bazel parity/script tests, `just parity`, and `just verify-reference` exit 0.
    - `git diff --check` and lifecycle validation exit 0.
  </acceptance_criteria>
  <commit_gate>
Before committing this task, run these commands separately and in this exact order, stopping on the first failure: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Then run the checklist comparison, all affected Bazel tests, `just parity`, `just verify-reference`, `git diff --check`, and lifecycle validation separately. Do not commit unless every command passes.
  </commit_gate>
  <done>The checklist is byte-identical to the Plan 02 baseline, validation metadata reflects only commands actually passed, every repo-native gate is green, and lifecycle validation succeeds.</done>
</task>

</tasks>

<verification>
- Run `cargo fmt --all`.
- Run `cargo clippy --all-targets --all-features -- -D warnings`.
- Run `cargo build --all-targets --all-features`.
- Run `cargo test --all-features`.
- Run `bazel test //tools/parity:tests //scripts:phase23_redacted_operator_evidence_test //scripts:phase25_live_stratum_evidence_test //scripts:phase27_live_hardware_bridge_evidence_test //scripts:phase28_evidence_test //scripts:phase29_doc_redaction_check_test`.
- Run `bazel run //scripts:phase29_doc_redaction_check -- --baseline-ref "$(git log -1 --format=%H -- .planning/phases/29-evidence-workflow-automation-closure/29-02-SUMMARY.md)" --evidence-root docs/parity/evidence/phase-29-evidence-workflow-automation-closure`.
- Run `just parity`.
- Run `just verify-reference`.
- Run available check-mode Markdown/shell formatters only when they preserve required frontmatter; never run mdformat write-mode on GSD frontmatter files.
- Run `git diff --check`.
- Run `node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 29 --require-plans --raw`.
</verification>

<success_criteria>
- Operators have one exact repo-owned command path for Phase 25, Phase 27, and Phase 28 validation.
- Phase 29 evidence is redaction-safe, category-only, and maps every requirement to tested artifacts.
- The parity checklist remains byte-identical while Phase 29 evidence documents the automation closure separately.
- Nyquist validation, repo-native verification, reference cleanliness, and lifecycle provenance all pass.
</success_criteria>

<output>
After completion, create `.planning/phases/29-evidence-workflow-automation-closure/29-03-SUMMARY.md` with all verification commands/results, checklist status-diff review, redaction review, and residual risks.
</output>
