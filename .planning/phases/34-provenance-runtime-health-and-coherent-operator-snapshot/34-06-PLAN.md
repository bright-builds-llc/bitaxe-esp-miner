---
phase: 34-provenance-runtime-health-and-coherent-operator-snapshot
plan: "06"
type: execute
wave: 6
depends_on:
  - "34-05"
autonomous: true
gap_closure: true
phase_lifecycle_id: 34-2026-07-15T03-26-15
lifecycle_mode: interactive
generated_by: gsd-plan-phase
generated_at: "2026-07-15T18:20:21Z"
requirements:
  - HLT-02
  - HLT-04
files_modified:
  - firmware/bitaxe/src/safety_adapter/watchdog.rs
  - firmware/bitaxe/BUILD.bazel
  - tools/parity/src/phase34_source_guard.rs
must_haves:
  truths:
    - "Every completed non-restart supervisor step publishes a new monotonic checkpoint even after the one-time yield log has been suppressed."
    - "Log deduplication controls only repeated log emission and cannot bypass checkpoint state mutation."
    - "A deterministic production-path regression proves repeated supervisor steps keep runtime health healthy instead of aging one startup checkpoint into stale or unhealthy."
    - "The correction remains passive and does not register, feed, configure, or reset a watchdog or touch hardware."
  artifacts:
    - path: firmware/bitaxe/src/safety_adapter/watchdog.rs
      provides: "Single-exit supervisor transition that separates optional logging from mandatory checkpoint publication"
    - path: firmware/bitaxe/BUILD.bazel
      provides: "Host-runnable test target compiled from the production watchdog module"
    - path: tools/parity/src/phase34_source_guard.rs
      provides: "Supplementary guard against a logging branch returning before checkpoint publication"
  key_links:
    - from: firmware/bitaxe/src/safety_adapter/watchdog.rs
      to: crates/bitaxe-core/src/runtime_health.rs
      via: "each accepted step records a validated CheckpointObservation with the supplied monotonic timestamp"
    - from: firmware/bitaxe/src/safety_adapter/watchdog.rs
      to: firmware/bitaxe/src/runtime_health_adapter.rs
      via: "the adapter reads the continually advancing checkpoint history used by age-derived health"
---

<objective>
Close HLT-02 and HLT-04 by ensuring recurring supervisor progress is recorded independently of one-time log suppression.

Purpose: remove the early return that freezes the producer checkpoint after the first yield and causes a healthy running supervisor to age into stale/unhealthy.
Output: a single-exit production transition, a host-runnable production-module regression, and source-order protection without any watchdog or hardware effect.
</objective>

<execution_context>
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-CONTEXT.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-VALIDATION.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-VERIFICATION.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-04-SUMMARY.md
</execution_context>

<threat-model level="ASVS-L1" block="high">

| ID | Threat | Severity | Required mitigation and verification |
| --- | --- | --- | --- |
| T-34-G6 | A duplicate-log suppression branch skips checkpoint publication and falsely reports a running supervisor as stale/unhealthy. | high | Make logging an optional outcome while checkpoint mutation is unconditional for every completed non-restart step; test many repeated yield decisions through the production transition. |
| T-34-G7 | A synthetic test helper diverges from the firmware path and masks a future recurrence. | high | Compile the production watchdog module into the host Bazel regression target and inject only the monotonic timestamp/state needed for deterministic testing. |
| T-34-G8 | Concurrent readers observe regressed, duplicated, or fabricated checkpoint history. | high | Keep the existing mutex boundary, checked sequence increment, observation validation, and previous/latest transition; tests assert strict sequence and timestamp progression. |
| T-34-G9 | The health fix becomes active watchdog control, fault/load injection, sleep-based test timing, or hardware/mining control. | critical | Preserve passive observation only; source guards reject task-watchdog mutation, reset, GPIO/I2C, fan/voltage/power/ASIC/mining, UART/pins, USB, credentials, and network paths. |
| T-34-G10 | Logs or failures disclose secrets or local identifiers. | high | Retain the existing bounded static reason/category vocabulary and add no credential, endpoint, device, path, or network values. |

</threat-model>

<commit-contract>
Task 34-06-01 ends in one atomic task commit and no push. Before that commit, from the repository root, run this exact Rust sequence in order and stop on the first failure: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Run the focused Cargo/Bazel checks and `git diff --check` after that sequence. Do not commit the red regression; capture its deterministic failure before production edits and commit only the green state.
</commit-contract>

<tasks>

<task type="auto" id="34-06-01">
  <name>Publish recurring supervisor checkpoints after log suppression</name>
  <read-first>
    - .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-VERIFICATION.md
    - firmware/bitaxe/src/safety_adapter/watchdog.rs
    - firmware/bitaxe/src/runtime_health_adapter.rs
    - crates/bitaxe-core/src/runtime_health.rs
    - crates/bitaxe-safety/src/watchdog.rs
    - firmware/bitaxe/BUILD.bazel
    - tools/parity/src/phase34_source_guard.rs
  </read-first>
  <files>
    - firmware/bitaxe/src/safety_adapter/watchdog.rs
    - firmware/bitaxe/BUILD.bazel
    - tools/parity/src/phase34_source_guard.rs
  </files>
  <action>
First add a deterministic Arrange/Act/Assert regression compiled from the production `watchdog.rs` module. Drive repeated supervisor steps at the existing 100 ms cadence with a local checkpoint state and injected monotonic timestamps. Require the second and later `YieldNow` decisions to advance the latest sequence/timestamp even though the informational yield log is already suppressed; evaluate the resulting checkpoint at the end with the Phase 34 runtime-health model and require `healthy`. Run the focused target and record the red failure caused by the current `return`; do not weaken the test or commit red.

Refactor the production step into a testable single-exit transition over `SupervisorCheckpointState`, `logged_yield`, and an injected `observed_at_millis`. The loop obtains the real uptime once and passes it in. A `YieldNow` decision may return an optional one-time log action, but no logging branch may return from the step before checkpoint publication. Preserve checked sequence increment, category bounds, previous/latest history, monotonic validation, poison handling, and the existing passive behavior for all decision variants. Do not add `unwrap()`, wall-clock sleeps to the test, active task-watchdog registration/feed/configuration, reset, or any device effect.

Add a focused Bazel `rust_test` whose crate root is the production watchdog source and whose dependencies are only the existing core/safety/log libraries. Extend the Phase 34 source guard to reject an early `return` in the duplicate-log branch and require checkpoint publication after decision/log handling; keep this source guard supplementary to the behavioral test. Update the runtime-health target group if necessary so the production-path regression runs in normal Bazel verification.

Run the exact commit-contract Rust sequence, then `cargo test -p bitaxe-core runtime_health`, `bazel test //firmware/bitaxe:supervisor_checkpoint_production_tests //firmware/bitaxe:runtime_health_tests //firmware/bitaxe:runtime_health_no_effects_test //tools/parity:tests`, and `git diff --check`; review and create one atomic commit for task 34-06-01 only.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-core runtime_health &amp;&amp; bazel test //firmware/bitaxe:supervisor_checkpoint_production_tests //firmware/bitaxe:runtime_health_tests //firmware/bitaxe:runtime_health_no_effects_test //tools/parity:tests &amp;&amp; git diff --check</automated>
  </verify>
  <done>Recurring supervisor steps advance checkpoint history after log suppression, and production-path health stays healthy at the documented cadence without active effects.</done>
  <acceptance-criteria>
    - The regression fails against the old early return and passes only when second and later yield decisions publish checkpoints.
    - At least ten deterministic consecutive steps produce strictly increasing sequence and monotonic observation time in the production transition.
    - The last checkpoint evaluates as healthy at the existing cadence; a separately frozen checkpoint still becomes stale/unhealthy under the Plan 34-04 thresholds.
    - The informational yield line is emitted at most once while checkpoint publication occurs on every completed step.
    - The production path contains no active task-watchdog, reset, load/fault, GPIO/I2C, fan/voltage/power/ASIC/mining, UART/pin, USB, credential, or network effect.
    - Task 34-06-01 has one atomic commit created only after the exact required Rust sequence and focused checks pass.
  </acceptance-criteria>
</task>

</tasks>

<verification>

- Confirm the production-path regression failed on the old duplicate-log early return and passes after the single-exit refactor.
- Run `cargo test -p bitaxe-core runtime_health` and `bazel test //firmware/bitaxe:supervisor_checkpoint_production_tests //firmware/bitaxe:runtime_health_tests //firmware/bitaxe:runtime_health_no_effects_test //tools/parity:tests`.
- Run the final exact sequence `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, `cargo test --all-features`, followed by `bazel test //...`, `just build`, `just package`, `just verify-reference`, and `git diff --check`.
- No shell file is modified by this plan, so shellcheck/shfmt are not applicable. If execution unexpectedly changes one, stop and re-plan before running the repository shell checks.
- Review for forbidden hardware/USB/network/credential/Phase 35/OTA/direct-UART/pin/archived-lineage work and for secret or local-identifier output; any occurrence fails the plan.
- Do not rerun or edit Phase 34 verification, requirements, roadmap, or state yet. Plan 34-07 must complete first.

</verification>

<success_criteria>

- HLT-02: the latest supervisor checkpoint category, sequence, and monotonic observation advance continuously while the supervisor runs.
- HLT-04: recurring checkpoints keep an active supervisor healthy, while the existing frozen-checkpoint test still proves stale/unhealthy transitions.
- Focused and repository-wide software verification pass without active watchdog behavior or hardware scope.

</success_criteria>

<output>
After implementation, create `.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-06-SUMMARY.md` using the normal GSD task-commit and plan-summary contract. Do not push, do not mark HLT-02 or HLT-04 complete, and do not rerun Phase 34 verification until Plan 34-07 also has a summary.
</output>
