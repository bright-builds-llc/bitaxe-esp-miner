---
phase: 34-provenance-runtime-health-and-coherent-operator-snapshot
plan: "08"
type: execute
wave: 8
depends_on:
  - "34-07"
autonomous: true
gap_closure: true
phase_lifecycle_id: 34-2026-07-15T03-26-15
lifecycle_mode: interactive
generated_by: gsd-plan-phase
generated_at: "2026-07-16T01:33:45.905Z"
requirements:
  - SYS-02
files_modified:
  - tools/flash/src/main.rs
  - tools/flash/src/package_admission.rs
  - tools/flash/Cargo.toml
  - tools/flash/BUILD.bazel
  - tools/parity/src/phase34_source_guard.rs
must_haves:
  truths:
    - "A non-dry-run package flash can select only the unique structurally admitted factory_merged_image artifact; manifest-listed ELF, extra artifacts, path aliases, and factory-like filenames cannot enter the full-flash effect path."
    - "The subprocess consumes an admission-owned private snapshot of the exact validated bytes, and package-path replacement after admission cannot change the bytes opened by espflash."
    - "The admitted OTA application is a complete supported ESP32-S3 image: chip identity and revision/header policy, segment bounds, XOR checksum, alignment padding, declared SHA-256 trailer, and exact end-of-image consumption all validate."
    - "Every admission failure occurs before port discovery, credential handling, command execution, USB, or hardware effects."
  artifacts:
    - path: tools/flash/src/package_admission.rs
      provides: "Pure complete ESP32-S3 application parser and typed admitted factory bytes"
    - path: tools/flash/src/main.rs
      provides: "Closed factory-artifact selection and admission-owned mode-0600 execution snapshot"
    - path: tools/parity/src/phase34_source_guard.rs
      provides: "Production ordering and no-path-reopen guards for exact-package admission"
  key_links:
    - from: tools/flash/src/main.rs
      to: tools/flash/src/package_admission.rs
      via: "resolve_flash_image returns one typed admitted factory value only after complete OTA/factory validation"
    - from: tools/flash/src/main.rs
      to: espflash child execution
      via: "the admitted value owns the private snapshot and remains alive until environment.execute returns"
---

<objective>
Close SYS-02 by making the exact bytes consumed by the flash subprocess the complete ESP32-S3 factory image proved by package admission.

Purpose: eliminate explicit-artifact bypass, admission-to-execution path replacement, and acceptance of corrupt or foreign ESP application trailers.
Output: a closed admitted artifact type, complete ESP32-S3 image validation, a private execution snapshot with deterministic lifetime and cleanup, and pre-effect adversarial regressions.
</objective>

<execution_context>
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-CONTEXT.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-RESEARCH.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-REVIEW.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-VERIFICATION.md
@.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-05-SUMMARY.md
</execution_context>

<standards>
Repo-local exact-source, no-hardware, credential, redaction, direct-UART/pin, Phase 35, and archived-lineage rules are binding. Bright Builds functional-core/imperative-shell, parse-at-boundaries, typed-invariant, code-shape, verification, testing, and Rust standards materially govern this plan. No active standards override applies.
</standards>

<threat-model level="ASVS-L1" block="high">

| ID | Threat | Severity | Required mitigation and verification |
| --- | --- | --- | --- |
| T-34-G6 | A manifest-listed ELF, extra artifact, alias, or factory-like filename bypasses the structurally admitted factory artifact. | critical | Represent the selected image as a closed admitted factory kind; non-dry-run explicit selection must resolve to the exact unique factory artifact and filename dispatch must not choose an effect. |
| T-34-G7 | Package bytes change after validation but before the child process opens the path. | critical | Materialize the already-admitted bytes once into an owner-only private snapshot, keep its owner alive through child completion, and execute only that snapshot; mutate the source tree between admission and execution in a deterministic regression. |
| T-34-G8 | A foreign-chip, revision-incompatible, checksum-invalid, badly padded, digest-invalid, or trailing-garbage ESP image carries otherwise valid identity markers. | high | Parse and validate the complete supported ESP32-S3 image envelope with checked arithmetic and exact trailer consumption; add one focused mutation regression per field. |
| T-34-G9 | Admission performs port, credential, process, network, USB, or hardware work before failing. | critical | Keep parsing pure over supplied bytes and assert every rejection wins over sentinel external failures. |
| T-34-G10 | Temporary artifacts leak admitted firmware bytes or local paths. | high | Create under an existing gitignored private root or OS temp directory with mode 0600, use redacted diagnostics, and prove owner-drop cleanup on success and failure. |

</threat-model>

<commit-contract>
Each task ends in one atomic task commit and no push. Before every task commit, run exactly in order from the repository root: `cargo fmt --all`; `cargo clippy --all-targets --all-features -- -D warnings`; `cargo build --all-targets --all-features`; `cargo test --all-features`. Stop on the first failure. Then run the task's focused checks and `git diff --check`. Capture each deterministic RED failure before production edits and commit only the GREEN state.
</commit-contract>

<tasks>

<task type="auto" id="34-08-01">
  <name>Validate the complete supported ESP32-S3 application envelope</name>
  <read-first>
    - .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-REVIEW.md
    - .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-VERIFICATION.md
    - tools/flash/src/package_admission.rs
    - tools/flash/src/main.rs
    - scripts/package-firmware.sh
    - firmware/bitaxe/sdkconfig.defaults
  </read-first>
  <files>
    - tools/flash/src/package_admission.rs
    - tools/flash/src/main.rs
    - tools/flash/BUILD.bazel
    - tools/parity/src/phase34_source_guard.rs
  </files>
  <action>
Add focused Arrange/Act/Assert RED tests around the structurally valid fixture. Independently mutate: image chip ID away from ESP32-S3 `9`; supported chip-revision/header-policy fields to an incompatible or reserved value; one segment byte without updating the XOR checksum seeded from `0xEF`; a required alignment/padding byte; the `hash_appended` declaration; one byte of the declared appended SHA-256; missing/truncated digest; and one trailing byte after the exact allowed trailer. Each test must expect a distinct stable redacted `identity_admission=blocked reason=...` category, and the valid fixture must continue to contain the exact application descriptor version, ELF SHA-256, and full source commit.

Extend the pure parser rather than adding marker scans or invoking Python/esptool at admission time. Parse the 24-byte ESP image header with checked offsets, require chip ID `9`, accept only the concrete revision/header flag combinations emitted by the repository's pinned ESP-IDF/espflash package path, and reject reserved or contradictory combinations. XOR every declared segment payload from seed `0xEF`; after the final segment, require zero-valued alignment padding through the checksum position and compare the stored checksum. If `hash_appended` is set, require exactly 32 following bytes equal to SHA-256 of the image through the checksum/alignment boundary. If it is clear, require no digest. In both cases require exact EOF with no unconsumed trailer. Preserve the existing bounded descriptor, source-commit, partition-table, factory-layout, and factory/OTA byte-equality checks. Keep all arithmetic checked and return stable categories without raw paths, bytes, hashes, or identifiers.

Update the package fixture builders and Phase 34 source guard to require ESP32-S3 chip validation, checksum validation, digest validation, and exact trailer consumption. Run the exact commit-contract sequence, then the focused commands, review the diff, and create one atomic commit for task 34-08-01.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-flash package_admission &amp;&amp; cargo test -p bitaxe-parity phase34_package_and_hardware_admission_source_guard &amp;&amp; bazel test //tools/flash:tests //tools/parity:tests &amp;&amp; git diff --check</automated>
  </verify>
  <done>The pure parser rejects every incomplete, corrupt, foreign, or trailing ESP application shape while accepting the canonical ESP32-S3 package fixture.</done>
  <acceptance-criteria>
    - `package_admission.rs` requires ESP32-S3 chip ID `9` and a closed supported revision/header policy before descriptor identity is accepted.
    - Separate tests reject chip ID, revision/header flags, checksum, alignment padding, digest declaration, digest bytes, digest truncation, and trailing garbage with stable categories.
    - The parser consumes the exact image length and uses checked arithmetic for every header, segment, alignment, checksum, and digest range.
    - Existing descriptor version, application ELF SHA-256, source commit, factory partition, and factory/OTA equality regressions remain green.
    - Task 34-08-01 has one atomic commit created only after the exact Rust sequence and focused checks pass.
  </acceptance-criteria>
</task>

<task type="auto" id="34-08-02">
  <name>Carry admitted immutable factory bytes through the flash effect</name>
  <read-first>
    - tools/flash/src/main.rs
    - tools/flash/src/package_admission.rs
    - tools/flash/Cargo.toml
    - tools/flash/BUILD.bazel
    - tools/parity/src/phase34_source_guard.rs
    - .planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-VERIFICATION.md
  </read-first>
  <files>
    - tools/flash/src/main.rs
    - tools/flash/src/package_admission.rs
    - tools/flash/Cargo.toml
    - tools/flash/BUILD.bazel
    - tools/parity/src/phase34_source_guard.rs
  </files>
  <action>
First add RED production-path tests for four bypasses: `--image` selecting the manifest ELF; selecting an extra manifest artifact; selecting a path alias to the factory; and selecting an extra artifact whose basename is `bitaxe-ultra205-factory.bin`. Assert each fails before port/credential/process sentinels. Add a deterministic race test whose fake environment replaces the original factory path after admission but before `execute`; assert the executed path contains the originally admitted factory bytes, is not the package path, has owner-only permissions on Unix, and disappears after success. Add equivalent child-failure cleanup and snapshot-write failure-before-port tests.

Replace the path tuple returned from `resolve_flash_image` with a closed `AdmittedFlashImage` (or equivalently named) value containing the manifest display path/category, the exact validated factory bytes, and no public constructor for arbitrary artifact kinds. For non-dry-run package flows, allow only the unique `factory_merged_image`; an explicit image is accepted only when its canonical manifest-relative resolution equals that exact artifact and must never select ELF or extra kinds. Keep the existing dry-run-without-manifest developer path inert and clearly outside evidence admission.

Materialize `AdmittedFlashImage` into an `AdmittedExecutionSnapshot` immediately before command construction using the already-present `tempfile` dependency. Write the owned byte vector once, flush/sync and set mode 0600 on Unix before effects, retain the `NamedTempFile` owner inside the prepared flash value until `environment.execute` returns, and pass only its private path to `espflash write-bin`. Derive the command from the admitted kind, never from basename. Snapshot construction and permission failures must occur before port discovery, credential reads, NVS generation, or execution. Do not re-read or re-hash the package path after admission; the immutable bytes and their typed provenance are the authority. Ensure drop cleans the snapshot on all normal/error returns and diagnostics expose only bounded categories.

Update the real fake-environment boundary so the race test crosses preparation and child execution rather than calling a pure helper copy. Extend source guards to reject `require_manifest_artifact_for_path` selection, basename-based effect dispatch, or returning a package path from identity admission. Run the exact commit-contract sequence, focused commands, canonical build/package/reference checks, review the diff, and create one atomic commit for task 34-08-02.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-flash identity_admission &amp;&amp; cargo test -p bitaxe-flash admitted_execution &amp;&amp; cargo test -p bitaxe-parity phase34_package_and_hardware_admission_source_guard &amp;&amp; bazel test //tools/flash:tests //tools/parity:tests &amp;&amp; just build &amp;&amp; just package &amp;&amp; just verify-reference &amp;&amp; git diff --check</automated>
  </verify>
  <done>Only the unique admitted factory kind reaches flashing, and the child consumes an owner-held private snapshot of the exact bytes admission validated.</done>
  <acceptance-criteria>
    - Non-dry-run explicit ELF, extra artifact, alias, and factory-like basename selections all fail before ports, credentials, NVS, or child execution.
    - `resolve_flash_image` returns a closed admitted value rather than `(manifest, path)`, and production effect selection does not inspect a filename.
    - The mutation regression replaces the package source after admission yet the fake child observes the original admitted bytes from a distinct mode-0600 private path.
    - Success, child failure, and command-construction failure leave no private snapshot; snapshot creation failure performs zero later effects.
    - No package artifact path is reopened between admission and the child consuming the private snapshot.
    - Task 34-08-02 has one atomic commit created only after the exact Rust sequence and focused checks pass.
  </acceptance-criteria>
</task>

</tasks>

<verification>

- Confirm every RED test fails for the intended current bypass before production changes and becomes green without weakening assertions.
- Run `cargo test -p bitaxe-flash package_admission`, `cargo test -p bitaxe-flash identity_admission`, `cargo test -p bitaxe-flash admitted_execution`, and `cargo test -p bitaxe-parity phase34_package_and_hardware_admission_source_guard`.
- Run `bazel test //tools/flash:tests //tools/parity:tests`, then the exact final Rust sequence, `bazel test //...`, `just build`, `just package`, `just verify-reference`, and `git diff --check`.
- Validate exactly two standalone frontmatter delimiters and lifecycle `34-2026-07-15T03-26-15` in the summary produced during execution.
- Review for package-path reopen, arbitrary artifact dispatch, unchecked arithmetic, incomplete trailer acceptance, secret/path diagnostics, hardware/USB/network/credential work, OTA execution, Phase 35, direct UART/pins, or archived-lineage work; any occurrence fails the plan.
- No shell file is planned. If execution needs to change shell, stop and re-plan ownership before applying it, then include available shellcheck and `shfmt -d`.

</verification>

<success_criteria>

- SYS-02 has production regressions proving the complete admitted ESP32-S3 factory bytes are the immutable bytes consumed by the child.
- Explicit selection, package mutation, foreign-chip/revision, checksum/padding, digest, and trailer attacks fail before external effects.
- Canonical build/package/reference and all focused/repository-wide software checks pass without hardware or credentials.

</success_criteria>

<output>
After implementation, create `.planning/phases/34-provenance-runtime-health-and-coherent-operator-snapshot/34-08-SUMMARY.md` under the normal GSD task-commit contract. Do not push or mark SYS-02 complete. Continue to Plan 34-09, then run fresh review and verification only after both summaries exist.
</output>
