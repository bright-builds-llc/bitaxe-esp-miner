---
phase: 18-firmware-ota-and-rollback-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase18-firmware-ota-evidence.sh
  - scripts/phase18-firmware-ota-evidence-test.sh
  - scripts/BUILD.bazel
  - docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/evidence-contract.md
  - docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/redaction-review.md
autonomous: true
requirements:
  - REL-02
  - REL-08
  - REL-07
  - EVD-05
must_haves:
  truths:
    - "Phase 18 has a repo-owned helper that can run firmware OTA evidence without using Phase 13 evidence paths."
    - "The helper requires an explicit origin-only DEVICE_URL or trusted USB flash-monitor source and records network_scan: disabled."
    - "Invalid firmware rejection remains separate from valid OTA, boot-validation, and rollback claims."
    - "The Phase 18 evidence directory has a contract and pending redaction review before live artifacts are cited."
  artifacts:
    - path: "scripts/phase18-firmware-ota-evidence.sh"
      provides: "Phase 18 OTA evidence wrapper"
      contains: "phase18_firmware_ota_evidence"
    - path: "scripts/phase18-firmware-ota-evidence-test.sh"
      provides: "Wrapper regression tests"
      contains: "phase18_firmware_ota_evidence_test passed"
    - path: "scripts/BUILD.bazel"
      provides: "Bazel wrapper and test targets"
      contains: "phase18_firmware_ota_evidence_test"
    - path: "docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/evidence-contract.md"
      provides: "Phase evidence artifact contract"
      contains: "network_scan: disabled"
    - path: "docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/redaction-review.md"
      provides: "Pending redaction review"
      contains: "redaction_status: pending"
  key_links:
    - from: "scripts/phase18-firmware-ota-evidence.sh"
      to: "scripts/phase13-firmware-ota-smoke.sh"
      via: "thin wrapper invocation with Phase 18 out-dir and target validation"
      pattern: "phase13-firmware-ota-smoke"
    - from: "scripts/phase18-firmware-ota-evidence.sh"
      to: "docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/target-lock.json"
      via: "sanitized target provenance"
      pattern: "target-lock"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 18-2026-07-03T14-06-29
generated_at: 2026-07-03T14:32:04Z
---

<objective>
Create the Phase 18 firmware OTA evidence wrapper and initial evidence contract.

Purpose: Phase 18 is evidence orchestration, not a new OTA implementation. This plan gives executors a phase-owned helper and tests so later plans can capture valid OTA, invalid rejection, boot-validation, and non-claims without mutating historical Phase 13 artifacts.
Output: Phase 18 helper script, helper test, Bazel targets, evidence contract, and pending redaction review.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/verification.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-firmware-ota-and-rollback-evidence/18-CONTEXT.md
@.planning/phases/18-firmware-ota-and-rollback-evidence/18-RESEARCH.md
@.planning/phases/18-firmware-ota-and-rollback-evidence/18-VALIDATION.md
@.planning/phases/13-final-ultra-205-release-evidence/13-04-SUMMARY.md
@.planning/phases/17-live-http-api-and-static-evidence/17-06-SUMMARY.md
@scripts/phase13-firmware-ota-smoke.sh
@scripts/phase13-firmware-ota-smoke-test.sh
@scripts/phase17-live-http-api-smoke.sh
@scripts/BUILD.bazel

<interfaces>
Existing OTA helper contract to wrap:
```bash
scripts/phase13-firmware-ota-smoke.sh \
  --device-url "$DEVICE_URL" \
  --manifest <manifest-json> \
  --ota-image <esp-miner.bin> \
  --port <detector-approved-port> \
  --out-dir docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/firmware-ota \
  --monitor-seconds 45
```

Phase 18 wrapper must expose:
```bash
scripts/phase18-firmware-ota-evidence.sh \
  --manifest docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate/bitaxe-ultra205-package.json \
  --ota-image bazel-bin/firmware/bitaxe/esp-miner.bin \
  --port <detector-approved-port> \
  --out-dir docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence \
  --monitor-seconds 45 \
  [--device-url "$DEVICE_URL" | --device-url-from-flash-evidence target/phase18-firmware-ota-and-rollback-evidence-dev-raw/flash-command-evidence.json]
```

Decision coverage in this plan:
```text
D-04: explicit origin-only target or trusted USB flash evidence; no scanning.
D-05: phase-specific evidence path and helper pattern.
D-06: fixed invalid fixture and invalid rejection is not rollback proof.
D-07: valid OTA requires esp-miner.bin, response, reboot, identity, and boot-validation markers.
D-08: same-image OTA is acceptable only with checksum, response, reboot identity, and boot-validation proof; refusal is recorded as below verified.
D-15: redaction review is required before promotion.
D-16: helper tests are part of final verification.
```
</interfaces>
</context>

<must_haves>
- The helper is a thin shell wrapper around existing repo-owned OTA and target-validation patterns.
- The helper writes only under `docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/` and `target/phase18-firmware-ota-and-rollback-evidence-dev-raw/` for local raw evidence.
- The helper refuses missing, path-bearing, query-bearing, fragment-bearing, userinfo-bearing, or non-HTTP(S) targets.
- The helper can extract exactly one origin-only raw `device_url` from a trusted `flash-command-evidence.json` whose command kind is flash-monitor, board is `205`, and trusted output is true.
- The helper records valid OTA, invalid rejection, boot validation, rollback, and non-claims as separate fields.
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 18 OTA evidence wrapper and tests</name>
  <requirements>REL-02, REL-08, EVD-05</requirements>
  <files>scripts/phase18-firmware-ota-evidence.sh, scripts/phase18-firmware-ota-evidence-test.sh, scripts/BUILD.bazel</files>
  <read_first>
    <file>.planning/phases/18-firmware-ota-and-rollback-evidence/18-CONTEXT.md</file>
    <file>.planning/phases/18-firmware-ota-and-rollback-evidence/18-RESEARCH.md</file>
    <file>.planning/phases/18-firmware-ota-and-rollback-evidence/18-VALIDATION.md</file>
    <file>scripts/phase13-firmware-ota-smoke.sh</file>
    <file>scripts/phase13-firmware-ota-smoke-test.sh</file>
    <file>scripts/phase17-live-http-api-smoke.sh</file>
    <file>scripts/BUILD.bazel</file>
  </read_first>
  <action>Create `scripts/phase18-firmware-ota-evidence.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. Keep it a thin wrapper: parse `--device-url`, `--device-url-from-flash-evidence`, `--manifest`, `--ota-image`, `--port`, `--out-dir`, `--target-lock-out`, `--monitor-seconds`, and `--target-lock-only`. Validate direct target URLs as origin-only `http://` or `https://` with no userinfo, path except `/`, query, or fragment. For `--device-url-from-flash-evidence`, parse JSON with `python3` and require `command_kind` containing `flash-monitor`, `board` equal to `205`, `trusted_output` equal to `true`, and exactly one `device_url=http(s)://...` marker in the referenced monitor log. Never scan networks, call mDNS, inspect ARP/router state, or infer a target from redacted committed artifacts. Write `target-lock.json` with `target_status`, `device_url_source`, `device_url_redacted`, `board`, `selected_port`, `source_commit`, `reference_commit`, `manifest`, `flash_evidence_json`, `network_scan: disabled`, and `created_from_explicit_input: true`. When not in `--target-lock-only`, invoke `scripts/phase13-firmware-ota-smoke.sh` with the resolved raw URL, manifest, OTA image, port, Phase 18 `firmware-ota` out-dir, and monitor seconds. Prefix the Phase 18 log with `phase18_firmware_ota_evidence`; leave the nested helper's `phase13_firmware_ota_smoke` marker as provenance, but do not write Phase 13 evidence paths. Add `scripts/phase18-firmware-ota-evidence-test.sh` using fake curl, fake monitor, fake flash evidence JSON, and fake monitor logs. Tests must cover missing target writes a blocked log without curl, invalid direct URL blocks, trusted flash evidence creates a redacted target lock, untrusted/wrong-board flash evidence blocks, invalid image rejection is recorded as not rollback proof, valid OTA success requires `firmware_commit=`, `reference_commit=`, and `ota_boot_validation=`, and missing boot-validation markers prevent passed status. Update `scripts/BUILD.bazel` with `sh_binary(name = "phase18_firmware_ota_evidence", srcs = ["phase18-firmware-ota-evidence.sh"])` and `sh_test(name = "phase18_firmware_ota_evidence_test", srcs = ["phase18-firmware-ota-evidence-test.sh"], data = ["phase18-firmware-ota-evidence.sh", "phase13-firmware-ota-smoke.sh", "phase13-monitor-capture.sh"], tags = ["local"])`. Do not edit `reference/esp-miner`.</action>
  <verify>
    <automated>bash -n scripts/phase18-firmware-ota-evidence.sh scripts/phase18-firmware-ota-evidence-test.sh</automated>
    <automated>bazel test //scripts:phase18_firmware_ota_evidence_test //scripts:phase13_firmware_ota_smoke_test</automated>
    <automated>rg -n "phase18_firmware_ota_evidence|target-lock-only|device-url-from-flash-evidence|network_scan|invalid image rejection is not rollback proof|ota_boot_validation|phase13-firmware-ota-smoke" scripts/phase18-firmware-ota-evidence.sh scripts/phase18-firmware-ota-evidence-test.sh</automated>
    <automated>rg -n "phase18_firmware_ota_evidence|phase18_firmware_ota_evidence_test" scripts/BUILD.bazel</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`scripts/phase18-firmware-ota-evidence.sh` exists, is executable, and contains `phase18_firmware_ota_evidence`.</criterion>
    <criterion>`scripts/phase18-firmware-ota-evidence-test.sh` exists, is executable, and tests direct target rejection, trusted flash evidence, invalid rejection, valid OTA, and missing boot-validation marker behavior.</criterion>
    <criterion>`scripts/BUILD.bazel` exposes both `phase18_firmware_ota_evidence` and `phase18_firmware_ota_evidence_test`.</criterion>
    <criterion>`bazel test //scripts:phase18_firmware_ota_evidence_test //scripts:phase13_firmware_ota_smoke_test` passes.</criterion>
  </acceptance_criteria>
  <done>Phase 18 has a tested helper wrapper that preserves explicit target, current package, invalid rejection, valid OTA, and boot-validation boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 18 evidence contract and pending redaction review</name>
  <requirements>REL-07, EVD-05</requirements>
  <files>docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/evidence-contract.md, docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/redaction-review.md</files>
  <read_first>
    <file>.planning/phases/18-firmware-ota-and-rollback-evidence/18-CONTEXT.md</file>
    <file>docs/parity/evidence/phase-17-live-http-api-and-static-evidence/redaction-review.md</file>
    <file>docs/parity/evidence/phase-17-live-http-api-and-static-evidence/summary.md</file>
  </read_first>
  <action>Create `docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/evidence-contract.md` without body `---` separators. It must list required artifact classes: `package-release-gate.md`, `package-release-gate/bitaxe-ultra205-package.json`, `serial-boot.md`, `serial-boot/detect-ultra205.log`, `serial-boot/flash-command-evidence.json`, `serial-boot/flash-monitor.log`, `target-lock.json`, `firmware-ota.md`, `firmware-ota/firmware-ota-smoke.log`, invalid OTA headers/body/curl-error artifacts, valid OTA headers/body/curl-error artifacts, `firmware-ota/post-ota-monitor.log`, `summary.md`, and `redaction-review.md`. The contract must record `network_scan: disabled`, board `205`, current source commit, reference commit, manifest path, `esp-miner.bin` checksum, fixed invalid fixture checksum, `DEVICE_URL` provenance, and explicit separate evidence claims for valid OTA, invalid rejection, boot validation, rollback, and non-claims. Create `redaction-review.md` with `redaction_status: pending`, a checklist for `DEVICE_URL`, IP addresses, MAC addresses, SSIDs, Wi-Fi credentials, pool credentials, worker secrets, API tokens, NVS secret values, request/response bodies, serial logs, and recovery logs, plus a search command using the Phase 18 evidence directory. Keep every artifact class initially `pending` or `absent - not cited`; do not mark review passed in this plan.</action>
  <verify>
    <automated>test -f docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/evidence-contract.md && test -f docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/redaction-review.md</automated>
    <automated>rg -n "network_scan: disabled|valid OTA|invalid rejection|boot validation|rollback|non-claim|target-lock.json|firmware-ota-smoke.log|post-ota-monitor.log|redaction_status: pending|DEVICE_URL|Wi-Fi credentials|pool credentials|NVS secret" docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/evidence-contract.md docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/redaction-review.md</automated>
    <automated>python3 -c 'import pathlib,sys; files=[pathlib.Path("docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/evidence-contract.md"), pathlib.Path("docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/redaction-review.md")]; bad=[]; [bad.append(str(p)) for p in files if any(line.strip()=="---" for line in p.read_text().splitlines())]; sys.exit("body frontmatter separator found: "+", ".join(bad) if bad else 0)'</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`evidence-contract.md` lists every required Phase 18 artifact path and separate evidence claim class.</criterion>
    <criterion>`redaction-review.md` starts with `redaction_status: pending`, not passed.</criterion>
    <criterion>No Phase 18 evidence Markdown file contains a standalone body `---` separator.</criterion>
  </acceptance_criteria>
  <done>Phase 18 evidence has a concrete artifact contract and a pending redaction gate before any checklist or release-doc promotion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| operator or flash evidence -> Phase 18 helper | Untrusted target input becomes an OTA destination only after origin-only validation and board/source checks. |
| Phase 13 helper output -> Phase 18 evidence | Historical helper output is reused but must write into Phase 18 paths and preserve Phase 18 claim boundaries. |
| live response artifacts -> committed evidence | HTTP bodies, curl errors, serial logs, and target provenance may contain secrets or private endpoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-18-01-01 | Spoofing | `scripts/phase18-firmware-ota-evidence.sh` target resolution | mitigate | Require explicit origin-only `DEVICE_URL` or trusted board `205` flash-monitor JSON; write `network_scan: disabled`; reject inferred targets. |
| T-18-01-02 | Tampering | OTA image selection | mitigate | Wrapper requires manifest-listed `esp-miner.bin` and delegates checksum validation to the existing helper before upload. |
| T-18-01-03 | Repudiation | invalid rejection vs rollback claims | mitigate | Tests require `invalid image rejection is not rollback proof`; evidence contract separates invalid, valid, boot-validation, rollback, and non-claims. |
| T-18-01-04 | Information Disclosure | Phase 18 evidence artifacts | mitigate | Start redaction review as pending and require the Phase 18 secret scan before any promotion. |
| T-18-01-05 | Denial of Service | valid OTA monitor capture | mitigate | Wrapper keeps bounded monitor seconds and records blocked evidence if post-OTA identity or boot-validation markers are missing. |
</threat_model>

<verification>
Run the task commands plus:

```bash
git diff --check -- scripts/phase18-firmware-ota-evidence.sh scripts/phase18-firmware-ota-evidence-test.sh scripts/BUILD.bazel docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence
```
</verification>

<success_criteria>
- Phase 18 wrapper and test are present, executable, and Bazel-visible.
- Wrapper tests prove missing target, target validation, trusted flash evidence, invalid rejection, valid OTA, and missing boot-validation behavior.
- Evidence contract and pending redaction review exist under `docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/`.
- No reference implementation files are modified.
</success_criteria>

<output>
After completion, create `.planning/phases/18-firmware-ota-and-rollback-evidence/18-01-SUMMARY.md`.
</output>
