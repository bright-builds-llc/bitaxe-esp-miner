---
phase: 18-firmware-ota-and-rollback-evidence
plan: "02"
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate.md
  - docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate/bitaxe-ultra205-package.json
  - docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate/package-command.log
  - docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate/release-gate.log
  - docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/serial-boot.md
  - docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/serial-boot/detect-ultra205.log
  - docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/serial-boot/flash-command-evidence.json
  - docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/serial-boot/flash-monitor.log
  - docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/target-lock.json
autonomous: true
requirements:
  - REL-02
  - REL-08
  - REL-07
  - EVD-05
user_setup:
  - service: ultra205_device_url
    why: "Phase 18 OTA upload requires an explicit origin-only target; helpers must not scan or infer it."
    env_vars:
      - name: DEVICE_URL
        source: "Optional direct origin-only http:// or https:// URL for the just-flashed Ultra 205. If absent, executor may use a trusted local raw flash-monitor evidence file under target/ only."
must_haves:
  truths:
    - "Phase 18 package evidence records the current source commit, reference commit, manifest path, firmware OTA image path, and checksum before any upload."
    - "Hardware work starts with just detect-ultra205 and continues only for exactly one board 205 ESP32-S3 port with board-info success."
    - "Wrapper-owned flash-monitor evidence ties board 205, selected port, source commit, reference commit, route registration, and safe boot to the package manifest."
    - "A sanitized target lock records explicit target provenance with network_scan: disabled or records an exact blocker without an upload."
  artifacts:
    - path: "docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate.md"
      provides: "Package and release-gate ledger"
      contains: "package_status: passed"
    - path: "docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate/bitaxe-ultra205-package.json"
      provides: "Copied package manifest"
      contains: "firmware_ota_image"
    - path: "docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/serial-boot.md"
      provides: "Detector and flash-monitor ledger"
      contains: "flash_monitor_status"
    - path: "docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/target-lock.json"
      provides: "Sanitized target provenance or exact target blocker"
      contains: "network_scan"
  key_links:
    - from: "docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate/bitaxe-ultra205-package.json"
      to: "docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/serial-boot/flash-command-evidence.json"
      via: "matching source/reference commits and board 205 flash-monitor output"
      pattern: "source_commit"
    - from: "docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/serial-boot/flash-command-evidence.json"
      to: "docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/target-lock.json"
      via: "direct DEVICE_URL or trusted local raw flash evidence"
      pattern: "device_url_source"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 18-2026-07-03T14-06-29
generated_at: 2026-07-03T14:32:04Z
---

<objective>
Capture Phase 18 package, detector, flash identity, and sanitized target provenance before any OTA upload.

Purpose: D-01 through D-04 make current package identity and explicit target provenance the hard gates for all live OTA evidence.
Output: Package/release-gate ledger, copied manifest, detector and flash-monitor ledgers, commit-ready serial artifacts, and sanitized `target-lock.json` or exact blocked target evidence.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/verification.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-firmware-ota-and-rollback-evidence/18-CONTEXT.md
@.planning/phases/18-firmware-ota-and-rollback-evidence/18-RESEARCH.md
@.planning/phases/18-firmware-ota-and-rollback-evidence/18-VALIDATION.md
@.planning/phases/18-firmware-ota-and-rollback-evidence/18-01-SUMMARY.md
@scripts/phase18-firmware-ota-evidence.sh
@scripts/detect-ultra205.sh
@scripts/package-firmware.sh
@tools/flash/src/main.rs
@tools/xtask/src/package_manifest.rs
@tools/parity/src/release_gate.rs
@docs/parity/evidence/phase-17-live-http-api-and-static-evidence/summary.md

<interfaces>
Decision coverage in this plan:
```text
D-01: run `just package`, release gate, and record source/reference/image/checksum before upload.
D-03: run `just detect-ultra205`; continue only for exactly one board 205 ESP32-S3 port with board-info success.
D-04: require explicit origin-only DEVICE_URL or trusted local flash evidence; no network scanning or target guessing.
D-05: write Phase 18 evidence under `docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/`.
D-15: commit only redacted target and serial evidence; local raw target evidence remains under target/.
```

Expected package release-gate command:
```bash
bazel run //tools/parity:report -- release-gate \
  --manifest docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate/bitaxe-ultra205-package.json
```

Expected target-lock command:
```bash
scripts/phase18-firmware-ota-evidence.sh \
  --target-lock-only \
  --manifest docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate/bitaxe-ultra205-package.json \
  --out-dir docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence \
  [--device-url "$DEVICE_URL" | --device-url-from-flash-evidence target/phase18-firmware-ota-and-rollback-evidence-dev-raw/flash-command-evidence.json]
```
</interfaces>
</context>

<must_haves>
- The copied manifest is produced by this plan's `just package` run and not reused from Phase 17.
- `firmware_ota_image` in the copied manifest is `esp-miner.bin` and has a SHA-256 checksum.
- Detector failure, multiple ports, wrong board, or board-info failure blocks hardware work and writes exact blocked evidence.
- Commit-ready serial evidence uses `redact-evidence=true`; any raw `device_url` source stays under `target/` and is not committed.
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Refresh current package and release-gate identity</name>
  <requirements>REL-02, REL-07, EVD-05</requirements>
  <files>docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate.md, docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate/bitaxe-ultra205-package.json, docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate/package-command.log, docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate/release-gate.log</files>
  <read_first>
    <file>.planning/phases/18-firmware-ota-and-rollback-evidence/18-CONTEXT.md</file>
    <file>scripts/package-firmware.sh</file>
    <file>tools/xtask/src/package_manifest.rs</file>
    <file>tools/parity/src/release_gate.rs</file>
    <file>docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/evidence-contract.md</file>
  </read_first>
  <action>Run `just package` and capture stdout/stderr to `docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate/package-command.log`. Copy `bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json` to `docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate/bitaxe-ultra205-package.json`. Run `bazel run //tools/parity:report -- release-gate --manifest docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate/bitaxe-ultra205-package.json` and capture output to `package-release-gate/release-gate.log`. Write `package-release-gate.md` with exact fields: `package_status`, `release_gate_status`, `source_commit`, `reference_commit`, `manifest_path`, `firmware_ota_image_path`, `firmware_ota_image_sha256`, `factory_image_path`, `www_image_sha256`, `command_log`, `release_gate_log`, `network_scan: disabled`, and `conclusion`. Treat the git HEAD at task start as the eligible Phase 18 package identity per D-01; if the manifest source commit differs from task-start HEAD, rerun `just package` once and fail with a visible blocker if it still differs. Do not commit large generated binaries; commit only manifest/log/ledger artifacts.</action>
  <verify>
    <automated>test -s docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate/bitaxe-ultra205-package.json</automated>
    <automated>rg -n "release_gate: passed" docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate/release-gate.log</automated>
    <automated>node -e 'const fs=require("fs"); const m=JSON.parse(fs.readFileSync("docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate/bitaxe-ultra205-package.json","utf8")); const ota=(m.artifacts||[]).find(a=>a.kind==="firmware_ota_image"); if (!m.source_commit) throw new Error("missing source_commit"); if (!m.reference_commit) throw new Error("missing reference_commit"); if (!ota) throw new Error("missing firmware_ota_image"); if (ota.path !== "esp-miner.bin") throw new Error("OTA path is not esp-miner.bin"); if (!/^[0-9a-f]{64}$/i.test(String(ota.sha256||""))) throw new Error("bad OTA sha256"); console.log("phase18_package_manifest_check: passed");'</automated>
    <automated>rg -n "package_status: passed|release_gate_status: passed|source_commit|reference_commit|firmware_ota_image_path: esp-miner.bin|firmware_ota_image_sha256|network_scan: disabled" docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate.md</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`package-command.log`, copied manifest, `release-gate.log`, and `package-release-gate.md` exist under the Phase 18 evidence directory.</criterion>
    <criterion>The copied manifest has `source_commit`, `reference_commit`, and `firmware_ota_image` path `esp-miner.bin` with a 64-character SHA-256.</criterion>
    <criterion>`release-gate.log` contains `release_gate: passed`.</criterion>
    <criterion>No generated `.bin` or `.elf` artifact is committed by this task.</criterion>
  </acceptance_criteria>
  <done>Phase 18 has current same-commit package and release-gate evidence ready for detector and OTA gates.</done>
</task>

<task type="auto">
  <name>Task 2: Capture detector, flash-monitor identity, and target lock</name>
  <requirements>REL-02, REL-08, REL-07, EVD-05</requirements>
  <files>docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/serial-boot.md, docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/serial-boot/detect-ultra205.log, docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/serial-boot/flash-command-evidence.json, docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/serial-boot/flash-monitor.log, docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/target-lock.json</files>
  <read_first>
    <file>AGENTS.md</file>
    <file>.planning/phases/18-firmware-ota-and-rollback-evidence/18-CONTEXT.md</file>
    <file>scripts/detect-ultra205.sh</file>
    <file>scripts/phase18-firmware-ota-evidence.sh</file>
    <file>docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate/bitaxe-ultra205-package.json</file>
    <file>docs/parity/evidence/phase-17-live-http-api-and-static-evidence/target-lock.json</file>
  </read_first>
  <action>Run `just detect-ultra205` first and tee output to `docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/serial-boot/detect-ultra205.log`. Continue only if the detector finds exactly one likely ESP32-S3 serial port and board-info succeeds for board `205`; otherwise write `serial-boot.md` with `detector_status: blocked - <exact reason>`, `flash_monitor_status: not run`, `target_lock_status: blocked`, `network_scan: disabled`, and `conclusion`, then stop before flash or OTA. If detector passes, extract the printed `port=<path>` and run `just flash-monitor board=205 port=<port> manifest=bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json wifi-credentials=wifi-credentials.json evidence-dir=docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/serial-boot capture-timeout-seconds=45 redact-evidence=true` only when `wifi-credentials.json` exists; otherwise omit the `wifi-credentials=...` argument. Do not read or print `wifi-credentials.json`. If `DEVICE_URL` is present, run `scripts/phase18-firmware-ota-evidence.sh --target-lock-only --device-url "$DEVICE_URL" --manifest docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate/bitaxe-ultra205-package.json --out-dir docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence --port <port>`. If `DEVICE_URL` is absent, executor may create local non-committed raw evidence with `just flash-monitor board=205 port=<port> manifest=bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json wifi-credentials=wifi-credentials.json evidence-dir=target/phase18-firmware-ota-and-rollback-evidence-dev-raw capture-timeout-seconds=45` and then run the Phase 18 helper with `--target-lock-only --device-url-from-flash-evidence target/phase18-firmware-ota-and-rollback-evidence-dev-raw/flash-command-evidence.json`; omit `wifi-credentials=...` if the file is absent. If neither direct URL nor trusted local raw flash evidence yields exactly one origin-only URL, write `target-lock.json` with `target_status: blocked`, `device_url_source: none`, `network_scan: disabled`, board `205`, selected port, source/reference commits, and exact blocker. Write `serial-boot.md` with detector status, flash-monitor status, target lock status, selected port, package manifest path, source/reference commits, safe startup markers, route registration markers, `network_scan: disabled`, and conclusion. Commit only redacted serial/target artifacts; keep `target/phase18-firmware-ota-and-rollback-evidence-dev-raw/` untracked.</action>
  <verify>
    <automated>test -s docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/serial-boot/detect-ultra205.log</automated>
    <automated>bash -lc 'ledger=docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/serial-boot.md; lock=docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/target-lock.json; test -f "$ledger"; test -f "$lock"; if rg -q "detector_status: blocked" "$ledger"; then rg -n "detector_status: blocked|flash_monitor_status: not run|target_lock_status: blocked|network_scan: disabled" "$ledger" "$lock"; else rg -n "detector_status: passed|flash_monitor_status: passed|board.*205|selected_port|source_commit|reference_commit|network_scan: disabled" "$ledger" "$lock"; fi'</automated>
    <automated>node -e 'const fs=require("fs"); const lock=JSON.parse(fs.readFileSync("docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/target-lock.json","utf8")); if (lock.network_scan !== "disabled") throw new Error("network_scan not disabled"); if (lock.board !== "205") throw new Error("board is not 205"); if (lock.target_status === "passed") { if (lock.created_from_explicit_input !== true) throw new Error("target not explicit"); if (!String(lock.device_url_redacted || "").includes("[redacted]")) throw new Error("raw device url not redacted"); } console.log("phase18_target_lock_check: passed");'</automated>
    <automated>bash -lc 'if test -f docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/serial-boot/flash-command-evidence.json; then node -e '\''const fs=require("fs"); const m=JSON.parse(fs.readFileSync("docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate/bitaxe-ultra205-package.json","utf8")); const f=JSON.parse(fs.readFileSync("docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/serial-boot/flash-command-evidence.json","utf8")); if (f.board !== "205") throw new Error("wrong board"); if (f.trusted_output !== true) throw new Error("untrusted"); if (f.firmware_commit !== m.source_commit) throw new Error("source mismatch"); if (f.reference_commit !== m.reference_commit) throw new Error("reference mismatch"); console.log("phase18_flash_identity_check: passed");'\''; fi'</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`just detect-ultra205` output is recorded before any flash, target, or OTA action.</criterion>
    <criterion>If detector passes, `serial-boot/flash-command-evidence.json` is wrapper-owned, board `205`, trusted output, and source/reference commits match the copied manifest.</criterion>
    <criterion>`target-lock.json` always records `network_scan: disabled`; passed target locks redact the raw URL and blocked locks state the exact missing prerequisite.</criterion>
    <criterion>No raw `DEVICE_URL`, Wi-Fi credential, pool credential, API token, or NVS secret value is committed.</criterion>
  </acceptance_criteria>
  <done>Phase 18 has current board/package/target provenance or a precise blocked target record before OTA upload work begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| USB detector -> hardware actions | Detector output decides whether any flash or OTA action is allowed. |
| package manifest -> flashed firmware | The manifest source/reference commits must match wrapper-owned flash evidence. |
| local raw flash evidence -> committed target lock | Raw `device_url` may exist only under `target/`; committed target lock must be sanitized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-18-02-01 | Spoofing | `just detect-ultra205` gate | mitigate | Block on zero, multiple, wrong-board, or board-info failure before flash or OTA. |
| T-18-02-02 | Tampering | package-to-flash identity | mitigate | Verify copied manifest source/reference commits equal `flash-command-evidence.json` firmware/reference commits. |
| T-18-02-03 | Information Disclosure | target lock and serial evidence | mitigate | Use `redact-evidence=true` for committed serial artifacts and write only redacted target provenance. |
| T-18-02-04 | Denial of Service | flash-monitor preparation | mitigate | Use repo-owned `just flash-monitor` with timeout and safe startup markers; stop on detector failure. |
| T-18-02-05 | Repudiation | evidence provenance | mitigate | Record exact commands, logs, selected port, manifest path, board-info result, source commit, reference commit, and conclusion. |
</threat_model>

<verification>
Run task-level commands plus:

```bash
git diff --check -- docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/package-release-gate.md docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/serial-boot.md docs/parity/evidence/phase-18-firmware-ota-and-rollback-evidence/target-lock.json
```
</verification>

<success_criteria>
- Package and release-gate evidence exists for the Phase 18 source commit.
- Detector output is recorded and controls all downstream hardware work.
- Flash-monitor evidence is wrapper-owned, commit-redacted, board `205`, and manifest-matched when detector passes.
- `target-lock.json` is sanitized and explicit, or blocked with an exact missing prerequisite.
</success_criteria>

<output>
After completion, create `.planning/phases/18-firmware-ota-and-rollback-evidence/18-02-SUMMARY.md`.
</output>
