---
phase: 02-ultra-205-config-and-nvs-model
plan: "02"
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - crates/bitaxe-config/BUILD.bazel
  - crates/bitaxe-config/src/lib.rs
  - crates/bitaxe-config/src/nvs.rs
  - crates/bitaxe-config/fixtures/nvs-schema.json
  - crates/bitaxe-config/fixtures/nvs-migrations.json
autonomous: true
requirements: ["CFG-03", "CFG-06"]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 2-2026-06-26T15-47-58
generated_at: 2026-06-26T16:10:11.762Z
must_haves:
  truths:
    - "The config crate exposes exact upstream NVS namespace, key names, stored types, REST names, defaults, ranges, and indexed-key behavior."
    - "Legacy NVS migrations produce typed write/erase decisions without touching ESP-IDF NVS."
    - "Missing keys and corrupt float strings load as configured defaults in the pure model."
  artifacts:
    - path: "crates/bitaxe-config/src/nvs.rs"
      provides: "Typed NVS schema, key validation, migration rules, and pure load defaults."
      exports: ["NVS_NAMESPACE", "NvsKeyName", "StoredType", "SettingSchema", "MigrationRule", "NvsWrite", "all_settings_schema"]
    - path: "crates/bitaxe-config/fixtures/nvs-schema.json"
      provides: "Reference-derived schema fixture for CFG-03/CFG-06."
    - path: "crates/bitaxe-config/fixtures/nvs-migrations.json"
      provides: "Reference-derived legacy migration fixture cases."
  key_links:
    - from: "crates/bitaxe-config/src/nvs.rs"
      to: "reference/esp-miner/main/nvs_config.c"
      via: "schema and migration breadcrumbs"
      pattern: "reference/esp-miner/main/nvs_config.c"
    - from: "crates/bitaxe-config/src/nvs.rs"
      to: "crates/bitaxe-config/fixtures/nvs-schema.json"
      via: "fixture/golden tests"
      pattern: "include_str!.*nvs-schema.json|fixtures/nvs-schema.json"
---

<objective>
Implement the pure NVS schema and migration model for upstream-compatible settings load behavior.

Purpose: CFG-03 depends on preserving active keys, legacy keys, storage encodings, missing-key defaults, and float-string behavior before settings validation and persistence can use the schema.
Output: `nvs.rs` with typed schema rows, key validation, migration decisions, and golden tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-ultra-205-config-and-nvs-model/02-CONTEXT.md
@.planning/phases/02-ultra-205-config-and-nvs-model/02-RESEARCH.md
@.planning/phases/02-ultra-205-config-and-nvs-model/02-VALIDATION.md
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@reference/esp-miner/main/nvs_config.h
@reference/esp-miner/main/nvs_config.c
@crates/bitaxe-config/src/lib.rs
@crates/bitaxe-config/fixtures/nvs-schema.json
@crates/bitaxe-config/fixtures/nvs-migrations.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement typed NVS schema rows and key constraints</name>
  <read_first>
    crates/bitaxe-config/src/lib.rs
    crates/bitaxe-config/src/nvs.rs
    crates/bitaxe-config/BUILD.bazel
    crates/bitaxe-config/fixtures/nvs-schema.json
    reference/esp-miner/main/nvs_config.h
    reference/esp-miner/main/nvs_config.c
    .planning/phases/02-ultra-205-config-and-nvs-model/02-VALIDATION.md
  </read_first>
  <files>crates/bitaxe-config/src/lib.rs, crates/bitaxe-config/src/nvs.rs, crates/bitaxe-config/BUILD.bazel, crates/bitaxe-config/fixtures/nvs-schema.json</files>
  <behavior>
    - Test `nvs_schema_uses_upstream_namespace_main`: `NVS_NAMESPACE` is exactly `main`.
    - Test `nvs_schema_preserves_active_and_legacy_key_names`: schema contains `asicfrequency_f`, legacy alias `asicfrequency`, `manualfanspeed`, legacy alias `fanspeed`, and upstream typo `usefbstartum`.
    - Test `nvs_schema_rejects_keys_longer_than_15_bytes`: `NvsKeyName::parse("1234567890123456")` returns an error, while `fbsv2authpubk`, `emc_ideality_f`, `emc_beta_comp`, `power_cons_tgt`, `selftest_temp`, `selftest_warm`, and `selftest_max` parse successfully.
    - Test `nvs_schema_maps_upstream_storage_types`: `TYPE_BOOL` maps to `StoredType::BoolAsU16` and `TYPE_FLOAT` maps to `StoredType::FloatString`.
  </behavior>
  <action>
    Add `pub mod nvs;` and re-export `NVS_NAMESPACE`, `NVS_KEY_NAME_MAX_BYTES`, `NvsKeyName`, `RestFieldName`, `StoredType`, `SettingDefault`, `SettingSchema`, `NvsSchemaError`, and `all_settings_schema` from `src/lib.rs`. Add `src/nvs.rs` to `crates/bitaxe-config/BUILD.bazel` `srcs`.

    In `src/nvs.rs`, define `pub const NVS_NAMESPACE: &str = "main";` and `pub const NVS_KEY_NAME_MAX_BYTES: usize = 15;` per D-05 and D-07. Implement `NvsKeyName::parse` to reject empty strings, non-ASCII names, and names longer than 15 bytes; do not silently truncate. Implement `RestFieldName` as a separate type so REST/API names never replace NVS keys per D-04.

    Define `StoredType` variants `Str`, `U16`, `I32`, `U64`, `FloatString`, and `BoolAsU16`, mapping upstream `TYPE_STR`, `TYPE_U16`, `TYPE_I32`, `TYPE_U64`, `TYPE_FLOAT`, and `TYPE_BOOL` from `nvs_config.h`. Define `SettingSchema` with fields for `key: NvsKeyName`, `stored_type: StoredType`, `default_value: Option<SettingDefault>`, `rest_name: Option<RestFieldName>`, `min: Option<i32>`, `max: Option<i32>`, `array_size: Option<usize>`, and `provenance: &'static str`.

    Implement `all_settings_schema()` from `reference/esp-miner/main/nvs_config.c` settings rows. Critical rows must include these exact values:
    - `hostname`: `StoredType::Str`, REST `hostname`, min `1`, max `32`.
    - `stratumurl`: `StoredType::Str`, REST `stratumURL`, min `0`, max `3999`.
    - `stratumport`: `StoredType::U16`, REST `stratumPort`, min `0`, max `65535`.
    - `stratumtls`: `StoredType::U16`, REST `stratumTLS`, min `0`, max `3`.
    - `stratumxnsub`: `StoredType::BoolAsU16`, REST `stratumExtranonceSubscribe`, min `0`, max `1`.
    - `fbsv2authpubk`: `StoredType::Str`, REST `fallbackStratumV2AuthorityPubkey`, min `0`, max `52`.
    - `asicfrequency_f`: `StoredType::FloatString`, REST `frequency`, min `1`, max `65535`.
    - `asicvoltage`: `StoredType::U16`, REST `coreVoltage`, min `1`, max `65535`.
    - `manualfanspeed`: `StoredType::U16`, REST `manualFanSpeed`, min `0`, max `100`.
    - `minfanspeed`: `StoredType::U16`, REST `minFanSpeed`, min `0`, max `99`.
    - `temptarget`: `StoredType::U16`, REST `temptarget`, min `35`, max `66`.
    - `overheat_mode`: `StoredType::BoolAsU16`, REST `overheat_mode`, min `0`, max `0`.
    - `scoreboard`: `StoredType::Str`, array size `20`.
    - `boardversion`: `StoredType::Str`, default `"000"`.
    - `devicemodel`: `StoredType::Str`, default `"unknown"`.
    - `asicmodel`: `StoredType::Str`, default `"unknown"`.
    - `selftest_temp`: `StoredType::U16`, default `65`; `selftest_warm`: default `55`; `selftest_max`: default `70`.

    Add module-level breadcrumbs for `reference/esp-miner/main/nvs_config.h` and `reference/esp-miner/main/nvs_config.c` per D-16. Keep this pure; do not add ESP-IDF `nvs_open`, `nvs_get_*`, `nvs_set_*`, or firmware adapter calls.
  </action>
  <verify>
    <automated>bazel test //crates/bitaxe-config:tests --test_filter=nvs_schema</automated>
  </verify>
  <acceptance_criteria>
    `crates/bitaxe-config/src/nvs.rs` contains `NVS_NAMESPACE: &str = "main"`, `NVS_KEY_NAME_MAX_BYTES: usize = 15`, `FloatString`, `BoolAsU16`, `RestFieldName`, and `all_settings_schema`.
    `rg -n 'asicfrequency_f|manualfanspeed|usefbstartum|fbsv2authpubk|emc_ideality_f|power_cons_tgt|selftest_max' crates/bitaxe-config/src/nvs.rs` finds matches.
    `rg -n 'max.*3999|min.*35|max.*66|array_size.*20|default.*unknown' crates/bitaxe-config/src/nvs.rs` finds matches.
    `crates/bitaxe-config/src/lib.rs` re-exports `NvsKeyName`, `StoredType`, and `all_settings_schema`.
    The focused Bazel command passes.
  </acceptance_criteria>
  <done>NVS schema rows are typed, exact-key guarded, key-length tested, and ready for migration/default-load behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure migration and default-load decisions</name>
  <read_first>
    crates/bitaxe-config/src/nvs.rs
    crates/bitaxe-config/fixtures/nvs-migrations.json
    reference/esp-miner/main/nvs_config.c
    reference/esp-miner/main/global_state.h
    reference/esp-miner/components/stratum_v2/include/sv2_protocol.h
    .planning/phases/02-ultra-205-config-and-nvs-model/02-CONTEXT.md
  </read_first>
  <files>crates/bitaxe-config/src/nvs.rs, crates/bitaxe-config/fixtures/nvs-migrations.json</files>
  <behavior>
    - Test `nvs_schema_migrates_legacy_asicfrequency_to_float_string`: legacy `asicfrequency=485` yields write `asicfrequency_f="485"`.
    - Test `nvs_schema_migrates_legacy_fanspeed_to_manualfanspeed`: legacy `fanspeed=42` yields write `manualfanspeed=42`.
    - Test `nvs_schema_migrates_stratum_protocol_u16_to_string`: `0` yields `SV1`, `1` yields `SV2` for `stratumprot` and `fbstratumprot`.
    - Test `nvs_schema_migrates_sv2_channel_type_u16_to_string`: `0` yields `extended`, `1` yields `standard`, including legacy `fbSv2ChanType`.
    - Test `nvs_schema_corrupt_float_uses_default`: stored `asicfrequency_f="bad"` loads as default `485`.
  </behavior>
  <action>
    Extend `src/nvs.rs` with pure data structures `StoredValue`, `LoadedValue`, `NvsWrite`, `NvsErase`, `MigrationRule`, and `MigrationDecision`. The model must return decisions an ESP-IDF adapter can apply, but it must not call ESP-IDF or write to hardware.

    Implement migration rules from `nvs_config_init_fallback` and `nvs_config_apply_fallback`:
    - Active key `asicfrequency_f` has legacy key `asicfrequency`. If active is missing and legacy u16 exists, return `NvsWrite::String { key: "asicfrequency_f", value: "485" }` for legacy `485`. When writing active frequency `485`, also return compatibility write `NvsWrite::U16 { key: "asicfrequency", value: 485 }`.
    - Active key `manualfanspeed` has legacy key `fanspeed`. If active is missing and legacy u16 exists, return `NvsWrite::U16 { key: "manualfanspeed", value: legacy }`. When writing active manual fan speed, also return `NvsWrite::U16 { key: "fanspeed", value }`.
    - `stratumprot` and `fbstratumprot` migrate old u16 storage to string: u16 `1` -> `SV2`, every other u16 -> `SV1`, with an erase decision for the old key followed by a string write to the same key.
    - `sv2chantype` and `fbsv2chantype` migrate old u16 storage to string: u16 `1` -> `standard`, every other u16 -> `extended`, with an erase decision for the old key followed by a string write to the same key.
    - Legacy mixed-case key `fbSv2ChanType` migrates to active key `fbsv2chantype`, with erase `fbSv2ChanType` and write `fbsv2chantype`.

    Implement pure load helpers so missing keys use `SettingSchema.default_value` and missing string defaults fall back to `""`. Implement `FloatString` parsing with `str::parse::<f32>()`: a valid stored string such as `"485"` loads to float `485.0`; corrupt strings such as `"bad"` load to the schema default for `asicfrequency_f`, which is `485.0` from Ultra 205 defaults. This directly mitigates T-02-04.

    Update `nvs-migrations.json` to include expected operation arrays for each case above, including exact strings `SV1`, `SV2`, `standard`, `extended`, `asicfrequency_f`, `asicfrequency`, `manualfanspeed`, `fanspeed`, `fbSv2ChanType`, and `fbsv2chantype`.
  </action>
  <verify>
    <automated>bazel test //crates/bitaxe-config:tests --test_filter=nvs_schema</automated>
  </verify>
  <acceptance_criteria>
    `crates/bitaxe-config/src/nvs.rs` contains `MigrationRule`, `MigrationDecision`, `NvsWrite`, `NvsErase`, `asicfrequency_f`, `asicfrequency`, `manualfanspeed`, `fanspeed`, `fbSv2ChanType`, `SV1`, `SV2`, `standard`, and `extended`.
    `crates/bitaxe-config/src/nvs.rs` contains a corrupt-float fallback branch that returns a default instead of an error or panic.
    `rg -n 'fbSv2ChanType|asicfrequency_f|manualfanspeed|standard|extended' crates/bitaxe-config/fixtures/nvs-migrations.json` finds matches.
    `rg -n 'nvs_open|nvs_get_|nvs_set_|esp_idf' crates/bitaxe-config/src/nvs.rs` returns no matches.
    The focused Bazel command passes.
  </acceptance_criteria>
  <done>NVS migrations, missing defaults, bool-as-u16 semantics, and corrupt float fallback are modeled as pure typed decisions with golden coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored NVS bytes -> typed settings | Potentially corrupt or legacy NVS values enter the pure model. |
| Upstream C schema -> Rust schema | Key names and storage types can drift during manual porting. |
| Pure migration decisions -> future firmware adapter | Decisions must be explicit so an adapter cannot invent behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / Elevation of Privilege | `NvsWrite` decisions for frequency, voltage, and fan keys | mitigate | Represent writes as inert data only; tests assert no ESP-IDF or hardware adapter calls exist in `nvs.rs`. |
| T-02-02 | Information Disclosure | NVS fixtures | mitigate | Continue using provenance-marked public defaults from Plan 01; this plan adds no Wi-Fi secrets or private operator credentials. |
| T-02-03 | Tampering / Denial of Service | NVS key schema and migrations | mitigate | Preserve exact active keys `asicfrequency_f` and `manualfanspeed`, legacy keys `asicfrequency` and `fanspeed`, namespace `main`, and all key-length tests. |
| T-02-04 | Denial of Service | Float string load path | mitigate | Corrupt `FloatString` values return the configured default; test `nvs_schema_corrupt_float_uses_default` proves `asicfrequency_f="bad"` loads as `485.0`. |
| T-02-05 | Spoofing / Safety governance failure | Non-205 board data | accept | This plan does not alter catalog scope; Plan 01 owns `VerificationScope` tests and Plan 04 rechecks parity notes. |
</threat_model>

<verification>
Run after both tasks:

```bash
bazel test //crates/bitaxe-config:tests
just parity
git status --short reference/esp-miner
```
</verification>

<success_criteria>
- CFG-03: NVS namespace, schema rows, defaults, missing-key behavior, legacy migration rules, bool storage, and float-string behavior are executable in pure Rust.
- CFG-06: NVS schema and migration fixtures are machine-readable and covered by focused tests.
- No firmware NVS adapter or hardware-effect code is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ultra-205-config-and-nvs-model/02-02-SUMMARY.md`.
</output>
