---
phase: 22-claim-ladder-and-safety-preconditions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/parity/BUILD.bazel
  - tools/parity/src/main.rs
  - tools/parity/src/claim_ladder.rs
  - docs/parity/evidence/phase-22-claim-ladder-and-safety-preconditions/claim-ladder.md
autonomous: true
requirements:
  - EVD-06
user_setup: []
must_haves:
  truths:
    - "Operator can distinguish version 1.0 controlled no-share evidence from version 1.1 prerequisite readiness, live socket/runtime evidence, live ASIC-derived share outcomes, and deferred non-claims."
    - "Phase 21 approved controlled no-share soak is labeled as controlled no-share closure and never as accepted-share or rejected-share proof."
    - "Parity tooling has a testable claim-ladder source of truth with stable tier identifiers and overclaim guardrails."
  artifacts:
    - path: "tools/parity/src/claim_ladder.rs"
      provides: "Claim ladder tier definitions, document validator, and tests"
      exports:
        - "claim_ladder_tiers"
        - "validate_claim_ladder_document"
    - path: "docs/parity/evidence/phase-22-claim-ladder-and-safety-preconditions/claim-ladder.md"
      provides: "Operator-visible claim ladder"
      contains: "version_1_1_live_asic_share_outcome"
  key_links:
    - from: "tools/parity/src/main.rs"
      to: "tools/parity/src/claim_ladder.rs"
      via: "module declaration"
      pattern: "mod claim_ladder;"
    - from: "tools/parity/src/claim_ladder.rs"
      to: "docs/parity/evidence/phase-22-claim-ladder-and-safety-preconditions/claim-ladder.md"
      via: "include_str! test fixture"
      pattern: "include_str!.*claim-ladder.md"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 22-2026-07-04T20-10-36
generated_at: 2026-07-04T20:16:00Z
---

<objective>
Create the operator-visible claim ladder and a parity-tooling guard that prevents Phase 21 controlled no-share evidence from being promoted into accepted/rejected production-share proof.

Purpose: This satisfies EVD-06 before any later production-mining work can cite or promote version 1.1 claims.
Output: A committed claim-ladder document plus Rust parity tests that enforce required tier names and overclaim boundaries.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-claim-ladder-and-safety-preconditions/22-CONTEXT.md
@.planning/phases/22-claim-ladder-and-safety-preconditions/22-RESEARCH.md
@.planning/phases/22-claim-ladder-and-safety-preconditions/22-VALIDATION.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/rust.md
@tools/parity/BUILD.bazel
@tools/parity/src/main.rs
@tools/parity/src/mining_allow.rs
@tools/parity/src/safety_allow.rs
@.planning/phases/21-live-mining-and-soak-evidence/21-VERIFICATION.md
@docs/parity/evidence/phase-21-live-mining-and-soak-evidence/summary.md

<interfaces>
From `tools/parity/src/mining_allow.rs`:
```rust
const ALLOWED_CLAIM_TIERS: &[&str] = &[
    "diagnostic-chip-detect",
    "diagnostic-work-result",
    "controlled-no-share",
    "live-pool-smoke",
    "bounded-soak",
    "unsupported-pending",
    "parity-redaction",
];
```

From `tools/parity/BUILD.bazel`:
```python
rust_binary(
    name = "report",
    srcs = [
        "src/api_compare.rs",
        "src/main.rs",
        "src/mining_allow.rs",
        "src/release_evidence.rs",
        "src/release_gate.rs",
        "src/safety_allow.rs",
    ],
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write operator claim ladder</name>
  <files>docs/parity/evidence/phase-22-claim-ladder-and-safety-preconditions/claim-ladder.md</files>
  <read_first>
    - .planning/phases/22-claim-ladder-and-safety-preconditions/22-CONTEXT.md
    - .planning/phases/22-claim-ladder-and-safety-preconditions/22-RESEARCH.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/21-live-mining-and-soak-evidence/21-VERIFICATION.md
    - docs/parity/evidence/phase-21-live-mining-and-soak-evidence/summary.md
    - AGENTS.md
  </read_first>
  <action>Create `docs/parity/evidence/phase-22-claim-ladder-and-safety-preconditions/claim-ladder.md` with sections named `Allowed Claims`, `Blocked Claims`, `Explicit Non-Claims`, and `Promotion Rules`. Per D-01 through D-04, define exactly these stable tier ids: `version_1_0_controlled_no_share`, `version_1_1_prerequisite_readiness`, `version_1_1_live_socket_runtime`, `version_1_1_live_asic_share_outcome`, and `explicit_deferred_non_claim`. State that Phase 21 `approved_controlled_no_share_soak` supports controlled no-share closure only and does not prove accepted shares, rejected shares, unbounded production mining, full active safety closure, non-205 board support, Stratum v2, OTA/recovery trust, runtime display/input parity, or BAP behavior. Do not add an end-to-end evidence root, live socket procedure, BM1366 production dispatch procedure, or accepted/rejected share claim because those are deferred to later phases.</action>
  <verify>
    <automated>rg "version_1_0_controlled_no_share|version_1_1_prerequisite_readiness|version_1_1_live_socket_runtime|version_1_1_live_asic_share_outcome|explicit_deferred_non_claim|approved_controlled_no_share_soak" docs/parity/evidence/phase-22-claim-ladder-and-safety-preconditions/claim-ladder.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg "Allowed Claims|Blocked Claims|Explicit Non-Claims|Promotion Rules" docs/parity/evidence/phase-22-claim-ladder-and-safety-preconditions/claim-ladder.md` finds all four section names.
    - `rg "accepted shares|rejected shares|unbounded production mining|full active safety closure|non-205 board support|Stratum v2|OTA/recovery trust|runtime display/input parity|BAP behavior" docs/parity/evidence/phase-22-claim-ladder-and-safety-preconditions/claim-ladder.md` finds the blocked/non-claim list.
    - The document contains no standalone body `---` separator after frontmatter; use headings only.
  </acceptance_criteria>
  <done>The operator-facing ladder fully implements D-01, D-02, D-03, and D-04 for EVD-06 without introducing later-phase live production mining claims.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add parity claim ladder guard</name>
  <files>tools/parity/BUILD.bazel, tools/parity/src/main.rs, tools/parity/src/claim_ladder.rs</files>
  <read_first>
    - tools/parity/BUILD.bazel
    - tools/parity/src/main.rs
    - tools/parity/src/mining_allow.rs
    - tools/parity/src/safety_allow.rs
    - docs/parity/evidence/phase-22-claim-ladder-and-safety-preconditions/claim-ladder.md
    - .planning/phases/22-claim-ladder-and-safety-preconditions/22-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `claim_ladder_tiers()` returns all five required tier ids from Task 1.
    - Test 2: `validate_claim_ladder_document(include_str!(...))` returns an empty error list for the Phase 22 ladder document.
    - Test 3: A document that places `approved_controlled_no_share_soak` beside `accepted share` or `rejected share` returns an error mentioning controlled no-share overclaim.
    - Test 4: A document missing `explicit_deferred_non_claim` returns an error mentioning the missing tier id.
  </behavior>
  <action>Create `tools/parity/src/claim_ladder.rs` with `pub(crate) struct ClaimTier { pub(crate) id: &'static str, pub(crate) allowed_claim: &'static str, pub(crate) blocked_claims: &'static [&'static str], pub(crate) evidence_required: &'static str }`, `pub(crate) fn claim_ladder_tiers() -> &'static [ClaimTier]`, and `pub(crate) fn validate_claim_ladder_document(markdown: &str) -> Vec<String>`. The validator must require all five tier ids, require the phrases `allowed claim`, `blocked claim`, and `explicit non-claim`, and reject controlled-no-share overclaim text when `approved_controlled_no_share_soak` appears within the same paragraph as `accepted share`, `accepted shares`, `rejected share`, or `rejected shares`. Add `mod claim_ladder;` to `tools/parity/src/main.rs` and add `"src/claim_ladder.rs"` to the `rust_binary(srcs = [...])` list in `tools/parity/BUILD.bazel`. Keep this as a test-enforced helper; do not add a new CLI subcommand unless needed for tests.</action>
  <verify>
    <automated>bazel test //tools/parity:tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg "mod claim_ladder;" tools/parity/src/main.rs` finds the module declaration.
    - `rg "\"src/claim_ladder.rs\"" tools/parity/BUILD.bazel` finds the Bazel source entry.
    - `rg "version_1_1_live_asic_share_outcome|controlled no-share overclaim|validate_claim_ladder_document" tools/parity/src/claim_ladder.rs` finds the tier, error, and validator.
    - `bazel test //tools/parity:tests` passes.
  </acceptance_criteria>
  <done>Parity tooling has an executable claim-ladder guard for EVD-06 and blocks Phase 21 controlled no-share overclaims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Evidence docs to parity guard | Markdown claim text crosses into Rust validation and may overstate evidence. |
| Prior Phase 21 evidence to version 1.1 claims | Controlled no-share artifacts may be misread as accepted/rejected production-share proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-22-01 | Tampering / Repudiation | `docs/parity/evidence/phase-22-claim-ladder-and-safety-preconditions/claim-ladder.md` | mitigate | Define exact allowed, blocked, and explicit non-claim language per D-01 through D-04, then test required tier ids with `tools/parity/src/claim_ladder.rs`. |
| T-22-02 | Tampering / Repudiation | `tools/parity/src/claim_ladder.rs` | mitigate | Reject any document paragraph that combines `approved_controlled_no_share_soak` with accepted/rejected share terms. |
| T-22-03 | Information Disclosure | Claim ladder evidence doc | mitigate | Keep only category labels and prior committed evidence paths; do not include pool URLs, workers, device URLs, IPs, MACs, Wi-Fi values, NVS secrets, raw Stratum payloads, raw share payloads, or raw BM1366 frames. |
</threat_model>

<verification>
Run `bazel test //tools/parity:tests` after the parity guard changes. Run `just parity` before the phase closes because the claim ladder affects evidence governance.
</verification>

<success_criteria>
- EVD-06 is represented by a claim ladder with all five required tiers.
- Phase 21 controlled no-share closure remains useful evidence but cannot be cited as accepted/rejected production-share proof.
- Claim ladder validation is covered by focused Rust tests.
</success_criteria>

<output>
After completion, create `.planning/phases/22-claim-ladder-and-safety-preconditions/22-01-SUMMARY.md`.
</output>
