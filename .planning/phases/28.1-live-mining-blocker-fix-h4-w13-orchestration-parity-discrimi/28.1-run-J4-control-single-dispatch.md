---
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 28.1-2026-07-07T00-34-17
generated_at: 2026-07-07T03:12:00Z
---

# Phase 28.1 Run J4 — Control Run (`single_dispatch_bounded_read`)

Date: 2026-07-07 (UTC)
Board: 205 (Bitaxe Ultra 205, BM1366)
Port: `/dev/cu.usbmodem1101` (detector-gated; same session gate as J3 — `just detect-ultra205` PASS, exactly one likely port, `espflash board-info --chip esp32s3 --non-interactive` OK, MAC redacted)
Source commit: `85a9ccaf38343334194ac750ab5b946bef54272d`
Reference commit: `c1915b0a63bfabebdb95a515cedfee05146c1d50`
Package: control (`bash scripts/phase27-live-hardware-bridge-package.sh --investigation single_dispatch_bounded_read`)
Package manifest: `bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json` (`source_commit` matches HEAD `85a9cca...`); flashed image identity confirmed on-device (`observed_firmware_commit=85a9ccaf3834`) and in the ELF (baked commit string `85a9ccaf3834` present)
pool_config: local-owner-supplied (category label only)
wifi_config: local-owner-supplied (category label only)

## Commands

1. `bash scripts/phase27-live-hardware-bridge-package.sh --investigation single_dispatch_bounded_read` — build green (passes `--action_env=BITAXE_WORK_RESULT_INVESTIGATION=single_dispatch_bounded_read`)
2. Foreground capture, same J2c-style procedure as J3:
   `just flash-monitor board=205 port=/dev/cu.usbmodem1101 image=bazel-bin/firmware/bitaxe/bitaxe-ultra205-factory.bin evidence-dir=<local-gitignored-dir> capture-timeout-seconds=360 wifi-credentials=wifi-credentials.json`
   with the local pool-input-bridge watcher applying `scripts/phase21-pool-input-bridge.sh --pool-credentials pool-credentials.json` once exactly one origin-only device URL candidate appeared in the fresh monitor log (applied once).
3. Capture ran the full 360 s (`capture_status=timed_out_after_trusted_output`, `trusted_output=true`); agent wall clock >= 480 s.

## Expected control-mode observations (each dispositioned)

All timestamps are device milliseconds from the capture log; quoted lines are verbatim.

| Expectation | Disposition | Evidence |
| --- | --- | --- |
| Exactly ONE `asic_production_status=work_dispatched` per work-queued event, no ~2 s cadence | **MET** | Exactly one dispatch in the whole capture: `I (17653) ... asic_production_status=work_dispatched` (first pool work after `active` at 17583). No further dispatches — the session fail-closed before any new pool work arrived. |
| ZERO `asic_bridge=job_redispatched` markers | **MET** | `grep -c "asic_bridge=job_redispatched"` over the control capture = **0** occurrences. (J3 parity-default capture had 20.) |
| No continuous-listener cadence | **MET** | Zero `h4_continuous_result=listener_armed`, zero `h4_continuous_result=timeout_continue`, zero `asic_bridge=timeout_streak` lines (Plan 04 wiring: `maybe_arm_continuous_listener` early-returns and `BridgeStep::Regenerate` is suppressed when `continuous == false`). J3 had 188 `timeout_streak` markers under identical hardware. |
| Fail-closed blocker reachable if the chip stays silent (`production_result_timeout`) | **MET (fail-closed reached via the malformed-read branch; timeout branch not reachable because the chip answered)** | The chip did NOT stay silent: the Plan 03 post-dispatch probe (`I (17683) ... asic_probe=register_read_tx stage=post_dispatch`) drew the fixed chip-identity response (`I (17813) ... asic_uart_trace=rx_chunk hex=aa 55 13 66 00 00 00 00 00 00 05`) into the bounded read, which fail-closed immediately: `W (17833) ... asic_production_status=fail_closed reason=production_result_malformed mining=disabled work_submission=disabled` followed by `I (17843) ... phase25_safe_stop_status=complete ...` and `phase25_live_stratum_status=stopped` (17863). This is the pre-28.1 fail-closed bounded-read pump: one dispatch, bounded read, first non-nonce read fail-closes the session. `production_result_timeout` itself did not appear because its 10 s deadline was preempted at ~150 ms by the received-but-malformed probe response — the same fail-closed blocker family (`asic_production_status=fail_closed reason=<blocker>`), reachable ONLY in this control build (zero fail-closed lines in J3 under identical silence). |

Boot-path parity with J3: `I (10043) ... asic_work_result_trace=post_init_retention bootstrap=initialized_no_mining` (W13 retention is unconditional and independent of the control lever) and `I (17623) ... asic_probe=power_baseline mw=810` before the dispatch (no `power_delta` — the session fail-closed before the 7 s sample).

## Regression-guard verdict

**REACHABLE.** The `--investigation single_dispatch_bounded_read` compile-time lever restores the pre-28.1 single-dispatch bounded-read fail-closed pump on real hardware: exactly one dispatch per queued pool work, no listener arming, no regeneration, and a fail-closed `asic_production_status=fail_closed` blocker terminating the session on the first bounded read that is not a valid nonce — observed end-to-end in this capture (dispatch 17653 -> fail-closed 17833 -> safe stop complete 17843). The parity-default behavior (J3) and the rollback behavior (J4) are both reachable from the same source tree, satisfying the regression guard.

Note for future control runs: because the Plan 03 post-dispatch register-read probe also fires in control mode, a chip that answers register reads will drive the control build into `production_result_malformed` fail-closed (~150 ms) instead of `production_result_timeout` (10 s). Only a fully silent chip can reach the timeout blocker in this build. This interaction is diagnostic-consistent — it independently re-confirms the J3 chip-alive finding on the retained production UART.

## Conclusion

Old behavior reachable; regression guard satisfied. The control run also independently corroborates the J3 discrimination: the BM1366 answers register reads post-dispatch (fixed chip-identity frame, no pool-derived material) yet produced no nonce in either orchestration mode.

Non-claims: no accepted/rejected share claim; no checklist promotion; no hashrate claim. No raw pool/Wi-Fi/network values, MACs, device URLs, or NVS secrets are committed; the local capture directory is gitignored session state.

***

*Phase: 28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi*
*Plan: 28.1-05 Task 2*
