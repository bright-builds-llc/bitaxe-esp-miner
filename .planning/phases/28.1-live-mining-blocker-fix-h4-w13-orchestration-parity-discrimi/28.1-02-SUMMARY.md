---
phase: 28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi
plan: 02
subsystem: firmware-bridge-orchestration
tags: [rust, esp32s3, bm1366, bridge-pump, w13, dispatch-priority, upstream-parity]

# Dependency graph
requires:
  - phase: 28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi
    provides: "Plan 01: pure BridgeOrchestrator, regenerate_work() with extranonce2 counter, bm1366_job_interval_ms(1)==2000"
  - phase: 27-live-hardware-asic-and-stratum-bridge
    provides: Phase27ProductionBridge mode, production peripheral retention, live bridge pump
provides:
  - W13 default boot semantics — chip detect + mining-ready init alone retain production peripherals and publish InitializedNoMining in bridge mode (no diagnostic nonce gate)
  - require_diagnostic_nonce / require_uart_proof_for_production preserved as compile-time opt-out rollback levers restoring the old boot gate
  - Bridge pump fully delegating step priority to BridgeOrchestrator (dispatch > regenerate > poll <= 100 ms > idle) with continuous listener + 2 s regeneration cadence as unconditional defaults
  - Timeout = continue-with-telemetry (timeout_streak marker); fail-closed ResultTimeout reachable only via the non-continuous bounded-read rollback path
  - Keep-mining-after-first-share pump (first-share NormalStop exit removed)
affects: [28.1-03 hardware probes, 28.1-04 flag retirement, 28.1-05 hardware evidence]

# Tech tracking
tech-stack:
  added: []
  patterns: [pure decision core + firmware interpreter for the bridge pump, thin AsicBridgeState wrapper embedding BridgeOrchestrator]

key-files:
  created: []
  modified:
    - firmware/bitaxe/src/asic_adapter.rs
    - firmware/bitaxe/src/live_stratum_runtime.rs

key-decisions:
  - "AsicBridgeState kept as thin wrapper (research recommendation) embedding BridgeOrchestrator; only shell-facing state (last_dispatch_generation, bounded-read fields) stays in firmware"
  - "Bounded-read rollback branch kept code-complete behind continuous == false for the Plan 04 single_dispatch_bounded_read control mode"
  - "Nothing-dispatchable dispatch outcomes call orchestrator.invalidate_session() so the pump idles until fresh pool work arrives instead of spinning"
  - "Correlation logic extracted into correlate_job_nonce shared by continuous poll and bounded-read paths (no logic duplication)"

patterns-established:
  - "Bridge pump interprets typed BridgeStep values only; zero orchestration-priority logic remains in the 2000-line firmware runtime file"

requirements-completed: []
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 28.1-2026-07-07T00-34-17
generated_at: 2026-07-07T02:16:30Z

# Metrics
duration: 40min
completed: 2026-07-07
---

# Phase 28.1 Plan 02: Upstream-Parity Bridge Defaults Summary

**W13 boot-gate removal (unconditional peripheral retention after init) plus bridge pump rewired to delegate every step decision to the host-tested BridgeOrchestrator — dispatch beats polling, regeneration every 2 s with fresh extranonce2, timeout is telemetry not failure, and mining survives the first classified share**

## Performance

- **Duration:** ~40 min (across an interrupted and a continuation executor session)
- **Completed:** 2026-07-07T02:16:30Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- **Task 1 (W13):** `run_work_result_uart_bootstrap_after_reset` now takes the early-retain path by DEFAULT in `Phase27ProductionBridge` mode — condition is `!require_diagnostic_nonce() && !require_uart_proof_for_production()`, no longer referencing `skip_boot_diagnostic_work()` or `phase27_initialized_no_mining_bootstrap(..)`. The exact marker string `asic_work_result_trace=skip_boot_diagnostic_work bootstrap=initialized_no_mining`, `publish_work_result_bootstrap_initialized_status()`, and `store_production_peripherals(uart, reset, true)` order is preserved, breadcrumbed to `reference/esp-miner/main/power/asic_init.c:58-61`. The diagnostic-work dispatch + blocking read + `fail_closed_work_result_read` path is fully intact behind the opt-out levers.
- **Task 2 (pump rewiring):** `AsicBridgeState` is a thin wrapper embedding `BridgeOrchestrator::new(bm1366_job_interval_ms(1))`. `run_asic_bridge_step` matches on `orchestrator.next_step(Instant::now())`: `Dispatch` → existing dispatch path + `note_dispatched`; `Regenerate` → `runtime.regenerate_work()` + `job_redispatched_marker(counter)` via `info_retained` then dispatch; `Poll { slice_ms }` → `min(slice_ms, SOCKET_TIMEOUT_MS)` bounded read + `note_result_received` on data; `Idle` → NoOp.
- Continuous listener + regeneration cadence are unconditional bridge defaults — zero reads of `continuous_result_task_enabled()` / `job_redispatch_pump_enabled()` / `JOB_REDISPATCH_INTERVAL_MS` remain in the runtime file.
- Poll timeout keeps `h4_continuous_result=timeout_continue` and adds retained `asic_bridge=timeout_streak count=<n>`; the fail-closed `ProductionAsicBlocker::ResultTimeout` branch is reachable only from `read_bounded_asic_result`, which requires `continuous == false` (Plan 04 `single_dispatch_bounded_read` control mode).
- The first-share NormalStop exit is deleted; safe-stop request, watchdog, socket errors, and cleanup exits are byte-identical. Session invalidation (`LiveRuntimeEvent::WorkInvalidated` → new `SocketLineOutcome::SessionInvalidated`) forwards to `orchestrator.invalidate_session()`.
- Marker emission sites for `work_dispatched`, `rx_chunk`, `h4_continuous_result=*`, `result_correlated`, `share_submission_status=*` unchanged (J-run comparability preserved).

## Task Commits

1. **Task 1: W13 unconditional post-init retention** - `cd09f88` (feat) — committed by the initial executor session
2. **Task 2: pump delegation to BridgeOrchestrator** - `dbf6321` (feat) — completed and committed by the continuation session

## Files Created/Modified

- `firmware/bitaxe/src/asic_adapter.rs` - bridge-mode default early-retain branch with asic_init.c:58-61 breadcrumb; rollback levers intact
- `firmware/bitaxe/src/asic_adapter/work_result_investigation.rs` - helper fns untouched in behavior (retirement is Plan 04); bridge default no longer depends on `skip_boot_diagnostic_work`/`initialized_no_mining_gate`
- `firmware/bitaxe/src/live_stratum_runtime.rs` - AsicBridgeState thin wrapper, run_asic_bridge_step delegation, dispatch_production_work / poll_continuous_asic_result / correlate_job_nonce / read_bounded_asic_result split, NormalStop-after-share exit removed, SessionInvalidated wiring

## Verification Evidence

- `cargo fmt --all` — no changes; `cargo clippy --all-targets --all-features -- -D warnings` — green
- `cargo build --all-targets --all-features` — green; `cargo test --all-features` — 0 failures; `just test` — 36/36 pass
- `just build` — xtensa firmware target builds (Bazel content-keyed action cache verified against the exact modified sources; `src/**/*.rs` glob confirmed as target inputs)
- Acceptance greps all pass: `bridge_orchestration` (4 sites), `bm1366_job_interval_ms` (constructor), both marker helpers, ZERO matches for `JOB_REDISPATCH_INTERVAL_MS` / `continuous_result_task_enabled` / `job_redispatch_pump_enabled` in the runtime file, no `maybe_submit_classified` NormalStop exit, `h4_continuous_result=timeout_continue` preserved, `ResultTimeout` only at the bounded-read site (line 930), `asic_init.c:58` breadcrumb + `bootstrap=initialized_no_mining` + `fail_closed_work_result_read` + `store_production_peripherals` all present in `asic_adapter.rs`
- `git status` clean of safety_adapter changes throughout (T-28.1-06 gate untouched)

## Decisions Made

- Kept `AsicBridgeState` as a thin wrapper (CONTEXT discretion point) rather than deleting it — preserves `Default` impl, executor handles, and the bounded-read rollback state without leaking shell types into the pure crate
- `dispatch_production_work` calls `orchestrator.invalidate_session()` when the plan yields nothing dispatchable, so a drained registry idles the cadence instead of re-triggering empty dispatches
- Extracted `correlate_job_nonce` so the continuous poll path and the bounded-read rollback path share one correlation/submit-queue implementation
- Regeneration no-op (`regenerate_work()` Err — no held notify) is treated as `Idle`, exactly per plan; regeneration never fabricates work

## Deviations from Plan

### Auto-fixed Issues

None — the plan's Task 2 spec was executed as written. (Execution was split across two executor sessions after an interruption; the continuation session verified the in-flight diff against the plan spec, found it complete, and ran all gates before committing. No code direction changed.)

### Out-of-scope discovery (not fixed)

Pre-existing firmware `#[cfg(test)]` bit-rot (`assert!(consumed)` on a `SocketLineOutcome` enum) predates this plan and never compiles under any gate; logged to `deferred-items.md` per scope-boundary rule. Plan item 7 (update ScriptedSocket tests asserting the old NormalStop-after-share exit) required no changes — no test references the removed exit or removed `AsicBridgeState` fields.

**Total deviations:** 0 auto-fixed, 1 out-of-scope discovery deferred
**Impact on plan:** None.

## Issues Encountered

- Initial executor session was interrupted mid-Task-2 leaving uncommitted work; continuation resumed by verifying Task 1's commit (`cd09f88`) and completing verification of the existing diff rather than rewriting it.

## Known Stubs

None — the bounded-read branch is intentionally dormant behind `continuous == false` (default `true`), which is planned Plan 28.1-04 surface, not a stub: it is fully implemented, reachable code kept for the `single_dispatch_bounded_read` A/B control mode.

## Threat Flags

None — no new network endpoints, auth paths, file access, or schema changes beyond the plan's threat model. New telemetry uses only the integer-only, redaction-tested marker helpers from the pure crate (T-28.1-08); `ResultTimeout` removal is compensated by `timeout_streak` telemetry (T-28.1-09); safety gates untouched (T-28.1-06); poll slices bounded to <= 100 ms (T-28.1-07); retention path stays compile-time gated on `Phase27ProductionBridge` (T-28.1-05, fail-closed `just build` verified).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 28.1-03 can hook the register-read and INA260 probes into the delegated pump structure (`poll_continuous_asic_result` already classifies `RegisterReadProof`)
- Plan 28.1-04 flips `continuous` via `single_dispatch_bounded_read` and retires the four dead investigation flags plus `JOB_REDISPATCH_INTERVAL_MS`
- Plan 28.1-05 hardware runs should observe multiple `work_dispatched` markers ~2 s apart, `asic_bridge=job_redispatched extranonce2_counter=<n>`, and `asic_bridge=timeout_streak count=<n>`; run docs must note `production_result_timeout` disappears from default runs by design

***
*Phase: 28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi*
*Completed: 2026-07-07*

## Self-Check: PASSED

All 3 claimed modified files exist on disk and both claimed commits (cd09f88, dbf6321) exist in git history.
