---
generated_by: gsd-code-review-fix
phase: "28.1"
fixed_at: 2026-07-06T00:00:00Z
review_path: .planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-REVIEW.md
iteration: 1
findings_in_scope: 2
fixed: 2
skipped: 0
status: all_fixed
---

# Phase 28.1: Code Review Fix Report

**Fixed at:** 2026-07-06
**Source review:** `.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-REVIEW.md`
**Iteration:** 1

**Summary:**

- Findings in scope: 2 (warnings only; the 4 info findings were intentionally left unfixed per fix scope)
- Fixed: 2
- Skipped: 0

## Fixed Issues

### WR-01: Safe function transmutes caller-chosen lifetime to `'static` (latent unsoundness)

**Files modified:** `firmware/bitaxe/src/safety_adapter/power_probe.rs`
**Commit:** 262ca67
**Applied fix:** Tightened `store_power_probe_bus` to take `BitaxeI2cBus<'static>` and deleted the `unsafe { std::mem::transmute(...) }` block and its `SAFETY` comment. The single call site in `phase27_bring_up.rs` needed no change — `run_phase27_hardware_bring_up` already bounds `I2C`/`SDA`/`SCL` with `'static`, so lifetime inference unifies the bus to `'static` without an annotation. The invariant the comment documented is now enforced by the type system; a future caller passing a stack-borrowing bus gets a compile error instead of a use-after-free.

### WR-02: `Regenerate` error arm can starve result polling until the next pool notify

**Files modified:** `firmware/bitaxe/src/live_stratum_runtime.rs`, `crates/bitaxe-stratum/src/v1/bridge_orchestration.rs`
**Commit:** 4db1d23
**Applied fix:** In `run_asic_bridge_step`, the `BridgeStep::Regenerate` → `runtime.regenerate_work()` `Err(_)` arm now calls `bridge.orchestrator.invalidate_session()` before returning `NoOp`, mirroring the "nothing dispatchable" arms in `dispatch_production_work`. This drops the cadence timer so `next_step` returns `Idle` instead of spinning `Regenerate` forever; fresh pool work re-arms `Dispatch` and `Poll` resumes after redispatch. Added the pure-crate regression test `regeneration_unavailable_must_not_permanently_shadow_poll` in `bridge_orchestration.rs` (Arrange/Act/Assert), encoding: elapsed cadence yields `Regenerate`; after `invalidate_session` the orchestrator idles (never `Regenerate` from a stale timer); `note_work_queued` yields `Dispatch`; after redispatch `Poll` resumes.

## Skipped Issues

None — both in-scope warnings were fixed. Info findings IN-01 through IN-04 were out of fix scope (`critical_warning`) and remain documented in the review.

## Verification

- `cargo fmt --all` — clean before each commit
- `cargo clippy --all-targets --all-features -- -D warnings` — clean
- `cargo build --all-targets --all-features` — success
- `cargo test --all-features` — all host suites green, including the new `regeneration_unavailable_must_not_permanently_shadow_poll` test (bitaxe-stratum now 115 tests)
- `just build` — xtensa firmware (`cargo build -p bitaxe-firmware --release --target xtensa-esp32s3-espidf` via Bazel wrapper) compiled successfully with both fixes, confirming the `'static` signature change needs no call-site annotation
- `just test` — 36 Bazel test targets pass, `//crates/bitaxe-stratum:tests` reran and passed

***

_Fixed: 2026-07-06_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
