---
phase: 28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi
plan: 03
type: execute
wave: 3
depends_on:
  - 28.1-02
files_modified:
  - firmware/bitaxe/src/asic_adapter.rs
  - firmware/bitaxe/src/live_stratum_runtime.rs
  - firmware/bitaxe/src/safety_adapter.rs
  - firmware/bitaxe/src/safety_adapter/power_probe.rs
  - firmware/bitaxe/src/safety_adapter/phase27_bring_up.rs
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Firmware sends a reg-0x00 (chip id) read after full init + baud switch AND again after first work dispatch, logging a redaction-safe TX marker each time."
    - "A register-read response is classified through the existing RegisterReadProof path (asic_production_trace=register_read_parsed) — chip-alive vs chip-silent becomes observable."
    - "INA260 power is sampled immediately before first dispatch and ~7 s after, and a numeric-only delta marker is logged — hashing vs not-hashing becomes observable."
    - "The I2C bus survives bring-up (OnceLock retention mirroring production.rs) so mid-bridge power sampling is possible."
  artifacts:
    - path: "firmware/bitaxe/src/safety_adapter/power_probe.rs"
      provides: "OnceLock-retained I2C bus + sample_power_mw() helper"
      contains: "OnceLock"
    - path: "firmware/bitaxe/src/live_stratum_runtime.rs"
      provides: "power-delta marker emission around first dispatch; post-dispatch register-read probe TX"
      contains: "asic_probe=power_delta"
    - path: "firmware/bitaxe/src/asic_adapter.rs"
      provides: "post-init register-read probe TX + bounded response check"
      contains: "asic_probe=register_read_tx stage=post_init"
  key_links:
    - from: "firmware/bitaxe/src/safety_adapter/phase27_bring_up.rs"
      to: "firmware/bitaxe/src/safety_adapter/power_probe.rs"
      via: "store_power_probe_bus(bus) instead of dropping the bus at end of bring-up"
      pattern: "store_power_probe_bus"
    - from: "firmware/bitaxe/src/asic_adapter.rs"
      to: "crates/bitaxe-asic/src/bm1366/command.rs"
      via: "Bm1366Command::ReadChipId frame TX"
      pattern: "ReadChipId"
    - from: "firmware/bitaxe/src/safety_adapter/power_probe.rs"
      to: "firmware/bitaxe/src/safety_adapter/ina260.rs"
      via: "ina260::read_sample(bus)"
      pattern: "read_sample"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1-2026-07-07T00-34-17
generated_at: 2026-07-07T00:53:22.000Z
---

<objective>
Add the two discriminating hardware probes (Workstream B): a post-init/post-dispatch BM1366 register-read probe and an INA260 power-delta marker with the I2C bus retention it requires.

Purpose: If nonces still do not appear after the Plan 02 orchestration fixes, these probes split the remaining search space: register-read response → chip alive + RX path fine at 1 MBaud → problem is nonce production; register-read silent → chip wedged by init or 1 M baud domain broken. Power delta large (~10 W class) → cores hashing; flat → cores not hashing regardless of UART.

Output: Probe TX hooks and markers compiled into the phase27 bridge firmware; a new retained-bus module; xtensa compile proof. Probe OUTCOMES are hardware evidence gathered in Plan 05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-CONTEXT.md
@.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-RESEARCH.md
@.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-02-SUMMARY.md

<interfaces>
Existing building blocks (verified in RESEARCH — probe work is TX-side + sampling only):
```rust
// crates/bitaxe-asic/src/bm1366/command.rs:58-61 — reg 0x00 chip-id read frame, CRC5 byte-matched
Bm1366Command::ReadChipId  // -> command_frame(COMMAND_HEADER_TYPE | GROUP_ALL | CMD_READ, read_register_payload(0x00))

// crates/bitaxe-asic/src/bm1366/result.rs:83,97-101 — 11-byte frame with job-response bit clear parses to
Bm1366ParsedResult::RegisterRead(Bm1366RegisterRead)
// firmware/bitaxe/src/asic_adapter/production.rs:175-178 — maps to ProductionReadOutcome::RegisterReadProof,
// logs "asic_production_trace=register_read_parsed"; bridge maps to AsicBridgeStepOutcome::UartProof

// firmware/bitaxe/src/safety_adapter/ina260.rs:20-37
pub fn read_sample(bus: &mut BitaxeI2cBus<'_>) -> Result<Ina260Sample>  // .power_mw (reg 0x03 * 10.0)

// firmware/bitaxe/src/asic_adapter/production.rs:28-70 — sanctioned OnceLock<Mutex<..>> +
// transmute-to-'static retention pattern with SAFETY note (ESP-IDF singletons live for process lifetime)

// firmware/bitaxe/src/asic_adapter/uart.rs — write_frame(:77), wait_tx_done(:64),
// try_read_exact(len, timeout_ms) -> Result<Option<Vec<u8>>> (:103)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register-read probe TX at post-init and post-dispatch</name>
  <files>firmware/bitaxe/src/asic_adapter.rs, firmware/bitaxe/src/live_stratum_runtime.rs</files>
  <read_first>
    - firmware/bitaxe/src/asic_adapter.rs (W13 retention path from Plan 02 — probe TX goes right after `store_production_peripherals`; existing uart helper usage in the bootstrap function)
    - firmware/bitaxe/src/asic_adapter/uart.rs (write_frame, wait_tx_done, try_read_exact signatures; `BM1366_RESULT_FRAME_LEN`)
    - firmware/bitaxe/src/asic_adapter/production.rs lines 140-185 (result read/classification path incl. `register_read_parsed` log; how the executor locks PRODUCTION_HANDLE)
    - crates/bitaxe-asic/src/bm1366/command.rs lines 50-70 (`ReadChipId` frame construction) and crates/bitaxe-asic/src/bm1366/result.rs lines 80-105 (RegisterRead parsing)
    - reference/esp-miner/components/asic/bm1366.c lines 385-400 (READ-ONLY: `BM1366_read_registers` — reg 0x00 chip-id read; breadcrumb target)
  </read_first>
  <action>
    Two probe TX sites (per locked decision "after full init + baud switch, and again after work dispatch"). Placement discretion resolved: firmware-side hooks (less code; RX classification already exists end-to-end).

    1. **Post-init probe** in `firmware/bitaxe/src/asic_adapter.rs`, on the W13 default retention path, immediately AFTER `store_production_peripherals(uart, reset, true)` — but using the still-local uart handle just before/around retention (read the Plan 02 code to pick the exact point where the uart is accessible):
       - write the `Bm1366Command::ReadChipId` frame (`write_frame` + `wait_tx_done`), log `info_retained` marker exactly: `asic_probe=register_read_tx stage=post_init`
       - bounded response check: `try_read_exact(BM1366_RESULT_FRAME_LEN, 500)`; on `Some(frame)` feed it through the existing BM1366 result parser — a `RegisterRead` parse logs the existing `asic_production_trace=register_read_parsed`; on `None` log exactly: `asic_probe=register_read_silent stage=post_init`
       - probe failure (UART error) must NOT fail the bootstrap — log the silent marker and continue (probe is diagnostic, never a gate)
       - breadcrumb: `// Reference: reference/esp-miner/components/asic/bm1366.c:389-399 (BM1366_read_registers, reg 0x00 = chip id)`
    2. **Post-dispatch probe** in `firmware/bitaxe/src/live_stratum_runtime.rs`: after the FIRST successful production dispatch of a socket session (guard with a `bool` on the bridge wrapper, reset on session invalidation), send one `ReadChipId` frame via the production executor's uart access path and log exactly: `asic_probe=register_read_tx stage=post_dispatch`. Do NOT block for the response — the continuous poll path already classifies a `RegisterRead` frame to `register_read_parsed`/`UartProof`; absence of that marker in the capture IS the silent outcome (recorded in Plan 05 run docs). Keep the added pump code minimal (delegate any new multi-line logic to a small helper fn, not inline in the 2002-line file body).
    3. Markers contain ONLY the fixed strings above — no register values beyond what existing `register_read_parsed` logging already emits, no frame hex at info level.
  </action>
  <verify>
    <automated>just build && cargo clippy --all-targets --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "asic_probe=register_read_tx stage=post_init" firmware/bitaxe/src/asic_adapter.rs` matches
    - `grep -n "asic_probe=register_read_silent stage=post_init" firmware/bitaxe/src/asic_adapter.rs` matches
    - `grep -n "asic_probe=register_read_tx stage=post_dispatch" firmware/bitaxe/src/live_stratum_runtime.rs` matches
    - `grep -rn "ReadChipId" firmware/bitaxe/src/` returns >= 2 matches (both probe sites)
    - `grep -n "bm1366.c:389" firmware/bitaxe/src/asic_adapter.rs` breadcrumb matches
    - Post-init probe code path contains no `return Err`/fail-closed on probe silence (read the branch: silence logs marker and continues)
    - `just build` succeeds
  </acceptance_criteria>
  <done>Both probe TX sites compiled in with exact markers; response classification rides the existing RegisterReadProof path; probe silence never blocks boot or mining.</done>
</task>

<task type="auto">
  <name>Task 2: INA260 power-delta probe with OnceLock I2C bus retention</name>
  <files>firmware/bitaxe/src/safety_adapter/power_probe.rs, firmware/bitaxe/src/safety_adapter.rs, firmware/bitaxe/src/safety_adapter/phase27_bring_up.rs, firmware/bitaxe/src/live_stratum_runtime.rs</files>
  <read_first>
    - firmware/bitaxe/src/safety_adapter/phase27_bring_up.rs (whole file; bus construction at ~95, bus dropped at return ~161, `Phase27SafetySnapshot` OnceLock at ~53 — the pattern already in this file)
    - firmware/bitaxe/src/asic_adapter/production.rs lines 28-70 (the sanctioned OnceLock<Mutex<State>> + transmute-to-'static retention pattern WITH its SAFETY comment wording — mirror it)
    - firmware/bitaxe/src/safety_adapter/ina260.rs (read_sample signature, Ina260Sample fields, `//! Reference:` breadcrumb style at line 3)
    - firmware/bitaxe/src/safety_adapter.rs (mod list — register the new module)
  </read_first>
  <action>
    1. Create `firmware/bitaxe/src/safety_adapter/power_probe.rs`:
       ```rust
       //! Mid-bridge INA260 power sampling for the Phase 28.1 hashing-discrimination probe.
       //! Reference: reference/esp-miner/main/power/INA260.c (sampling semantics via ina260.rs).
       ```
       - `static POWER_PROBE_BUS: OnceLock<Mutex<PowerProbeState>>` holding the `BitaxeI2cBus<'static>` — transmute the bus lifetime to `'static` exactly like `production.rs:28-70`, with a SAFETY comment stating the ESP-IDF I2C singleton lives for the process lifetime and the bus is stored once at bring-up and never reconstructed
       - `pub fn store_power_probe_bus(bus: BitaxeI2cBus<'_>) -> bool` (false if already stored; never panics)
       - `pub fn sample_power_mw() -> Option<u32>` — locks the state, calls `ina260::read_sample(bus)`, returns `Some(sample.power_mw as u32)`; any error or missing bus → `None` (probe is diagnostic, never a gate; no `unwrap()`)
    2. Register `pub mod power_probe;` in `firmware/bitaxe/src/safety_adapter.rs`.
    3. In `phase27_bring_up.rs`: at the end of `run_phase27_hardware_bring_up`, instead of letting the bus drop, call `power_probe::store_power_probe_bus(bus)` and log `info_retained`: `phase27_safety_bring_up=power_probe_bus_retained`. All existing bring-up ordering, snapshot publication, and safety checks stay byte-identical before that point.
    4. In `firmware/bitaxe/src/live_stratum_runtime.rs` (small helper fn, minimal pump diff):
       - immediately BEFORE the first production dispatch of a session: `maybe_baseline_mw = power_probe::sample_power_mw()` (store on the bridge wrapper; no marker yet, or log `asic_probe=power_baseline mw=<n>` — executor choice, numeric only)
       - on the first pump iteration where `now >= first_dispatch_at + 7s` (7 s constant, inside the locked 5-10 s window): take the second sample and log `info_retained` exactly once per session: `asic_probe=power_delta baseline_mw=<b> after_mw=<a> delta_mw=<d>` where `d = a - b` as a signed value; if either sample is `None`, log `asic_probe=power_delta unavailable=true` once instead
       - reset the once-guard + baseline on session invalidation
    5. Markers are numeric-only power values (mW) plus fixed keys — no endpoint/credential/frame material. Delta magnitude is diagnostic data, NOT a pass/fail gate (Assumption A2).
  </action>
  <verify>
    <automated>just build && cargo fmt --all --check && cargo clippy --all-targets --all-features -- -D warnings && cargo test --all-features && just test</automated>
  </verify>
  <acceptance_criteria>
    - `firmware/bitaxe/src/safety_adapter/power_probe.rs` exists; `grep -n "OnceLock" firmware/bitaxe/src/safety_adapter/power_probe.rs` matches; `grep -n "SAFETY" firmware/bitaxe/src/safety_adapter/power_probe.rs` matches
    - `grep -n "pub mod power_probe" firmware/bitaxe/src/safety_adapter.rs` matches
    - `grep -n "store_power_probe_bus" firmware/bitaxe/src/safety_adapter/phase27_bring_up.rs` matches
    - `grep -n "asic_probe=power_delta baseline_mw=" firmware/bitaxe/src/live_stratum_runtime.rs` matches
    - `grep -n "unwrap()" firmware/bitaxe/src/safety_adapter/power_probe.rs` returns zero matches
    - `git diff firmware/bitaxe/src/safety_adapter/phase27_bring_up.rs` shows additions only at/after the end-of-function bus handling (bring-up ordering and safety checks unchanged)
    - `just build` succeeds; full host gates green
  </acceptance_criteria>
  <done>I2C bus survives bring-up via the sanctioned OnceLock pattern; power delta around first dispatch is logged as a numeric-only marker exactly once per session; probe failure degrades to an `unavailable` marker, never a blocker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware logs → committed evidence | probe markers land in retained logs promoted into redacted evidence |
| probe TX → shared production UART | probe frames share the UART/mutex with production dispatch |
| retained I2C bus → power rail reads | new 'static retention widens peripheral lifetime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28.1-10 | I (Info disclosure) | probe markers | mitigate | Markers are fixed strings + integer mW values only; no frames, endpoints, or credential material; existing `register_read_parsed` logging reused unmodified |
| T-28.1-11 | D (DoS) | post-init bounded read (500 ms) + UART mutex | mitigate | Post-init probe runs before the bridge loop starts (no contention); post-dispatch probe is TX-only; probe errors log-and-continue, never fail-closed |
| T-28.1-12 | E (Elevation) | retained 'static I2C bus | mitigate | Mirrors the sanctioned production.rs OnceLock pattern with SAFETY note; store-once API returns false on double-store; sampling is read-only (INA260 measurement registers) — no voltage/fan control surface added |
| T-28.1-13 | E (Elevation) | default build | accept | Probe code lives behind the same Phase27ProductionBridge mode paths as Plan 02; default `just build` remains fail-closed (verified in gates) |
</threat_model>

<verification>
Pre-commit gates plus `just build` xtensa compile proof. Probe OUTCOMES (V10, V11, V12 in 28.1-VALIDATION.md) are hardware evidence collected in Plan 05 — this plan only proves the instrumentation compiles and is marker-exact.
</verification>

<success_criteria>
- Both probe TX sites and the power-delta marker compile into the phase27 bridge firmware with exact marker strings grep-able in source
- I2C bus retention exists with SAFETY documentation and no new control surface
- No probe can block boot, dispatch, or safe stop
</success_criteria>

<output>
After completion, create `.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-03-SUMMARY.md`
</output>
