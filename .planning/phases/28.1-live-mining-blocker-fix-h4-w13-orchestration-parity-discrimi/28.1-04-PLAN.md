---
phase: 28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi
plan: 04
type: execute
wave: 4
depends_on:
  - 28.1-02
  - 28.1-03
files_modified:
  - firmware/bitaxe/src/asic_adapter/work_result_investigation.rs
  - firmware/bitaxe/src/asic_adapter.rs
  - firmware/bitaxe/src/live_stratum_runtime.rs
  - .planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-flag-disposition-note.md
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Investigation modes continuous_result_task, job_redispatch_pump, initialized_no_mining_gate, and skip_boot_diagnostic_work no longer exist anywhere in firmware or crates source (they became defaults in Plans 02/03)."
    - "A new single_dispatch_bounded_read opt-out mode restores the old control behavior: one dispatch, bounded read, fail-closed ResultTimeout on silence."
    - "require_diagnostic_nonce, require_uart_proof_for_production, and clear_rx_before_production_work remain available."
    - "The default build stays fail-closed: no compile envs → no mining (unchanged by flag retirement)."
    - "This plan is one independently revertible flag-disposition change set, landed after all behavior changes."
  artifacts:
    - path: "firmware/bitaxe/src/asic_adapter/work_result_investigation.rs"
      provides: "retired flags removed; single_dispatch_bounded_read_enabled() added; JOB_REDISPATCH_INTERVAL_MS deleted"
      contains: "single_dispatch_bounded_read"
    - path: ".planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-flag-disposition-note.md"
      provides: "run-doc note: retired modes are defaults; old --investigation invocations are no-ops; production_result_timeout disappears from default runs by design"
      contains: "production_result_timeout"
  key_links:
    - from: "firmware/bitaxe/src/live_stratum_runtime.rs"
      to: "firmware/bitaxe/src/asic_adapter/work_result_investigation.rs"
      via: "new_for_phase27 sets continuous = !single_dispatch_bounded_read_enabled()"
      pattern: "single_dispatch_bounded_read_enabled"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1-2026-07-07T00-34-17
generated_at: 2026-07-07T00:53:22.000Z
---

<objective>
Land the locked flag disposition as the final, independently revertible code commit: retire the four investigation modes that Plans 02/03 turned into defaults, add the `single_dispatch_bounded_read` opt-out for the A/B control run, delete the now-unused `JOB_REDISPATCH_INTERVAL_MS`, and document the disposition for run-doc comparability.

Purpose: Old Phase 27 run docs reference `--investigation continuous_result_task,job_redispatch_pump,...` invocations that must become explicit no-ops, and Plan 05 needs a single compile-time lever that provably restores the old single-dispatch behavior as a regression control.

Output: Clean flag inventory, control-run lever, fail-closed default build proof, disposition note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-CONTEXT.md
@.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-RESEARCH.md
@.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-02-SUMMARY.md
@.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-03-SUMMARY.md

<interfaces>
Flag disposition table (locked, from CONTEXT + RESEARCH flag inventory):

| Mode | Current default | Disposition |
| --- | --- | --- |
| `continuous_result_task` | off | RETIRE (default-on behavior since Plan 02) |
| `job_redispatch_pump` | off | RETIRE (default-on behavior since Plan 02) |
| `initialized_no_mining_gate` | off | RETIRE (default-on behavior since Plan 02) |
| `skip_boot_diagnostic_work` | off | RETIRE (default-on behavior since Plan 02) |
| `require_diagnostic_nonce` | off | KEEP (opt-out rollback lever) |
| `require_uart_proof_for_production` | off | KEEP (opt-out rollback lever) |
| `clear_rx_before_production_work` | off | KEEP (opt-in) |
| `single_dispatch_bounded_read` | (new) | ADD (opt-out control for A/B; zero matches repo-wide at HEAD) |
| `frequency_ramp` / `skip_frequency_ramp` / `skip_max_baud` / `skip_asic_max_baud` / `post_max_baud_delay_2000` / `skip_mining_ready_init` | various | UNTOUCHED |

Mode parsing: `investigation_modes_contain(...)` in `crates/bitaxe-asic/src/work_result_investigation.rs:6-12` (comma-separated, trimmed) — reuse, do not re-parse.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire obsolete investigation flags and add single_dispatch_bounded_read</name>
  <files>firmware/bitaxe/src/asic_adapter/work_result_investigation.rs, firmware/bitaxe/src/asic_adapter.rs, firmware/bitaxe/src/live_stratum_runtime.rs</files>
  <read_first>
    - firmware/bitaxe/src/asic_adapter/work_result_investigation.rs (whole file — every helper and the `JOB_REDISPATCH_INTERVAL_MS` const at ~99)
    - firmware/bitaxe/src/live_stratum_runtime.rs (`new_for_phase27` and the `continuous` field from Plan 02; the retained non-continuous bounded-read branch with `ResultTimeout`)
    - firmware/bitaxe/src/asic_adapter.rs (bootstrap function — remaining references to retired helpers, if any survived Plan 02)
    - crates/bitaxe-asic/src/work_result_investigation.rs lines 1-20 (`investigation_modes_contain` — the parser to reuse)
  </read_first>
  <action>
    1. In `firmware/bitaxe/src/asic_adapter/work_result_investigation.rs`:
       - DELETE the helpers and any mode-string constants for: `continuous_result_task`, `job_redispatch_pump`, `initialized_no_mining_gate`, `skip_boot_diagnostic_work` (including `phase27_initialized_no_mining_bootstrap` if nothing references it after Plans 02/03 — confirm with grep before deleting)
       - DELETE `JOB_REDISPATCH_INTERVAL_MS` (~line 99; Plan 02 replaced its use with `bm1366_job_interval_ms`)
       - ADD `pub fn single_dispatch_bounded_read_enabled() -> bool` implemented via `investigation_modes_contain("single_dispatch_bounded_read")`
       - KEEP unchanged: `require_diagnostic_nonce`, `require_uart_proof_for_production`, `clear_rx_before_production_work`, frequency-ramp/baud/delay modes
    2. In `firmware/bitaxe/src/live_stratum_runtime.rs`: `new_for_phase27()` sets the Plan 02 `continuous` field to `!work_result_investigation::single_dispatch_bounded_read_enabled()`. When `continuous == false` the bridge must behave like the pre-28.1 control: dispatch once on WorkQueued, no listener arming, no regeneration, bounded read with deadline, and on deadline the retained fail-closed `Blocked { reason: ProductionAsicBlocker::ResultTimeout }` branch (this is what makes the Plan 05 control run show `production_result_timeout` if the chip is silent).
    3. Remove any remaining call sites of retired helpers repo-wide (firmware only; crates never referenced them). Do NOT modify `scripts/phase27-live-hardware-bridge-package.sh` — its `--investigation MODE` passthrough is generic and now simply forwards `single_dispatch_bounded_read` for the control run.
    4. This task is the ONLY behavior-adjacent code change in the plan; keep it a single commit (locked: "flag retirement last, own commit") so it is independently revertible.
  </action>
  <verify>
    <automated>just build && cargo clippy --all-targets --all-features -- -D warnings && cargo test --all-features && just test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "continuous_result_task\|job_redispatch_pump\|initialized_no_mining_gate\|skip_boot_diagnostic_work" firmware/ crates/ tools/ scripts/` returns ZERO matches in Rust source and scripts (matches in `.planning/` and `docs/parity/` historical artifacts are allowed and expected)
    - `grep -rn "JOB_REDISPATCH_INTERVAL_MS" firmware/ crates/` returns ZERO matches
    - `grep -n "single_dispatch_bounded_read" firmware/bitaxe/src/asic_adapter/work_result_investigation.rs` matches
    - `grep -n "single_dispatch_bounded_read_enabled" firmware/bitaxe/src/live_stratum_runtime.rs` matches
    - `grep -n "require_diagnostic_nonce\|require_uart_proof_for_production\|clear_rx_before_production_work" firmware/bitaxe/src/asic_adapter/work_result_investigation.rs` — all three still present
    - `grep -n "ResultTimeout" firmware/bitaxe/src/live_stratum_runtime.rs` still matches (control-mode fail-closed branch retained)
    - `just build` and full host gates green
  </acceptance_criteria>
  <done>Retired modes are gone from source, the control lever exists and selects the old single-dispatch bounded-read behavior, rollback levers intact, all gates green, landed as one revertible commit.</done>
</task>

<task type="auto">
  <name>Task 2: Fail-closed default proof and flag-disposition note</name>
  <files>.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-flag-disposition-note.md</files>
  <read_first>
    - scripts/phase27-live-hardware-bridge-package.sh (build envs: `BITAXE_MINING_EVIDENCE_MODE=phase27-live-hardware-asic-stratum-bridge`, ack env, `--investigation` passthrough, manifest output)
    - .planning/phases/27-live-hardware-asic-and-stratum-bridge/phase27-04-defer-note.md (evidence-tier table style and the wrapper-defect note this disposition note extends)
    - .planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-CONTEXT.md (telemetry decision: run docs must note production_result_timeout disappears by design)
  </read_first>
  <action>
    1. Fail-closed default proof (commands, record outputs in the SUMMARY):
       - `just build` (NO action_env) must succeed — default firmware still builds with mining disabled
       - `grep -rn "BITAXE_MINING_EVIDENCE_MODE" firmware/ crates/ | head -20` must show the compile-env gate still keyed on the evidence mode (non-empty)
       - `bash scripts/phase27-live-hardware-bridge-package.sh --help` (or a dry inspection of the script) confirms the package path and `--investigation single_dispatch_bounded_read` forwarding are intact
       - `just verify-reference` passes (reference pin untouched by this phase's code work)
    2. Write `.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-flag-disposition-note.md` (NO standalone `---` in the body; headings/`***` only) containing:
       - the flag disposition table from this plan's `<interfaces>` block, verbatim
       - a "Run-doc comparability" section stating exactly: old documented `--investigation continuous_result_task,...` invocations from Phase 27 run docs are now no-ops (modes retired as defaults); the marker `production_result_timeout` disappears from default-package runs BY DESIGN (replaced by `asic_bridge=timeout_streak count=<n>` continue telemetry); `single_dispatch_bounded_read` is the Phase 28.1 control-run input and the only way to reproduce `production_result_timeout`
       - a "Rollback levers" section: `require_diagnostic_nonce` / `require_uart_proof_for_production` restore the W13 boot gate; `single_dispatch_bounded_read` restores single-dispatch pump behavior; full revert = revert the Task 1 commit
    3. No checklist or parity promotion in this task.
  </action>
  <verify>
    <automated>just build && just verify-reference && test -f .planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-flag-disposition-note.md</automated>
  </verify>
  <acceptance_criteria>
    - `28.1-flag-disposition-note.md` exists; `grep -n "production_result_timeout" .planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-flag-disposition-note.md` matches
    - `grep -n "single_dispatch_bounded_read" .planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-flag-disposition-note.md` matches
    - `grep -c "^---$" .planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-flag-disposition-note.md` equals 0 or 2 (frontmatter delimiters only; note may omit frontmatter entirely)
    - `just build` (fail-closed) and `just verify-reference` both pass
    - `grep -rn "BITAXE_MINING_EVIDENCE_MODE" firmware/ crates/` non-empty (compile-env mining gate intact)
  </acceptance_criteria>
  <done>Default build proven fail-closed after flag retirement; disposition note documents no-op legacy invocations, the by-design disappearance of production_result_timeout, and all rollback levers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compile-time env → firmware behavior | investigation-mode retirement must not widen what a default (env-less) build can do |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28.1-14 | E (Elevation) | flag retirement vs fail-closed default | mitigate | Retired flags gated bridge-internal behavior only; mining enablement remains keyed on `BITAXE_MINING_EVIDENCE_MODE` + hardware ack env (grep-verified in Task 2); `just build` fail-closed proof required |
| T-28.1-15 | R (Repudiation) | loss of investigation reproducibility | mitigate | Disposition note records that legacy invocations are no-ops and names the exact lever (`single_dispatch_bounded_read`) that reproduces old behavior; Task 1 is one revertible commit |
| T-28.1-16 | T (Tampering) | accidental behavior drift during deletion | mitigate | Deletion happens only AFTER Plans 02/03 made behavior unconditional; full host gates + `just test` + xtensa build must pass; control branch (`ResultTimeout`) grep-verified as retained |
</threat_model>

<verification>
Pre-commit gates on Task 1; `just build` + `just verify-reference` + note checks on Task 2. Control-mode behavior itself is verified on hardware in Plan 05 (V13).
</verification>

<success_criteria>
- Flag inventory matches the locked disposition table exactly (grep-clean retirement, keepers present, new lever wired)
- Default build fail-closed proof recorded
- Disposition note exists with the by-design `production_result_timeout` statement
</success_criteria>

<output>
After completion, create `.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-04-SUMMARY.md`
</output>
