---
phase: 28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi
plan: 01
subsystem: mining-orchestration
tags: [rust, stratum-v1, bm1366, bridge-orchestrator, extranonce2, bazel, tdd]

# Dependency graph
requires:
  - phase: 24-production-bm1366-work
    provides: ProductionWorkRegistry, session-generation tracking, MiningWorkBuilder
  - phase: 27-live-hardware-asic-and-stratum-bridge
    provides: AsicBridgeState imperative behavior and the J2c dispatch-starvation evidence this plan encodes as tests
provides:
  - Pure BridgeOrchestrator (crates/bitaxe-stratum/src/v1/bridge_orchestration.rs) with dispatch-before-poll priority, injected-clock cadence, timeout-continue, and redaction-safe marker helpers
  - LiveStratumRuntime::regenerate_work() with held-notify + extranonce2 counter, reset on all four session invalidations
  - bm1366_job_interval_ms(asic_count) upstream-parity interval function in bitaxe-asic
affects: [28.1-02 firmware pump rewiring, 28.1-03 hardware probes, 28.1-04 flag retirement, 28.1-05 hardware evidence]

# Tech tracking
tech-stack:
  added: []
  patterns: [pure decision core + shell interpreter for bridge pump, injected Instant clock for deterministic cadence tests, include_str purity self-check with concatenated forbidden tokens]

key-files:
  created:
    - crates/bitaxe-stratum/src/v1/bridge_orchestration.rs
  modified:
    - crates/bitaxe-asic/src/bm1366/mining_ready.rs
    - crates/bitaxe-stratum/src/v1/live_runtime.rs
    - crates/bitaxe-stratum/src/v1/production_work.rs
    - crates/bitaxe-stratum/src/v1.rs
    - crates/bitaxe-stratum/BUILD.bazel

key-decisions:
  - "BridgeStep has no fatal variant so timeout cannot starve mining (illegal state unrepresentable)"
  - "extranonce2 regeneration asserts the fixture-tested little-endian hex encoding (01000000), matching upstream mining.c, instead of the plan's literal 00000001"
  - "Session invalidations clear regeneration context via one shared reset_regeneration_context helper called from all four paths"
  - "Pre-existing rustfmt drift and clippy gate failures at HEAD were fixed in a standalone chore commit before task commits"

patterns-established:
  - "BridgeOrchestrator pattern: every time-sensitive method takes now: Instant so tests inject a deterministic clock"
  - "Purity self-check: include_str! of own module asserts absence of shell tokens built by concatenation so the test cannot match itself"

requirements-completed: []
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 28.1-2026-07-07T00-34-17
generated_at: 2026-07-07T01:33:21Z

# Metrics
duration: 13min
completed: 2026-07-07
---

# Phase 28.1 Plan 01: Pure Orchestration Core Summary

**Pure BridgeOrchestrator fixing the J2c dispatch-starvation bug at the type/priority level, plus regenerate_work() with an upstream-parity extranonce2 counter and bm1366_job_interval_ms(1)==2000 — all host-tested under cargo AND Bazel**

## Performance

- **Duration:** 13 min
- **Started:** 2026-07-07T01:20:43Z
- **Completed:** 2026-07-07T01:33:21Z
- **Tasks:** 3
- **Files modified:** 6 (plan scope) + 28 (pre-existing toolchain drift chore)

## Accomplishments

- `BridgeOrchestrator::next_step()` encodes dispatch > regenerate > poll > idle priority; the named regression test `dispatch_beats_poll_when_work_queued_and_listener_armed` proves the J2c fix, and `BridgeStep` has no fatal variant so timeout = continue is structural
- `LiveStratumRuntime::regenerate_work()` rebuilds held notify work with fresh extranonce2 (counter 0,1,2...) and stride-8 job ids; counter and held notify reset on clean_jobs, reconnect, authorization reset, and session replacement; first dispatch stays `extranonce2="00000000"` (golden fixture at `production_work.rs` still passing)
- `bm1366_job_interval_ms(asic_count)` mirrors upstream `ASIC_get_asic_job_frequency_ms` (2000 ms / next_power_of_two, zero-count guard), breadcrumbed to `asic.c:149-158` and `device_config.h:93`
- 17-dispatch stride-8 wraparound test proves id-0 slot replacement and correlation resolution to the replacing job (upstream `bm1366.c:323-325` slot-free parity)
- New module wired into BOTH `v1.rs` and the explicit Bazel `srcs` list — `cargo test --all-features` (592 pass) and `just test` (36/36) both exercise it, no dual-graph drift

## Task Commits

Each task was committed atomically (TDD RED verified as failing runs; RED+GREEN combined per commit because repo pre-commit gates forbid committing failing states):

1. **Task 1: bm1366_job_interval_ms** - `51d3a96` (feat)
2. **Task 2: BridgeOrchestrator pure module** - `24cf7fc` (feat)
3. **Task 3: regenerate_work + extranonce2 counter + wraparound tests** - `a4542bf` (feat)

**Pre-task gate remediation:** `68d68d8` (chore: pre-existing rustfmt drift + clippy failures)

## Files Created/Modified

- `crates/bitaxe-stratum/src/v1/bridge_orchestration.rs` - BridgeOrchestrator, BridgeStep, marker helpers, 8 unit tests (J2c regression, cadence, timeout-continue, invalidation, keep-mining, poll bound, marker redaction, purity self-check)
- `crates/bitaxe-asic/src/bm1366/mining_ready.rs` - bm1366_job_interval_ms with upstream breadcrumbs and 4 A/A/A tests
- `crates/bitaxe-stratum/src/v1/live_runtime.rs` - maybe_current_notify + extranonce2_counter fields, regenerate_work(), reset_regeneration_context() on all four invalidations, 7 new tests
- `crates/bitaxe-stratum/src/v1/production_work.rs` - 17-dispatch stride-8 wraparound test (test-only change; dispatch_next stays a pure dequeue)
- `crates/bitaxe-stratum/src/v1.rs` - pub mod bridge_orchestration
- `crates/bitaxe-stratum/BUILD.bazel` - explicit srcs entry for bridge_orchestration.rs

## Decisions Made

- `BridgeStep` poll slices are capped by a shared `MAX_POLL_SLICE_MS = 100` constant the shell can also import
- No-op regeneration returns `StratumV1Error::MissingField("current_notify")` rather than a new error variant
- Held notify is stored as-received; `regenerate_work()` clears `work.clean_jobs` post-build (mirroring `apply_notify`) so regeneration never re-triggers clean-jobs invalidation
- Invalidation reset uses one shared `reset_regeneration_context()` helper (DRY across the four paths; satisfies the plan's alternative acceptance grep)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Pre-existing rustfmt drift and clippy gate failures at HEAD**
- **Found during:** Task 1 (first pre-commit gate run)
- **Issue:** `cargo fmt --all` with the pinned esp toolchain reformatted 28 files untouched by this plan, and `cargo clippy --all-targets --all-features -- -D warnings` failed at HEAD (3 unused test imports in `live_runtime.rs`, 1 explicit_auto_deref in `mining_loop.rs`, 6 manual_contains in `tools/parity/src/mining_allow.rs`). Every task commit was blocked by the mandatory gates.
- **Fix:** Mechanical fmt + clippy-suggested fixes committed as a standalone chore before task commits
- **Files modified:** 28 files (fmt-only hunks plus the 10 clippy suggestions)
- **Verification:** fmt/clippy/build/test gates green; `just test` 36/36
- **Committed in:** `68d68d8` (dedicated chore commit, not mixed into task commits)

**2. [Rule 1 - Bug] Plan's expected extranonce2 hex literals were endianness-wrong**
- **Found during:** Task 3 (regeneration sequence test design)
- **Issue:** Plan Test 1 expected `"00000001"`/`"00000002"` for counters 1/2, but the fixture-tested `extranonce_2_generate` (and upstream `mining.c` bin2hex of the raw little-endian integer) renders `"01000000"`/`"02000000"`. Asserting the plan's literals would have either failed or forced a parity-breaking encoder change.
- **Fix:** Tests assert the parity-correct little-endian encoding; counter sequence semantics (0,1,2) unchanged
- **Files modified:** crates/bitaxe-stratum/src/v1/live_runtime.rs (test expectations only)
- **Verification:** golden first-dispatch `"00000000"` fixture still passes; all 114 bitaxe-stratum tests green
- **Committed in:** `a4542bf` (Task 3 commit)

***

**Total deviations:** 2 auto-fixed (1 blocking, 1 plan-spec bug)
**Impact on plan:** Gate remediation was isolated to its own chore commit; the endianness correction preserves upstream parity that the plan's literal values would have broken. No scope creep. Zero firmware behavior changes — the only firmware-file diffs are fmt-only hunks in the chore commit.

## Issues Encountered

- TDD RED states cannot be committed in this repo (pre-commit gates require green fmt/clippy/build/test), so each TDD task ran RED as an uncommitted failing-test verification (captured in execution logs) and committed test+implementation together once green.

## Known Stubs

None — all new code paths are wired and covered by executable tests; no placeholder values flow anywhere.

## Threat Flags

None — this plan adds no network endpoints, auth paths, file access, or schema changes beyond the plan's threat model; marker output redaction (T-28.1-02) and invalidation resets (T-28.1-03) are unit-tested mitigations.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 28.1-02 can now delegate `AsicBridgeState` to `BridgeOrchestrator` and consume `bm1366_job_interval_ms(1)` + `regenerate_work()` from the pure crates
- Marker helper output (`asic_bridge=job_redispatched extranonce2_counter=<n>`, `asic_bridge=timeout_streak count=<n>`) is ready for firmware logging in Plan 02
- No blockers; hardware evidence work stays in Plan 05

***
*Phase: 28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi*
*Completed: 2026-07-07*

## Self-Check: PASSED

All 6 claimed files exist on disk and all 4 claimed commits (68d68d8, 51d3a96, 24cf7fc, a4542bf) exist in git history.
