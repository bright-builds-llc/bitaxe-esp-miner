---
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 28.1-2026-07-07T00-34-17
generated_at: 2026-07-07T02:45:00Z
---

# Phase 28.1 Flag Disposition Note

Date: 2026-07-07 (Plan 28.1-04, landed after all Phase 28.1 behavior changes as one independently revertible change set)

This note extends the Phase 27 run-doc record ([`../27-live-hardware-asic-and-stratum-bridge/phase27-04-defer-note.md`](../27-live-hardware-asic-and-stratum-bridge/phase27-04-defer-note.md)) with the locked Phase 28.1 investigation-flag disposition so old and new hardware captures stay comparable.

## Flag disposition table (locked, from CONTEXT + RESEARCH flag inventory)

| Mode | Current default | Disposition |
| --- | --- | --- |
| `continuous_result_task` | off | RETIRE (default-on behavior since Plan 02) |
| `job_redispatch_pump` | off | RETIRE (default-on behavior since Plan 02) |
| `initialized_no_mining_gate` | off | RETIRE (default-on behavior since Plan 02) |
| `skip_boot_diagnostic_work` | off | RETIRE (default-on behavior since Plan 02) |
| `require_diagnostic_nonce` | off | KEEP (opt-out rollback lever) |
| `require_uart_proof_for_production` | off | KEEP (opt-out rollback lever) |
| `clear_rx_before_production_work` | off | KEEP (opt-in) |
| `single_dispatch_bounded_read` | (new) | ADD (opt-out control for A/B; no helper fn exists repo-wide at HEAD) |
| `frequency_ramp` / `skip_frequency_ramp` / `skip_max_baud` / `skip_asic_max_baud` / `post_max_baud_delay_2000` / `skip_mining_ready_init` | various | UNTOUCHED |

Mode parsing: `investigation_modes_contain(...)` in `crates/bitaxe-asic/src/work_result_investigation.rs:6-12` (comma-separated, trimmed) — reuse, do not re-parse.

Known pre-existing literals (NOT retirement targets): `crates/bitaxe-asic/src/work_result_investigation.rs:21-51` — the generic `investigation_modes_contain` parser unit tests use the retired mode names as literal FIXTURE strings (11 matches today). These are arbitrary parser inputs, not behavior references; they stay untouched under this plan.

## Run-doc comparability

- Old documented `--investigation continuous_result_task,...` invocations from Phase 27 run docs are now no-ops (modes retired as defaults). The package script's `--investigation MODE` passthrough is generic and unchanged; unrecognized mode strings compile in but gate nothing.
- The marker `production_result_timeout` disappears from default-package runs BY DESIGN (replaced by `asic_bridge=timeout_streak count=<n>` continue telemetry).
- `single_dispatch_bounded_read` is the Phase 28.1 control-run input and the only way to reproduce `production_result_timeout`: it restores one dispatch per queued pool work, no listener arming, no regeneration, bounded 10 s result read, and fail-closed `ResultTimeout` on chip silence.
- W13 boot marker rename: the retention trace is now `asic_work_result_trace=post_init_retention bootstrap=initialized_no_mining` (previously prefixed `skip_boot_diagnostic_work`). The grep-able `bootstrap=initialized_no_mining` substring used by the Phase 28.1 success ladder and Plan 05 run docs is unchanged.

## Rollback levers

- `require_diagnostic_nonce` / `require_uart_proof_for_production` restore the W13 boot gate (diagnostic nonce / UART proof required before production peripheral retention).
- `single_dispatch_bounded_read` restores single-dispatch pump behavior for A/B control runs.
- Full revert = revert the Task 1 commit of Plan 28.1-04 (flag retirement landed as one commit with no unrelated refactors).

## Fail-closed default proof (2026-07-07, post-retirement)

- `just build` with NO action_env succeeds; default firmware builds with mining disabled (no compile envs, no mining).
- `grep -rn "BITAXE_MINING_EVIDENCE_MODE" firmware/ crates/` shows the compile-env mining gate intact: `firmware/bitaxe/build.rs:7`, `firmware/bitaxe/src/asic_adapter.rs:200`, `firmware/bitaxe/src/mining_evidence_mode.rs:11`.
- `bash scripts/phase27-live-hardware-bridge-package.sh --help` confirms the package path and `--investigation single_dispatch_bounded_read` forwarding are intact.
- `just verify-reference` passes: reference clean at `c1915b0a63bfabebdb95a515cedfee05146c1d50`.

***

*Phase: 28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi*
*Plan: 28.1-04*
