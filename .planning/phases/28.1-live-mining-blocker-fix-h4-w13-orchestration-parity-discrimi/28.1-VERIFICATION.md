---
phase: 28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi
verified: 2026-07-07T03:51:47Z
status: passed
score: 24/24 must-haves verified
generated_by: gsd-verifier
lifecycle_mode: yolo
phase_lifecycle_id: 28.1-2026-07-07T00-34-17
generated_at: 2026-07-07T03:51:47Z
lifecycle_validated: true
overrides_applied: 0
---

# Phase 28.1: Live Mining Blocker Fix (H4/W13 Orchestration Parity + Discriminating Probes) Verification Report

**Phase Goal:** The Ultra 205 produces post-dispatch BM1366 UART evidence (register-read proof and/or nonces) under upstream-parity default orchestration, or the blocker is conclusively isolated to hardware via discriminating probes and the approved upstream A/B fallback.
**Verified:** 2026-07-07T03:51:47Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

The phase goal is achieved on both branches simultaneously: (a) post-dispatch BM1366 UART evidence exists under parity-default orchestration — the post-dispatch register-read probe drew the fixed chip-identity response on the retained production UART (J3, device-ms 19483, after dispatch at 19313) — and (b) the blocker is conclusively dispositioned via the discriminating probes plus the executed, user-approved upstream A/B: hardware healthy (upstream v2.14.0 mined 171 nonces with 11 accepted responses on the same board, counts only), `disposition: firmware-nonce-production`, recovery to Rust firmware verified in-session, reference pin clean at `c1915b0a` (re-verified now: pin HEAD matches, worktree clean).

### Observable Truths

Roadmap has no formal success-criteria array for this inserted phase; must-haves are the merged `must_haves.truths` from all five PLAN frontmatter blocks (24 truths), verified against code, tests, and committed run docs — not SUMMARY claims.

**Plan 01 — Pure orchestration core**

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | BridgeOrchestrator returns Dispatch (not Poll) when work queued + listener armed; J2c encoded as executable test | ✓ VERIFIED | `bridge_orchestration.rs:111-114` checks `pending_dispatch` first; test `dispatch_beats_poll_when_work_queued_and_listener_armed` (line 161) passes — ran now: 9/9 module tests green |
| 2 | Regeneration produces fresh extranonce2 1,2,3... while first dispatch stays extranonce2=0 | ✓ VERIFIED | `live_runtime.rs:306-327` increments counter then `.with_extranonce2_value`; `regenerate_work_uses_fresh_extranonce2_counter_sequence` passes; golden `"00000000"` fixture intact (documented endianness correction: hex renders little-endian `01000000`, semantics 0,1,2 preserved) |
| 3 | Counter + held notify reset on clean_jobs, reconnect, authorization reset, session replacement | ✓ VERIFIED | Shared `reset_regeneration_context()` (`live_runtime.rs:293-295`) called from all four invalidate paths (lines 266, 272, 279, 286) |
| 4 | `bm1366_job_interval_ms(1) == 2000` mirroring upstream | ✓ VERIFIED | `mining_ready.rs:72` with `asic.c:149-158` breadcrumb; 4/4 interval tests pass (ran now) |
| 5 | cargo test AND just test both compile the new module (no Bazel srcs drift) | ✓ VERIFIED | `BUILD.bazel` contains `src/v1/bridge_orchestration.rs` (gsd-tools artifact check); `pub mod bridge_orchestration` in `v1.rs`; cargo tests ran now; `just test` 36/36 recorded in Plan 01/REVIEW-FIX evidence |

**Plan 02 — Firmware upstream-parity defaults**

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 6 | W13: chip detect + mining-ready init alone retain production peripherals and publish InitializedNoMining in bridge mode | ✓ VERIFIED | `asic_adapter.rs:287-302` default branch with `asic_init.c:58-61` breadcrumb, `bootstrap=initialized_no_mining` marker, `store_production_peripherals(uart, reset, true)`; hardware-confirmed J3 R1 MET at 10043 ms |
| 7 | `require_diagnostic_nonce` / `require_uart_proof_for_production` remain opt-out rollback levers | ✓ VERIFIED | Both helpers present (`work_result_investigation.rs:60-67`); old diagnostic path with `fail_closed_work_result_read` retained (`asic_adapter.rs:340,638`) |
| 8 | `run_asic_bridge_step` delegates to BridgeOrchestrator; dispatch beats poll; slices ≤ 100 ms | ✓ VERIFIED | Delegation imports/use at `live_stratum_runtime.rs:38,698,787,954`; `MAX_POLL_SLICE_MS` bound unit-tested in pure crate; gsd-tools key-link check passed |
| 9 | Continuous listener + regeneration cadence default; timeout = continue-with-telemetry; mining continues after first share | ✓ VERIFIED | Zero flag reads in runtime file; `ResultTimeout` only at bounded-read site (line 1024, `continuous == false` path); zero `maybe_submit_classified` matches (NormalStop exit gone); hardware: J3 shows 188 `timeout_streak` markers, 22 dispatches, no fail-closed on default path |
| 10 | Power, thermal, hardware-ack, safe-stop gates untouched | ✓ VERIFIED | All safety-file diffs in phase range trace to fmt-only chore `68d68d8` (verified diff content); plan-03 additions to `safety_adapter/` are the new probe module + additive bus retention only; REVIEW confirms `mining_loop_gate` runs before every dispatch |

**Plan 03 — Discriminating probes**

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 11 | Reg-0x00 read after init+baud switch AND after first dispatch, with redaction-safe TX markers | ✓ VERIFIED | `stage=post_init` TX/silent/unparsed markers (helper `run_post_init_register_read_probe` defined at `asic_adapter.rs:367-407`, invoked on the W13 retention branch at line 301, immediately before `store_production_peripherals` at 302); `stage=post_dispatch` at `live_stratum_runtime.rs:888`; 5 `ReadChipId` references in firmware; hardware: J3 R4 both TX markers + responses observed |
| 12 | Register-read response classification makes chip-alive vs chip-silent observable | ✓ VERIFIED (with documented parse caveat) | RegisterReadProof/`register_read_parsed` path exists in `production.rs`; on hardware the chip-identity frame was RECEIVED both post-init and post-dispatch but classified `register_read_unparsed`/`production_result_malformed` — chip-alive became observable either way and drove the disposition; parser follow-up documented in Plan 05 SUMMARY (does not affect discrimination) |
| 13 | INA260 sampled before first dispatch and ~7 s after with numeric-only delta marker | ✓ VERIFIED | `power_baseline mw=` (line 903) and `power_delta baseline_mw=... delta_mw=` (line 928); hardware: J3 R5 `baseline_mw=1400 after_mw=1010 delta_mw=-390` (not hashing) |
| 14 | I2C bus survives bring-up for mid-bridge sampling | ✓ VERIFIED | `power_probe.rs` OnceLock retention; `store_power_probe_bus` called at `phase27_bring_up.rs:173`; hardware: `power_probe_bus_retained` marker in J3 capture; WR-01 fix landed — signature now requires `BitaxeI2cBus<'static>`, transmute deleted (commit `262ca67`) |

**Plan 04 — Flag disposition**

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 15 | Retired helpers/mode literals gone from firmware/tools/scripts; crate parser fixtures exempt | ✓ VERIFIED | Fresh greps: 0 matches for retired names + `JOB_REDISPATCH_INTERVAL_MS` in `firmware/ tools/ scripts/`; 0 matches in `crates/` outside the exempt fixture file; fixture file's only phase-range diff is fmt-only from chore `68d68d8` (fixture strings unchanged) |
| 16 | `single_dispatch_bounded_read` restores old control behavior (one dispatch, bounded read, fail-closed on silence) | ✓ VERIFIED | `single_dispatch_bounded_read_enabled()` (`work_result_investigation.rs:77`); `continuous: !...enabled()` (`live_stratum_runtime.rs:699`); hardware: J4 verdict REACHABLE — one dispatch, zero redispatch/listener markers, fail-closed at ~150 ms |
| 17 | Three keeper levers remain available | ✓ VERIFIED | All three present at `work_result_investigation.rs:60-71` |
| 18 | Default build stays fail-closed (no compile envs → no mining) | ✓ VERIFIED | `BITAXE_MINING_EVIDENCE_MODE` gate intact (`build.rs:7`, `mining_evidence_mode.rs:11`, `asic_adapter.rs:200`); Plan 04 recorded `just build` (no action_env) green post-retirement |
| 19 | Flag disposition landed last as one independently revertible commit | ✓ VERIFIED | Single code commit `3969df0` after Plans 02/03 commits, followed only by docs commits; `28.1-flag-disposition-note.md` documents no-op legacy invocations and by-design disappearance of `production_result_timeout` |

**Plan 05 — Hardware evidence and disposition**

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 20 | Detector-gated parity-default run records every ladder rung explicitly met/not-met | ✓ VERIFIED | J3 doc: 9-row ladder, every rung dispositioned with quoted timestamped markers; detector gate + board-info + commits + manifest recorded; negative check MET (zero `production_result_timeout`) |
| 21 | Multiple `work_dispatched` ~2 s apart recorded as J2c falsification milestone | ✓ VERIFIED | J3 R3: 22 dispatches, spacings 2050-2110 ms, `job_redispatched extranonce2_counter=1..19`; dedicated "J2c confound falsified" section independent of R7/R8 |
| 22 | Control run proves old behavior reachable (regression guard) | ✓ VERIFIED | J4: exactly one dispatch, 0 `job_redispatched`, 0 listener/timeout_streak markers, fail-closed family reached (`production_result_malformed` preempted the 10 s timeout because the chip answered — explicitly dispositioned); verdict REACHABLE |
| 23 | Blocker conclusively isolated via approved upstream A/B with executed recovery and clean pin | ✓ VERIFIED | Trigger evaluation documented (R3 met, stalled before R7, chip-alive-but-no-nonces); upstream v2.14.0 provenance + sha256; nonce/share evidence quoted as counts only; recovery reflash with trusted Rust boot markers; `disposition: firmware-nonce-production`; follow-up (upstream `BM1366_SERIALTX_DEBUG` wire-byte diff) documented; pin re-verified clean now (`c1915b0a`, worktree clean) |
| 24 | All committed run docs redaction-clean | ✓ VERIFIED | Fresh sweep on all four committed docs (J3, J4, disposition, flag-disposition-note): zero matches for pool/wifi keys, IPv4, MAC, `http(s)://`; category labels only; `^---$` count = 2 on each (frontmatter only) |

**Score:** 24/24 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `crates/bitaxe-stratum/src/v1/bridge_orchestration.rs` | Orchestrator + tests, min 200 lines | ✓ VERIFIED | 331 lines; 9 tests pass (8 planned + WR-02 regression `regeneration_unavailable_must_not_permanently_shadow_poll`) |
| `crates/bitaxe-asic/src/bm1366/mining_ready.rs` | `pub fn bm1366_job_interval_ms` | ✓ VERIFIED | Present with breadcrumb; 4 tests pass |
| `crates/bitaxe-stratum/src/v1/live_runtime.rs` | `pub fn regenerate_work` + counter storage | ✓ VERIFIED | Present; reset helper wired to all four invalidations |
| `crates/bitaxe-stratum/BUILD.bazel` | explicit srcs entry | ✓ VERIFIED | gsd-tools artifact check passed |
| `firmware/bitaxe/src/live_stratum_runtime.rs` | pump delegation + markers | ✓ VERIFIED | `BridgeOrchestrator` embedded; both marker helpers called; probes + power delta hooks present |
| `firmware/bitaxe/src/asic_adapter.rs` | W13 retention + post-init probe | ✓ VERIFIED | Breadcrumb `asic_init.c:58`, retention branch, probe markers |
| `firmware/bitaxe/src/safety_adapter/power_probe.rs` | OnceLock bus retention | ✓ VERIFIED | Post-WR-01: `'static` signature, no transmute, no `unwrap()` |
| `firmware/bitaxe/src/asic_adapter/work_result_investigation.rs` | retired flags gone, control lever added | ✓ VERIFIED | Greps clean; lever + keepers present |
| `28.1-flag-disposition-note.md` | disposition table + comparability | ✓ VERIFIED | Contains `production_result_timeout` by-design statement |
| `28.1-run-J3-parity-default.md` | ladder run doc | ✓ VERIFIED | Contains `work_dispatched` evidence, reference commit `c1915b0a` |
| `28.1-run-J4-control-single-dispatch.md` | control run doc | ✓ VERIFIED | Contains `single_dispatch_bounded_read`, REACHABLE verdict |
| `28.1-blocker-disposition.md` | final disposition | ✓ VERIFIED | `disposition: firmware-nonce-production`, exact non-claims section |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `v1.rs` | `bridge_orchestration.rs` | `pub mod` | ✓ WIRED | gsd-tools verified |
| `live_runtime.rs` | `mining.rs` | `with_extranonce2_value` in `regenerate_work` | ✓ WIRED | gsd-tools verified |
| `live_stratum_runtime.rs` | `bridge_orchestration.rs` | `AsicBridgeState` delegates to orchestrator | ✓ WIRED | Imports + 4 call sites |
| `live_stratum_runtime.rs` | `mining_ready.rs` | `BridgeOrchestrator::new(bm1366_job_interval_ms(1))` | ✓ WIRED | Line 698 |
| `asic_adapter.rs` | `production.rs` | `store_production_peripherals` on W13 default path | ✓ WIRED | Line 302 |
| `phase27_bring_up.rs` | `power_probe.rs` | `store_power_probe_bus(bus)` | ✓ WIRED | Line 173 |
| `asic_adapter.rs` | `command.rs` | `Bm1366Command::ReadChipId` TX | ✓ WIRED | 5 firmware references |
| `power_probe.rs` | `ina260.rs` | `read_sample(bus)` | ✓ WIRED | gsd-tools verified |
| `live_stratum_runtime.rs` | `work_result_investigation.rs` | `continuous = !single_dispatch_bounded_read_enabled()` | ✓ WIRED | Line 699 |
| J3 run doc | capture log | per-rung quoted markers | ✓ WIRED | `asic_production_status=work_dispatched` quoted with timestamps (gsd-tools relative-path miss verified manually) |
| disposition doc | J3/J4 run docs | evidence citations | ✓ WIRED | `28.1-run-J3` cited in trigger evaluation and rationale table (verified manually) |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| Bridge pump markers | `extranonce2_counter` / `timeout_streak` | `regenerate_work()` return value / `note_poll_timeout()` | Yes — hardware capture shows counters 1..19 and streak up to 188 | ✓ FLOWING |
| Power-delta marker | `baseline_mw` / `after_mw` | `power_probe::sample_power_mw()` → INA260 | Yes — hardware values 1400/1010 mW recorded | ✓ FLOWING |
| Run docs / disposition | ladder rungs | 360 s foreground captures | Yes — timestamped verbatim log lines quoted per rung | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| J2c regression + orchestrator invariants | `cargo test -p bitaxe-stratum bridge_orchestration` | 9 passed, 0 failed | ✓ PASS |
| Upstream interval parity | `cargo test -p bitaxe-asic bm1366_job_interval` | 4 passed, 0 failed | ✓ PASS |
| Extranonce2 regeneration | `cargo test -p bitaxe-stratum regenerate_work` | 2 passed, 0 failed | ✓ PASS |
| Reference pin clean | `git -C reference/esp-miner rev-parse HEAD` + status | `c1915b0a...`, worktree clean | ✓ PASS |
| Checklist untouched by phase | `git diff --stat c78c087..HEAD -- docs/parity/checklist.md` | empty | ✓ PASS |
| Redaction sweep (4 committed docs) | regex sweep for pool/wifi/IP/MAC/URL material | zero matches | ✓ PASS |

Hardware behaviors (dispatch cadence, probe responses, upstream A/B mining, recovery) were executed and captured during Plan 05 under the repo's standing autonomous Ultra 205 permission; the committed redacted run docs are the repo-sanctioned evidence artifacts and were verified for internal consistency, required fields, quoted markers, and redaction. Raw captures remain local gitignored session state by policy.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| (none) | all plans declare `requirements: []` | Inserted urgent phase; no formal REQUIREMENTS.md mapping | ✓ CONSISTENT | REQUIREMENTS.md maps no IDs to Phase 28.1; STR-09/CFG-07 are context only and NOT promoted — `docs/parity/checklist.md` has zero diff in the phase range; disposition doc's non-claims section explicitly declines promotion (R8 not met) |

No orphaned requirements: REQUIREMENTS.md contains no Phase 28.1 mappings.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `firmware/bitaxe/src/asic_adapter.rs` | 375, 381, 386 | `register_read_silent` also emitted on local encode/TX fault paths (REVIEW IN-02) | ℹ️ Info | Deferred by review scope; adjacent warn lines disambiguate; moot for this phase's evidence (chip responded, silence path never fired) |
| — | — | No TODO/FIXME/placeholder/stub patterns in new phase files | — | Scan clean |

Review status: `28.1-REVIEW.md` is `status: clean` (0 blockers; 2 warnings WR-01/WR-02 fixed via `28.1-REVIEW-FIX.md`, commits `262ca67` and `4db1d23`, both re-verified in code above; 4 info findings documented and deferred). Pre-existing firmware `#[cfg(test)]` bit-rot deferred to `deferred-items.md` (out of phase scope, predates phase).

### Human Verification Required

None. All must-haves were verifiable from repo artifacts, executable host tests, and the committed detector-gated hardware run docs produced under the repo's standing autonomous Ultra 205 permission and Agent-Performed Simple UAT policy.

### Gaps Summary

No gaps. All 24 must-haves verified. The phase goal is achieved: parity-default orchestration works on hardware (J2c confound falsified with 22 dispatches at ~2 s cadence), the discriminating probes produced a conclusive chip-alive-but-not-hashing reading, and the executed upstream A/B isolated the blocker to `firmware-nonce-production` with the recovery path verified and the reference pin clean. No parity checklist promotion was made or claimed. The documented follow-up (upstream `BM1366_SERIALTX_DEBUG` wire-byte diff, with version-rolling as a priority observation) is the recommended next-phase input.

***

_Verified: 2026-07-07T03:51:47Z_
_Verifier: Claude (gsd-verifier)_
