---
phase: 28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/bitaxe-asic/src/bm1366/mining_ready.rs
  - crates/bitaxe-stratum/src/v1/bridge_orchestration.rs
  - crates/bitaxe-stratum/src/v1.rs
  - crates/bitaxe-stratum/BUILD.bazel
  - crates/bitaxe-stratum/src/v1/live_runtime.rs
  - crates/bitaxe-stratum/src/v1/production_work.rs
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "A pure BridgeOrchestrator returns a Dispatch step (not Poll) when work is queued and the continuous listener is armed, encoding the J2c regression as an executable test."
    - "Work regeneration produces fresh extranonce2 values 1,2,3... while the first dispatch stays extranonce2=0 for golden-fixture parity."
    - "The extranonce2 counter and held notify reset on clean_jobs, reconnect, authorization reset, and session replacement."
    - "bm1366_job_interval_ms(1) == 2000 mirroring upstream ASIC_get_asic_job_frequency_ms."
    - "cargo test and just test both compile and run the new bridge_orchestration module (no Bazel srcs drift)."
  artifacts:
    - path: "crates/bitaxe-stratum/src/v1/bridge_orchestration.rs"
      provides: "BridgeOrchestrator with injected-clock next_step(), dispatch-before-poll priority, regeneration cadence, timeout-continue, marker helper fns, unit tests"
      min_lines: 200
    - path: "crates/bitaxe-asic/src/bm1366/mining_ready.rs"
      provides: "bm1366_job_interval_ms(asic_count) with upstream breadcrumb and tests"
      contains: "pub fn bm1366_job_interval_ms"
    - path: "crates/bitaxe-stratum/src/v1/live_runtime.rs"
      provides: "maybe_current_notify + extranonce2_counter storage and regenerate_work()"
      contains: "pub fn regenerate_work"
    - path: "crates/bitaxe-stratum/BUILD.bazel"
      provides: "explicit srcs entry for bridge_orchestration.rs"
      contains: "src/v1/bridge_orchestration.rs"
  key_links:
    - from: "crates/bitaxe-stratum/src/v1.rs"
      to: "crates/bitaxe-stratum/src/v1/bridge_orchestration.rs"
      via: "pub mod bridge_orchestration"
      pattern: "pub mod bridge_orchestration"
    - from: "crates/bitaxe-stratum/src/v1/live_runtime.rs"
      to: "crates/bitaxe-stratum/src/v1/mining.rs"
      via: "MiningWorkBuilder::with_extranonce2_value in regenerate_work"
      pattern: "with_extranonce2_value"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1-2026-07-07T00-34-17
generated_at: 2026-07-07T00:53:22.000Z
---

<objective>
Build the pure functional core for upstream-parity bridge orchestration: the `BridgeOrchestrator` that fixes the J2c dispatch-starvation bug at the type/priority level, `regenerate_work()` with an extranonce2 counter in `live_runtime.rs`, and `bm1366_job_interval_ms()` in `bitaxe-asic` — all with executable host tests.

Purpose: Phase 27 evidence run J2c never dispatched work because `awaiting_result_read` was checked before `maybe_pending_dispatch`, and the redispatch pump regenerated nothing. All orchestration decisions must move into host-testable crates (firmware crate tests do NOT execute — see RESEARCH "Test Surfaces"). This plan has zero firmware risk.

Output: New `bridge_orchestration.rs` module (wired into `v1.rs` AND the explicit Bazel `srcs` list), extended `live_runtime.rs`/`production_work.rs` tests, new interval function, all green under `cargo test --all-features` and `just test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-CONTEXT.md
@.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-RESEARCH.md
@.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-VALIDATION.md

<interfaces>
Existing contracts the executor uses directly (verified at HEAD 0d89e7cb per RESEARCH — re-verify line anchors if HEAD moved):

From crates/bitaxe-stratum/src/v1/mining.rs:
```rust
// mining.rs:64-66 — already exists, const fn, default extranonce2_value = 0 (mining.rs:50)
pub const fn with_extranonce2_value(self, value: u64) -> Self;
// build() calls extranonce_2_generate(self.extranonce2_value, extranonce2_len) (mining.rs:69-80)
```

From crates/bitaxe-stratum/src/v1/live_runtime.rs:
```rust
// live_runtime.rs:416-420 — stride-8 job id: wrapping_add(8) % 128, initial 0 (line 183)
fn next_asic_job_id(&mut self) -> Bm1366JobId;
// live_runtime.rs:377-408 apply_notify — the ONLY MiningWork build site today; notify currently consumed, not stored
// live_runtime.rs:259-278 — invalidate_for_clean_jobs / invalidate_for_reconnect /
//   invalidate_for_authorization_reset / invalidate_for_session_replacement (all forward to registry)
```

From crates/bitaxe-stratum/src/v1/production_work.rs:
```rust
// production_work.rs:226-239 — dispatch_next() dequeues; HashMap active_work insert overwrites on job-id reuse (231-232)
// production_work.rs:590 — golden assert: first active.extranonce2 == "00000000" (MUST keep passing)
```

From crates/bitaxe-asic/src/bm1366/mining_ready.rs:
```rust
// mining_ready.rs:55 — pub(or crate) fn next_power_of_two(value: u32) -> u32 (already tested)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bm1366_job_interval_ms to bitaxe-asic mining_ready</name>
  <files>crates/bitaxe-asic/src/bm1366/mining_ready.rs</files>
  <read_first>
    - crates/bitaxe-asic/src/bm1366/mining_ready.rs (whole file: `next_power_of_two` near line 55, existing tests near lines 445-448, existing breadcrumb comment style)
    - reference/esp-miner/components/asic/asic.c lines 140-165 (READ-ONLY upstream behavior: `default_asic_timeout / _next_power_of_two(asic_count)`)
    - reference/esp-miner/main/device_config.h line 93 (READ-ONLY: BM1366 `default_asic_timeout = 2000`)
  </read_first>
  <behavior>
    - Test 1: `bm1366_job_interval_ms(1) == 2000`
    - Test 2: `bm1366_job_interval_ms(2) == 1000`
    - Test 3: `bm1366_job_interval_ms(5) == 250` (next_power_of_two(5) = 8)
    - Test 4: `bm1366_job_interval_ms(0) == 2000` (guard: zero count treated as one chip, no divide-by-zero)
  </behavior>
  <action>
    Add to `crates/bitaxe-asic/src/bm1366/mining_ready.rs` (beside `next_power_of_two`):

    ```rust
    /// BM1366 per-job dispatch interval in milliseconds for a given chain length.
    ///
    /// Reference: reference/esp-miner/components/asic/asic.c:149-158
    /// (ASIC_get_asic_job_frequency_ms) and
    /// reference/esp-miner/main/device_config.h:93 (BM1366 default_asic_timeout = 2000).
    pub fn bm1366_job_interval_ms(asic_count: u32) -> u32 {
        const BM1366_DEFAULT_ASIC_TIMEOUT_MS: u32 = 2000;
        if asic_count == 0 {
            return BM1366_DEFAULT_ASIC_TIMEOUT_MS;
        }
        BM1366_DEFAULT_ASIC_TIMEOUT_MS / next_power_of_two(asic_count)
    }
    ```

    Breadcrumb comments only — do NOT copy upstream C expression or comment text (GPL rule). Write the four Arrange/Act/Assert tests from <behavior> in the existing `#[cfg(test)]` module. Export the function through whatever re-export path `mining_ready` items already use (check `crates/bitaxe-asic/src/bm1366.rs` mod/pub use style) so firmware can call `bitaxe_asic::bm1366::mining_ready::bm1366_job_interval_ms` (Plan 02 consumes it). Do NOT touch `firmware/bitaxe/src/asic_adapter/work_result_investigation.rs` `JOB_REDISPATCH_INTERVAL_MS` yet — Plan 02 stops using it, Plan 04 deletes it.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-asic bm1366_job_interval</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub fn bm1366_job_interval_ms" crates/bitaxe-asic/src/bm1366/mining_ready.rs` returns exactly one match
    - `grep -n "asic.c:149" crates/bitaxe-asic/src/bm1366/mining_ready.rs` returns a breadcrumb match
    - `cargo test -p bitaxe-asic bm1366_job_interval` passes with 4 new tests
    - `grep -c "// Arrange" crates/bitaxe-asic/src/bm1366/mining_ready.rs` increased by 4 (A/A/A comments present in new tests)
  </acceptance_criteria>
  <done>`bm1366_job_interval_ms` exists with upstream breadcrumb, 4 passing tests, no upstream expression copied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create BridgeOrchestrator pure module with J2c regression tests and marker helpers</name>
  <files>crates/bitaxe-stratum/src/v1/bridge_orchestration.rs, crates/bitaxe-stratum/src/v1.rs, crates/bitaxe-stratum/BUILD.bazel</files>
  <read_first>
    - firmware/bitaxe/src/live_stratum_runtime.rs lines 661-935 (current `AsicBridgeState`, `run_asic_bridge_step`, `maybe_arm_continuous_listener`, `note_job_redispatch_if_due`, `read_and_correlate_asic_result` — the imperative behavior the orchestrator must model; note line 783 poll-before-dispatch bug)
    - crates/bitaxe-stratum/src/v1.rs (mod list at lines 8-19 — where to add the new mod)
    - crates/bitaxe-stratum/BUILD.bazel (explicit srcs list at lines 5-23 — no glob; the rust_test target at lines 39-42 reuses the crate)
    - crates/bitaxe-asic/src/bm1366/production.rs lines 313-320 (include_str self-check test pattern: `production_executor_module_never_references_diagnostic_work`)
    - reference/esp-miner/main/tasks/create_jobs_task.c lines 55-190 (READ-ONLY: timed-dequeue cadence, extranonce_2++ on timeout, no-work idle at 149-151)
    - reference/esp-miner/main/tasks/asic_result_task.c lines 20-45 (READ-ONLY: NULL-on-timeout continue, never fatal)
  </read_first>
  <behavior>
    - Test 1 (J2c regression, literal): Arrange orchestrator with listener armed (`note_listener_armed()`) AND work queued (`note_work_queued()`); Act `next_step(t0)`; Assert step == `BridgeStep::Dispatch` — NOT `Poll`. Name the test so the bug is legible, e.g. `dispatch_beats_poll_when_work_queued_and_listener_armed`.
    - Test 2 (cadence, injected clock): after `note_dispatched(t0)` with `job_interval_ms = 2000`, `next_step(t0 + 1999ms)` is `Poll {..}` and `next_step(t0 + 2000ms)` is `Regenerate`.
    - Test 3 (timeout = continue): `note_poll_timeout(..)` increments a streak counter and returns it; subsequent `next_step` still yields `Poll`/`Regenerate` — there is NO fatal/blocked variant in `BridgeStep` (illegal state unrepresentable).
    - Test 4 (invalidation clears timer): after `note_dispatched(t0)` then `invalidate_session()`, `next_step(t0 + 10s)` is NOT `Regenerate` (returns `Idle` or `Poll` only after new work/dispatch).
    - Test 5 (keep-mining, V14 surrogate): after `note_result_received()` (result correlated / share classified), `next_step` continues the Poll/Regenerate cadence — the orchestrator has no terminal state.
    - Test 6 (poll slice bound): every `Poll { slice_ms }` returned has `slice_ms <= 100`.
    - Test 7 (marker redaction): `job_redispatched_marker(3)` == exactly `"asic_bridge=job_redispatched extranonce2_counter=3"`; `timeout_streak_marker(2)` == exactly `"asic_bridge=timeout_streak count=2"`; assert neither contains "0x", "extranonce2=" (raw-value key), "job_id=", or any hex payload.
    - Test 8 (purity self-check): `include_str!("bridge_orchestration.rs")` does not contain "esp_idf", "firmware", or "UartDriver" (pattern from production.rs:313-320).
  </behavior>
  <action>
    Create `crates/bitaxe-stratum/src/v1/bridge_orchestration.rs` — a pure module (std only, no firmware/esp-idf types). Design (naming is discretion, semantics are locked):

    ```rust
    /// Upstream-parity bridge step decisions.
    /// Reference: reference/esp-miner/main/tasks/create_jobs_task.c:60,84-86,183-187
    /// (timed-dequeue regeneration cadence) and
    /// reference/esp-miner/main/tasks/asic_result_task.c:24-36 (timeout = continue, never fatal).
    #[derive(Debug, Clone, Copy, PartialEq, Eq)]
    pub enum BridgeStep {
        /// Dispatch queued pool work now (always beats polling — J2c fix).
        Dispatch,
        /// Regenerate work with a fresh extranonce2 and dispatch it.
        Regenerate,
        /// Poll the result path for at most slice_ms (<= 100).
        Poll { slice_ms: u32 },
        /// Nothing to do this iteration.
        Idle,
    }

    pub struct BridgeOrchestrator { /* fields below */ }
    ```

    State fields: `pending_dispatch: bool`, `listener_armed: bool`, `maybe_last_dispatch_at: Option<Instant>`, `job_interval_ms: u32` (constructor parameter — firmware passes `bm1366_job_interval_ms(1)`), `timeout_streak: u64`. Clock injection: every time-sensitive method takes `now: std::time::Instant` as a parameter (no trait needed; fully deterministic tests).

    Methods (exact semantics):
    - `new(job_interval_ms: u32) -> Self`
    - `note_work_queued(&mut self)` → `pending_dispatch = true`
    - `note_listener_armed(&mut self)` → `listener_armed = true`
    - `note_dispatched(&mut self, now: Instant)` → `pending_dispatch = false; maybe_last_dispatch_at = Some(now)`
    - `note_poll_timeout(&mut self) -> u64` → increments and returns `timeout_streak` (never fails)
    - `note_result_received(&mut self)` → `timeout_streak = 0` (cadence continues)
    - `invalidate_session(&mut self)` → `pending_dispatch = false; maybe_last_dispatch_at = None; timeout_streak = 0` (clears the redispatch timer; listener stays armed)
    - `next_step(&self, now: Instant) -> BridgeStep` with priority order:
      1. `pending_dispatch` → `Dispatch` (BEFORE any poll check — this line is the J2c fix)
      2. `maybe_last_dispatch_at` elapsed `>= job_interval_ms` → `Regenerate`
      3. `listener_armed` and a dispatch has happened (`maybe_last_dispatch_at.is_some()`) → `Poll { slice_ms: 100 }`
      4. otherwise `Idle`
    - `pub fn job_redispatched_marker(counter: u64) -> String` → `format!("asic_bridge=job_redispatched extranonce2_counter={counter}")`
    - `pub fn timeout_streak_marker(count: u64) -> String` → `format!("asic_bridge=timeout_streak count={count}")`

    Wiring (BOTH required in this task — Pitfall 4 Bazel srcs drift):
    1. Add `pub mod bridge_orchestration;` to the mod list in `crates/bitaxe-stratum/src/v1.rs`.
    2. Add `"src/v1/bridge_orchestration.rs"` to the explicit `srcs` list in `crates/bitaxe-stratum/BUILD.bazel`.

    Write all 8 tests from <behavior> with `// Arrange` / `// Act` / `// Assert` comments. No `unwrap()`; `maybe_` prefix on Option fields.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-stratum bridge_orchestration && just test</automated>
  </verify>
  <acceptance_criteria>
    - `crates/bitaxe-stratum/src/v1/bridge_orchestration.rs` exists; `grep -n "enum BridgeStep" crates/bitaxe-stratum/src/v1/bridge_orchestration.rs` matches
    - `grep -n "pub mod bridge_orchestration" crates/bitaxe-stratum/src/v1.rs` matches
    - `grep -n "src/v1/bridge_orchestration.rs" crates/bitaxe-stratum/BUILD.bazel` matches
    - `grep -n "dispatch_beats_poll" crates/bitaxe-stratum/src/v1/bridge_orchestration.rs` matches (J2c regression test present)
    - `grep -n "asic_bridge=job_redispatched extranonce2_counter=" crates/bitaxe-stratum/src/v1/bridge_orchestration.rs` and `grep -n "asic_bridge=timeout_streak count=" crates/bitaxe-stratum/src/v1/bridge_orchestration.rs` both match
    - `grep -cn "esp_idf\|UartDriver" crates/bitaxe-stratum/src/v1/bridge_orchestration.rs` — matches appear ONLY inside the purity self-check test's assertion strings (or zero if the test greps via include_str for other tokens)
    - `cargo test -p bitaxe-stratum bridge_orchestration` passes with >= 8 tests
    - `just test` passes (Bazel rust_test compiled the new module — proves no srcs drift)
  </acceptance_criteria>
  <done>BridgeOrchestrator exists in the pure crate with dispatch-before-poll priority proven by a named J2c regression test, cadence/invalidation/timeout-continue/marker tests green under both cargo and Bazel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add regenerate_work + extranonce2 counter to live_runtime and stride-8 wraparound tests</name>
  <files>crates/bitaxe-stratum/src/v1/live_runtime.rs, crates/bitaxe-stratum/src/v1/production_work.rs</files>
  <read_first>
    - crates/bitaxe-stratum/src/v1/live_runtime.rs (esp. `apply_notify` at 377-408, `next_asic_job_id` at 416-420, the four `invalidate_for_*` methods at 259-278, test helper fns near 651-663, struct fields near 183)
    - crates/bitaxe-stratum/src/v1/production_work.rs (esp. `dispatch_next` at 226-239, `invalidate_*` at 310-329, golden assert at line 590 `active.extranonce2 == "00000000"`, existing test builders near 379+)
    - crates/bitaxe-stratum/src/v1/mining.rs lines 40-85 (`MiningWorkBuilder`, `with_extranonce2_value`, `extranonce_2_generate`)
    - reference/esp-miner/main/tasks/create_jobs_task.c lines 55-135 (READ-ONLY: `extranonce_2 = 0` init at :59, reset on new dequeued work at :132, increment on regeneration at :183-186)
  </read_first>
  <behavior>
    - Test 1: after `apply_notify` (builds work with default extranonce2=0), calling `regenerate_work()` enqueues work whose extranonce2 hex is "00000001" (with the standard 4-byte extranonce2_len fixture), then "00000002" on the next call — counter sequence 0,1,2.
    - Test 2: `regenerate_work()` with no held notify returns an Err/None (no-op) — it must NOT fabricate work.
    - Test 3: each of `invalidate_for_clean_jobs`, `invalidate_for_reconnect`, `invalidate_for_authorization_reset`, `invalidate_for_session_replacement` resets the counter to 0 AND clears the held notify (subsequent `regenerate_work()` is a no-op until a fresh notify arrives; the first work from that fresh notify uses extranonce2=0).
    - Test 4 (17-job stride-8 wraparound): 17 sequential dispatches produce job ids 0,8,16,...,120,0; the 17th (id 0) REPLACES the record for the first job in `active_work` (HashMap insert overwrite, matching upstream slot-free semantics at bm1366.c:323-325); correlation against the replaced (stale) job id yields the NEW job's work.
    - Test 5 (golden regression): existing `production_work.rs:590` fixture test still passes — first dispatch extranonce2 stays "00000000".
  </behavior>
  <action>
    In `crates/bitaxe-stratum/src/v1/live_runtime.rs`:

    1. Add struct fields: `maybe_current_notify: Option<...>` (same owned notify type `apply_notify` receives — store a clone of the notify together with whatever extranonce/difficulty/version-mask context `apply_notify` uses so regeneration rebuilds an identical builder except extranonce2 and job id) and `extranonce2_counter: u64` (init 0).
    2. In `apply_notify` (377-408): store the notify (and its build context) into `maybe_current_notify`, reset `extranonce2_counter = 0`, keep the existing build unchanged (builder default extranonce2 = 0 — golden parity, Pitfall 2). Reference breadcrumb: `reference/esp-miner/main/tasks/create_jobs_task.c:132` (counter resets when new work arrives).
    3. Add `pub fn regenerate_work(&mut self) -> ...`:
       - `let Some(context) = self.maybe_current_notify.as_ref() else { return <no-op error/None>; };`
       - increment first: `self.extranonce2_counter += 1;`
       - rebuild via `MiningWorkBuilder::new(...)` + same difficulty/version-mask context + `.with_extranonce2_value(self.extranonce2_counter)` + `builder.build(self.next_asic_job_id())` (stride-8 id from the existing method)
       - enqueue into the registry exactly the way `apply_notify` does, and return the new counter value (firmware logs it in the `job_redispatched` marker)
       - breadcrumb: `reference/esp-miner/main/tasks/create_jobs_task.c:183-186` (regenerate + extranonce_2++ on cadence timeout)
    4. In all four `invalidate_for_*` methods (259-278): add `self.maybe_current_notify = None; self.extranonce2_counter = 0;` before/after the existing registry forwarding (keep existing behavior intact).

    In `crates/bitaxe-stratum/src/v1/production_work.rs`: add the 17-dispatch wraparound test (Test 4) using existing test builders; assert `active_work` record replacement on id-0 reuse and correlation behavior. Do not change `production_work.rs` non-test code — `dispatch_next` stays a pure dequeue.

    All tests Arrange/Act/Assert with explicit comments. No `unwrap()` in non-test code.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-stratum && cargo test --all-features && just test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub fn regenerate_work" crates/bitaxe-stratum/src/v1/live_runtime.rs` matches
    - `grep -n "maybe_current_notify" crates/bitaxe-stratum/src/v1/live_runtime.rs` matches (maybe_ prefix per standards)
    - `grep -n "extranonce2_counter" crates/bitaxe-stratum/src/v1/live_runtime.rs` matches
    - `grep -c "maybe_current_notify = None" crates/bitaxe-stratum/src/v1/live_runtime.rs` >= 4 (all four invalidate paths) OR a single shared reset helper called from all four (grep the helper call count >= 4)
    - `grep -n "create_jobs_task.c:183" crates/bitaxe-stratum/src/v1/live_runtime.rs` breadcrumb matches
    - `cargo test -p bitaxe-stratum` passes including a test whose name contains `wraparound` in production_work.rs and a test asserting extranonce2 "00000001"
    - Existing golden test at production_work.rs:590 unchanged and passing (`grep -n "00000000" crates/bitaxe-stratum/src/v1/production_work.rs` still present)
  </acceptance_criteria>
  <done>Regeneration with fresh extranonce2 exists and is fully covered: sequence 0,1,2; reset on all four invalidations; no-op without notify; stride-8 wraparound proven; golden first-dispatch parity intact; full pre-commit gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pool socket → runtime work state | notify/clean_jobs from the pool drive work regeneration and counter resets |
| pure crate → firmware markers | marker helper output is later written to retained device logs and evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28.1-01 | D (DoS) | BridgeOrchestrator::next_step | mitigate | Dispatch-before-poll priority encoded as named unit test (`dispatch_beats_poll_when_work_queued_and_listener_armed`); `BridgeStep` has no fatal variant so timeout cannot starve mining |
| T-28.1-02 | I (Info disclosure) | job_redispatched_marker / timeout_streak_marker | mitigate | Marker helpers emit only integer counters; redaction unit test asserts exact format and absence of job/extranonce/hex material |
| T-28.1-03 | T (Tampering) | regenerate_work session integrity | mitigate | Counter + held notify cleared on all four invalidation paths (unit tested), preventing stale-session work regeneration after clean_jobs/reconnect |
| T-28.1-04 | E (Elevation) | default build mining gates | accept | This plan touches only pure crates; no compile-env gate or firmware behavior changes — fail-closed default build unaffected |
</threat_model>

<verification>
Full host gate after final task: `cargo fmt --all` → `cargo clippy --all-targets --all-features -- -D warnings` → `cargo build --all-targets --all-features` → `cargo test --all-features` → `just test`. All green before commit (repo pre-commit rule).
</verification>

<success_criteria>
- V1 (dispatch beats poll), V2 (cadence + interval fn), V3 (counter/timer resets), V4 (wraparound), V5 (golden extranonce2=0), V6 unit half (timeout-continue), V7 (marker redaction), V14 unit half (no terminal state) from 28.1-VALIDATION.md all have passing tests in `crates/*`
- `just test` and `cargo test --all-features` both exercise `bridge_orchestration.rs` (no dual-graph drift)
- Zero firmware files modified
</success_criteria>

<output>
After completion, create `.planning/phases/28.1-live-mining-blocker-fix-h4-w13-orchestration-parity-discrimi/28.1-01-SUMMARY.md`
</output>
