---
phase: 17-live-http-api-and-static-evidence
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/phase17-live-http-api-smoke.sh
  - scripts/phase17-live-http-api-smoke-test.sh
  - scripts/phase17-websocket-capture.mjs
  - scripts/BUILD.bazel
  - docs/parity/evidence/phase-17-live-http-api-and-static-evidence/README.md
  - docs/parity/evidence/phase-17-live-http-api-and-static-evidence/redaction-review.md
autonomous: true
requirements:
  - API-09
  - REL-01
  - REL-07
  - EVD-05
must_haves:
  truths:
    - "Executor can run a Phase 17 helper test before touching live hardware or network evidence"
    - "Live route probes accept only explicit origin-only DEVICE_URL input and never scan or infer targets"
    - "HTTP no-upgrade WebSocket checks are recorded as route coexistence only"
    - "Real WebSocket frame capture exists as a separate bounded helper for `/api/ws/live` and `/api/ws`"
    - "Redaction review scaffold exists before any Phase 17 live artifacts are cited"
  artifacts:
    - path: "scripts/phase17-live-http-api-smoke.sh"
      provides: "Phase 17 HTTP/static/API route evidence helper"
      contains: "phase17_live_http_api_smoke"
    - path: "scripts/phase17-live-http-api-smoke-test.sh"
      provides: "Fake-command tests for target validation, route coverage, identity blocking, and redaction"
      contains: "test_userinfo_path_query_fragment_rejected"
    - path: "scripts/phase17-websocket-capture.mjs"
      provides: "Bounded WebSocket frame capture for Phase 17"
      contains: "allowedWebSocketPaths"
    - path: "docs/parity/evidence/phase-17-live-http-api-and-static-evidence/redaction-review.md"
      provides: "Artifact matrix and absent-artifact review scaffold"
      contains: "redaction_status: pending"
  key_links:
    - from: "scripts/phase17-live-http-api-smoke.sh"
      to: "docs/parity/evidence/phase-17-live-http-api-and-static-evidence/target-lock.json"
      via: "sanitized explicit target/package/flash identity output"
      pattern: "target-lock"
    - from: "scripts/phase17-websocket-capture.mjs"
      to: "/api/ws/live"
      via: "allowlisted WebSocket path conversion"
      pattern: "/api/ws/live"
    - from: "scripts/BUILD.bazel"
      to: "scripts/phase17-live-http-api-smoke-test.sh"
      via: "sh_test registration"
      pattern: "phase17_live_http_api_smoke_test"
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 17-2026-07-02T01-09-48
generated_at: 2026-07-02T01:37:09Z
---

<objective>
Create Phase 17 Wave 0 helper, test, WebSocket capture, Bazel, and redaction-review surfaces before live evidence is collected.

Purpose: Phase 17 must close live HTTP/static/API/WebSocket evidence gaps without weakening target provenance, leaking secrets, or overclaiming OTA/WebSocket behavior.
Output: Tested Phase 17 route helper, bounded WebSocket capture helper, Bazel test wiring, and evidence/redaction scaffold.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/verification.md
@standards/core/testing.md
@standards/core/operability.md
@standards/languages/typescript-javascript.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-live-http-api-and-static-evidence/17-CONTEXT.md
@.planning/phases/17-live-http-api-and-static-evidence/17-RESEARCH.md
@.planning/phases/17-live-http-api-and-static-evidence/17-VALIDATION.md
@scripts/phase16-http-static-smoke.sh
@scripts/phase16-http-static-smoke-test.sh
@scripts/phase15-websocket-capture.mjs
@scripts/phase14-live-telemetry.sh
@scripts/BUILD.bazel
@docs/parity/evidence/phase-16-current-commit-release-evidence-completion/redaction-review.md

<interfaces>
From `scripts/phase16-http-static-smoke.sh`, reuse the per-route artifact pattern:
```bash
<id>.headers.txt
<id>.body.txt
<id>.curl-error.txt
http-static-smoke.log
```

Phase 17 route set from D-08:
```text
GET /
GET /assets/app.css.gz
GET /phase17-missing-static
GET /recovery
GET /api/system/info
GET /api/phase17-unknown
GET /api/ws
GET /api/ws/live
POST /api/system/OTA
POST /api/system/OTAWWW
```

From `scripts/phase15-websocket-capture.mjs`, keep bounded capture shape:
```javascript
new globalThis.WebSocket(wsUrl);
durationMs <= 30000;
maxFrames <= 10;
```
</interfaces>
</context>

<read_first>
  <file>AGENTS.md</file>
  <file>AGENTS.bright-builds.md</file>
  <file>standards/core/verification.md</file>
  <file>standards/core/testing.md</file>
  <file>standards/languages/typescript-javascript.md</file>
  <file>.planning/phases/17-live-http-api-and-static-evidence/17-CONTEXT.md</file>
  <file>.planning/phases/17-live-http-api-and-static-evidence/17-RESEARCH.md</file>
  <file>.planning/phases/17-live-http-api-and-static-evidence/17-VALIDATION.md</file>
  <file>scripts/phase16-http-static-smoke.sh</file>
  <file>scripts/phase16-http-static-smoke-test.sh</file>
  <file>scripts/phase15-websocket-capture.mjs</file>
  <file>scripts/BUILD.bazel</file>
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the Phase 17 HTTP/static/API smoke helper</name>
  <requirements>API-09, REL-01, EVD-05</requirements>
  <files>scripts/phase17-live-http-api-smoke.sh, scripts/phase17-live-http-api-smoke-test.sh, scripts/BUILD.bazel</files>
  <read_first>
    <file>scripts/phase17-live-http-api-smoke.sh</file>
    <file>scripts/phase17-live-http-api-smoke-test.sh</file>
    <file>scripts/BUILD.bazel</file>
    <file>scripts/phase16-http-static-smoke.sh</file>
    <file>scripts/phase16-http-static-smoke-test.sh</file>
    <file>.planning/phases/17-live-http-api-and-static-evidence/17-CONTEXT.md</file>
    <file>.planning/phases/17-live-http-api-and-static-evidence/17-VALIDATION.md</file>
    <file>docs/parity/evidence/phase-16-current-commit-release-evidence-completion/redaction-review.md</file>
  </read_first>
  <behavior>
    - Test 1: Missing `DEVICE_URL` writes `DEVICE_URL status: blocked - missing DEVICE_URL`, `network_scan: disabled`, and calls no curl command.
    - Test 2: `ftp://device.local`, `http://user:pass@device.local`, `http://device.local/path`, `http://device.local?x=1`, and `http://device.local#frag` are rejected before curl.
    - Test 3: Stale or mismatched package/flash identity writes `identity_status: blocked` and calls no curl command.
    - Test 4: Fake successful routes cover all ten D-08 paths and record route-specific markers for D-09.
    - Test 5: `/api/ws` and `/api/ws/live` 400 or 426 responses record `websocket_no_upgrade_claim: route-coexistence-only` and never `websocket_frame_status: passed`.
    - Test 6: API bodies and curl errors redact `ssid`, `wifiPass`, `stratumUser`, `poolUrl`, private IPs, MACs, tokens, and raw URLs.
  </behavior>
  <action>Create `scripts/phase17-live-http-api-smoke.sh` as a Phase-owned Bash helper, not a mutation of Phase 16 history. It must use `#!/usr/bin/env bash` and `set -euo pipefail`. Accepted inputs: `--device-url URL`, `--manifest PATH`, `--flash-evidence-json PATH`, `--out-dir PATH`, `--target-lock-out PATH`, and `--curl-bin PATH`; default `DEVICE_URL` may come from the explicit `DEVICE_URL` environment variable, default manifest is `bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json`, default out dir is `docs/parity/evidence/phase-17-live-http-api-and-static-evidence/http-static-api`, default target lock is `docs/parity/evidence/phase-17-live-http-api-and-static-evidence/target-lock.json`, and default curl is `curl`. Implement D-01, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-17, D-18, D-19, and D-20 exactly: validate `DEVICE_URL` as origin-only `http://` or `https://`; reject userinfo, path other than `/`, query, and fragment; never scan, mDNS, ARP, router-scrape, serial-log parse, or guess; require package manifest `source_commit` and `reference_commit`; require `--flash-evidence-json` before live probes; require flash JSON `command_kind` contains `flash-monitor`, `board` equals `205`, `trusted_output` equals `true`, `firmware_commit` equals manifest `source_commit`, `reference_commit` equals manifest `reference_commit`, `observed_reference_commit` equals manifest `reference_commit`, and `observed_firmware_commit` equals the full source commit or a prefix of at least 12 characters. If any target or identity prerequisite is missing/stale/wrong, write a blocked log and exit 0 without curl. For passed preflight, write sanitized `target-lock.json` containing `target_status`, `device_url_source`, `device_url_redacted`, `board`, `selected_port` when present in flash JSON, `source_commit`, `reference_commit`, `manifest`, `flash_evidence_json`, `network_scan: disabled`, and `created_from_explicit_input: true`; do not write the raw `DEVICE_URL`. Probe the exact D-08 route list in this order: `GET /`, `GET /assets/app.css.gz`, `GET /phase17-missing-static`, `GET /recovery`, `GET /api/system/info`, `GET /api/phase17-unknown`, `GET /api/ws`, `GET /api/ws/live`, `POST /api/system/OTA`, and `POST /api/system/OTAWWW`. Route pass criteria: `/` status 200 and body contains `AxeOS unavailable`, `Open recovery`, and `Release metadata`; `/assets/app.css.gz` status 200 and selected headers include `Content-Type`, `Content-Encoding: gzip`, and `Cache-Control`; missing static status 302, `Location: /`, and body contains `Redirect to the captive portal`; `/recovery` status 200 and body contains `AxeOS Recovery` and `Response:`; `/api/system/info` status 200, JSON-like body, and body contains current-device markers `205` and either `BM1366` or `Ultra`; unknown API status 404 and body contains `{"error":"unknown route"}`; WebSocket no-upgrade routes status 400 or 426 and log `websocket_no_upgrade_claim: route-coexistence-only`; empty `POST /api/system/OTA` status 400, 409, 413, 415, 422, or 500 with OTA validation text and log `ota_route_presence_claim: route-presence-only` plus `ota_non_claims: valid OTA upload, invalid image rejection, reboot, rollback, selected partition, boot validation not claimed`; empty `POST /api/system/OTAWWW` status 400 and body contains `Wrong API input`, with `otawww_rel03_status: deferred`. Write per-route `.headers.txt`, `.body.txt`, and `.curl-error.txt` files plus `http-static-api.log`, but log only selected headers and redacted body snippets. Create `scripts/phase17-live-http-api-smoke-test.sh` with fake manifest, fake flash JSON, fake curl, and assertions for all behavior bullets above. Register `phase17_live_http_api_smoke` as `sh_binary` and `phase17_live_http_api_smoke_test` as `sh_test` in `scripts/BUILD.bazel`; include `phase17-websocket-capture.mjs` as `data` for the test once Task 2 creates it.</action>
  <verify>
    <automated>bash -n scripts/phase17-live-http-api-smoke.sh && bash -n scripts/phase17-live-http-api-smoke-test.sh && bazel test //scripts:phase17_live_http_api_smoke_test</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg -n "phase17_live_http_api_smoke|origin-only|target-lock|identity_status|network_scan: disabled|POST /api/system/OTA|POST /api/system/OTAWWW|websocket_no_upgrade_claim|route-coexistence-only|ota_route_presence_claim|ota_non_claims|Wrong API input" scripts/phase17-live-http-api-smoke.sh` returns matches.</criterion>
    <criterion>`rg -n "test_missing_target_blocks_without_curl|test_userinfo_path_query_fragment_rejected|test_stale_flash_identity_blocks_without_curl|test_fake_success_records_required_phase17_routes|test_no_upgrade_does_not_claim_frames|test_redacts_response_secrets" scripts/phase17-live-http-api-smoke-test.sh` returns matches.</criterion>
    <criterion>`rg -n "phase17_live_http_api_smoke|phase17_live_http_api_smoke_test" scripts/BUILD.bazel` returns matches.</criterion>
    <criterion>`bazel test //scripts:phase17_live_http_api_smoke_test` passes.</criterion>
  </acceptance_criteria>
  <done>Phase 17 HTTP/static/API route evidence has a tested helper that enforces explicit target input, current flashed identity, route-specific pass criteria, and non-claim boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the bounded WebSocket frame capture helper</name>
  <requirements>API-09, EVD-05</requirements>
  <files>scripts/phase17-websocket-capture.mjs, scripts/phase17-live-http-api-smoke-test.sh, scripts/BUILD.bazel</files>
  <read_first>
    <file>scripts/phase17-websocket-capture.mjs</file>
    <file>scripts/phase17-live-http-api-smoke-test.sh</file>
    <file>scripts/BUILD.bazel</file>
    <file>scripts/phase15-websocket-capture.mjs</file>
    <file>scripts/phase14-live-telemetry.sh</file>
    <file>.planning/phases/17-live-http-api-and-static-evidence/17-CONTEXT.md</file>
    <file>.planning/phases/17-live-http-api-and-static-evidence/17-RESEARCH.md</file>
  </read_first>
  <behavior>
    - Test 1: Missing target writes blocked output when `--out` is supplied and never opens a socket.
    - Test 2: Non-origin URL inputs are rejected with `websocket_target_status=blocked`.
    - Test 3: Unsupported path such as `/api/other` is rejected; only `/api/ws` and `/api/ws/live` are allowed.
    - Test 4: Fake `/api/ws/live` frame writes redacted `websocket_frame_1=` and `websocket_frame_status=passed frames=1`.
    - Test 5: Fake `/api/ws` open with no frame writes `websocket_open_status=opened` and `websocket_frame_status=pending - open timeout without raw log frame`.
    - Test 6: Duration and max-frame bounds reject values over 30000 ms or 10 frames.
  </behavior>
  <action>Create `scripts/phase17-websocket-capture.mjs` using Node's built-in `globalThis.WebSocket`; do not add npm dependencies. Accepted inputs: `--device-url URL`, `--path /api/ws|/api/ws/live`, `--out PATH`, `--duration-ms N`, and `--max-frames N`. Default duration is `5000`, default max frames is `3`, maximum duration is `30000`, and maximum frames is `10`. Validate the target with the same origin-only rules as Task 1: protocol must be `http:` or `https:`, userinfo must be empty, search/hash must be empty, and path must be empty or `/`. Convert `http:` to `ws:` and `https:` to `wss:` after validation, then set pathname to the allowlisted WebSocket path. Output lines must include `phase17_websocket_capture`, `path=<path>`, `websocket_capture_url=ws://[redacted]<path>` or `wss://[redacted]<path>`, `network_scan=disabled - DEVICE_URL must be explicit`, `websocket_route_claim=frame-capture-required`, `duration_ms=<N>`, `max_frames=<N>`, `websocket_open_status=<opened|blocked|not-run>`, redacted `websocket_frame_N=<snippet>` lines when frames arrive, and one `websocket_frame_status=` line. For `/api/ws/live`, at least one frame is required for `websocket_frame_status=passed`; timeout without frames is `pending - no live frame before timeout`. For `/api/ws`, an open timeout without frames is `pending - open timeout without raw log frame`, not a failed route claim. Redact `DEVICE_URL`, private endpoints, IP addresses, MAC addresses, Wi-Fi credentials, pool credentials, worker secrets, API tokens, NVS secret values, `ssid`, `wifiPass`, `stratumUser`, `stratumPassword`, `poolUrl`, `fallbackPoolUrl`, `hostname`, `ip`, `gateway`, `netmask`, `dns`, `token`, `apiKey`, and `password`. Add deterministic test hooks only under environment variables named `PHASE17_FAKE_WEBSOCKET_MODE=open-frame|open-timeout|error` and `PHASE17_FAKE_WEBSOCKET_PAYLOAD=<json>` so `scripts/phase17-live-http-api-smoke-test.sh` can test the Node helper without network access. Extend the shell test from Task 1 to run `node --check scripts/phase17-websocket-capture.mjs` and fake-mode tests for the behavior bullets above. Ensure `scripts/BUILD.bazel` includes the `.mjs` file in the Phase 17 test data.</action>
  <verify>
    <automated>node --check scripts/phase17-websocket-capture.mjs && bash -n scripts/phase17-live-http-api-smoke-test.sh && bazel test //scripts:phase17_live_http_api_smoke_test</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg -n "allowedWebSocketPaths|/api/ws/live|/api/ws|PHASE17_FAKE_WEBSOCKET_MODE|websocket_frame_status=passed|open timeout without raw log frame|duration-ms|30000|max-frames|10" scripts/phase17-websocket-capture.mjs` returns matches.</criterion>
    <criterion>`rg -n "test_websocket_live_fake_frame_passes|test_websocket_raw_log_open_timeout_stays_pending|test_websocket_rejects_unsupported_path|test_websocket_rejects_non_origin_target" scripts/phase17-live-http-api-smoke-test.sh` returns matches.</criterion>
    <criterion>`node --check scripts/phase17-websocket-capture.mjs` passes.</criterion>
    <criterion>`bazel test //scripts:phase17_live_http_api_smoke_test` passes.</criterion>
  </acceptance_criteria>
  <done>Phase 17 has bounded real WebSocket frame capture separate from no-upgrade HTTP route coexistence checks.</done>
</task>

<task type="auto">
  <name>Task 3: Add the Phase 17 evidence and redaction scaffold</name>
  <requirements>REL-07, EVD-05</requirements>
  <files>docs/parity/evidence/phase-17-live-http-api-and-static-evidence/README.md, docs/parity/evidence/phase-17-live-http-api-and-static-evidence/redaction-review.md</files>
  <read_first>
    <file>docs/parity/evidence/phase-17-live-http-api-and-static-evidence/README.md</file>
    <file>docs/parity/evidence/phase-17-live-http-api-and-static-evidence/redaction-review.md</file>
    <file>docs/parity/evidence/phase-16-current-commit-release-evidence-completion/redaction-review.md</file>
    <file>docs/parity/evidence/phase-16-current-commit-release-evidence-completion.md</file>
    <file>.planning/phases/17-live-http-api-and-static-evidence/17-CONTEXT.md</file>
    <file>.planning/phases/17-live-http-api-and-static-evidence/17-VALIDATION.md</file>
  </read_first>
  <action>Create `docs/parity/evidence/phase-17-live-http-api-and-static-evidence/README.md` and `redaction-review.md`. `README.md` must include headings `Scope`, `Required Command Order`, `Target And Identity Gate`, `HTTP Static API Evidence`, `WebSocket Evidence`, `Redaction Rules`, `Checklist Promotion Rules`, `Explicit Non-Claims`, and `Blocked Evidence Rules`. The command order must name `just package`, `bazel run //tools/parity:report -- release-gate --manifest bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json`, `just detect-ultra205`, `just flash-monitor board=205 port=<port> manifest=bazel-bin/firmware/bitaxe/bitaxe-ultra205-package.json evidence-dir=docs/parity/evidence/phase-17-live-http-api-and-static-evidence/serial-boot capture-timeout-seconds=35`, `scripts/phase17-live-http-api-smoke.sh --device-url <origin> --manifest docs/parity/evidence/phase-17-live-http-api-and-static-evidence/package-release-gate/bitaxe-ultra205-package.json --flash-evidence-json docs/parity/evidence/phase-17-live-http-api-and-static-evidence/serial-boot/flash-command-evidence.json --out-dir docs/parity/evidence/phase-17-live-http-api-and-static-evidence/http-static-api`, both WebSocket capture commands for `/api/ws/live` and `/api/ws`, the redaction scan, `just parity`, and `just verify-reference`. `redaction-review.md` must start with `# Phase 17 Redaction Review`, include `redaction_status: pending`, and include artifact matrix rows for package manifest, package/release-gate logs, detector log, flash evidence JSON, serial monitor log, target lock, HTTP route log, HTTP headers, HTTP bodies, HTTP curl errors, WebSocket `/api/ws/live` output, WebSocket `/api/ws` output, summary ledger, release docs snippets, terminal snippets, and absent artifacts. Include checkboxes for private `DEVICE_URL`, private endpoints, IPs, MACs, Wi-Fi credentials, pool credentials, worker secrets, API tokens, NVS secret values, local terminal secrets, and `absent - not cited`. Do not use standalone body `---` separators.</action>
  <verify>
    <automated>test -f docs/parity/evidence/phase-17-live-http-api-and-static-evidence/README.md && test -f docs/parity/evidence/phase-17-live-http-api-and-static-evidence/redaction-review.md && rg -n "Required Command Order|Target And Identity Gate|HTTP Static API Evidence|WebSocket Evidence|Explicit Non-Claims|redaction_status: pending|target lock|HTTP headers|HTTP bodies|WebSocket /api/ws/live|absent - not cited|just verify-reference" docs/parity/evidence/phase-17-live-http-api-and-static-evidence/README.md docs/parity/evidence/phase-17-live-http-api-and-static-evidence/redaction-review.md</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`docs/parity/evidence/phase-17-live-http-api-and-static-evidence/README.md` contains every required heading and exact command surface named in the action.</criterion>
    <criterion>`docs/parity/evidence/phase-17-live-http-api-and-static-evidence/redaction-review.md` contains `redaction_status: pending` and an artifact matrix with all required artifact classes.</criterion>
    <criterion>`rg -n "^---$" docs/parity/evidence/phase-17-live-http-api-and-static-evidence/README.md docs/parity/evidence/phase-17-live-http-api-and-static-evidence/redaction-review.md` returns no body separator matches except YAML frontmatter if frontmatter is added later.</criterion>
  </acceptance_criteria>
  <done>Phase 17 evidence collection starts with an explicit artifact contract and redaction scaffold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| operator target input to helper | Explicit `DEVICE_URL` controls live HTTP/WebSocket requests. |
| package/flash identity to live evidence | Package manifest and flash JSON decide whether route probes can support just-flashed claims. |
| live device responses to committed evidence | Headers, bodies, curl errors, WebSocket frames, and terminal snippets may contain private data. |
| route evidence to parity claims | HTTP route presence, WebSocket no-upgrade responses, WebSocket frames, OTA validation, and OTAWWW gap behavior support different claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-17-target | Spoofing/Tampering | `scripts/phase17-live-http-api-smoke.sh`, `scripts/phase17-websocket-capture.mjs` | mitigate | Accept only explicit origin-only `DEVICE_URL` or explicit `--device-url`; reject userinfo/path/query/fragment; record `network_scan: disabled`; do not scan, parse mDNS, inspect ARP/router state, infer from serial logs, or guess. |
| T-17-identity | Tampering/Repudiation | Phase 17 HTTP helper identity preflight | mitigate | Require manifest source/reference commits, board `205`, trusted `flash-monitor` JSON, selected port when present, observed firmware/reference commit markers, and blocked evidence on missing/stale/mismatched package or flash identity. |
| T-17-redaction | Information Disclosure | Phase 17 evidence scaffold and helper snippets | mitigate | Store only sanitized snippets and allowlisted fields, redact raw target URLs, private endpoints, IPs, MACs, Wi-Fi/pool/API/NVS secrets, and mark absent artifacts `absent - not cited` before citation. |
| T-17-overclaim | Repudiation/Elevation of Privilege | Route helper, WebSocket helper, checklist promotion rules | mitigate | Log WebSocket no-upgrade as route-coexistence-only, require real bounded frames for WebSocket claims, mark empty OTA POST as route-presence-only, keep OTAWWW as fail-closed gap response, and list valid OTA/reboot/rollback/boot-validation/whole-www OTAWWW as non-claims. |
</threat_model>

<verification>
Plan-level checks:

- `bash -n scripts/phase17-live-http-api-smoke.sh`
- `node --check scripts/phase17-websocket-capture.mjs`
- `bazel test //scripts:phase17_live_http_api_smoke_test`
- `rg -n "redaction_status: pending|absent - not cited|Explicit Non-Claims" docs/parity/evidence/phase-17-live-http-api-and-static-evidence/README.md docs/parity/evidence/phase-17-live-http-api-and-static-evidence/redaction-review.md`
</verification>

<success_criteria>
Wave 0 is assigned and executable: the missing Phase 17 helper, test, Bazel, WebSocket capture, and redaction-review surfaces from `17-VALIDATION.md` exist, are tested, and encode every locked target, identity, redaction, and overclaim decision from D-01 through D-26 that applies before live capture.
</success_criteria>

<output>
After completion, create `.planning/phases/17-live-http-api-and-static-evidence/17-01-SUMMARY.md`.
</output>
