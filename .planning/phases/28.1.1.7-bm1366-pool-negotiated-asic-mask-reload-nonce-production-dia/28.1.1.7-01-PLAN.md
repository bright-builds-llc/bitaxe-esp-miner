---
phase: 28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase28.1.1.7-asic-mask-reload-compare.mjs
  - scripts/phase28.1.1.7-asic-mask-reload-compare-test.mjs
autonomous: true
requirements: [CFG-07, ASIC-11, STR-09]
user_setup: []
must_haves:
  truths:
    - "A repo-owned ASIC mask-reload comparator emits configure_observed, mask_stored_class, mask_applied_to_work, mask_reload_tx_observed, version_mask_tx_class (init vs post_configure_runtime), power_delta_class, result_correlated, and fake_pool_submit_observed without raw UART hex or credentials"
    - "When configure is observed, mask is stored, mask_applied_to_work is true, and mask_reload_tx_observed is false, forced_ab_label and preferred recommended_investigation are pool_negotiated_mask_asic_reload"
    - "recommended_investigation never emits post_max_baud_delay_2000, match_upstream_register_read_poll, upstream_like_long_block_receive, ticket_mask_asic_difficulty, count_asic_chips_rx_loop_parity, or negotiated_version_mask_work_field_parity"
  artifacts:
    - path: "scripts/phase28.1.1.7-asic-mask-reload-compare.mjs"
      provides: "Redaction-safe ASIC mask-reload category report extending 28.1.1.6 taxonomy"
      contains: "pool_negotiated_mask_asic_reload"
    - path: "scripts/phase28.1.1.7-asic-mask-reload-compare-test.mjs"
      provides: "Fixture tests for reload-vs-init, forced_ab_label, falsified-knob ban"
      contains: "forced_ab_label"
  key_links:
    - from: "scripts/phase28.1.1.7-asic-mask-reload-compare.mjs"
      to: "scripts/phase28.1.1.6-version-rolling-compare.mjs"
      via: "compose/extend 28.1.1.6 fields + 28.1.1.2 parsers"
      pattern: "phase28.1.1.6-version-rolling-compare|phase28.1.1.2-result-path-compare"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1.7-2026-07-09T14-38-00
generated_at: 2026-07-09T14:42:00Z
---

<objective>
Ship Wave 0 redaction-safe ASIC mask-reload comparator so configure/mask-store/mask-applied-to-work vs post-configure reload TX, version_mask_tx_class (init vs post_configure_runtime), and power_delta/correlate/submit are category-compared before any runtime SetVersionMask patch or hardware A/B.

Purpose: D-02/D-08/D-09 require golden category evidence that mask-on-MiningWork is retained but init-only SetVersionMask leaves `mask_reload_tx_observed: false`; RESEARCH forces first A/B label `pool_negotiated_mask_asic_reload` (D-04/D-12).
Output: `scripts/phase28.1.1.7-asic-mask-reload-compare.mjs` + test (compose/extend 28.1.1.6 — D-10). Compact firmware markers deferred to Plan 02 (D-11).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-CONTEXT.md
@.planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-RESEARCH.md
@.planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-VALIDATION.md
@scripts/phase28.1.1.6-version-rolling-compare.mjs
@scripts/phase28.1.1.2-result-path-compare.mjs

<interfaces>
<!-- Compose 28.1.1.6 / 28.1.1.2 — do not invent a parallel taxonomy (D-10). -->

From scripts/phase28.1.1.6-version-rolling-compare.mjs:
- Reuse configure/mask_stored/mask_applied_to_work/power_delta/correlate/submit taxonomy
- Extend with `mask_reload_tx_observed` + init vs `post_configure_runtime` class rules

From scripts/phase28.1.1.2-result-path-compare.mjs:
- `classifyFrame`, `lineEvents`, `summarizeLog`
- Events: `job_tx`, `result_correlated`, `fake_pool_submit`, `version_mask_tx` when present

Confirmed RESEARCH facts (encode in fixtures, not guess):
- Configure + mask store + mask_applied_to_work already match (28.1.1.6 retained)
- SetVersionMask only at init (prelude ×3 + init register `0x1fffe000`); no post-configure runtime reload
- Fake pool returns `version-rolling.mask=1fffe000` (equals init default) — gate on reload TX timing, not value delta (D-05)
- Init-only version_mask frames must NOT count as `mask_reload_tx_observed` (Pitfall 2)

D-08 required report fields (exact keys):
- `configure_observed: true|false`
- `mask_stored_class: stored|missing|unavailable`
- `mask_applied_to_work: true|false`
- `mask_reload_tx_observed: true|false`
- `mask_value_class: default_1fffe000|other_nonzero|zero|unavailable`
- `version_mask_tx_class: prelude_3|init_register|post_configure_runtime|none|unavailable`
- `job_version_field_class: base_notify|rolled_or_masked|unavailable`
- `power_delta_class: rising_hashing|flat|falling|unavailable`
- `result_correlated: true|false`
- `fake_pool_submit_observed: true|false`
- `wire_parity_mask_on_work_retained: true|false`
- `wire_parity_ticket_mask_retained: true|false`
- `wire_parity_rx_loop_retained: true|false`
- `forced_ab_label: pool_negotiated_mask_asic_reload|none`
- `recommended_investigation:` closed enum below
- `raw_bytes_committed: false`
- `credential_contents_read: false`
- `phase30_promotion_input: pending`

Closed `recommended_investigation` set for this phase (D-09):
`pool_negotiated_mask_asic_reload` | `remaining_nonce_production_blocker_narrowing` | `none`

HARD BAN (fail tests if emitted as recommended_investigation or forced_ab_label):
`post_max_baud_delay_2000`, `match_upstream_register_read_poll`, `upstream_like_long_block_receive`, `ticket_mask_asic_difficulty`, `count_asic_chips_rx_loop_parity`, `negotiated_version_mask_work_field_parity`

`forced_ab_label` default (RESEARCH Pattern 1–2 / D-04/D-12):
1. If blocked → `none`
2. Else if configure_observed + mask_stored + `mask_applied_to_work: true` + `mask_reload_tx_observed: false` + still no `result_correlated` → `pool_negotiated_mask_asic_reload`
3. Else → `none`
4. **Never** emit HARD BAN labels (including `negotiated_version_mask_work_field_parity`)

Recommender preference (D-09):
1. If blocked → `none`
2. Else if mask applied to work + reload TX not observed + no correlate → `pool_negotiated_mask_asic_reload`
3. Else if reload TX observed + still no correlate → `remaining_nonce_production_blocker_narrowing` (recommend only; Plan 03 names concrete evidence label)
4. Else → `none`

`mask_reload_tx_observed` rules (D-05/D-08/D-11; Pitfall 2):
- `true` only if `post_configure_runtime` marker (or equivalent) appears **after** configure/mask_stored evidence in the same capture
- Do **not** count prelude_3 / init_register frames as reload success
- Pre-patch Wave 0 baseline must show init-only classes without `post_configure_runtime`

power_delta_class rules (fast feedback only — not phase pass gate; D-05/D-07):
- `unavailable` if marker missing or `unavailable=true`
- `rising_hashing` if `delta_mw >= 2000`
- `falling` if `delta_mw <= -200`
- `flat` otherwise
- Do **not** require rising power from identical mask value
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ASIC mask-reload comparator CLI with D-08 fields</name>
  <files>scripts/phase28.1.1.7-asic-mask-reload-compare.mjs</files>
  <read_first>
    - scripts/phase28.1.1.6-version-rolling-compare.mjs
    - scripts/phase28.1.1.2-result-path-compare.mjs
    - .planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-CONTEXT.md (D-02, D-04, D-05, D-08, D-09, D-10)
    - .planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-RESEARCH.md (Patterns 1–2, Pitfalls 1/2/4, Already Matched table)
  </read_first>
  <behavior>
    - Missing upstream or rust log → `comparison_status: blocked_safe_prerequisite`, `recommended_investigation: none`, `forced_ab_label: none`
    - Fixture: configure_observed true, mask_stored, mask_applied_to_work true, mask_reload_tx_observed false, init-only version_mask_tx_class, power_delta falling, no correlate → `forced_ab_label: pool_negotiated_mask_asic_reload`, `recommended_investigation: pool_negotiated_mask_asic_reload`
    - Fixture: init-only SetVersionMask frames present → `mask_reload_tx_observed: false` (must not count prelude/init as reload)
    - Fixture: post_configure_runtime marker after configure → `mask_reload_tx_observed: true`, `version_mask_tx_class: post_configure_runtime`; if still no correlate → recommend `remaining_nonce_production_blocker_narrowing`, `forced_ab_label: none`
    - Never recommend falsified knobs including `negotiated_version_mask_work_field_parity`
    - Report has no multi-byte hex dumps or credential sentinels
  </behavior>
  <action>
    Create `scripts/phase28.1.1.7-asic-mask-reload-compare.mjs` (Node ESM, stdlib only) accepting `--upstream`, `--rust`, `--out` (per D-10 compose/extend 28.1.1.6 — import or reuse parsers from `scripts/phase28.1.1.6-version-rolling-compare.mjs` and/or `scripts/phase28.1.1.2-result-path-compare.mjs`; do not invent a parallel capture stack).

    Filename note: orchestrator outline uses `asic-mask-reload-compare`; VALIDATION draft may say `mask-reload-compare` — implement the `asic-mask-reload-compare` path and Plan 04 will align VALIDATION checkboxes.

    Classify configure/mask/work/reload/submit categories from markers and frame events only (no committed hex). Detect configure / mask-store / mask_applied_to_work / version_mask_tx via existing event taxonomy or compact markers — never require raw hex in fixtures (D-08).

    Derive `mask_reload_tx_observed` strictly from `post_configure_runtime` (or equivalent compact marker) appearing after configure evidence — never from prelude_3 / init_register alone (D-05/D-11; Pitfall 2).

    Recommender (D-09 / D-04):
    1. If blocked → `none`
    2. Else if mask applied + reload not observed + no correlate → `pool_negotiated_mask_asic_reload`
    3. Else if reload observed + still no correlate → `remaining_nonce_production_blocker_narrowing`
    4. Else → `none`
    5. **Never** emit HARD BAN strings (including `negotiated_version_mask_work_field_parity`)

    Emit `forced_ab_label` per RESEARCH Pattern 2 rules in `<interfaces>` (default `pool_negotiated_mask_asic_reload` for expected Ultra 205 pre-patch fixture).

    Emit Markdown with all D-08 keys listed in `<interfaces>`. Keep `phase30_promotion_input: pending` (D-15/D-18). Assert wire_parity retention flags from prior phase retention when markers present (status only — not recommenders).
  </action>
  <acceptance_criteria>
    - `test -f scripts/phase28.1.1.7-asic-mask-reload-compare.mjs`
    - `rg -n "pool_negotiated_mask_asic_reload" scripts/phase28.1.1.7-asic-mask-reload-compare.mjs`
    - `rg -n "forced_ab_label" scripts/phase28.1.1.7-asic-mask-reload-compare.mjs`
    - `rg -n "mask_reload_tx_observed" scripts/phase28.1.1.7-asic-mask-reload-compare.mjs`
    - `rg -n "post_configure_runtime" scripts/phase28.1.1.7-asic-mask-reload-compare.mjs`
    - `rg -n "negotiated_version_mask_work_field_parity" scripts/phase28.1.1.7-asic-mask-reload-compare.mjs` (ban list present)
    - `rg -n "phase28.1.1.6-version-rolling-compare|phase28.1.1.2-result-path-compare" scripts/phase28.1.1.7-asic-mask-reload-compare.mjs`
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/phase28.1.1.7-asic-mask-reload-compare.mjs && rg -n "forced_ab_label|pool_negotiated_mask_asic_reload|mask_reload_tx_observed|post_configure_runtime|negotiated_version_mask_work_field_parity" scripts/phase28.1.1.7-asic-mask-reload-compare.mjs</automated>
  </verify>
  <done>Comparator CLI exists, extends 28.1.1.6 taxonomy, emits D-08 category fields, closed recommender, and forced_ab_label. Behavioral recommender/ban fixtures are gated by Task 2 compare-test (Wave 0 behavioral gate).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 fixture tests locking recommender and bans</name>
  <files>scripts/phase28.1.1.7-asic-mask-reload-compare-test.mjs</files>
  <read_first>
    - scripts/phase28.1.1.6-version-rolling-compare-test.mjs
    - scripts/phase28.1.1.7-asic-mask-reload-compare.mjs
    - .planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-VALIDATION.md
  </read_first>
  <behavior>
    - Unit fixtures cover: mask_applied true + reload false + init-only TX → `forced_ab_label: pool_negotiated_mask_asic_reload`; init-only must not set mask_reload_tx_observed; post_configure_runtime after configure → observed true; blocked missing logs; `power_delta_class` falling/rising/unavailable
    - Assert report body never matches multi-byte hex dump patterns like `55 AA 52` / `55 AA 51` sequences as committed content, and never contains `poolPassword` / `poolURL` / credential JSON keys
    - Assert HARD BAN strings never appear as `recommended_investigation` or `forced_ab_label` values (including `negotiated_version_mask_work_field_parity` and `count_asic_chips_rx_loop_parity`)
    - No firmware marker required in this plan (Plan 02 owns D-11 markers)
  </behavior>
  <action>
    Create `scripts/phase28.1.1.7-asic-mask-reload-compare-test.mjs` using `node:assert` (mirror 28.1.1.6 test style). Include synthetic upstream/rust log fixtures (inline strings, not committed scratch hex dumps) that encode:
    1. Expected Ultra 205 diverge: configure_observed, mask_stored, mask_applied_to_work true, mask_reload_tx_observed false, init-only version_mask_tx, `delta_mw=-390` → falling, `forced_ab_label: pool_negotiated_mask_asic_reload`, recommend `pool_negotiated_mask_asic_reload`
    2. Init-only trap: prelude/init SetVersionMask markers present without post_configure_runtime → `mask_reload_tx_observed: false`
    3. Reload-observed path: post_configure_runtime after configure, still no correlate → recommend `remaining_nonce_production_blocker_narrowing`, `forced_ab_label: none`
    4. Missing log → blocked / `forced_ab_label: none`
    5. Falsified-knob ban: after running recommender on gap fixtures, assert recommendation and forced_ab_label ∉ HARD BAN set (including `negotiated_version_mask_work_field_parity` and `ticket_mask_asic_difficulty`)

    Do not patch production.rs / live_stratum_runtime in this plan (Plan 02 owns the patch). Do not edit firmware markers in Wave 0.
  </action>
  <acceptance_criteria>
    - `test -f scripts/phase28.1.1.7-asic-mask-reload-compare-test.mjs`
    - `node scripts/phase28.1.1.7-asic-mask-reload-compare-test.mjs` exits 0
    - `rg -n "pool_negotiated_mask_asic_reload" scripts/phase28.1.1.7-asic-mask-reload-compare-test.mjs`
    - `rg -n "post_max_baud_delay_2000|match_upstream_register_read_poll|upstream_like_long_block_receive|ticket_mask_asic_difficulty|count_asic_chips_rx_loop_parity|negotiated_version_mask_work_field_parity" scripts/phase28.1.1.7-asic-mask-reload-compare-test.mjs` (ban assertions present)
    - `rg -n "forced_ab_label" scripts/phase28.1.1.7-asic-mask-reload-compare-test.mjs`
    - `rg -n "mask_reload_tx_observed" scripts/phase28.1.1.7-asic-mask-reload-compare-test.mjs`
    - `rg -n "post_configure_runtime" scripts/phase28.1.1.7-asic-mask-reload-compare-test.mjs`
  </acceptance_criteria>
  <verify>
    <automated>node scripts/phase28.1.1.7-asic-mask-reload-compare-test.mjs</automated>
  </verify>
  <done>Wave 0 compare-test green; recommender, forced_ab_label, init-vs-reload distinction, and redaction bans locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| USB monitor logs → comparator → committed Markdown | Untrusted device logs may contain UART hex, SSIDs, IPs; committed reports must stay category/boolean only |
| Local credential files → flash/monitor (not this plan) | Credentials must never be read into comparator inputs or reports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28117-01 | I | asic-mask-reload-compare.mjs report | mitigate | Emit field categories/counts/booleans only; unit tests fail on hex dumps and credential key leakage (CFG-07) |
| T-28117-02 | I | synthetic fixtures in compare-test | mitigate | Inline category markers only; never commit raw UART hex dumps |
| T-28117-03 | E | phase30_promotion_input / checklist | mitigate | Hard-code `phase30_promotion_input: pending`; no checklist edits in this plan (D-15) |
| T-28117-04 | E | overclaim share outcomes / reload success | mitigate | Comparator records correlate/submit and reload-TX booleans only; does not claim STR-09 verified; init frames must not count as reload |
| T-28117-05 | T | reference/esp-miner | accept | This plan does not touch reference; later plans use scratch-only helpers + verify-reference |
</threat_model>

<verification>
- `node scripts/phase28.1.1.7-asic-mask-reload-compare-test.mjs` passes
- Sample CLI run on synthetic fixtures writes report with D-08 keys and `raw_bytes_committed: false`
</verification>

<success_criteria>
- Comparator detects expected Ultra 205 mask-applied-but-no-post-configure-reload diverge and sets `forced_ab_label: pool_negotiated_mask_asic_reload`
- Init-only SetVersionMask never sets `mask_reload_tx_observed: true`
- Falsified investigation knobs (including work-field-only) cannot appear as recommendations
- No raw UART hex or credentials in committed report output
</success_criteria>

<output>
After completion, create `.planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-01-SUMMARY.md`
</output>
