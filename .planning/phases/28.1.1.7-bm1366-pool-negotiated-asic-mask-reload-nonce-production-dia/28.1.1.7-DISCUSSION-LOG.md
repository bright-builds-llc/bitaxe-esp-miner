---
generated_by: gsd-discuss-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1.7-2026-07-09T14-38-00
generated_at: 2026-07-09T14:38:00Z
---

# Phase 28.1.1.7 Discussion Log

**Mode:** YOLO — recommended decisions auto-accepted (no interactive review).

## Gray areas considered

1. Is mask-on-MiningWork enough, or is ASIC UART reload required?
2. Can A/B succeed when fake-pool mask equals init default?
3. Forced first A/B label?
4. Fail-closed next hypothesis if falsified?

## Recommended answers (accepted)

1. **ASIC UART reload is required** — init-only `SetVersionMask`; negotiated mask never reloads post-configure.
2. **Yes structurally** — gate on post-configure reload TX markers, not mask-value delta; do not require power rise from identical mask.
3. **`pool_negotiated_mask_asic_reload`** as forced first A/B.
4. **Evidence-named narrower** if reload TX observed but hashing still idle; default placeholder `remaining_nonce_production_blocker_narrowing` only if evidence is silent — Plan 03 must pick a concrete label. Do not invent a second speculative patch in this phase.

## Carry-forward bans

- Job-byte field patches
- `post_max_baud_delay_2000`
- `match_upstream_register_read_poll`
- `upstream_like_long_block_receive`
- `ticket_mask_asic_difficulty` as sole blocker (ASIC-256 retained)
- `count_asic_chips_rx_loop_parity` as sole blocker (RX-loop retained)
- `negotiated_version_mask_work_field_parity` as sole blocker (mask-on-MiningWork retained)
- ReadChipId `0x0A` byte patch
- OR mask into UART job `version`
- Phase 30 checklist `verified` edits
- Second speculative patch in the same phase

## Hardware policy

Detector-gated Ultra 205; ≥360 s fake-pool preferred; wall clock ≥420 s; redaction-safe commits only.
