---
phase: 28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia
plan: 02
type: execute
wave: 2
depends_on:
  - 28.1.1.7-01
files_modified:
  - firmware/bitaxe/src/asic_adapter/production.rs
  - firmware/bitaxe/src/asic_adapter.rs
  - firmware/bitaxe/src/live_stratum_runtime.rs
  - crates/bitaxe-stratum/src/v1/live_runtime.rs
  - scripts/phase28.1.1.7-asic-mask-reload-diagnostic.sh
  - scripts/phase28.1.1.7-asic-mask-reload-diagnostic-test.sh
  - .planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-mask-reload-redacted.md
  - .planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-ab-pool_negotiated_mask_asic_reload-redacted.md
autonomous: true
requirements: [STR-09, ASIC-11, CFG-07]
user_setup: []
must_haves:
  truths:
    - "After configure stores negotiated mask and production_ready() is true, firmware emits one runtime Bm1366Command::SetVersionMask on the production UART with compact markers mask_reload_tx_observed=true and version_mask_tx_class=post_configure_runtime, without regressing init prelude×3 + init register"
    - "Diagnostic wrapper composes detect-ultra205, verify-reference, existing source-work helpers, comparator, and exactly one ≥360s fake-pool A/B for pool_negotiated_mask_asic_reload"
    - "Redacted A/B evidence records ab_outcome, mask_reload_tx_observed, version_mask_tx_class, power_delta_class, result_correlated, fake_pool_submit_observed, and wire_parity retention flags without hex or credentials; lever structural gate is reload TX after configure, not mask-value delta"
  artifacts:
    - path: "firmware/bitaxe/src/asic_adapter/production.rs"
      provides: "apply_negotiated_version_mask TX helper on retained production UART"
      contains: "apply_negotiated_version_mask"
    - path: "firmware/bitaxe/src/live_stratum_runtime.rs"
      provides: "Pending flush after configure + production_ready with post_configure_runtime markers"
      contains: "post_configure_runtime"
    - path: "scripts/phase28.1.1.7-asic-mask-reload-diagnostic.sh"
      provides: "Detector-gated diagnostic + forced mask-reload A/B wrapper"
      contains: "pool_negotiated_mask_asic_reload"
    - path: ".planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-ab-pool_negotiated_mask_asic_reload-redacted.md"
      provides: "Redacted ≥360s A/B outcome"
      contains: "ab_outcome:"
  key_links:
    - from: "firmware/bitaxe/src/live_stratum_runtime.rs"
      to: "firmware/bitaxe/src/asic_adapter/production.rs"
      via: "flush pending → apply_negotiated_version_mask when production_ready"
      pattern: "apply_negotiated_version_mask|production_ready|post_configure_runtime"
    - from: "scripts/phase28.1.1.7-asic-mask-reload-diagnostic.sh"
      to: "scripts/phase28.1.1.1-source-work-aligned-capture.sh"
      via: "compose fake-pool capture path"
      pattern: "source-work|duration-seconds|360"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28.1.1.7-2026-07-09T14-38-00
generated_at: 2026-07-09T14:42:00Z
---

<objective>
Apply the one confirmed divergence patch — post-configure runtime `SetVersionMask` with the negotiated mask after `production_ready()` (upstream flag semantics) via `apply_negotiated_version_mask` in production.rs flushed from live_stratum_runtime — emit compact reload markers distinguishing init vs `post_configure_runtime`, then run a detector-gated ≥360 s fake-pool hardware A/B labeled `pool_negotiated_mask_asic_reload` and record redacted evidence.

Purpose: RESEARCH verified configure+store+mask-on-MiningWork already land; remaining diverge is init-only SetVersionMask with no post-configure ASIC UART reload (D-04/D-12). Gate A/B on reload TX observed after configure, not mask-value delta (D-05). Retain mask-on-MiningWork + ASIC-256 + RX-loop (D-15). Never modify reference/esp-miner (D-17).
Output: production.rs helper + live_stratum flush + markers; optional pending bit in live_runtime; diagnostic wrapper; redacted baseline + A/B reports.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-CONTEXT.md
@.planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-RESEARCH.md
@firmware/bitaxe/src/asic_adapter/production.rs
@firmware/bitaxe/src/live_stratum_runtime.rs
@crates/bitaxe-stratum/src/v1/live_runtime.rs
@crates/bitaxe-asic/src/bm1366/command.rs
@crates/bitaxe-asic/src/bm1366/init_plan.rs
@scripts/phase28.1.1.7-asic-mask-reload-compare.mjs
@scripts/phase28.1.1.1-source-work-aligned-capture.sh
@scripts/phase28.1.1.6-version-rolling-diagnostic.sh
@scripts/phase27-live-hardware-bridge-package.sh

<interfaces>
Forced A/B label (RESEARCH PRIMARY / D-04/D-12): `pool_negotiated_mask_asic_reload`

HOW to reload (RESEARCH — do not invent value-delta gate):
1. On configure / set_version_mask store: raise once-shot pending reload (pure live_runtime bit and/or firmware shell pending) — upstream `new_stratum_version_rolling_msg`.
2. When `runtime.maybe_version_mask().is_some()` AND `asic_adapter::production_ready()` AND pending: call production helper once; clear pending — upstream create_jobs gate on `ASIC_initalized`.
3. Prefer flush in Phase 27 pump before `dispatch_production_work` / on first ready after configure.
4. TX encoding: reuse `Bm1366Command::SetVersionMask(mask).frame_bytes()` + `version_mask_payload` (already matched). Do not require mask ≠ `0x1fffe000`.
5. Markers on success (counts/classes only, no hex — D-11):
   `mask_reload_tx_observed=true version_mask_tx_class=post_configure_runtime redacted=true`
6. Do **not** regress init_plan prelude ×3 + init register SetVersionMask(0x1fffe000).
7. Do **not** OR mask into job version UART bytes; do not expand midstates; retain mask-on-MiningWork + ASIC-256 + RX-loop.

Confirmed patch targets (D-04 / RESEARCH HOW):
```rust
// production.rs — NEW beside probe_register_read_tx (~106-120):
pub fn apply_negotiated_version_mask(mask: VersionMask) -> bool {
    // SetVersionMask(mask).frame_bytes() → write_frame → wait_tx_done on retained UART
    // return bool; never block mining beyond logging on failure
}

// live_stratum_runtime.rs — flush when pending + production_ready():
// apply_negotiated_version_mask(mask); emit post_configure_runtime markers; clear pending

// live_runtime.rs — OPTIONAL: pending_reload bit on mask store (~426-428)
```

HARD BANS this plan:
- Do NOT gate TX on mask-value delta vs init default (D-05)
- Do NOT OR mask into job version UART bytes or expand midstates on wire (D-15)
- Do NOT reopen falsified levers; retain mask-on-MiningWork + RX-loop + ASIC-256 (D-15)
- Do NOT implement a second speculative patch (midstate/frequency/etc.) (D-06/D-15)
- Never modify `reference/esp-miner` (D-17)
- Do NOT edit Phase 30 checklist verified rows
- Do NOT count init prelude as reload success in evidence

Hardware (D-03 / D-16):
1. `just detect-ultra205` — exactly one port + board-info success (Ultra 205 detector-gated; hardware A/B unblocked per D-03)
2. `just verify-reference`
3. Prefer fake-pool via `phase28.1.1.1-source-work-aligned-capture.sh` composition
4. `--duration-seconds` / capture-timeout ≥ 360; agent wall clock ≥ 420
5. Never print/read wifi or pool credential file contents
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Runtime SetVersionMask helper + pending flush + markers</name>
  <files>firmware/bitaxe/src/asic_adapter/production.rs, firmware/bitaxe/src/asic_adapter.rs, firmware/bitaxe/src/live_stratum_runtime.rs, crates/bitaxe-stratum/src/v1/live_runtime.rs</files>
  <read_first>
    - firmware/bitaxe/src/asic_adapter/production.rs (probe_register_read_tx ~106-120; production_ready ~83-88)
    - firmware/bitaxe/src/live_stratum_runtime.rs (production_ready gates ~504, ~747, ~924; dispatch_production_work ~812+; mask_applied_to_work marker ~1201-1204)
    - crates/bitaxe-stratum/src/v1/live_runtime.rs (configure mask store ~426-428; maybe_version_mask)
    - crates/bitaxe-asic/src/bm1366/command.rs (SetVersionMask)
    - crates/bitaxe-asic/src/bm1366/init_plan.rs (prelude ×3 + init register — do not regress)
    - .planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-RESEARCH.md (HOW section, Pitfalls 1/3/5)
    - Wave 0 compare report / fixtures from Plan 01 if present (confirm forced_ab_label)
  </read_first>
  <behavior>
    - `apply_negotiated_version_mask` returns false when UART not retained / not production_ready; true after successful write_frame + wait_tx_done
    - Pending flush runs once after configure stores mask and production_ready becomes true; does not require mask ≠ 0x1fffe000
    - Success path logs compact `mask_reload_tx_observed=true version_mask_tx_class=post_configure_runtime redacted=true`
    - Init prelude ×3 + init register SetVersionMask path unchanged (no init_plan edits that drop baseline)
    - Existing mask_applied_to_work marker retained; job version stays base_notify; no job-byte OR
    - Host/unit tests cover pending clear + encode path where practical (firmware host tests or pure pending-flag tests)
  </behavior>
  <action>
    Per D-04 / RESEARCH primary lever `pool_negotiated_mask_asic_reload` (only this divergence family):

    1. Add `apply_negotiated_version_mask(mask: VersionMask) -> bool` in `production.rs` beside `probe_register_read_tx`: encode `Bm1366Command::SetVersionMask(mask).frame_bytes()`, write on retained production UART, `wait_tx_done`; return bool; never block mining beyond logging. Re-export from `asic_adapter.rs` as needed.

    2. Raise once-shot pending on mask store (optional pure bit in `live_runtime.rs` and/or firmware shell pending). Flush from `live_stratum_runtime.rs` when `maybe_version_mask().is_some()` AND `production_ready()` AND pending — prefer before `dispatch_production_work` / first ready after configure (upstream create_jobs semantics). Clear pending after attempt.

    3. On successful TX emit compact markers (D-11): `mask_reload_tx_observed=true version_mask_tx_class=post_configure_runtime redacted=true`. Keep init-path markers distinct (prelude_3 / init_register). Do not emit hex.

    4. Do **not** edit `init_plan.rs` to use negotiated mask (configure arrives after init). Do not OR into job version bytes. Do not reopen falsified levers. Retain mask-on-MiningWork + ASIC-256 + RX-loop.

    5. Run focused host/crate tests available for the touched pure surfaces (e.g. pending-flag / SetVersionMask encode). Firmware compile via existing package path when practical for the bridge build used by diagnostic.

    Do not implement a second speculative patch in this plan.
  </action>
  <acceptance_criteria>
    - `rg -n "fn apply_negotiated_version_mask" firmware/bitaxe/src/asic_adapter/production.rs`
    - `rg -n "apply_negotiated_version_mask" firmware/bitaxe/src/live_stratum_runtime.rs`
    - `rg -n "post_configure_runtime" firmware/bitaxe/src/live_stratum_runtime.rs`
    - `rg -n "mask_reload_tx_observed" firmware/bitaxe/src/live_stratum_runtime.rs`
    - `rg -n "SetVersionMask" firmware/bitaxe/src/asic_adapter/production.rs`
    - `git diff -- crates/bitaxe-asic/src/bm1366/init_plan.rs` empty OR only non-regressing comments (no prelude/init register removal)
    - `git diff -- crates/bitaxe-asic/src/bm1366/work.rs` empty (no job layout change)
    - `rg -n "mask_applied_to_work=true" firmware/bitaxe/src/live_stratum_runtime.rs` still present
  </acceptance_criteria>
  <verify>
    <automated>rg -n "fn apply_negotiated_version_mask" firmware/bitaxe/src/asic_adapter/production.rs && rg -n "post_configure_runtime|mask_reload_tx_observed|apply_negotiated_version_mask" firmware/bitaxe/src/live_stratum_runtime.rs && test -z "$(git diff -- crates/bitaxe-asic/src/bm1366/work.rs crates/bitaxe-asic/src/bm1366/init_plan.rs)"</automated>
  </verify>
  <done>Runtime SetVersionMask helper + pending flush + post_configure_runtime markers land; init baseline and mask-on-work retained; no job-byte patch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Diagnostic wrapper + ≥360s fake-pool A/B evidence</name>
  <files>scripts/phase28.1.1.7-asic-mask-reload-diagnostic.sh, scripts/phase28.1.1.7-asic-mask-reload-diagnostic-test.sh, .planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-mask-reload-redacted.md, .planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-ab-pool_negotiated_mask_asic_reload-redacted.md</files>
  <read_first>
    - scripts/phase28.1.1.6-version-rolling-diagnostic.sh
    - scripts/phase28.1.1.6-version-rolling-diagnostic-test.sh
    - scripts/phase28.1.1.1-source-work-aligned-capture.sh
    - scripts/phase28.1.1.7-asic-mask-reload-compare.mjs
    - AGENTS.md (detect-ultra205, capture-timeout ≥360, redaction, fake-pool preferred)
    - .planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-CONTEXT.md (D-12, D-13, D-15, D-16, D-17)
  </read_first>
  <behavior>
    - Dry-run: requires --port, --wifi-credentials, --duration-seconds≥360, --capture-dir; refuses duration &lt; 360; does not recommend falsified knobs; prints forced A/B label `pool_negotiated_mask_asic_reload`
    - Hardware path: detect-ultra205 → verify-reference → baseline compare (scratch or fresh) → package/flash reload candidate → ≥360s fake-pool capture → compare → write redacted A/B
    - If detect/credentials/capture blocked → write `ab_outcome: blocked_safe_prerequisite` precisely; do not invent frame bytes or implement a second patch
    - Lever structural success recorded via `mask_reload_tx_observed` / `post_configure_runtime` — not mask-value delta or required power rise (D-05)
  </behavior>
  <action>
    Create `scripts/phase28.1.1.7-asic-mask-reload-diagnostic.sh` composing existing helpers (D-10), modeled on `phase28.1.1.6-version-rolling-diagnostic.sh`:

    Required flags: `--port`, `--wifi-credentials`, `--duration-seconds` (≥360), `--capture-dir` (must be gitignored), optional `--dry-run`.

    Flow:
    1. Enforce duration ≥ 360
    2. Prefer composing `phase28.1.1.1-source-work-aligned-capture.sh` / phase27 bridge package for Rust flash+monitor (fake-pool preferred — already returns version-rolling.mask)
    3. Run `phase28.1.1.7-asic-mask-reload-compare.mjs` for baseline (pre-patch rust log OK for category baseline showing reload false)
    4. Forced single A/B label: `pool_negotiated_mask_asic_reload` — flash/run the runtime-reload candidate only (no second lever — D-12/D-15)
    5. Capture timeout ≥360 s; do not treat early exit as failure until timeout elapses unless hard flash error
    6. Write redacted Markdown under the phase dir:
       - `28.1.1.7-mask-reload-redacted.md` — baseline comparator fields (D-08 keys)
       - `28.1.1.7-ab-pool_negotiated_mask_asic_reload-redacted.md` — must include:
         - `ab_label: pool_negotiated_mask_asic_reload`
         - `ab_outcome: improved|unchanged|regressed|blocked_safe_prerequisite`
         - `configure_observed:` / `mask_stored_class:` / `mask_applied_to_work:`
         - `mask_reload_tx_observed:` / `version_mask_tx_class:` (expect `post_configure_runtime` after successful patch)
         - `job_version_field_class:` (expect `base_notify`)
         - `power_delta_class:`
         - `result_correlated:` / `fake_pool_submit_observed:`
         - `wire_parity_mask_on_work_retained: true|false`
         - `wire_parity_ticket_mask_retained: true|false`
         - `wire_parity_rx_loop_retained: true|false`
         - `capture_timeout_seconds:` (≥360)
         - `board: 205`
         - `raw_bytes_committed: false` / `credential_contents_read: false`
         - `phase30_promotion_input: pending`
         - `job_byte_patch_applied: false` and falsified-lever false flags including `negotiated_version_mask_work_field_parity_reopened_as_sole_blocker: false`
         - Note: if reload TX not observed after patch → treat as incomplete patch / blocked evidence, not hashing falsification (RESEARCH Pattern 4)

    `improved` definition for later Plan 03 (record honestly here): `result_correlated: true` and/or `fake_pool_submit_observed: true` (D-13). Optionally note `rising_hashing` as corroboration only. Note Pitfall 1: fake-pool mask equals init default — unchanged hashing after observed reload is a valid falsification path.

    Create `scripts/phase28.1.1.7-asic-mask-reload-diagnostic-test.sh` dry-run assertions (duration floor, forced label, no falsified knobs, gitignore capture-dir check).

    Before hardware: run `just detect-ultra205` and `just verify-reference`. If detection fails, stop with blocked evidence — do not guess.

    Never commit raw logs; keep under `scratch/` or ignored `hardware-runs/`.
  </action>
  <acceptance_criteria>
    - `test -f scripts/phase28.1.1.7-asic-mask-reload-diagnostic.sh`
    - `test -f scripts/phase28.1.1.7-asic-mask-reload-diagnostic-test.sh`
    - `bash scripts/phase28.1.1.7-asic-mask-reload-diagnostic-test.sh` exits 0
    - `rg -n "pool_negotiated_mask_asic_reload" scripts/phase28.1.1.7-asic-mask-reload-diagnostic.sh`
    - `rg -n "duration_seconds|360" scripts/phase28.1.1.7-asic-mask-reload-diagnostic.sh`
    - `rg -n "^ab_outcome:" .planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-ab-pool_negotiated_mask_asic_reload-redacted.md`
    - `rg -n "raw_bytes_committed: false" .planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-ab-pool_negotiated_mask_asic_reload-redacted.md`
    - `rg -n "mask_reload_tx_observed:" .planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-ab-pool_negotiated_mask_asic_reload-redacted.md`
    - `rg -n "wire_parity_rx_loop_retained:" .planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-ab-pool_negotiated_mask_asic_reload-redacted.md`
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase28.1.1.7-asic-mask-reload-diagnostic-test.sh</automated>
  </verify>
  <done>Diagnostic dry-run green; redacted ≥360s A/B (or precise blocked_safe_prerequisite) recorded for pool_negotiated_mask_asic_reload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wifi-credentials.json / pool-credentials → flash tools | Pass-by-path only; never read/print/commit contents |
| UART monitor → evidence Markdown | Hex and endpoints must stay in ignored scratch; committed docs use categories |
| reference/esp-miner → scratch clone | Upstream edits only in scratch; verify-reference must stay clean |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28117-06 | I | diagnostic.sh + A/B Markdown | mitigate | Category labels only; `credential_contents_read: false`; redaction sweep before commit (CFG-07) |
| T-28117-07 | I | uart_trace / SERIALTX captures | mitigate | Store under gitignored capture-dir/scratch; reports forbid hex dumps |
| T-28117-08 | T | reference/esp-miner | mitigate | `just verify-reference` before/after; D-17 scratch-only golden helpers |
| T-28117-09 | E | overclaim STR-09 verified / share outcomes / reload-as-hashing | mitigate | `phase30_promotion_input: pending`; Plan 03/04 own disposition; no checklist verified edits; record correlate/submit honestly; structural lever gate is reload TX not power rise |
| T-28117-10 | D | unsafe voltage/fan stress | accept | Out of scope; only standard bring-up + fake-pool mining path |
</threat_model>

<verification>
- `apply_negotiated_version_mask` + post_configure_runtime markers present in firmware sources
- `bash scripts/phase28.1.1.7-asic-mask-reload-diagnostic-test.sh` passes
- Redacted A/B file exists with `ab_outcome` and ≥360 capture timeout field
</verification>

<success_criteria>
- `pool_negotiated_mask_asic_reload` is the sole code divergence family introduced (runtime SetVersionMask after configure+ready)
- One detector-gated ≥360 s fake-pool A/B (or precise blocked record) exists
- No job-byte / falsified-lever / reference-tree mutations; mask-on-work + RX-loop + ASIC-256 retained
- Lever evidence keyed on `mask_reload_tx_observed` / `post_configure_runtime`, not mask-value delta
</success_criteria>

<output>
After completion, create `.planning/phases/28.1.1.7-bm1366-pool-negotiated-asic-mask-reload-nonce-production-dia/28.1.1.7-02-SUMMARY.md`
</output>
