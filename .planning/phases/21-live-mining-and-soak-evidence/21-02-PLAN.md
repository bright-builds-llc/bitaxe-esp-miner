---
phase: 21-live-mining-and-soak-evidence
plan: "02"
type: execute
wave: 1
depends_on:
  - "21-01"
files_modified:
  - firmware/bitaxe/src/main.rs
  - firmware/bitaxe/src/mining_evidence_mode.rs
  - firmware/bitaxe/src/runtime_snapshot.rs
  - scripts/phase21-live-mining-package.sh
  - scripts/phase21-live-mining-package-test.sh
  - scripts/BUILD.bazel
  - docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-enablement.md
  - docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-enablement/package/bitaxe-ultra205-package.json
autonomous: true
requirements:
  - ASIC-07
  - STR-06
  - STR-07
  - SAFE-09
  - EVD-05
must_haves:
  truths:
    - "Default firmware continues to publish blocked mining status unless a Phase 21 compile-time evidence mode and hardware acknowledgement are present."
    - "When readiness is `blocked_by_default`, executors have a controlled package path that can publish `work_submission=ready` only from the typed mining gate."
    - "Live smoke and soak plans can consume a redacted `live-mining-enablement.md` ledger with source/reference commits, package manifest, recovery steps, and safe-stop markers."
    - "If controlled enablement cannot be built or proven, later live smoke and soak plans block instead of running with disabled mining."
  artifacts:
    - path: "firmware/bitaxe/src/mining_evidence_mode.rs"
      provides: "Package-gated controlled live-mining readiness mode"
      contains: "BITAXE_MINING_EVIDENCE_MODE"
    - path: "scripts/phase21-live-mining-package.sh"
      provides: "Controlled live-mining package builder"
      contains: "BITAXE_HARDWARE_EVIDENCE_ACK=ultra205-live-mining-smoke-safe-bench"
    - path: "docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-enablement.md"
      provides: "Enablement evidence ledger consumed by live smoke and soak"
      contains: "controlled_live_mining_package_status:"
    - path: "docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-enablement/package/bitaxe-ultra205-package.json"
      provides: "Package manifest for the controlled live-mining evidence image"
      contains: "\"source_commit\""
  key_links:
    - from: "scripts/phase21-live-mining-package.sh"
      to: "firmware/bitaxe/src/mining_evidence_mode.rs"
      via: "Bazel action_env compile-time evidence mode"
      pattern: "BITAXE_MINING_EVIDENCE_MODE=live-mining-smoke"
    - from: "firmware/bitaxe/src/mining_evidence_mode.rs"
      to: "crates/bitaxe-stratum/src/v1/mining_loop.rs"
      via: "MiningLoopGate decision"
      pattern: "MiningLoopGate"
    - from: "docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-enablement.md"
      to: "scripts/phase21-live-mining-evidence.sh"
      via: "--enablement-summary prerequisite"
      pattern: "controlled_live_mining_package_status: ready"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 21-2026-07-04T01-35-47
generated_at: 2026-07-04T02:02:55Z
---

<objective>
Add the controlled live-mining firmware/package enablement path required when the Wave 0 readiness audit reports `firmware_live_mining_status: blocked_by_default`.

Purpose: STR-06 cannot be closed by an audit that only proves mining is blocked. This plan creates the explicit, package-scoped path later smoke and soak plans must consume, while preserving fail-closed default firmware behavior.
Output: Firmware evidence-mode module, package helper/test target, and a redacted live-mining enablement ledger.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-live-mining-and-soak-evidence/21-CONTEXT.md
@.planning/phases/21-live-mining-and-soak-evidence/21-RESEARCH.md
@.planning/phases/21-live-mining-and-soak-evidence/21-VALIDATION.md
@.planning/phases/21-live-mining-and-soak-evidence/21-01-SUMMARY.md
@AGENTS.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@firmware/bitaxe/src/main.rs
@firmware/bitaxe/src/runtime_snapshot.rs
@crates/bitaxe-stratum/src/v1/mining_loop.rs
@scripts/phase15-bm1366-diagnostic-package.sh
@scripts/BUILD.bazel
@docs/parity/evidence/phase-21-live-mining-and-soak-evidence/readiness-audit.md
@docs/parity/evidence/phase-21-live-mining-and-soak-evidence/evidence-contract.md
@docs/parity/evidence/phase-21-live-mining-and-soak-evidence/redaction-review.md

<interfaces>
Existing typed mining-loop gate:
```rust
pub struct MiningLoopGate {
    pub asic_initialized: bool,
    pub maybe_power_evidence: Option<PowerEvidenceToken>,
    pub maybe_thermal_evidence: Option<ThermalEvidenceToken>,
    pub maybe_safety_evidence: Option<SafetyCriticalEvidence>,
    pub safety_status: SafetyStatus,
    pub hardware_evidence_ack: bool,
}

impl MiningLoopGate {
    pub fn decision(self) -> MiningLoopDecision;
    pub fn apply_to_state(self, state: &mut MiningRuntimeState);
}
```

New package command shape:
```bash
scripts/phase21-live-mining-package.sh --out-dir docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-enablement --readiness-audit docs/parity/evidence/phase-21-live-mining-and-soak-evidence/readiness-audit.md
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fail-closed controlled live-mining evidence mode</name>
  <files>firmware/bitaxe/src/main.rs, firmware/bitaxe/src/mining_evidence_mode.rs, firmware/bitaxe/src/runtime_snapshot.rs</files>
  <read_first>
    - firmware/bitaxe/src/main.rs
    - firmware/bitaxe/src/runtime_snapshot.rs
    - crates/bitaxe-stratum/src/v1/mining_loop.rs
    - crates/bitaxe-stratum/src/v1/state.rs
    - crates/bitaxe-safety/src/evidence.rs
    - crates/bitaxe-safety/src/power.rs
    - crates/bitaxe-safety/src/thermal.rs
    - .planning/phases/21-live-mining-and-soak-evidence/21-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: With no compile-time evidence env, firmware keeps logging `mining_loop_status=blocked reason=hardware_evidence_ack_missing work_submission=disabled`.
    - Test 2: With `BITAXE_MINING_EVIDENCE_MODE=live-mining-smoke` and `BITAXE_HARDWARE_EVIDENCE_ACK=ultra205-live-mining-smoke-safe-bench`, the evidence mode builds a `MiningLoopGate`, applies it to `MiningRuntimeState`, and publishes `mining_loop_status=ready reason=phase21_controlled_evidence work_submission=ready` only when the gate decision is ready.
    - Test 3: With evidence mode set but the acknowledgement missing or wrong, the firmware publishes `mining_loop_status=blocked reason=hardware_evidence_ack_missing work_submission=disabled`.
  </behavior>
  <action>Create `firmware/bitaxe/src/mining_evidence_mode.rs` as the firmware shell for Phase 21 controlled live-mining readiness. It must parse `option_env!("BITAXE_MINING_EVIDENCE_MODE")` and `option_env!("BITAXE_HARDWARE_EVIDENCE_ACK")`; default to blocked when either is absent, mismatched, or unsupported. For the single accepted mode `live-mining-smoke` plus ack `ultra205-live-mining-smoke-safe-bench`, build the existing `MiningLoopGate` using evidence labels that cite Phase 20/21 hardware evidence and then call `apply_to_state` on a `MiningRuntimeState`. Add a small `runtime_snapshot::replace_mining_runtime_state_for_evidence(state)` helper so API/WebSocket snapshots can expose the controlled readiness state without changing command-route semantics. Wire `main.rs` to call the new module after `startup_diagnostics?`; keep the old blocked call as the default branch. Do not read pool credentials, `DEVICE_URL`, Wi-Fi credentials, NVS secrets, or network state in firmware. Do not add raw pool sockets, raw BM1366 writes, voltage/fan controls, erase, rollback, interrupted-update, or unbounded loops in this task.</action>
  <verify>
    <automated>cargo test -p bitaxe-stratum --all-features mining_loop</automated>
    <automated>cargo build --package bitaxe-firmware --target xtensa-esp32s3-espidf</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "BITAXE_MINING_EVIDENCE_MODE|ultra205-live-mining-smoke-safe-bench|MiningLoopGate|mining_loop_status=ready|work_submission=ready|replace_mining_runtime_state_for_evidence" firmware/bitaxe/src/main.rs firmware/bitaxe/src/mining_evidence_mode.rs firmware/bitaxe/src/runtime_snapshot.rs` returns matches.
    - Default branch still contains `hardware_evidence_ack_missing` and `work_submission=disabled`.
    - `cargo test -p bitaxe-stratum --all-features mining_loop` exits 0.
    - Firmware build exits 0 for the package target or records the exact toolchain blocker without changing default fail-closed behavior.
  </acceptance_criteria>
  <done>Firmware has a controlled live-mining readiness path that remains unreachable by default and can only be activated by the Phase 21 package helper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add controlled live-mining package helper and enablement ledger</name>
  <files>scripts/phase21-live-mining-package.sh, scripts/phase21-live-mining-package-test.sh, scripts/BUILD.bazel, docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-enablement.md, docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-enablement/package/bitaxe-ultra205-package.json</files>
  <read_first>
    - scripts/phase15-bm1366-diagnostic-package.sh
    - scripts/package-firmware.sh
    - scripts/BUILD.bazel
    - docs/parity/evidence/phase-21-live-mining-and-soak-evidence/readiness-audit.md
    - docs/parity/evidence/phase-21-live-mining-and-soak-evidence/evidence-contract.md
    - docs/parity/evidence/phase-21-live-mining-and-soak-evidence/redaction-review.md
    - AGENTS.md
  </read_first>
  <behavior>
    - Test 1: Missing readiness audit writes `controlled_live_mining_package_status: blocked`, `blocker: missing readiness audit`, and does not invoke Bazel.
    - Test 2: Readiness audit without `firmware_live_mining_status: blocked_by_default` writes `controlled_live_mining_package_status: blocked`, `blocker: unexpected readiness status`, and does not invoke Bazel.
    - Test 3: Valid readiness audit invokes Bazel with `--action_env=BITAXE_MINING_EVIDENCE_MODE=live-mining-smoke` and `--action_env=BITAXE_HARDWARE_EVIDENCE_ACK=ultra205-live-mining-smoke-safe-bench`.
    - Test 4: The helper writes `live-mining-enablement.md` without raw pool, Wi-Fi, private target, token, IP, or MAC values.
  </behavior>
  <action>Create `scripts/phase21-live-mining-package.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. It must accept `--out-dir` and `--readiness-audit` only. It must block before Bazel when the readiness audit is missing or does not contain `firmware_live_mining_status: blocked_by_default` and `controlled_enablement_required: true`. On the valid path, run `bazel build --action_env=BITAXE_MINING_EVIDENCE_MODE=live-mining-smoke --action_env=BITAXE_HARDWARE_EVIDENCE_ACK=ultra205-live-mining-smoke-safe-bench //firmware/bitaxe:firmware`, package the resulting ELF through `scripts/package-firmware.sh`, and write `live-mining-enablement.md` with exact fields `controlled_live_mining_package_status: ready` or `controlled_live_mining_package_status: blocked`, `readiness_status: blocked_by_default`, `enablement_mode: live-mining-smoke`, `hardware_evidence_ack: ultra205-live-mining-smoke-safe-bench`, `package_manifest: docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-enablement/package/bitaxe-ultra205-package.json`, `source_commit:`, `reference_commit:`, `recovery_steps: stop wrapper, power-cycle board if serial stalls, reflash default safe package if live mode does not safe-stop`, `prerequisite_artifacts: readiness-audit.md`, `evidence_dir: docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-enablement`, `redaction_reviewer: required-before-citation`, and `post_action_safe_state_marker: safe_state: mining=disabled`, `hardware_control=disabled`, `work_submission=disabled`. Add `scripts/phase21-live-mining-package-test.sh` and register `sh_binary(name = "phase21_live_mining_package")` plus `sh_test(name = "phase21_live_mining_package_test")` in `scripts/BUILD.bazel`. Update `redaction-review.md` to include a `live-mining-enablement` row with `pending` status until the scan is reviewed. This implements D-02, D-04, D-05, D-06, D-07, D-08, D-16, D-20, and D-23 for the enablement prerequisite.</action>
  <verify>
    <automated>bash -n scripts/phase21-live-mining-package.sh scripts/phase21-live-mining-package-test.sh</automated>
    <automated>bazel test //scripts:phase21_live_mining_package_test</automated>
    <automated>rg -n "controlled_live_mining_package_status: (ready|blocked)|BITAXE_MINING_EVIDENCE_MODE=live-mining-smoke|ultra205-live-mining-smoke-safe-bench|redaction_reviewer: required-before-citation|live-mining-enablement" scripts/phase21-live-mining-package.sh scripts/phase21-live-mining-package-test.sh docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-enablement.md docs/parity/evidence/phase-21-live-mining-and-soak-evidence/redaction-review.md</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase21-live-mining-package.sh` and its test are executable.
    - `scripts/BUILD.bazel` contains `phase21_live_mining_package` and `phase21_live_mining_package_test`.
    - `live-mining-enablement.md` contains `controlled_live_mining_package_status: ready` only after the package manifest exists and includes `"source_commit"` plus `"reference_commit"`.
    - If package creation is blocked, `live-mining-enablement.md` contains `controlled_live_mining_package_status: blocked` and later plans must not run live smoke or soak.
    - Redaction review has a `live-mining-enablement` row and no raw credential or private endpoint is committed.
  </acceptance_criteria>
  <done>A Phase 21 controlled live-mining package exists for downstream smoke/soak plans, or a blocked enablement ledger prevents later live execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| compile-time env to firmware | Evidence-mode env vars alter firmware mining readiness and must stay package-scoped. |
| package helper to Bazel | Helper passes action env into firmware build and produces package artifacts. |
| firmware runtime state to HTTP/WebSocket | Controlled readiness state becomes API-visible telemetry. |
| enablement ledger to later plans | Live smoke and soak depend on the ledger before hardware/network effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-21-02-01 | Elevation of Privilege / Tampering | `firmware/bitaxe/src/mining_evidence_mode.rs` | mitigate | Default remains fail-closed; only `BITAXE_MINING_EVIDENCE_MODE=live-mining-smoke` plus `BITAXE_HARDWARE_EVIDENCE_ACK=ultra205-live-mining-smoke-safe-bench` can publish ready status. |
| T-21-02-02 | Information Disclosure | package helper and enablement ledger | mitigate | Do not read pool, Wi-Fi, target, token, or NVS secrets; committed ledger contains only category labels, package paths, commits, recovery steps, and safe-state markers. |
| T-21-02-03 | Denial of Service | controlled live-mining readiness | mitigate | No raw pool sockets, raw BM1366 writes, voltage/fan controls, erase, rollback, interrupted-update, or unbounded loops are added; later plans own bounded runtime effects. |
| T-21-02-04 | Repudiation | enablement evidence | mitigate | Ledger records readiness source, package manifest, source/reference commits, prerequisite artifacts, recovery steps, and redaction reviewer status. |
</threat_model>

<verification>
Run the task-level checks plus:

```bash
cargo test -p bitaxe-stratum --all-features mining_loop
cargo test -p bitaxe-api --all-features mining
bazel test //scripts:phase21_live_mining_package_test
just parity
just verify-reference
git diff -- reference/esp-miner --exit-code
```
</verification>

<success_criteria>
- Default firmware mining readiness remains blocked without Phase 21 compile-time evidence mode.
- Controlled live-mining package helper can create or block the package path deterministically from `readiness-audit.md`.
- Later live smoke and bounded soak plans have a required `--enablement-summary` artifact and cannot run on the blocked-by-default firmware path.
- No secret-bearing pool, target, Wi-Fi, token, NVS, IP, or MAC value is introduced into committed enablement evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/21-live-mining-and-soak-evidence/21-02-SUMMARY.md`.
</output>
