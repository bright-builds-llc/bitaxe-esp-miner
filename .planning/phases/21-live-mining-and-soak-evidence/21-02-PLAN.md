---
phase: 21-live-mining-and-soak-evidence
plan: "02"
type: execute
wave: 1
depends_on:
  - "21-01"
files_modified:
  - crates/bitaxe-stratum/src/v1/controlled_runtime.rs
  - crates/bitaxe-stratum/src/v1/mining_loop.rs
  - crates/bitaxe-stratum/src/v1.rs
  - crates/bitaxe-stratum/BUILD.bazel
  - firmware/bitaxe/src/main.rs
  - firmware/bitaxe/src/mining_evidence_mode.rs
  - firmware/bitaxe/src/controlled_mining_runtime.rs
  - firmware/bitaxe/src/runtime_snapshot.rs
  - firmware/bitaxe/src/http_api.rs
  - scripts/phase21-live-mining-package.sh
  - scripts/phase21-live-mining-package-test.sh
  - scripts/BUILD.bazel
  - docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-enablement.md
  - docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-enablement/package/bitaxe-ultra205-package.json
autonomous: true
requirements:
  - ASIC-07
  - STR-06
  - STR-07
  - SAFE-09
  - EVD-05
must_haves:
  truths:
    - "Default firmware continues to publish blocked mining status unless Phase 21 compile-time evidence mode, hardware acknowledgement, board `205`, and safety/mining gates are present."
    - "A deterministic controlled runtime/harness wires settings-derived pool config into Stratum subscribe, authorize, notify, guarded work queue, typed BM1366 dispatch, result parsing, share submission outcome, runtime state, and API/WebSocket telemetry updates."
    - "Live smoke and soak plans can consume a redacted `live-mining-enablement.md` ledger proving the controlled runtime/harness exists and listing runtime log markers that must appear during controlled package boot."
    - "If the controlled runtime/harness cannot be built, tested, packaged, or observed, later live smoke and soak plans block instead of running with readiness-only firmware."
  artifacts:
    - path: "crates/bitaxe-stratum/src/v1/controlled_runtime.rs"
      provides: "Pure controlled mining runtime/harness contract"
      contains: "ControlledMiningRuntimePlan"
    - path: "firmware/bitaxe/src/controlled_mining_runtime.rs"
      provides: "Firmware-side bounded controlled mining harness"
      contains: "phase21_controlled_runtime_status"
    - path: "firmware/bitaxe/src/mining_evidence_mode.rs"
      provides: "Package-gated Phase 21 evidence mode"
      contains: "BITAXE_MINING_EVIDENCE_MODE"
    - path: "scripts/phase21-live-mining-package.sh"
      provides: "Controlled live-mining runtime package builder"
      contains: "BITAXE_HARDWARE_EVIDENCE_ACK=ultra205-live-mining-runtime-safe-bench"
    - path: "docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-enablement.md"
      provides: "Runtime enablement evidence ledger consumed by live smoke and soak"
      contains: "controlled_runtime_harness_status:"
  key_links:
    - from: "crates/bitaxe-stratum/src/v1/controlled_runtime.rs"
      to: "crates/bitaxe-stratum/src/v1/mining_loop.rs"
      via: "Controlled runtime uses GuardedMiningLoopInputs and typed BM1366 command emission"
      pattern: "GuardedMiningLoopInputs"
    - from: "firmware/bitaxe/src/http_api.rs"
      to: "firmware/bitaxe/src/controlled_mining_runtime.rs"
      via: "PATCH /api/system refreshes the settings-derived controlled runtime when Phase 21 mode is active"
      pattern: "maybe_refresh_from_settings"
    - from: "firmware/bitaxe/src/controlled_mining_runtime.rs"
      to: "firmware/bitaxe/src/runtime_snapshot.rs"
      via: "runtime state publish path used by API and WebSocket telemetry"
      pattern: "replace_mining_runtime_state_for_evidence"
    - from: "docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-enablement.md"
      to: "docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-smoke/controlled-package-boot/flash-monitor.log"
      via: "21-06 must observe the runtime markers from the controlled package before live smoke can pass"
      pattern: "phase21_controlled_runtime_status: ready"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 21-2026-07-04T01-35-47
generated_at: 2026-07-04T02:02:55Z
---

<objective>
Add the bounded controlled mining runtime/harness required before live smoke or soak evidence can pass.

Purpose: STR-06 cannot be closed by a readiness bit or package flag. The firmware must have a real bounded runtime path that consumes settings-derived pool config, exercises Stratum lifecycle, queues BM1366 work, dispatches through typed ASIC commands, parses results, records share outcome, and publishes runtime/API/WebSocket telemetry.
Output: Pure runtime contract, firmware-side controlled harness, package helper/test target, and a redacted enablement ledger that downstream smoke/soak plans must observe.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-live-mining-and-soak-evidence/21-CONTEXT.md
@.planning/phases/21-live-mining-and-soak-evidence/21-RESEARCH.md
@.planning/phases/21-live-mining-and-soak-evidence/21-VALIDATION.md
@.planning/phases/21-live-mining-and-soak-evidence/21-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@crates/bitaxe-stratum/src/v1/mining_loop.rs
@crates/bitaxe-stratum/src/v1/messages.rs
@crates/bitaxe-stratum/src/v1/mining.rs
@crates/bitaxe-stratum/src/v1/queue.rs
@crates/bitaxe-stratum/src/v1/state.rs
@crates/bitaxe-stratum/src/v1/fake_pool.rs
@crates/bitaxe-asic/src/bm1366/command.rs
@crates/bitaxe-asic/src/bm1366/result.rs
@crates/bitaxe-asic/src/bm1366/work.rs
@firmware/bitaxe/src/main.rs
@firmware/bitaxe/src/http_api.rs
@firmware/bitaxe/src/runtime_snapshot.rs
@firmware/bitaxe/src/settings_adapter.rs
@firmware/bitaxe/src/asic_adapter.rs
@scripts/phase15-bm1366-diagnostic-package.sh
@scripts/BUILD.bazel
@docs/parity/evidence/phase-21-live-mining-and-soak-evidence/readiness-audit.md
@docs/parity/evidence/phase-21-live-mining-and-soak-evidence/evidence-contract.md
@docs/parity/evidence/phase-21-live-mining-and-soak-evidence/redaction-review.md

<interfaces>
Existing typed mining-loop gate and queue contract:
```rust
pub struct MiningLoopGate {
    pub asic_initialized: bool,
    pub maybe_power_evidence: Option<PowerEvidenceToken>,
    pub maybe_thermal_evidence: Option<ThermalEvidenceToken>,
    pub maybe_safety_evidence: Option<SafetyCriticalEvidence>,
    pub safety_status: SafetyStatus,
    pub hardware_evidence_ack: bool,
}

pub struct GuardedMiningLoopInputs {
    pub gate: MiningLoopGate,
    pub pool_defaults: Ultra205Defaults,
    pub source: GuardedMiningLoopSource,
    pub work_queue: MiningWorkQueue,
    pub runtime_state: MiningRuntimeState,
    pub maybe_nonce_result: Option<Bm1366NonceResult>,
}

pub struct GuardedBm1366DispatchPlan {
    pub fields: Bm1366WorkFields,
    pub maybe_command: Option<Bm1366Command>,
}
```

Existing API-visible runtime state boundary:
```rust
pub fn collect_api_snapshot() -> ApiSnapshot;
pub fn mining_runtime_state() -> MiningRuntimeState;
```

New controlled package command shape:
```bash
scripts/phase21-live-mining-package.sh --out-dir docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-enablement --readiness-audit docs/parity/evidence/phase-21-live-mining-and-soak-evidence/readiness-audit.md
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the pure controlled mining runtime contract</name>
  <files>crates/bitaxe-stratum/src/v1/controlled_runtime.rs, crates/bitaxe-stratum/src/v1/mining_loop.rs, crates/bitaxe-stratum/src/v1.rs, crates/bitaxe-stratum/BUILD.bazel</files>
  <read_first>
    - crates/bitaxe-stratum/src/v1/mining_loop.rs
    - crates/bitaxe-stratum/src/v1/messages.rs
    - crates/bitaxe-stratum/src/v1/mining.rs
    - crates/bitaxe-stratum/src/v1/queue.rs
    - crates/bitaxe-stratum/src/v1/state.rs
    - crates/bitaxe-stratum/src/v1/fake_pool.rs
    - crates/bitaxe-asic/src/bm1366/command.rs
    - crates/bitaxe-asic/src/bm1366/result.rs
    - crates/bitaxe-asic/src/bm1366/work.rs
    - standards/core/testing.md
    - standards/languages/rust.md
  </read_first>
  <behavior>
    - Test 1: A blocked `MiningLoopGate` produces `ControlledMiningRuntimeStatus::Blocked`, no Stratum client messages, no queue mutation, no BM1366 dispatch command, no share submission, and `MiningRuntimeState` remains safe-blocked.
    - Test 2: Settings-derived pool identity produces `mining.subscribe` and `mining.authorize` client messages, but `ControlledMiningRuntimeEvidence::redacted_summary()` never contains raw pool URL, username, password, worker, `DEVICE_URL`, IP, MAC, token, Wi-Fi, or NVS secret values.
    - Test 3: A deterministic subscribe/authorize/notify transcript enqueues one `MiningWork`, records lifecycle `Subscribed -> Authorized -> Active`, and `GuardedMiningLoopInputs::plan()` emits `Some(Bm1366Command::SendDiagnosticWork(Bm1366WorkPayload::new(job_id, fields)))` instead of a placeholder `None`.
    - Test 4: A typed BM1366 nonce result for the active job maps to `ShareSubmission`, emits a `mining.submit` client message, and accepted/rejected pool responses update `MiningRuntimeState` share counters and rejection reasons.
    - Test 5: The controlled runtime marks `safe_stop_required=true` and reports watchdog-yield checkpoints for each bounded step.
  </behavior>
  <action>Create `crates/bitaxe-stratum/src/v1/controlled_runtime.rs` as the functional core for Phase 21. Define typed inputs such as `ControlledPoolConfig`, `ControlledMiningRuntimeInput`, `ControlledMiningRuntimePlan`, `ControlledMiningRuntimeEvidence`, `ControlledMiningRuntimeStatus`, and `ControlledShareOutcome`; parse raw settings into those types at the boundary and keep secret-bearing fields out of `Debug`, `Display`, and evidence summary output. Use existing `StratumV1ClientMessage`, `StratumV1ServerMessage`, `MiningWorkBuilder`, `MiningWorkQueue`, `GuardedMiningLoopInputs`, `parse_bm1366_result_frame`, and `ShareSubmission` instead of shell/string logic. Update `GuardedMiningLoopInputs::plan()` so queued work produces a typed `Bm1366Command::SendDiagnosticWork(Bm1366WorkPayload::new(work.asic_job_id, work.fields))`; this is the ASIC dispatch contract firmware adapters consume. Add `pub mod controlled_runtime;` to `crates/bitaxe-stratum/src/v1.rs` and add the new source file to `crates/bitaxe-stratum/BUILD.bazel`. Do not add pool sockets, firmware I/O, raw BM1366 writes, voltage/fan controls, long-running loops, or secret logging to the pure crate.</action>
  <verify>
    <automated>sh -c 'cargo test -p bitaxe-stratum --all-features controlled_runtime && cargo test -p bitaxe-stratum --all-features mining_loop'</automated>
    <automated>bazel test //crates/bitaxe-stratum:tests --test_filter=controlled_runtime</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub mod controlled_runtime|ControlledMiningRuntimePlan|ControlledPoolConfig|ControlledShareOutcome|safe_stop_required|redacted_summary|Bm1366Command::SendDiagnosticWork|Bm1366WorkPayload::new" crates/bitaxe-stratum/src/v1.rs crates/bitaxe-stratum/src/v1/controlled_runtime.rs crates/bitaxe-stratum/src/v1/mining_loop.rs crates/bitaxe-stratum/BUILD.bazel` returns matches for the new module, runtime types, safe-stop marker, redaction method, and typed BM1366 command emission.
    - `cargo test -p bitaxe-stratum --all-features controlled_runtime` and `cargo test -p bitaxe-stratum --all-features mining_loop` exit 0.
    - `bazel test //crates/bitaxe-stratum:tests --test_filter=controlled_runtime` exits 0.
    - `rg -n "println!|eprintln!|raw pool|raw-bm1366|voltage-control|fan-control|erase-flash|rollback|interrupted-update|unbounded" crates/bitaxe-stratum/src/v1/controlled_runtime.rs` returns no matches.
  </acceptance_criteria>
  <done>The pure runtime contract proves settings-derived Stratum lifecycle, guarded work queue, typed BM1366 dispatch, result-to-share mapping, runtime state updates, safe-stop markers, and secret-redacted evidence without firmware effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the firmware-side bounded runtime harness and telemetry update path</name>
  <files>firmware/bitaxe/src/main.rs, firmware/bitaxe/src/mining_evidence_mode.rs, firmware/bitaxe/src/controlled_mining_runtime.rs, firmware/bitaxe/src/runtime_snapshot.rs, firmware/bitaxe/src/http_api.rs</files>
  <read_first>
    - firmware/bitaxe/src/main.rs
    - firmware/bitaxe/src/http_api.rs
    - firmware/bitaxe/src/runtime_snapshot.rs
    - firmware/bitaxe/src/settings_adapter.rs
    - firmware/bitaxe/src/asic_adapter.rs
    - firmware/bitaxe/src/asic_adapter/status.rs
    - crates/bitaxe-stratum/src/v1/controlled_runtime.rs
    - crates/bitaxe-stratum/src/v1/mining_loop.rs
    - crates/bitaxe-api/src/mining.rs
    - crates/bitaxe-api/src/telemetry.rs
    - .planning/phases/21-live-mining-and-soak-evidence/21-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: With no compile-time evidence env, firmware keeps logging `mining_loop_status=blocked reason=hardware_evidence_ack_missing work_submission=disabled` and never starts the Phase 21 runtime harness.
    - Test 2: With `BITAXE_MINING_EVIDENCE_MODE=live-mining-runtime` and `BITAXE_HARDWARE_EVIDENCE_ACK=ultra205-live-mining-runtime-safe-bench`, missing settings-derived pool config publishes `phase21_controlled_runtime_status=blocked reason=missing_pool_settings` and leaves work submission disabled.
    - Test 3: After a successful `PATCH /api/system` with disposable Stratum fields, Phase 21 mode refreshes from `settings_adapter::current_settings_snapshot()`, runs one bounded controlled harness cycle, logs subscribe/authorize/notify, BM1366 dispatch, result receive or bounded no-result, share submission outcome, watchdog-yield checkpoints, and safe-stop markers without raw secret values.
    - Test 4: The bounded harness publishes its final `MiningRuntimeState` through `runtime_snapshot::replace_mining_runtime_state_for_evidence`, and `collect_api_snapshot()` plus `/api/ws/live` telemetry surfaces expose pool lifecycle, share counters, difficulty, hashrate inputs, mining activity, and work-submission status from that runtime state.
  </behavior>
  <action>Create `firmware/bitaxe/src/mining_evidence_mode.rs` to parse `option_env!("BITAXE_MINING_EVIDENCE_MODE")` and `option_env!("BITAXE_HARDWARE_EVIDENCE_ACK")`; default to fail-closed unless the single accepted pair is `live-mining-runtime` and `ultra205-live-mining-runtime-safe-bench`. Create `firmware/bitaxe/src/controlled_mining_runtime.rs` as the imperative shell around the pure runtime from Task 1. It must expose `maybe_start_after_asic_gate()` for boot-time blocked/ready publication and `maybe_refresh_from_settings()` for the settings PATCH path. In Phase 21 mode, `maybe_refresh_from_settings()` must read only the typed settings snapshot, parse pool config into redacted controlled runtime input, run at most one bounded harness cycle, dispatch only typed `Bm1366Command` adapter actions through the existing BM1366 adapter boundary, parse at most one BM1366 result frame through `parse_bm1366_result_frame`, submit/share-classify only through the pure Stratum client message/result path, publish watchdog-yield checkpoints, and call `runtime_snapshot::replace_mining_runtime_state_for_evidence(state)`. Update `runtime_snapshot.rs` with that helper. Update `http_api.rs` so `apply_settings_effects` calls `controlled_mining_runtime::maybe_refresh_from_settings()` after persisted settings writes; this is the required settings/pool config -> runtime link. Update `main.rs` to load the modules and call the boot-time Phase 21 runtime publication after `startup_diagnostics?` and before HTTP/WebSocket startup. Do not add production/default mining enablement, raw pool sockets, raw pool commands, raw BM1366 writes, active voltage/fan/fault controls, network scanning, target inference, erase, rollback, interrupted-update, unbounded loops, or raw credential logging.</action>
  <verify>
    <automated>sh -c 'cargo test -p bitaxe-stratum --all-features controlled_runtime && cargo test -p bitaxe-stratum --all-features mining_loop'</automated>
    <automated>sh -c 'cargo test -p bitaxe-api --all-features mining && cargo test -p bitaxe-api --all-features telemetry'</automated>
    <automated>cargo build --package bitaxe-firmware --target xtensa-esp32s3-espidf</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "BITAXE_MINING_EVIDENCE_MODE|live-mining-runtime|ultra205-live-mining-runtime-safe-bench|phase21_controlled_runtime_status|maybe_refresh_from_settings|replace_mining_runtime_state_for_evidence|stratum_subscribe_status|stratum_authorize_status|stratum_notify_status|bm1366_work_dispatch_status|result_receive_status|share_submission_status|runtime_snapshot_status" firmware/bitaxe/src/main.rs firmware/bitaxe/src/mining_evidence_mode.rs firmware/bitaxe/src/controlled_mining_runtime.rs firmware/bitaxe/src/runtime_snapshot.rs firmware/bitaxe/src/http_api.rs` returns matches.
    - Default firmware code path still contains `hardware_evidence_ack_missing`, `mining=disabled`, and `work_submission=disabled`.
    - `cargo test -p bitaxe-stratum --all-features controlled_runtime` and `cargo test -p bitaxe-stratum --all-features mining_loop` exit 0.
    - `cargo test -p bitaxe-api --all-features mining` and `cargo test -p bitaxe-api --all-features telemetry` exit 0.
    - Firmware build exits 0, or records the exact ESP toolchain blocker while preserving the fail-closed default path and keeping this plan incomplete until fixed.
  </acceptance_criteria>
  <done>Firmware has a bounded Phase 21 runtime/harness path that consumes settings, runs the Stratum/work/result/share lifecycle, updates runtime state, and exposes API/WebSocket telemetry only under controlled evidence mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add controlled runtime package helper and enablement ledger</name>
  <files>scripts/phase21-live-mining-package.sh, scripts/phase21-live-mining-package-test.sh, scripts/BUILD.bazel, docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-enablement.md, docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-enablement/package/bitaxe-ultra205-package.json</files>
  <read_first>
    - scripts/phase15-bm1366-diagnostic-package.sh
    - scripts/package-firmware.sh
    - scripts/BUILD.bazel
    - docs/parity/evidence/phase-21-live-mining-and-soak-evidence/readiness-audit.md
    - docs/parity/evidence/phase-21-live-mining-and-soak-evidence/evidence-contract.md
    - docs/parity/evidence/phase-21-live-mining-and-soak-evidence/redaction-review.md
    - AGENTS.md
  </read_first>
  <behavior>
    - Test 1: Missing readiness audit writes `controlled_live_mining_package_status: blocked`, `controlled_runtime_harness_status: blocked`, `blocker: missing readiness audit`, and does not invoke Bazel.
    - Test 2: Readiness audit without `firmware_live_mining_status: blocked_by_default` writes blocked status and does not invoke Bazel.
    - Test 3: Valid readiness audit invokes Bazel with `--action_env=BITAXE_MINING_EVIDENCE_MODE=live-mining-runtime` and `--action_env=BITAXE_HARDWARE_EVIDENCE_ACK=ultra205-live-mining-runtime-safe-bench`.
    - Test 4: The helper writes `live-mining-enablement.md` with runtime harness markers required by 21-06 and 21-07, without raw pool, Wi-Fi, private target, token, IP, MAC, or secret values.
  </behavior>
  <action>Create or update `scripts/phase21-live-mining-package.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. It must accept `--out-dir` and `--readiness-audit` only. It must block before Bazel when the readiness audit is missing or does not contain `firmware_live_mining_status: blocked_by_default` and `controlled_enablement_required: true`. On the valid path, run `bazel build --action_env=BITAXE_MINING_EVIDENCE_MODE=live-mining-runtime --action_env=BITAXE_HARDWARE_EVIDENCE_ACK=ultra205-live-mining-runtime-safe-bench //firmware/bitaxe:firmware`, package the resulting ELF through `scripts/package-firmware.sh`, and write `live-mining-enablement.md` with exact fields `controlled_live_mining_package_status: ready` or `controlled_live_mining_package_status: blocked`, `controlled_runtime_harness_status: ready` or `controlled_runtime_harness_status: blocked`, `controlled_runtime_contract_tests: passed`, `runtime_required_log_markers: phase21_controlled_runtime_status, stratum_subscribe_status, stratum_authorize_status, stratum_notify_status, bm1366_work_dispatch_status, result_receive_status, share_submission_status, runtime_snapshot_status, api_websocket_telemetry_update_status, safe_stop_status`, `readiness_status: blocked_by_default`, `enablement_mode: live-mining-runtime`, `hardware_evidence_ack: ultra205-live-mining-runtime-safe-bench`, `package_manifest: docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-enablement/package/bitaxe-ultra205-package.json`, `source_commit:`, `reference_commit:`, `recovery_steps: stop wrapper, power-cycle board if serial stalls, reflash default safe package if live mode does not safe-stop`, `prerequisite_artifacts: readiness-audit.md`, `evidence_dir: docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-enablement`, `redaction_reviewer: required-before-citation`, and `post_action_safe_state_marker: safe_state: mining=disabled`, `hardware_control=disabled`, `work_submission=disabled`. Add `scripts/phase21-live-mining-package-test.sh` and register `sh_binary(name = "phase21_live_mining_package")` plus `sh_test(name = "phase21_live_mining_package_test")` in `scripts/BUILD.bazel`. Update `redaction-review.md` to include a `live-mining-enablement` row with `pending` status until the scan is reviewed. This implements D-02, D-04, D-05, D-06, D-07, D-08, D-16, D-20, D-23, and closes PC-08 by making the runtime/harness artifact a prerequisite for live smoke and soak.</action>
  <verify>
    <automated>bash -n scripts/phase21-live-mining-package.sh scripts/phase21-live-mining-package-test.sh</automated>
    <automated>bazel test //scripts:phase21_live_mining_package_test</automated>
    <automated>rg -n "controlled_live_mining_package_status: (ready|blocked)|controlled_runtime_harness_status: (ready|blocked)|BITAXE_MINING_EVIDENCE_MODE=live-mining-runtime|ultra205-live-mining-runtime-safe-bench|phase21_controlled_runtime_status|stratum_subscribe_status|bm1366_work_dispatch_status|share_submission_status|runtime_snapshot_status|redaction_reviewer: required-before-citation|live-mining-enablement" scripts/phase21-live-mining-package.sh scripts/phase21-live-mining-package-test.sh docs/parity/evidence/phase-21-live-mining-and-soak-evidence/live-mining-enablement.md docs/parity/evidence/phase-21-live-mining-and-soak-evidence/redaction-review.md</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase21-live-mining-package.sh` and its test are executable.
    - `scripts/BUILD.bazel` contains `phase21_live_mining_package` and `phase21_live_mining_package_test`.
    - `live-mining-enablement.md` contains `controlled_live_mining_package_status: ready` and `controlled_runtime_harness_status: ready` only after the firmware package manifest exists, runtime contract tests passed, and the manifest includes `"source_commit"` plus `"reference_commit"`.
    - If package creation or runtime harness readiness is blocked, `live-mining-enablement.md` contains blocked statuses and later plans must not run live smoke or soak.
    - Redaction review has a `live-mining-enablement` row and no raw credential or private endpoint is committed.
  </acceptance_criteria>
  <done>A Phase 21 controlled runtime package exists for downstream smoke/soak plans, or a blocked enablement ledger prevents later live execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| compile-time env to firmware | Evidence-mode env vars alter firmware mining readiness and runtime harness activation and must stay package-scoped. |
| settings PATCH to runtime harness | Operator-supplied pool settings cross from HTTP/NVS into the controlled runtime and must be parsed, redacted, and bounded. |
| runtime harness to ASIC adapter | Typed BM1366 work dispatch and result parsing cross into hardware effects and must stay behind safety/mining gates. |
| runtime state to HTTP/WebSocket | Controlled runtime observations become API-visible telemetry and evidence. |
| enablement ledger to later plans | Live smoke and soak depend on the ledger and boot-observed runtime markers before hardware/network effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-21-02-01 | Elevation of Privilege / Tampering | `firmware/bitaxe/src/mining_evidence_mode.rs` | mitigate | Default remains fail-closed; only `BITAXE_MINING_EVIDENCE_MODE=live-mining-runtime` plus `BITAXE_HARDWARE_EVIDENCE_ACK=ultra205-live-mining-runtime-safe-bench` can activate the harness. |
| T-21-02-02 | Information Disclosure | settings-derived pool config and evidence ledger | mitigate | Parse raw settings at the boundary, keep secret-bearing values out of `Debug`/`Display`, log only redacted category labels, and redaction-scan committed ledgers. |
| T-21-02-03 | Denial of Service | controlled runtime harness | mitigate | Run at most one bounded lifecycle cycle per refresh, include watchdog-yield checkpoints, require safe-stop markers, and prohibit unbounded loops. |
| T-21-02-04 | Tampering / Denial of Service | BM1366 dispatch | mitigate | Dispatch only typed `Bm1366Command::SendDiagnosticWork` produced by the pure guarded runtime; no raw BM1366 writes or voltage/fan/fault controls. |
| T-21-02-05 | Repudiation | enablement evidence | mitigate | Ledger records runtime contract tests, required boot log markers, package manifest, source/reference commits, prerequisite artifacts, recovery steps, and redaction reviewer status. |
| T-21-02-06 | Information Disclosure / Spoofing | API/WebSocket telemetry update path | mitigate | Runtime snapshot publishes structured state only; no `DEVICE_URL` inference, no network scanning, and no raw pool config in telemetry or logs. |
</threat_model>

<verification>
Run the task-level checks plus:

```bash
cargo test -p bitaxe-stratum --all-features controlled_runtime
cargo test -p bitaxe-stratum --all-features mining_loop
cargo test -p bitaxe-api --all-features mining
cargo test -p bitaxe-api --all-features telemetry
bazel test //crates/bitaxe-stratum:tests --test_filter=controlled_runtime
bazel test //scripts:phase21_live_mining_package_test
just parity
just verify-reference
git diff -- reference/esp-miner --exit-code
```
</verification>

<success_criteria>
- Default firmware mining readiness remains blocked without Phase 21 compile-time evidence mode.
- A bounded controlled runtime/harness exists and proves settings-derived Stratum subscribe/authorize/notify, guarded work queue, typed BM1366 dispatch, result parsing, share submission outcome, runtime state, and API/WebSocket telemetry updates.
- Controlled live-mining package helper can create or block the package path deterministically from `readiness-audit.md`.
- Later live smoke and bounded soak plans have a required `live-mining-enablement.md` artifact and boot-observed runtime markers; readiness-only artifacts cannot pass.
- No secret-bearing pool, target, Wi-Fi, token, NVS, IP, or MAC value is introduced into committed enablement evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/21-live-mining-and-soak-evidence/21-02-SUMMARY.md`.
</output>
