---
phase: 06-safety-controllers-and-self-test
plan: "04"
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - crates/bitaxe-safety/src/thermal.rs
  - crates/bitaxe-safety/src/fault.rs
  - crates/bitaxe-safety/fixtures/safety/fan-pid-cases.json
  - crates/bitaxe-safety/fixtures/safety/thermal-fault-cases.json
  - crates/bitaxe-safety/fixtures/safety/overheat-state-cases.json
autonomous: true
requirements: [SAFE-02, SAFE-03, SAFE-04, SAFE-07, SAFE-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-06-28T02-37-37
generated_at: 2026-06-28T03:30:00Z
must_haves:
  truths:
    - "Thermal and fan inputs are parsed into typed observations before they influence control decisions."
    - "PID, fan mode, RPM fault, and overheat decisions are host-testable without firmware I/O."
    - "Sustained thermal, fan, or ASIC safety faults enter safe-blocked status and visible fail-closed effects."
  artifacts:
    - path: "crates/bitaxe-safety/src/thermal.rs"
      provides: "Pure thermal/fan/PID decisions"
      exports: ["PidController", "FanControlDecision", "ThermalObservation", "ThermalEvidenceToken", "OverheatDecision"]
    - path: "crates/bitaxe-safety/src/fault.rs"
      provides: "Shared fault classification and safe-block policy"
      exports: ["SafetyFault", "FaultDecision"]
    - path: "crates/bitaxe-safety/fixtures/safety/fan-pid-cases.json"
      provides: "PID and fan mode fixture cases"
    - path: "crates/bitaxe-safety/fixtures/safety/overheat-state-cases.json"
      provides: "Overheat stop/cool/restart fixture cases"
  key_links:
    - from: "crates/bitaxe-safety/src/thermal.rs"
      to: "crates/bitaxe-safety/src/effects.rs"
      via: "fan and overheat effect plans"
      pattern: "SafetyEffectPlan"
    - from: "crates/bitaxe-safety/src/fault.rs"
      to: "crates/bitaxe-safety/src/status.rs"
      via: "visible fault status mapping"
      pattern: "SafetyStatus::SafeBlocked"
---

<objective>
Implement pure thermal, fan, PID, overheat, and fault decision behavior for Ultra 205 safety control.

Purpose: Fan and thermal control must be deterministic and fail-closed before firmware PWM, sensor, or mining effects consume it.
Output: Host-tested thermal/fan/PID/fault modules plus provenance-rich fixture cases.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-safety-controllers-and-self-test/06-CONTEXT.md
@.planning/phases/06-safety-controllers-and-self-test/06-RESEARCH.md
@.planning/phases/06-safety-controllers-and-self-test/06-VALIDATION.md
@.planning/phases/06-safety-controllers-and-self-test/06-01-SUMMARY.md
@.planning/phases/06-safety-controllers-and-self-test/06-02-SUMMARY.md
@crates/bitaxe-safety/src/effects.rs
@crates/bitaxe-safety/src/evidence.rs
@crates/bitaxe-safety/src/status.rs
@crates/bitaxe-config/src/validation.rs
@reference/esp-miner/main/thermal/thermal.c
@reference/esp-miner/main/thermal/PID.c
@reference/esp-miner/main/tasks/fan_controller_task.c
@reference/esp-miner/main/tasks/power_management_task.c
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement thermal observation, PID, and fan duty decisions</name>
  <files>crates/bitaxe-safety/src/thermal.rs, crates/bitaxe-safety/fixtures/safety/fan-pid-cases.json, crates/bitaxe-safety/fixtures/safety/thermal-fault-cases.json</files>
  <read_first>
    <file>crates/bitaxe-safety/src/thermal.rs</file>
    <file>crates/bitaxe-safety/src/effects.rs</file>
    <file>crates/bitaxe-config/src/validation.rs</file>
    <file>reference/esp-miner/main/thermal/thermal.c</file>
    <file>reference/esp-miner/main/thermal/PID.c</file>
    <file>reference/esp-miner/main/tasks/fan_controller_task.c</file>
    <file>.planning/phases/06-safety-controllers-and-self-test/06-RESEARCH.md</file>
  </read_first>
  <behavior>
    - Test 1: PID constants are exactly `P=5.0`, `I=0.1`, `D=2.0`, sample time `100 ms`, EMA alpha `0.2`, and output clamp `min_fan..=100`.
    - Test 2: raw thermal sentinels `-1`, `127`, non-finite values, and implausible values below `-40 C` or above `150 C` classify as faults/unavailable before fan decisions.
    - Test 3: fan modes emit duty `100%` for overheat, `70%` for startup, `30%` for paused/no-pool, manual parsed `0..=100`, and auto PID clamped to the configured minimum fan floor.
    - Test 4: only fresh safe thermal observations can produce a `ThermalEvidenceToken`; faulted, unavailable, or overheat states cannot satisfy downstream ASIC or mining gates.
  </behavior>
  <action>In `thermal.rs`, define constants `PID_KP: f64 = 5.0`, `PID_KI: f64 = 0.1`, `PID_KD: f64 = 2.0`, `PID_SAMPLE_TIME_MS: u32 = 100`, `PID_EMA_ALPHA: f64 = 0.2`, `STARTUP_FAN_DUTY_PERCENT: u8 = 70`, `PAUSED_FAN_DUTY_PERCENT: u8 = 30`, `OVERHEAT_FAN_DUTY_PERCENT: u8 = 100`, `THERMAL_UNAVAILABLE_SENTINEL: f64 = -1.0`, `THERMAL_DIODE_FAULT_SENTINEL: f64 = 127.0`, `MIN_PLAUSIBLE_TEMP_C: f64 = -40.0`, and `MAX_PLAUSIBLE_TEMP_C: f64 = 150.0`. Implement `ThermalReading`, `ThermalObservationStatus::{Fresh, Fault, Unavailable}`, `ThermalObservation`, `ThermalEvidenceToken`, `PidState`, `PidController`, `FanControlMode::{Overheat, Startup, PausedOrNoPool, Manual, Auto}`, `FanControlInputs`, and `FanControlDecision`. `ThermalEvidenceToken` must be constructible only from a fresh safe `ThermalObservation` plus acceptable safety evidence, and must be absent for faulted, unavailable, implausible, or overheat states before Plan 06-06 consumes it in ASIC and mining gates. Parse manual and minimum fan values through `FanDutyPercent::parse` and `MinFanDutyPercent::parse`. Ensure raw sentinel/fault values return `SafetyEffectPlan::fail_closed("thermal_reading_invalid")` or `"thermal_reading_unavailable"` before PID or fan duty calculations. Add `fan-pid-cases.json` and `thermal-fault-cases.json` with checklist IDs `THR-001`, `THR-002`, `THR-003`, `SAFE-02`, `SAFE-03`, and `SAFE-07`, plus pinned reference commit, source file, source behavior, license posture, and derivation metadata.</action>
  <acceptance_criteria>
    - `rg -n "PID_KP: f64 = 5\\.0|PID_KI: f64 = 0\\.1|PID_KD: f64 = 2\\.0|PID_SAMPLE_TIME_MS: u32 = 100|PID_EMA_ALPHA: f64 = 0\\.2" crates/bitaxe-safety/src/thermal.rs` finds exact PID constants.
    - `rg -n "THERMAL_UNAVAILABLE_SENTINEL: f64 = -1\\.0|THERMAL_DIODE_FAULT_SENTINEL: f64 = 127\\.0|MIN_PLAUSIBLE_TEMP_C: f64 = -40\\.0|MAX_PLAUSIBLE_TEMP_C: f64 = 150\\.0" crates/bitaxe-safety/src/thermal.rs` finds exact sentinel/plausibility constants.
    - `rg -n "STARTUP_FAN_DUTY_PERCENT: u8 = 70|PAUSED_FAN_DUTY_PERCENT: u8 = 30|OVERHEAT_FAN_DUTY_PERCENT: u8 = 100|FanControlMode|FanControlDecision|PidController|ThermalEvidenceToken" crates/bitaxe-safety/src/thermal.rs` finds fan/PID decision types and the downstream thermal evidence token.
    - `rg -n "THR-001|THR-002|THR-003|SAFE-02|SAFE-03|SAFE-07|c1915b0a63bfabebdb95a515cedfee05146c1d50" crates/bitaxe-safety/fixtures/safety/fan-pid-cases.json crates/bitaxe-safety/fixtures/safety/thermal-fault-cases.json` finds fixture provenance.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p bitaxe-safety --all-features safety_thermal && bazel test //crates/bitaxe-safety:tests --test_filter=safety_thermal</automated>
  </verify>
  <done>Thermal readings, PID output, and fan duty targets are deterministic, typed, fixture-backed, and fail closed before firmware fan or sensor effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement overheat and sustained fault state decisions</name>
  <files>crates/bitaxe-safety/src/thermal.rs, crates/bitaxe-safety/src/fault.rs, crates/bitaxe-safety/fixtures/safety/overheat-state-cases.json</files>
  <read_first>
    <file>crates/bitaxe-safety/src/thermal.rs</file>
    <file>crates/bitaxe-safety/src/fault.rs</file>
    <file>crates/bitaxe-safety/src/status.rs</file>
    <file>crates/bitaxe-safety/src/effects.rs</file>
    <file>reference/esp-miner/main/tasks/power_management_task.c</file>
    <file>reference/esp-miner/main/tasks/fan_controller_task.c</file>
    <file>.planning/phases/06-safety-controllers-and-self-test/06-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Test 1: chip temperature at or above `75 C` transitions from normal to safe stop with reset held low, fan `100%`, voltage write suppressed, and mining blocked.
    - Test 2: cooling below or equal to `45 C` produces a restart-candidate state but does not resume mining without external ASIC, power, thermal, safety, and hardware-evidence gates.
    - Test 3: zero RPM for at least `3` consecutive samples when target duty is at least `30%`, fan set failure, thermal fault, and ASIC fault all map to visible safe-blocked statuses.
  </behavior>
  <action>Extend `thermal.rs` and `fault.rs` with safe overheat and fault policy. Define constants `ASIC_THROTTLE_TEMP_C: f64 = 75.0`, `SAFE_RESTART_TEMP_C: f64 = 45.0`, `ZERO_RPM_FAULT_MIN_DUTY_PERCENT: u8 = 30`, and `ZERO_RPM_FAULT_SAMPLE_COUNT: u8 = 3`. Implement `OverheatState::{Normal, SafeStopped, Cooling, RestartCandidate, SafeBlocked}`, `OverheatInputs`, `OverheatDecision`, `SafetyFault::{Overheat, FanZeroRpm, FanSetFailed, ThermalSensorFault, PowerFault, AsicFault}`, and `FaultDecision`. Ensure overheat stop effects include `HoldResetLow`, `SuppressVoltageWrite`, `BlockWorkSubmission { reason: "overheat_safe_stop" }`, `SetFanDutyPercent { percent: 100 }`, and `PublishStatus(SafetyStatus::ThermalFault { reason: "overheat_safe_stop" })`. Ensure restart candidate uses status `SafeBlocked { reason: "restart_requires_hardware_gates" }` and never produces a mining-ready effect. Add `overheat-state-cases.json` with normal, overheat stop, cool-down, restart candidate, fan zero RPM, fan set failure, power fault, thermal fault, and ASIC fault cases, including checklist IDs `THR-001`, `THR-002`, `PWR-001`, `PWR-002`, `SAFE-04`, and `SAFE-08`.</action>
  <acceptance_criteria>
    - `rg -n "ASIC_THROTTLE_TEMP_C: f64 = 75\\.0|SAFE_RESTART_TEMP_C: f64 = 45\\.0|ZERO_RPM_FAULT_MIN_DUTY_PERCENT: u8 = 30|ZERO_RPM_FAULT_SAMPLE_COUNT: u8 = 3" crates/bitaxe-safety/src/thermal.rs crates/bitaxe-safety/src/fault.rs` finds all constants.
    - `rg -n "OverheatState|SafeStopped|Cooling|RestartCandidate|restart_requires_hardware_gates|overheat_safe_stop" crates/bitaxe-safety/src/thermal.rs` finds overheat state machine and reason strings.
    - `rg -n "SafetyFault|FanZeroRpm|FanSetFailed|ThermalSensorFault|PowerFault|AsicFault|FaultDecision" crates/bitaxe-safety/src/fault.rs` finds fault classifications.
    - `rg -n "THR-001|THR-002|PWR-001|PWR-002|SAFE-04|SAFE-08|not verified" crates/bitaxe-safety/fixtures/safety/overheat-state-cases.json` finds fixture provenance and non-verification wording.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p bitaxe-safety --all-features safety_fault && bazel test //crates/bitaxe-safety:tests --test_filter=safety_fault</automated>
  </verify>
  <done>Overheat, fan, power, thermal, and ASIC fault paths produce typed safe states and visible fail-closed effects without resuming hardware or mining automatically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw thermal/fan readings -> safety domain | Sensor sentinels, diode faults, zero RPM, and set failures are untrusted hardware data. |
| Safety policy -> mining/power state | Overheat or fan decisions can stop, cool, or restart hardware if mishandled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | `ThermalObservation` parsing | mitigate | Convert `-1`, `127`, non-finite, and implausible values into typed faults before fan or PID decisions. |
| T-06-04-02 | Denial of Service | PID/fan control | mitigate | Keep PID pure and bounded with exact clamps, no sleeps, no loops, and no firmware effects. |
| T-06-04-03 | Elevation of Privilege | Overheat restart | mitigate | Model restart as `RestartCandidate` requiring later ASIC/power/thermal/safety/hardware gates; do not emit mining-ready effects. |
| T-06-04-04 | Information Disclosure | Public status mapping | accept | Status reason strings are operational safety states and contain no secrets or user credentials. |
</threat_model>

<verification>
Run `cargo test -p bitaxe-safety --all-features safety_thermal`, `cargo test -p bitaxe-safety --all-features safety_fault`, and `bazel test //crates/bitaxe-safety:tests`. Run `just test && just parity` after Wave 3 completes. Before any commit, also run the repo Rust gate from `AGENTS.md`: `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`.
</verification>

<success_criteria>
Thermal, fan, PID, and fault decisions are pure, typed, unit-tested, fixture-backed, and fail closed on sentinel, implausible, zero-RPM, set-failure, overheat, power, thermal, or ASIC faults.
</success_criteria>

<output>
After completion, create `.planning/phases/06-safety-controllers-and-self-test/06-04-SUMMARY.md`.
</output>
