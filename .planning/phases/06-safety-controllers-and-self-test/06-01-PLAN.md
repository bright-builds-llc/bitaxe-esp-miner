---
phase: 06-safety-controllers-and-self-test
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - MODULE.bazel.lock
  - crates/bitaxe-safety/Cargo.toml
  - crates/bitaxe-safety/BUILD.bazel
  - crates/bitaxe-safety/src/lib.rs
  - crates/bitaxe-safety/src/effects.rs
  - crates/bitaxe-safety/src/evidence.rs
  - crates/bitaxe-safety/src/status.rs
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-03, SAFE-04, SAFE-05, SAFE-07, SAFE-08, SAFE-09]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-06-28T02-37-37
generated_at: 2026-06-28T03:30:00Z
must_haves:
  truths:
    - "Safety policy exists in a pure Rust crate before firmware hardware effects are touched."
    - "Executors can implement power, thermal, self-test, watchdog, API, and firmware slices against named shared status, evidence, and effect contracts."
    - "Safety-critical verification state distinguishes implementation from hardware-smoke or hardware-regression evidence."
  artifacts:
    - path: "crates/bitaxe-safety/src/lib.rs"
      provides: "Public safety crate facade"
      exports: ["effects", "evidence", "status"]
    - path: "crates/bitaxe-safety/src/effects.rs"
      provides: "Typed fail-closed action and effect plan contract"
      contains: "SafetyEffectPlan"
    - path: "crates/bitaxe-safety/src/evidence.rs"
      provides: "Hardware evidence classification contract"
      contains: "SafetyCriticalEvidence"
    - path: "crates/bitaxe-safety/src/status.rs"
      provides: "Shared user-visible safety status contract"
      contains: "SafetyStatus"
  key_links:
    - from: "crates/bitaxe-safety/src/lib.rs"
      to: "crates/bitaxe-safety/src/effects.rs"
      via: "public module export"
      pattern: "pub mod effects"
    - from: "crates/bitaxe-safety/src/evidence.rs"
      to: "docs/parity/checklist.md"
      via: "safety evidence semantics used by later checklist work"
      pattern: "hardware-smoke|hardware-regression"
---

<objective>
Create the Phase 6 safety domain crate and shared status/evidence/effect contracts that downstream power, thermal, self-test, watchdog, API, firmware, and parity plans can consume.

Purpose: The hardest constraint in Phase 6 is preventing unsafe hardware effects and false verification claims. This plan creates the pure contract layer first so later firmware adapters can only interpret typed plans.
Output: `crates/bitaxe-safety` plus workspace/Bazel wiring and shared status/evidence/effect contracts.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-safety-controllers-and-self-test/06-CONTEXT.md
@.planning/phases/06-safety-controllers-and-self-test/06-RESEARCH.md
@.planning/phases/06-safety-controllers-and-self-test/06-VALIDATION.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@crates/bitaxe-config/src/catalog.rs
@crates/bitaxe-config/src/validation.rs
@crates/bitaxe-asic/src/bm1366/init_plan.rs
@crates/bitaxe-stratum/src/v1/mining_loop.rs
@crates/bitaxe-api/src/snapshot.rs

<interfaces>
From `crates/bitaxe-asic/src/bm1366/init_plan.rs`:
```rust
pub struct PowerPreflightEvidence;
pub struct ThermalPreflightEvidence;
pub struct SafetyPreflightEvidence;
pub enum FailClosedAction { HoldResetLow, DisableAsicEnable, NoHardwareEffect }
```

From `crates/bitaxe-stratum/src/v1/mining_loop.rs`:
```rust
pub struct MiningLoopGate {
    pub asic_initialized: bool,
    pub safety_evidence: bool,
    pub hardware_evidence_ack: bool,
}
pub const SAFETY_PREFLIGHT_EVIDENCE_MISSING: &str = "safety_preflight_evidence_missing";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the bitaxe-safety crate and shared contract facade</name>
  <files>Cargo.toml, Cargo.lock, MODULE.bazel.lock, crates/bitaxe-safety/Cargo.toml, crates/bitaxe-safety/BUILD.bazel, crates/bitaxe-safety/src/lib.rs</files>
  <read_first>
    <file>AGENTS.md</file>
    <file>standards/languages/rust.md</file>
    <file>Cargo.toml</file>
    <file>crates/bitaxe-config/Cargo.toml</file>
    <file>crates/bitaxe-api/BUILD.bazel</file>
    <file>crates/bitaxe-core/BUILD.bazel</file>
    <file>.planning/phases/06-safety-controllers-and-self-test/06-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Test 1: `bitaxe_safety::phase06_contract_name()` returns `safety-controllers-and-self-test`.
    - Test 2: public modules `effects`, `evidence`, and `status` are importable without ESP-IDF dependencies.
    - Test 3: `cargo test -p bitaxe-safety --all-features safety_contract` runs on the host target.
  </behavior>
  <action>Create a new pure Rust crate at `crates/bitaxe-safety` and add it to both `workspace.members` and `workspace.default-members` in the root `Cargo.toml`. Use Rust 2021 through `edition.workspace = true`, `license.workspace = true`, and dependencies `bitaxe-config = { path = "../bitaxe-config" }`, `serde.workspace = true`, and `thiserror.workspace = true`; add `serde_json.workspace = true` only under `[dev-dependencies]`. Add `crates/bitaxe-safety/BUILD.bazel` with a public `rust_library(name = "bitaxe_safety")`, `rust_test(name = "tests")`, `compile_data = glob(["fixtures/**"], allow_empty = True)`, and Bazel deps `//crates/bitaxe-config:bitaxe_config`, `@crates//:serde`, and `@crates//:thiserror`. In `src/lib.rs`, export exactly `effects`, `evidence`, and `status`, and add `pub const fn phase06_contract_name() -> &'static str { "safety-controllers-and-self-test" }`. Keep this foundational plan focused on the shared contracts; Plan 06-02 owns the power/thermal/fault/self-test/watchdog module graph so each plan stays below the scope threshold. Refresh `Cargo.lock` and `MODULE.bazel.lock` through the normal Cargo/Bazel commands if they change.</action>
  <acceptance_criteria>
    - `rg -n "crates/bitaxe-safety" Cargo.toml` finds the crate in both workspace arrays.
    - `rg -n "name = \"bitaxe-safety\"|bitaxe-config|serde|thiserror" crates/bitaxe-safety/Cargo.toml` finds all required package/dependency strings.
    - `rg -n "pub mod effects|pub mod evidence|pub mod status" crates/bitaxe-safety/src/lib.rs` finds all shared contract exports.
    - `! rg -n "esp_idf|I2cDriver|PinDriver|UartDriver|std::thread::sleep" crates/bitaxe-safety/src` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p bitaxe-safety --all-features safety_contract && bazel test //crates/bitaxe-safety:tests</automated>
  </verify>
  <done>`crates/bitaxe-safety` builds and tests as a host-only pure crate with shared contracts available to later plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define shared safety status, effect, and evidence contracts</name>
  <files>crates/bitaxe-safety/src/effects.rs, crates/bitaxe-safety/src/evidence.rs, crates/bitaxe-safety/src/status.rs, crates/bitaxe-safety/src/lib.rs</files>
  <read_first>
    <file>crates/bitaxe-safety/src/lib.rs</file>
    <file>crates/bitaxe-asic/src/bm1366/init_plan.rs</file>
    <file>crates/bitaxe-stratum/src/v1/mining_loop.rs</file>
    <file>tools/parity/src/main.rs</file>
    <file>docs/parity/checklist.md</file>
    <file>.planning/phases/06-safety-controllers-and-self-test/06-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Test 1: `SafetyCriticalEvidence::implemented_not_verified("unit")` is not valid verification evidence.
    - Test 2: `SafetyCriticalEvidence::hardware_smoke("phase-06-ultra-205-safety-hardware-smoke")` is valid verification evidence.
    - Test 3: `SafetyEffectPlan::fail_closed("power_fault")` contains `HoldResetLow`, `SuppressVoltageWrite`, `BlockWorkSubmission`, and `PublishStatus(SafetyStatus::SafeBlocked { reason: "power_fault" })`.
  </behavior>
  <action>Implement shared contracts that later plans consume. In `evidence.rs`, define `SafetyCriticalEvidence` with variants `Missing`, `ImplementedNotVerified { source: &'static str }`, `HardwareSmoke { evidence_id: &'static str }`, and `HardwareRegression { evidence_id: &'static str }`, plus methods `implemented_not_verified`, `hardware_smoke`, `hardware_regression`, `is_hardware_verified`, and `label` returning exactly `pending`, `unit`, `hardware-smoke`, or `hardware-regression`. In `status.rs`, define `SafetyStatus` variants `Normal`, `Unavailable { reason: &'static str }`, `SafeBlocked { reason: &'static str }`, `PowerFault { reason: &'static str }`, `ThermalFault { reason: &'static str }`, `FanFault { reason: &'static str }`, `SelfTestRunning`, `SelfTestPassed`, `SelfTestFailed { reason: &'static str }`, and method `public_reason()`. In `effects.rs`, define `SafetyEffect` variants `HoldResetLow`, `DisableAsicEnable`, `SuppressVoltageWrite`, `SetFanDutyPercent { percent: u8 }`, `BlockWorkSubmission { reason: &'static str }`, `PublishStatus(SafetyStatus)`, and `YieldWatchdog { after_ms: u32 }`; define `SafetyEffectPlan { status: SafetyStatus, effects: Vec<SafetyEffect>, evidence: SafetyCriticalEvidence }` with constructors `observe_only(status, evidence)`, `fail_closed(reason)`, and `with_effects(status, effects, evidence)`. Keep these modules pure and `serde`-serializable where needed for fixture tests. Reference D-03, D-08, D-17, D-18, and D-20 in code comments only at module/behavior boundaries.</action>
  <acceptance_criteria>
    - `rg -n "enum SafetyCriticalEvidence|ImplementedNotVerified|HardwareSmoke|HardwareRegression|is_hardware_verified" crates/bitaxe-safety/src/evidence.rs` finds evidence variants and verification method.
    - `rg -n "enum SafetyStatus|SafeBlocked|PowerFault|ThermalFault|FanFault|SelfTestRunning|public_reason" crates/bitaxe-safety/src/status.rs` finds status variants and method.
    - `rg -n "enum SafetyEffect|HoldResetLow|SuppressVoltageWrite|BlockWorkSubmission|YieldWatchdog|struct SafetyEffectPlan|fail_closed" crates/bitaxe-safety/src/effects.rs` finds the shared effect plan contract.
    - `cargo test -p bitaxe-safety --all-features evidence` output includes passing tests for hardware-smoke and implemented-not-verified distinction.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p bitaxe-safety --all-features safety_contract && cargo test -p bitaxe-safety --all-features evidence && bazel test //crates/bitaxe-safety:tests</automated>
  </verify>
  <done>Shared Phase 6 contracts represent fail-closed safety actions, user-visible status, and hardware evidence state without enabling any firmware effect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Safety crate public API -> firmware adapters | Pure contracts will later control GPIO, I2C, PWM, UART, mining, and API-visible status. |
| Evidence metadata -> parity claims | Evidence labels can affect whether safety-critical behavior appears verified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | `SafetyEffectPlan` | mitigate | Make fail-closed effects explicit and unit-test that `fail_closed` includes reset, voltage suppression, work blocking, and status publication. |
| T-06-01-02 | Spoofing | `SafetyCriticalEvidence` | mitigate | Only `HardwareSmoke` and `HardwareRegression` return `is_hardware_verified() == true`; implemented/unit evidence cannot satisfy safety-critical verification. |
| T-06-01-03 | Elevation of Privilege | `crates/bitaxe-safety` crate boundary | mitigate | Keep the crate free of ESP-IDF, I2C, UART, GPIO, thread sleeping, and firmware imports so pure contracts cannot perform effects directly. |
| T-06-01-04 | Repudiation | Reference breadcrumbs | mitigate | Require module-level breadcrumbs to pinned upstream files and checklist rows for each safety surface. |
</threat_model>

<verification>
Run `cargo test -p bitaxe-safety --all-features` and `bazel test //crates/bitaxe-safety:tests`. Before any commit, also run the repo Rust gate from `AGENTS.md`: `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`.
</verification>

<success_criteria>
The repo contains a host-testable `bitaxe-safety` crate with shared Phase 6 contracts, no hardware side effects, no ESP-IDF imports, and an explicit hardware-evidence distinction that later plans can use for ASIC init, mining gates, API telemetry, firmware adapters, and parity checklist enforcement.
</success_criteria>

<output>
After completion, create `.planning/phases/06-safety-controllers-and-self-test/06-01-SUMMARY.md`.
</output>
