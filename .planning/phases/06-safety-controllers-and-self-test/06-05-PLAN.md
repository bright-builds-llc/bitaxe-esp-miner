---
phase: 06-safety-controllers-and-self-test
plan: "05"
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - crates/bitaxe-safety/src/self_test.rs
  - crates/bitaxe-safety/src/watchdog.rs
  - crates/bitaxe-safety/fixtures/safety/self-test-lifecycle-cases.json
  - crates/bitaxe-safety/fixtures/safety/watchdog-step-cases.json
autonomous: true
requirements: [SAFE-05, SAFE-09]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-06-28T02-37-37
generated_at: 2026-06-28T03:30:00Z
must_haves:
  truths:
    - "Self-test has explicit factory, manual, running, pass, fail, restart, cancel, and result-reporting states."
    - "Production mining gates remain closed during self-test unless diagnostic safety and hardware evidence are present."
    - "Long-running safety and self-test work is represented as bounded steps with watchdog/yield decisions."
  artifacts:
    - path: "crates/bitaxe-safety/src/self_test.rs"
      provides: "Pure self-test lifecycle and effect planning"
      exports: ["SelfTestLifecycle", "SelfTestState", "SelfTestDecision"]
    - path: "crates/bitaxe-safety/src/watchdog.rs"
      provides: "Watchdog-friendly step supervision"
      exports: ["StepSupervisor", "WatchdogDecision"]
    - path: "crates/bitaxe-safety/fixtures/safety/self-test-lifecycle-cases.json"
      provides: "Self-test lifecycle fixture cases"
    - path: "crates/bitaxe-safety/fixtures/safety/watchdog-step-cases.json"
      provides: "Bounded-step fixture cases"
  key_links:
    - from: "crates/bitaxe-safety/src/self_test.rs"
      to: "crates/bitaxe-safety/src/watchdog.rs"
      via: "bounded lifecycle steps"
      pattern: "WatchdogDecision"
    - from: "crates/bitaxe-safety/src/self_test.rs"
      to: "crates/bitaxe-safety/src/effects.rs"
      via: "safe mining/blocking effects"
      pattern: "BlockWorkSubmission"
---

<objective>
Implement the pure self-test lifecycle and watchdog-friendly step supervision needed before firmware self-test behavior can exist.

Purpose: Upstream self-test touches fan, voltage, ASIC, mining work, and stored factory flags; Phase 6 must model the lifecycle without copying blocking tasks or enabling production mining.
Output: Host-tested self-test state machine, effect plans, result reporting, flag clearing rules, and bounded-step watchdog decisions.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-safety-controllers-and-self-test/06-CONTEXT.md
@.planning/phases/06-safety-controllers-and-self-test/06-RESEARCH.md
@.planning/phases/06-safety-controllers-and-self-test/06-VALIDATION.md
@.planning/phases/06-safety-controllers-and-self-test/06-01-SUMMARY.md
@.planning/phases/06-safety-controllers-and-self-test/06-02-SUMMARY.md
@crates/bitaxe-safety/src/effects.rs
@crates/bitaxe-safety/src/evidence.rs
@crates/bitaxe-safety/src/status.rs
@reference/esp-miner/main/self_test/self_test.c
@reference/esp-miner/main/tasks/fan_controller_task.c
@reference/esp-miner/main/tasks/power_management_task.c
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Model self-test factory/manual lifecycle and result effects</name>
  <files>crates/bitaxe-safety/src/self_test.rs, crates/bitaxe-safety/fixtures/safety/self-test-lifecycle-cases.json</files>
  <read_first>
    <file>crates/bitaxe-safety/src/self_test.rs</file>
    <file>crates/bitaxe-safety/src/effects.rs</file>
    <file>crates/bitaxe-safety/src/evidence.rs</file>
    <file>reference/esp-miner/main/self_test/self_test.c</file>
    <file>crates/bitaxe-config/src/defaults.rs</file>
    <file>crates/bitaxe-config/src/nvs.rs</file>
    <file>.planning/phases/06-safety-controllers-and-self-test/06-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Test 1: configured factory flag and boot-button start both transition `Idle` to running self-test with production mining blocked.
    - Test 2: pass and cancel clear the factory flag when the lifecycle origin is factory, while fail records a result and keeps mining blocked.
    - Test 3: restart and cancel are explicit effects and no lifecycle branch produces production work submission without all diagnostic safety and hardware evidence acknowledgments.
  </behavior>
  <action>In `self_test.rs`, define constants `SELF_TEST_MIN_FAN_RPM: u16 = 1000`, `SELF_TEST_MIN_FAN_DUTY_PERCENT: u8 = 10`, `SELF_TEST_MAX_FAN_DUTY_PERCENT: u8 = 100`, `SELF_TEST_DOMAIN_HASHRATE_TOLERANCE: f64 = 0.33`, `SELF_TEST_REJECTED_WARN_RATIO: f64 = 0.25`, `SELF_TEST_CORE_VOLTAGE_TOLERANCE: f64 = 0.10`, `SELF_TEST_POWER_MARGIN_WATTS: f64 = 3.0`, `SELF_TEST_INPUT_VOLTAGE_MARGIN_RATIO: f64 = 0.10`, and `SELF_TEST_DIFFICULTY: u32 = 16`. Implement `SelfTestOrigin::{FactoryFlag, BootButton, ManualApi}`, `SelfTestCommand::{StartFactory, StartBootButton, StartManual, Step, Pass, Fail { reason }, Restart, Cancel}`, `SelfTestState::{Idle, Running { origin, step }, Passed, Failed { reason }, Canceled, RestartRequested}`, `SelfTestEffect::{SetFactoryFlag { enabled }, ClearFactoryFlag, BlockProductionMining, UseDiagnosticWorkOnly, RecordResult { passed }, PublishStatus(SafetyStatus), RestartSelfTest}`, and `SelfTestDecision`. Add `SelfTestLifecycle::apply(state, command, evidence)` where `evidence` must include safety, power, thermal, ASIC, and hardware-evidence acknowledgment for diagnostic hardware submodes; otherwise it only emits `BlockProductionMining`, `PublishStatus(SafetyStatus::SafeBlocked { reason: "self_test_hardware_evidence_missing" })`, and safe reporting effects. Add `self-test-lifecycle-cases.json` with factory start, boot-button start, manual start, pass, fail, restart, cancel, missing evidence, fake nonce injection, and mock Stratum work cases; include checklist ID `SELF-001` and requirement IDs `SAFE-05`, `SAFE-08`, and `SAFE-09`.</action>
  <acceptance_criteria>
    - `rg -n "SELF_TEST_MIN_FAN_RPM: u16 = 1000|SELF_TEST_MIN_FAN_DUTY_PERCENT: u8 = 10|SELF_TEST_MAX_FAN_DUTY_PERCENT: u8 = 100|SELF_TEST_DOMAIN_HASHRATE_TOLERANCE: f64 = 0\\.33|SELF_TEST_REJECTED_WARN_RATIO: f64 = 0\\.25|SELF_TEST_CORE_VOLTAGE_TOLERANCE: f64 = 0\\.10|SELF_TEST_POWER_MARGIN_WATTS: f64 = 3\\.0|SELF_TEST_INPUT_VOLTAGE_MARGIN_RATIO: f64 = 0\\.10|SELF_TEST_DIFFICULTY: u32 = 16" crates/bitaxe-safety/src/self_test.rs` finds all constants.
    - `rg -n "SelfTestOrigin|FactoryFlag|BootButton|ManualApi|SelfTestCommand|SelfTestState|RestartRequested|SelfTestDecision" crates/bitaxe-safety/src/self_test.rs` finds lifecycle types.
    - `rg -n "ClearFactoryFlag|BlockProductionMining|UseDiagnosticWorkOnly|self_test_hardware_evidence_missing|RecordResult" crates/bitaxe-safety/src/self_test.rs` finds effects and missing-evidence reason.
    - `rg -n "SELF-001|SAFE-05|SAFE-08|SAFE-09|fake nonce|mock Stratum|c1915b0a63bfabebdb95a515cedfee05146c1d50" crates/bitaxe-safety/fixtures/safety/self-test-lifecycle-cases.json` finds required fixture coverage and provenance.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p bitaxe-safety --all-features self_test && bazel test //crates/bitaxe-safety:tests --test_filter=self_test</automated>
  </verify>
  <done>Self-test lifecycle behavior covers factory flags, start, pass, fail, restart, cancel, result reporting, and safe missing-evidence handling without enabling production mining.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add watchdog-friendly bounded step supervision</name>
  <files>crates/bitaxe-safety/src/watchdog.rs, crates/bitaxe-safety/src/self_test.rs, crates/bitaxe-safety/fixtures/safety/watchdog-step-cases.json</files>
  <read_first>
    <file>crates/bitaxe-safety/src/watchdog.rs</file>
    <file>crates/bitaxe-safety/src/self_test.rs</file>
    <file>reference/esp-miner/main/self_test/self_test.c</file>
    <file>reference/esp-miner/main/tasks/fan_controller_task.c</file>
    <file>reference/esp-miner/main/tasks/power_management_task.c</file>
    <file>.planning/phases/06-safety-controllers-and-self-test/06-RESEARCH.md</file>
  </read_first>
  <behavior>
    - Test 1: a safety step with elapsed work at or below `25 ms` can continue without yielding.
    - Test 2: elapsed work greater than `25 ms` or accumulated elapsed time at or above `100 ms` returns a watchdog yield/reset decision.
    - Test 3: self-test `Step` uses the supervisor and never models an unbounded blocking loop.
  </behavior>
  <action>In `watchdog.rs`, define constants `SAFETY_STEP_BUDGET_MS: u32 = 25`, `WATCHDOG_YIELD_INTERVAL_MS: u32 = 100`, and `MAX_CONSECUTIVE_STEPS_BEFORE_YIELD: u8 = 4`. Implement `StepKind::{Power, Thermal, Fan, SelfTest, Telemetry}`, `StepProgress { kind, elapsed_ms, consecutive_steps }`, `WatchdogDecision::{Continue, YieldNow { reason }, ResetOrFeedWatchdog { reason }}`, and `StepSupervisor::decision(progress)`. Use reason strings exactly `step_budget_exceeded`, `yield_interval_reached`, and `consecutive_step_limit_reached`. Update `self_test.rs` so `SelfTestCommand::Step` accepts or creates a `StepProgress` and surfaces the `WatchdogDecision` through `SelfTestDecision`. Add `watchdog-step-cases.json` with power, thermal, fan, self-test, and telemetry cases; include requirement ID `SAFE-09`, source references to upstream 100 ms task loops, and the pinned reference commit.</action>
  <acceptance_criteria>
    - `rg -n "SAFETY_STEP_BUDGET_MS: u32 = 25|WATCHDOG_YIELD_INTERVAL_MS: u32 = 100|MAX_CONSECUTIVE_STEPS_BEFORE_YIELD: u8 = 4" crates/bitaxe-safety/src/watchdog.rs` finds all watchdog constants.
    - `rg -n "StepKind|StepProgress|WatchdogDecision|StepSupervisor|step_budget_exceeded|yield_interval_reached|consecutive_step_limit_reached" crates/bitaxe-safety/src/watchdog.rs` finds supervision types and reasons.
    - `rg -n "WatchdogDecision|SelfTestCommand::Step|StepProgress" crates/bitaxe-safety/src/self_test.rs` shows self-test uses bounded step supervision.
    - `rg -n "SAFE-09|Power|Thermal|Fan|SelfTest|Telemetry|100 ms|c1915b0a63bfabebdb95a515cedfee05146c1d50" crates/bitaxe-safety/fixtures/safety/watchdog-step-cases.json` finds fixture coverage.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p bitaxe-safety --all-features watchdog && cargo test -p bitaxe-safety --all-features self_test && bazel test //crates/bitaxe-safety:tests --test_filter='watchdog|self_test'</automated>
  </verify>
  <done>Safety and self-test work is represented as bounded steps with explicit watchdog/yield decisions, not blocking upstream-style task loops.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Factory flag / boot button / API command -> self-test lifecycle | User, stored, or input-triggered self-test requests can drive hardware-sensitive diagnostics. |
| Self-test / safety step -> watchdog responsiveness | Long-running work can starve HTTP/WebSocket/log/mining safety status if modeled as blocking loops. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Elevation of Privilege | `SelfTestLifecycle::apply` | mitigate | Require safety, power, thermal, ASIC, and hardware-evidence acknowledgments for diagnostic hardware submodes; production mining remains blocked. |
| T-06-05-02 | Tampering | Factory flag clearing | mitigate | Clear factory flag only on modeled successful or canceled factory lifecycle; fail records result but does not silently clear. |
| T-06-05-03 | Denial of Service | `StepSupervisor` | mitigate | Enforce 25 ms step budget, 100 ms yield interval, and 4-step consecutive limit with unit tests. |
| T-06-05-04 | Repudiation | Self-test result reporting | mitigate | Emit typed `RecordResult` and visible `SafetyStatus` effects for pass, fail, cancel, and restart. |
</threat_model>

<verification>
Run `cargo test -p bitaxe-safety --all-features self_test`, `cargo test -p bitaxe-safety --all-features watchdog`, and `bazel test //crates/bitaxe-safety:tests`. Run `just test && just parity` after Wave 3 completes. Before any commit, also run the repo Rust gate from `AGENTS.md`: `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`.
</verification>

<success_criteria>
Self-test behavior is explicit, stepwise, cancellable, restartable, result-reporting, factory-flag-aware, and production-mining-blocked unless all diagnostic safety and hardware gates are present.
</success_criteria>

<output>
After completion, create `.planning/phases/06-safety-controllers-and-self-test/06-05-SUMMARY.md`.
</output>
