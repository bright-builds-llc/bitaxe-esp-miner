---
phase: 06-safety-controllers-and-self-test
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - crates/bitaxe-safety/src/lib.rs
  - crates/bitaxe-safety/src/power.rs
  - crates/bitaxe-safety/src/thermal.rs
  - crates/bitaxe-safety/src/fault.rs
  - crates/bitaxe-safety/src/self_test.rs
  - crates/bitaxe-safety/src/watchdog.rs
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-03, SAFE-04, SAFE-05, SAFE-07, SAFE-08, SAFE-09]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-06-28T02-37-37
generated_at: 2026-06-28T03:30:00Z
must_haves:
  truths:
    - "Power, thermal, fault, self-test, and watchdog modules exist before parallel feature plans add behavior."
    - "Feature module boundaries are pure, breadcrumbed, and free of firmware side effects."
    - "Downstream executors can import stable module paths without editing the crate facade in parallel."
  artifacts:
    - path: "crates/bitaxe-safety/src/lib.rs"
      provides: "Public safety crate module graph"
      exports: ["effects", "evidence", "status", "power", "thermal", "fault", "self_test", "watchdog"]
    - path: "crates/bitaxe-safety/src/power.rs"
      provides: "Power/voltage/current module boundary"
    - path: "crates/bitaxe-safety/src/thermal.rs"
      provides: "Thermal/fan/PID module boundary"
    - path: "crates/bitaxe-safety/src/fault.rs"
      provides: "Fault-policy module boundary"
    - path: "crates/bitaxe-safety/src/self_test.rs"
      provides: "Self-test lifecycle module boundary"
    - path: "crates/bitaxe-safety/src/watchdog.rs"
      provides: "Watchdog supervision module boundary"
  key_links:
    - from: "crates/bitaxe-safety/src/lib.rs"
      to: "crates/bitaxe-safety/src/power.rs"
      via: "public module export"
      pattern: "pub mod power"
    - from: "crates/bitaxe-safety/src/lib.rs"
      to: "crates/bitaxe-safety/src/watchdog.rs"
      via: "public module export"
      pattern: "pub mod watchdog"
---

<objective>
Create the safety crate module graph that enables power, thermal, fault, self-test, and watchdog plans to run without concurrent edits to `lib.rs`.

Purpose: This keeps the foundational crate plan small while preserving stable module paths for the Phase 6 feature slices.
Output: Pure module boundary files with reference breadcrumbs and import tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-safety-controllers-and-self-test/06-CONTEXT.md
@.planning/phases/06-safety-controllers-and-self-test/06-RESEARCH.md
@.planning/phases/06-safety-controllers-and-self-test/06-VALIDATION.md
@.planning/phases/06-safety-controllers-and-self-test/06-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@crates/bitaxe-safety/src/lib.rs
@reference/esp-miner/main/power/DS4432U.c
@reference/esp-miner/main/power/INA260.c
@reference/esp-miner/main/thermal/thermal.c
@reference/esp-miner/main/thermal/PID.c
@reference/esp-miner/main/tasks/fan_controller_task.c
@reference/esp-miner/main/tasks/power_management_task.c
@reference/esp-miner/main/self_test/self_test.c

<interfaces>
From Plan 06-01:
```rust
pub mod effects;
pub mod evidence;
pub mod status;
pub const fn phase06_contract_name() -> &'static str;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 6 feature module boundaries</name>
  <files>crates/bitaxe-safety/src/lib.rs, crates/bitaxe-safety/src/power.rs, crates/bitaxe-safety/src/thermal.rs, crates/bitaxe-safety/src/fault.rs, crates/bitaxe-safety/src/self_test.rs, crates/bitaxe-safety/src/watchdog.rs</files>
  <read_first>
    <file>crates/bitaxe-safety/src/lib.rs</file>
    <file>.planning/phases/06-safety-controllers-and-self-test/06-CONTEXT.md</file>
    <file>.planning/phases/06-safety-controllers-and-self-test/06-RESEARCH.md</file>
  </read_first>
  <behavior>
    - Test 1: public modules `power`, `thermal`, `fault`, `self_test`, and `watchdog` are importable from `bitaxe_safety`.
    - Test 2: module boundary files compile on the host target without ESP-IDF, UART, GPIO, I2C, PWM, networking, sleep loops, or firmware imports.
    - Test 3: each module boundary file contains a module-level breadcrumb to the relevant pinned upstream reference files from 06-RESEARCH.
  </behavior>
  <action>Update `src/lib.rs` to export `power`, `thermal`, `fault`, `self_test`, and `watchdog` in addition to the Plan 06-01 shared modules. Create `power.rs`, `thermal.rs`, `fault.rs`, `self_test.rs`, and `watchdog.rs` as pure module boundary files with module-level breadcrumbs to the corresponding upstream files named in 06-RESEARCH. Add import/contract tests named `safety_module_graph` that prove the modules are public and that no module boundary imports `esp_idf`, `I2cDriver`, `PinDriver`, `UartDriver`, PWM drivers, networking sockets, or `std::thread::sleep`. This split preserves the full decisions: Plan 06-03 owns power/voltage/current behavior, Plan 06-04 owns thermal/fan/fault behavior, and Plan 06-05 owns self-test/watchdog behavior.</action>
  <acceptance_criteria>
    - `rg -n "pub mod power|pub mod thermal|pub mod fault|pub mod self_test|pub mod watchdog" crates/bitaxe-safety/src/lib.rs` finds all feature module exports.
    - `rg -n "DS4432U|INA260|thermal\\.c|PID\\.c|fan_controller_task|power_management_task|self_test\\.c" crates/bitaxe-safety/src/power.rs crates/bitaxe-safety/src/thermal.rs crates/bitaxe-safety/src/fault.rs crates/bitaxe-safety/src/self_test.rs crates/bitaxe-safety/src/watchdog.rs` finds reference breadcrumbs.
    - `! rg -n "esp_idf|I2cDriver|PinDriver|UartDriver|std::thread::sleep|TcpStream|UdpSocket" crates/bitaxe-safety/src` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p bitaxe-safety --all-features safety_module_graph && bazel test //crates/bitaxe-safety:tests --test_filter=safety_module_graph</automated>
  </verify>
  <done>The safety crate exposes stable pure module paths for Phase 6 feature plans without introducing hardware or firmware effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Safety crate facade -> downstream feature plans | Public module paths determine where later safety behavior is implemented. |
| Reference breadcrumbs -> provenance claims | Breadcrumbs can be mistaken for copied implementation if not scoped clearly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Elevation of Privilege | Feature module boundaries | mitigate | Keep module boundary files free of firmware, ESP-IDF, I2C, UART, GPIO, PWM, networking, and sleep-loop imports. |
| T-06-02-02 | Repudiation | Reference breadcrumbs | mitigate | Add module-level breadcrumbs only; feature behavior and fixtures are implemented in later plans with their own evidence. |
| T-06-02-03 | Tampering | `src/lib.rs` module graph | mitigate | Add focused import tests so downstream plans do not need concurrent facade edits. |
</threat_model>

<verification>
Run `cargo test -p bitaxe-safety --all-features safety_module_graph` and `bazel test //crates/bitaxe-safety:tests --test_filter=safety_module_graph`. Before any commit, also run the repo Rust gate from `AGENTS.md`: `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`.
</verification>

<success_criteria>
The safety crate has a complete pure module graph for Phase 6 feature work, all module boundary files are importable and breadcrumbed, and no boundary file can perform hardware, firmware, network, or blocking-loop effects.
</success_criteria>

<output>
After completion, create `.planning/phases/06-safety-controllers-and-self-test/06-02-SUMMARY.md`.
</output>
