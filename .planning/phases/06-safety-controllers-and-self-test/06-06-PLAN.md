---
phase: 06-safety-controllers-and-self-test
plan: "06"
type: execute
wave: 4
depends_on: ["06-03", "06-04", "06-05"]
files_modified:
  - Cargo.lock
  - MODULE.bazel.lock
  - crates/bitaxe-asic/Cargo.toml
  - crates/bitaxe-asic/BUILD.bazel
  - crates/bitaxe-asic/src/bm1366/init_plan.rs
  - crates/bitaxe-stratum/Cargo.toml
  - crates/bitaxe-stratum/BUILD.bazel
  - crates/bitaxe-stratum/src/v1/mining_loop.rs
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-04, SAFE-08, SAFE-09]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-06-28T02-37-37
generated_at: 2026-06-28T03:30:00Z
must_haves:
  truths:
    - "BM1366 full-init preflight consumes power, thermal, and safety-core-derived evidence tokens."
    - "Stratum work submission requires power evidence, thermal evidence, safety evidence/status, and hardware-evidence acknowledgment."
    - "Missing power evidence and missing thermal evidence each block mining work submission in concrete tests."
  artifacts:
    - path: "crates/bitaxe-asic/src/bm1366/init_plan.rs"
      provides: "BM1366 preflight evidence backed by safety crate tokens"
      contains: "from_power_token"
    - path: "crates/bitaxe-stratum/src/v1/mining_loop.rs"
      provides: "Mining loop safety gate backed by power, thermal, and safety tokens"
      contains: "maybe_power_evidence"
  key_links:
    - from: "crates/bitaxe-asic/src/bm1366/init_plan.rs"
      to: "crates/bitaxe-safety/src/power.rs"
      via: "PowerEvidenceToken"
      pattern: "PowerEvidenceToken"
    - from: "crates/bitaxe-asic/src/bm1366/init_plan.rs"
      to: "crates/bitaxe-safety/src/thermal.rs"
      via: "ThermalEvidenceToken"
      pattern: "ThermalEvidenceToken"
    - from: "crates/bitaxe-stratum/src/v1/mining_loop.rs"
      to: "crates/bitaxe-safety/src/evidence.rs"
      via: "SafetyCriticalEvidence"
      pattern: "SafetyCriticalEvidence"
---

<objective>
Wire the safety core into BM1366 full-init and Stratum mining-loop gates.

Purpose: D-18 requires power, thermal, and safety evidence tokens to keep ASIC initialization and work submission closed until every safety precondition exists.
Output: ASIC and mining gate constructors/tests that consume safety-core evidence and fail closed for missing power, thermal, safety, or hardware acknowledgment.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-safety-controllers-and-self-test/06-CONTEXT.md
@.planning/phases/06-safety-controllers-and-self-test/06-RESEARCH.md
@.planning/phases/06-safety-controllers-and-self-test/06-VALIDATION.md
@.planning/phases/06-safety-controllers-and-self-test/06-03-SUMMARY.md
@.planning/phases/06-safety-controllers-and-self-test/06-04-SUMMARY.md
@.planning/phases/06-safety-controllers-and-self-test/06-05-SUMMARY.md
@crates/bitaxe-asic/src/bm1366/init_plan.rs
@crates/bitaxe-stratum/src/v1/mining_loop.rs
@crates/bitaxe-safety/src/power.rs
@crates/bitaxe-safety/src/thermal.rs
@crates/bitaxe-safety/src/fault.rs
@crates/bitaxe-safety/src/evidence.rs
@crates/bitaxe-safety/src/status.rs

<interfaces>
Existing gate constants:
```rust
pub const HARDWARE_EVIDENCE_ACK_MISSING: &str = "hardware_evidence_ack_missing";
pub const SAFETY_PREFLIGHT_EVIDENCE_MISSING: &str = "safety_preflight_evidence_missing";
```

Required D-18 mining gate shape:
```rust
pub const POWER_PREFLIGHT_EVIDENCE_MISSING: &str = "power_preflight_evidence_missing";
pub const THERMAL_PREFLIGHT_EVIDENCE_MISSING: &str = "thermal_preflight_evidence_missing";

pub struct MiningLoopGate {
    pub asic_initialized: bool,
    pub maybe_power_evidence: Option<PowerEvidenceToken>,
    pub maybe_thermal_evidence: Option<ThermalEvidenceToken>,
    pub maybe_safety_evidence: Option<SafetyCriticalEvidence>,
    pub safety_status: SafetyStatus,
    pub hardware_evidence_ack: bool,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Feed power, thermal, and safety evidence into ASIC and mining gates</name>
  <files>Cargo.lock, MODULE.bazel.lock, crates/bitaxe-asic/Cargo.toml, crates/bitaxe-asic/BUILD.bazel, crates/bitaxe-asic/src/bm1366/init_plan.rs, crates/bitaxe-stratum/Cargo.toml, crates/bitaxe-stratum/BUILD.bazel, crates/bitaxe-stratum/src/v1/mining_loop.rs</files>
  <read_first>
    <file>crates/bitaxe-asic/src/bm1366/init_plan.rs</file>
    <file>crates/bitaxe-stratum/src/v1/mining_loop.rs</file>
    <file>crates/bitaxe-safety/src/evidence.rs</file>
    <file>crates/bitaxe-safety/src/power.rs</file>
    <file>crates/bitaxe-safety/src/thermal.rs</file>
    <file>crates/bitaxe-safety/src/fault.rs</file>
    <file>.planning/phases/06-safety-controllers-and-self-test/06-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Test 1: BM1366 full init without power, thermal, and safety tokens fails with `power_thermal_evidence_missing` or `safety_preflight_evidence_missing`.
    - Test 2: Stratum work submission with missing power evidence blocks with `power_preflight_evidence_missing`.
    - Test 3: Stratum work submission with missing thermal evidence blocks with `thermal_preflight_evidence_missing`.
    - Test 4: Stratum work submission with missing/faulted safety evidence or missing hardware evidence acknowledgment remains blocked with the existing safety/hardware reason strings.
  </behavior>
  <action>Add `bitaxe-safety = { path = "../bitaxe-safety" }` to `crates/bitaxe-asic/Cargo.toml` and `crates/bitaxe-stratum/Cargo.toml`, and add `//crates/bitaxe-safety:bitaxe_safety` to both Bazel libraries. In `crates/bitaxe-asic/src/bm1366/init_plan.rs`, replace empty evidence token internals with safety-backed constructors while preserving public names: `PowerPreflightEvidence::from_power_token(PowerEvidenceToken)`, `ThermalPreflightEvidence::from_thermal_token(ThermalEvidenceToken)`, and `SafetyPreflightEvidence::from_safety_status(SafetyCriticalEvidence, SafetyStatus)`. Remove any public no-context `present()` constructor or make it test-only under `#[cfg(test)]`; production callers must pass safety-core-derived tokens. Keep fail-closed reasons exactly `power_thermal_evidence_missing` and `safety_preflight_evidence_missing` for missing or unsafe ASIC preflight tokens. In `crates/bitaxe-stratum/src/v1/mining_loop.rs`, replace the boolean `safety_evidence` field with D-18 token requirements: `maybe_power_evidence: Option<PowerEvidenceToken>`, `maybe_thermal_evidence: Option<ThermalEvidenceToken>`, `maybe_safety_evidence: Option<SafetyCriticalEvidence>`, `safety_status: SafetyStatus`, and `hardware_evidence_ack`. Add reason constants `POWER_PREFLIGHT_EVIDENCE_MISSING = "power_preflight_evidence_missing"` and `THERMAL_PREFLIGHT_EVIDENCE_MISSING = "thermal_preflight_evidence_missing"`. `MiningLoopGate::decision()` must block missing power before safety, missing thermal before safety, missing/faulted safety evidence with `SAFETY_PREFLIGHT_EVIDENCE_MISSING`, and false hardware ack with `HARDWARE_EVIDENCE_ACK_MISSING`. Update tests and refresh `Cargo.lock`/`MODULE.bazel.lock` if Cargo/Bazel dependency metadata changes. This implements D-18 without letting unit evidence count as hardware verification.</action>
  <acceptance_criteria>
    - `rg -n "bitaxe-safety" crates/bitaxe-asic/Cargo.toml crates/bitaxe-stratum/Cargo.toml crates/bitaxe-asic/BUILD.bazel crates/bitaxe-stratum/BUILD.bazel` finds new dependency edges.
    - `rg -n "from_power_token|from_thermal_token|from_safety_status|PowerEvidenceToken|ThermalEvidenceToken|SafetyCriticalEvidence" crates/bitaxe-asic/src/bm1366/init_plan.rs` finds safety-backed preflight constructors.
    - `rg -n "maybe_power_evidence|maybe_thermal_evidence|maybe_safety_evidence|POWER_PREFLIGHT_EVIDENCE_MISSING|THERMAL_PREFLIGHT_EVIDENCE_MISSING|SAFETY_PREFLIGHT_EVIDENCE_MISSING|HARDWARE_EVIDENCE_ACK_MISSING" crates/bitaxe-stratum/src/v1/mining_loop.rs` finds the D-18 mining gate shape and reasons.
    - `rg -n "missing_power|power_preflight_evidence_missing|missing_thermal|thermal_preflight_evidence_missing" crates/bitaxe-stratum/src/v1/mining_loop.rs` finds concrete missing-token mining gate tests.
    - `! rg -n "pub const fn present\\(\\)" crates/bitaxe-asic/src/bm1366/init_plan.rs` returns no production public no-context evidence constructor matches.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p bitaxe-asic --all-features init_plan && cargo test -p bitaxe-stratum --all-features mining_loop && bazel test //crates/bitaxe-asic:tests //crates/bitaxe-stratum:tests --test_filter='init_plan|mining_loop'</automated>
  </verify>
  <done>BM1366 init and Stratum mining readiness are driven by power, thermal, and safety-core evidence tokens and remain fail-closed for missing, unsafe, or unacknowledged evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Safety core -> ASIC init/mining gates | Pure evidence tokens decide whether hardware-sensitive init and work submission may proceed. |
| Hardware evidence acknowledgment -> mining readiness | Acknowledgment must not be conflated with unit-tested implementation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Elevation of Privilege | `MiningLoopGate` | mitigate | Require separate power, thermal, and safety evidence tokens plus hardware ack before work submission can become ready. |
| T-06-06-02 | Tampering | `Bm1366Preflight` | mitigate | Remove production public no-context evidence constructors and require safety-core-derived tokens. |
| T-06-06-03 | Spoofing | `SafetyCriticalEvidence` use in mining gate | mitigate | Missing/faulted safety evidence blocks work submission and unit evidence does not count as hardware verification. |
| T-06-06-04 | Denial of Service | Missing token handling | mitigate | Missing power and thermal tokens fail fast with deterministic reason strings instead of falling through to ambiguous safety state. |
</threat_model>

<verification>
Run scoped tests for `bitaxe-asic` and `bitaxe-stratum`, then `just test && just parity` after the plan. Before any commit, also run the repo Rust gate from `AGENTS.md`: `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`.
</verification>

<success_criteria>
Phase 6 power, thermal, and safety tokens feed BM1366 and mining gates; missing power, missing thermal, missing/faulted safety, or missing hardware acknowledgment blocks ASIC initialization or work submission with explicit reason strings.
</success_criteria>

<output>
After completion, create `.planning/phases/06-safety-controllers-and-self-test/06-06-SUMMARY.md`.
</output>
