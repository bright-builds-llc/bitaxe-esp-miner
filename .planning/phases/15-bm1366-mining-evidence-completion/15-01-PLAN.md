---
phase: 15-bm1366-mining-evidence-completion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/parity/src/mining_allow.rs
  - tools/parity/src/main.rs
  - tools/parity/BUILD.bazel
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/README.md
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/redaction-review.md
autonomous: true
requirements:
  - STR-07
  - SAFE-09
  - EVD-05
must_haves:
  truths:
    - "Phase 15 ASIC/mining evidence-producing procedures cannot run without a machine-validated mining allow manifest"
    - "Phase 15 evidence packs define exact claim tiers and redaction status before generated logs are cited"
    - "Missing detector, board-info, package identity, recovery, safe-stop, telemetry, or redaction prerequisites produce pending evidence instead of checklist promotion"
  artifacts:
    - path: "tools/parity/src/mining_allow.rs"
      provides: "Typed Phase 15 mining allow-manifest parser and validator"
      contains: "MiningAllowManifest"
    - path: "docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/README.md"
      provides: "Phase 15 evidence ladder, required metadata, and stop conditions"
      contains: "Evidence Ladder"
    - path: "docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/redaction-review.md"
      provides: "Artifact-specific secret redaction review template"
      contains: "pool credentials"
  key_links:
    - from: "scripts/phase15-*.sh"
      to: "tools/parity:report mining-allow"
      via: "procedure manifest validation before evidence-producing hardware commands"
      pattern: "mining-allow --manifest"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-07-01T02-07-59
generated_at: 2026-07-01T02:36:53Z
---

<objective>
Create the Phase 15 mining evidence gate and evidence-pack scaffold before any BM1366 or mining evidence artifact is cited.

Purpose: D-09, D-13, D-14, D-15, and D-16 require a procedure-scoped evidence ladder, exact-claim promotion, machine-checkable preflight for active diagnostic/mining procedures, and redaction review before generated evidence is committed.
Output: `tools/parity` gains a tested `mining-allow` subcommand and Phase 15 gets an evidence README plus redaction review template.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/verification.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-bm1366-mining-evidence-completion/15-CONTEXT.md
@.planning/phases/15-bm1366-mining-evidence-completion/15-RESEARCH.md
@.planning/phases/15-bm1366-mining-evidence-completion/15-VALIDATION.md
@tools/parity/src/main.rs
@tools/parity/src/safety_allow.rs
@tools/parity/BUILD.bazel
@docs/parity/checklist.md

<interfaces>
From `tools/parity/src/main.rs`:
```rust
#[derive(Debug, Subcommand)]
enum CliCommand {
    Report(ReportArgs),
    ApiCompare(ApiCompareArgs),
    ReleaseGate(ReleaseGateArgs),
    SafetyAllow(SafetyAllowArgs),
}
```

From `tools/parity/src/safety_allow.rs`:
```rust
pub(crate) struct SafetyAllowFilters {
    pub(crate) maybe_surface: Option<String>,
    pub(crate) maybe_allowed_command: Option<String>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the `mining-allow` manifest gate</name>
  <requirements>STR-07, SAFE-09, EVD-05</requirements>
  <files>tools/parity/src/mining_allow.rs, tools/parity/src/main.rs, tools/parity/BUILD.bazel</files>
  <read_first>
    <file>tools/parity/src/main.rs</file>
    <file>tools/parity/src/safety_allow.rs</file>
    <file>tools/parity/BUILD.bazel</file>
    <file>.planning/phases/15-bm1366-mining-evidence-completion/15-CONTEXT.md</file>
    <file>.planning/phases/15-bm1366-mining-evidence-completion/15-VALIDATION.md</file>
  </read_first>
  <behavior>
    - Test 1: board `601` is rejected with `board must be 205`.
    - Test 2: `detector_command` other than `just detect-ultra205` is rejected with `detector command must be just detect-ultra205`.
    - Test 3: `detector_port` different from `port` is rejected with `detector port mismatch`.
    - Test 4: `board_info_status` other than `passed` is rejected with `board-info must pass`.
    - Test 5: package manifest `source_commit` or `reference_commit` mismatch is rejected with `package identity mismatch`.
    - Test 6: unsupported `surface` values are rejected; accepted surfaces are exactly `bm1366-chip-detect`, `bm1366-work-result`, `mining-smoke`, `bounded-soak`, and `parity-redaction`.
    - Test 7: `live-pool-smoke` requires `allowed_inputs.pool_config` equal to `disposable-or-non-secret` and `allowed_inputs.device_url` equal to `explicit`.
    - Test 8: `bounded-soak` requires `duration_seconds` between `60` and `600` inclusive, abort conditions, recovery steps, and post-action safe-state markers.
  </behavior>
  <action>Create `tools/parity/src/mining_allow.rs` patterned after `safety_allow.rs`, but with mining-specific names and surfaces per D-09 through D-15. Define `MiningAllowManifest` with these exact JSON fields: `board`, `port`, `detector_command`, `detector_port`, `board_info_command`, `board_info_status`, `package_manifest`, `source_commit`, `reference_commit`, `surface`, `claim_tier`, `evidence_class`, `allowed_command`, `allowed_inputs`, `abort_conditions`, `recovery_steps`, `post_action_safe_state_markers`, `prerequisite_artifacts`, `evidence_dir`, `redaction_reviewer`, and `checklist_rows`. Use `serde::Deserialize`, `serde_json::Value`, and `camino::Utf8PathBuf`; do not add dependencies. Accept claim tiers exactly `diagnostic-chip-detect`, `diagnostic-work-result`, `controlled-no-share`, `live-pool-smoke`, `bounded-soak`, `unsupported-pending`, and `parity-redaction`. Require `hardware-smoke` for diagnostic and smoke tiers, `soak` for `bounded-soak`, and `workflow` for `unsupported-pending` and `parity-redaction`. Require abort conditions containing `detector_mismatch`, `board_info_failure`, `missing_trusted_wrapper_markers`, `redaction_uncertainty`, `unsafe_temperature_or_power`, and `watchdog_unresponsive`. Require post-action safe-state markers containing `safe_state: mining=disabled`, `hardware_control=disabled`, and `work_submission=disabled`. Add `mod mining_allow;`, a `MiningAllow(MiningAllowArgs)` CLI variant, `--manifest`, optional `--surface`, and optional `--allowed-command` filters to `tools/parity/src/main.rs`. Register `src/mining_allow.rs` in `tools/parity/BUILD.bazel`. Do not weaken existing `safety-allow`, release-gate, or checklist guard behavior.</action>
  <verify>
    <automated>cargo test -p bitaxe-parity --all-features mining_allow</automated>
    <automated>bazel test //tools/parity:tests --test_filter=mining_allow</automated>
    <automated>just parity</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`tools/parity/src/mining_allow.rs` contains `struct MiningAllowManifest` and every field named in the action.</criterion>
    <criterion>`tools/parity/src/main.rs` contains `MiningAllow` and `run_mining_allow_command`.</criterion>
    <criterion>`tools/parity/BUILD.bazel` contains `src/mining_allow.rs`.</criterion>
    <criterion>`cargo test -p bitaxe-parity --all-features mining_allow` exits 0.</criterion>
    <criterion>`just parity` exits 0 after adding the new subcommand.</criterion>
  </acceptance_criteria>
  <done>Phase 15 has a machine-checkable mining evidence allow gate before active diagnostic or mining wrappers are added.</done>
</task>

<task type="auto">
  <name>Task 2: Create the Phase 15 evidence scaffold and redaction template</name>
  <requirements>STR-07, SAFE-09, EVD-05</requirements>
  <files>docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/README.md, docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/redaction-review.md</files>
  <read_first>
    <file>docs/parity/evidence/phase-14-safety-hardware-evidence-completion/README.md</file>
    <file>docs/parity/evidence/phase-14-safety-hardware-evidence-completion/redaction-review.md</file>
    <file>docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence.md</file>
    <file>.planning/phases/15-bm1366-mining-evidence-completion/15-CONTEXT.md</file>
    <file>.planning/phases/15-bm1366-mining-evidence-completion/15-RESEARCH.md</file>
  </read_first>
  <action>Create `docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/README.md` with these sections: `Scope`, `Required Hardware Gate`, `Mining Allow Manifest Contract`, `Evidence Ladder`, `Required Metadata For Every Pack`, `Component Packs`, `Stop Conditions`, `Prohibited Actions`, and `Checklist Promotion Rules`. The evidence ladder must list tiers exactly: `detector/package/safe boot`, `trusted BM1366 chip-detect or staged init`, `typed diagnostic work/result`, `controlled mining smoke`, `bounded mining soak`, and `checklist promotion`. The component packs must be exactly `bm1366-chip-detect`, `bm1366-work-result`, `mining-smoke`, `bounded-soak`, and `parity-redaction`. The README must state that every live run starts with `just detect-ultra205`, continues only for board `205` with successful `espflash board-info --chip esp32s3 --port <port> --non-interactive`, and records pending evidence on zero/multiple ports, board-info failure, non-205 target, missing recovery, missing telemetry prerequisites, or redaction uncertainty. Create `redaction-review.md` with unchecked review items for pool credentials, worker names/secrets, Wi-Fi credentials, private endpoints, private `DEVICE_URL`, API tokens, NVS secret values, local terminal secrets, serial logs, JSON artifacts, Markdown evidence, API responses, WebSocket captures, pasted output, and manual observations. Do not use standalone body `---` separators.</action>
  <verify>
    <automated>rg -n "Evidence Ladder|detector/package/safe boot|trusted BM1366 chip-detect|typed diagnostic work/result|controlled mining smoke|bounded mining soak|bm1366-chip-detect|bm1366-work-result|mining-smoke|bounded-soak|parity-redaction|pool credentials|DEVICE_URL|NVS secret" docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/README.md docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/redaction-review.md</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`README.md` names all five component packs and all six evidence ladder tiers.</criterion>
    <criterion>`README.md` includes `just detect-ultra205` and `espflash board-info --chip esp32s3 --port <port> --non-interactive`.</criterion>
    <criterion>`redaction-review.md` includes every secret category named in D-16.</criterion>
    <criterion>No standalone body separator line `---` exists after the YAML frontmatter delimiter in either file.</criterion>
  </acceptance_criteria>
  <done>Phase 15 executors have an evidence contract and artifact-specific redaction template before generated evidence is cited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| JSON allow manifest to hardware wrapper | Manifest data controls whether diagnostic or mining evidence-producing commands may run. |
| Package manifest to mining evidence | Package identity determines whether hardware evidence belongs to the cited firmware commit. |
| Generated artifacts to checklist promotion | Logs and evidence files determine whether ASIC/mining rows can be promoted. |
| Evidence artifacts to repository | Logs, API bodies, WebSocket frames, and terminal output may contain secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-15-01-01 | Tampering | `tools/parity/src/mining_allow.rs` | mitigate | Parse manifests into typed fields and reject non-205 boards, detector mismatch, board-info failure, package identity mismatch, unsupported surfaces, missing recovery, and missing safe-state markers. |
| T-15-01-02 | Elevation of Privilege | diagnostic and mining wrappers | mitigate | Require exact `allowed_command`, claim tier, evidence class, abort conditions, recovery steps, and post-action safe-state markers before wrappers proceed. |
| T-15-01-03 | Repudiation | evidence scaffold | mitigate | README requires board, port, commits, command, package manifest, observed behavior, conclusion, and redaction status for every pack. |
| T-15-01-04 | Information Disclosure | redaction review | mitigate | Redaction template explicitly covers pool credentials, worker secrets, Wi-Fi credentials, private endpoints, `DEVICE_URL`, API tokens, NVS secret values, and local terminal secrets before citation. |
</threat_model>

<verification>
Plan-level checks:
- `cargo test -p bitaxe-parity --all-features mining_allow`
- `bazel test //tools/parity:tests --test_filter=mining_allow`
- `just parity`
</verification>

<success_criteria>
Phase 15 has a tested mining allow gate plus evidence and redaction scaffolding that blocks unsupported hardware/mining claims before evidence collection begins.
</success_criteria>

<output>
After completion, create `.planning/phases/15-bm1366-mining-evidence-completion/15-01-SUMMARY.md`.
</output>
