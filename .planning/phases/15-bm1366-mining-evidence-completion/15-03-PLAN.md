---
phase: 15-bm1366-mining-evidence-completion
plan: "03"
type: execute
wave: 3
depends_on:
  - "15-02"
files_modified:
  - crates/bitaxe-asic/src/bm1366/adapter_gate.rs
  - firmware/bitaxe/src/asic_adapter.rs
  - firmware/bitaxe/src/asic_adapter/status.rs
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result.md
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result/detect-ultra205.log
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result/allow-work-result.json
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result/diagnostic-package-summary.json
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result/flash-command-evidence.json
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result/flash-monitor.log
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/redaction-review.md
autonomous: true
requirements:
  - ASIC-07
  - STR-06
  - SAFE-09
  - EVD-05
must_haves:
  truths:
    - "A repo-owned typed firmware diagnostic exercises BM1366 diagnostic work dispatch and result-or-timeout parsing without raw serial writes"
    - "Work/result evidence is bounded, non-mining, detector-gated, package-backed, and redaction-reviewed"
    - "The diagnostic never claims production mining, accepted shares, live pool parity, frequency transition, or voltage behavior"
  artifacts:
    - path: "crates/bitaxe-asic/src/bm1366/adapter_gate.rs"
      provides: "Compile-time gate for `work-result` diagnostic mode"
      contains: "WORK_RESULT_DIAGNOSTIC"
    - path: "firmware/bitaxe/src/asic_adapter.rs"
      provides: "Typed UART work/result diagnostic interpreter"
      contains: "run_work_result_diagnostic"
    - path: "docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result.md"
      provides: "Detector-gated work/result evidence summary"
      contains: "bm1366-work-result"
  key_links:
    - from: "firmware/bitaxe/src/asic_adapter.rs"
      to: "crates/bitaxe-asic/src/bm1366/work.rs"
      via: "diagnostic_job_frame"
      pattern: "diagnostic_job_frame"
    - from: "firmware/bitaxe/src/asic_adapter.rs"
      to: "crates/bitaxe-asic/src/bm1366/result.rs"
      via: "parse_bm1366_result_frame with Bm1366ValidJobIds"
      pattern: "parse_bm1366_result_frame"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-07-01T02-07-59
generated_at: 2026-07-01T02:36:53Z
---

<objective>
Add and run a bounded typed BM1366 work-send/result-receive diagnostic.

Purpose: D-05 through D-08 require repo-owned typed firmware diagnostics over the USB serial console, exercising existing BM1366 command, work, valid-job, result, and adapter behavior without raw serial writes or production mining claims.
Output: Firmware supports a compile-gated `work-result` diagnostic and Phase 15 records detector-gated work/result evidence or a conservative pending artifact.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/architecture.md
@standards/core/verification.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-bm1366-mining-evidence-completion/15-CONTEXT.md
@.planning/phases/15-bm1366-mining-evidence-completion/15-RESEARCH.md
@.planning/phases/15-bm1366-mining-evidence-completion/15-VALIDATION.md
@docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/README.md
@docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect.md
@docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/redaction-review.md
@scripts/phase15-bm1366-diagnostic-package.sh
@tools/parity/src/mining_allow.rs
@firmware/bitaxe/src/asic_adapter.rs
@firmware/bitaxe/src/asic_adapter/status.rs
@firmware/bitaxe/src/asic_adapter/uart.rs
@crates/bitaxe-asic/src/bm1366/adapter_gate.rs
@crates/bitaxe-asic/src/bm1366/work.rs
@crates/bitaxe-asic/src/bm1366/result.rs
@crates/bitaxe-stratum/src/v1/mining_loop.rs

<interfaces>
From `crates/bitaxe-asic/src/bm1366/work.rs`:
```rust
pub fn diagnostic_job_frame(
    job_id: Bm1366JobId,
    fields: Bm1366WorkFields,
) -> Result<DiagnosticWorkFrame, Bm1366ProtocolFault>;
```

From `crates/bitaxe-asic/src/bm1366/result.rs`:
```rust
pub fn parse_bm1366_result_frame(
    bytes: &[u8],
    valid_jobs: &Bm1366ValidJobIds,
    address_interval: u8,
) -> Result<Bm1366ParsedResult, Bm1366ProtocolFault>;
```

From `firmware/bitaxe/src/asic_adapter/uart.rs`:
```rust
pub const RESULT_WORK_TIMEOUT_MS: u32 = 10_000;
pub fn write_frame(&mut self, frame: &[u8]) -> Result<()>;
pub fn read_exact(&mut self, len: usize, timeout_ms: u32) -> Result<Vec<u8>>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the bounded typed work/result diagnostic mode</name>
  <requirements>ASIC-07, STR-06, SAFE-09, EVD-05</requirements>
  <files>crates/bitaxe-asic/src/bm1366/adapter_gate.rs, firmware/bitaxe/src/asic_adapter.rs, firmware/bitaxe/src/asic_adapter/status.rs</files>
  <read_first>
    <file>crates/bitaxe-asic/src/bm1366/adapter_gate.rs</file>
    <file>crates/bitaxe-asic/src/bm1366/work.rs</file>
    <file>crates/bitaxe-asic/src/bm1366/result.rs</file>
    <file>firmware/bitaxe/src/asic_adapter.rs</file>
    <file>firmware/bitaxe/src/asic_adapter/status.rs</file>
    <file>firmware/bitaxe/src/asic_adapter/uart.rs</file>
  </read_first>
  <behavior>
    - Test 1: `AsicAdapterMode::from_compile_env(Some("work-result"), Some("ultra205-work-result-safe-bench"))` returns `WorkResultDiagnostic`.
    - Test 2: `work-result` with a missing or wrong ack returns `FailClosed`.
    - Test 3: status helpers expose exact log markers `asic_work_result_diagnostic=started mining=disabled`, `bm1366_diagnostic_work=dispatched`, `bm1366_diagnostic_result=parsed`, `bm1366_diagnostic_result=timeout fail_closed=true`, and `bm1366_diagnostic_result=invalid fail_closed=true`.
    - Test 4: firmware diagnostic code calls `diagnostic_job_frame`, creates `Bm1366ValidJobIds::single(job_id)`, and calls `parse_bm1366_result_frame`.
  </behavior>
  <action>Extend `AsicAdapterMode` with `WorkResultDiagnostic`, constants `WORK_RESULT_DIAGNOSTIC: &str = "work-result"` and `WORK_RESULT_HARDWARE_EVIDENCE_ACK: &str = "ultra205-work-result-safe-bench"`, and focused tests. Update `firmware/bitaxe/src/asic_adapter.rs` so `run_boot_gate_with_peripherals` and `run_boot_gate_without_peripherals` handle `WorkResultDiagnostic` without changing the default `FailClosed` behavior. Implement a private `run_work_result_diagnostic` that initializes reset and UART through existing adapters, publishes `asic_work_result_diagnostic=started mining=disabled work_submission=disabled`, builds one diagnostic job using `Bm1366JobId::new(0x28)` and deterministic non-secret `Bm1366WorkFields`, writes the typed `diagnostic_job_frame` bytes through `uart.write_frame`, records `bm1366_diagnostic_work=dispatched job_id=0x28 bytes=88 mining=disabled`, registers `Bm1366ValidJobIds::single(job_id)`, reads exactly `BM1366_RESULT_FRAME_LEN` with `RESULT_WORK_TIMEOUT_MS`, and parses through `parse_bm1366_result_frame(&frame, &valid_jobs, 16)`. On success, log `bm1366_diagnostic_result=parsed job_id=0x28 mining=disabled work_submission=disabled`. On timeout, partial read, bad CRC, invalid job, invalid core, or any adapter error, hold reset low best-effort, publish fail-closed status, and log either `bm1366_diagnostic_result=timeout fail_closed=true mining=disabled work_submission=disabled` or `bm1366_diagnostic_result=invalid fail_closed=true mining=disabled work_submission=disabled error=<typed-or-adapter-error>`. Do not add HTTP diagnostic routes, raw serial writes, pool sockets, accepted share claims, voltage/fan behavior, or production mining enablement.</action>
  <verify>
    <automated>cargo test -p bitaxe-asic --all-features adapter_gate work result transcript</automated>
    <automated>bazel build //firmware/bitaxe:firmware</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`crates/bitaxe-asic/src/bm1366/adapter_gate.rs` contains `WORK_RESULT_DIAGNOSTIC`, `ultra205-work-result-safe-bench`, and `WorkResultDiagnostic`.</criterion>
    <criterion>`firmware/bitaxe/src/asic_adapter.rs` contains `run_work_result_diagnostic`, `diagnostic_job_frame`, `Bm1366ValidJobIds::single`, and `parse_bm1366_result_frame`.</criterion>
    <criterion>`firmware/bitaxe/src/asic_adapter.rs` does not contain raw hard-coded BM1366 hex frames or host serial writes.</criterion>
    <criterion>`bazel build //firmware/bitaxe:firmware` exits 0.</criterion>
  </acceptance_criteria>
  <done>Firmware has a compile-gated, bounded, typed BM1366 work/result diagnostic that stays non-mining by construction.</done>
</task>

<task type="auto">
  <name>Task 2: Capture typed work/result diagnostic evidence</name>
  <requirements>ASIC-07, STR-06, SAFE-09, EVD-05</requirements>
  <files>docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result.md, docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result/detect-ultra205.log, docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result/allow-work-result.json, docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result/diagnostic-package-summary.json, docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result/flash-command-evidence.json, docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result/flash-monitor.log, docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/redaction-review.md</files>
  <read_first>
    <file>docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/README.md</file>
    <file>docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect.md</file>
    <file>docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/redaction-review.md</file>
    <file>scripts/phase15-bm1366-diagnostic-package.sh</file>
    <file>tools/parity/src/mining_allow.rs</file>
    <file>tools/flash/src/main.rs</file>
    <file>AGENTS.md</file>
  </read_first>
  <action>Run this task's work/result hardware tier only after Plan 15-02 `chip-detect.md` contains the exact conclusion `passed for package-backed chip-detect smoke`. If `chip-detect.md` contains `hardware evidence pending - <blocker>` or lacks that exact passed conclusion, write `work-result.md` with conclusion `hardware evidence pending - chip-detect prerequisite not passed: <blocker-or-missing-passed-conclusion>`, update `redaction-review.md` for any generated pending artifact, and stop without flashing, running work/result hardware, or creating a hardware-smoke allow manifest. Do not treat pending chip-detect, unsafe chip-detect status, redaction uncertainty, missing restore, detector failure, or missing trusted wrapper markers as permission to run work/result hardware. After the exact chip-detect pass is present, start with `just detect-ultra205` and write the output to `docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result/detect-ultra205.log`; continue only for one selected `port=<path>` and passed board-info for board `205`. Build the package with `scripts/phase15-bm1366-diagnostic-package.sh --mode work-result --out-dir docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result`. Create `allow-work-result.json` with `surface` `bm1366-work-result`, `claim_tier` `diagnostic-work-result`, `evidence_class` `hardware-smoke`, actual board/port/commits/package, prerequisite artifact `docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect.md`, checklist rows `ASIC-003`, `ASIC-004`, `STR-006`, `SAFE-09`, and `EVD-05`, and `allowed_command` equal to the exact flash-monitor command. Validate it with `bazel run //tools/parity:report -- mining-allow --manifest docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result/allow-work-result.json --surface bm1366-work-result --allowed-command "bazel run //tools/flash:flash -- flash-monitor --board 205 --port <path> --manifest docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result/package/bitaxe-ultra205-package.json --evidence-dir docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result --capture-timeout-seconds 45"`. If allow validation passes, run that exact command with the actual port. Write `work-result.md` with commands, board, port, package manifest, source/reference commits, diagnostic mode, observed markers, result category, fail-closed state, safe-state evidence, redaction status, conclusion, and non-claims. Allowed result categories are `work packet dispatched`, `result frame parsed`, `bounded timeout with fail-closed state`, `invalid job rejected`, or `no result observed before timeout`. Allowed conclusions are `passed for diagnostic work/result smoke`, `passed for diagnostic work dispatch with bounded no-result`, or `hardware evidence pending - <exact blocker>`. Update `redaction-review.md` before commit for every generated work-result artifact. Do not claim accepted shares, live pool behavior, production mining, API/WebSocket telemetry, statistics producer behavior, frequency transition, voltage behavior, or fan behavior from this diagnostic.</action>
  <verify>
    <automated>rg -n "bm1366-work-result|work packet dispatched|result frame parsed|bounded timeout with fail-closed state|no result observed before timeout|non_claims|redaction" docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result.md docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/redaction-review.md</automated>
    <automated>just parity</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`work-result.md` records `board 205`, selected `port=`, source commit, reference commit, package manifest, exact flash-monitor command, observed markers, and conclusion.</criterion>
    <criterion>`work-result.md` contains one allowed result category from the action.</criterion>
    <criterion>If `chip-detect.md` lacks the exact conclusion `passed for package-backed chip-detect smoke`, `work-result.md` contains `hardware evidence pending - chip-detect prerequisite not passed` and no work/result flash-monitor or hardware-smoke allow-manifest action is run.</criterion>
    <criterion>`work-result.md` contains `non_claims: accepted shares, live pool behavior, production mining, API/WebSocket telemetry, statistics producer behavior, frequency transition, voltage behavior, fan behavior`.</criterion>
    <criterion>`redaction-review.md` marks the work-result component pack `passed` or `blocked` before task completion.</criterion>
    <criterion>`just parity` exits 0 with broad ASIC and mining claims still below `verified` unless exact evidence supports them.</criterion>
  </acceptance_criteria>
  <done>Typed work/result diagnostic evidence exists, or the blocker is recorded without unsafe mining or checklist overclaim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Compile-time diagnostic env to UART effects | `work-result` mode enables bounded ASIC UART writes and reads. |
| Typed ASIC core to firmware adapter | Firmware must use `bitaxe-asic` work/result types instead of raw frames. |
| Work/result serial log to mining claim | Diagnostic result-or-timeout evidence must not become production mining parity. |
| Generated artifacts to repository | Serial output and JSON evidence may contain bench identifiers or secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-15-03-01 | Tampering | `firmware/bitaxe/src/asic_adapter.rs` | mitigate | Build work frames via `diagnostic_job_frame`, track valid jobs with `Bm1366ValidJobIds`, and parse only through `parse_bm1366_result_frame`. |
| T-15-03-02 | Elevation of Privilege | `WorkResultDiagnostic` mode | mitigate | Require exact compile-time ack, detector gate, mining allow manifest, package identity, and no-mining markers; default remains `FailClosed`. |
| T-15-03-03 | Denial of Service | UART read loop | mitigate | Use `RESULT_WORK_TIMEOUT_MS`, fail closed on partial reads/timeouts/errors, and hold reset low best-effort. |
| T-15-03-04 | Repudiation | `work-result.md` | mitigate | Record command, board, port, commits, package manifest, result category, conclusion, and non-claims. |
| T-15-03-05 | Information Disclosure | work-result artifacts | mitigate | Redaction review must pass or block before commit; do not include pool, Wi-Fi, private endpoint, API token, NVS secret, or local terminal secret values. |
</threat_model>

<verification>
Plan-level checks:
- `cargo test -p bitaxe-asic --all-features adapter_gate work result transcript`
- `bazel build //firmware/bitaxe:firmware`
- `just parity`
- Hardware commands actually used must be recorded in `work-result.md`
</verification>

<success_criteria>
The firmware and evidence path prove bounded typed work/result behavior or record a clear blocker, with no raw serial writes and no production mining overclaim.
</success_criteria>

<output>
After completion, create `.planning/phases/15-bm1366-mining-evidence-completion/15-03-SUMMARY.md`.
</output>
