---
phase: 15-bm1366-mining-evidence-completion
plan: "04"
type: execute
wave: 4
depends_on:
  - "15-03"
files_modified:
  - scripts/phase15-controlled-mining.sh
  - scripts/phase15-controlled-mining-test.sh
  - scripts/phase15-websocket-capture.mjs
  - scripts/BUILD.bazel
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/mining-smoke.md
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/mining-smoke/allow-mining-smoke.json
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/mining-smoke/mining-smoke.log
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/bounded-soak.md
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/bounded-soak/allow-bounded-soak.json
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/bounded-soak/bounded-soak.log
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/redaction-review.md
autonomous: true
requirements:
  - STR-06
  - STR-07
  - SAFE-09
  - EVD-05
user_setup:
  - service: ultra205-network
    why: "Optional live API/WebSocket telemetry during controlled mining smoke or bounded soak"
    env_vars:
      - name: DEVICE_URL
        source: "Explicit reachable URL for the just-flashed Ultra 205; never infer or scan"
  - service: controlled-pool
    why: "Optional live pool micro-smoke using disposable or non-secret pool configuration"
    env_vars:
      - name: BITAXE_POOL_URL
        source: "Disposable or non-secret pool endpoint; redact before commit"
      - name: BITAXE_POOL_USER
        source: "Disposable or non-secret worker identifier; redact before commit"
      - name: BITAXE_POOL_PASSWORD
        source: "Disposable credential only; never commit"
must_haves:
  truths:
    - "Controlled mining smoke does not run unless detector, package, chip-detect, work/result, safety, recovery, telemetry, safe-stop, and redaction prerequisites are satisfied or recorded pending"
    - "Missing pool env or `DEVICE_URL` produces controlled no-share or pending evidence instead of live-pool overclaim"
    - "Bounded soak has duration, abort conditions, watchdog/API/WebSocket or blocker status, and final safe-stop or restore evidence"
  artifacts:
    - path: "scripts/phase15-controlled-mining.sh"
      provides: "Controlled mining smoke and bounded soak evidence wrapper"
      contains: "controlled_no_share_condition"
    - path: "docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/mining-smoke.md"
      provides: "Controlled mining smoke evidence or blocker"
      contains: "mining-smoke"
    - path: "docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/bounded-soak.md"
      provides: "Bounded soak evidence or blocker"
      contains: "bounded-soak"
  key_links:
    - from: "scripts/phase15-controlled-mining.sh"
      to: "tools/parity:report mining-allow"
      via: "manifest validation before smoke or soak"
      pattern: "mining-allow --manifest"
    - from: "scripts/phase15-controlled-mining.sh"
      to: "docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result.md"
      via: "work/result prerequisite check"
      pattern: "bm1366-work-result"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-07-01T02-07-59
generated_at: 2026-07-01T02:36:53Z
---

<objective>
Add and run the controlled mining smoke and bounded soak evidence path.

Purpose: D-09 through D-12 require a layered evidence ladder that prefers controlled no-share or deterministic evidence when live pool credentials, explicit `DEVICE_URL`, telemetry, safe-stop, or redaction prerequisites are missing.
Output: A tested controlled mining wrapper plus redaction-reviewed smoke and soak artifacts with exact claims or blockers.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/verification.md
@standards/core/testing.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-bm1366-mining-evidence-completion/15-CONTEXT.md
@.planning/phases/15-bm1366-mining-evidence-completion/15-RESEARCH.md
@.planning/phases/15-bm1366-mining-evidence-completion/15-VALIDATION.md
@docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/README.md
@docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect.md
@docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result.md
@docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/redaction-review.md
@scripts/BUILD.bazel
@scripts/phase14-live-telemetry.sh
@tools/parity/src/mining_allow.rs
@crates/bitaxe-stratum/src/v1/fake_pool.rs
@crates/bitaxe-stratum/src/v1/mining_loop.rs
@crates/bitaxe-stratum/src/v1/state.rs
@crates/bitaxe-api/src/mining.rs
@firmware/bitaxe/src/http_api.rs

<interfaces>
From `crates/bitaxe-stratum/src/v1/state.rs`:
```rust
pub struct MiningRuntimeState {
    pub lifecycle: PoolLifecycleStatus,
    pub counters: ShareCounters,
    pub fallback_active: bool,
    pub work_submission: WorkSubmissionGate,
    pub hashrate_inputs: HashrateInputs,
    pub mining_activity: MiningActivityStatus,
}
```

From `crates/bitaxe-api/src/mining.rs`:
```rust
pub fn mining_state_from_runtime(state: &MiningRuntimeState) -> MiningStateWire;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add controlled mining smoke and soak wrappers</name>
  <requirements>STR-06, STR-07, SAFE-09, EVD-05</requirements>
  <files>scripts/phase15-controlled-mining.sh, scripts/phase15-controlled-mining-test.sh, scripts/phase15-websocket-capture.mjs, scripts/BUILD.bazel</files>
  <read_first>
    <file>scripts/phase14-live-telemetry.sh</file>
    <file>scripts/phase14-live-telemetry-test.sh</file>
    <file>scripts/BUILD.bazel</file>
    <file>tools/parity/src/mining_allow.rs</file>
    <file>docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/README.md</file>
    <file>.planning/phases/15-bm1366-mining-evidence-completion/15-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Test 1: missing manifest records `controlled_mining_status: pending - missing manifest` and exits 0 without hardware commands.
    - Test 2: failed `mining-allow` records `controlled_mining_status: pending - allow validation failed`.
    - Test 3: missing `BITAXE_POOL_URL`, `BITAXE_POOL_USER`, `BITAXE_POOL_PASSWORD`, or `DEVICE_URL` records `controlled_no_share_condition=missing_live_prerequisites`.
    - Test 4: the wrapper logs `network_scan: disabled - DEVICE_URL must be explicit`.
    - Test 5: `--surface bounded-soak --duration-seconds 120` records `duration_seconds=120`, abort conditions, watchdog status, and safe-stop status.
    - Test 6: `scripts/phase15-websocket-capture.mjs` rejects missing or non-HTTP(S) `DEVICE_URL` and redacts URLs in output.
  </behavior>
  <action>Create `scripts/phase15-controlled-mining.sh` with `set -euo pipefail`, arguments `--manifest PATH`, `--out-dir PATH`, `--surface mining-smoke|bounded-soak`, `--chip-detect-summary PATH`, `--work-result-summary PATH`, optional `--device-url URL`, optional `--duration-seconds N`, optional `--curl-bin PATH`, and optional `--websocket-helper PATH`. The wrapper must validate `bazel run //tools/parity:report -- mining-allow --manifest <manifest> --surface <surface> --allowed-command <exact command>` before recording evidence. It must check the chip-detect and work-result summaries for successful or explicitly pending prerequisite conclusions; if an unsafe blocker, missing redaction, or missing restore marker exists, record pending evidence and do not run live smoke or soak. It must never scan the network, infer a target, run raw serial writes, erase, rollback, voltage/fan stress, or unbounded mining stress. When `DEVICE_URL` or any pool env is missing, record `pool_category=controlled-no-share`, `controlled_no_share_condition=missing_live_prerequisites`, `share_outcome=controlled no-share condition`, `api_telemetry_status=pending - missing DEVICE_URL` when applicable, `websocket_frame_status=pending - missing DEVICE_URL or helper blocked`, and `safe_stop_status=confirmed-or-pending` based on available markers. When all live prerequisites are present, log only sanitized pool category and redacted endpoint status; do not write raw pool URL, username, password, private endpoint, or device URL. Create `scripts/phase15-websocket-capture.mjs` using Node's global `WebSocket` to capture bounded `/api/ws/live` frames only when passed an explicit HTTP(S) `DEVICE_URL`; redact URLs, IPs, MAC addresses, pool fields, tokens, and credentials before writing output. Add `scripts/phase15-controlled-mining-test.sh` using fake `mining-allow`, fake summaries, fake curl, and fake WebSocket helper. Register `phase15_controlled_mining`, `phase15_controlled_mining_test`, and the Node helper as test data in `scripts/BUILD.bazel`.</action>
  <verify>
    <automated>bash -n scripts/phase15-controlled-mining.sh scripts/phase15-controlled-mining-test.sh</automated>
    <automated>bazel test //scripts:phase15_controlled_mining_test</automated>
    <automated>cargo test -p bitaxe-stratum --all-features mining_loop fake_pool queue</automated>
    <automated>cargo test -p bitaxe-api --all-features mining</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`scripts/phase15-controlled-mining.sh` contains `controlled_no_share_condition=missing_live_prerequisites` and `network_scan: disabled - DEVICE_URL must be explicit`.</criterion>
    <criterion>`scripts/phase15-controlled-mining.sh` validates `mining-allow --manifest` before smoke or soak output.</criterion>
    <criterion>`scripts/phase15-controlled-mining.sh` does not contain `erase-flash`, `rollback`, `write-bin`, raw serial write commands, voltage-write commands, or fan-duty commands.</criterion>
    <criterion>`bazel test //scripts:phase15_controlled_mining_test` exits 0.</criterion>
  </acceptance_criteria>
  <done>Phase 15 has controlled mining smoke and bounded soak wrappers that safely fall back to controlled no-share or pending evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Run controlled mining smoke and bounded soak evidence</name>
  <requirements>STR-06, STR-07, SAFE-09, EVD-05</requirements>
  <files>docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/mining-smoke.md, docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/mining-smoke/allow-mining-smoke.json, docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/mining-smoke/mining-smoke.log, docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/bounded-soak.md, docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/bounded-soak/allow-bounded-soak.json, docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/bounded-soak/bounded-soak.log, docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/redaction-review.md</files>
  <read_first>
    <file>scripts/phase15-controlled-mining.sh</file>
    <file>docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/README.md</file>
    <file>docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect.md</file>
    <file>docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result.md</file>
    <file>docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/redaction-review.md</file>
    <file>tools/parity/src/mining_allow.rs</file>
    <file>AGENTS.md</file>
  </read_first>
  <action>Run this task only after Plan 15-03 records a safe diagnostic work/result conclusion or a blocker that explicitly permits controlled no-share evidence. Start with `just detect-ultra205` and stop to pending evidence on zero/multiple ports, board-info failure, non-205 target, missing recovery, missing telemetry prerequisites, or redaction uncertainty. Create `mining-smoke/allow-mining-smoke.json` with `surface` `mining-smoke`, `claim_tier` `controlled-no-share` unless all live prerequisites are present, `evidence_class` `hardware-smoke`, actual board/port/commits/package, prerequisite artifacts `chip-detect.md` and `work-result.md`, checklist rows `STR-006`, `STR-007`, `STR-008`, `API-002`, `API-006`, `STAT-002`, `SAFE-09`, and `EVD-05`, and `allowed_command` equal to the exact smoke wrapper invocation. Run `scripts/phase15-controlled-mining.sh --manifest docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/mining-smoke/allow-mining-smoke.json --surface mining-smoke --out-dir docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/mining-smoke --chip-detect-summary docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect.md --work-result-summary docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result.md`. Pass `--device-url "$DEVICE_URL"` only when the environment variable is set. Do not pass or log raw pool values; the wrapper reads pool env only to determine whether live prerequisites exist and must redact. Write `mining-smoke.md` with pool category, share outcome, hashrate inputs status, API/WebSocket status or blocker, watchdog/serial responsiveness, safe-stop status, conclusion, non-claims, and redaction status. For bounded soak, create `bounded-soak/allow-bounded-soak.json` only after the smoke artifact records either `controlled_no_share_condition=missing_live_prerequisites` with safe-stop/pending-safe-stop status or a live smoke pass. Set `duration_seconds` to `120`, abort conditions to include `unsafe_temperature_or_power`, `watchdog_unresponsive`, `serial_silence`, `redaction_uncertainty`, and `missing_safe_stop`, and `claim_tier` to `bounded-soak` only when soak evidence actually runs; otherwise use `unsupported-pending`. Run `scripts/phase15-controlled-mining.sh --manifest docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/bounded-soak/allow-bounded-soak.json --surface bounded-soak --duration-seconds 120 --out-dir docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/bounded-soak --chip-detect-summary docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect.md --work-result-summary docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/work-result.md`. Write `bounded-soak.md` with duration, abort conditions, samples or blockers, watchdog/API/WebSocket/serial responsiveness, reconnect/fallback scope if exercised, final safe-stop or restore evidence, conclusion, and non-claims. Update `redaction-review.md` for smoke and soak artifacts before commit.</action>
  <verify>
    <automated>rg -n "mining-smoke|controlled no-share condition|pool_category|share_outcome|watchdog|safe_stop|bounded-soak|duration_seconds=120|redaction" docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/mining-smoke.md docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/bounded-soak.md docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/redaction-review.md</automated>
    <automated>just parity</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`mining-smoke.md` contains `share_outcome=` and either `controlled no-share condition`, `accepted share`, `rejected share`, or `hardware evidence pending -`.</criterion>
    <criterion>`bounded-soak.md` contains `duration_seconds=120` when a soak run occurs, or `bounded_soak_status: pending -` when prerequisites block it.</criterion>
    <criterion>No committed smoke or soak artifact contains raw `BITAXE_POOL_PASSWORD`, raw pool URL credentials, private `DEVICE_URL`, Wi-Fi credential, API token, or NVS secret value.</criterion>
    <criterion>`redaction-review.md` marks mining-smoke and bounded-soak component packs `passed` or `blocked` before task completion.</criterion>
    <criterion>`just parity` exits 0 and `STR-008` is promoted only when share/no-share metadata, board, port, commits, redaction, and conclusion are present.</criterion>
  </acceptance_criteria>
  <done>Controlled mining smoke and bounded soak evidence is captured, blocked, or classified as controlled no-share without secrets or overclaims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Environment variables to smoke/soak evidence | Pool and `DEVICE_URL` inputs can contain secrets and private endpoints. |
| Mining allow manifest to wrapper behavior | Manifest fields authorize controlled smoke or soak execution. |
| Device network to API/WebSocket captures | Explicit `DEVICE_URL` can expose private device data. |
| Smoke/soak logs to checklist promotion | Share/no-share and telemetry claims affect `STR-008`, API, and statistics rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-15-04-01 | Information Disclosure | `scripts/phase15-controlled-mining.sh` | mitigate | Redact pool values, worker identifiers, private endpoints, `DEVICE_URL`, tokens, Wi-Fi data, NVS secrets, IPs, and MACs before writing artifacts. |
| T-15-04-02 | Elevation of Privilege | controlled smoke/soak commands | mitigate | Require `mining-allow`, detector gate, board `205`, prerequisite artifacts, allowed command, abort conditions, recovery steps, and safe-state markers. |
| T-15-04-03 | Denial of Service | bounded soak | mitigate | Bound duration to `120` seconds by plan, abort on unsafe temperature/power, watchdog unresponsiveness, serial silence, redaction uncertainty, or missing safe-stop. |
| T-15-04-04 | Repudiation | smoke/soak evidence summaries | mitigate | Record command, board, port, commits, package manifest, pool category, share/no-share result, telemetry/watchdog status, safe-stop, conclusion, and redaction status. |
| T-15-04-05 | Tampering | checklist promotion | mitigate | `just parity` must validate `STR-008` metadata before any live mining smoke or soak row can be verified. |
</threat_model>

<verification>
Plan-level checks:
- `bash -n scripts/phase15-controlled-mining.sh scripts/phase15-controlled-mining-test.sh`
- `bazel test //scripts:phase15_controlled_mining_test`
- `cargo test -p bitaxe-stratum --all-features mining_loop fake_pool queue`
- `cargo test -p bitaxe-api --all-features mining`
- `just parity`
- Hardware commands actually used must be recorded in smoke/soak evidence files
</verification>

<success_criteria>
Phase 15 records controlled mining smoke and bounded soak evidence, controlled no-share status, or explicit blockers while preserving watchdog responsiveness and redaction boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/15-bm1366-mining-evidence-completion/15-04-SUMMARY.md`.
</output>
