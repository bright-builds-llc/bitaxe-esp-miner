---
phase: 15-bm1366-mining-evidence-completion
plan: "02"
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - scripts/phase15-bm1366-diagnostic-package.sh
  - scripts/phase15-bm1366-diagnostic-package-test.sh
  - scripts/BUILD.bazel
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect.md
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect/detect-ultra205.log
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect/allow-chip-detect.json
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect/diagnostic-package-summary.json
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect/flash-command-evidence.json
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect/flash-monitor.log
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/safe-boot-restore/flash-command-evidence.json
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/safe-boot-restore/flash-monitor.log
  - docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/redaction-review.md
autonomous: true
requirements:
  - ASIC-07
  - EVD-05
must_haves:
  truths:
    - "BM1366 chip-detect or staged-init diagnostics run from a package-backed flow that preserves trusted wrapper markers"
    - "Chip-detect evidence remains scoped to chip-detect, staged no-mining init, timeout, partial read, chip-count mismatch, or fail-closed status"
    - "Generated chip-detect artifacts are redaction-reviewed before commit"
  artifacts:
    - path: "scripts/phase15-bm1366-diagnostic-package.sh"
      provides: "Package-backed diagnostic firmware builder for Phase 15"
      contains: "BITAXE_ASIC_DIAGNOSTIC"
    - path: "docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect.md"
      provides: "Trusted or pending chip-detect evidence summary"
      contains: "bm1366-chip-detect"
    - path: "docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect/flash-command-evidence.json"
      provides: "Wrapper-owned machine evidence for chip-detect flash-monitor capture"
      contains: "trusted_output"
  key_links:
    - from: "scripts/phase15-bm1366-diagnostic-package.sh"
      to: "scripts/package-firmware.sh"
      via: "diagnostic firmware ELF packaged with SPIFFS and factory image"
      pattern: "package-firmware.sh --firmware-elf"
    - from: "docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect/flash-monitor.log"
      to: "tools/flash/src/main.rs"
      via: "trusted wrapper marker classifier"
      pattern: "spiffs_mount=available"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-07-01T02-07-59
generated_at: 2026-07-01T02:36:53Z
---

<objective>
Close the Phase 12 chip-detect trust root cause with a package-backed BM1366 diagnostic flow.

Purpose: D-01 through D-04 require a trusted package/SPIFFS wrapper path for BM1366 chip-detect or staged-init evidence without weakening `tools/flash` trust markers or overclaiming production mining.
Output: A tested package-backed diagnostic packaging script and detector-gated chip-detect evidence with redaction review.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@standards/core/verification.md
@standards/core/testing.md
@standards/languages/rust.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-bm1366-mining-evidence-completion/15-CONTEXT.md
@.planning/phases/15-bm1366-mining-evidence-completion/15-RESEARCH.md
@.planning/phases/15-bm1366-mining-evidence-completion/15-VALIDATION.md
@docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/README.md
@docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/redaction-review.md
@docs/parity/evidence/phase-12-asic-and-mining-hardware-evidence.md
@scripts/build-firmware.sh
@scripts/package-firmware.sh
@scripts/detect-ultra205.sh
@scripts/BUILD.bazel
@tools/flash/src/main.rs
@tools/parity/src/mining_allow.rs
@firmware/bitaxe/src/asic_adapter.rs
@crates/bitaxe-asic/src/bm1366/adapter_gate.rs

<interfaces>
From `crates/bitaxe-asic/src/bm1366/adapter_gate.rs`:
```rust
pub const CHIP_DETECT_DIAGNOSTIC: &str = "chip-detect";
pub const HARDWARE_EVIDENCE_ACK: &str = "ultra205-chip-detect-safe-bench";
pub enum AsicAdapterMode {
    FailClosed,
    ChipDetectOnly,
}
```

From `tools/flash/src/main.rs`:
```rust
fn monitor_log_has_trusted_boot_markers(log: &str) -> bool
```
</interfaces>
</context>

<execution_guidance>
This plan owns a large builder-plus-evidence artifact set. If execution context becomes tight, preserve Wave 2 and split operationally at the task boundary: complete and verify Task 1 first, then continue Task 2 in a fresh executor context using this same plan and the Task 1 summary. Do not reduce the required generated artifacts, skip redaction review, loosen detector/package gates, or replace hardware-gated evidence with static claims to fit one context.
</execution_guidance>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the package-backed diagnostic builder</name>
  <requirements>ASIC-07, EVD-05</requirements>
  <files>scripts/phase15-bm1366-diagnostic-package.sh, scripts/phase15-bm1366-diagnostic-package-test.sh, scripts/BUILD.bazel</files>
  <read_first>
    <file>scripts/build-firmware.sh</file>
    <file>scripts/package-firmware.sh</file>
    <file>scripts/BUILD.bazel</file>
    <file>firmware/bitaxe/src/asic_adapter.rs</file>
    <file>crates/bitaxe-asic/src/bm1366/adapter_gate.rs</file>
    <file>.planning/phases/15-bm1366-mining-evidence-completion/15-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Test 1: `--mode chip-detect` runs `bazel build --action_env=BITAXE_ASIC_DIAGNOSTIC=chip-detect --action_env=BITAXE_HARDWARE_EVIDENCE_ACK=ultra205-chip-detect-safe-bench //firmware/bitaxe:firmware`.
    - Test 2: `--mode work-result` runs `bazel build --action_env=BITAXE_ASIC_DIAGNOSTIC=work-result --action_env=BITAXE_HARDWARE_EVIDENCE_ACK=ultra205-work-result-safe-bench //firmware/bitaxe:firmware`.
    - Test 3: the script calls `scripts/package-firmware.sh --reference-guard scripts/verify-reference-clean.sh --firmware-elf <bazel-bin>/firmware/bitaxe/bitaxe-firmware.elf --out-dir <out-dir>/package --manifest <out-dir>/package/bitaxe-ultra205-package.json`.
    - Test 4: the script writes `diagnostic-package-summary.json` containing `mode`, `diagnostic_env`, `hardware_evidence_ack`, `package_manifest`, and `flash_monitor_command_shape`.
    - Test 5: unknown modes fail with exit code 2 and no hardware command.
  </behavior>
  <action>Create `scripts/phase15-bm1366-diagnostic-package.sh` with `set -euo pipefail`, arguments `--mode chip-detect|work-result` and `--out-dir PATH`, and no raw serial or flash behavior. The script must build only `//firmware/bitaxe:firmware` with the mode-specific `--action_env` values listed in behavior, resolve `bazel info bazel-bin`, call `scripts/package-firmware.sh` into `<out-dir>/package`, and write `<out-dir>/diagnostic-package-summary.json`. The summary must include `flash_monitor_command_shape` exactly `bazel run //tools/flash:flash -- flash-monitor --board 205 --port <port> --manifest <out-dir>/package/bitaxe-ultra205-package.json --evidence-dir <evidence-dir> --capture-timeout-seconds <seconds>`. Add `scripts/phase15-bm1366-diagnostic-package-test.sh` using fake `bazel` and fake `package-firmware.sh` commands on `PATH`; test both modes, unknown mode rejection, summary JSON field presence, and that no `espflash`, raw serial, erase, rollback, voltage, fan, or mining stress command appears in the script. Register `phase15_bm1366_diagnostic_package` and `phase15_bm1366_diagnostic_package_test` in `scripts/BUILD.bazel`.</action>
  <verify>
    <automated>bash -n scripts/phase15-bm1366-diagnostic-package.sh scripts/phase15-bm1366-diagnostic-package-test.sh</automated>
    <automated>bazel test //scripts:phase15_bm1366_diagnostic_package_test</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`scripts/phase15-bm1366-diagnostic-package.sh` contains `BITAXE_ASIC_DIAGNOSTIC=chip-detect` and `BITAXE_HARDWARE_EVIDENCE_ACK=ultra205-chip-detect-safe-bench`.</criterion>
    <criterion>`scripts/phase15-bm1366-diagnostic-package.sh` contains `BITAXE_ASIC_DIAGNOSTIC=work-result` and `BITAXE_HARDWARE_EVIDENCE_ACK=ultra205-work-result-safe-bench`.</criterion>
    <criterion>`scripts/phase15-bm1366-diagnostic-package.sh` contains `scripts/package-firmware.sh` and `diagnostic-package-summary.json`.</criterion>
    <criterion>`bazel test //scripts:phase15_bm1366_diagnostic_package_test` exits 0.</criterion>
  </acceptance_criteria>
  <done>Phase 15 can build and package diagnostic firmware without repeating the Phase 12 ELF-only trust failure.</done>
</task>

<task type="auto">
  <name>Task 2: Capture package-backed BM1366 chip-detect evidence</name>
  <requirements>ASIC-07, EVD-05</requirements>
  <files>docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect.md, docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect/detect-ultra205.log, docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect/allow-chip-detect.json, docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect/diagnostic-package-summary.json, docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect/flash-command-evidence.json, docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect/flash-monitor.log, docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/safe-boot-restore/flash-command-evidence.json, docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/safe-boot-restore/flash-monitor.log, docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/redaction-review.md</files>
  <read_first>
    <file>docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/README.md</file>
    <file>docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/redaction-review.md</file>
    <file>scripts/phase15-bm1366-diagnostic-package.sh</file>
    <file>tools/parity/src/mining_allow.rs</file>
    <file>tools/flash/src/main.rs</file>
    <file>AGENTS.md</file>
  </read_first>
  <action>Run the hardware tier only after `bash -n` and `bazel test //scripts:phase15_bm1366_diagnostic_package_test` pass. Start with `just detect-ultra205` and save the full output to `docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect/detect-ultra205.log`. Continue only when that output contains exactly one `port=<path>` and board-info success for board `205`; otherwise create `chip-detect.md` with `chip_detect_status: blocked - detector gate failed`, update `redaction-review.md` for the generated detector log, and stop this task without flashing. When detector passes, run `scripts/phase15-bm1366-diagnostic-package.sh --mode chip-detect --out-dir docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect`. Create `allow-chip-detect.json` with `surface` `bm1366-chip-detect`, `claim_tier` `diagnostic-chip-detect`, `evidence_class` `hardware-smoke`, actual board `205`, actual `port`, `detector_command` `just detect-ultra205`, actual `board_info_command`, package manifest path `docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect/package/bitaxe-ultra205-package.json`, actual source/reference commits from the package manifest, `allowed_command` equal to the exact flash-monitor command below, required abort conditions and safe-state markers from Plan 15-01, `redaction_reviewer` set to the executor identity after review, and checklist rows `ASIC-002`, `ASIC-005`, `ASIC-007`, and `EVD-05`. Validate it with `bazel run //tools/parity:report -- mining-allow --manifest docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect/allow-chip-detect.json --surface bm1366-chip-detect --allowed-command "bazel run //tools/flash:flash -- flash-monitor --board 205 --port <path> --manifest docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect/package/bitaxe-ultra205-package.json --evidence-dir docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect --capture-timeout-seconds 35"`. If allow validation passes, run that exact flash-monitor command with the actual port. Accept only wrapper-owned output with `trusted_output=true` or `capture_status=timed_out_after_trusted_output`. Write `chip-detect.md` with scope, commands, board, port, package manifest, source commit, reference commit, observed markers, conclusion, non-claims, and residual blockers. The only passing conclusions allowed are `passed for package-backed chip-detect smoke` or `hardware evidence pending - <exact blocker>`. If the diagnostic package is unstable, run `just detect-ultra205` again and restore trusted packaged safe boot with `just flash-monitor board=205 port=<path> evidence-dir=docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/safe-boot-restore`, then record D-04 pending evidence instead of checklist promotion. Update `redaction-review.md` before commit for every detector log, JSON file, serial log, Markdown file, and terminal output created by this task.</action>
  <verify>
    <automated>rg -n "bm1366-chip-detect|chip_detect_status|trusted_output|spiffs_mount=available|axeos_api_route_shell=started|safe_state: mining=disabled|redaction" docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/chip-detect.md docs/parity/evidence/phase-15-bm1366-mining-evidence-completion/redaction-review.md</automated>
    <automated>just parity</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Every live hardware command in `chip-detect.md` is preceded by `just detect-ultra205`.</criterion>
    <criterion>`chip-detect.md` contains either `passed for package-backed chip-detect smoke` or `hardware evidence pending -`.</criterion>
    <criterion>`chip-detect.md` contains `non_claims: production mining, frequency transition, voltage behavior, work-send, result-receive, accepted shares`.</criterion>
    <criterion>`redaction-review.md` marks the chip-detect component pack `passed` or `blocked` before the task is complete.</criterion>
    <criterion>`just parity` exits 0 and no broad ASIC/mining row is promoted from chip-detect alone.</criterion>
  </acceptance_criteria>
  <done>Package-backed BM1366 chip-detect evidence exists with trusted wrapper scope or a redaction-reviewed pending artifact and restored safe state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Diagnostic compile env to firmware behavior | Compile-time env enables ASIC diagnostic modes that must remain bounded and no-mining. |
| Package manifest to flash-monitor evidence | The manifest-selected factory image must carry SPIFFS and source/reference identity. |
| Serial log to checklist claim | Chip-detect markers must not be interpreted as production mining or work/result proof. |
| Generated artifacts to repository | Detector output, JSON, serial logs, and Markdown may contain bench identifiers or secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-15-02-01 | Tampering | `scripts/phase15-bm1366-diagnostic-package.sh` | mitigate | Build diagnostic firmware through Bazel action env and package through `scripts/package-firmware.sh`; do not flash or mutate hardware from the packaging script. |
| T-15-02-02 | Elevation of Privilege | BM1366 chip-detect diagnostic | mitigate | Require `just detect-ultra205`, mining allow validation, board `205`, package identity, no-mining safe-state markers, and wrapper trust before evidence can support any claim. |
| T-15-02-03 | Repudiation | `chip-detect.md` | mitigate | Record exact commands, board, port, source/reference commits, manifest, observed markers, conclusion, non-claims, and redaction status. |
| T-15-02-04 | Information Disclosure | generated chip-detect artifacts | mitigate | Update `redaction-review.md` before commit and scan for pool credentials, Wi-Fi credentials, private endpoints, `DEVICE_URL`, API tokens, NVS secrets, and local terminal secrets. |
| T-15-02-05 | Denial of Service | connected Ultra 205 | mitigate | Stop on detector failure, board-info failure, missing trusted markers, unsafe status, redaction uncertainty, or package instability; restore trusted packaged safe boot when the diagnostic path is unstable. |
</threat_model>

<verification>
Plan-level checks:
- `bash -n scripts/phase15-bm1366-diagnostic-package.sh scripts/phase15-bm1366-diagnostic-package-test.sh`
- `bazel test //scripts:phase15_bm1366_diagnostic_package_test`
- `cargo test -p bitaxe-parity --all-features mining_allow`
- `just parity`
- Hardware commands actually used must be recorded in `chip-detect.md`
</verification>

<success_criteria>
Phase 15 has a package-backed BM1366 chip-detect diagnostic path that preserves wrapper trust markers or records a conservative pending artifact, with no work/result, mining, voltage, fan, or share overclaims.
</success_criteria>

<output>
After completion, create `.planning/phases/15-bm1366-mining-evidence-completion/15-02-SUMMARY.md`.
</output>
