---
phase: 10-route-manifest-and-api-compare-unification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/parity/src/api_compare.rs
autonomous: true
requirements:
  - API-09
  - API-10
  - REL-01
  - REL-02
  - REL-03
  - EVD-01
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-06-29T15-52-48
generated_at: 2026-06-29T16:18:32Z
must_haves:
  truths:
    - "`tools/parity api-compare` consumes the Phase 7 typed route manifest for route presence and route-kind policy checks."
    - "Phase 5 schema and captured-response checks remain active."
    - "API compare fails when a Phase 7 route is missing, downgraded, or classified as verified from weak evidence."
  artifacts:
    - path: "tools/parity/src/api_compare.rs"
      provides: "Phase 7 manifest-backed API/static/OTA compare policy and regression tests"
      contains: "run_api_compare_with_routes"
  key_links:
    - from: "tools/parity/src/api_compare.rs"
      to: "crates/bitaxe-api/src/route_shell.rs"
      via: "phase07_routes() production route slice"
      pattern: "phase07_routes\\(\\)"
    - from: "tools/parity/src/api_compare.rs"
      to: "tools/parity/fixtures/api/phase05-required-routes.json"
      via: "schema and captured-response fixture parsing"
      pattern: "captured_response_checks"
---

<objective>
Refactor API compare route checks to use the Phase 7 typed manifest and add regressions for route drift and evidence overclaim.

Purpose: Catch missing or downgraded static, recovery, firmware OTA, and OTAWWW routes before Phase 13 live release evidence.
Output: API compare policy checks backed by `phase07_routes()` in production and injectable route slices in tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-route-manifest-and-api-compare-unification/10-CONTEXT.md
@.planning/phases/10-route-manifest-and-api-compare-unification/10-RESEARCH.md
@.planning/phases/10-route-manifest-and-api-compare-unification/10-VALIDATION.md
@tools/parity/src/api_compare.rs
@tools/parity/src/main.rs
@tools/parity/fixtures/api/phase05-required-routes.json
@tools/parity/fixtures/api/axeos-route-usage.json
@crates/bitaxe-api/src/route_shell.rs

<interfaces>
Current production API compare entrypoint:

```rust
pub fn run_api_compare(
    request: &ApiCompareRequest<'_>,
    loader: &impl JsonFixtureLoader,
) -> Result<ApiCompareReport>
```

Current route source to replace for Phase 7 presence and kind policy:

```rust
fn rust_route_set() -> BTreeSet<String> {
    phase05_routes()
        .iter()
        .map(|route| route_key(route_method_label(route.method), route.path))
        .collect()
}
```

Phase 7 route kinds that must be enforced:

```rust
("/api/system/OTA", RouteMethod::Post, RouteKind::FirmwareUpdate)
("/api/system/OTAWWW", RouteMethod::Post, RouteKind::AxeOsStaticUpdateGap)
("/recovery", RouteMethod::Get, RouteKind::Recovery)
("/*", RouteMethod::Get, RouteKind::StaticFiles)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make API compare consume Phase 7 typed routes</name>
  <read_first>
    <file>.planning/phases/10-route-manifest-and-api-compare-unification/10-CONTEXT.md</file>
    <file>.planning/phases/10-route-manifest-and-api-compare-unification/10-VALIDATION.md</file>
    <file>tools/parity/src/api_compare.rs</file>
    <file>tools/parity/fixtures/api/phase05-required-routes.json</file>
    <file>tools/parity/fixtures/api/axeos-route-usage.json</file>
    <file>crates/bitaxe-api/src/route_shell.rs</file>
  </read_first>
  <files>tools/parity/src/api_compare.rs</files>
  <behavior>
    - Test 1: production `run_api_compare` passes with the checked-in OpenAPI text and fixtures while using `phase07_routes()` for route presence and kind policy.
    - Test 2: schema and captured-response evidence labels still render as `schema` and `captured-response`.
    - Test 3: checked-in fixture parsing remains typed through `serde` structs, not source-string scans.
  </behavior>
  <action>Change `run_api_compare` to delegate to a private helper named `run_api_compare_with_routes(request, loader, rust_routes)` and pass `phase07_routes()` in production per D-02 and D-11. Import `AxeosRoute`, `RouteKind`, `RouteMethod`, `phase05_routes`, and `phase07_routes` as needed: `phase05_routes()` may remain only for Phase 5 schema/captured-response compatibility per D-03 and D-12, while Phase 7 route presence and kind checks must use the `rust_routes` slice. Add a typed `validate_phase07_route_policy` check with exact required entries for `POST /api/system/OTA => RouteKind::FirmwareUpdate`, `POST /api/system/OTAWWW => RouteKind::AxeOsStaticUpdateGap`, `GET /recovery => RouteKind::Recovery`, and `GET /* => RouteKind::StaticFiles`, per D-14 and D-15. Keep `schema_routes` and `captured_response_checks` validation intact; do not add an OpenAPI parser dependency or JSON-to-Rust codegen per D-04 and D-13.</action>
  <verify>
    <automated>bazel test //tools/parity:tests</automated>
    <automated>bazel run //tools/parity:report -- api-compare</automated>
  </verify>
  <acceptance_criteria>
    - `tools/parity/src/api_compare.rs` contains `run_api_compare_with_routes`.
    - Production `run_api_compare` calls `phase07_routes()`.
    - Route-kind policy checks compare against `RouteKind::FirmwareUpdate`, `RouteKind::AxeOsStaticUpdateGap`, `RouteKind::Recovery`, and `RouteKind::StaticFiles`.
    - `schema_routes` and `captured_response_checks` remain parsed and validated from `phase05-required-routes.json`.
    - `bazel run //tools/parity:report -- api-compare` prints `validation_errors: none`.
  </acceptance_criteria>
  <done>API compare uses Phase 7 typed route data for route presence and kind policy without losing Phase 5 schema or captured-response evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add missing-route, downgrade, and overclaim regressions</name>
  <read_first>
    <file>tools/parity/src/api_compare.rs</file>
    <file>tools/parity/fixtures/api/axeos-route-usage.json</file>
    <file>crates/bitaxe-api/src/route_shell.rs</file>
    <file>.planning/phases/10-route-manifest-and-api-compare-unification/10-CONTEXT.md</file>
  </read_first>
  <files>tools/parity/src/api_compare.rs</files>
  <behavior>
    - Test 1: removing `GET /recovery` from an injected Phase 7 route slice produces a validation error naming `GET /recovery`.
    - Test 2: downgrading `POST /api/system/OTA` away from `RouteKind::FirmwareUpdate` produces a validation error naming `POST /api/system/OTA`.
    - Test 3: downgrading `POST /api/system/OTAWWW`, `GET /recovery`, or `GET /*` away from their required `RouteKind` values produces validation errors naming the changed route.
    - Test 4: marking a static, recovery, firmware OTA, or OTAWWW policy route as `verified` from only `unit`, `workflow`, `package`, `api-compare`, or `static-route` evidence fails validation.
  </behavior>
  <action>Add focused regression tests named `api_compare_fails_when_phase07_route_is_missing_from_rust_manifest`, `api_compare_fails_when_firmware_ota_route_kind_is_downgraded`, `api_compare_fails_when_otawww_route_kind_is_downgraded`, `api_compare_fails_when_recovery_or_static_route_kind_is_downgraded`, and `api_compare_fails_when_release_sensitive_route_claims_verified_from_weak_evidence`, per D-14, D-15, D-16, and D-24. Use `phase07_routes().to_vec()` and mutate injected `AxeosRoute` copies instead of editing global constants. For the verified-overclaim case, extend the typed static route usage fixture parser with an optional `verified_claim` object containing `status` and `evidence` fields, defaulting to absent for current fixtures; reject `status == "verified"` for `/api/system/OTA`, `/api/system/OTAWWW`, `/recovery`, or `/*` when evidence contains only weak labels (`unit`, `workflow`, `package`, `api-compare`, `static-route`). Keep errors specific enough to name the route and the weak-evidence overclaim.</action>
  <verify>
    <automated>bazel test //tools/parity:tests</automated>
    <automated>bazel run //tools/parity:report -- api-compare</automated>
  </verify>
  <acceptance_criteria>
    - `tools/parity/src/api_compare.rs` has tests covering missing route, route kind downgrade, and weak-evidence verified overclaim.
    - Missing-route failure names the missing method and path.
    - Downgrade failures name the changed route and expected `RouteKind`.
    - Verified-overclaim failure names the release-sensitive route and rejects weak evidence labels.
    - Existing checked-in fixtures still pass with `validation_errors: none`.
  </acceptance_criteria>
  <done>API compare has regressions that fail visibly for Phase 7 route removal, route-kind downgrade, and release-sensitive verified overclaim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| checked-in JSON fixtures to parity policy | Structured fixture data crosses from source files into release/evidence validation. |
| typed Rust route manifest to API compare | `phase07_routes()` determines route presence and kind policy for the parity report. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-04 | Tampering | `tools/parity/src/api_compare.rs` route checks | mitigate | Production API compare uses `phase07_routes()` and tests inject missing/downgraded route slices. |
| T-10-05 | Repudiation | release-sensitive route evidence policy | mitigate | Add typed `verified_claim` parsing and reject weak-evidence verified claims for OTA, OTAWWW, recovery, and static routes. |
| T-10-06 | Tampering | fixture parser | mitigate | Keep `serde` structs for `phase05-required-routes.json` and `axeos-route-usage.json`; do not replace with ad hoc `rg` or string scanning. |
| T-10-07 | Information Disclosure | validation errors and report text | accept | Route policy errors name public method/path/kind values only and do not include request bodies, private URLs, Wi-Fi credentials, pool credentials, or NVS secret values. |
</threat_model>

<verification>
Plan-level verification:

```bash
bazel test //tools/parity:tests
bazel run //tools/parity:report -- api-compare
```
</verification>

<success_criteria>
- API compare production route policy uses `phase07_routes()`.
- Phase 5 OpenAPI/schema and captured-response checks still pass.
- Tests fail when Phase 7 static/recovery/update route presence or kind is downgraded.
- Tests fail when weak evidence is used to classify release-sensitive routes as verified.
- No `reference/esp-miner` file is modified.
</success_criteria>

<output>
After completion, create `.planning/phases/10-route-manifest-and-api-compare-unification/10-02-SUMMARY.md`.
</output>
