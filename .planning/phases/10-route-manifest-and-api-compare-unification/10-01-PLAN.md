---
phase: 10-route-manifest-and-api-compare-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/bitaxe-api/src/route_shell.rs
  - crates/bitaxe-api/src/lib.rs
  - firmware/bitaxe/src/http_api.rs
autonomous: true
requirements:
  - API-09
  - REL-01
  - REL-02
  - REL-03
  - EVD-01
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-06-29T15-52-48
generated_at: 2026-06-29T16:18:32Z
must_haves:
  truths:
    - "Firmware route-shell reporting uses the Phase 7 route manifest rather than the Phase 5 compatibility route view."
    - "Route reporting exposes firmware OTA, OTAWWW gap, recovery, and static wildcard ownership counts."
    - "Firmware HTTP handler registration remains explicit, and the static wildcard remains registered after API, OTA, WebSocket, and unknown API fallback handlers."
  artifacts:
    - path: "crates/bitaxe-api/src/route_shell.rs"
      provides: "Phase 7 route report helper and route ownership regression tests"
      contains: "phase07_route_report"
    - path: "crates/bitaxe-api/src/lib.rs"
      provides: "Public export for firmware route reporting"
      contains: "phase07_route_report"
    - path: "firmware/bitaxe/src/http_api.rs"
      provides: "Manifest-derived route-shell startup log"
      contains: "manifest_routes="
  key_links:
    - from: "firmware/bitaxe/src/http_api.rs"
      to: "crates/bitaxe-api/src/route_shell.rs"
      via: "phase07_route_report import"
      pattern: "phase07_route_report"
    - from: "crates/bitaxe-api/src/route_shell.rs"
      to: "PHASE07_ROUTES"
      via: "helper derives counts from phase07_routes()"
      pattern: "phase07_routes\\(\\)"
---

<objective>
Create a pure Phase 7 route reporting contract and make firmware startup logging consume it.

Purpose: Close the firmware route-count drift without converting ESP-IDF handler registration into manifest-driven registration.
Output: A host-tested `phase07_route_report()` helper, public export, and firmware log output that reports Phase 7 manifest ownership metadata.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-route-manifest-and-api-compare-unification/10-CONTEXT.md
@.planning/phases/10-route-manifest-and-api-compare-unification/10-RESEARCH.md
@.planning/phases/10-route-manifest-and-api-compare-unification/10-VALIDATION.md
@crates/bitaxe-api/src/route_shell.rs
@crates/bitaxe-api/src/lib.rs
@firmware/bitaxe/src/http_api.rs
@firmware/bitaxe/src/static_files.rs

<interfaces>
Existing route manifest contracts:

```rust
pub const fn phase05_routes() -> &'static [AxeosRoute];
pub const fn phase07_routes() -> &'static [AxeosRoute];

pub enum RouteKind {
    Http,
    WebSocket(WebSocketRouteKind),
    SafeUnsupportedUpdate,
    FirmwareUpdate,
    AxeOsStaticUpdateGap,
    Recovery,
    StaticFiles,
}
```

Current firmware drift to replace:

```rust
log::info!(
    "axeos_api_route_shell=started registered_routes={}",
    phase05_routes().len()
);
```

Registration order to preserve:

```rust
static_files::register_recovery(server, filesystem_status)?;
// API, OTA, WebSocket, and unknown /api/* fallback handlers
static_files::register_static(server, filesystem_status)?;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 7 route report helper</name>
  <read_first>
    <file>.planning/phases/10-route-manifest-and-api-compare-unification/10-CONTEXT.md</file>
    <file>.planning/phases/10-route-manifest-and-api-compare-unification/10-VALIDATION.md</file>
    <file>crates/bitaxe-api/src/route_shell.rs</file>
    <file>crates/bitaxe-api/src/lib.rs</file>
  </read_first>
  <files>crates/bitaxe-api/src/route_shell.rs, crates/bitaxe-api/src/lib.rs</files>
  <behavior>
    - Test 1: `phase07_route_report().total_routes` equals `phase07_routes().len()`.
    - Test 2: `phase07_route_report()` counts exactly one `FirmwareUpdate`, one `AxeOsStaticUpdateGap`, one `Recovery`, and one `StaticFiles` route.
    - Test 3: `phase05_routes()` still reports `/api/system/OTA` and `/api/system/OTAWWW` as `RouteKind::SafeUnsupportedUpdate`.
  </behavior>
  <action>Add `pub struct Phase07RouteReport` to `crates/bitaxe-api/src/route_shell.rs` with public fields `total_routes`, `firmware_update_routes`, `otawww_gap_routes`, `recovery_routes`, and `static_file_routes`. Add `pub fn phase07_route_report() -> Phase07RouteReport` that derives every field from `phase07_routes()` and `RouteKind` enum comparisons, per D-01, D-02, D-06, D-07, D-09, D-22, and D-23. Preserve `phase05_routes()` exactly as the Phase 5 compatibility projection per D-03, keep JSON-to-Rust codegen out per D-04, and export `Phase07RouteReport` plus `phase07_route_report` from `crates/bitaxe-api/src/lib.rs`.</action>
  <verify>
    <automated>bazel test //crates/bitaxe-api:tests</automated>
  </verify>
  <acceptance_criteria>
    - `crates/bitaxe-api/src/route_shell.rs` contains `pub struct Phase07RouteReport`.
    - `crates/bitaxe-api/src/route_shell.rs` contains `pub fn phase07_route_report() -> Phase07RouteReport`.
    - `crates/bitaxe-api/src/lib.rs` re-exports `Phase07RouteReport` and `phase07_route_report`.
    - A route-shell test proves `/api/system/OTA` is `RouteKind::FirmwareUpdate` in `phase07_routes()` and `RouteKind::SafeUnsupportedUpdate` in `phase05_routes()`.
    - A route-shell test proves `/api/system/OTAWWW` is `RouteKind::AxeOsStaticUpdateGap`, `/recovery` is `RouteKind::Recovery`, and `/*` is `RouteKind::StaticFiles` in `phase07_routes()`.
  </acceptance_criteria>
  <done>The pure API crate has a tested Phase 7 report helper that can be consumed by firmware without duplicating route counts or ownership rules.</done>
</task>

<task type="auto">
  <name>Task 2: Switch firmware startup log to manifest-derived reporting</name>
  <read_first>
    <file>.planning/phases/10-route-manifest-and-api-compare-unification/10-CONTEXT.md</file>
    <file>firmware/bitaxe/src/http_api.rs</file>
    <file>firmware/bitaxe/src/static_files.rs</file>
    <file>crates/bitaxe-api/src/route_shell.rs</file>
    <file>crates/bitaxe-api/src/lib.rs</file>
  </read_first>
  <files>firmware/bitaxe/src/http_api.rs</files>
  <action>Replace the `phase05_routes` firmware import with `phase07_route_report`, then change the startup log after `start_live_telemetry_cadence_task(server.handle())?` to compute `let route_report = phase07_route_report();` and emit `axeos_api_route_shell=started manifest_routes={} firmware_update_routes={} otawww_gap_routes={} recovery_routes={} static_file_routes={}`. Do not use the old `registered_routes={}` label because it implies every ESP-IDF URI handler is counted. Keep all handler registration calls explicit and in the existing order per D-05 and D-08: `register_recovery`, API routes, firmware OTA, OTAWWW gap, WebSockets, unknown `/api/*` fallbacks, then `register_static` last. Because registration remains explicit in this phase, treat the Phase 7 report helper tests plus firmware build as the adapter/reporting contract regression per D-10.</action>
  <verify>
    <automated>bazel build //firmware/bitaxe:firmware</automated>
    <automated>rg -n "phase05_routes|registered_routes=" firmware/bitaxe/src/http_api.rs; test $? -eq 1</automated>
    <automated>rg -n "manifest_routes=.*firmware_update_routes=.*otawww_gap_routes=.*recovery_routes=.*static_file_routes=" firmware/bitaxe/src/http_api.rs</automated>
  </verify>
  <acceptance_criteria>
    - `firmware/bitaxe/src/http_api.rs` imports `phase07_route_report`.
    - `firmware/bitaxe/src/http_api.rs` no longer imports or calls `phase05_routes`.
    - The startup log contains `manifest_routes=`, `firmware_update_routes=`, `otawww_gap_routes=`, `recovery_routes=`, and `static_file_routes=`.
    - `static_files::register_static(server, filesystem_status)?` remains after the unknown `/api/*` fallback registrations.
    - No firmware handler registration is generated from the manifest in this phase.
  </acceptance_criteria>
  <done>Firmware route reporting is Phase 7 manifest-derived while route registration remains an explicit ESP-IDF adapter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| route manifest to firmware log | Pure Rust route metadata crosses into ESP-IDF firmware evidence logs. |
| firmware adapter registration | Manual ESP-IDF handler order controls whether API/update routes or the static wildcard handles a request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-10-01 | Tampering | `phase07_routes()` reporting | mitigate | Add `phase07_route_report()` derived from `phase07_routes()` and tests for firmware OTA, OTAWWW gap, recovery, and static route owners. |
| T-10-02 | Denial of Service | `firmware/bitaxe/src/http_api.rs` handler order | mitigate | Preserve explicit registration order and keep the static wildcard registration after API, OTA, WebSocket, and unknown API fallback routes. |
| T-10-03 | Information Disclosure | route-shell startup log | accept | Route count and route-kind totals contain no credentials, private endpoints, request IPs, Wi-Fi settings, pool credentials, or NVS secret values. |
</threat_model>

<verification>
Plan-level verification:

```bash
bazel test //crates/bitaxe-api:tests
bazel build //firmware/bitaxe:firmware
rg -n "phase05_routes|registered_routes=" firmware/bitaxe/src/http_api.rs; test $? -eq 1
rg -n "manifest_routes=.*firmware_update_routes=.*otawww_gap_routes=.*recovery_routes=.*static_file_routes=" firmware/bitaxe/src/http_api.rs
```
</verification>

<success_criteria>
- Phase 7 route report helper exists, is exported, and is tested against `phase07_routes()`.
- Firmware logging no longer reports a Phase 5 route count.
- Firmware registration order and explicit handler ownership remain unchanged.
- No `reference/esp-miner` file is modified.
</success_criteria>

<output>
After completion, create `.planning/phases/10-route-manifest-and-api-compare-unification/10-01-SUMMARY.md`.
</output>
