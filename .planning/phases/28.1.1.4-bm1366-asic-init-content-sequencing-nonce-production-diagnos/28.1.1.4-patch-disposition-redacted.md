# Phase 28.1.1.4 Patch Disposition

ab_label: ticket_mask_asic_difficulty
ab_outcome: unchanged
result_correlated: false
fake_pool_submit_observed: false
difficulty_mask_match: false
frequency_final_match: true
nonce_space_match: true
power_delta_class: falling
patch_disposition: falsified_ticket_mask_asic_difficulty_as_sole_blocker
wire_parity_retained: true
next_hypothesis: match_upstream_chip_enumerate_before_init
second_speculative_patch_applied: false
job_byte_patch_applied: false
post_max_baud_delay_2000_reintroduced: false
register_read_poll_defaulted: false
upstream_like_long_block_receive_defaulted: false
phase30_promotion_input: pending
raw_bytes_committed: false
credential_contents_read: false
hypothesis_rescope: cores_idle_despite_enable_present
files_changed: []
default_promotion_skipped: true
asic_256_ticket_mask_retained: true

## Gate application

Plan 02 A/B reported `ab_outcome: unchanged` with `result_correlated: false` and `fake_pool_submit_observed: false`.
Per D-11 / Plan 03 decision gate, ticket-mask ASIC-256 is **not** promoted as a sole-blocker fix (`patch_disposition: falsified_ticket_mask_asic_difficulty_as_sole_blocker`).

`difficulty_mask_match: false` in the A/B report is early-init window asymmetry (upstream comparator class `diff_16` vs Rust `diff_256`). RESEARCH last-pre-job upstream golden remains `diff_256`; PLL (`frequency_final_match: true`) and HCN (`nonce_space_match: true`) finals match. Dynamic mining-ready finals are therefore treated as matched for D-05 routing.

Task 2 keep/revert: **KEEP** ASIC-256 wire-correct ticket mask (`wire_parity_retained: true`). A/B did not `regressed` correlate/submit relative to baseline, so pool-1000 revert is not warranted. No second speculative patch.

## Next hypothesis (deferred)

`match_upstream_chip_enumerate_before_init` — upstream chip-enumerate (`55 AA 52 05 00 00 0A`) before init may be required for cores to hash.
Do **not** implement chip-enumerate in this phase (D-05 / D-12 / D-13).

## Non-claims

- No job-byte patch (D-13).
- No `post_max_baud_delay_2000` reintroduction (D-13).
- No register-read poll default-on (D-13).
- No upstream-like long-block RX default-on (D-13).
- No Phase 30 checklist `verified` edits (D-13/D-16); `phase30_promotion_input: pending`.
- No chip-enumerate implementation this plan.
- Sole-blocker claim for `ticket_mask_asic_difficulty` is falsified; wire-parity ASIC-256 retained.
