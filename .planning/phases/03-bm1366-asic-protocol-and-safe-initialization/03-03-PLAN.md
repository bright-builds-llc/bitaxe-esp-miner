---
phase: 03-bm1366-asic-protocol-and-safe-initialization
plan: "03"
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - crates/bitaxe-asic/BUILD.bazel
  - crates/bitaxe-asic/src/lib.rs
  - crates/bitaxe-asic/src/dispatch.rs
  - crates/bitaxe-asic/src/bm1366.rs
  - crates/bitaxe-asic/src/bm1366/command.rs
  - crates/bitaxe-asic/src/bm1366/observation.rs
  - crates/bitaxe-asic/src/bm1366/transcript.rs
  - crates/bitaxe-asic/fixtures/bm1366/transcript-cases.json
autonomous: true
requirements: [ASIC-03, ASIC-04, ASIC-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 3-2026-06-26T22-40-40
generated_at: 2026-06-26T23:14:06Z
must_haves:
  truths:
    - "BM1366 is the only active V1 ASIC dispatch path."
    - "Other upstream ASIC families are explicit deferred or not-yet-verified states, not accidentally active implementations."
    - "Firmware-facing ASIC behavior is expressed as typed commands, adapter actions, and observations rather than raw packet construction."
    - "Fake UART transcripts can exercise exact reads, timeouts, partial reads, bad preambles, bad CRCs, unknown registers, and chip-count mismatches."
  artifacts:
    - path: "crates/bitaxe-asic/src/dispatch.rs"
      provides: "ASIC model dispatch with active BM1366 and deferred non-205 families"
    - path: "crates/bitaxe-asic/src/bm1366/command.rs"
      provides: "Semantic BM1366 command and adapter-action types"
    - path: "crates/bitaxe-asic/src/bm1366/observation.rs"
      provides: "Semantic BM1366 observation and status types"
    - path: "crates/bitaxe-asic/src/bm1366/transcript.rs"
      provides: "Fake UART transcript harness"
  key_links:
    - from: "crates/bitaxe-asic/src/dispatch.rs"
      to: "crates/bitaxe-config/src/catalog.rs"
      via: "model/scope values are consumed by later init plan and firmware adapter"
      pattern: "ActiveBm1366|Deferred"
    - from: "crates/bitaxe-asic/src/bm1366/command.rs"
      to: "crates/bitaxe-asic/src/bm1366/packet.rs"
      via: "commands encode to crate-owned frame bytes"
      pattern: "CommandFrame|JobFrame"
    - from: "crates/bitaxe-asic/src/bm1366/transcript.rs"
      to: "crates/bitaxe-asic/src/bm1366/result.rs"
      via: "transcript read bytes decode into observations"
      pattern: "Bm1366ParsedResult|Bm1366Observation"
---

<objective>
Create semantic BM1366 dispatch, command, observation, and fake UART transcript boundaries.

Purpose: ASIC-03 and ASIC-04 require a narrow boundary where active BM1366 behavior is typed and testable, while non-205 ASICs stay deferred and firmware never constructs raw protocol bytes.
Output: The pure ASIC crate exposes semantic commands, adapter actions, observations, dispatch status, and fake transcript tests that cover malformed UART behavior.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-CONTEXT.md
@.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-RESEARCH.md
@.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-VALIDATION.md
@.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-01-SUMMARY.md
@.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-02-SUMMARY.md
@crates/bitaxe-config/src/catalog.rs
@reference/esp-miner/components/asic/asic.c
@reference/esp-miner/components/asic/bm1366.c
@reference/esp-miner/components/asic/asic_common.c
@docs/adr/0008-reference-breadcrumb-comments.md
@docs/adr/0014-pivot-to-ultra-205-bm1366-first-parity.md

<interfaces>
Semantic command surface required by Phase 3 decisions:
```rust
pub enum Bm1366Command {
    SetVersionMask(VersionMask),
    ReadChipId,
    SetChipAddress(ChipAddress),
    WriteRegister(RegisterWrite),
    SetFrequency(FrequencyPlan),
    SetNonceSpace(NonceSpacePlan),
    SendDiagnosticWork(Bm1366WorkPayload),
}

pub enum Bm1366Observation {
    ChipId { chip_id: ChipId, asic_index: AsicIndex },
    RegisterRead(Bm1366RegisterRead),
    JobNonce(Bm1366NonceResult),
    ProtocolFault(Bm1366ProtocolFault),
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add active BM1366 dispatch and semantic command surface</name>
  <files>crates/bitaxe-asic/BUILD.bazel, crates/bitaxe-asic/src/lib.rs, crates/bitaxe-asic/src/dispatch.rs, crates/bitaxe-asic/src/bm1366.rs, crates/bitaxe-asic/src/bm1366/command.rs, crates/bitaxe-asic/src/bm1366/observation.rs</files>
  <read_first>
    - crates/bitaxe-asic/src/lib.rs
    - crates/bitaxe-asic/src/bm1366.rs
    - crates/bitaxe-asic/BUILD.bazel
    - crates/bitaxe-config/src/catalog.rs
    - reference/esp-miner/components/asic/asic.c
    - reference/esp-miner/components/asic/bm1366.c
    - .planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: Dispatch for model string `BM1366` and board version `205` returns `AsicDispatch::ActiveBm1366`.
    - Test 2: Dispatch for `BM1370`, `BM1368`, and `BM1397` returns `AsicDispatch::Deferred` with scope `NotHardwareVerified`.
    - Test 3: `Bm1366Command::ReadChipId` encodes through crate-owned `CommandFrame`, and firmware-facing callers receive `Bm1366AdapterAction::WriteFrame`.
    - Test 4: The semantic command enum contains `SendDiagnosticWork`, not production `SendWork`.
  </behavior>
  <action>Create `src/dispatch.rs` with `AsicDispatch::{ActiveBm1366, Deferred(DeferredAsic)}`, `DeferredAsicModel::{Bm1370, Bm1368, Bm1397, Other}`, and `DeferredAsicReason::{NotV1ActiveTarget, MissingHardwareEvidence}`. The active path is exactly Ultra 205 board version `205`, family `Ultra`, ASIC model `BM1366`, ASIC count `1`, and verification scope `ActiveUltra205`; every other model or board scope returns `Deferred`. Create `bm1366/command.rs` with `Bm1366Command::{SetVersionMask, ReadChipId, SetChipAddress, WriteRegister, SetFrequency, SetNonceSpace, SendDiagnosticWork}` and `Bm1366AdapterAction::{UseDefaultBaud { baud: 115_200 }, UseMaxBaud { baud: 1_000_000 }, WaitTxDone { timeout_ms: 1_000 }, ClearRx, WriteFrame(FrameBytes), ReadExact { len: 11, timeout_ms: 1_000 }, DelayMs(u32), ResetPulse { low_ms: 100, high_ms: 100 }, HoldResetLow, PublishStatus(AsicInitStatus)}`. Create `bm1366/observation.rs` with `Bm1366Observation::{ChipId, RegisterRead, JobNonce, ProtocolFault}` plus typed `ChipId`, `AsicIndex`, and `ChipAddress`. Raw preamble bytes, CRCs, and register details must be consumed from Plan 03-01 modules; do not duplicate packet-building constants in `dispatch.rs` or firmware-facing APIs. Export `dispatch`, `command`, and `observation` from crate roots and update `BUILD.bazel` source lists.</action>
  <verify>
    <automated>cargo test -p bitaxe-asic dispatch --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub mod dispatch|pub use dispatch" crates/bitaxe-asic/src/lib.rs` finds dispatch exports.
    - `rg -n "ActiveBm1366|DeferredAsicModel|Bm1370|Bm1368|Bm1397|MissingHardwareEvidence" crates/bitaxe-asic/src/dispatch.rs` finds active and deferred dispatch states.
    - `rg -n "SetVersionMask|ReadChipId|SetChipAddress|WriteRegister|SetFrequency|SetNonceSpace|SendDiagnosticWork" crates/bitaxe-asic/src/bm1366/command.rs` finds all semantic commands.
    - `rg -n "UseDefaultBaud.*115_200|UseMaxBaud.*1_000_000|ReadExact.*11|timeout_ms.*1_000|ResetPulse.*100|HoldResetLow" crates/bitaxe-asic/src/bm1366/command.rs` finds adapter-action constants.
    - `rg -n "ChipId|RegisterRead|JobNonce|ProtocolFault" crates/bitaxe-asic/src/bm1366/observation.rs` finds all observation variants.
    - `! rg -n "SendWork\\b" crates/bitaxe-asic/src` succeeds; Phase 3 must use `SendDiagnosticWork`.
  </acceptance_criteria>
  <done>ASIC dispatch exposes only BM1366 as the active V1 path, and semantic commands/observations create the adapter contract without raw byte leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fake UART transcript coverage for adapter faults</name>
  <files>crates/bitaxe-asic/BUILD.bazel, crates/bitaxe-asic/src/bm1366.rs, crates/bitaxe-asic/src/bm1366/transcript.rs, crates/bitaxe-asic/fixtures/bm1366/transcript-cases.json</files>
  <read_first>
    - crates/bitaxe-asic/src/bm1366/command.rs
    - crates/bitaxe-asic/src/bm1366/observation.rs
    - crates/bitaxe-asic/src/bm1366/result.rs
    - crates/bitaxe-asic/src/bm1366/packet.rs
    - reference/esp-miner/components/asic/asic_common.c
    - .planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-CONTEXT.md
    - .planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: Exact chip-ID transcript emits `ReadChipId`, writes the expected frame, reads exactly 11 bytes, and produces `Bm1366Observation::ChipId`.
    - Test 2: Timeout returns `ProtocolFault` and `FailClosed` status.
    - Test 3: Partial read returns `ProtocolFault(InvalidLength)` and includes `ClearRx`.
    - Test 4: Bad preamble and bad CRC return `ProtocolFault` and no chip observation.
    - Test 5: Chip-count mismatch expected `1` actual `0` returns `ChipCountMismatch` and fail-closed status.
  </behavior>
  <action>Create `bm1366/transcript.rs` with `FakeUartTranscript`, `FakeUartEvent::{ExpectWrite(FrameBytes), ReadBytes(Vec<u8>), Timeout, PartialRead(Vec<u8>), MalformedPreamble(Vec<u8>), BadCrc(Vec<u8>)}`, `TranscriptOutcome`, and `TranscriptStatus::{Complete, FailClosed}`. The transcript runner must verify expected writes in order, use Plan 03-02 result parsing for reads, convert timeout, partial length, malformed preamble, bad CRC, unknown register, invalid job ID, and chip-count mismatch into `Bm1366Observation::ProtocolFault`, and emit `ClearRx` on malformed or partial reads. Add fixture metadata cases for exact read, timeout, partial read, malformed preamble, bad CRC, unknown register, and chip-count mismatch with checklist IDs `ASIC-002`, `ASIC-004`, and `ASIC-005`. Export `transcript` from `bm1366.rs` and update `BUILD.bazel` source and fixture data lists.</action>
  <verify>
    <automated>cargo test -p bitaxe-asic transcript --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub mod transcript" crates/bitaxe-asic/src/bm1366.rs` finds the module export.
    - `rg -n "FakeUartTranscript|ExpectWrite|ReadBytes|Timeout|PartialRead|MalformedPreamble|BadCrc|ChipCountMismatch" crates/bitaxe-asic/src/bm1366/transcript.rs` finds all transcript cases.
    - `rg -n "TranscriptStatus::FailClosed|ClearRx|ProtocolFault" crates/bitaxe-asic/src/bm1366/transcript.rs` finds fail-closed behavior.
    - `rg -n "exact read|timeout|partial read|malformed preamble|bad CRC|unknown register|chip-count mismatch" crates/bitaxe-asic/fixtures/bm1366/transcript-cases.json` finds fixture coverage.
    - `cargo test -p bitaxe-asic --all-features` passes.
  </acceptance_criteria>
  <done>Fake UART transcripts can prove adapter-boundary success and malformed input failure cases without live hardware or firmware side effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ASIC model selection -> active behavior | Catalog/model data decides whether any active BM1366 path is available. |
| UART transcript bytes -> semantic observations | Test transcript bytes are decoded into typed observations or faults. |
| Pure ASIC crate -> firmware adapter | Firmware receives typed adapter actions, not packet internals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Elevation of Privilege | `dispatch.rs` | mitigate | Only board `205`, family `Ultra`, model `BM1366`, count `1`, and active Ultra 205 scope can return `ActiveBm1366`; every other ASIC path is deferred. |
| T-03-03-02 | Tampering | `transcript.rs` | mitigate | Timeout, partial read, malformed preamble, bad CRC, unknown register, invalid job, and chip-count mismatch all emit `ProtocolFault` plus fail-closed status. |
| T-03-03-03 | Information Disclosure | Adapter boundary | mitigate | Raw frame bytes remain wrapped in `FrameBytes`; firmware-facing APIs expose semantic commands/actions/observations only. |
| T-03-03-04 | Repudiation | Deferred ASIC claims | mitigate | Non-205 and non-BM1366 families are represented as deferred/not verified, preventing Ultra 205 evidence from being reused for other boards or ASICs. |
</threat_model>

<verification>
Run after this plan:

```bash
cargo test -p bitaxe-asic --all-features
bazel test //crates/bitaxe-asic:tests
rg -n "ActiveBm1366|SendDiagnosticWork|FakeUartTranscript" crates/bitaxe-asic/src
git status --short reference/esp-miner
```
</verification>

<success_criteria>
- BM1366 is the only active V1 dispatch path.
- Semantic commands and observations exist with the exact variants required by D-05.
- Fake transcript tests cover exact reads and all required fail-closed malformed-read cases from D-08.
- Raw BM1366 protocol details remain inside `bitaxe-asic`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-03-SUMMARY.md`
</output>
