---
phase: 03-bm1366-asic-protocol-and-safe-initialization
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - crates/bitaxe-asic/BUILD.bazel
  - crates/bitaxe-asic/src/bm1366.rs
  - crates/bitaxe-asic/src/bm1366/work.rs
  - crates/bitaxe-asic/src/bm1366/result.rs
  - crates/bitaxe-asic/fixtures/bm1366/work-result-cases.json
autonomous: true
requirements: [ASIC-02, ASIC-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 3-2026-06-26T22-40-40
generated_at: 2026-06-26T23:14:06Z
must_haves:
  truths:
    - "Developer can encode BM1366 diagnostic work payloads without enabling production mining."
    - "Developer can parse 11-byte BM1366 result frames into typed nonce, ASIC, core, small-core, version, and register observations."
    - "Invalid job IDs, malformed result frames, unknown registers, and CRC failures become typed protocol faults."
  artifacts:
    - path: "crates/bitaxe-asic/src/bm1366/work.rs"
      provides: "BM1366 fixed-size job payload and job-id semantics"
    - path: "crates/bitaxe-asic/src/bm1366/result.rs"
      provides: "BM1366 result frame parsing and nonce decoding"
    - path: "crates/bitaxe-asic/fixtures/bm1366/work-result-cases.json"
      provides: "Reference-derived work/result fixture metadata"
  key_links:
    - from: "crates/bitaxe-asic/src/bm1366/work.rs"
      to: "crates/bitaxe-asic/src/bm1366/packet.rs"
      via: "JobFrame wraps the 82-byte BM1366 work payload"
      pattern: "JobFrame"
    - from: "crates/bitaxe-asic/src/bm1366/result.rs"
      to: "crates/bitaxe-asic/src/bm1366/registers.rs"
      via: "Register responses map through typed register IDs"
      pattern: "Bm1366Register"
    - from: "crates/bitaxe-asic/src/bm1366/result.rs"
      to: "crates/bitaxe-asic/src/error.rs"
      via: "Invalid frames return Bm1366ProtocolFault"
      pattern: "Bm1366ProtocolFault"
---

<objective>
Implement BM1366 work encoding and result parsing as pure Rust.

Purpose: ASIC-02 needs deterministic work/result behavior before any mining loop exists. This plan models BM1366 work packets, job IDs, nonce decoding, register responses, and result faults while keeping runtime work submission diagnostic-only and outside firmware control logic.
Output: `bitaxe-asic` can encode 82-byte BM1366 work payloads, build job frames, parse 11-byte result frames, and reject invalid jobs without Stratum or production mining.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-CONTEXT.md
@.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-RESEARCH.md
@.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-VALIDATION.md
@.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-01-SUMMARY.md
@reference/esp-miner/components/asic/bm1366.c
@reference/esp-miner/components/asic/include/bm1366.h
@reference/esp-miner/components/asic/asic_common.c
@reference/esp-miner/components/asic/include/asic_common.h
@docs/adr/0008-reference-breadcrumb-comments.md
@docs/adr/0012-parity-verification-evidence.md

<interfaces>
Expected contracts created by Plan 03-01:
```rust
pub mod bm1366;
pub mod error;
pub use error::Bm1366ProtocolFault;
```

Concrete BM1366 work/result values from the pinned reference:
```text
BM1366_job fields: job_id(1), num_midstates(1), starting_nonce(4), nbits(4), ntime(4), merkle_root(32), prev_block_hash(32), version(4)
BM1366 job payload length: 82 bytes
BM1366 job frame total length: 88 bytes
num_midstates: 0x01
Job ID advance: (id + 8) % 128
Valid job lookup mask: job_id & 0xf8
Result frame length: 11 bytes
Receive preamble word: 0xaa55
Job result nonce decode: asic = ((nonce_be >> 17) & 0xff) / address_interval
Job result core decode: core_id = (nonce_be >> 25) & 0x7f
Job result small-core decode: result_id & 0x07
Version roll bits: version_be << 13
BM1366 normal cores: 112
BM1366 small-core IDs per job-id low bits: 8
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode diagnostic BM1366 work payloads and job IDs</name>
  <files>crates/bitaxe-asic/BUILD.bazel, crates/bitaxe-asic/src/bm1366.rs, crates/bitaxe-asic/src/bm1366/work.rs, crates/bitaxe-asic/fixtures/bm1366/work-result-cases.json</files>
  <read_first>
    - crates/bitaxe-asic/src/bm1366.rs
    - crates/bitaxe-asic/BUILD.bazel
    - reference/esp-miner/components/asic/bm1366.c
    - reference/esp-miner/components/asic/include/bm1366.h
    - .planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-CONTEXT.md
    - .planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `Bm1366JobId::new(0).advance()` returns `8`, and advancing from `120` returns `0`.
    - Test 2: `Bm1366JobId::new(0x2f).lookup_key()` returns `0x28`, and `small_core_id()` returns `7`.
    - Test 3: A `Bm1366WorkPayload` encodes to exactly 82 bytes with `num_midstates` byte `0x01`.
    - Test 4: `diagnostic_job_frame()` produces an 88-byte job frame and does not create any runtime mining or Stratum type.
  </behavior>
  <action>Create `bm1366/work.rs` with `BM1366_JOB_PAYLOAD_LEN: usize = 82`, `BM1366_JOB_FRAME_LEN: usize = 88`, `BM1366_NUM_MIDSTATES: u8 = 0x01`, `JOB_ID_STEP: u8 = 8`, `JOB_ID_MODULUS: u8 = 128`, and `JOB_ID_LOOKUP_MASK: u8 = 0xf8`. Add `Bm1366JobId`, `Bm1366WorkPayload`, `Bm1366WorkFields`, and `DiagnosticWorkFrame` types. `Bm1366WorkFields` must use fixed arrays: `starting_nonce: [u8; 4]`, `nbits: [u8; 4]`, `ntime: [u8; 4]`, `merkle_root: [u8; 32]`, `prev_block_hash: [u8; 32]`, and `version: [u8; 4]`. Implement encoding in the exact field order from `BM1366_job` and wrap it with Plan 03-01 `JobFrame`. Name the public constructor `diagnostic_job_frame` to enforce D-02: Phase 3 can model and test work bytes, but production `SendWork` remains out of scope until Phase 4. Export `work` from `bm1366.rs`, update `BUILD.bazel` `srcs`, and add fixture metadata with `checklist_ids: ["ASIC-003"]`, `source_file: "reference/esp-miner/components/asic/bm1366.c"`, `source_behavior: "BM1366_send_work job payload layout and job-id advance"`, `reference_commit: "c1915b0a63bfabebdb95a515cedfee05146c1d50"`, `license_posture: "reference-derived fixture data; not MIT source expression"`, and `derivation: "manual"`. Every object in `fixtures/bm1366/work-result-cases.json` must include `checklist_ids`, `source_file`, `source_behavior`, `reference_commit`, `license_posture`, and `derivation`.</action>
  <verify>
    <automated>cargo test -p bitaxe-asic bm1366_work --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub mod work" crates/bitaxe-asic/src/bm1366.rs` finds the module export.
    - `rg -n "BM1366_JOB_PAYLOAD_LEN.*82|BM1366_JOB_FRAME_LEN.*88|BM1366_NUM_MIDSTATES.*0x01|JOB_ID_STEP.*8|JOB_ID_MODULUS.*128|JOB_ID_LOOKUP_MASK.*0xf8" crates/bitaxe-asic/src/bm1366/work.rs` finds all constants.
    - `rg -n "starting_nonce: \\[u8; 4\\]|merkle_root: \\[u8; 32\\]|prev_block_hash: \\[u8; 32\\]|diagnostic_job_frame" crates/bitaxe-asic/src/bm1366/work.rs` finds the fixed-size work contract.
    - `rg -n "ASIC-003|BM1366_send_work|c1915b0a63bfabebdb95a515cedfee05146c1d50|license_posture|derivation" crates/bitaxe-asic/fixtures/bm1366/work-result-cases.json` finds fixture provenance.
    - `rg -n "checklist_ids|source_file|source_behavior|reference_commit|license_posture|derivation" crates/bitaxe-asic/fixtures/bm1366/work-result-cases.json` finds all required provenance fields.
    - `! rg -n "stratum|pool|accepted share|production" crates/bitaxe-asic/src/bm1366/work.rs` returns success, proving Phase 4 mining concepts were not introduced.
  </acceptance_criteria>
  <done>BM1366 work encoding and job-id handling are pure, fixed-size, diagnostic-only, and covered by focused tests and provenance metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parse BM1366 result frames and nonce-derived fields</name>
  <files>crates/bitaxe-asic/BUILD.bazel, crates/bitaxe-asic/src/bm1366.rs, crates/bitaxe-asic/src/bm1366/result.rs, crates/bitaxe-asic/fixtures/bm1366/work-result-cases.json</files>
  <read_first>
    - crates/bitaxe-asic/src/bm1366.rs
    - crates/bitaxe-asic/src/bm1366/work.rs
    - crates/bitaxe-asic/src/bm1366/registers.rs
    - crates/bitaxe-asic/src/error.rs
    - reference/esp-miner/components/asic/bm1366.c
    - reference/esp-miner/components/asic/asic_common.c
    - reference/esp-miner/components/asic/include/asic_common.h
    - .planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: A result frame with preamble `0xaa55`, valid CRC5 residue, and job response bit set parses as `Bm1366ParsedResult::JobNonce`.
    - Test 2: Job result ID `0x2f` yields lookup job ID `0x28` and small-core ID `7`.
    - Test 3: Nonce-derived `core_id` uses `(nonce_be >> 25) & 0x7f` and rejects values outside `0..112`.
    - Test 4: Register response for register `0x8c` maps to total-count register, and register `0xff` returns `Bm1366ProtocolFault::UnknownRegister`.
    - Test 5: Timeout, partial length, bad preamble, and bad CRC fixture cases return protocol faults and do not produce observations.
  </behavior>
  <action>Create `bm1366/result.rs` with `BM1366_RECEIVE_PREAMBLE: u16 = 0xaa55`, `BM1366_RESULT_FRAME_LEN: usize = 11`, `BM1366_NORMAL_CORE_COUNT: u8 = 112`, and `BM1366_SMALL_CORE_IDS: u8 = 8`. Add `Bm1366ParsedResult::{JobNonce(Bm1366NonceResult), RegisterRead(Bm1366RegisterRead)}`, `Bm1366NonceResult`, `Bm1366RegisterRead`, and `ResultFrameBytes([u8; 11])`. Implement parsing that validates exact length, receive preamble, CRC5 residue over bytes `2..11`, job-response discriminator from byte 10 high bit, register mapping through `Bm1366Register`, job lookup key `id & 0xf8`, small-core ID `id & 0x07`, ASIC index `((nonce_be >> 17) & 0xff) / address_interval`, core ID `(nonce_be >> 25) & 0x7f`, and version bits `(version_be as u32) << 13`. Require a typed valid-job set or callback so invalid job IDs return `Bm1366ProtocolFault::InvalidJobId` instead of silently accepting stale results. Export `result` from `bm1366.rs`, update `BUILD.bazel`, and extend `work-result-cases.json` with job nonce, register read, invalid job, bad preamble, partial length, and bad CRC cases tied to checklist IDs `ASIC-004` and `ASIC-006`. Every result fixture object must include `checklist_ids`, `source_file`, `source_behavior`, `reference_commit`, `license_posture`, and `derivation`.</action>
  <verify>
    <automated>cargo test -p bitaxe-asic bm1366_result --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub mod result" crates/bitaxe-asic/src/bm1366.rs` finds the module export.
    - `rg -n "BM1366_RECEIVE_PREAMBLE.*0xaa55|BM1366_RESULT_FRAME_LEN.*11|BM1366_NORMAL_CORE_COUNT.*112|BM1366_SMALL_CORE_IDS.*8" crates/bitaxe-asic/src/bm1366/result.rs` finds required constants.
    - `rg -n "id .*& 0xf8|id .*& 0x07|nonce_be >> 17|nonce_be >> 25|version_be.*<< 13" crates/bitaxe-asic/src/bm1366/result.rs` finds nonce/job decode logic.
    - `rg -n "InvalidJobId|UnknownRegister|BadPreamble|BadCrc|InvalidLength" crates/bitaxe-asic/src/bm1366/result.rs` finds all fail-closed fault paths.
    - `rg -n "ASIC-004|Invalid job|bad preamble|partial length|bad CRC" crates/bitaxe-asic/fixtures/bm1366/work-result-cases.json` finds result fixture coverage.
    - `rg -n "checklist_ids|source_file|source_behavior|reference_commit|license_posture|derivation" crates/bitaxe-asic/fixtures/bm1366/work-result-cases.json` finds all required provenance fields after result cases are appended.
    - `cargo test -p bitaxe-asic --all-features` passes.
  </acceptance_criteria>
  <done>BM1366 result parsing produces typed observations or typed faults for every modeled frame path, and no malformed or invalid result reaches downstream control logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pool/mining-shaped work data -> BM1366 diagnostic payload | Fixed arrays enter pure work encoding without enabling production mining. |
| UART result bytes -> typed observations | 11-byte result frames become typed job nonce or register observations only after validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | `bm1366/result.rs` | mitigate | Exact length, preamble, CRC5 residue, register, core, and valid-job checks must pass before producing `Bm1366ParsedResult`. |
| T-03-02-02 | Elevation of Privilege | `bm1366/work.rs` | mitigate | Work constructor is named diagnostic and remains pure; no Stratum, pool, accepted-share, or production runtime dispatch code is introduced. |
| T-03-02-03 | Denial of Service | Job-id tracking | mitigate | Invalid or stale job IDs return `Bm1366ProtocolFault::InvalidJobId`, preventing old results from being treated as current work. |
| T-03-02-04 | Supply Chain | Work/result fixtures | mitigate | Fixture metadata records source behavior and pinned commit; tests use independently authored Rust rather than copied C expression. |
</threat_model>

<verification>
Run after this plan:

```bash
cargo test -p bitaxe-asic --all-features
bazel test //crates/bitaxe-asic:tests
rg -n "ASIC-003|ASIC-004|ASIC-006" crates/bitaxe-asic/fixtures/bm1366/work-result-cases.json
git status --short reference/esp-miner

# Required Rust pre-commit sequence before committing implementation work:
cargo fmt --all
cargo clippy --all-targets --all-features -- -D warnings
cargo build --all-targets --all-features
cargo test --all-features
```
</verification>

<success_criteria>
- BM1366 diagnostic work payload length is 82 bytes and job frame length is 88 bytes.
- Result frames are accepted only after exact validation and parsed into typed nonce/register outputs.
- Invalid result cases fail closed with typed errors.
- Runtime production mining and Stratum remain absent from Phase 3 code.
</success_criteria>

<output>
After completion, create `.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-02-SUMMARY.md`
</output>
