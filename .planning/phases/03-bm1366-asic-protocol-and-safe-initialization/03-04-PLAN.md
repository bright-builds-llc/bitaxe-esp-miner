---
phase: 03-bm1366-asic-protocol-and-safe-initialization
plan: "04"
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - crates/bitaxe-asic/Cargo.toml
  - crates/bitaxe-asic/BUILD.bazel
  - crates/bitaxe-asic/src/bm1366.rs
  - crates/bitaxe-asic/src/bm1366/init_plan.rs
  - crates/bitaxe-asic/src/bm1366/frequency_voltage.rs
  - crates/bitaxe-asic/fixtures/bm1366/init-plan-cases.json
autonomous: true
requirements: [ASIC-05, ASIC-06, ASIC-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 3-2026-06-26T22-40-40
generated_at: 2026-06-26T23:14:06Z
must_haves:
  truths:
    - "Ultra 205 BM1366 initialization is represented as a staged pure plan that can fail closed before firmware effects."
    - "Chip-detect-only staging is available before full staged init."
    - "Full init is blocked when power or thermal preflight evidence is missing."
    - "Frequency and voltage transitions are range-checked as pure decisions and remain below verified without hardware evidence."
  artifacts:
    - path: "crates/bitaxe-asic/src/bm1366/init_plan.rs"
      provides: "Bm1366InitPlan state machine, preflight tokens, statuses, and fail-closed decisions"
    - path: "crates/bitaxe-asic/src/bm1366/frequency_voltage.rs"
      provides: "Pure frequency and voltage transition decisions"
    - path: "crates/bitaxe-asic/fixtures/bm1366/init-plan-cases.json"
      provides: "Reference-derived init/frequency safety fixture metadata"
  key_links:
    - from: "crates/bitaxe-asic/src/bm1366/init_plan.rs"
      to: "crates/bitaxe-config/src/catalog.rs"
      via: "ultra_205_catalog_entry() gate facts"
      pattern: "ultra_205_catalog_entry"
    - from: "crates/bitaxe-asic/src/bm1366/frequency_voltage.rs"
      to: "crates/bitaxe-config/src/validation.rs"
      via: "AsicFrequencyMhz and CoreVoltageMv constructors"
      pattern: "AsicFrequencyMhz|CoreVoltageMv"
    - from: "crates/bitaxe-asic/src/bm1366/init_plan.rs"
      to: "crates/bitaxe-asic/src/bm1366/command.rs"
      via: "pure init plan emits adapter actions"
      pattern: "Bm1366AdapterAction"
---

<objective>
Implement fail-closed BM1366 staged initialization and pure frequency/voltage decisions.

Purpose: ASIC-05 and ASIC-06 require safety gates before live initialization or hardware-sensitive transitions. This plan builds the functional core that firmware can execute later, with Ultra 205 board/config facts reused from Phase 2.
Output: `Bm1366InitPlan` can produce chip-detect-only actions, block full init without power/thermal evidence, and model frequency/voltage transition bytes as unverified pure decisions.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-CONTEXT.md
@.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-RESEARCH.md
@.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-VALIDATION.md
@.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-03-SUMMARY.md
@crates/bitaxe-config/src/catalog.rs
@crates/bitaxe-config/src/defaults.rs
@crates/bitaxe-config/src/validation.rs
@reference/esp-miner/main/device_config.h
@reference/esp-miner/main/power/asic_init.c
@reference/esp-miner/main/power/asic_reset.c
@reference/esp-miner/components/asic/bm1366.c
@reference/esp-miner/components/asic/asic_common.c
@docs/adr/0012-parity-verification-evidence.md
@docs/adr/0014-pivot-to-ultra-205-bm1366-first-parity.md

<interfaces>
Phase 2 config exports to use directly:
```rust
pub use catalog::{ultra_205_catalog_entry, VerificationScope};
pub use validation::{AsicFrequencyMhz, CoreVoltageMv, BoardVersion};
```

Ultra 205/BM1366 gate facts:
```text
board_version: "205"
family: "Ultra"
asic_model: "BM1366"
asic_count: 1
verification_scope: ActiveUltra205
default_frequency_mhz: 485
default_voltage_mv: 1200
frequency_options: 400,425,450,475,485,500,525,550,575
voltage_options: 1100,1150,1200,1250,1300
capabilities: ds4432u=true, ina260=true, asic_enable=true
pre-init thermal rule: chip temperature is not meaningful before init; require lower-level thermal/power evidence or fail closed.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Bm1366InitPlan with fail-closed preflight gates</name>
  <files>crates/bitaxe-asic/Cargo.toml, crates/bitaxe-asic/BUILD.bazel, crates/bitaxe-asic/src/bm1366.rs, crates/bitaxe-asic/src/bm1366/init_plan.rs, crates/bitaxe-asic/fixtures/bm1366/init-plan-cases.json</files>
  <read_first>
    - crates/bitaxe-asic/Cargo.toml
    - crates/bitaxe-asic/BUILD.bazel
    - crates/bitaxe-asic/src/bm1366/command.rs
    - crates/bitaxe-config/src/catalog.rs
    - crates/bitaxe-config/src/defaults.rs
    - reference/esp-miner/main/power/asic_init.c
    - reference/esp-miner/main/power/asic_reset.c
    - .planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `Bm1366InitPlan::chip_detect_only()` with board `205`, active Ultra 205 scope, model `BM1366`, count `1`, and config evidence emits reset, default-baud, read-chip-id, and read-exact actions.
    - Test 2: Missing board scope fails closed with `AsicInitStatus::PreflightMissing` and `FailClosedAction::HoldResetLow`.
    - Test 3: `Bm1366InitPlan::full_init()` without `PowerPreflightEvidence` and `ThermalPreflightEvidence` fails closed before register init, frequency, nonce setup, max baud, or initialized status.
    - Test 4: Chip-detect-only never emits `SendDiagnosticWork`.
  </behavior>
  <action>Add `bitaxe-config = { path = "../bitaxe-config" }` to `crates/bitaxe-asic/Cargo.toml` and `//crates/bitaxe-config:bitaxe_config` to the Bazel library deps. Create `bm1366/init_plan.rs` with `Bm1366InitPlan::{chip_detect_only, full_init}`, `Bm1366InitStage::{Preflight, Reset, UartDefaultBaud, ChipDetect, RegisterInit, FrequencyNonceSetup, MaxBaud, InitializedNoMining}`, `Bm1366Preflight`, `PowerPreflightEvidence`, `ThermalPreflightEvidence`, `ConfigPreflightEvidence`, `AsicInitStatus::{PreflightMissing, ChipDetectOnly, ChipDetectedNoMining, InitializedNoMining, FailedClosed}`, and `FailClosedAction::{HoldResetLow, DisableAsicEnable, NoHardwareEffect}`. Gate inputs must use `ultra_205_catalog_entry()` and require board version `205`, family `Ultra`, model `BM1366`, count `1`, `VerificationScope::ActiveUltra205`, `ds4432u=true`, `ina260=true`, `asic_enable=true`, and config defaults `485 MHz` and `1200 mV`. `chip_detect_only()` may run with board and config evidence but must publish `ChipDetectOnly`/`ChipDetectedNoMining` and no mining state. `full_init()` must require explicit `PowerPreflightEvidence` and `ThermalPreflightEvidence`; if either is absent, return fail-closed with reason `power_thermal_evidence_missing` and safe action `HoldResetLow`. Export `init_plan` from `bm1366.rs`, update `BUILD.bazel`, and add fixture cases for board mismatch, missing config, missing power/thermal, chip-count mismatch, and chip-detect success metadata.</action>
  <verify>
    <automated>cargo test -p bitaxe-asic init_plan --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "bitaxe-config.*path = \"\\.\\./bitaxe-config\"" crates/bitaxe-asic/Cargo.toml` finds the dependency.
    - `rg -n "//crates/bitaxe-config:bitaxe_config" crates/bitaxe-asic/BUILD.bazel` finds the Bazel dependency.
    - `rg -n "pub mod init_plan" crates/bitaxe-asic/src/bm1366.rs` finds the module export.
    - `rg -n "chip_detect_only|full_init|Bm1366InitStage|PowerPreflightEvidence|ThermalPreflightEvidence|power_thermal_evidence_missing|HoldResetLow" crates/bitaxe-asic/src/bm1366/init_plan.rs` finds required init contracts.
    - `rg -n "ultra_205_catalog_entry|ActiveUltra205|BM1366|205|485|1200|ds4432u|ina260|asic_enable" crates/bitaxe-asic/src/bm1366/init_plan.rs` finds all gate facts.
    - `! rg -n "SendDiagnosticWork" crates/bitaxe-asic/src/bm1366/init_plan.rs` succeeds, proving init planning does not emit work submission.
    - `rg -n "power_thermal_evidence_missing|chip-detect success|chip-count mismatch|ASIC-002|ASIC-005" crates/bitaxe-asic/fixtures/bm1366/init-plan-cases.json` finds fixture coverage.
  </acceptance_criteria>
  <done>BM1366 staged initialization is modeled as pure fail-closed decisions with chip-detect-only staging and no production work submission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add pure frequency and voltage transition decisions</name>
  <files>crates/bitaxe-asic/BUILD.bazel, crates/bitaxe-asic/src/bm1366.rs, crates/bitaxe-asic/src/bm1366/frequency_voltage.rs, crates/bitaxe-asic/fixtures/bm1366/init-plan-cases.json</files>
  <read_first>
    - crates/bitaxe-asic/src/bm1366/init_plan.rs
    - crates/bitaxe-asic/src/bm1366/registers.rs
    - crates/bitaxe-config/src/catalog.rs
    - crates/bitaxe-config/src/validation.rs
    - reference/esp-miner/components/asic/bm1366.c
    - reference/esp-miner/components/asic/asic_common.c
    - docs/adr/0012-parity-verification-evidence.md
  </read_first>
  <behavior>
    - Test 1: Frequency `485` and voltage `1200` produce accepted pure decisions for Ultra 205 BM1366.
    - Test 2: Frequency `390` and voltage `1400` return validation errors for Ultra 205 BM1366.
    - Test 3: A frequency decision emits a typed register-write command for register `0x08` but marks the hardware effect `MissingHardwareEvidence`.
    - Test 4: A voltage decision produces no firmware power effect in Phase 3 and marks parity `NotVerified`.
  </behavior>
  <action>Create `bm1366/frequency_voltage.rs` with `Bm1366FrequencyPlan`, `Bm1366VoltagePlan`, `HardwareEffectEvidence::{MissingHardwareEvidence, HardwareSmokeRecorded}`, and `ParityEffectStatus::{ImplementedNotVerified, VerifiedWithHardwareEvidence}`. Parse frequency values through `AsicFrequencyMhz::ultra_205_bm1366` and voltage values through `CoreVoltageMv::ultra_205_bm1366`; do not duplicate option arrays. Frequency decisions may produce typed register data for BM1366 frequency register `0x08` through `Bm1366Command::SetFrequency`, but must carry `HardwareEffectEvidence::MissingHardwareEvidence` until an Ultra 205 evidence file exists. Voltage decisions must be pure data only in this phase; do not add DS4432U, INA260, power, I2C, or GPIO side effects. Export `frequency_voltage` from `bm1366.rs`, update `BUILD.bazel`, and add fixture metadata for accepted `485 MHz`, accepted `1200 mV`, rejected `390 MHz`, rejected `1400 mV`, and unverified hardware-effect status tied to checklist ID `ASIC-007`.</action>
  <verify>
    <automated>cargo test -p bitaxe-asic frequency_voltage --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub mod frequency_voltage" crates/bitaxe-asic/src/bm1366.rs` finds the module export.
    - `rg -n "AsicFrequencyMhz::ultra_205_bm1366|CoreVoltageMv::ultra_205_bm1366" crates/bitaxe-asic/src/bm1366/frequency_voltage.rs` proves Phase 2 validation reuse.
    - `rg -n "Bm1366FrequencyPlan|Bm1366VoltagePlan|MissingHardwareEvidence|ImplementedNotVerified|VerifiedWithHardwareEvidence" crates/bitaxe-asic/src/bm1366/frequency_voltage.rs` finds transition status types.
    - `rg -n "0x08|SetFrequency|485|1200|390|1400" crates/bitaxe-asic/src/bm1366/frequency_voltage.rs crates/bitaxe-asic/fixtures/bm1366/init-plan-cases.json` finds concrete values.
    - `! rg -n "DS4432U|INA260|gpio|i2c|power" crates/bitaxe-asic/src/bm1366/frequency_voltage.rs` succeeds, proving no hardware side effects entered the pure crate.
    - `cargo test -p bitaxe-asic --all-features` passes.
  </acceptance_criteria>
  <done>Frequency and voltage transitions are range-checked pure decisions with explicit not-verified hardware-effect status and no firmware side effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config/catalog data -> ASIC init decision | Phase 2 board/config facts gate whether any ASIC init stage can run. |
| Pure init plan -> firmware effects | Adapter actions are emitted only after preflight decisions and may fail closed. |
| Frequency/voltage config -> hardware-sensitive transition | Numeric settings are parsed into domain values before any transition bytes are modeled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | `init_plan.rs` | mitigate | Missing board, config, power, thermal, or safety evidence returns `PreflightMissing` and `HoldResetLow` before register init, frequency, nonce, max baud, or initialized status. |
| T-03-04-02 | Denial of Service | Chip-detect/full-init staging | mitigate | Chip-detect-only is a separate plan path; full init is blocked without explicit power and thermal preflight tokens. |
| T-03-04-03 | Tampering | `frequency_voltage.rs` | mitigate | Reuse `AsicFrequencyMhz::ultra_205_bm1366` and `CoreVoltageMv::ultra_205_bm1366`; reject unsupported values before transition modeling. |
| T-03-04-04 | Repudiation | Hardware-effect status | mitigate | Transition decisions carry `MissingHardwareEvidence` and cannot be marked verified by unit tests alone. |
</threat_model>

<verification>
Run after this plan:

```bash
cargo test -p bitaxe-asic --all-features
bazel test //crates/bitaxe-asic:tests
rg -n "PreflightMissing|MissingHardwareEvidence|power_thermal_evidence_missing" crates/bitaxe-asic/src crates/bitaxe-asic/fixtures
git status --short reference/esp-miner
```
</verification>

<success_criteria>
- Chip-detect-only init staging exists and is pure.
- Full init fails closed when power or thermal preflight evidence is missing.
- Frequency and voltage options reuse Phase 2 validation types.
- No hardware-control effect is marked verified or executed by this pure plan.
</success_criteria>

<output>
After completion, create `.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-04-SUMMARY.md`
</output>
