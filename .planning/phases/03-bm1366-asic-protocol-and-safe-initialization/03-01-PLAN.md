---
phase: 03-bm1366-asic-protocol-and-safe-initialization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/bitaxe-asic/Cargo.toml
  - crates/bitaxe-asic/BUILD.bazel
  - crates/bitaxe-asic/src/lib.rs
  - crates/bitaxe-asic/src/error.rs
  - crates/bitaxe-asic/src/bm1366.rs
  - crates/bitaxe-asic/src/bm1366/crc.rs
  - crates/bitaxe-asic/src/bm1366/packet.rs
  - crates/bitaxe-asic/src/bm1366/registers.rs
  - crates/bitaxe-asic/fixtures/bm1366/protocol-cases.json
autonomous: true
requirements: [ASIC-01, ASIC-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 3-2026-06-26T22-40-40
generated_at: 2026-06-26T23:14:06Z
must_haves:
  truths:
    - "Developer can run BM1366 CRC, packet framing, and register command tests with upstream-compatible outputs."
    - "Raw preamble, length, CRC, and register bytes are constructed inside crates/bitaxe-asic, not firmware."
    - "BM1366 protocol files include behavior breadcrumbs to the pinned reference without copying C expression into MIT source."
  artifacts:
    - path: "crates/bitaxe-asic/src/bm1366.rs"
      provides: "BM1366 public module facade using bm1366.rs plus bm1366/ child modules"
    - path: "crates/bitaxe-asic/src/bm1366/crc.rs"
      provides: "CRC5 and CRC16-FALSE pure functions"
    - path: "crates/bitaxe-asic/src/bm1366/packet.rs"
      provides: "CommandFrame and JobFrame constructors with 0x55 0xAA transmit preamble"
    - path: "crates/bitaxe-asic/src/bm1366/registers.rs"
      provides: "Typed BM1366 register IDs and register command payloads"
    - path: "crates/bitaxe-asic/fixtures/bm1366/protocol-cases.json"
      provides: "Metadata-rich reference-derived protocol fixture cases"
  key_links:
    - from: "crates/bitaxe-asic/src/lib.rs"
      to: "crates/bitaxe-asic/src/bm1366.rs"
      via: "pub mod bm1366"
      pattern: "pub mod bm1366"
    - from: "crates/bitaxe-asic/src/bm1366/packet.rs"
      to: "crates/bitaxe-asic/src/bm1366/crc.rs"
      via: "CommandFrame uses CRC5 and JobFrame uses CRC16-FALSE"
      pattern: "crc5|crc16_false"
    - from: "crates/bitaxe-asic/BUILD.bazel"
      to: "crates/bitaxe-asic/fixtures/bm1366/protocol-cases.json"
      via: "fixture data available to Bazel tests"
      pattern: "fixtures/\\*\\*"
---

<objective>
Create the BM1366 protocol foundation in the pure ASIC crate.

Purpose: This plan replaces the Phase 1 deferred ASIC status enum with a compile-visible BM1366 module graph, typed protocol errors, CRC implementations, packet framing, register payload modeling, and provenance-rich fixtures for ASIC-01 and ASIC-08.
Output: `bitaxe-asic` owns CRC5, CRC16-FALSE, command/job frame construction, and typed register command payloads. Firmware still receives no raw BM1366 byte-building responsibility.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-CONTEXT.md
@.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-RESEARCH.md
@.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-VALIDATION.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/rust.md
@reference/esp-miner/components/asic/crc.c
@reference/esp-miner/components/asic/include/crc.h
@reference/esp-miner/components/asic/bm1366.c
@reference/esp-miner/components/asic/include/bm1366.h
@reference/esp-miner/components/asic/asic_common.c
@docs/adr/0005-read-only-reference-implementation.md
@docs/adr/0008-reference-breadcrumb-comments.md
@docs/adr/0013-mit-first-with-gpl-guardrails.md

<interfaces>
Existing crate surface to replace:
```rust
pub enum AsicRuntimeStatus {
    DeferredUntilPhase3,
}
```

Protocol constants from the pinned reference that this plan must encode:
```text
BM1366 chip id: 0x1366
Transmit preamble bytes: 0x55 0xAA
Command packet type bit: 0x40
Job packet type bit: 0x20
Group all bit: 0x10
Group single bit: 0x00
Command op set address: 0x00
Command op write: 0x01
Command op read: 0x02
Command op inactive: 0x03
Command total length: data_len + 5
Command length field: data_len + 3
Job total length: data_len + 6
Job length field: data_len + 4
Command CRC: CRC5 over frame bytes starting at header
Job CRC: CRC16-FALSE over frame bytes starting at header
CRC5 initial value: 0x1f
CRC16-FALSE initial value: 0xffff
CRC16 polynomial: 0x1021
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish BM1366 module graph and error contracts</name>
  <files>crates/bitaxe-asic/Cargo.toml, crates/bitaxe-asic/BUILD.bazel, crates/bitaxe-asic/src/lib.rs, crates/bitaxe-asic/src/error.rs, crates/bitaxe-asic/src/bm1366.rs, crates/bitaxe-asic/fixtures/bm1366/protocol-cases.json</files>
  <read_first>
    - crates/bitaxe-asic/Cargo.toml
    - crates/bitaxe-asic/BUILD.bazel
    - crates/bitaxe-asic/src/lib.rs
    - .planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-CONTEXT.md
    - .planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-RESEARCH.md
    - docs/adr/0008-reference-breadcrumb-comments.md
    - docs/adr/0013-mit-first-with-gpl-guardrails.md
  </read_first>
  <behavior>
    - Test 1: `bitaxe_asic::bm1366::BM1366_CHIP_ID` equals `0x1366`.
    - Test 2: `bitaxe_asic::bm1366::BM1366_RESULT_FRAME_LEN` equals `11`.
    - Test 3: `Bm1366ProtocolFault::BadCrc` renders an error string containing `bad BM1366 CRC`.
  </behavior>
  <action>Create `src/error.rs` with a `thiserror::Error` enum named `Bm1366ProtocolFault` containing at least `BadPreamble { expected: u16, actual: u16 }`, `BadCrc`, `InvalidLength { expected: usize, actual: usize }`, `UnknownRegister { register: u8 }`, `InvalidJobId { job_id: u8 }`, `ChipCountMismatch { expected: u8, actual: u8 }`, and `PreflightMissing { reason: &'static str }`. Update `src/lib.rs` to export `pub mod bm1366;` and `pub mod error;`, and re-export `Bm1366ProtocolFault`. Create `src/bm1366.rs` as the facade with module-level breadcrumbs to `reference/esp-miner/components/asic/bm1366.c`, `crc.c`, and checklist rows `ASIC-001`, `ASIC-002`, `ASIC-006`; define constants `BM1366_CHIP_ID: u16 = 0x1366` and `BM1366_RESULT_FRAME_LEN: usize = 11`. Add `thiserror.workspace = true` to `Cargo.toml`. Update `BUILD.bazel` to include `src/error.rs` and `src/bm1366.rs`, to expose `compile_data = glob(["fixtures/**"])`, and to add `@crates//:thiserror` to library deps. Create `fixtures/bm1366/protocol-cases.json` with fields `checklist_ids`, `source_file`, `source_behavior`, `reference_commit`, `license_posture`, `derivation`, and at least one `crc5` metadata case using reference commit `c1915b0a63bfabebdb95a515cedfee05146c1d50`. Do not create `crates/bitaxe-asic/src/bm1366/mod.rs`; the repo requires `bm1366.rs` plus `bm1366/`.</action>
  <verify>
    <automated>cargo test -p bitaxe-asic bm1366_contract --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub mod bm1366|pub mod error|pub use error::Bm1366ProtocolFault" crates/bitaxe-asic/src/lib.rs` finds all three exports.
    - `rg -n "BM1366_CHIP_ID.*0x1366|BM1366_RESULT_FRAME_LEN.*11" crates/bitaxe-asic/src/bm1366.rs` finds both constants.
    - `rg -n "BadPreamble|BadCrc|InvalidLength|UnknownRegister|InvalidJobId|ChipCountMismatch|PreflightMissing" crates/bitaxe-asic/src/error.rs` finds all fault variants.
    - `rg -n "thiserror\\.workspace = true" crates/bitaxe-asic/Cargo.toml` finds the dependency.
    - `rg -n "compile_data = glob\\(\\[\"fixtures/\\*\\*\"\\]\\)|@crates//:thiserror" crates/bitaxe-asic/BUILD.bazel` finds Bazel fixture and dependency wiring.
    - `rg -n "c1915b0a63bfabebdb95a515cedfee05146c1d50|license_posture|ASIC-006" crates/bitaxe-asic/fixtures/bm1366/protocol-cases.json` finds fixture provenance.
    - `rg -n "checklist_ids|source_file|source_behavior|reference_commit|license_posture|derivation" crates/bitaxe-asic/fixtures/bm1366/protocol-cases.json` finds all required provenance fields.
    - `test ! -e crates/bitaxe-asic/src/bm1366/mod.rs` succeeds.
  </acceptance_criteria>
  <done>The ASIC crate has a BM1366 module facade, typed error contract, Bazel/Cargo dependency wiring, and provenance metadata fixture seed without modifying `reference/esp-miner`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CRC, packet, and register codecs</name>
  <files>crates/bitaxe-asic/src/bm1366/crc.rs, crates/bitaxe-asic/src/bm1366/packet.rs, crates/bitaxe-asic/src/bm1366/registers.rs, crates/bitaxe-asic/src/bm1366.rs, crates/bitaxe-asic/BUILD.bazel, crates/bitaxe-asic/fixtures/bm1366/protocol-cases.json</files>
  <read_first>
    - crates/bitaxe-asic/src/bm1366.rs
    - crates/bitaxe-asic/BUILD.bazel
    - crates/bitaxe-asic/fixtures/bm1366/protocol-cases.json
    - reference/esp-miner/components/asic/crc.c
    - reference/esp-miner/components/asic/include/crc.h
    - reference/esp-miner/components/asic/bm1366.c
    - reference/esp-miner/components/asic/asic_common.c
    - .planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `crc5([0x52, 0x05, 0x00, 0x00])` returns `0x0a`, matching the upstream read-register-00 fixture `55 aa 52 05 00 00 0a`.
    - Test 2: A command frame for header `0x52` and data `[0x00, 0x00]` emits exactly `[0x55, 0xaa, 0x52, 0x05, 0x00, 0x00, 0x0a]`.
    - Test 3: A job frame uses CRC16-FALSE and appends two CRC bytes after the job payload, not a one-byte CRC5.
    - Test 4: Register IDs `0x4c`, `0x88`, `0x89`, `0x8a`, `0x8b`, and `0x8c` map to typed register categories, and unknown register `0xff` returns `Bm1366ProtocolFault::UnknownRegister`.
  </behavior>
  <action>Create `bm1366/crc.rs` with independently authored `crc5(data: &[u8]) -> u8` and `crc16_false(data: &[u8]) -> u16`; use CRC5 initial `0x1f`, CRC5 polynomial behavior `x^5 + x^2 + 1` MSB-first, CRC16-FALSE initial `0xffff`, and CRC16 polynomial `0x1021`. Do not copy the upstream `crc16_table` into MIT source; compute bitwise or generate internally with a short provenance comment. Create `bm1366/packet.rs` with `BM1366_COMMAND_PREAMBLE: [u8; 2] = [0x55, 0xaa]`, `COMMAND_HEADER_TYPE: u8 = 0x40`, `JOB_HEADER_TYPE: u8 = 0x20`, `GROUP_ALL: u8 = 0x10`, `GROUP_SINGLE: u8 = 0x00`, `CMD_SET_ADDRESS: u8 = 0x00`, `CMD_WRITE: u8 = 0x01`, `CMD_READ: u8 = 0x02`, `CMD_INACTIVE: u8 = 0x03`, a `FrameBytes` newtype, a `CommandFrame` constructor using total length `data_len + 5` and length field `data_len + 3`, and a `JobFrame` constructor using total length `data_len + 6` and length field `data_len + 4`. Create `bm1366/registers.rs` with `Bm1366Register` variants for `ErrorCount(0x4c)`, `Domain0Count(0x88)`, `Domain1Count(0x89)`, `Domain2Count(0x8a)`, `Domain3Count(0x8b)`, and `TotalCount(0x8c)`, plus typed payload builders for read, write, set-address, inactive-chain, version-mask register `0xa4`, hash-counting register `0x10`, frequency register `0x08`, misc-control register `0x18`, and difficulty mask register `0x14`. Export `crc`, `packet`, and `registers` from `bm1366.rs`. Extend `BUILD.bazel` `srcs` with the three new module files. Add fixture cases for the exact read-register-00 command frame and one register-write frame with metadata fields from Task 1.</action>
  <verify>
    <automated>cargo test -p bitaxe-asic bm1366_crc --all-features && cargo test -p bitaxe-asic bm1366_packet --all-features && cargo test -p bitaxe-asic bm1366_register --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub mod crc|pub mod packet|pub mod registers" crates/bitaxe-asic/src/bm1366.rs` finds all three child modules.
    - `rg -n "crc5|crc16_false|0x1f|0xffff|0x1021" crates/bitaxe-asic/src/bm1366/crc.rs` finds both functions and constants.
    - `rg -n "BM1366_COMMAND_PREAMBLE.*0x55.*0xaa|COMMAND_HEADER_TYPE.*0x40|JOB_HEADER_TYPE.*0x20|CMD_INACTIVE.*0x03" crates/bitaxe-asic/src/bm1366/packet.rs` finds packet constants.
    - `rg -n "data_len \\+ 5|data_len \\+ 3|data_len \\+ 6|data_len \\+ 4" crates/bitaxe-asic/src/bm1366/packet.rs` finds the command and job length rules.
    - `rg -n "0x4c|0x88|0x89|0x8a|0x8b|0x8c|0xa4|0x10|0x08|0x18|0x14" crates/bitaxe-asic/src/bm1366/registers.rs` finds required register IDs.
    - `rg -n "55 aa 52 05 00 00 0a|ASIC-006|reference/esp-miner/components/asic/crc.c" crates/bitaxe-asic/fixtures/bm1366/protocol-cases.json` finds the exact protocol fixture and breadcrumb.
    - `cargo test -p bitaxe-asic --all-features` passes.
  </acceptance_criteria>
  <done>BM1366 CRC, packet, and register codecs are implemented as pure Rust with reference-derived fixtures and breadcrumbs, and raw frame construction remains owned by `crates/bitaxe-asic`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reference data -> Rust fixtures | GPL-covered reference facts are transformed into metadata-rich fixtures and independent Rust tests. |
| Raw protocol bytes -> typed ASIC crate API | Packet bytes, CRCs, lengths, and registers are parsed or constructed before leaving `bitaxe-asic`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | `bm1366/packet.rs` | mitigate | Validate command/job length rules and CRCs in constructors; reject invalid lengths with `Bm1366ProtocolFault::InvalidLength` before an adapter can send bytes. |
| T-03-01-02 | Elevation of Privilege | Firmware packet construction | mitigate | Keep `FrameBytes` construction in `bitaxe-asic`; Plan 05 must fail static review if firmware contains `0x55`, `0xaa`, `crc5`, or `crc16` protocol construction. |
| T-03-01-03 | Supply Chain | Fixture and breadcrumb provenance | mitigate | Record `checklist_ids`, `source_file`, `source_behavior`, `reference_commit`, `license_posture`, and `derivation` in JSON fixtures; do not copy the upstream CRC table into MIT source. |
| T-03-01-04 | Repudiation | Checklist-backed parity claims | mitigate | This plan may only support `unit` or `golden` evidence for ASIC-001/ASIC-006; no hardware-control row may become `verified` from these tests. |
</threat_model>

<verification>
Run after this plan:

```bash
cargo test -p bitaxe-asic --all-features
bazel test //crates/bitaxe-asic:tests
git status --short reference/esp-miner

# Required Rust pre-commit sequence before committing implementation work:
cargo fmt --all
cargo clippy --all-targets --all-features -- -D warnings
cargo build --all-targets --all-features
cargo test --all-features
```
</verification>

<success_criteria>
- `crates/bitaxe-asic` no longer exposes only `DeferredUntilPhase3`.
- CRC5, CRC16-FALSE, command frame, job frame, and register tests pass.
- BM1366 protocol fixtures include reference breadcrumbs, checklist IDs, pinned commit, license posture, and manual derivation metadata.
- `reference/esp-miner` remains unmodified.
</success_criteria>

<output>
After completion, create `.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-01-SUMMARY.md`
</output>
