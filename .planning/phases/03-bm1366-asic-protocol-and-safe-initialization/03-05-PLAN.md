---
phase: 03-bm1366-asic-protocol-and-safe-initialization
plan: "05"
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - crates/bitaxe-asic/BUILD.bazel
  - crates/bitaxe-asic/src/bm1366.rs
  - crates/bitaxe-asic/src/bm1366/adapter_gate.rs
  - firmware/bitaxe/Cargo.toml
  - firmware/bitaxe/sdkconfig.defaults
  - firmware/bitaxe/src/main.rs
  - firmware/bitaxe/src/asic_adapter.rs
  - firmware/bitaxe/src/asic_adapter/uart.rs
  - firmware/bitaxe/src/asic_adapter/reset.rs
  - firmware/bitaxe/src/asic_adapter/status.rs
  - docs/parity/checklist.md
  - docs/parity/evidence/phase-03-ultra-205-bm1366-chip-detect.md
autonomous: false
requirements: [ASIC-04, ASIC-05, ASIC-07, ASIC-08]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 3-2026-06-26T22-40-40
generated_at: 2026-06-26T23:14:06Z
must_haves:
  truths:
    - "Firmware interprets typed BM1366 adapter actions through ESP-IDF UART/GPIO/status effects without constructing raw BM1366 packets."
    - "Default firmware remains fail-closed unless an explicit chip-detect diagnostic build is requested with a hardware-evidence acknowledgement."
    - "Live Ultra 205 chip-detect evidence is recorded or explicitly marked not run/skipped without claiming verified parity."
    - "Parity checklist rows separate pure unit evidence from live hardware evidence and never mark ASIC init verified without Ultra 205 logs."
  artifacts:
    - path: "firmware/bitaxe/src/asic_adapter.rs"
      provides: "Narrow firmware interpreter for Bm1366AdapterAction"
    - path: "firmware/bitaxe/src/asic_adapter/uart.rs"
      provides: "UART1 TX17/RX18 ESP-IDF adapter"
    - path: "firmware/bitaxe/src/asic_adapter/reset.rs"
      provides: "ASIC reset/hold-low adapter using reference GPIO defaults"
    - path: "firmware/bitaxe/src/asic_adapter/status.rs"
      provides: "Visible fail-closed ASIC status logs"
    - path: "crates/bitaxe-asic/src/bm1366/adapter_gate.rs"
      provides: "Host-testable compile-env diagnostic gate and default fail-closed status text"
    - path: "docs/parity/evidence/phase-03-ultra-205-bm1366-chip-detect.md"
      provides: "Chip-detect evidence record or explicit skipped-gate record"
  key_links:
    - from: "firmware/bitaxe/src/asic_adapter.rs"
      to: "crates/bitaxe-asic/src/bm1366/command.rs"
      via: "interprets Bm1366AdapterAction"
      pattern: "Bm1366AdapterAction"
    - from: "firmware/bitaxe/src/main.rs"
      to: "firmware/bitaxe/src/asic_adapter/status.rs"
      via: "logs asic_status fail-closed reason"
      pattern: "asic_status="
    - from: "firmware/bitaxe/src/asic_adapter.rs"
      to: "crates/bitaxe-asic/src/bm1366/adapter_gate.rs"
      via: "uses pure compile-env gate before UART/GPIO effects"
      pattern: "AsicAdapterMode|default_fail_closed_status_log"
    - from: "docs/parity/checklist.md"
      to: "docs/parity/evidence/phase-03-ultra-205-bm1366-chip-detect.md"
      via: "ASIC rows cite Phase 3 evidence without false verified status"
      pattern: "phase-03-ultra-205-bm1366-chip-detect"
---

<objective>
Add the narrow firmware UART/reset/status adapter and Phase 3 evidence gate.

Purpose: ASIC-04, ASIC-05, and ASIC-07 require a firmware boundary that can interpret typed ASIC actions while staying fail-closed and evidence-scoped. Live chip-detect is human-gated because it touches connected Ultra 205 hardware.
Output: Firmware can build with a typed BM1366 adapter, default boot remains safe/fail-closed, chip-detect diagnostic mode is explicitly gated, and parity evidence/checklist rows reflect what was actually proven.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-CONTEXT.md
@.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-RESEARCH.md
@.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-VALIDATION.md
@.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-04-SUMMARY.md
@firmware/bitaxe/src/main.rs
@firmware/bitaxe/Cargo.toml
@firmware/bitaxe/sdkconfig.defaults
@reference/esp-miner/components/asic/include/serial.h
@reference/esp-miner/components/asic/serial.c
@reference/esp-miner/main/power/asic_reset.c
@reference/esp-miner/main/Kconfig.projbuild
@docs/parity/checklist.md
@docs/adr/0012-parity-verification-evidence.md
@docs/adr/0013-mit-first-with-gpl-guardrails.md

<interfaces>
Firmware adapter facts from the pinned reference:
```text
UART unit: UART1
UART initial baud: 115200
UART TX pin: IO17
UART RX pin: IO18
UART data bits: 8
UART parity: disabled
UART stop bits: 1
UART flow control: disabled
UART RX buffer: BUF_SIZE * 2 with BUF_SIZE 1024
Chip-detect read timeout: 1000 ms
Work/result read timeout: 10000 ms
Baud change: wait TX done with 1000 ms timeout before setting baud
Reset GPIO Kconfig default: CONFIG_GPIO_ASIC_RESET=1
ASIC enable GPIO Kconfig default: CONFIG_GPIO_ASIC_ENABLE=10
Reset pulse: low 100 ms, high 100 ms
```

Existing safe boot log invariant:
```rust
safe_state: mining=disabled asic_work_submission=disabled hardware_control=disabled
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement narrow firmware ASIC adapter with default fail-closed status</name>
  <files>crates/bitaxe-asic/BUILD.bazel, crates/bitaxe-asic/src/bm1366.rs, crates/bitaxe-asic/src/bm1366/adapter_gate.rs, firmware/bitaxe/Cargo.toml, firmware/bitaxe/sdkconfig.defaults, firmware/bitaxe/src/main.rs, firmware/bitaxe/src/asic_adapter.rs, firmware/bitaxe/src/asic_adapter/uart.rs, firmware/bitaxe/src/asic_adapter/reset.rs, firmware/bitaxe/src/asic_adapter/status.rs</files>
  <read_first>
    - firmware/bitaxe/Cargo.toml
    - firmware/bitaxe/sdkconfig.defaults
    - firmware/bitaxe/src/main.rs
    - crates/bitaxe-asic/src/bm1366.rs
    - crates/bitaxe-asic/src/bm1366/command.rs
    - crates/bitaxe-asic/src/bm1366/init_plan.rs
    - reference/esp-miner/components/asic/serial.c
    - reference/esp-miner/main/power/asic_reset.c
    - reference/esp-miner/main/Kconfig.projbuild
    - docs/adr/0012-parity-verification-evidence.md
  </read_first>
  <behavior>
    - Test 1: Host unit test proves `default_fail_closed_status_log()` returns exactly `asic_status=preflight_missing reason=hardware_evidence_ack_missing initialized=false mining=disabled work_submission=disabled`.
    - Test 2: Host unit test proves `AsicAdapterMode::from_compile_env(None, None)` returns `FailClosed`.
    - Test 3: Host unit test proves `AsicAdapterMode::from_compile_env(Some("chip-detect"), Some("ultra205-chip-detect-safe-bench"))` returns `ChipDetectOnly`.
    - Test 4: Firmware source contains no raw BM1366 frame constants `0x55`, `0xaa`, `0x1366`, `crc5`, or `crc16`.
  </behavior>
  <action>Create `crates/bitaxe-asic/src/bm1366/adapter_gate.rs` and export it from `bm1366.rs`. The module must contain host-testable pure logic: `AsicAdapterMode::{FailClosed, ChipDetectOnly}`, `default_fail_closed_status_log()`, and `AsicAdapterMode::from_compile_env(maybe_diagnostic: Option<&str>, maybe_hardware_evidence_ack: Option<&str>)`. Unit tests in this module must cover the exact default fail-closed log string, missing diagnostic env, missing ack env, wrong ack env, and the accepted `chip-detect` plus `ultra205-chip-detect-safe-bench` gate. Update `crates/bitaxe-asic/BUILD.bazel` for the new source. Add firmware path dependencies `bitaxe-asic = { path = "../../crates/bitaxe-asic" }` and `bitaxe-config = { path = "../../crates/bitaxe-config" }`. Add `CONFIG_GPIO_ASIC_RESET=1` and `CONFIG_GPIO_ASIC_ENABLE=10` to `firmware/bitaxe/sdkconfig.defaults` with breadcrumbs to `reference/esp-miner/main/Kconfig.projbuild`; the adapter may drive reset hold-low and ASIC-enable low only as fail-closed actions in this phase, never enable power as a success path. Create `firmware/bitaxe/src/asic_adapter.rs` plus `asic_adapter/uart.rs`, `reset.rs`, and `status.rs`. `asic_adapter.rs` must interpret `Bm1366AdapterAction` and use `AsicAdapterMode::from_compile_env(option_env!("BITAXE_ASIC_DIAGNOSTIC"), option_env!("BITAXE_HARDWARE_EVIDENCE_ACK"))`; without both accepted values, publish `default_fail_closed_status_log()` and perform no UART or GPIO effects. `uart.rs` must use `esp_idf_svc::hal` or existing ESP-IDF Rust bindings for UART1 at `115_200`, TX `17`, RX `18`, 8N1, no flow control, read exact `11` bytes with `1_000 ms` for chip detect, preserve `10_000 ms` as the result/work timeout constant, call wait-TX-done with `1_000 ms` before baud changes, and clear RX after malformed/partial responses. `reset.rs` must implement reset pulse low `100 ms` then high `100 ms`, and hold reset low, using the configured reset GPIO value `1`. Update `main.rs` to keep the existing safe-state log and then call only the adapter status gate by default; chip-detect mode may run only when both compile-time env gates are present. Do not add API telemetry, Stratum, production work submission, voltage control, fan control, thermal control, or mining behavior.</action>
  <verify>
    <automated>cargo test -p bitaxe-asic adapter_gate --all-features && source "$HOME/export-esp.sh" && cargo check -p bitaxe-firmware --target xtensa-esp32s3-espidf</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub mod adapter_gate" crates/bitaxe-asic/src/bm1366.rs` finds the pure gate export.
    - `rg -n "default_fail_closed_status_log|AsicAdapterMode|from_compile_env|hardware_evidence_ack_missing|ultra205-chip-detect-safe-bench" crates/bitaxe-asic/src/bm1366/adapter_gate.rs` finds host-testable gate logic.
    - `cargo test -p bitaxe-asic adapter_gate --all-features` passes before firmware cargo check.
    - `rg -n "bitaxe-asic|bitaxe-config" firmware/bitaxe/Cargo.toml` finds both dependencies.
    - `rg -n "CONFIG_GPIO_ASIC_RESET=1|CONFIG_GPIO_ASIC_ENABLE=10" firmware/bitaxe/sdkconfig.defaults` finds GPIO defaults.
    - `rg -n "AsicAdapterMode|BITAXE_ASIC_DIAGNOSTIC|BITAXE_HARDWARE_EVIDENCE_ACK|ultra205-chip-detect-safe-bench|Bm1366AdapterAction" firmware/bitaxe/src/asic_adapter.rs` finds the diagnostic gate and typed action boundary.
    - `rg -n "115_200|17|18|1_000|10_000|wait_tx_done|clear_rx|Uart" firmware/bitaxe/src/asic_adapter/uart.rs` finds UART constants and operations.
    - `rg -n "100|HoldResetLow|ASIC_RESET_GPIO|1" firmware/bitaxe/src/asic_adapter/reset.rs` finds reset fail-closed behavior.
    - `rg -n "asic_status=preflight_missing reason=hardware_evidence_ack_missing initialized=false mining=disabled work_submission=disabled" firmware/bitaxe/src/main.rs firmware/bitaxe/src/asic_adapter/status.rs` finds the exact visible fail-closed status.
    - `! rg -n "0x55|0xaa|0x1366|crc5|crc16" firmware/bitaxe/src` succeeds, proving raw BM1366 protocol construction did not leak into firmware.
  </acceptance_criteria>
  <done>Firmware has a typed, fail-closed ASIC adapter boundary; default boot remains no-mining/no-work/no-control unless a chip-detect diagnostic build is explicitly requested.</done>
</task>

<task type="auto">
  <name>Task 2: Record Phase 3 parity evidence boundaries and checklist updates</name>
  <files>docs/parity/checklist.md, docs/parity/evidence/phase-03-ultra-205-bm1366-chip-detect.md</files>
  <read_first>
    - docs/parity/checklist.md
    - docs/parity/evidence/phase-02-ultra-205-config-nvs-model.md
    - .planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-CONTEXT.md
    - .planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-VALIDATION.md
    - docs/adr/0006-parity-checklist-as-audit-evidence.md
    - docs/adr/0012-parity-verification-evidence.md
    - docs/adr/0013-mit-first-with-gpl-guardrails.md
  </read_first>
  <action>Create `docs/parity/evidence/phase-03-ultra-205-bm1366-chip-detect.md` with sections `Command`, `Board`, `Port`, `Firmware commit`, `Reference commit`, `Relevant logs`, `Observed result`, `Skipped gates`, and `Conclusion`. Pre-fill the command template using `bazel build --action_env=BITAXE_ASIC_DIAGNOSTIC=chip-detect --action_env=BITAXE_HARDWARE_EVIDENCE_ACK=ultra205-chip-detect-safe-bench //firmware/bitaxe:firmware_image` followed by `bazel run //tools/flash:flash -- flash-monitor --board 205 --port <port> --image bazel-bin/firmware/bitaxe/bitaxe-ultra205.elf --evidence-dir docs/parity/evidence/phase-03-ultra-205-bm1366-chip-detect`. Record default conclusion as `not run - hardware verification pending` until the human checkpoint captures logs. Update `docs/parity/checklist.md` rows `ASIC-001` through `ASIC-007`: mark pure rows with unit/golden evidence only where Plans 03-01 through 03-04 produced tests, mark live init/serial/frequency rows no higher than `implemented` or `in-progress`, cite this evidence file, and keep all safety-critical hardware-control rows below `verified` unless the checkpoint adds board-named Ultra 205 hardware-smoke evidence. Leave `ASIC-008` BM1370 deferred, and do not update ASIC-009/BM1368 or ASIC-010/BM1397 as verified.</action>
  <verify>
    <automated>just parity && rg -n "phase-03-ultra-205-bm1366-chip-detect|not run - hardware verification pending|ASIC-00[1-7]" docs/parity/checklist.md docs/parity/evidence/phase-03-ultra-205-bm1366-chip-detect.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "^## Command|^## Board|^## Port|^## Firmware commit|^## Reference commit|^## Relevant logs|^## Observed result|^## Skipped gates|^## Conclusion" docs/parity/evidence/phase-03-ultra-205-bm1366-chip-detect.md` finds all required evidence sections.
    - `rg -n "BITAXE_ASIC_DIAGNOSTIC=chip-detect|BITAXE_HARDWARE_EVIDENCE_ACK=ultra205-chip-detect-safe-bench|flash-monitor --board 205" docs/parity/evidence/phase-03-ultra-205-bm1366-chip-detect.md` finds the exact command template.
    - `rg -n "not run - hardware verification pending" docs/parity/evidence/phase-03-ultra-205-bm1366-chip-detect.md` finds the default conclusion unless live logs were captured.
    - `rg -n "ASIC-001|ASIC-002|ASIC-003|ASIC-004|ASIC-005|ASIC-006|ASIC-007" docs/parity/checklist.md` finds all updated ASIC rows.
    - `just parity` reports `validation_errors: none`.
    - `! rg -n "ASIC-00[2-7].*verified.*unit|ASIC-00[2-7].*verified.*golden" docs/parity/checklist.md` succeeds, proving safety-critical live behavior was not verified from pure tests alone.
  </acceptance_criteria>
  <done>Parity evidence distinguishes pure fixture coverage from live hardware evidence, and no ASIC initialization, serial, frequency, or voltage row is falsely verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-gated Ultra 205 chip-detect smoke review</name>
  <files>docs/parity/evidence/phase-03-ultra-205-bm1366-chip-detect.md, docs/parity/checklist.md</files>
  <read_first>
    - docs/parity/evidence/phase-03-ultra-205-bm1366-chip-detect.md
    - docs/parity/checklist.md
    - docs/parity/evidence/ultra-205-pivot-safe-state-smoke-2026-06-26.md
    - .planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-CONTEXT.md
  </read_first>
  <action>Pause after automated build/checklist work. The user must confirm whether a real Ultra 205 is connected in a safe bench setup and whether chip-detect diagnostic mode may run. If approved, run the exact command sequence from the evidence file with the actual port, capture `flash-monitor.log`, and update the evidence file with board `Ultra 205`, board version `205`, ASIC `BM1366`, port, firmware commit, reference commit `c1915b0a63bfabebdb95a515cedfee05146c1d50`, relevant `asic_status`/chip-detect logs, observed chip count, skipped power/thermal gates, and fail-closed conclusion. If not approved or no port is visible, keep the evidence conclusion `not run - hardware verification pending` and leave checklist rows below `verified`.</action>
  <verify>
    <automated>rg -n "Conclusion|not run - hardware verification pending|hardware-smoke|Ultra 205|BM1366" docs/parity/evidence/phase-03-ultra-205-bm1366-chip-detect.md docs/parity/checklist.md</automated>
  </verify>
  <acceptance_criteria>
    - Evidence file either contains `not run - hardware verification pending` or contains all of: `Ultra 205`, `205`, `BM1366`, `c1915b0a63bfabebdb95a515cedfee05146c1d50`, `flash-monitor.log`, and `fail-closed`.
    - If `docs/parity/checklist.md` contains `ASIC-002` or `ASIC-005` with `verified`, the evidence file must contain `hardware-smoke` and board-named Ultra 205 chip-detect logs.
    - If live run is skipped, `just parity` must still report `validation_errors: none`.
  </acceptance_criteria>
  <done>Human verification either captures live chip-detect evidence safely or explicitly records that hardware evidence remains pending without overclaiming parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pure ASIC actions -> ESP-IDF effects | Typed adapter actions cross into UART/GPIO/status side effects. |
| Firmware default mode -> diagnostic chip detect | Compile-time diagnostic gates decide whether live hardware actions run. |
| Hardware logs -> parity checklist | Evidence files determine whether safety-critical rows may be verified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Elevation of Privilege | Firmware ASIC adapter | mitigate | Firmware interprets `Bm1366AdapterAction` only; static grep blocks raw `0x55`, `0xaa`, `0x1366`, `crc5`, or `crc16` packet construction in firmware. |
| T-03-05-02 | Tampering | Diagnostic mode gate | mitigate | Chip-detect effects require both `BITAXE_ASIC_DIAGNOSTIC=chip-detect` and `BITAXE_HARDWARE_EVIDENCE_ACK=ultra205-chip-detect-safe-bench`; default mode logs fail-closed status and performs no UART/GPIO effects. |
| T-03-05-03 | Denial of Service | Reset/GPIO effects | mitigate | Phase 3 may hold reset low or ASIC-enable low only as fail-closed actions; it must not enable power, voltage, fan, thermal, mining, or production work paths. |
| T-03-05-04 | Repudiation | Checklist/evidence | mitigate | `just parity` plus grep checks block `verified` safety-critical rows unless hardware-smoke evidence exists for the named Ultra 205 board. |
| T-03-05-05 | Supply Chain | Reference/provenance | mitigate | Evidence records the pinned reference commit and `reference/esp-miner` remains read-only; no copied C expression enters firmware source. |
</threat_model>

<verification>
Run after this plan:

```bash
cargo test -p bitaxe-asic --all-features
cargo test -p bitaxe-asic adapter_gate --all-features
source "$HOME/export-esp.sh" && cargo check -p bitaxe-firmware --target xtensa-esp32s3-espidf
bazel test //crates/bitaxe-asic:tests
bazel build //firmware/bitaxe:firmware
just parity
git status --short reference/esp-miner

# Required Rust pre-commit sequence before committing implementation work:
cargo fmt --all
cargo clippy --all-targets --all-features -- -D warnings
cargo build --all-targets --all-features
cargo test --all-features
```
</verification>

<success_criteria>
- Firmware compiles with `bitaxe-asic` and `bitaxe-config` dependencies.
- Default fail-closed status and diagnostic compile-env gate have host-testable `bitaxe-asic` unit coverage.
- Raw BM1366 packet construction remains absent from firmware.
- Default boot logs a fail-closed ASIC status while preserving safe-state mining/work/control disabled logs.
- Chip-detect diagnostic mode is human-gated and evidence-scoped.
- ASIC checklist rows do not claim verified safety-critical behavior without Ultra 205 hardware-smoke evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/03-bm1366-asic-protocol-and-safe-initialization/03-05-SUMMARY.md`
</output>
