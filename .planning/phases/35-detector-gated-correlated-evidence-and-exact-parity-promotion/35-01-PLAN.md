---
phase: 35-detector-gated-correlated-evidence-and-exact-parity-promotion
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
phase_lifecycle_id: 35-2026-07-17T17-00-37
lifecycle_mode: yolo
generated_by: gsd-plan-phase
generated_at: "2026-07-17T20:30:00Z"
requirements:
  - EVD-11
  - EVD-12
  - EVD-14
files_modified:
  - tools/parity/src/phase35_evidence.rs
  - tools/parity/src/main.rs
  - tools/parity/fixtures/phase35/eligible.json
  - tools/parity/fixtures/phase35/invalid/
  - tools/parity/BUILD.bazel
must_haves:
  truths:
    - "One typed evidence root binds exact source, reference, package, board-205, target-lock, run, inventory, and two-epoch provenance."
    - "Boot A and Boot B are each internally coherent; only the Phase 35 join may correlate them across the one approved reboot."
    - "Malformed paths, inventory drift, chronology drift, mixed sessions, identity drift, or incomplete cleanup fail closed before admission."
    - "The shareable projection is constructed from a typed allowlist and cannot contain raw device, network, target, credential, or secret values."
  artifacts:
    - path: tools/parity/src/phase35_evidence.rs
      provides: "Pure Phase 35 evidence schema, validator, two-epoch join, inventory and redaction evaluator"
    - path: tools/parity/fixtures/phase35/eligible.json
      provides: "Minimal eligible two-epoch fixture"
    - path: tools/parity/fixtures/phase35/invalid/
      provides: "Fail-closed mutation fixtures for every evidence boundary"
  key_links:
    - from: tools/parity/src/phase35_evidence.rs
      to: tools/flash/src/package_admission.rs
      via: "typed ExactPackageCapability consumes canonical manifest-v3 and immutable executable/factory image admission"
    - from: tools/parity/src/phase35_evidence.rs
      to: tools/parity/src/operator_snapshot_evidence.rs
      via: "each boot-local epoch is validated by the existing coherent snapshot contract"
    - from: tools/parity/src/phase35_evidence.rs
      to: tools/parity/src/operator_evidence/inventory.rs
      via: "content-addressed inventory and safe relative-path validation"
---

<objective>
Implement the pure typed admission boundary for one protected Phase 35 evidence root.

Purpose: make EVD-11, EVD-12, and the evidence-validation portion of EVD-14 deterministic and exhaustively testable before any hardware action exists.
Output: a dependency-light Phase 35 schema/evaluator, CLI validation entrypoint, eligible fixture, and negative mutation suite.
</objective>

<execution_context>
@.planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-CONTEXT.md
@.planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-RESEARCH.md
@.planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-VALIDATION.md
</execution_context>

<threat-model>

| ID | Threat | Severity | Required mitigation and verification |
| --- | --- | --- | --- |
| T-35-01 | Evidence from a different source, reference, manifest-v3, executable/factory image, package, detector capability, board, or target lock is spliced into an eligible root. | critical | Bind every artifact and both epochs to typed exact-package and detector-run capabilities plus one root contract; mutation fixtures must reject each mismatch. |
| T-35-02 | Boot-local snapshot validators are weakened so mixed-session data appears coherent. | high | Keep existing validators unchanged; validate each epoch separately, then join only `N` to `N+1` under the Phase 35 type. |
| T-35-03 | Path traversal, symlinks, duplicate roles, missing files, or extra files bypass inventory checks. | high | Accept only normalized relative paths and an exact role set; reject symlinks and recompute every digest. |
| T-35-04 | A redacted projection exports a raw device, network, target, credential, hostname, or secret value. | critical | Construct output from a closed typed allowlist and run forbidden-key/raw-canary tests before serialization. |

</threat-model>

<tasks>

<task type="auto" id="35-01-01">
  <name>Implement the typed two-epoch evidence root and invariant evaluator</name>
  <read_first>
    - .planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-CONTEXT.md
    - .planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-RESEARCH.md
    - tools/parity/src/operator_snapshot_evidence.rs
    - tools/parity/src/operator_evidence/inventory.rs
    - tools/parity/src/v12_admission.rs
    - tools/flash/src/package_admission.rs
    - tools/parity/src/main.rs
    - tools/parity/BUILD.bazel
  </read_first>
  <files>
    - tools/parity/src/phase35_evidence.rs
    - tools/parity/src/main.rs
    - tools/parity/BUILD.bazel
  </files>
  <action>
Create `phase35_evidence.rs` as a pure functional core with serde-backed public inputs and private validated outputs. Define:

- `ExactPackageCapability` with `source_commit`, `reference_commit`, `reference_clean`, `manifest_schema = "manifest-v3"`, `manifest_digest`, `executable_image_digest`, `factory_image_digest`, `package_digest`, `runtime_identity_digest`, `current_head_verified`, and `capability_digest`.
- `DetectorRunCapability` with `board_category`, `detector_capability_digest`, `physical_identity_digest`, `board_info_verified`, `single_candidate_verified`, `run_id_digest`, and `capability_digest`.
- `RootAdmissionFacts` with `root_contract_digest`, `target_lock_digest`, `lifecycle_id`, `lifecycle_verified`, `current_head_rechecked`, `reference_cleanliness_rechecked`, `runtime_identity_rechecked`, `no_actuation_verified`, `inventory_verified`, `chronology_verified`, `restoration_verified`, `cleanup_verified`, and `redaction_verified`.
- `EvidenceEventInput` with checked monotonic `sequence`, `category`, `monotonic_millis`, `payload_digest`, and `predecessor_event_digest`; the first event uses the root-contract digest as its predecessor and every subsequent event uses the digest of the immediately prior canonical event.
- `Phase35EvidenceRootInput` with `schema_version`, `exact_package`, `detector_run`, `admission_facts`, ordered `events`, ordered `inventory`, `boot_a`, and `boot_b`.
- `EvidenceEpochInput` with `boot_ordinal`, `boot_session_digest`, monotonic `started_millis`/`ended_millis`, coherent system-info/WebSocket/retained-log evidence, storage revision/value digest, and reset category.
- `ValidatedPhase35Evidence` exposing only digests, enums, booleans, counts, durations, ordinals, and existing redacted coherent-snapshot facts.
- `Phase35EvidenceError` variants for unsupported schema, invalid digest, manifest-v3 mismatch, executable/factory/package/runtime identity mismatch, invalid package capability, invalid detector capability, root-contract mismatch, wrong board, inventory mismatch, unsafe path, symlink, chronology/predecessor violation, duplicate/missing checkpoint, mixed session, boot ordinal mismatch, unapproved reset, physical identity drift, value/revision mismatch, stale current head, dirty reference, lifecycle mismatch, no-actuation failure, restoration failure, cleanup failure, and forbidden projection field.

Require schema version `phase35-evidence-v1`, manifest schema `manifest-v3`, board category exactly `205`, lowercase 64-hex digests, Boot B ordinal exactly Boot A ordinal plus one with checked arithmetic, reset category exactly `software_cpu`, same physical-identity digest, strictly increasing chronology and sequence, exact predecessor-event linkage, and one exact-package/detector/root provenance tuple across both epochs. Recompute and validate both capability digests and the root-contract digest. Validate Boot A and Boot B independently with the existing Phase 34 snapshot evidence API; do not relax or bypass its same-session checks.

Add safe relative-path validation that rejects absolute paths, `..`, empty components, and symlinks; require an exact declared role set and recompute SHA-256 for every file through the existing inventory API. `ValidatedPhase35Evidence::shareable_projection()` must build a closed structure rather than clone/filter input JSON.

Wire a read-only `validate-phase35-evidence --root <path>` subcommand into `main.rs`. It must require all `RootAdmissionFacts` to be true and must live-recheck lifecycle ID, current HEAD, reference cleanliness, exact package/runtime identity, and no-actuation evidence before returning eligibility. It may print only the redacted projection and one root digest; errors must not echo raw values or local paths.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-parity phase35_evidence --all-features &amp;&amp; bazel test //tools/parity:tests &amp;&amp; git diff --check</automated>
  </verify>
  <done>The parity tool can parse and validate one two-epoch Phase 35 root without hardware or side effects.</done>
  <acceptance_criteria>
    - `tools/parity/src/phase35_evidence.rs` contains `ExactPackageCapability`, `DetectorRunCapability`, `RootAdmissionFacts`, `EvidenceEventInput`, `Phase35EvidenceRootInput`, `EvidenceEpochInput`, `ValidatedPhase35Evidence`, and `Phase35EvidenceError`.
    - The evaluator accepts only `phase35-evidence-v1`, `manifest-v3`, board category `205`, exact lowercase 64-hex digests, boot ordinals `N` and `N+1`, and reset category `software_cpu`.
    - Tests prove manifest, executable-image, factory-image, package, runtime-identity, package-capability, detector-capability, root-contract, event, and predecessor digests are recomputed and bound.
    - Tests prove current-head, reference-cleanliness, lifecycle, runtime-identity, no-actuation, restoration, cleanup, inventory, chronology, and redaction facts are mandatory at eligibility time.
    - Each boot epoch uses the existing coherent snapshot validator and a test proves a mixed boot-local session is rejected.
    - Inventory tests reject absolute paths, traversal, symlinks, duplicate roles, missing roles, extra roles, and digest mismatches.
    - The CLI emits no raw local path or input value on success or error.
    - `bazel test //tools/parity:tests` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" id="35-01-02">
  <name>Add eligible and exhaustive negative fixtures for evidence admission</name>
  <read_first>
    - tools/parity/src/phase35_evidence.rs
    - tools/parity/fixtures/operator-snapshot/
    - .planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-VALIDATION.md
  </read_first>
  <files>
    - tools/parity/fixtures/phase35/eligible.json
    - tools/parity/fixtures/phase35/invalid/
    - tools/parity/BUILD.bazel
  </files>
  <action>
Add one minimal eligible fixture and table-driven mutation tests for: source/reference/manifest-v3/executable-image/factory-image/package/runtime-identity drift; package-capability/detector-capability/root-contract/target-lock drift; current-head/reference-cleanliness/lifecycle/no-actuation false; wrong board; invalid digest; missing/extra/duplicate inventory role; path traversal; symlink; digest mismatch; chronology or sequence inversion; broken predecessor digest; duplicate checkpoint; mixed boot session; skipped or additional reboot; wrong reset category; physical identity drift; storage revision mismatch; hostname digest mismatch; restoration false; process-tree cleanup false; unexpected serial holder; forbidden raw key; and a raw-value canary appearing in the shareable projection.

Fixtures must contain only synthetic values. Use Arrange/Act/Assert sections and assert the exact `Phase35EvidenceError` variant for one concern per test.
  </action>
  <verify>
    <automated>cargo test -p bitaxe-parity phase35_evidence --all-features &amp;&amp; bazel test //tools/parity:tests &amp;&amp; git diff --check</automated>
  </verify>
  <done>Every typed Phase 35 admission boundary has a deterministic negative regression guard.</done>
  <acceptance_criteria>
    - `eligible.json` passes the validator and uses only synthetic digests/categories.
    - Every mandatory negative fixture listed in `35-VALIDATION.md` maps to an exact error assertion.
    - A completeness test fails if an invalid-fixture category is removed from the test table.
    - No fixture contains an HTTP origin, IP address, MAC address, device node, SSID, BTC address, worker, pool endpoint, credential, or secret.
  </acceptance_criteria>
</task>

</tasks>

<verification>

- Run the automated command for both tasks.
- Run `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features` in order.
- Run `bazel test //tools/parity:tests`, `just verify-reference`, and `git diff --check`.
- Review the diff for hardware I/O, target discovery, credential access, reset, serial, HTTP mutation, checklist mutation, or promotion side effects; any such code fails this plan.

</verification>

<success_criteria>

- EVD-11 provenance and inventory are exact, content-addressed, and fail closed.
- EVD-12 validates two coherent boot-local epochs without weakening Phase 34.
- EVD-14 evidence/redaction gates are pure, exhaustive, and independently testable.
- No hardware, credential, promotion, checklist, or archived-lineage action occurs.

</success_criteria>

<output>
After completion, create `.planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-01-SUMMARY.md`; use normal GSD atomic task commits and do not push.
</output>
