---
phase: 35-detector-gated-correlated-evidence-and-exact-parity-promotion
plan: "04"
type: execute
wave: 4
depends_on:
  - "35-01"
  - "35-02"
  - "35-03"
autonomous: true
phase_lifecycle_id: 35-2026-07-17T17-00-37
lifecycle_mode: yolo
generated_by: gsd-plan-phase
generated_at: "2026-07-17T20:33:00Z"
requirements:
  - CFG-12
  - EVD-10
  - EVD-11
  - EVD-12
  - EVD-13
  - EVD-14
  - EVD-15
files_modified:
  - docs/parity/evidence/phase-35-detector-gated-correlated-evidence-and-exact-parity-promotion/
  - docs/parity/checklist.md
  - .planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-HARDWARE-EVIDENCE.md
must_haves:
  truths:
    - "Every fresh qualification attempt uses the exact current source/reference/package after all software gates pass and detector proves exactly one Ultra 205; the first eligible root completes the phase."
    - "The admitted root jointly proves CFG-12 and EVD-13 across Boot A and exactly one approved normal reboot to same-board Boot B."
    - "Restoration, process/serial cleanup, inventory, redaction, no-actuation, reference-cleanliness, current-head, and lifecycle validation pass before admission."
    - "Only dedicated allowlisted rows promote; every excluded or broad scope remains a typed non-promotion."
  artifacts:
    - path: docs/parity/evidence/phase-35-detector-gated-correlated-evidence-and-exact-parity-promotion/
      provides: "Admitted redacted evidence projection and typed decision manifest"
    - path: .planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-HARDWARE-EVIDENCE.md
      provides: "Redacted execution record with commands, categories, digests, gates, behavior, and conclusion"
    - path: docs/parity/checklist.md
      provides: "Only exact dedicated row promotions from the admitted root"
  key_links:
    - from: .planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-HARDWARE-EVIDENCE.md
      to: docs/parity/evidence/phase-35-detector-gated-correlated-evidence-and-exact-parity-promotion/
      via: "one redacted admitted evidence-root digest"
    - from: docs/parity/evidence/phase-35-detector-gated-correlated-evidence-and-exact-parity-promotion/
      to: docs/parity/checklist.md
      via: "closed Phase35PromotionMatrix and atomic generation"
---

<objective>
Run progress-gated fresh detector-gated Ultra 205 qualifications and atomically admit only the first root with exact eligible parity outcomes.

Purpose: close CFG-12 and EVD-10 through EVD-15 with real exact-current-package evidence while preserving every excluded claim.
Output: on success, one admitted redacted evidence generation, a redacted hardware record, and exact checklist promotions. A sealed non-promotion is recovery evidence that blocks/fails phase completion.
</objective>

<attempt_policy>
Attempts 1 through 12 remain sealed and immutable. This plan must not reuse,
rewrite, retry, splice, or promote any artifact from those attempts.

Attempt 13 is the first authorized fresh execution. Before it runs, the
repository must pass a fresh exact-current-HEAD preflight containing the
private-first classifier boundary and satisfy every fresh-attempt invariant in
`docs/hardware/hardware-attempt-policy.md`. Each fresh root invokes the full
Phase 35 command exactly once. A later ordinal is allowed only after
`continue_after_verified_fix` or `continue_after_manual_remediation`; every stop
outcome blocks the loop, and only `complete` unlocks Task 3 and
`35-04-SUMMARY.md`.

Attempt 13 executed once at exact source
`02f128db56b332e50e11f57935f29e22e3830f66` and repeated the post-fix primary
category `http_diagnostic_invalid`. Its immutable non-promotion seal selects
`stop_repeated_boundary`. No later ordinal is authorized by this plan, and Task
2 remains incomplete.

The later sealed-input diagnosis resolved two additional deterministic adapter
defects in commit `53d8bcee`: curl scheme case is canonicalized, and a configured
10-second timeout is separated from a bounded 11-second observed-duration
envelope. The exact attempt-13 shape now reaches
`request_transmission_incomplete`, so this verified fix selects
`continue_after_verified_fix` and authorizes fresh attempt 14 after the complete
exact-current-HEAD software and preflight gate. The user's 2026-07-21 standing
authority imposes no fixed cap on later ordinals that each follow another
distinct verified fix or confirmed non-invasive remediation. It does not permit
an unchanged retry, root reuse, evidence splicing, or bypass of any stop,
safety, privacy, restoration, cleanup, or authority boundary.

Attempt 14 executed once at exact source
`8afbed3248fb00e02d1a09f726b48ec241b552da` and exposed the new primary category
`request_transmission_incomplete` before PATCH or reboot. Its root is sealed
non-promotion and non-reusable. This new category returns the workflow to the
software diagnosis loop; attempt 15 requires a deterministic reproduction,
regression-backed fix, clean committed head, and complete preflight.

That diagnosis is complete in commit `0dd2134e`. A deterministic fake-curl
regression and isolated loopback observation proved that curl exit 56 is a
receive-side failure that can follow the complete bodyless GET even when its raw
request-byte counter is zero. The implementation preserves the raw counter,
derives request completion from the closed receive-error category, and keeps
exit 55 as the send-failure boundary. Exact sealed-input replay now reaches
`response_status_missing` without changing attempt 14's immutable conclusion or
digests. This verified fix selects `continue_after_verified_fix` and authorizes
fresh attempt 15 after the complete exact-current-HEAD software and preflight
gate. No unchanged retry is authorized.

Attempt 15 executed once at exact source
`1c4979f67c0b12daee356ae5df1c1c5468ba1013` and repeated
`request_transmission_incomplete`, this time with curl timeout category 28 after
TCP connection at the configured deadline rather than attempt 14's receive
error. It stopped before mutation or reboot, sealed the root non-promotion and
non-reusable, and completed cleanup without a secondary failure. The current
policy selects `stop_repeated_boundary`. Software diagnosis is permitted, but
attempt 16 is not authorized by this plan unless a later explicit policy
decision validly reopens the progress loop after a distinct verified fix.

That explicit decision is now satisfied. Attempt 15's transport subtype differs
from attempt 14 even though the coarse terminal category matched, and local
peers proved the host curl request-size counter remains zero after complete and
successful bodyless GETs. Commit `d097bbbf` replaces curl with a schema-v2 Rust
probe whose positive send-completion timestamp is recorded only after the full
request write and transport flush succeed. Real adapter/runfiles regressions
cover a valid response, a silent response after complete send, a short-write
failure, and TLS failure. The hardware policy now compares closed redacted
authoritative boundary signatures and forbids category renaming as an escape.
The user's latest 2026-07-21 post-fix authority selects
`continue_after_verified_fix` and authorizes fresh attempt 16 after the complete
exact-current-HEAD software and preflight gate. No unchanged retry, root reuse,
or evidence reinterpretation is authorized.

Attempt 16 executed once at exact source
`823309599209cde451435c85bb882fe8a456f80d` after both gates passed. The
schema-v2 probe connected and completed the full write plus transport flush,
then received no response status, headers, or body before its bounded deadline.
The root is sealed non-promotable and non-reusable with authoritative signature
`response_status_missing` plus `response_timeout`; mutation did not start. This
new response-side boundary authorizes deterministic software diagnosis, not an
unchanged attempt 17. A later ordinal requires a regression-backed verified fix
or confirmed permitted non-invasive remediation and the complete exact-head
gate.

Exact release-ELF analysis then proved that Phase 34's ordered system-info
publisher reserves 6,080 bytes and candidate collection reserves 1,456 bytes
before framework, retained-record, and JSON-writer overhead on the unchanged
8 KiB ESP-IDF HTTP server task. The targeted fix raises only that task to an
explicit 16 KiB and adds a source regression while preserving the required
completion-ordered retention and issuance contract. This distinct verified fix
selects `continue_after_verified_fix` and authorizes fresh attempt 17 after the
complete clean exact-current-HEAD software and preflight gate. Hardware remains
the required runtime proof; no unchanged retry or root reuse is authorized.

Attempt 17 executed once at exact source
`98463e8a735233b4e283b6535d3c9f375a984523` after both gates passed. It
validates the stack repair: the original-settings read reached `ready` with a
complete response in 396 milliseconds. The supervisor then stopped before
mutation with the new category `pre_patch_mismatch`; its root is sealed
non-promotable and non-reusable with no secondary restoration or cleanup
failure. This new capture-coherence boundary authorizes deterministic software
diagnosis, not an unchanged attempt 18.

The diagnosis proved that fixture mode and production capture implemented
different epoch contracts. Production omitted the setting digest, read boot
ordinal from an API field that does not exist, stored the WebSocket envelope
under the wrong identity, fabricated retained evidence and duration, and
required equal API/WebSocket revisions even though completion-ordered
publication makes the live snapshot later. The repair consumes serial-classified
boot identity plus the protected API, WebSocket data object, and actual retained
log; hashes the private setting; records real monotonic bounds; and requires a
same-session strictly later WebSocket revision. The reboot path also establishes
service loss before taking the Boot B trace boundary and forwards the baseline
session and ordinal to the typed classifier. Hermetic production-path and Rust
regressions cover these boundaries. This distinct verified fix selects
`continue_after_verified_fix` and authorizes fresh attempt 18 after the complete
clean exact-current-HEAD software gate and preflight. Attempt 17 remains sealed
and non-reusable; no in-place retry or evidence reinterpretation is authorized.

Attempt 18 executed once at exact source
`065240279c4657945ffce70d2baa501b4da7ceae` after both gates passed. Boot A
pre-capture and PATCH succeeded, then the post-PATCH retained-log request
encountered malformed chunk framing after a coherent API and WebSocket capture.
The supervisor preserved `boot_a_capture_failed`, confirmed restoration and
cleanup, and sealed the root non-promotable and non-reusable. The diagnosis
proved that the WebSocket helper returned after initiating close instead of
awaiting the close event, so no completed connection-lifecycle boundary existed
before the next HTTP request. The repair waits for bounded peer closure and
requires an exact closed marker before retained-log capture. A delayed-close
real-process regression and the focused Phase 17/35 suites cover the boundary.
This distinct verified fix selects `continue_after_verified_fix` and authorizes
fresh attempt 19 after the complete clean exact-current-HEAD software gate and
preflight. Attempt 18 remains immutable and cannot be retried in place.

Attempt 19 executed once at exact source
`6a88300f84d0db1907455974372fe0468f4957e3` after the complete gate and
exact-head preflight passed. The internal detector admitted one board-205
target, then the flash process failed to connect before Boot A capture or
mutation. Cleanup passed and the root is sealed non-promotable and non-reusable.
Its redacted authoritative signature is `flash_or_boot_a_failed` plus
`target_connection_failed`. This is an environmental transport boundary, not a
verified repository defect, so policy selects `continue_after_manual_remediation`.
Request one USB and barrel-power reset and wait for user confirmation. After
confirmation, fresh attempt 20 still requires a clean exact-head preflight and
one fresh root. If the same signature recurs after remediation, select
`stop_hardware_blocker`; do not reinterpret it as another fix opportunity or
retry unchanged.

The user confirmed the exact USB and barrel-power remediation. Attempt 20 then
executed once at exact source
`b06bf416cf65283c53aa0f69c15ed216a9858eaa` after a fresh exact-head
preflight. The internal detector again admitted one board-205 target, then the
same `flash_or_boot_a_failed` plus `target_connection_failed` signature recurred
before Boot A capture or mutation. Cleanup passed and the fresh root is sealed
non-promotable and non-reusable. This satisfies `stop_hardware_blocker` under
the progress-gated policy. Do not issue attempt 21, weaken the signature, use a
stronger electrical interface, or create `35-04-SUMMARY.md`. Task 2 and Phase 35
remain incomplete pending a separately authorized materially different path or
an independently changed external hardware state.

That stop remains authoritative for an unchanged command and unchanged
observability. The user has now separately authorized a materially different
software repair: exact espflash 4.5.0 admission, explicit native-USB reset
flags, private typed stage capture, three-sample reset stability, and one
read-only 4 KiB checksum probe inside the full Phase 35 invocation. Offline
classification of the already-recorded safe counters maps both attempts 19 and
20 to `stage=factory` and `post_info_pre_transfer_failed`; their sealed roots
remain untouched. After the complete clean software gate and exact-current-HEAD
preflight, fresh attempt 21 may run once in a fresh root. The probe must be
`ready` before credential access or writes. Recurrence of
`flash_or_boot_a_failed/factory/post_info_pre_transfer_failed` selects
`stop_repeated_boundary`; a genuinely different typed signature follows the
existing progress-gated diagnosis loop.

Boot A classification and private target derivation must read only the
mode-0600 `flash-monitor.classifier-input.log` under the mode-0700 protected
root. Dual `flash-monitor` must close and hash only that private log and its
private record; it must not create an admitted log or record. After classification
passes, the supervisor must verify the digest, invoke the already-built
software-only finalizer directly, verify the digest again, and only then consume
the distinct `flash-monitor.log` commit-redacted compatibility and admission
projection. Classifier failure must leave both admitted outputs absent.

After the user-confirmed remediation, attempt 22 executed once at exact source
`55a8f31ac9be6a2c056cd04f8cc226b923782b22`. Detector admission, the read-only
checksum probe, factory, NVS, and monitor all reached typed `ready`. The legacy
one-shot monitor trust check nevertheless returned nonzero before the Phase 33
baseline classifier ran. Offline classification of the immutable private input
passed with `category=none`, so the defect is the premature wrapper gate rather
than missing Boot A evidence. The verified repair permits only a timed-out dual
capture to return as `timed_out_pending_private_classification`; ordinary mode,
spawn/child failures, and admitted derivation remain fail-closed. Finalization
changes that state only after the supervisor's digest-bound classifier gate.
Attempt 22 remains sealed and non-promotable. Fresh attempt 23 requires the
complete clean software gate, atomic repair/checkpoint commits, and another
exact-current-HEAD preflight under `continue_after_verified_fix`.

Attempt 23 executed once at exact source
`ead2347d32ed0dbb8be43c74a3fb3a85a32734a1`. It passed private Boot A baseline
classification and dual finalization, then stopped before mutation with
`boot_a_pre_capture_failed` because the retained-log response contained invalid
chunk framing after a closed WebSocket exchange. The server-side repair assigns
each connection a generation lease with disconnect cleanup, queues every
background WebSocket frame and lease into HTTPD context with owned payload
data, and rechecks the exact lease plus current WebSocket protocol state before
the only direct send. Attempt 23 remains sealed and non-promotable.
Fresh attempt 24 requires the complete clean software gate, atomic
repair/checkpoint commits, and another exact-current-HEAD preflight under
`continue_after_verified_fix`. Recurrence of this same retained-chunk signature
after the targeted fix selects `stop_repeated_boundary`.

Attempt 24 executed once at exact source
`dec8b8a6bef8f504ec83a7eebe03b69a08be5064` and stopped at the read-only probe
before credential access or writes with `flash_boundary_invalid`. The protected
output shape and installed espflash 4.5.0 source prove a valid checksum lost one
leading zero under espflash's unpadded lowercase `u128` formatter. The adapter
incorrectly required exactly 32 digits, and the classifier projection used
`post_info_pre_transfer_failure` instead of the canonical
`post_info_pre_transfer_failed`. The repair accepts exactly one official
1-through-32-digit checksum line and explicitly serializes the canonical
boundary. Attempt 24 remains sealed and non-promotable. Fresh Attempt 25
requires the complete clean gate, atomic checkpoint commit, and another
exact-current-HEAD preflight under `continue_after_verified_fix`.
</attempt_policy>

<execution_context>
@AGENTS.md
@.planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-CONTEXT.md
@.planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-RESEARCH.md
@.planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-VALIDATION.md
@.planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-01-SUMMARY.md
@.planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-02-SUMMARY.md
@.planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-03-SUMMARY.md
</execution_context>

<threat-model>

| ID | Threat | Severity | Required mitigation and verification |
| --- | --- | --- | --- |
| T-35-17 | Hardware work runs from stale/dirty source, wrong package, wrong board, or failed detector. | critical | Run the complete software gate, current-head/reference/package lock, then `just detect-ultra205`; stop before sensitive/effectful work on any mismatch. |
| T-35-18 | The approved reboot or recovery path leaves changed settings, leaked holders, or an ambiguous board/session. | critical | Require one normal reboot, same physical-identity digest, storage-confirmed restoration, full PID/PGID/descendant reap, and no unexpected holder before root close. |
| T-35-19 | Raw local identifiers, network values, hostname, credential data, target, process data, or secrets enter committed evidence. | critical | Keep raw data under protected ignored root, admit only typed redacted projection, and run forbidden-key/raw-canary scans before staging. |
| T-35-20 | Successful lifecycle/tests are used to promote mining, control, watchdog, credentials, UART/pins, OTA, other boards, or archived claims. | critical | Require the exhaustive Phase35PromotionMatrix and contract tests; review the checklist diff and preserve typed non-promotion for all exclusions. |

</threat-model>

<tasks>

<task type="auto" id="35-04-01">
  <name>Freeze the exact current package and prove every software gate</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-VALIDATION.md
    - scripts/phase35-correlated-evidence.sh
    - tools/parity/src/phase35_evidence.rs
    - tools/parity/src/phase35_promotion.rs
    - scripts/package-firmware.sh
    - scripts/verify-reference-clean.sh
  </read_first>
  <files>
    - .planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-HARDWARE-EVIDENCE.md
  </files>
  <action>
Before any detector or hardware action, require a clean implementation state whose current commit contains Plans 35-01 through 35-03. Run:

1. `cargo fmt --all`
2. `cargo clippy --all-targets --all-features -- -D warnings`
3. `cargo build --all-targets --all-features`
4. `cargo test --all-features`
5. `bazel test //scripts:hardware_attempt_policy_contract_test //tools/parity:tests //scripts:phase35_correlated_evidence_test //scripts:phase35_promotion_contract_test //scripts:phase30_no_promotion_contract_test`
6. `just verify-reference`
7. `just parity`
8. `node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 35 --require-plans --raw`
9. `just phase35-evidence preflight-only=true`

The preflight-only command must exit before detector use and prove manifest schema `manifest-v3`, immutable executable-image digest, frozen factory-image digest, complete package/runtime-identity digest, reference cleanliness, lifecycle ID, and current HEAD equality to the source/package identity. Build/package through that canonical repo command, then record only the source commit, reference commit, manifest/executable/factory/package capability digests, board category, lifecycle ID, command strings without local paths/values, test pass/fail categories, and timestamps in `35-HARDWARE-EVIDENCE.md`. Do not record a raw package path, device path, USB identity, target, origin, hostname value, SSID, IP/MAC, credential, endpoint, address, worker, token, PID, or secret.

If any command fails, stop before detector/hardware work and record a redacted non-promotion conclusion.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; bazel test //scripts:hardware_attempt_policy_contract_test //tools/parity:tests //scripts:phase35_correlated_evidence_test //scripts:phase35_promotion_contract_test //scripts:phase30_no_promotion_contract_test &amp;&amp; just verify-reference &amp;&amp; just parity &amp;&amp; node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 35 --require-plans --raw &amp;&amp; just phase35-evidence preflight-only=true</automated>
  </verify>
  <done>All software, package, reference, lifecycle, redaction, cleanup, and non-promotion gates are green before detector use.</done>
  <acceptance_criteria>
    - All nine ordered gates exit 0.
    - The current commit is recorded and equals the source commit bound into manifest-v3 and the executable/factory/package/runtime identity capability.
    - `35-HARDWARE-EVIDENCE.md` contains lifecycle ID `35-2026-07-17T17-00-37`, board category `205`, redaction mode, test categories, and no raw local/sensitive field.
    - No detector, flash, monitor, reset, target, credential, or HTTP operation occurred before all software gates passed.
  </acceptance_criteria>
</task>

<task type="auto" id="35-04-02">
  <name>Run a fresh detector-gated correlated capture and admit its exact decisions</name>
  <read_first>
    - AGENTS.md
    - scripts/detect-ultra205.sh
    - scripts/phase35-correlated-evidence.sh
    - tools/parity/src/phase35_evidence.rs
    - tools/parity/src/phase35_promotion.rs
    - .planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-HARDWARE-EVIDENCE.md
  </read_first>
  <files>
    - docs/parity/evidence/phase-35-detector-gated-correlated-evidence-and-exact-parity-promotion/
    - docs/parity/checklist.md
    - .planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-HARDWARE-EVIDENCE.md
  </files>
  <action>
For each fresh attempt, do not invoke `just detect-ultra205` separately. Pass the literal opaque credential-path argument `wifi-credentials.json` to the repo-owned Phase 35 command without checking, opening, reading, printing, summarizing, copying, or statting it in the caller. Do not use pool credentials. Invoke the full command exactly once for that fresh root with `capture-timeout-seconds=360`, a caller/tool timeout of at least 420 seconds, redaction enabled, and the credential path used only as local runtime input. Do not echo the expanded command.

Inside that command, Gate 1 must revalidate the exact-package capability; Gate 2 must perform the sole `just detect-ultra205` invocation and continue only when it reports exactly one likely ESP USB serial port, board category `205`, and successful `espflash board-info --chip esp32s3 --non-interactive`; only after Gate 2 succeeds may Gate 3 validate/access the credential path, resolve the target, and perform effects. Do not copy the raw port or USB identity into tracked output.

The command must execute the exact state machine from Plan 35-02: locked package; Boot A coherent pre-PATCH; committed PATCH; storage-confirmed immediate readback; matching Boot A projection; exactly one approved normal reboot; full serial cleanup; same-board reacquisition; Boot B `software_cpu` and ordinal `N+1`; storage-confirmed matching hostname digest; coherent Boot B projection; restoration; storage-confirmed restoration readback; final process/holder cleanup; inventory close; typed validation.

If detector, credentials, target derivation, capture, reboot, same-board reacquisition, restoration, cleanup, inventory, redaction, validation, or current-head/reference/package recheck fails, stop that invocation, seal the protected root as non-promotion, preserve the earliest typed failure, and select exactly one outcome from `docs/hardware/hardware-attempt-policy.md`. Never invoke admission or retry in place. A positive continuation outcome returns to the full software gate and a new ordinal/root; a stop outcome blocks the task and phase. A sealed non-promotion is safe recovery, not completion of Phase 35.

On success, invoke `admit-phase35-evidence` against the protected root and a new staging directory. Publish only the typed redacted generation after the atomic exchange succeeds. Update `35-HARDWARE-EVIDENCE.md` with board category, source/reference/package/root digests, boot ordinals, allowed reset category, gate booleans, durations/counts, restoration/cleanup facts, promotion row IDs, non-promotion reason enums, and conclusion. Never include raw values or identifiers.
  </action>
  <verify>
    <automated>bazel test //tools/parity:tests //scripts:phase35_correlated_evidence_test //scripts:phase35_promotion_contract_test //scripts:phase30_no_promotion_contract_test &amp;&amp; just verify-reference &amp;&amp; just parity &amp;&amp; git diff --check</automated>
  </verify>
  <done>The first eligible exact-current-package Ultra 205 root is atomically admitted. Any sealed non-promotion leaves this task and phase incomplete unless the canonical policy selects a positive continuation for a later fresh ordinal.</done>
  <acceptance_criteria>
    - Detector output proves exactly one board-205 candidate and successful board-info before all later operations.
    - The admitted projection contains one source/reference/package/run/root provenance tuple and Boot A/B ordinals `N`/`N+1`.
    - CFG-12/EVD-13 correlation proves pre-PATCH, committed response, immediate storage readback, one approved normal reboot, same-board reacquisition, matching storage-confirmed Boot B value, and restoration.
    - Cleanup records a reaped process tree and zero unexpected serial holders before admission.
    - Committed evidence contains only allowed redacted categories/digests/booleans/counts/durations/ordinals/reasons.
    - Checklist changes are limited to dedicated allowlisted row IDs and every excluded scope has a typed non-promotion.
  </acceptance_criteria>
</task>

<task type="auto" id="35-04-03">
  <name>Audit the admitted generation and freeze the final non-claims</name>
  <read_first>
    - docs/parity/evidence/phase-35-detector-gated-correlated-evidence-and-exact-parity-promotion/
    - docs/parity/checklist.md
    - .planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-HARDWARE-EVIDENCE.md
    - scripts/phase35-promotion-contract-test.sh
    - scripts/phase30-no-promotion-contract-test.sh
  </read_first>
  <files>
    - docs/parity/evidence/phase-35-detector-gated-correlated-evidence-and-exact-parity-promotion/
    - docs/parity/checklist.md
    - .planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-HARDWARE-EVIDENCE.md
  </files>
  <action>
Recompute all committed evidence digests and rerun the Phase 35 validator/promotion evaluator from the committed projection. Run forbidden-key and raw-value canary scans. Compare the checklist before/after fingerprints and list only dedicated allowlisted changed row IDs in the redacted record.

Explicitly record non-promotion for active control, self-test effects, watchdog intervention, mining/Stratum/ASIC, Phase 28.1.1 and descendants, credentials, direct UART/pins, OTA, other boards, lifecycle/test-only proof, and every broad/unmapped row. State that administrative completion is not parity evidence.

Run the full verification command again. If any committed digest, row fingerprint, redaction scan, non-promotion contract, current-head/reference/package binding, or lifecycle check fails, revert only the staged Phase 35 admission through the repo-owned atomic generation recovery path, retain the protected root as non-promotable, and mark the plan failed; do not hand-edit historical evidence or rows.
  </action>
  <verify>
    <automated>bazel test //tools/parity:tests //scripts:phase35_correlated_evidence_test //scripts:phase35_promotion_contract_test //scripts:phase30_no_promotion_contract_test &amp;&amp; just verify-reference &amp;&amp; just parity &amp;&amp; node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 35 --require-plans --raw &amp;&amp; git diff --check</automated>
  </verify>
  <done>The final generation is reproducibly eligible, narrowly promoted, redacted, and exhaustive about non-claims.</done>
  <acceptance_criteria>
    - All committed evidence digests recompute successfully.
    - Forbidden-key/raw-value scans report zero violations.
    - Checklist diff contains only dedicated allowlisted Phase 35 row IDs.
    - STR-09, CFG-07, ASIC-11, Phase 30, Phase 28.1.1, and all other excluded rows are unchanged and explicitly non-promoted.
    - Lifecycle verification and all Phase 35/Phase 30 contract tests exit 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>

- Enforce the software gate and detector order exactly as written.
- Before each fresh attempt, run `//scripts:hardware_attempt_policy_contract_test` and the complete software/preflight gate.
- Budget at least 360 seconds for each hardware capture and at least 420 seconds shell/tool wall clock.
- Review protected raw files only locally and never print or copy their values into tracked artifacts.
- Run the ordered Rust gate, Phase 35 Bazel targets, Phase 30 non-promotion contract, reference-clean check, parity report, lifecycle verifier, and `git diff --check`.
- Review the final diff for exact dedicated row changes and zero raw/sensitive identifiers.
- Attempt 22 is sealed non-promotable after all typed flash stages reached `ready` but the legacy one-shot monitor trust gate stopped before the authoritative Phase 33 classifier. Offline classification passed. Fresh Attempt 23 requires the verified dual-mode deferred-classification repair, atomic commits, the complete clean gate, and exact-head preflight.
- Attempt 23 is sealed non-promotable after validating the private-classifier handoff and then exposing a server-side stale-WebSocket-write corruption of the retained HTTP chunk stream. Fresh Attempt 24 requires the verified HTTPD-context serialization repair, atomic commits, the complete clean gate, and exact-head preflight. The same retained-chunk signature recurring after this targeted fix stops the loop.
- Attempt 24 is sealed non-promotable after a valid unpadded espflash checksum and a noncanonical projection spelling collapsed the read-only probe into `flash_boundary_invalid`. Fresh Attempt 25 requires the verified variable-width checksum and canonical-boundary repair, atomic checkpoint commit, the complete clean gate, and exact-head preflight.

</verification>

<success_criteria>

- CFG-12 and EVD-13 are jointly closed by one exact-current-package two-epoch durability chain.
- EVD-10 proves detector/board-info gating precedes every sensitive/effectful action.
- EVD-11/EVD-12 bind complete provenance and coherent correlated epochs.
- EVD-14 proves inventory, redaction, lifecycle cleanup, restoration, no-actuation, reference-cleanliness, and current-head admission.
- EVD-15 promotes only exact dedicated allowlisted rows and deterministically preserves every excluded/broad claim as non-promotion.

</success_criteria>

<output>
After completion, create `.planning/phases/35-detector-gated-correlated-evidence-and-exact-parity-promotion/35-04-SUMMARY.md`; use normal GSD atomic task commits and do not push.
</output>
