---
quick_task: 260719-g3g-add-deterministic-lesson-loading-and-mai
quick_id: 260719-g3g
phase: quick
type: execute
mode: quick-full
plan: "01"
wave: 1
depends_on: []
files_modified:
  - /Users/peterryszkiewicz/.codex/AGENTS.md
  - AGENTS.md
  - /Users/peterryszkiewicz/.codex/tasks/lesson-audits.md
  - .codex/tasks/lesson-audits.md
autonomous: true
generated_by: gsd-plan-phase
lifecycle_mode: direct-fallback
phase_lifecycle_id: quick-260719-g3g
generated_at: 2026-07-19T00:00:00-05:00
requirements:
  - QUICK-G3G-01
  - QUICK-G3G-02
  - QUICK-G3G-03
must_haves:
  truths:
    - "Before first substantive work, an agent deterministically resolves and loads the global active lesson file plus the repository active lesson file, excludes archives, and fully reads both if and only if their combined size is at most 24,000 bytes and the sum of ceil(each present file's bytes / 3) is at most 8,000."
    - "When active lessons exceed the budget, headings are inventoried first and only complete lesson blocks are loaded in the prescribed priority order; the partial read is disclosed and an audit is flagged."
    - "Lesson audits run only for explicit triggers, preserve safety-critical guardrails, and never archive or consolidate merely because a lesson is old or large."
    - "The initial repository and global audit baselines retain all 17 repository lessons and all 4 global lessons without creating archive files."
  artifacts:
    - path: "/Users/peterryszkiewicz/.codex/AGENTS.md"
      provides: "Global deterministic lesson-loading, auditing, consolidation, and archival rules"
    - path: "AGENTS.md"
      provides: "Repository-local deterministic lesson-loading rules outside the Bright Builds managed block"
    - path: "/Users/peterryszkiewicz/.codex/tasks/lesson-audits.md"
      provides: "Append-only global lesson-audit baseline"
    - path: ".codex/tasks/lesson-audits.md"
      provides: "Append-only repository lesson-audit baseline"
  key_links:
    - from: "AGENTS.md"
      to: ".codex/tasks/lessons.md"
      via: "resolved repository active lesson-file contract"
      pattern: "tasks/lessons\\.md|\\.codex/tasks/lessons\\.md"
    - from: "/Users/peterryszkiewicz/.codex/AGENTS.md"
      to: "/Users/peterryszkiewicz/.codex/tasks/lessons.md"
      via: "global active lesson-file contract"
      pattern: "\\.codex/tasks/lessons\\.md"
    - from: ".codex/tasks/lesson-audits.md"
      to: ".codex/tasks/lessons.md"
      via: "baseline counts, byte totals, estimates, and retained lesson IDs"
      pattern: "18167|6056"
---

<objective>
Make lesson loading deterministic and bounded across global and repository instructions, then establish append-only audit baselines that preserve every currently active lesson.

Purpose: Ensure agents consult relevant lessons before substantive work without silently truncating guardrails, creating audit loops, or treating age and size as reasons to discard durable guidance.

Output: Updated global and repository instruction contracts plus global and repository lesson-audit baselines. No lesson archive is created during this quick task.
</objective>

<execution-context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution-context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards/index.md
@standards/core/verification.md
@standards/core/testing.md
@standards/languages/rust.md
@.codex/tasks/lessons.md
@/Users/peterryszkiewicz/.codex/AGENTS.md
@/Users/peterryszkiewicz/.codex/tasks/lessons.md

Constraints:

- Modify the repository AGENTS.md only outside the `bright-builds-rules-managed` block.
- Do not edit any backup mirror and do not enable GSD global learnings.
- Do not run hardware, serial, USB, network, credential, or evidence-truth diagnostics.
- Do not read or expose credential values, and redact local identifiers or sensitive content from commands, logs, diffs, summaries, and audit records.
- Do not push.
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Define deterministic active-lesson loading and lifecycle policy</name>
  <files>/Users/peterryszkiewicz/.codex/AGENTS.md, AGENTS.md</files>
  <action>
Update the global instruction file and add repository guidance outside the Bright Builds managed block. Define the active inputs as the global `/Users/peterryszkiewicz/.codex/tasks/lessons.md` plus exactly one resolved repository lesson file: `tasks/lessons.md` when `tasks/` exists, otherwise `.codex/tasks/lessons.md`. Resolve paths canonically and de-duplicate them if they identify the same file. Explicitly exclude archive files and archive directories from startup loading.

Require this loading before the first substantive plan, review, implementation, diagnosis, audit, or answer about repository work. Measure both active files in bytes and record the deterministic conservative token estimate as the sum of `ceil(file_bytes / 3)` for each present file. Require a complete read of both files if and only if combined active content is at most 24,000 bytes and that summed estimate is at most 8,000. If either combined limit is exceeded, require reading all lesson headings first, then complete lesson blocks in this priority order: safety/privacy/security/authorization/evidence guardrails; blocks relevant to the current task; global blocks; blocks created or materially changed in the previous 90 days; then remaining blocks as capacity permits. Never split a lesson block: stop at the preceding block boundary, disclose which active files or blocks were not read, and flag a lesson audit.

Encode the exact audit triggers: no audit baseline exists; the active combined total crosses 75% of either the 24,000-byte or summed 8,000-estimated-token loading limit for the first time since the last baseline; 90 days have elapsed since the last baseline and active lessons changed; 10 new active lessons have accumulated since the last baseline; or a proposed append would make the combined active total exceed 24,000 bytes or make the sum of `ceil(file_bytes / 3)` for each present file exceed 8,000. State that a completed audit above 75% does not recursively trigger another audit unless a distinct later trigger occurs.

Define consolidation and archive policy precisely. Consolidate only blocks with the same durable cause, preventive rule, and trigger signal, retaining the clearest stable lesson ID. Archive only lessons proven obsolete, duplicate, or fully superseded, preserving the original four fields (date, what went wrong, preventive rule, trigger signal) plus archive date, archive reason, and replacement ID when applicable. Age or size alone is never sufficient. Safety, privacy, security, authorization, and evidence guardrails may be consolidated or archived only when an equally strong active replacement exists. Preserve append-only stable-block editing conventions for lessons and audits.
</action>
<verify>
<automated>node \<<'JS'
const fs = require('node:fs');
const globalText = fs.readFileSync('/Users/peterryszkiewicz/.codex/AGENTS.md', 'utf8');
const repoText = fs.readFileSync('AGENTS.md', 'utf8');
const combined = `${globalText}\n${repoText}`;
const required = ['24,000', 'ceil', '75%', '90 days', '10 new', '8,000', 'block boundary', 'Age or size alone', 'equally strong'];
const missing = required.filter((item) => !combined.includes(item));
if (missing.length) throw new Error(`Missing policy terms: ${missing.join(', ')}`);
const managed = repoText.split('\<!-- bright-builds-rules-managed:begin -->')[1].split('\<!-- bright-builds-rules-managed:end -->')[0];
if (managed.includes('24,000') || managed.toLowerCase().includes('lesson audit')) throw new Error('Managed block changed');
JS</automated>
</verify>
<done>Both instruction files state one deterministic, bounded active-lesson loading algorithm, the five non-looping audit triggers, and loss-averse consolidation/archive rules; repository changes are outside the managed block.</done>
</task>

<task type="auto">
  <name>Task 2: Record repository and global lesson-audit baselines</name>
  <files>.codex/tasks/lesson-audits.md, /Users/peterryszkiewicz/.codex/tasks/lesson-audits.md</files>
  <action>
Create each audit log if absent and append one stable timestamped baseline block without rewriting unrelated existing blocks. Use a distinct stable audit ID per scope. Each block must record: audit timestamp; trigger (`no baseline` and, because the supplied current aggregate is the first observed total above 75% of the combined byte/token budget, the initial 75% crossing); source paths; active lesson counts; per-source and combined byte counts; the per-file calculation and summed conservative estimate using `ceil(bytes / 3)`; retained, consolidated, and archived lesson IDs; archive files created; and the next baseline values needed to evaluate every trigger.

The baseline must truthfully record the measured source-specific counts and bytes while preserving the supplied current aggregate: 17 repository lessons plus 4 global lessons, 21 total active lessons, 18,167 combined bytes, and a conservative combined estimate calculated as the sum of each file's `ceil(bytes / 3)` value. If calculating `ceil(18,167 / 3)` as a combined illustrative value, record it as 6,056 and do not substitute it for the required per-file-summed estimate if the two differ. Retain all 17 repository IDs and all 4 global IDs. Record no consolidated IDs, no archived IDs, and no archive files created.

Before writing, parse active lesson headings and confirm each lesson has the four required fields: date, what went wrong, preventive rule, and trigger signal. Do not copy lesson bodies into audits. Add a deterministic calculation check, performed in a temporary directory or inline without adding production dependencies, for: both files present below both limits; combined bytes over budget; summed estimated tokens over budget while combined bytes remain at or below 24,000; repository file missing; and global-only input. The cases must prove missing files count as zero, the estimate is the sum of per-file ceilings, complete loading occurs if and only if both limits pass, and over-budget selection inventories headings and retains only complete blocks without splitting a block. Do not create lesson archive files in this task.
</action>
<verify>
<automated>node \<<'JS'
const fs = require('node:fs');
const repoPath = '.codex/tasks/lessons.md';
const globalPath = '/Users/peterryszkiewicz/.codex/tasks/lessons.md';
const repoBytes = fs.statSync(repoPath).size;
const globalBytes = fs.statSync(globalPath).size;
const estimate = (...sizes) => sizes.reduce((sum, size) => sum + Math.ceil(size / 3), 0);
const canLoadFully = (...sizes) => sizes.reduce((sum, size) => sum + size, 0) \<= 24000 && estimate(...sizes) \<= 8000;
if (repoBytes + globalBytes !== 18167) throw new Error('Unexpected active byte total');
if (Math.ceil(18167 / 3) !== 6056) throw new Error('Unexpected illustrative aggregate estimate');
if (![6056, 6057].includes(estimate(repoBytes, globalBytes))) throw new Error('Unexpected per-file estimate');
for (const auditPath of ['.codex/tasks/lesson-audits.md', '/Users/peterryszkiewicz/.codex/tasks/lesson-audits.md']) {
const text = fs.readFileSync(auditPath, 'utf8');
for (const value of ['17', '4', '21', '18,167', 'retained', 'consolidated', 'archived', 'next baseline']) {
if (!text.toLowerCase().includes(value.toLowerCase())) throw new Error(`${auditPath} missing ${value}`);
}
if (!/archive files created\\s\*:\\s\*(none|[])/i.test(text)) throw new Error(`${auditPath} has no archive outcome`);
}
if (!canLoadFully(11999, 11999)) throw new Error('Below-budget pair must fully load');
if (canLoadFully(12001, 12000)) throw new Error('Byte-over-budget pair must not fully load');
if (canLoadFully(12001, 11999)) throw new Error('Token-over-budget pair must not fully load');
if (!canLoadFully(0, 9000) || estimate(0, 9000) !== 3000) throw new Error('Missing-repo/global-only case failed');
if (!canLoadFully(0, 0)) throw new Error('Missing files must count as zero');
const blocks = \[
{ id: 'safety', priority: 1, text: '## safety\\n' + 's'.repeat(40) },
{ id: 'relevant', priority: 2, text: '## relevant\\n' + 'r'.repeat(40) },
{ id: 'global', priority: 3, text: '## global\\n' + 'g'.repeat(40) },
\];
const selectCompleteBlocks = (items, byteLimit) => {
const headings = items.map(({ id }) => id);
const selected = [];
let used = 0;
for (const block of [...items].sort((a, b) => a.priority - b.priority)) {
const bytes = Buffer.byteLength(block.text);
if (used + bytes > byteLimit) break;
selected.push(block.text);
used += bytes;
}
return { headings, selected, used };
};
const selection = selectCompleteBlocks(blocks, Buffer.byteLength(blocks[0].text) + Buffer.byteLength(blocks[1].text) + 5);
if (selection.headings.length !== 3 || selection.selected.length !== 2) throw new Error('Over-budget heading/block selection failed');
if (selection.used !== selection.selected.reduce((sum, block) => sum + Buffer.byteLength(block), 0)) throw new Error('A partial block was selected');
if (!selection.selected[0].startsWith('## safety') || !selection.selected[1].startsWith('## relevant')) throw new Error('Priority order failed');
JS</automated>
</verify>
<done>Both append-only audit files contain a complete initial baseline retaining all 21 active lesson IDs, reporting the 18,167-byte aggregate and correct ceiling calculations, with zero consolidation, zero archival, zero archive files, and validated below/over/missing/global-only cases.</done>
</task>

<task type="auto">
  <name>Task 3: Validate lesson integrity, instruction scope, and repository gates</name>
  <files>/Users/peterryszkiewicz/.codex/AGENTS.md, AGENTS.md, /Users/peterryszkiewicz/.codex/tasks/lesson-audits.md, .codex/tasks/lesson-audits.md</files>
  <action>
Review all active lesson IDs and prove there are exactly 17 repository and 4 global blocks with unique IDs and all four required fields. Validate both audit blocks against the required append-only format, reconcile their counts, byte totals, ceiling estimates, retained IDs, trigger state, and next-baseline data against the active source files, and confirm no archive file was created. Inspect the plan and changed Markdown for frontmatter correctness, including exactly two standalone `---` lines in this PLAN.md.

Perform a redaction review proving the audits and diffs do not expose credentials, tokens, passwords, pool or Wi-Fi values, private endpoints, USB paths, serial identifiers, MAC/IP values, or evidence-truth claims. Review the global diff separately from the repository diff because the global file is outside the repository. Confirm no backup mirror changed and GSD global learnings remain disabled/unmodified.

Run repository Markdown and diff checks plus the repo-required read-only reference/parity and Phase 35 lifecycle checks. Before the repository commit, run the mandatory Rust sequence in order: `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo build --all-targets --all-features`, and `cargo test --all-features`; stop and fix any failure rather than bypassing it. Create one atomic implementation commit containing only the repository implementation files `AGENTS.md` and `.codex/tasks/lesson-audits.md`. Never include global files in a git commit. Write the quick SUMMARY.md required by this plan but do not commit it; the orchestrator later commits PLAN.md, SUMMARY.md, VERIFICATION.md, and STATE.md. Do not push. Do not run hardware, network, serial, USB, credential, evidence capture, or diagnostic commands.
</action>
<verify>
<automated>set -euo pipefail
test "$(grep -c '^---$' .planning/quick/260719-g3g-add-deterministic-lesson-loading-and-mai/260719-g3g-PLAN.md)" -eq 2
git diff --check
node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs frontmatter validate .planning/quick/260719-g3g-add-deterministic-lesson-loading-and-mai/260719-g3g-PLAN.md --schema plan
just verify-reference
just parity
node /Users/peterryszkiewicz/.codex/get-shit-done/bin/gsd-tools.cjs verify lifecycle 35
cargo fmt --all
cargo clippy --all-targets --all-features -- -D warnings
cargo build --all-targets --all-features
cargo test --all-features</automated>
</verify>
<done>Lesson IDs and four-field structure, audit records, PLAN frontmatter, redaction, separate global/repository diffs, reference/parity state, Phase 35 lifecycle, and the full Rust pre-commit sequence all pass; exactly `AGENTS.md` and `.codex/tasks/lesson-audits.md` are committed as the atomic repository implementation commit, global files and the executor-written SUMMARY remain uncommitted, and nothing is pushed.</done>
</task>

</tasks>

<threat-model>
## Trust Boundaries

| Boundary                                | Description                                                                                              |
| --------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| Global instructions to repository agent | Global policy affects every repository and must not silently override stronger local safety constraints. |
| Active lessons to audit records         | Potentially sensitive lesson text is summarized as IDs and metrics without copying protected content.    |
| Repository to global filesystem         | Global files are intentionally modified but must remain outside repository commits and backup mirrors.   |

## STRIDE Threat Register

| Threat ID       | Category               | Component                                   | Disposition | Mitigation Plan                                                                                                         |
| --------------- | ---------------------- | ------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------- |
| T-G3G-01        | Tampering              | Bright Builds managed AGENTS.md block       | mitigate    | Add repository guidance only outside the managed markers and assert the managed block lacks the new policy text.        |
| T-G3G-02        | Information disclosure | Lesson audits and diffs                     | mitigate    | Store IDs, counts, byte metrics, and policy outcomes only; run explicit sensitive-value redaction review before commit. |
| T-G3G-03        | Repudiation            | Lesson audit lifecycle                      | mitigate    | Use append-only timestamped stable audit IDs with trigger, retained/consolidated/archived IDs, and next baseline.       |
| T-G3G-04        | Denial of service      | Repeated 75% audits                         | mitigate    | A completed over-75% audit cannot retrigger without a distinct later trigger.                                           |
| T-G3G-05        | Elevation of privilege | Safety/privacy/authorization lesson removal | mitigate    | Require an equally strong active replacement and forbid age- or size-only archival.                                     |
| </threat-model> |                        |                                             |             |                                                                                                                         |

<verification>
- Confirm deterministic source resolution, archive exclusion, complete loading if and only if combined bytes are at most 24,000 and the sum of per-file ceiling estimates is at most 8,000, complete-block over-budget loading, disclosure, and audit flagging.
- Confirm all five audit triggers and the completed-over-75% non-loop rule are stated identically in global and repository guidance.
- Confirm the audit baselines retain all 17 repository and 4 global lesson IDs, report 18,167 combined bytes, create no archive files, and include next-baseline data.
- Confirm no managed-block, backup-mirror, global-learning, credential, hardware, network, serial, evidence-truth, or push side effect occurred.
- Confirm repository Markdown/diff, reference/parity, Phase 35 lifecycle, and ordered Rust pre-commit gates pass before the repository implementation commit.
</verification>

<success-criteria>
- Global and repository agents have a deterministic, bounded, block-safe lesson-loading contract before substantive work.
- Audit triggers, consolidation, and archival preserve durable and safety-critical guidance without recursive audit churn.
- Both append-only audit baselines truthfully describe the 21 active lessons and retain every current lesson.
- Exactly the repository implementation files `AGENTS.md` and `.codex/tasks/lesson-audits.md` are committed atomically; global changes and the executor-written SUMMARY remain uncommitted, and nothing is pushed.
</success-criteria>

<output>
After completion, create `.planning/quick/260719-g3g-add-deterministic-lesson-loading-and-mai/260719-g3g-SUMMARY.md`.
</output>
