---
status: completed
created: 2026-06-28
quick_id: 260628-l4b
title: Refactor Factory Image Merging To ESP Tooling
---

# Plan

## Scope

- Replace production factory payload byte overlays with ESP-managed image merging.
- Keep Rust-side package validation that proves the final factory image contains `www.bin` and `otadata-initial.bin` at the contract offsets.
- Document `.embuild/` as a local managed ESP-IDF/esp-rs tooling workspace that repo automation may use.

## Tasks

- [x] Resolve managed `esptool.py` from `PATH` or `.embuild/` in `scripts/package-firmware.sh`.
- [x] Use `esptool.py merge_bin` to create `bitaxe-ultra205-factory.bin` from a base `espflash save-image --merge --skip-padding` output plus `www.bin` and `otadata-initial.bin`.
- [x] Remove the production `xtask overlay-factory-payloads` subcommand and manual byte-copy helpers.
- [x] Update docs, repo-local rules, and release inventory for `.embuild/` and managed ESP tools.
- [x] Add shell coverage for package script merge command behavior and doctor `.embuild` reporting.
- [x] Run requested verification and record residual risk.
