---
quick_id: 260712-0a9
mode: quick-full
status: planned
created_at: "2026-07-12T05:30:00Z"
autonomous: true
files_modified:
  - crates/bitaxe-api/src/logs.rs
  - firmware/bitaxe/src/runtime_uptime.rs
  - firmware/bitaxe/src/main.rs
  - firmware/bitaxe/src/boot_evidence.rs
  - firmware/bitaxe/src/runtime_snapshot.rs
  - firmware/bitaxe/src/http_api.rs
  - firmware/bitaxe/src/live_stratum_runtime.rs
  - firmware/bitaxe/src/log_buffer.rs
  - firmware/bitaxe/BUILD.bazel
  - scripts/phase28.1.1-accepted-state-lifecycle-compare.mjs
  - scripts/phase28.1.1-accepted-state-lifecycle-compare-test.mjs
  - scripts/phase28.1.1-accepted-state-diagnostic.sh
  - scripts/phase28.1.1-accepted-state-diagnostic-test.sh
  - scripts/phase28.1.1-hardware-attempt-state.mjs
  - scripts/phase28.1.1-hardware-attempt-state-test.mjs
  - scripts/phase28.1.1-exact-head-hardware-attempt-test.sh
  - scripts/phase28.1.1-boot-evidence-replay-ownership-test.sh
  - scripts/BUILD.bazel
must_haves:
  truths:
    - "Normal firmware starts one boot-lifetime observer immediately after logger initialization and emits the exact serial-only runtime_heartbeat marker from the shared 128-bit boot session without depending on USB, Wi-Fi, HTTP, Stratum, pool, or ASIC progress"
    - "A host-tested pure heartbeat model owns marker rendering, sequence, cadence, next-deadline, and false-to-true listener state; the ESP adapter supplies only canonical uptime, the shared nonce, synchronization, sleeping, and log::info!"
    - "Heartbeat sequence and ESP monotonic uptime increase without backfill bursts: a sample at uptime 120000 reports cadence_ms=1000 and schedules the next normal deadline at 130000, while later samples report 10000 and listener_armed changes only false-to-true"
    - "Heartbeat lines use log::info! only and never enter retained/API/WebSocket logs; Plan 13 accepted-state replay remains a separate 10000 ms schedule with its existing 1880000 ms window in the same observer thread"
    - "Reinit requires its original boot/listener markers, dedicated same-session evidence, and a valid heartbeat; cold start alone may combine same-session dedicated evidence and heartbeat state, with listener_armed=true as the only heartbeat listener fallback"
    - "Malformed, absent, conflicting, non-monotonic, cadence-invalid, or listener-incomplete heartbeat evidence produces one stable member-specific machine code with deterministic precedence and direct closed blocker mapping, while promoted output contains only counts, booleans, categories, and digests"
    - "All focused firmware, lifecycle, state, adapter, Bazel, reference, and mandatory Rust software gates pass before the root workflow creates and pushes the hardware exact HEAD; this quick task performs no hardware action or push"
  artifacts:
    - path: firmware/bitaxe/src/boot_evidence.rs
      provides: "One always-on ESP observer adapter, shared boot nonce, synchronized pure heartbeat model, and independent Plan 13 replay schedule"
      contains: "runtime_heartbeat"
    - path: crates/bitaxe-api/src/logs.rs
      provides: "Host-compatible pure heartbeat marker, cadence/deadline, sequence, and listener-latch model"
      contains: "RuntimeHeartbeatModel"
    - path: firmware/bitaxe/src/runtime_uptime.rs
      provides: "Sole ESP-IDF monotonic uptime conversion used by heartbeat, runtime snapshots, and HTTP telemetry"
      contains: "esp_timer_get_time"
    - path: scripts/phase28.1.1-accepted-state-lifecycle-compare.mjs
      provides: "Strict heartbeat parser, cold-only stateful fallback, and redacted lifecycle summary"
      contains: "heartbeat"
    - path: scripts/phase28.1.1-hardware-attempt-state.mjs
      provides: "Closed blocker vocabulary for each heartbeat validation boundary"
      contains: "heartbeat_absent"
  key_links:
    - from: firmware/bitaxe/src/main.rs
      to: firmware/bitaxe/src/boot_evidence.rs
      via: "starts the sole observer immediately after EspLogger and records original boot evidence after the unchanged boot line"
      pattern: "initialize.*observer|record.*booted"
    - from: firmware/bitaxe/src/boot_evidence.rs
      to: crates/bitaxe-api/src/logs.rs
      via: "delegates marker, sequence, listener latch, cadence, and next-deadline transitions to the host-tested pure model"
      pattern: "RuntimeHeartbeatModel"
    - from: firmware/bitaxe/src/boot_evidence.rs
      to: firmware/bitaxe/src/runtime_uptime.rs
      via: "formats and schedules heartbeats from canonical ESP monotonic uptime"
      pattern: "uptime_millis"
    - from: firmware/bitaxe/src/live_stratum_runtime.rs
      to: firmware/bitaxe/src/boot_evidence.rs
      via: "latches listener readiness unconditionally before the dedicated listener marker is emitted"
      pattern: "record_listener_armed"
    - from: scripts/phase28.1.1-accepted-state-diagnostic.sh
      to: scripts/phase28.1.1-accepted-state-lifecycle-compare.mjs
      via: "maps exact comparator failures into the closed hardware-attempt blocker vocabulary"
      pattern: "heartbeat_(absent|malformed|session_conflict|monotonicity_failed|cadence_invalid)"
---

# Quick Task 260712-0a9: Implement the always-on serial-only runtime heartbeat

## Goal

Add an always-on USB-monitor-visible boot heartbeat and make it valid, redaction-safe fallback evidence for retained Plan 13 cold starts without weakening reinit proof, replay ownership, or exact-head safety.

## Tasks

<tasks>

<task type="auto">
  <name>Task 1: Build the boot-lifetime heartbeat and canonical uptime boundary</name>
  <files>crates/bitaxe-api/src/logs.rs, firmware/bitaxe/src/runtime_uptime.rs, firmware/bitaxe/src/main.rs, firmware/bitaxe/src/boot_evidence.rs, firmware/bitaxe/src/runtime_snapshot.rs, firmware/bitaxe/src/http_api.rs, firmware/bitaxe/src/live_stratum_runtime.rs, firmware/bitaxe/src/log_buffer.rs, firmware/bitaxe/BUILD.bazel, scripts/phase28.1.1-boot-evidence-replay-ownership-test.sh</files>
  <action>Add a host-compatible `RuntimeHeartbeatModel` and immutable sample type to `bitaxe_api::logs`; this pure model owns sequence, listener latch, exact marker rendering, due checks, cadence labels, and next deadlines. It starts with sequence 0, listener false, and next deadline 1000; `arm_listener` is idempotent and has no disarm operation; each due call returns at most one sample and increments sequence once. Label a sample at observed uptime `&lt;=120000` with cadence 1000 and one after 120000 with cadence 10000. Compute the next deadline from the single observed post-emission time, never from missed ticks: observed uptime below 120000 adds 1000, observed uptime exactly 120000 adds 10000 so the next normal deadline is 130000, and observed uptime above 120000 adds 10000. Thus a delayed wake emits once and chooses one future deadline without backfill. Render exactly `runtime_heartbeat session=&lt;32hex&gt; sequence=&lt;u64&gt; uptime_ms=&lt;u64&gt; cadence_ms=&lt;1000|10000&gt; listener_armed=&lt;true|false&gt; redacted=true`.

Create one crate-private firmware `runtime_uptime` adapter around non-positive-safe `esp_timer_get_time()` conversion and route runtime snapshot seconds/milliseconds, HTTP telemetry milliseconds, and heartbeat uptime through it. Split boot-evidence initialization so `main` creates the hardware-RNG 128-bit session, initializes the synchronized pure model, and starts exactly one observer immediately after `EspLogger::initialize_default()`, while the unchanged original `bitaxe-rust boot` line remains followed by the gated dedicated `booted` evidence marker. The observer is unconditional in all firmware modes and owns two independent schedules: heartbeat forever, plus existing accepted-state replay at 10000 ms inside the unchanged 1880000 ms window only when evidence mode enables replay. Its imperative work is limited to canonical uptime reads, synchronized model transitions, sleeping until the earlier due boundary, reading the shared nonce, accepted-state replay, and `log::info!`. `record_listener_armed` must arm the synchronized model before conditionally emitting the dedicated listener marker and must be called unconditionally when the production listener arms. Do not append, replay, expose through API/WebSocket, or persist heartbeat lines; keep replay allowlisted to `plan13_boot_evidence` and `accepted_state_snapshot`.

Put exact marker, first due, 119999/120000/120001 cadence, 120000-to-130000 deadline, delayed coalescing, sequence, and latch-transition Arrange/Act/Assert tests in `bitaxe-api`, which is host-compatible. Cover the ESP-only integration with the static ownership test and canonical firmware build: assert logger-adjacent unconditional startup, one observer spawn, shared nonce/model use, listener arm before dedicated marker, independent replay constants, `log::info!`-only heartbeat output, and absence from retained/replay/API paths. Do not attempt a host unit test of the ESP firmware binary.</action>
  <verify><automated>cargo test -p bitaxe-api --all-features runtime_heartbeat &amp;&amp; bash scripts/phase28.1.1-boot-evidence-replay-ownership-test.sh &amp;&amp; bazel test //scripts:phase28_1_1_boot_evidence_replay_ownership_test &amp;&amp; bazel build //firmware/bitaxe:firmware</automated></verify>
  <done>One service-independent observer emits exact monotonic heartbeats for the boot lifetime, preserves the independent bounded replay schedule, shares one nonce with dedicated boot evidence, latches listener readiness, and cannot pollute retained logs.</done>
</task>

<task type="auto">
  <name>Task 2: Add strict Plan 13 heartbeat parsing, cold-only fallback, and blockers</name>
  <files>scripts/phase28.1.1-accepted-state-lifecycle-compare.mjs, scripts/phase28.1.1-accepted-state-lifecycle-compare-test.mjs, scripts/phase28.1.1-accepted-state-diagnostic.sh, scripts/phase28.1.1-accepted-state-diagnostic-test.sh, scripts/phase28.1.1-hardware-attempt-state.mjs, scripts/phase28.1.1-hardware-attempt-state-test.mjs, scripts/phase28.1.1-exact-head-hardware-attempt-test.sh, scripts/BUILD.bazel</files>
  <action>Extend the lifecycle comparator with an exact-token heartbeat parser: require only the six named fields after `runtime_heartbeat`, a 32-lowercase-hex session, unsigned decimal u64 sequence/uptime, cadence 1000 at uptime `&lt;=120000` or 10000 at uptime `&gt;120000`, boolean listener state, and `redacted=true`. Every capture requires at least one valid heartbeat; across observed heartbeats require one session, strictly increasing sequence and uptime (gaps allowed), and no true-to-false listener regression. Unify dedicated and heartbeat sessions before proof evaluation. Reinit stays strict: exactly one original boot line, exactly one original listener line, and dedicated `booted` plus `listener_armed` states are mandatory; heartbeat can never replace them. Cold start may prove `booted` from any valid same-session heartbeat and may prove listener readiness only from a same-session heartbeat with `listener_armed=true`; dedicated and heartbeat states may be mixed, and equivalent dedicated replays remain logically deduplicated.

Replace human-text inference with an exported typed comparator error and one stable CLI record `accepted_state_lifecycle_failure_code=&lt;code&gt;`. The heartbeat/proof codes are exactly `reinit_heartbeat_malformed|cold_start_heartbeat_malformed`, `reinit_heartbeat_session_conflict|cold_start_heartbeat_session_conflict`, `reinit_heartbeat_monotonicity_failed|cold_start_heartbeat_monotonicity_failed`, `reinit_heartbeat_cadence_invalid|cold_start_heartbeat_cadence_invalid`, `reinit_heartbeat_absent|cold_start_heartbeat_absent`, and `reinit_listener_proof_absent|cold_start_listener_proof_absent`. Parse both members into candidate faults before choosing one, so line order cannot change the result. Across coexisting faults, choose by this exact kind precedence: malformed, session conflict, monotonicity/listener regression, cadence, absent heartbeat, absent listener; for the same kind choose reinit before cold-start. Existing dedicated-only boot/session/original-marker and accepted-state-stage failures retain their existing semantics after this heartbeat precedence, except any conflict involving a heartbeat is the heartbeat session-conflict code. The CLI emits exactly one code record on stderr and exits nonzero; missing, duplicate, or unknown code output is a validator failure.

In the shell adapter, parse only the exact code record with a closed `case`, never regex-match prose. Map either member-specific absent code to `heartbeat_absent`, malformed to `heartbeat_malformed`, session conflict to `heartbeat_session_conflict`, monotonicity/listener regression to `heartbeat_monotonicity_failed`, cadence to `heartbeat_cadence_invalid`, and either listener code to existing `listener_proof_absent`; map missing/duplicate/unknown codes to `validator_error`. Add the five heartbeat blockers to the sole state authority and its effect-result/route tables. Reinit validation must capture and map the code before `flash_reinit_runtime` reports a failed effect; cold validation must map it before lifecycle terminalization. The same blocker routes to the existing conservative prerequisite terminal before lifecycle authorization and evidence-invalid terminal after authorization, with cleanup in both paths.

Extend the redacted report with per-member heartbeat counts/presence, `early_only|steady_only|early_and_steady` uptime categories, `valid` cadence categories, and listener-fallback booleans; never render sessions, raw lines, paths, process/device identities, or secrets. Keep trace/log digests in the existing private-attempt-to-promoted projection. Add focused Arrange/Act/Assert fixtures for strict reinit non-fallback, cold heartbeat-only and mixed proof, false listener, equivalent dedicated duplicates, every exact machine code on both members, precedence independent of log order, direct code-to-blocker mappings, second/conflicting sessions, repeated/decreasing sequence or uptime, gaps, cadence boundary errors, listener regression, missing heartbeat, missing listener, redaction, and category-only unavailable output.

Add three real `sh_test` rules to `scripts/BUILD.bazel`: `phase28_1_1_accepted_state_lifecycle_compare_test` with comparator data and inherited `PATH`; `phase28_1_1_hardware_attempt_state_test` after adding its Node shebang/executable entry, with state and strict-classifier data plus `PATH`; and local/no-sandbox `phase28_1_1_accepted_state_diagnostic_test` with `HOME`/`PATH`, `//:.git/HEAD`, `//:.git/logs/HEAD`, and all directly invoked sources (`accepted-state-diagnostic`, lifecycle comparator, accepted-state comparator, state authority, strict classifier, exact-head runner, lifecycle-frame helper, process-group helper, phase13 monitor capture and its serial trace helper, and upstream-wire-capture). Make the diagnostic fixture use `TEST_TMPDIR` when present and a deterministic test-head injection under Bazel so it never needs an undeclared writable source-tree scratch path or undeclared Git ref. Keep direct invocations alongside the new Bazel targets.</action>
  <verify><automated>node scripts/phase28.1.1-accepted-state-lifecycle-compare-test.mjs &amp;&amp; node scripts/phase28.1.1-hardware-attempt-state-test.mjs &amp;&amp; bash scripts/phase28.1.1-accepted-state-diagnostic-test.sh &amp;&amp; bash scripts/phase28.1.1-exact-head-hardware-attempt-test.sh &amp;&amp; bazel test //scripts:phase28_1_1_accepted_state_lifecycle_compare_test //scripts:phase28_1_1_accepted_state_diagnostic_test //scripts:phase28_1_1_hardware_attempt_state_test //scripts:phase28_1_1_exact_head_hardware_attempt_test</automated></verify>
  <done>Both Plan 13 captures require valid heartbeat telemetry, only retained cold start can use same-session stateful fallback, every malformed/conflicting/unstable case fails through an exact closed blocker, and summaries remain category-only.</done>
</task>

<task type="auto">
  <name>Task 3: Run the complete software authority and exact-head readiness gates</name>
  <files>crates/bitaxe-api/src/logs.rs, firmware/bitaxe/src/boot_evidence.rs, firmware/bitaxe/src/runtime_uptime.rs, scripts/phase28.1.1-accepted-state-lifecycle-compare.mjs, scripts/phase28.1.1-hardware-attempt-state.mjs, scripts/phase28.1.1-accepted-state-diagnostic.sh, scripts/BUILD.bazel</files>
  <action>Perform a simplification and boundary review: keep one observer rather than a second task, one host-tested heartbeat model, one uptime adapter rather than duplicate unsafe conversions, typed comparator codes around a closed shell mapping, and no heartbeat insertion into `log_buffer`. Run shell formatting/lint for touched shell, the host-compatible heartbeat tests, focused comparator/state/adapter/monitor suites, all six affected Bazel targets (including the three newly declared rules), canonical ESP32-S3 firmware build, reference cleanliness, redaction/denylist and protected-artifact checks that are applicable without promoted hardware evidence, then the mandatory Rust sequence in exact order: `cargo fmt --all`, Clippy with warnings denied, all-target/all-feature build, and all-feature tests. Review `git diff --check`, confirm no tracked generated/private hardware output, and leave the implementation ready for the root workflow to commit and push as a clean exact HEAD. Do not run detector, board-info, package/flash/monitor, credential readers, reset, erase, factory-reset, raw-write, network discovery, real hardware, or `git push` in this task.</action>
  <verify><automated>shellcheck scripts/phase28.1.1-accepted-state-diagnostic.sh scripts/phase28.1.1-accepted-state-diagnostic-test.sh scripts/phase28.1.1-boot-evidence-replay-ownership-test.sh &amp;&amp; shfmt -d scripts/phase28.1.1-accepted-state-diagnostic.sh scripts/phase28.1.1-accepted-state-diagnostic-test.sh scripts/phase28.1.1-boot-evidence-replay-ownership-test.sh &amp;&amp; cargo test -p bitaxe-api --all-features runtime_heartbeat &amp;&amp; bash scripts/phase28.1.1-boot-evidence-replay-ownership-test.sh &amp;&amp; node scripts/phase28.1.1-accepted-state-lifecycle-compare-test.mjs &amp;&amp; node scripts/phase28.1.1-hardware-attempt-state-test.mjs &amp;&amp; bash scripts/phase28.1.1-accepted-state-diagnostic-test.sh &amp;&amp; bash scripts/phase28.1.1-exact-head-hardware-attempt-test.sh &amp;&amp; bash scripts/phase13-monitor-capture-test.sh &amp;&amp; bazel test //scripts:phase28_1_1_boot_evidence_replay_ownership_test //scripts:phase28_1_1_accepted_state_lifecycle_compare_test //scripts:phase28_1_1_accepted_state_diagnostic_test //scripts:phase28_1_1_hardware_attempt_state_test //scripts:phase28_1_1_exact_head_hardware_attempt_test //scripts:phase13_monitor_capture_test &amp;&amp; bazel build //firmware/bitaxe:firmware &amp;&amp; just verify-reference &amp;&amp; cargo fmt --all &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings &amp;&amp; cargo build --all-targets --all-features &amp;&amp; cargo test --all-features &amp;&amp; git diff --check</automated></verify>
  <done>All software authority, ESP firmware, redaction, reference, and mandatory Rust gates pass with a reviewed minimal diff; no hardware or push occurred, and the root workflow can create the fresh exact HEAD for the single Plan 13 hardware chain.</done>
</task>

</tasks>

## Verification Boundary

This quick-full plan ends after software verification. The root workflow owns the subsequent commit/push and the one fresh detector-gated Plan 13 hardware chain; that chain must separately prove both cadence phases in each 360-second capture, same-session listener readiness, stable USB ownership/identity, and zero holders/processes after cleanup.
