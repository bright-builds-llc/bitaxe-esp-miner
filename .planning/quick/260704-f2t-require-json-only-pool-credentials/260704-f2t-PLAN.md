---
status: completed
created: 2026-07-04
quick_id: 260704-f2t
title: Require JSON-Only Pool Credentials
---

# Plan

## Scope

- Make `pool-credentials*.json` the only supported local pool credential interface for Phase 21 live mining verification.
- Keep `BITAXE_POOL_*` variables only as wrapper-internal values populated from validated JSON.
- Preserve redaction, ignored real credential files, and existing fresh `DEVICE_URL` safety boundaries.

## Tasks

- [x] Delete the committed `.env` sample and remove `.env` fallback docs.
- [x] Clear inherited `BITAXE_POOL_*` values before live prerequisite checks.
- [x] Load pool env only from validated JSON passed with `--pool-credentials`.
- [x] Replace env-var success coverage with env-only rejection coverage.
- [x] Keep JSON success, malformed JSON, missing JSON field, missing credential, HTTP/WebSocket probe, and redaction coverage.
- [x] Run the requested ignore, wording, wrapper, Bazel, diff, and Rust verification before commit.
