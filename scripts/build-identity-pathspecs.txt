# Firmware and shared Rust sources
:(top,glob)firmware/**
:(top,glob)crates/**
:(top,glob).cargo/**
:(top,glob)tools/xtask/**

# Canonical root build inputs
:(top)Cargo.toml
:(top)Cargo.lock
:(top)rust-toolchain.toml
:(top).bazelversion
:(top).bazelrc
:(top)MODULE.bazel
:(top)MODULE.bazel.lock
:(top)BUILD.bazel
:(top)Justfile

# Firmware build, package, identity, and typed automation
:(top)scripts/BUILD.bazel
:(top)scripts/build-identity-pathspecs.txt
:(top)scripts/build_identity.bzl
:(top,glob)tools/automation/**
:(top,glob)crates/bitaxe-automation-contracts/**
